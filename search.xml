<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Git 版本控制入门</title>
    <url>/2026/05/01/Git/</url>
    <content><![CDATA[<p><strong>Git</strong> 是分布式版本控制系统，用来记录代码变更、多人协作与回滚历史。下面从「安装与身份 → 建仓库 &#x2F; 克隆 &#x2F; <strong>本地 init 后关联远程</strong> → 日常提交流程 → 分支与合并 → 远程与推送 → 忽略文件 → <strong>误提交后回滚远程</strong> → <strong><code>cherry-pick</code>（挑选提交）</strong>」顺序写清楚；命令在 <strong>Linux &#x2F; macOS</strong> 终端或 <strong>Windows</strong> 的 <strong>Git Bash &#x2F; PowerShell</strong> 中均可使用（路径按你的本机修改）。</p>
<hr>
<h1 id="一、安装与验证"><a href="#一、安装与验证" class="headerlink" title="一、安装与验证"></a>一、安装与验证</h1><ol>
<li>打开官网下载并按向导安装：<strong><a href="https://git-scm.com/">https://git-scm.com/</a></strong>  </li>
<li><strong>Windows</strong> 安装时可勾选 <strong>「Git Bash Here」</strong>，便于在资源管理器右键进入终端。</li>
</ol>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git --version</span><br></pre></td></tr></table></figure>

<p>能输出版本号（如 <strong><code>git version 2.x.x</code></strong>）即表示安装成功。</p>
<hr>
<h1 id="二、首次使用前配置身份"><a href="#二、首次使用前配置身份" class="headerlink" title="二、首次使用前配置身份"></a>二、首次使用前配置身份</h1><p>提交记录里会显示作者信息，<strong>每台电脑只需配置一次</strong>（全局）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;你的名字或昵称&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;你的邮箱@example.com&quot;</span></span><br></pre></td></tr></table></figure>

<p>查看是否生效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global --list</span><br></pre></td></tr></table></figure>

<p>仅某个仓库使用不同身份时，在该仓库目录去掉 <strong><code>--global</code></strong> 再执行一次即可。</p>
<hr>
<h1 id="三、创建仓库与克隆"><a href="#三、创建仓库与克隆" class="headerlink" title="三、创建仓库与克隆"></a>三、创建仓库与克隆</h1><h2 id="1-在本地新建仓库"><a href="#1-在本地新建仓库" class="headerlink" title="1. 在本地新建仓库"></a>1. 在本地新建仓库</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /你的项目目录</span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<p>当前目录下会出现 <strong><code>.git</code></strong> 隐藏目录，表示已纳入 <strong>Git</strong> 管理。</p>
<h2 id="2-从远程克隆已有仓库"><a href="#2-从远程克隆已有仓库" class="headerlink" title="2. 从远程克隆已有仓库"></a>2. 从远程克隆已有仓库</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/用户名/仓库名.git</span><br><span class="line"><span class="built_in">cd</span> 仓库名</span><br></pre></td></tr></table></figure>

<p><strong>SSH</strong> 地址形如 <strong><code>git@github.com:用户名/仓库名.git</code></strong>，需先配置好公钥（见平台文档）。</p>
<h2 id="3-本地-git-init-后关联远程并首次提交、推送"><a href="#3-本地-git-init-后关联远程并首次提交、推送" class="headerlink" title="3. 本地 git init 后关联远程并首次提交、推送"></a>3. 本地 <code>git init</code> 后关联远程并首次提交、推送</h2><p>先在 <strong>Gitee &#x2F; GitHub</strong> 等平台<strong>新建空仓库</strong>（复制页面给出的 <strong>HTTPS</strong> 或 <strong>SSH</strong> 地址）。本地已有代码目录时：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /你的项目目录</span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<p>若全局已配置过 <strong><code>user.name</code> &#x2F; <code>user.email</code></strong>（见上文「二」），可直接暂存、提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;init: 首次提交&quot;</span></span><br></pre></td></tr></table></figure>

<p>关联远程（<strong><code>origin</code></strong> 为常用别名，地址换成你的仓库）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin https://gitee.com/你的用户/你的仓库.git</span><br><span class="line">git remote -v</span><br></pre></td></tr></table></figure>

<p>确认本地<strong>默认分支名</strong>与远程一致。远程多为 <strong><code>main</code></strong>；若你本地仍是 <strong><code>master</code></strong>，可改名为 <strong><code>main</code></strong> 再推送：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -M main</span><br><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure>

<p>之后在本仓库内只需 <strong><code>git push</code></strong> &#x2F; <strong><code>git pull</code></strong>（<strong><code>-u</code></strong> 已绑定上游）。</p>
<p><strong>若新建远程仓库时勾选了「自动生成 README &#x2F; .gitignore」</strong>，远程已有提交，直接 <strong><code>push</code></strong> 会被拒绝。可先拉取再合并（保留双方历史）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull origin main --allow-unrelated-histories</span><br></pre></td></tr></table></figure>

<p>解决若有冲突后 <strong><code>git add</code></strong> → <strong><code>git commit</code></strong>，再 <strong><code>git push -u origin main</code></strong>。更省事的做法是：<strong>建远程仓库时不要勾选初始化文件</strong>，保持完全空库，再按上文 <strong><code>push</code></strong> 即可。</p>
<hr>
<h1 id="四、日常开发流程（最常用）"><a href="#四、日常开发流程（最常用）" class="headerlink" title="四、日常开发流程（最常用）"></a>四、日常开发流程（最常用）</h1><h2 id="1-查看状态"><a href="#1-查看状态" class="headerlink" title="1. 查看状态"></a>1. 查看状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure>

<p>红色为「未暂存」变更，绿色为「已暂存待提交」。</p>
<h2 id="2-加入暂存区并提交"><a href="#2-加入暂存区并提交" class="headerlink" title="2. 加入暂存区并提交"></a>2. 加入暂存区并提交</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;描述本次修改（写清楚便于以后查找）&quot;</span></span><br></pre></td></tr></table></figure>

<p>只暂存某个文件时，把 <strong><code>.</code></strong> 换成路径，例如 <strong><code>git add README.md</code></strong>。</p>
<h2 id="3-查看历史"><a href="#3-查看历史" class="headerlink" title="3. 查看历史"></a>3. 查看历史</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline -10</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、分支与合并（简要）"><a href="#五、分支与合并（简要）" class="headerlink" title="五、分支与合并（简要）"></a>五、分支与合并（简要）</h1><h2 id="1-新建并切换分支"><a href="#1-新建并切换分支" class="headerlink" title="1. 新建并切换分支"></a>1. 新建并切换分支</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch feature-login</span><br><span class="line">git checkout feature-login</span><br></pre></td></tr></table></figure>

<p><strong>Git 2.23+</strong> 可用一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch -c feature-login</span><br></pre></td></tr></table></figure>

<h2 id="2-切回主分支并合并"><a href="#2-切回主分支并合并" class="headerlink" title="2. 切回主分支并合并"></a>2. 切回主分支并合并</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git merge feature-login</span><br></pre></td></tr></table></figure>

<p>不同团队主分支名可能是 <strong><code>main</code></strong> 或 <strong><code>master</code></strong>，以你仓库默认分支为准。</p>
<h2 id="3-删除已合并的本地分支（可选）"><a href="#3-删除已合并的本地分支（可选）" class="headerlink" title="3. 删除已合并的本地分支（可选）"></a>3. 删除已合并的本地分支（可选）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -d feature-login</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、远程仓库：添加、拉取与推送"><a href="#六、远程仓库：添加、拉取与推送" class="headerlink" title="六、远程仓库：添加、拉取与推送"></a>六、远程仓库：添加、拉取与推送</h1><h2 id="1-查看是否已配置远程"><a href="#1-查看是否已配置远程" class="headerlink" title="1. 查看是否已配置远程"></a>1. 查看是否已配置远程</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure>

<p>若为空，需要添加；若地址错误，可先 <strong><code>git remote remove origin</code></strong> 再重新添加。</p>
<h2 id="2-关联远程（origin-为常用别名）"><a href="#2-关联远程（origin-为常用别名）" class="headerlink" title="2. 关联远程（origin 为常用别名）"></a>2. 关联远程（<code>origin</code> 为常用别名）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin https://gitee.com/你的用户/你的仓库.git</span><br></pre></td></tr></table></figure>

<p>若仓库是先在本地 <strong><code>git init</code></strong>、再第一次连远程，从 <strong><code>add</code> &#x2F; <code>commit</code></strong> 到 <strong><code>push</code></strong> 的完整顺序见上文 <strong>「三、3」</strong>。</p>
<h2 id="3-首次推送"><a href="#3-首次推送" class="headerlink" title="3. 首次推送"></a>3. 首次推送</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure>

<p>之后同一分支只需 <strong><code>git push</code></strong>。<strong><code>-u</code></strong> 会记住上游分支，减少下次参数。</p>
<h2 id="4-拉取他人更新"><a href="#4-拉取他人更新" class="headerlink" title="4. 拉取他人更新"></a>4. 拉取他人更新</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull</span><br></pre></td></tr></table></figure>

<p>若本地有未提交修改且与远程冲突，先 <strong><code>commit</code></strong> 或 <strong><code>stash</code></strong> 再拉取，按提示解决冲突。</p>
<hr>
<h1 id="七、忽略不必提交的文件（-gitignore）"><a href="#七、忽略不必提交的文件（-gitignore）" class="headerlink" title="七、忽略不必提交的文件（.gitignore）"></a>七、忽略不必提交的文件（<code>.gitignore</code>）</h1><p>在项目根目录新建 <strong><code>.gitignore</code></strong>，每行一个规则，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">node_modules/</span><br><span class="line">dist/</span><br><span class="line">.env</span><br><span class="line">*.log</span><br><span class="line">.DS_Store</span><br></pre></td></tr></table></figure>

<p>保存后执行 <strong><code>git status</code></strong>，上述路径不应再出现在「待添加」列表中（已被跟踪的文件需先 <strong><code>git rm --cached</code></strong> 再忽略，按需查阅文档）。</p>
<hr>
<h1 id="八、误提交后回滚远程分支"><a href="#八、误提交后回滚远程分支" class="headerlink" title="八、误提交后回滚远程分支"></a>八、误提交后回滚远程分支</h1><p>「误提交」分两种：<strong>还没 <code>push</code></strong>（只改本地即可）与 <strong>已经 <code>push</code> 到远程</strong>（需要让远程分支也撤销或抵消）。下面默认远程别名为 <strong><code>origin</code></strong>，分支名为 <strong><code>main</code></strong>，请改成你的实际分支名。</p>
<h2 id="1-仅误提交在本地、尚未推送"><a href="#1-仅误提交在本地、尚未推送" class="headerlink" title="1. 仅误提交在本地、尚未推送"></a>1. 仅误提交在本地、尚未推送</h2><p>撤销最近一次提交，但<strong>保留工作区修改</strong>（可重新改完再提交）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --soft HEAD~1</span><br></pre></td></tr></table></figure>

<p>撤销最近一次提交，并<strong>取消暂存</strong>（文件仍在工作区，与 <strong><code>git add</code> 前</strong>类似）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --mixed HEAD~1</span><br></pre></td></tr></table></figure>

<p>完全丢弃最近一次提交及其改动（<strong>危险</strong>，慎用）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD~1</span><br></pre></td></tr></table></figure>

<h2 id="2-已推送到远程：用-revert（推荐，适合多人共享分支）"><a href="#2-已推送到远程：用-revert（推荐，适合多人共享分支）" class="headerlink" title="2. 已推送到远程：用 revert（推荐，适合多人共享分支）"></a>2. 已推送到远程：用 <code>revert</code>（推荐，适合多人共享分支）</h2><p><strong><code>git revert</code></strong> 会<strong>新增一条「反向提交」</strong>，不改写已有历史，他人 <strong><code>pull</code></strong> 时不易产生「历史分叉」冲突，适合 <strong><code>main</code> &#x2F; <code>develop</code></strong> 等公共分支。</p>
<ol>
<li>查出误提交哈希：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline -5</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>对<strong>单个错误提交</strong>执行还原并推送：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git revert &lt;错误提交的完整或短哈希&gt; --no-edit</span><br><span class="line">git push origin main</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>若误提交是<strong>合并提交（merge commit）</strong>，需指定主线父提交（一般为 <strong><code>1</code></strong>），具体以 <strong><code>git log</code></strong> 为准：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git revert -m 1 &lt;merge提交的哈希&gt; --no-edit</span><br><span class="line">git push origin main</span><br></pre></td></tr></table></figure>

<h2 id="3-已推送到远程：用-reset-强推（仅个人分支或团队已约定）"><a href="#3-已推送到远程：用-reset-强推（仅个人分支或团队已约定）" class="headerlink" title="3. 已推送到远程：用 reset + 强推（仅个人分支或团队已约定）"></a>3. 已推送到远程：用 <code>reset</code> + 强推（仅个人分支或团队已约定）</h2><p>若确定<strong>没有他人基于当前远程历史开发</strong>，或误推在自己独占的功能分支上，可把本地分支指回正确提交，再强推覆盖远程：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard &lt;正确的提交哈希&gt;</span><br><span class="line">git push --force-with-lease origin main</span><br></pre></td></tr></table></figure>

<ul>
<li>优先使用 <strong><code>--force-with-lease</code></strong>，避免覆盖掉你<strong>未拉取到的</strong>他人新提交；若你明确要无条件覆盖，才考虑 <strong><code>--force</code></strong>（仍须团队同意）。  </li>
<li><strong>GitHub &#x2F; Gitee</strong> 等对 <strong><code>main</code></strong> 常开启<strong>分支保护</strong>，禁止强推，此时只能用 <strong><code>revert</code></strong> 或走平台「回滚 PR」流程。</li>
</ul>
<h2 id="4-协作与分支保护注意"><a href="#4-协作与分支保护注意" class="headerlink" title="4. 协作与分支保护注意"></a>4. 协作与分支保护注意</h2><table>
<thead>
<tr>
<th>情况</th>
<th>建议</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>main</code> 多人共用</strong></td>
<td>用 <strong><code>git revert</code></strong>，不要用强推改历史</td>
</tr>
<tr>
<td><strong>个人功能分支</strong></td>
<td>可与维护者约定后 <strong><code>reset</code> + <code>--force-with-lease</code></strong></td>
</tr>
<tr>
<td><strong>平台禁止 force push</strong></td>
<td>只能 <strong><code>revert</code></strong> 或在网页端操作</td>
</tr>
</tbody></table>
<p>回滚后通知同事 <strong><code>git pull</code></strong>；若曾 <strong><code>revert</code></strong>，后续再「撤销这次 revert」可用 <strong><code>git revert</code> 那次 revert 产生的提交哈希</strong>。</p>
<hr>
<h1 id="九、cherry-pick：把指定提交「摘」到当前分支"><a href="#九、cherry-pick：把指定提交「摘」到当前分支" class="headerlink" title="九、cherry-pick：把指定提交「摘」到当前分支"></a>九、<code>cherry-pick</code>：把指定提交「摘」到当前分支</h1><p><strong><code>git merge</code></strong> 会把另一条分支上的<strong>全部差异</strong>合进来；若你只想把对方分支上的<strong>某几条提交</strong>搬到当前分支（例如 <strong><code>hotfix</code></strong> 上修了一个 <strong>Bug</strong>，要同步到 <strong><code>release</code></strong>，又不想合并整棵分支树），用 <strong><code>git cherry-pick</code></strong>。</p>
<h2 id="1-基本用法"><a href="#1-基本用法" class="headerlink" title="1. 基本用法"></a>1. 基本用法</h2><p>先切到<strong>要接收改动</strong>的分支，再给出<strong>被挑选提交的哈希</strong>（在对方分支上用 <strong><code>git log --oneline</code></strong> 查看）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch release</span><br><span class="line">git cherry-pick abc1234</span><br></pre></td></tr></table></figure>

<p><strong>Git</strong> 会在当前分支<strong>新建一个提交</strong>，内容与 <strong><code>abc1234</code></strong> 的补丁相同（哈希会变，因为父提交不同）。</p>
<p>一次挑多个提交时，按<strong>时间先后</strong>从左到右列出（先发生的先写）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git cherry-pick 1111111 2222222 3333333</span><br></pre></td></tr></table></figure>

<h2 id="2-连续区间（慎用，先-log-确认范围）"><a href="#2-连续区间（慎用，先-log-确认范围）" class="headerlink" title="2. 连续区间（慎用，先 log 确认范围）"></a>2. 连续区间（慎用，先 <code>log</code> 确认范围）</h2><p>从 <strong><code>A</code> 之后</strong>到 <strong><code>B</code>（含）</strong> 的提交链上挑选，可用（<strong><code>A</code> 本身不会被挑进</strong>，<strong><code>B</code> 会</strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git cherry-pick A^..B</span><br></pre></td></tr></table></figure>

<p>不同仓库历史形状不同，区间语义容易踩坑；不确定时改为<strong>逐个哈希</strong>或<strong>多次单条 <code>cherry-pick</code></strong> 更稳。</p>
<h2 id="3-冲突与中止"><a href="#3-冲突与中止" class="headerlink" title="3. 冲突与中止"></a>3. 冲突与中止</h2><p>与 <strong><code>merge</code></strong> 类似，可能冲突。改完文件后：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git cherry-pick --<span class="built_in">continue</span></span><br></pre></td></tr></table></figure>

<p>放弃本次挑选、回到 <strong><code>cherry-pick</code> 之前</strong>的状态：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git cherry-pick --abort</span><br></pre></td></tr></table></figure>

<h2 id="4-常用选项"><a href="#4-常用选项" class="headerlink" title="4. 常用选项"></a>4. 常用选项</h2><table>
<thead>
<tr>
<th>选项</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>-x</code></strong></td>
<td>在提交说明末尾附一行 <strong><code>(cherry picked from commit …)</code></strong>，便于审计、追溯来源</td>
</tr>
<tr>
<td><strong><code>-n</code> &#x2F; <code>--no-commit</code></strong></td>
<td>只应用补丁到暂存区 &#x2F; 工作区，<strong>不自动提交</strong>；可连续挑多条再一次性 <strong><code>commit</code></strong>（类似手工 squash）</td>
</tr>
</tbody></table>
<h2 id="5-与-merge-的取舍（简要）"><a href="#5-与-merge-的取舍（简要）" class="headerlink" title="5. 与 merge 的取舍（简要）"></a>5. 与 <code>merge</code> 的取舍（简要）</h2><table>
<thead>
<tr>
<th>需求</th>
<th>更合适</th>
</tr>
</thead>
<tbody><tr>
<td>需要对方分支<strong>当前全部内容</strong></td>
<td><strong><code>git merge</code></strong></td>
</tr>
<tr>
<td>只要<strong>个别提交</strong>、保持当前分支历史线性感</td>
<td><strong><code>git cherry-pick</code></strong></td>
</tr>
</tbody></table>
<p>若在 <strong><code>main</code></strong> 上 <strong><code>cherry-pick</code></strong> 了已在功能分支合并过的同一补丁，可能产生<strong>重复改动</strong>或冲突，需团队约定「以哪条分支为准」。</p>
<hr>
<h1 id="十、小结"><a href="#十、小结" class="headerlink" title="十、小结"></a>十、小结</h1><table>
<thead>
<tr>
<th>场景</th>
<th>常用命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装验证</td>
<td><strong><code>git --version</code></strong></td>
</tr>
<tr>
<td>身份配置</td>
<td><strong><code>git config --global user.name</code> &#x2F; <code>user.email</code></strong></td>
</tr>
<tr>
<td>新建仓库</td>
<td><strong><code>git init</code></strong></td>
</tr>
<tr>
<td>本地 init 后连远程</td>
<td><strong><code>git remote add origin &lt;url&gt;</code></strong> → <strong><code>git add</code> &#x2F; <code>commit</code></strong> → <strong><code>git push -u origin main</code></strong></td>
</tr>
<tr>
<td>克隆</td>
<td><strong><code>git clone &lt;url&gt;</code></strong></td>
</tr>
<tr>
<td>提交</td>
<td><strong><code>git add</code></strong> → <strong><code>git commit -m</code></strong></td>
</tr>
<tr>
<td>分支</td>
<td><strong><code>git switch -c</code></strong> &#x2F; <strong><code>git merge</code></strong></td>
</tr>
<tr>
<td>远程</td>
<td><strong><code>git remote add</code></strong> → <strong><code>git push -u</code></strong></td>
</tr>
<tr>
<td>同步</td>
<td><strong><code>git pull</code></strong></td>
</tr>
<tr>
<td>忽略文件</td>
<td>编辑 <strong><code>.gitignore</code></strong></td>
</tr>
<tr>
<td>远程回滚（安全）</td>
<td><strong><code>git revert &lt;hash&gt;</code></strong> → <strong><code>git push</code></strong></td>
</tr>
<tr>
<td>远程回滚（改历史）</td>
<td><strong><code>git reset --hard &lt;hash&gt;</code></strong> → <strong><code>git push --force-with-lease</code></strong></td>
</tr>
<tr>
<td>挑选提交</td>
<td><strong><code>git cherry-pick &lt;hash&gt;</code></strong>；冲突后 <strong><code>--continue</code></strong> &#x2F; <strong><code>--abort</code></strong></td>
</tr>
</tbody></table>
<p>更高级的主题（<strong><code>rebase</code></strong>、<strong>子模块</strong>、<strong>大文件 LFS</strong> 等）可在熟练上述流程后，按 <a href="https://git-scm.com/book/zh/v2">Pro Git 中文版</a> 与团队规范继续深入。</p>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo 博客</title>
    <url>/2026/04/30/Hexo/</url>
    <content><![CDATA[<p>Hexo 是基于 Node.js 的静态博客框架，用 Markdown 写作，一键生成全站并可免费部署到 GitHub Pages。下面从准备、安装、写作、预览、部署、主题与常用命令，一步步讲清楚。</p>
<hr>
<h1 id="一、环境准备（必须）"><a href="#一、环境准备（必须）" class="headerlink" title="一、环境准备（必须）"></a>一、环境准备（必须）</h1><p>先装三个软件：</p>
<ol>
<li><strong>Nvm</strong> 安装管理Node版本</li>
<li><strong>Node.js</strong>（v22.22.2）：<a href="https://nodejs.org/">https://nodejs.org/</a> （自带 npm）</li>
<li><strong>Git</strong>：<a href="https://git-scm.com/">https://git-scm.com/</a></li>
</ol>
<p>验证安装（终端输入）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm list</span><br><span class="line">nvm use v22.22.2</span><br><span class="line">node -v</span><br><span class="line">npm -v</span><br><span class="line">git --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、安装-Hexo"><a href="#二、安装-Hexo" class="headerlink" title="二、安装 Hexo"></a>二、安装 Hexo</h1><h2 id="1-全局安装-Hexo-命令行"><a href="#1-全局安装-Hexo-命令行" class="headerlink" title="1. 全局安装 Hexo 命令行"></a>1. 全局安装 Hexo 命令行</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>国内慢可先切淘宝镜像：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h2 id="2-初始化博客"><a href="#2-初始化博客" class="headerlink" title="2. 初始化博客"></a>2. 初始化博客</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建博客文件夹（如 my919）</span></span><br><span class="line">hexo init my919</span><br><span class="line"><span class="built_in">cd</span> my919</span><br><span class="line"><span class="comment"># 安装依赖</span></span><br><span class="line">npm install</span><br></pre></td></tr></table></figure>
<p>目录结构（关键）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">my919/</span><br><span class="line">├── _config.yml   # 全站配置（网站名、部署等）</span><br><span class="line">├── source/       # 文章、图片等资源</span><br><span class="line">│   └── _posts/   # 存放 Markdown 文章</span><br><span class="line">├── themes/       # 主题文件夹</span><br><span class="line">└── node_modules/ # 项目依赖</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="三、写作与本地预览"><a href="#三、写作与本地预览" class="headerlink" title="三、写作与本地预览"></a>三、写作与本地预览</h1><h2 id="1-新建文章"><a href="#1-新建文章" class="headerlink" title="1. 新建文章"></a>1. 新建文章</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">&quot;我的第一篇博客&quot;</span></span><br></pre></td></tr></table></figure>
<p>自动生成：<code>source/_posts/我的第一篇博客.md</code></p>
<h2 id="2-编辑文章"><a href="#2-编辑文章" class="headerlink" title="2. 编辑文章"></a>2. 编辑文章</h2><p>用 VS Code 等编辑器打开 <code>.md</code> 文件，格式示例：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 我的第一篇博客</span><br><span class="line">date: 2026-04-30 20:00:00</span><br><span class="line">tags: [Hexo, 博客]</span><br><span class="line"><span class="section">categories: 教程</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line"><span class="section"># 欢迎使用 Hexo</span></span><br><span class="line">这是我的第一篇博客文章，用 Markdown 写作。</span><br></pre></td></tr></table></figure>

<h2 id="3-本地预览（实时看效果）"><a href="#3-本地预览（实时看效果）" class="headerlink" title="3. 本地预览（实时看效果）"></a>3. 本地预览（实时看效果）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 清理缓存 → 生成静态文件 → 启动本地服务</span></span><br><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</span><br></pre></td></tr></table></figure>
<p>浏览器打开：<a href="http://192.168.1.22:4000/">http://192.168.1.22:4000</a></p>
<hr>
<h1 id="四、部署到-GitHub-Pages（免费上线）"><a href="#四、部署到-GitHub-Pages（免费上线）" class="headerlink" title="四、部署到 GitHub Pages（免费上线）"></a>四、部署到 GitHub Pages（免费上线）</h1><h2 id="1-准备-GitHub-仓库"><a href="#1-准备-GitHub-仓库" class="headerlink" title="1. 准备 GitHub 仓库"></a>1. 准备 GitHub 仓库</h2><ul>
<li>登录 GitHub → 新建仓库</li>
<li>仓库名必须为：<code>你的用户名.github.io</code>（公开仓库）</li>
<li>例如：<code>my919.github.io</code></li>
</ul>
<h2 id="2-安装部署插件"><a href="#2-安装部署插件" class="headerlink" title="2. 安装部署插件"></a>2. 安装部署插件</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<h2 id="3-配置部署（修改-config-yml-末尾）"><a href="#3-配置部署（修改-config-yml-末尾）" class="headerlink" title="3. 配置部署（修改 _config.yml 末尾）"></a>3. 配置部署（修改 <code>_config.yml</code> 末尾）</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">    <span class="attr">repo:</span></span><br><span class="line">      <span class="attr">github:</span> <span class="string">https://github.com/my919/my919.github.io.git</span></span><br><span class="line">    <span class="attr">branch:</span></span><br><span class="line">      <span class="attr">github:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>

<h2 id="4-一键部署"><a href="#4-一键部署" class="headerlink" title="4. 一键部署"></a>4. 一键部署</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure>
<p>部署成功后访问：<a href="https://my919.github.io/">https://my919.github.io</a></p>
<hr>
<h1 id="五、更换主题（以-hexo-theme-next-为例）"><a href="#五、更换主题（以-hexo-theme-next-为例）" class="headerlink" title="五、更换主题（以 hexo-theme-next 为例）"></a>五、更换主题（以 hexo-theme-next 为例）</h1><h2 id="1-安装主题"><a href="#1-安装主题" class="headerlink" title="1. 安装主题"></a>1. 安装主题</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-theme-next</span><br></pre></td></tr></table></figure>
<h2 id="2-启用主题"><a href="#2-启用主题" class="headerlink" title="2. 启用主题"></a>2. 启用主题</h2><p>修改 <code>_config.yml</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">theme:</span> <span class="string">next</span></span><br></pre></td></tr></table></figure>

<h2 id="3-修改hexo-theme-next配置"><a href="#3-修改hexo-theme-next配置" class="headerlink" title="3. 修改hexo-theme-next配置"></a>3. 修改<code>hexo-theme-next</code>配置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cp</span> node_modules/hexo-theme-next/_config.yml _config.next.yml</span><br><span class="line"><span class="comment"># Schemes</span></span><br><span class="line"><span class="comment"># scheme: Muse</span></span><br><span class="line"><span class="comment">#scheme: Mist</span></span><br><span class="line"><span class="comment">#scheme: Pisces</span></span><br><span class="line">scheme: Gemini</span><br><span class="line"><span class="built_in">cp</span> _config.next.yml node_modules/hexo-theme-next/_config.yml </span><br></pre></td></tr></table></figure>


<h2 id="4-预览效果"><a href="#4-预览效果" class="headerlink" title="4. 预览效果"></a>4. 预览效果</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<h2 id="5-About、分类、标签页"><a href="#5-About、分类、标签页" class="headerlink" title="5. About、分类、标签页"></a>5. About、分类、标签页</h2><p>NexT 的「关于」「分类」「标签云」是 <strong>独立页面</strong>：在 <code>source/</code> 下放置带 <code>type</code> 的 <code>index.md</code>，并在 <code>_config.next.yml</code> 的 <code>menu</code> 中挂上链接后，顶部导航即可访问。</p>
<h3 id="新建页面（路径与菜单-URL-一致）"><a href="#新建页面（路径与菜单-URL-一致）" class="headerlink" title="新建页面（路径与菜单 URL 一致）"></a>新建页面（路径与菜单 URL 一致）</h3><table>
<thead>
<tr>
<th>页面</th>
<th>创建指令（在项目根目录执行）</th>
<th>生成文件</th>
<th><code>type</code></th>
</tr>
</thead>
<tbody><tr>
<td>关于</td>
<td><code>hexo new page about</code></td>
<td><code>source/about/index.md</code></td>
<td><code>about</code></td>
</tr>
<tr>
<td>分类</td>
<td><code>hexo new page categories</code></td>
<td><code>source/categories/index.md</code></td>
<td><code>categories</code></td>
</tr>
<tr>
<td>标签</td>
<td><code>hexo new page tags</code></td>
<td><code>source/tags/index.md</code></td>
<td><code>tags</code></td>
</tr>
</tbody></table>
<p><code>hexo new page</code> 只会生成带默认 front-matter 的页面，<strong>打开对应 <code>index.md</code> 补上</strong> <code>type</code>（及按需设置 <code>comments: false</code> 等），NexT 才会按关于页 &#x2F; 分类页 &#x2F; 标签页渲染。</p>
<p><code>source/about/index.md</code> 示例：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">关于我</span></span><br><span class="line"><span class="attr">date:</span> <span class="number">2025-01-01 00:00:00</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">about</span></span><br><span class="line"><span class="attr">comments:</span> <span class="literal">false</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="meta"></span></span><br><span class="line"><span class="string">正文写自我介绍或站点说明（支持</span> <span class="string">Markdown）。</span></span><br></pre></td></tr></table></figure>

<p>分类页、标签页的 <code>index.md</code> 把 <code>title</code>、<code>type</code> 改成 <code>分类</code> &#x2F; <code>categories</code> 与 <code>标签</code> &#x2F; <code>tags</code> 即可，<strong>正文常留空</strong>；分类列表与标签云由主题根据各篇文章的 <code>categories</code>、<code>tags</code>  front-matter 生成。<code>type</code> 写错则不会套用对应模板。</p>
<h3 id="导航菜单（-config-next-yml）"><a href="#导航菜单（-config-next-yml）" class="headerlink" title="导航菜单（_config.next.yml）"></a>导航菜单（<code>_config.next.yml</code>）</h3><p>在 <code>menu</code> 中启用或注释对应行（不需要的整行加 <code>#</code>）：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">home:</span> <span class="string">/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-home</span></span><br><span class="line">  <span class="attr">about:</span> <span class="string">/about/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-user</span></span><br><span class="line">  <span class="attr">tags:</span> <span class="string">/tags/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-tags</span></span><br><span class="line">  <span class="attr">categories:</span> <span class="string">/categories/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-th</span></span><br><span class="line">  <span class="attr">archives:</span> <span class="string">/archives/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-archive</span></span><br></pre></td></tr></table></figure>

<p><code>||</code> 前为路径，后为 Font Awesome 图标类名。侧边栏文章数等统计由 <code>site_state: true</code> 控制（仍在 <code>_config.next.yml</code> 的 Sidebar 一段）。</p>
<h3 id="标签云（可选）"><a href="#标签云（可选）" class="headerlink" title="标签云（可选）"></a>标签云（可选）</h3><p>同一文件中的 <code>tagcloud</code> 控制标签页字号与数量等：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">tagcloud:</span></span><br><span class="line">  <span class="attr">min:</span> <span class="number">12</span>        <span class="comment"># 最小字号(px)</span></span><br><span class="line">  <span class="attr">max:</span> <span class="number">30</span>        <span class="comment"># 最大字号(px)</span></span><br><span class="line">  <span class="attr">amount:</span> <span class="number">200</span>    <span class="comment"># 参与展示的标签个数上限</span></span><br><span class="line">  <span class="attr">orderby:</span> <span class="string">name</span>  <span class="comment"># 排序字段，如 name</span></span><br><span class="line">  <span class="attr">order:</span> <span class="number">1</span>       <span class="comment"># 1 升序，-1 降序</span></span><br></pre></td></tr></table></figure>

<p>若采用「把 <code>_config.next.yml</code> 复制进 <code>node_modules/hexo-theme-next/_config.yml</code>」的方式，修改后需再执行一次拷贝命令使主题生效。</p>
<p>文档：<a href="https://theme-next.js.org/docs/theme-settings/custom-pages">NexT Custom Pages</a></p>
<hr>
<h1 id="六、添加远程仓库"><a href="#六、添加远程仓库" class="headerlink" title="六、添加远程仓库"></a>六、添加远程仓库</h1><h2 id="1-进入本地项目根目录"><a href="#1-进入本地项目根目录" class="headerlink" title="1. 进入本地项目根目录"></a>1. 进入本地项目根目录</h2><p>打开终端，<code>cd</code> 到你的项目文件夹：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /本地项目路径</span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<h2 id="2-查看当前是否已有远程仓库"><a href="#2-查看当前是否已有远程仓库" class="headerlink" title="2. 查看当前是否已有远程仓库"></a>2. 查看当前是否已有远程仓库</h2><p>先检查一下，避免重复关联：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure>
<ul>
<li>如果输出为空 → 可以直接执行下一步</li>
<li>如果已有地址 → 先删除旧关联：<code>git remote remove origin</code></li>
</ul>
<h2 id="3-关联远程仓库（核心命令）"><a href="#3-关联远程仓库（核心命令）" class="headerlink" title="3. 关联远程仓库（核心命令）"></a>3. 关联远程仓库（核心命令）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin 你的远程仓库Git地址</span><br></pre></td></tr></table></figure>
<p>示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin https://gitee.com/my919/my919.git</span><br></pre></td></tr></table></figure>
<ul>
<li><code>origin</code> 是远程仓库的默认别名（可以自定义，一般不改）</li>
<li>地址必须是<strong>Git仓库地址</strong>（以 <code>.git</code> 结尾）</li>
</ul>
<h2 id="4-提交本地所有代码"><a href="#4-提交本地所有代码" class="headerlink" title="4. 提交本地所有代码"></a>4. 提交本地所有代码</h2><p>先把本地修改提交到本地仓库：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 添加所有文件到暂存区</span></span><br><span class="line">git add .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交并写备注</span></span><br><span class="line">git commit -m <span class="string">&quot;init&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="5-推送到远程仓库"><a href="#5-推送到远程仓库" class="headerlink" title="5. 推送到远程仓库"></a>5. 推送到远程仓库</h2><h3 id="第一次推送（加-f-参数，以本地覆盖远程分支）"><a href="#第一次推送（加-f-参数，以本地覆盖远程分支）" class="headerlink" title="第一次推送（加 -f 参数，以本地覆盖远程分支）"></a>第一次推送（加 <code>-f</code> 参数，以本地覆盖远程分支）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push -f origin master</span><br></pre></td></tr></table></figure>

<h1 id="七、SEO-与订阅"><a href="#七、SEO-与订阅" class="headerlink" title="七、SEO 与订阅"></a>七、SEO 与订阅</h1><ul>
<li>hexo-generator-sitemap：生成站点地图（Google）</li>
</ul>
<p><code>npm install hexo-generator-sitemap --save</code></p>
<ul>
<li>hexo-generator-feed：生成 RSS 订阅</li>
</ul>
<p><code>npm install hexo-generator-feed --save</code></p>
<h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>打开你 Hexo 根目录的 _config.yml（不是主题的），拉到最下面,添加如下配置</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 站点地图（Google）</span></span><br><span class="line"><span class="attr">sitemap:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">sitemap.xml</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># RSS 订阅</span></span><br><span class="line"><span class="attr">feed:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">atom</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">atom.xml</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">20</span></span><br><span class="line">  <span class="attr">hub:</span></span><br><span class="line">  <span class="attr">content:</span></span><br><span class="line">  <span class="attr">content_limit:</span> <span class="number">140</span></span><br><span class="line">  <span class="attr">content_limit_delim:</span> <span class="string">&#x27; &#x27;</span></span><br><span class="line">  <span class="attr">order_by:</span> <span class="string">-date</span></span><br></pre></td></tr></table></figure>

<h1 id="八、内容增强"><a href="#八、内容增强" class="headerlink" title="八、内容增强"></a>八、内容增强</h1><ul>
<li>hexo-word-counter：文章字数 &#x2F; 阅读时间统计（NexT&#x2F;Butterfly 常用）</li>
</ul>
<p><code>npm install hexo-word-counter --save</code></p>
<h2 id="hexo-word-counter-配置"><a href="#hexo-word-counter-配置" class="headerlink" title="hexo-word-counter 配置"></a>hexo-word-counter 配置</h2><p>打开根目录 _config.yml，添加：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 文章字数统计</span></span><br><span class="line"><span class="attr">symbols_count_time:</span></span><br><span class="line">  <span class="attr">symbols:</span> <span class="literal">true</span>          <span class="comment"># 文章内显示字数</span></span><br><span class="line">  <span class="attr">time:</span> <span class="literal">true</span>             <span class="comment"># 文章内显示阅读时间</span></span><br><span class="line">  <span class="attr">total_symbols:</span> <span class="literal">true</span>    <span class="comment"># 页脚全站总字数</span></span><br><span class="line">  <span class="attr">total_time:</span> <span class="literal">true</span>       <span class="comment"># 页脚全站总阅读时间</span></span><br><span class="line">  <span class="attr">exclude_codeblock:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">awl:</span> <span class="number">4</span>                 <span class="comment"># 平均词长（英文等），一般保持默认即可</span></span><br><span class="line">  <span class="attr">wpm:</span> <span class="number">300</span>               <span class="comment"># 中文偏多可试 280–350；中英混合常用 275</span></span><br></pre></td></tr></table></figure>


<h1 id="九、性能优化（提速-压缩）"><a href="#九、性能优化（提速-压缩）" class="headerlink" title="九、性能优化（提速 + 压缩）"></a>九、性能优化（提速 + 压缩）</h1><ul>
<li>hexo-all-minifier：一键压缩 HTML&#x2F;CSS&#x2F;JS&#x2F; 图片（必装）</li>
</ul>
<p><code>npm install hexo-all-minifier --save</code></p>
<ul>
<li>hexo-filter-nofollow：外链自动加 nofollow（防权重流失）</li>
</ul>
<p><code>npm install hexo-filter-nofollow --saveHexo</code></p>
<p>在<strong>根目录 <code>_config.yml</code></strong> 添加</p>
<h2 id="hexo-all-minifier（全站压缩-图片压缩）"><a href="#hexo-all-minifier（全站压缩-图片压缩）" class="headerlink" title="hexo-all-minifier（全站压缩+图片压缩）"></a>hexo-all-minifier（全站压缩+图片压缩）</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 全站资源压缩（HTML/CSS/JS/图片）</span></span><br><span class="line"><span class="attr">all_minifier:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">exclude:</span></span><br><span class="line">  <span class="attr">html:</span></span><br><span class="line">    <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">remove_comments:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">collapse_whitespace:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">css:</span></span><br><span class="line">    <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">exclude:</span></span><br><span class="line">  <span class="attr">js:</span></span><br><span class="line">    <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">exclude:</span></span><br><span class="line">    <span class="attr">mangle:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">image:</span></span><br><span class="line">    <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">only_smaller:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">jpg:</span></span><br><span class="line">      <span class="attr">quality:</span> <span class="number">85</span></span><br><span class="line">    <span class="attr">png:</span></span><br><span class="line">      <span class="attr">quality:</span> [<span class="number">60</span>, <span class="number">80</span>]</span><br><span class="line">    <span class="attr">webp:</span></span><br><span class="line">      <span class="attr">quality:</span> <span class="number">85</span></span><br></pre></td></tr></table></figure>

<h3 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h3><ul>
<li>压缩 HTML、CSS、JS</li>
<li>压缩 JPG&#x2F;PNG&#x2F;WebP 图片</li>
<li>大幅加快网站打开速度</li>
<li>不影响任何功能</li>
</ul>
<hr>
<h2 id="hexo-filter-nofollow（自动给外链加-nofollow）"><a href="#hexo-filter-nofollow（自动给外链加-nofollow）" class="headerlink" title="hexo-filter-nofollow（自动给外链加 nofollow）"></a>hexo-filter-nofollow（自动给外链加 nofollow）</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 外链自动添加 nofollow</span></span><br><span class="line"><span class="attr">nofollow:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">site</span></span><br><span class="line">  <span class="attr">exclude:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&#x27;gitee.com&#x27;</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&#x27;github.com&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="作用-1"><a href="#作用-1" class="headerlink" title="作用"></a>作用</h3><ul>
<li>自动给<strong>所有外部链接</strong>加上 <code>rel=&quot;external nofollow&quot;</code></li>
<li>防止网站权重流失</li>
<li>内部链接不会加，非常安全</li>
</ul>
<hr>
<h1 id="十、添加站内搜索（NexT-Local-Search）"><a href="#十、添加站内搜索（NexT-Local-Search）" class="headerlink" title="十、添加站内搜索（NexT + Local Search）"></a>十、添加站内搜索（NexT + Local Search）</h1><p>NexT 自带 <strong>Local Search</strong> 与 <strong>Algolia</strong> 两种对接方式。个人博客常用 <strong>本地搜索</strong>：由 <strong><code>hexo-generator-searchdb</code></strong> 在生成站点时输出检索索引（如 <strong><code>search.xml</code></strong>），主题在侧栏或菜单中加载该文件完成前端检索，无需外网账号。</p>
<h2 id="1-安装生成索引的插件"><a href="#1-安装生成索引的插件" class="headerlink" title="1. 安装生成索引的插件"></a>1. 安装生成索引的插件</h2><p>在项目根执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>

<p>（若 <strong><code>package.json</code></strong> 里已有 <strong><code>hexo-generator-searchdb</code></strong>，可跳过本步。）</p>
<h2 id="2-配置站点根目录-config-yml"><a href="#2-配置站点根目录-config-yml" class="headerlink" title="2. 配置站点根目录 _config.yml"></a>2. 配置站点根目录 <code>_config.yml</code></h2><p>在 Hexo <strong>根目录</strong> <strong><code>_config.yml</code></strong>（不是主题配置）中增加或核对 <strong><code>search</code></strong> 段，使生成器写出 NexT 可用的索引文件。示例：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">search:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">search.xml</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">post</span></span><br><span class="line">  <span class="attr">content:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">format:</span> <span class="string">html</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">10000</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>path</code></strong>：输出文件名，需与 NexT 默认约定一致（一般为 <strong><code>search.xml</code></strong>）。  </li>
<li><strong><code>content: true</code></strong>：索引正文，中文检索更完整，生成体积会变大。<br>修改后执行 <strong><code>hexo clean &amp;&amp; hexo g</code></strong>，确认 <strong><code>public/</code></strong> 下出现 <strong><code>search.xml</code></strong>（或你配置的 <code>path</code>）。</li>
</ul>
<h2 id="3-在-config-next-yml-中开启-Local-Search"><a href="#3-在-config-next-yml-中开启-Local-Search" class="headerlink" title="3. 在 _config.next.yml 中开启 Local Search"></a>3. 在 <code>_config.next.yml</code> 中开启 Local Search</h2><p>打开 <strong><code>_config.next.yml</code></strong>，在 <strong>Search Services</strong> 一段将 <strong><code>local_search.enable</code></strong> 设为 <strong><code>true</code></strong>，并按需调整触发方式等，例如：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">local_search:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">trigger:</span> <span class="string">auto</span>          <span class="comment"># auto：输入即搜；manual：需回车或点按钮</span></span><br><span class="line">  <span class="attr">top_n_per_article:</span> <span class="number">1</span>   <span class="comment"># 每篇文章在结果列表中展示条数</span></span><br><span class="line">  <span class="attr">unescape:</span> <span class="literal">true</span>         <span class="comment"># 建议 true，减轻 HTML 实体导致的中文显示问题</span></span><br></pre></td></tr></table></figure>

<h2 id="4-使主题配置生效"><a href="#4-使主题配置生效" class="headerlink" title="4. 使主题配置生效"></a>4. 使主题配置生效</h2><p>若你采用「将 <strong><code>_config.next.yml</code></strong> 复制到 <strong><code>node_modules/hexo-theme-next/_config.yml</code></strong>」的方式，修改后需再执行一次复制。</p>
<h2 id="5-生成与预览"><a href="#5-生成与预览" class="headerlink" title="5. 生成与预览"></a>5. 生成与预览</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</span><br></pre></td></tr></table></figure>

<p>在站点上打开搜索入口（常见为侧栏放大镜或菜单 <strong>Search</strong>），输入关键词，应能命中文章标题与正文（若已索引 <strong><code>content</code></strong>）。</p>
<p>文档：<a href="https://theme-next.js.org/docs/third-party-services/search-services">NexT Search Services</a> · <a href="https://github.com/next-theme/hexo-generator-searchdb">hexo-generator-searchdb</a></p>
<hr>
<h1 id="十一、Python-文档目录"><a href="#十一、Python-文档目录" class="headerlink" title="十一、Python 文档目录"></a>十一、Python 文档目录</h1><p>把 Python 相关博文统一放在 <strong><code>source/_posts/Python/</code></strong> 下，便于本地整理；站点上仍由文章的 <code>categories</code>、<code>tags</code> 控制归档与导航。</p>
<h2 id="1-创建目录（首次）"><a href="#1-创建目录（首次）" class="headerlink" title="1. 创建目录（首次）"></a>1. 创建目录（首次）</h2><p>在项目根目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p <span class="built_in">source</span>/_posts/Python</span><br></pre></td></tr></table></figure>

<p>若使用下面「带 <code>--path</code> 新建文章」的方式，<strong>首次也可以不手动建目录</strong>，Hexo 会随文章一起创建 <code>Python</code> 文件夹。</p>
<p>为让 Git 跟踪空目录，可在其中保留占位文件（本仓库可含 <code>source/_posts/Python/.gitkeep</code>，可删可留）。</p>
<h2 id="2-在-Python-子目录下新建文章"><a href="#2-在-Python-子目录下新建文章" class="headerlink" title="2. 在 Python 子目录下新建文章"></a>2. 在 <code>Python</code> 子目录下新建文章</h2><p>在<strong>站点根目录</strong>执行（<code>--path</code> 为相对 <code>source/_posts/</code> 的路径，<strong>不要</strong>写 <code>.md</code> 后缀）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new post <span class="string">&quot;文章显示标题&quot;</span> --path Python/文件基名</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new post <span class="string">&quot;Python的UV工具&quot;</span> --path Python/Python-UV</span><br></pre></td></tr></table></figure>

<p>会生成：<code>source/_posts/Python/Python-UV.md</code>，<code>title</code> 为「Python的UV工具」。</p>
<h3 id="创建时指定分类、标签（可选）"><a href="#创建时指定分类、标签（可选）" class="headerlink" title="创建时指定分类、标签（可选）"></a>创建时指定分类、标签（可选）</h3><p>Hexo 会把 <code>hexo new</code> 里除标题、<code>--path</code>、<code>--slug</code> 等保留项以外的参数写进 front-matter，因此<strong>可在同一条命令里</strong>带上 <code>categories</code>、<code>tags</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new post <span class="string">&quot;Python的UV工具&quot;</span> --path Python/Python-UV --categories Python --tags UV --tags Python</span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>--categories</code></strong>：一般写一个即可（与分类页一致）。  </li>
<li><strong><code>--tags</code></strong>：需要多个标签时，<strong>重复写</strong> <code>--tags 标签A --tags 标签B</code>（会生成 YAML 列表）；若只写一个字符串如 <code>--tags &quot;UV,Python&quot;</code>，会落成<strong>单个</strong>标签字符串，通常不如多次 <code>--tags</code> 清晰。</li>
</ul>
<p>未在命令行写全的字段，仍可打开 <code>.md</code> 再改。</p>
<h2 id="3-生成与预览"><a href="#3-生成与预览" class="headerlink" title="3. 生成与预览"></a>3. 生成与预览</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</span><br></pre></td></tr></table></figure>

<p>在本地确认文章列表与分类、标签展示是否符合预期后再部署。</p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>DL</title>
    <url>/2026/05/01/AI/DL/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>DL</tag>
      </tags>
  </entry>
  <entry>
    <title>LLM</title>
    <url>/2026/05/01/AI/LLM/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>LLM</tag>
      </tags>
  </entry>
  <entry>
    <title>ML</title>
    <url>/2026/05/01/AI/ML/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>ML</tag>
      </tags>
  </entry>
  <entry>
    <title>Flink</title>
    <url>/2026/05/01/BigData/Flink/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>BigData</category>
      </categories>
      <tags>
        <tag>Flink</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka</title>
    <url>/2026/05/01/BigData/Kafka/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>BigData</category>
      </categories>
      <tags>
        <tag>Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>NFT</title>
    <url>/2026/05/01/Blockchain/NFT/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Blockchain</category>
      </categories>
      <tags>
        <tag>NFT</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark</title>
    <url>/2026/05/01/BigData/Spark/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>BigData</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>元宇宙(Metaverse)</title>
    <url>/2026/05/01/Blockchain/%E5%85%83%E5%AE%87%E5%AE%99/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Blockchain</category>
      </categories>
      <tags>
        <tag>元宇宙</tag>
      </tags>
  </entry>
  <entry>
    <title>区块链</title>
    <url>/2026/05/01/Blockchain/%E5%8C%BA%E5%9D%97%E9%93%BE/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Blockchain</category>
      </categories>
      <tags>
        <tag>区块链</tag>
      </tags>
  </entry>
  <entry>
    <title>C</title>
    <url>/2026/05/01/C/hello/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>C</category>
      </categories>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 ClickHouse</title>
    <url>/2026/05/01/ClickHouse/ClickHouse-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>ClickHouse</strong>，适合本地开发、与 <strong>Compose</strong> 编排其它服务、或在 <strong>Kubernetes</strong> 中单节点部署。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；密码与数据卷请按环境自行替换，勿把生产密钥提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、ClickHouse-镜像选择"><a href="#二、ClickHouse-镜像选择" class="headerlink" title="二、ClickHouse 镜像选择"></a>二、ClickHouse 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>clickhouse/clickhouse-server</code></strong>（Docker Hub 官方组织镜像，与 <strong>ClickHouse</strong> 发行节奏一致）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>clickhouse/clickhouse-server:24.8</code></strong>：固定次版本，便于与文档、驱动对齐。  </li>
<li><strong><code>clickhouse/clickhouse-server:latest</code></strong> 或 <strong><code>...-lts</code></strong>：滚动更新（以 <a href="https://hub.docker.com/r/clickhouse/clickhouse-server/tags">Docker Hub clickhouse-server tags</a> 为准）。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定具体补丁标签或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>访问控制</strong></td>
<td>设置 <strong><code>CLICKHOUSE_USER</code></strong>、<strong><code>CLICKHOUSE_PASSWORD</code></strong> 时，建议同时 <strong><code>CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1</code></strong>，以便用 <strong>SQL</strong> 管理用户与权限（与官方镜像说明一致）。</td>
</tr>
<tr>
<td><strong>协议与端口</strong></td>
<td><strong>HTTP</strong> 默认 <strong>8123</strong>，<strong>Native</strong> 默认 <strong>9000</strong>；客户端按协议选择端口。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>多数标签提供多架构；若拉取失败，换标签或在 Hub 查看 <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区编排模板与 <strong><code>bitnami/clickhouse</code></strong> 等路径、变量不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-初始化脚本"><a href="#3-初始化脚本" class="headerlink" title="3. 初始化脚本"></a>3. 初始化脚本</h2><p>将 <strong><code>.sql</code> &#x2F; <code>.sh</code></strong> 放入 <strong><code>/docker-entrypoint-initdb.d</code></strong>（通过卷挂载），仅在<strong>首次初始化空数据目录</strong>时执行；已有数据卷时不会重复执行。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>在<strong>当前目录</strong>先建数据与初始化目录（仅首次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./ch-data ./ch-init</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（请按需改账号、密码、库名、端口、卷路径；示例仅供本地）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name clickhouse-dev -e CLICKHOUSE_USER=app -e CLICKHOUSE_PASSWORD=dev-change-me -e CLICKHOUSE_DB=default -e CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1 -p 8123:8123 -p 9000:9000 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/ch-data:/var/lib/clickhouse&quot;</span> -v <span class="string">&quot;<span class="subst">$(pwd)</span>/ch-init:/docker-entrypoint-initdb.d:ro&quot;</span> clickhouse/clickhouse-server:24.8</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f clickhouse-dev</span><br><span class="line">docker stop clickhouse-dev &amp;&amp; docker <span class="built_in">rm</span> clickhouse-dev</span><br></pre></td></tr></table></figure>

<p>本机 <strong>HTTP</strong> 探测示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sS <span class="string">&quot;http://127.0.0.1:8123/ping&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>Native</strong> 客户端示例（与镜像内 <strong><code>clickhouse-client</code></strong> 主版本一致即可）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -it clickhouse-dev clickhouse-client -u app --password dev-change-me --query <span class="string">&quot;SELECT version()&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.clickhouse.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。敏感项建议放在同目录 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>），勿长期写死在仓库。</p>
<p><strong>一条命令</strong>启动全栈（在 <strong><code>docker-compose.clickhouse.yml</code></strong> 所在目录执行）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.clickhouse.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.clickhouse.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">clickhouse-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">clickhouse:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">clickhouse/clickhouse-server:24.8</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">clickhouse-24</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">CLICKHOUSE_USER:</span> <span class="string">$&#123;CLICKHOUSE_USER:-app&#125;</span></span><br><span class="line">      <span class="attr">CLICKHOUSE_PASSWORD:</span> <span class="string">$&#123;CLICKHOUSE_PASSWORD:-dev-change-me&#125;</span></span><br><span class="line">      <span class="attr">CLICKHOUSE_DB:</span> <span class="string">$&#123;CLICKHOUSE_DB:-default&#125;</span></span><br><span class="line">      <span class="attr">CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8123:8123&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9000:9000&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ch_data:/var/lib/clickhouse</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./ch-init:/docker-entrypoint-initdb.d:ro</span></span><br><span class="line">    <span class="attr">ulimits:</span></span><br><span class="line">      <span class="attr">nofile:</span></span><br><span class="line">        <span class="attr">soft:</span> <span class="number">262144</span></span><br><span class="line">        <span class="attr">hard:</span> <span class="number">262144</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;wget --no-verbose --tries=1 --spider http://127.0.0.1:8123/ping || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">12</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">40s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">ch_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>同目录可选 <strong><code>.env</code></strong>（勿提交）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CLICKHOUSE_USER=app</span><br><span class="line">CLICKHOUSE_PASSWORD=your-strong-password</span><br><span class="line">CLICKHOUSE_DB=default</span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.clickhouse.yml ps</span><br><span class="line">docker compose -f docker-compose.clickhouse.yml logs -f clickhouse</span><br><span class="line">docker compose -f docker-compose.clickhouse.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留命名卷 <strong><code>ch_data</code></strong>；若需清空数据加 <strong><code>docker compose ... down -v</code></strong>（<strong>会删库</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>clickhouse-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f clickhouse-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>Secret</strong>（业务用户与库名）、<strong>PVC</strong>、<strong>Deployment</strong>、<strong>ClusterIP Service</strong>（<strong>8123</strong>、<strong>9000</strong>）。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">clickhouse-secret</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">CLICKHOUSE_USER:</span> <span class="string">&quot;app&quot;</span></span><br><span class="line">  <span class="attr">CLICKHOUSE_PASSWORD:</span> <span class="string">&quot;请替换为强密码&quot;</span></span><br><span class="line">  <span class="attr">CLICKHOUSE_DB:</span> <span class="string">&quot;default&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">clickhouse-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">50Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">clickhouse</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">clickhouse</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">clickhouse</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">clickhouse</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">clickhouse</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">clickhouse/clickhouse-server:24.8</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8123</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9000</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">native</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">CLICKHOUSE_USER</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">clickhouse-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">CLICKHOUSE_USER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">CLICKHOUSE_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">clickhouse-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">CLICKHOUSE_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">CLICKHOUSE_DB</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">clickhouse-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">CLICKHOUSE_DB</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;Asia/Shanghai&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/var/lib/clickhouse</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">httpGet:</span></span><br><span class="line">              <span class="attr">path:</span> <span class="string">/ping</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">8123</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">24</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">httpGet:</span></span><br><span class="line">              <span class="attr">path:</span> <span class="string">/ping</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">8123</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">60</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;500m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;4Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;2000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">clickhouse-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">clickhouse</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">clickhouse</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">clickhouse</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">8123</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">8123</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">native</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9000</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9000</span></span><br></pre></td></tr></table></figure>

<p>查看资源与连接前检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get pods,svc,pvc -l app=clickhouse</span><br></pre></td></tr></table></figure>

<p>集群内 <strong>HTTP</strong> 示例：<strong><code>http://clickhouse.default.svc.cluster.local:8123/</code></strong>（带鉴权的查询需在 <strong>URL</strong> 或 <strong>Header</strong> 中按官方 <strong>HTTP</strong> 接口说明传用户与密码）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；<strong>Keeper</strong>、分片、副本与高可用请使用 <strong>ClickHouse Operator</strong> 或专用编排方案，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先 <strong><code>clickhouse/clickhouse-server:24.8</code></strong> &#x2F; 固定补丁标签；<strong><code>CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1</code></strong> 与账号环境变量配套使用</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong> 后一条 <strong><code>docker run ...</code></strong>（暴露 <strong>8123</strong>、<strong>9000</strong>）</td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.clickhouse.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>clickhouse-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请配合备份、监控、网络策略与 <strong>TLS</strong>（<strong>8123</strong> 对外时务必加固）；<strong>Secret</strong> 建议对接 <strong>External Secrets</strong> &#x2F; <strong>Sealed Secrets</strong> 等管控流程。</p>
]]></content>
      <categories>
        <category>ClickHouse</category>
      </categories>
      <tags>
        <tag>ClickHouse</tag>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>ClickHouse</title>
    <url>/2026/05/01/ClickHouse/ClickHouse/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>ClickHouse</category>
      </categories>
      <tags>
        <tag>ClickHouse</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 1Panel</title>
    <url>/2026/05/02/Cloud/Docker-1Panel/</url>
    <content><![CDATA[<p>在已安装 <strong>Docker</strong> 的环境中，可用 <strong><code>docker run</code></strong> 拉起 <strong>1Panel V2</strong>（社区镜像 <strong><code>moelin/1panel</code></strong>，非 <strong>FIT2CLOUD</strong> 官方安装包；升级路径与支持策略与官方宿主版可能不同）。镜像说明、标签与环境变量以 <strong><a href="https://hub.docker.com/r/moelin/1panel">Docker Hub：moelin&#x2F;1panel</a></strong> 为准；生产请固定<strong>小版本标签</strong>（如 <strong><code>moelin/1panel:v2.0.6</code></strong>），勿盲目追 <strong><code>latest</code></strong>。官方在线安装见：<a href="https://1panel.cn/docs/v2/installation/online_installation/">在线安装</a>。</p>
<p>部署按宿主机与引擎分<strong>两类</strong>，下文<strong>分开写</strong>，勿混用 <strong><code>--network host</code></strong> 与 <strong><code>-p</code></strong>：</p>
<ol>
<li><strong>Windows + Docker Desktop + WSL</strong>：在 <strong>WSL</strong>（如 <strong>Rocky Linux 10</strong> 等发行版）或 <strong>Windows CMD</strong> 里执行 <strong><code>docker run</code></strong>，引擎为 <strong>Docker Desktop</strong> 时须用<strong>端口映射</strong>。  </li>
<li><strong>Linux Docker 主机</strong>：云服务器、物理机或虚拟机内<strong>原生 Docker Engine</strong>，可用 <strong><code>--network host</code></strong>。</li>
</ol>
<hr>
<h2 id="共用前提"><a href="#共用前提" class="headerlink" title="共用前提"></a>共用前提</h2><ul>
<li><strong><code>docker version</code></strong> 客户端与 <strong>Server</strong> 均正常，能拉取 <strong>Docker Hub</strong>（或已配置镜像加速）。  </li>
<li>建议内存 <strong>≥ 1GB</strong>；多架构以 <strong>Hub</strong> 页为准。  </li>
<li>将 <strong><code>PASSWORD</code></strong>、<strong><code>ENTRANCE</code></strong> 等换成自己的值；变量含义见文末 <strong>「常用环境变量」</strong>。</li>
</ul>
<hr>
<h2 id="第一类：Windows-Docker-Desktop-WSL"><a href="#第一类：Windows-Docker-Desktop-WSL" class="headerlink" title="第一类：Windows + Docker Desktop + WSL"></a>第一类：Windows + Docker Desktop + WSL</h2><p><strong>场景</strong>：宿主机为 <strong>Windows</strong>，容器由 <strong>Docker Desktop</strong> 提供引擎；你在 <strong>WSL</strong> 里打开终端（发行版可为 <strong>Rocky Linux 10</strong>、<strong>Ubuntu</strong> 等），<strong><code>docker version</code></strong> 的 <strong>Server</strong> 往往显示 <strong>Docker Desktop</strong> 或上下文为 <strong>desktop-linux</strong>。此时容器里的 <strong><code>host</code></strong> 网络与你在 <strong>WSL</strong> 里访问的 <strong><code>localhost</code></strong> 不是同一概念，<strong>不要</strong>使用 <strong><code>--network host</code></strong>，应使用 <strong><code>-p &lt;PORT&gt;:&lt;PORT&gt;</code></strong>。</p>
<p>若曾误用 <strong><code>host</code></strong> 建过同名容器，先 <strong><code>docker rm -f 1panel-v2</code></strong> 再按本节重建。</p>
<h3 id="WSL-Git-Bash-macOS（行末-续行）"><a href="#WSL-Git-Bash-macOS（行末-续行）" class="headerlink" title="WSL &#x2F; Git Bash &#x2F; macOS（行末 \ 续行）"></a>WSL &#x2F; Git Bash &#x2F; macOS（行末 <code>\</code> 续行）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># wsl子系统rocky-10中执行不能正常访问，猜测子系统无ip</span></span><br><span class="line">docker run -d\</span><br><span class="line">  --name 1panel-v2\</span><br><span class="line">  --restart always\</span><br><span class="line">  -p 10086:10086\</span><br><span class="line">  -v /var/run/docker.sock:/var/run/docker.sock\</span><br><span class="line">  -v /opt:/opt\</span><br><span class="line">  -e TZ=Asia/Shanghai\</span><br><span class="line">  -e PORT=10086\</span><br><span class="line">  -e USERNAME=admin\</span><br><span class="line">  -e PASSWORD=<span class="string">&#x27;pi=3.1415&#x27;</span>\</span><br><span class="line">  -e ENTRANCE=<span class="string">&#x27;panel-v2&#x27;</span>\</span><br><span class="line">  -e BASE_DIR=/opt\</span><br><span class="line">  moelin/1panel:v2</span><br></pre></td></tr></table></figure>

<h3 id="Windows-CMD（行末-续行）"><a href="#Windows-CMD（行末-续行）" class="headerlink" title="Windows CMD（行末 ^ 续行）"></a>Windows CMD（行末 <code>^</code> 续行）</h3><p><strong><code>^</code></strong> 须为行末最后一个字符（后无空格），末行不写 <strong><code>^</code></strong>。<strong><code>-e</code></strong> 的值在 <strong>CMD</strong> 里用双引号较稳妥。</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">docker run -d^</span><br><span class="line">  --name <span class="number">1</span>panel-v2^</span><br><span class="line">  --restart always^</span><br><span class="line">  -p <span class="number">10086</span>:<span class="number">10086</span>^</span><br><span class="line">  -v /var/run/docker.sock:/var/run/docker.sock^</span><br><span class="line">  -v /opt:/opt^</span><br><span class="line">  -e TZ=Asia/Shanghai^</span><br><span class="line">  -e PORT=<span class="number">10086</span>^</span><br><span class="line">  -e USERNAME=admin^</span><br><span class="line">  -e PASSWORD=&quot;pi=<span class="number">3</span>.<span class="number">1415</span>&quot;^</span><br><span class="line">  -e ENTRANCE=&quot;panel-v2&quot;^</span><br><span class="line">  -e BASE_DIR=/opt^</span><br><span class="line">  moelin/<span class="number">1</span>panel:v2</span><br></pre></td></tr></table></figure>

<p><strong>卷说明</strong>（与 <strong>Linux</strong> 类一致，路径在 <strong>Docker Desktop</strong> 下仍写 <strong><code>/var/run/docker.sock</code></strong>、<strong><code>/opt</code></strong>，由 <strong>Desktop</strong> 映射到引擎；异常时以当前 <strong>Desktop</strong> 文档为准。若 <strong><code>-v</code></strong> 与 <strong>WSL2 &#x2F; Linux 容器</strong> 后端要求不符，按 <strong>Docker Desktop</strong> 文档调整。）</p>
<h3 id="浏览器访问（本机）"><a href="#浏览器访问（本机）" class="headerlink" title="浏览器访问（本机）"></a>浏览器访问（本机）</h3><p>多数情况：<strong><code>http://127.0.0.1:10086/&lt;ENTRANCE&gt;</code></strong>（与示例 <strong><code>ENTRANCE</code></strong> 一致时）。在 <strong>WSL</strong> 内若 <strong><code>localhost</code></strong> 仍不通，可试 <strong><code>http://$(grep -m1 &#39;^nameserver&#39; /etc/resolv.conf | awk &#39;{print $2}&#39;):10086/&lt;ENTRANCE&gt;</code></strong>（常为 <strong>WSL2</strong> 下可达的 <strong>Windows</strong> 主机 <strong>IP</strong>，以本机为准）。</p>
<h3 id="可选：docker-compose（端口映射）"><a href="#可选：docker-compose（端口映射）" class="headerlink" title="可选：docker compose（端口映射）"></a>可选：<code>docker compose</code>（端口映射）</h3><p>将下文保存为同目录下的 <strong><code>compose.yaml</code></strong> 或 <strong><code>docker-compose.yml</code></strong>（二选一即可，<strong>Compose V2</strong> 默认会读取二者）。<strong>Docker Desktop &#x2F; WSL</strong> 使用 <strong><code>ports</code></strong>，<strong>不要</strong>写 <strong><code>network_mode: host</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># compose.yaml（或 docker-compose.yml）</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">1panel-v2:</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">1panel-v2</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">moelin/1panel:v2</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10086:10086&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/var/run/docker.sock:/var/run/docker.sock</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/opt:/opt</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">      <span class="attr">PORT:</span> <span class="string">&quot;10086&quot;</span></span><br><span class="line">      <span class="attr">USERNAME:</span> <span class="string">admin</span></span><br><span class="line">      <span class="attr">PASSWORD:</span> <span class="string">&quot;请改为强密码&quot;</span></span><br><span class="line">      <span class="attr">ENTRANCE:</span> <span class="string">&quot;请改为安全入口&quot;</span></span><br><span class="line">      <span class="attr">BASE_DIR:</span> <span class="string">/opt</span></span><br></pre></td></tr></table></figure>

<p>在同目录执行 <strong><code>docker compose up -d</code></strong>。</p>
<hr>
<h2 id="第二类：Linux-Docker-主机"><a href="#第二类：Linux-Docker-主机" class="headerlink" title="第二类：Linux Docker 主机"></a>第二类：Linux Docker 主机</h2><p><strong>场景</strong>：<strong>Linux</strong> 云服务器、物理机或虚拟机内安装 <strong>Docker Engine</strong>（<strong>非 Docker Desktop</strong> 作为服务端）。可用 <strong><code>--network host</code></strong>，面板监听端口即宿主机端口，与安全组、防火墙规则一致。</p>
<h3 id="多行-docker-run（-network-host）"><a href="#多行-docker-run（-network-host）" class="headerlink" title="多行 docker run（--network host）"></a>多行 <code>docker run</code>（<code>--network host</code>）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name 1panel-v2 \</span><br><span class="line">  --restart always \</span><br><span class="line">  --network host \</span><br><span class="line">  -v /var/run/docker.sock:/var/run/docker.sock \</span><br><span class="line">  -v /opt:/opt \</span><br><span class="line">  -e TZ=Asia/Shanghai \</span><br><span class="line">  -e PORT=10086 \</span><br><span class="line">  -e USERNAME=admin \</span><br><span class="line">  -e PASSWORD=<span class="string">&#x27;pi=3.1415&#x27;</span> \</span><br><span class="line">  -e ENTRANCE=<span class="string">&#x27;panel-v2&#x27;</span> \</span><br><span class="line">  -e BASE_DIR=/opt \</span><br><span class="line">  moelin/1panel:v2</span><br></pre></td></tr></table></figure>

<p><strong><code>-v /var/run/docker.sock</code></strong>：面板管理宿主机 <strong>Docker</strong>。<br><strong><code>-v /opt:/opt</code></strong> 与 <strong><code>BASE_DIR=/opt</code></strong>：数据目录可改，须与挂载路径同步。<br><strong><code>moelin/1panel:v2</code></strong>：跟踪 <strong>V2</strong> 最新构建；生产建议改为 <strong>Hub</strong> 具体版本号。</p>
<h3 id="可选：docker-compose（host-网络）"><a href="#可选：docker-compose（host-网络）" class="headerlink" title="可选：docker compose（host 网络）"></a>可选：<code>docker compose</code>（<code>host</code> 网络）</h3><p>将下文保存为同目录下的 <strong><code>compose.yaml</code></strong> 或 <strong><code>docker-compose.yml</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># compose.yaml（或 docker-compose.yml）</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">1panel-v2:</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">1panel-v2</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">network_mode:</span> <span class="string">host</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">moelin/1panel:v2</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/var/run/docker.sock:/var/run/docker.sock</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/opt:/opt</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">      <span class="attr">PORT:</span> <span class="string">&quot;10086&quot;</span></span><br><span class="line">      <span class="attr">USERNAME:</span> <span class="string">admin</span></span><br><span class="line">      <span class="attr">PASSWORD:</span> <span class="string">&quot;请改为强密码&quot;</span></span><br><span class="line">      <span class="attr">ENTRANCE:</span> <span class="string">&quot;请改为安全入口&quot;</span></span><br><span class="line">      <span class="attr">BASE_DIR:</span> <span class="string">/opt</span></span><br></pre></td></tr></table></figure>

<p>在同目录执行 <strong><code>docker compose up -d</code></strong>。</p>
<hr>
<h2 id="常用环境变量（V2）"><a href="#常用环境变量（V2）" class="headerlink" title="常用环境变量（V2）"></a>常用环境变量（V2）</h2><table>
<thead>
<tr>
<th>变量</th>
<th>默认值（未设置时）</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>PORT</code></strong></td>
<td><strong><code>10086</code></strong></td>
<td>Web 端口</td>
</tr>
<tr>
<td><strong><code>USERNAME</code></strong></td>
<td><strong><code>1panel</code></strong></td>
<td>管理员用户名</td>
</tr>
<tr>
<td><strong><code>PASSWORD</code></strong></td>
<td>随机或默认（见 <strong>Hub</strong>）</td>
<td>登录控制台密码，务必改为强密码或从日志读取随机值</td>
</tr>
<tr>
<td><strong><code>ENTRANCE</code></strong></td>
<td><strong><code>entrance</code></strong></td>
<td>安全入口的 <strong>URL 路径前缀</strong>（非第二端口），访问 <strong><code>http://IP:PORT/ENTRANCE</code></strong>，用于降低默认路径被扫的概率</td>
</tr>
<tr>
<td><strong><code>BASE_DIR</code></strong></td>
<td><strong><code>/opt</code></strong></td>
<td>数据目录，需与 <strong><code>/opt</code></strong> 挂载一致</td>
</tr>
<tr>
<td><strong><code>TZ</code></strong></td>
<td>常见 <strong><code>Asia/Shanghai</code></strong></td>
<td>时区</td>
</tr>
<tr>
<td><strong><code>RESET</code></strong></td>
<td><strong><code>false</code></strong></td>
<td>置 <strong><code>true</code></strong> 可强制重置配置（慎用）</td>
</tr>
</tbody></table>
<p>完整说明与 <strong>V1</strong>、<strong>Global</strong> 标签见 <strong>Hub</strong> 文档。</p>
<hr>
<h2 id="访问与日常命令（Linux-公网）"><a href="#访问与日常命令（Linux-公网）" class="headerlink" title="访问与日常命令（Linux 公网）"></a>访问与日常命令（Linux 公网）</h2><p>浏览器访问（云服务器需在安全组放行 <strong><code>PORT</code></strong>）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">http://&lt;服务器 IP&gt;:&lt;PORT&gt;/&lt;ENTRANCE&gt;</span><br></pre></td></tr></table></figure>

<p>首次未显式设置强密码时，镜像可能生成随机密码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs 1panel-v2</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f 1panel-v2</span><br><span class="line">docker restart 1panel-v2</span><br><span class="line">docker <span class="built_in">exec</span> -it 1panel-v2 bash   <span class="comment"># 进入容器后可用 1pctl 等（以镜像内为准）</span></span><br></pre></td></tr></table></figure>

<p><strong>禁止</strong>在容器内随意点面板「更新」覆盖社区镜像行为；升级应 <strong><code>docker pull</code> 新镜像 → 备份数据卷 → 按 <strong>Hub</strong> 说明重建容器</strong>。</p>
<p><strong>V1 → V2</strong> 不可直接原地升级，须按官方迁移说明：<a href="https://1panel.cn/docs/v2/installation/v1_migrate/">V1 迁移到 V2</a>。</p>
<hr>
<h2 id="官方脚本安装（对照）"><a href="#官方脚本安装（对照）" class="headerlink" title="官方脚本安装（对照）"></a>官方脚本安装（对照）</h2><p>若希望使用 <strong>FIT2CLOUD</strong> 官方脚本在<strong>宿主机</strong>安装（非本镜像）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash -c <span class="string">&quot;<span class="subst">$(curl -sSL https://resource.fit2cloud.com/1panel/package/v2/quick_start.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<p>与 <strong>Docker</strong> 一键方案二选一，避免同一主机装两套面板。</p>
<hr>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><table>
<thead>
<tr>
<th>项</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong>第一类（Windows + Docker Desktop + WSL）</strong></td>
<td><strong><code>-p 10086:10086</code></strong>；<strong>WSL</strong> 内 <strong>Bash</strong> 或 <strong>Windows CMD</strong> 见上文；<strong>compose</strong> 用 <strong><code>ports</code></strong>，不用 <strong><code>network_mode: host</code></strong></td>
</tr>
<tr>
<td><strong>第二类（Linux Docker 主机）</strong></td>
<td><strong><code>--network host</code></strong> 或 <strong>compose</strong> 的 <strong><code>network_mode: host</code></strong>；一条命令见上文</td>
</tr>
<tr>
<td>数据</td>
<td><strong><code>/opt</code></strong> 与 <strong><code>BASE_DIR</code></strong> 一致；挂载 <strong><code>docker.sock</code></strong> 以便管 <strong>Docker</strong></td>
</tr>
<tr>
<td>访问与安全</td>
<td><strong><code>http://IP:PORT/ENTRANCE</code></strong>；随机密码 <strong><code>docker logs</code></strong></td>
</tr>
<tr>
<td>参考</td>
<td><strong><a href="https://hub.docker.com/r/moelin/1panel">moelin&#x2F;1panel</a></strong>；官方宿主安装见 <strong>在线安装</strong></td>
</tr>
</tbody></table>
<p>部署前阅读 <strong>Hub</strong> 注意事项与官方迁移文档；生产使用强密码、限制暴露面并做好备份。</p>
]]></content>
      <categories>
        <category>Cloud</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>1Panel</tag>
        <tag>Docker Desktop</tag>
        <tag>WSL</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker</title>
    <url>/2026/05/01/Cloud/Docker/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Kubernetes</title>
    <url>/2026/05/01/Cloud/Kubernetes/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Kubernetes</category>
      </categories>
      <tags>
        <tag>Kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>Bluetooth</title>
    <url>/2026/05/01/Embedded/Bluetooth/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>CAN</title>
    <url>/2026/05/01/Embedded/CAN/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>CoAP</title>
    <url>/2026/05/01/Embedded/CoAP/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>I2C</title>
    <url>/2026/05/01/Embedded/I2C/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>LIN</title>
    <url>/2026/05/01/Embedded/LIN/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>Modbus ASCII</title>
    <url>/2026/05/01/Embedded/ModbusASCII/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Modbus</category>
      </categories>
      <tags>
        <tag>Modbus</tag>
      </tags>
  </entry>
  <entry>
    <title>Modbus RTU</title>
    <url>/2026/05/01/Embedded/ModbusRTU/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Modbus</category>
      </categories>
      <tags>
        <tag>Modbus</tag>
      </tags>
  </entry>
  <entry>
    <title>LoRaWAN</title>
    <url>/2026/05/01/Embedded/LoRaWAN/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>Modbus TCP</title>
    <url>/2026/05/01/Embedded/ModbusTCP/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Modbus</category>
      </categories>
      <tags>
        <tag>Modbus</tag>
      </tags>
  </entry>
  <entry>
    <title>NFC</title>
    <url>/2026/05/01/Embedded/NFC/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>RS232</title>
    <url>/2026/05/01/Embedded/RS232/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>RS485</title>
    <url>/2026/05/01/Embedded/RS485/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>SPI</title>
    <url>/2026/05/01/Embedded/SPI/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>UART</title>
    <url>/2026/05/01/Embedded/UART/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>USB</title>
    <url>/2026/05/01/Embedded/USB/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>Zigbee</title>
    <url>/2026/05/01/Embedded/Zigbee/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Embedded</category>
      </categories>
      <tags>
        <tag>Embedded</tag>
      </tags>
  </entry>
  <entry>
    <title>FFmpeg</title>
    <url>/2026/05/01/FFmpeg/FFmpeg/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>FFmpeg</category>
      </categories>
      <tags>
        <tag>FFmpeg</tag>
      </tags>
  </entry>
  <entry>
    <title>VUE3</title>
    <url>/2026/05/01/Frontend/VUE3/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Frontend</category>
      </categories>
      <tags>
        <tag>VUE3</tag>
      </tags>
  </entry>
  <entry>
    <title>Vite</title>
    <url>/2026/05/01/Frontend/Vite/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Frontend</category>
      </categories>
      <tags>
        <tag>Vite</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang</title>
    <url>/2026/05/01/Golang/Golang/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Golang</category>
      </categories>
      <tags>
        <tag>Golang</tag>
      </tags>
  </entry>
  <entry>
    <title>GraphQL</title>
    <url>/2026/05/01/GraphQL/GraphQL/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>GraphQL</category>
      </categories>
      <tags>
        <tag>GraphQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Java全局异常处理</title>
    <url>/2026/05/04/Java/Java%E5%85%A8%E5%B1%80%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="定义业务异常类"><a href="#定义业务异常类" class="headerlink" title="定义业务异常类"></a>定义业务异常类</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 业务异常，由业务代码主动抛出，由全局异常处理器统一转换为 &#123;@code Result&#125;。</span><br><span class="line"> */</span><br><span class="line">public class BusinessException extends RuntimeException &#123;</span><br><span class="line"></span><br><span class="line">    private final Integer code;</span><br><span class="line"></span><br><span class="line">    public BusinessException(String message) &#123;</span><br><span class="line">        this(400, message);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public BusinessException(Integer code, String message) &#123;</span><br><span class="line">        super(message);</span><br><span class="line">        this.code = code;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public Integer getCode() &#123;</span><br><span class="line">        return code;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="添加全局异常处理类"><a href="#添加全局异常处理类" class="headerlink" title="添加全局异常处理类"></a>添加全局异常处理类</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">import jakarta.validation.ConstraintViolation;</span><br><span class="line">import jakarta.validation.ConstraintViolationException;</span><br><span class="line">import org.my919.springboot.controller.entity.Result;</span><br><span class="line">import org.slf4j.Logger;</span><br><span class="line">import org.slf4j.LoggerFactory;</span><br><span class="line">import org.springframework.http.HttpStatus;</span><br><span class="line">import org.springframework.http.ResponseEntity;</span><br><span class="line">import org.springframework.http.converter.HttpMessageNotReadableException;</span><br><span class="line">import org.springframework.validation.BindException;</span><br><span class="line">import org.springframework.web.HttpRequestMethodNotSupportedException;</span><br><span class="line">import org.springframework.web.bind.MethodArgumentNotValidException;</span><br><span class="line">import org.springframework.web.bind.MissingServletRequestParameterException;</span><br><span class="line">import org.springframework.web.bind.annotation.ExceptionHandler;</span><br><span class="line">import org.springframework.web.bind.annotation.RestControllerAdvice;</span><br><span class="line">import org.springframework.web.method.annotation.MethodArgumentTypeMismatchException;</span><br><span class="line"></span><br><span class="line">@RestControllerAdvice</span><br><span class="line">public class GlobalExceptionHandler &#123;</span><br><span class="line"></span><br><span class="line">    private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(BusinessException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleBusiness(BusinessException ex) &#123;</span><br><span class="line">        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(Result.fail(ex.getCode(), ex.getMessage()));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(MethodArgumentNotValidException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleMethodArgumentNotValid(MethodArgumentNotValidException ex) &#123;</span><br><span class="line">        String msg = ex.getBindingResult().getFieldErrors().stream()</span><br><span class="line">                .findFirst()</span><br><span class="line">                .map(err -&gt; err.getField() + &quot;: &quot; + err.getDefaultMessage())</span><br><span class="line">                .orElse(&quot;参数校验失败&quot;);</span><br><span class="line">        return ResponseEntity.badRequest().body(Result.fail(400, msg));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(ConstraintViolationException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleConstraintViolation(ConstraintViolationException ex) &#123;</span><br><span class="line">        String msg = ex.getConstraintViolations().stream()</span><br><span class="line">                .findFirst()</span><br><span class="line">                .map(ConstraintViolation::getMessage)</span><br><span class="line">                .orElse(&quot;参数约束校验失败&quot;);</span><br><span class="line">        return ResponseEntity.badRequest().body(Result.fail(400, msg));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(BindException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleBindException(BindException ex) &#123;</span><br><span class="line">        String msg = ex.getBindingResult().getFieldErrors().stream()</span><br><span class="line">                .findFirst()</span><br><span class="line">                .map(err -&gt; err.getField() + &quot;: &quot; + err.getDefaultMessage())</span><br><span class="line">                .orElse(&quot;参数绑定失败&quot;);</span><br><span class="line">        return ResponseEntity.badRequest().body(Result.fail(400, msg));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(&#123;</span><br><span class="line">            IllegalArgumentException.class,</span><br><span class="line">            MissingServletRequestParameterException.class,</span><br><span class="line">            MethodArgumentTypeMismatchException.class,</span><br><span class="line">    &#125;)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleBadRequest(Exception ex) &#123;</span><br><span class="line">        return ResponseEntity.badRequest().body(Result.fail(400, ex.getMessage()));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(HttpMessageNotReadableException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleHttpMessageNotReadable(HttpMessageNotReadableException ex) &#123;</span><br><span class="line">        return ResponseEntity.badRequest().body(Result.fail(400, &quot;请求体格式错误或无法解析&quot;));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(HttpRequestMethodNotSupportedException.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleMethodNotSupported(HttpRequestMethodNotSupportedException ex) &#123;</span><br><span class="line">        return ResponseEntity.status(HttpStatus.METHOD_NOT_ALLOWED)</span><br><span class="line">                .body(Result.fail(405, ex.getMessage()));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @ExceptionHandler(Exception.class)</span><br><span class="line">    public ResponseEntity&lt;Result&lt;Void&gt;&gt; handleUnhandled(Exception ex) &#123;</span><br><span class="line">        log.error(&quot;Unhandled exception&quot;, ex);</span><br><span class="line">        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)</span><br><span class="line">                .body(Result.fail(500, &quot;服务器内部错误&quot;));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java动态数据源</title>
    <url>/2026/05/04/Java/Java%E5%8A%A8%E6%80%81%E6%95%B0%E6%8D%AE%E6%BA%90/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>多数据源常见于：主从读写分离、按库分业务、迁移期新旧库并存等。<a href="https://baomidou.com/">Baomidou</a> 的 <strong>dynamic-datasource</strong> 在 Spring Boot 下用统一抽象管理多个 <code>DataSource</code>，通过注解或上下文切换当前线程使用的数据源，对 <strong>MyBatis-Plus</strong>、<strong>MyBatis</strong>、<strong>Spring Data JDBC</strong>、<strong>JdbcTemplate</strong> 等常见访问方式都适用。</p>
<p>下文以 <strong>Spring Boot 4</strong>、<strong>MyBatis-Plus 3.5.x</strong> 与 <strong>dynamic-datasource 4.x</strong> 为例（Boot 3 请改用 <code>dynamic-datasource-spring-boot3-starter</code>、<code>mybatis-plus-spring-boot3-starter</code> 坐标）。</p>
<h1 id="添加依赖"><a href="#添加依赖" class="headerlink" title="添加依赖"></a>添加依赖</h1><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">dependencies</span> &#123;</span><br><span class="line">    implementation <span class="string">&#x27;com.baomidou:dynamic-datasource-spring-boot4-starter:4.5.0&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;com.baomidou:mybatis-plus-spring-boot4-starter:3.5.15&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;org.springframework.boot:spring-boot-starter-jdbc&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;org.springframework.boot:spring-boot-starter-aspectj&#x27;</span></span><br><span class="line">    runtimeOnly <span class="string">&#x27;com.mysql:mysql-connector-j&#x27;</span></span><br><span class="line">    runtimeOnly <span class="string">&#x27;org.postgresql:postgresql&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><strong>切面</strong>：<code>@DS</code> 依赖 AOP，此处使用 <code>spring-boot-starter-aspectj</code>（与当前工程一致）。</li>
<li><strong>MyBatis-Plus</strong>：与 Boot 4 配套使用 <code>mybatis-plus-spring-boot4-starter</code>；Boot 3 使用 <code>mybatis-plus-spring-boot3-starter</code>。具体版本与 <a href="https://baomidou.com/en/getting-started/install/">MyBatis-Plus 安装说明</a> 及 <strong>JDK</strong> 要求一致即可。</li>
<li><strong>版本</strong>：其余坐标与 Spring Boot、<strong>JDK</strong> 对齐，以 <a href="https://central.sonatype.com/">Maven Central</a> 上各 artifact 说明为准。</li>
</ul>
<h1 id="Spring-Boot-配置"><a href="#Spring-Boot-配置" class="headerlink" title="Spring Boot 配置"></a>Spring Boot 配置</h1><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">springboot</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">dynamic:</span></span><br><span class="line">      <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">      <span class="attr">primary:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">datasource:</span></span><br><span class="line">        <span class="attr">mysql:</span></span><br><span class="line">          <span class="attr">url:</span> <span class="string">jdbc:mysql://192.168.1.22:30000/test?useUnicode=true&amp;characterEncoding=utf8&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true</span></span><br><span class="line">          <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">          <span class="attr">password:</span> <span class="string">Pi=3.1415</span></span><br><span class="line">          <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">        <span class="attr">pgsql:</span></span><br><span class="line">          <span class="attr">url:</span> <span class="string">jdbc:postgresql://192.168.1.22:30001/rbac</span></span><br><span class="line">          <span class="attr">username:</span> <span class="string">postgres</span></span><br><span class="line">          <span class="attr">password:</span> <span class="string">Pi=3.1415</span></span><br><span class="line">          <span class="attr">driver-class-name:</span> <span class="string">org.postgresql.Driver</span></span><br></pre></td></tr></table></figure>

<p>要点：</p>
<ul>
<li><strong><code>primary</code></strong>：未加 <code>@DS</code> 时默认使用的数据源逻辑名，需与 <code>datasource</code> 下某一键一致。</li>
<li><strong>逻辑名</strong>：<code>mysql</code>、<code>pgsql</code> 等为自定义别名，在 <code>@DS(&quot;pgsql&quot;)</code> 等处引用。</li>
</ul>
<h1 id="使用-DS-切换数据源"><a href="#使用-DS-切换数据源" class="headerlink" title="使用 @DS 切换数据源"></a>使用 @DS 切换数据源</h1><p>在 <strong>类</strong> 或 <strong>方法</strong> 上使用 <code>@DS(&quot;逻辑名&quot;)</code>；方法级优先于类级。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baomidou.dynamic.datasource.annotation.DS;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="meta">@DS(&quot;mysql&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">alwaysMysql</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 继承类上的 mysql</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@DS(&quot;pgsql&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">queryFromPg</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 本方法走 pgsql</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Controller</strong> 上也可标注，但更推荐标在 <strong>Service</strong> 或 <strong>Mapper</strong> 层，避免 Web 层与持久化策略绑死。</p>
<h1 id="编程式切换"><a href="#编程式切换" class="headerlink" title="编程式切换"></a>编程式切换</h1><p>需要按参数、分支在运行时决定库时，可使用上下文（用完建议清理，避免线程池复用线程时串库）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baomidou.dynamic.datasource.toolkit.DynamicDataSourceContextHolder;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">runOnPgThenClear</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        DynamicDataSourceContextHolder.push(<span class="string">&quot;pgsql&quot;</span>);</span><br><span class="line">        <span class="comment">// 此处执行的 SQL 走 pgsql</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        DynamicDataSourceContextHolder.poll();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>与 <code>@DS</code> 混用时，以官方文档当前版本行为为准；同一调用链内尽量只采用一种风格，降低心智负担。</p>
<h1 id="事务与边界情况"><a href="#事务与边界情况" class="headerlink" title="事务与边界情况"></a>事务与边界情况</h1><ul>
<li><strong>同一事务内</strong>：已进入 <code>@Transactional</code> 且已绑定连接后，再切换数据源通常<strong>不会</strong>按预期切到新库（连接已固定）。多数据源写操作应拆到不同事务方法，或调整事务边界，使切换发生在开启事务之前。</li>
<li><strong>只读事务</strong>：若使用 <code>@Transactional(readOnly = true)</code>，仍要注意连接从哪个数据源取得，与 <code>@DS</code> 所在方法的事务传播一致。</li>
<li><strong>自调用</strong>：同类内 <code>this.method()</code> 不会经过 Spring 代理，<code>@DS</code> 可能不生效，应抽到另一个 <strong>Bean</strong> 或通过代理调用。</li>
</ul>
<h1 id="与-MyBatis-Plus-MyBatis-的配合"><a href="#与-MyBatis-Plus-MyBatis-的配合" class="headerlink" title="与 MyBatis-Plus &#x2F; MyBatis 的配合"></a>与 MyBatis-Plus &#x2F; MyBatis 的配合</h1><p>引入 <strong>MyBatis-Plus</strong> 后，<strong>SqlSessionFactory</strong> 仍使用 Spring 容器中的动态 <code>DataSource</code>，与 <strong>dynamic-datasource</strong> 无额外冲突。<strong>Mapper</strong>（含继承 <code>BaseMapper</code> 的接口）或 <strong>XML</strong> 无需单独为多数据源改一套配置即可随当前线程数据源路由；若某几个 <strong>Mapper</strong> 固定走副库，可在 Mapper 接口上加 <code>@DS(&quot;pgsql&quot;)</code>。</p>
<h1 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h1><table>
<thead>
<tr>
<th>现象</th>
<th>可能原因</th>
</tr>
</thead>
<tbody><tr>
<td>始终走 <code>primary</code></td>
<td>未走到带 <code>@DS</code> 的代理方法；或自调用绕过代理</td>
</tr>
<tr>
<td>切换无效</td>
<td>已在事务中取连接；或 <code>push</code> 后未 <code>poll</code> 导致线程本地状态错乱</td>
</tr>
<tr>
<td>启动报错找不到驱动</td>
<td><code>driver-class-name</code> 与 <code>runtimeOnly</code> 驱动依赖不一致</td>
</tr>
<tr>
<td>Boot 版本不匹配</td>
<td>Boot 4 用 <code>spring-boot4-starter</code>，Boot 3 用 <code>spring-boot3-starter</code></td>
</tr>
</tbody></table>
<p>更细的读写分离、分片与 <strong>Seata</strong> 等组合方案见 <a href="https://www.baomidou.com/pages/a61e2b/">Baomidou 动态数据源文档</a>。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java泛型符号</title>
    <url>/2026/05/04/Java/Java%E6%B3%9B%E5%9E%8B%E7%AC%A6%E5%8F%B7/</url>
    <content><![CDATA[<h1 id="Java-泛型全套符号实战代码（T-E-K-V-U-extends-super）"><a href="#Java-泛型全套符号实战代码（T-E-K-V-U-extends-super）" class="headerlink" title="Java 泛型全套符号实战代码（T&#x2F;E&#x2F;K&#x2F;V&#x2F;U&#x2F;? extends&#x2F;? super）"></a>Java 泛型全套符号实战代码（T&#x2F;E&#x2F;K&#x2F;V&#x2F;U&#x2F;? extends&#x2F;? super）</h1><p>直接复制就能运行，全覆盖常用泛型符号。</p>
<h2 id="1-约定字母：T、E、K、V、U"><a href="#1-约定字母：T、E、K、V、U" class="headerlink" title="1. 约定字母：T、E、K、V、U"></a>1. 约定字母：T、E、K、V、U</h2><h3 id="1-1-T-通用类型（Type）"><a href="#1-1-T-通用类型（Type）" class="headerlink" title="1.1 T 通用类型（Type）"></a>1.1 T 通用类型（Type）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 单个泛型 T</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GenericDemo</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T value;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> T <span class="title function_">getValue</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setValue</span><span class="params">(T value)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.value = value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 测试</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Test</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        GenericDemo&lt;String&gt; t1 = <span class="keyword">new</span> <span class="title class_">GenericDemo</span>&lt;&gt;();</span><br><span class="line">        t1.setValue(<span class="string">&quot;我是T类型&quot;</span>);</span><br><span class="line">        System.out.println(t1.getValue());</span><br><span class="line"></span><br><span class="line">        GenericDemo&lt;Integer&gt; t2 = <span class="keyword">new</span> <span class="title class_">GenericDemo</span>&lt;&gt;();</span><br><span class="line">        t2.setValue(<span class="number">666</span>);</span><br><span class="line">        System.out.println(t2.getValue());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-2-E-集合元素（Element）"><a href="#1-2-E-集合元素（Element）" class="headerlink" title="1.2 E 集合元素（Element）"></a>1.2 E 集合元素（Element）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 模拟 List&lt;E&gt;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyList</span>&lt;E&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> E[] arr;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyList</span><span class="params">()</span> &#123;</span><br><span class="line">        arr = (E[]) <span class="keyword">new</span> <span class="title class_">Object</span>[<span class="number">10</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(<span class="type">int</span> index, E e)</span> &#123;</span><br><span class="line">        arr[index] = e;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> arr[index];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">MyList&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">MyList</span>&lt;&gt;();</span><br><span class="line">list.set(<span class="number">0</span>, <span class="string">&quot;张三&quot;</span>);</span><br><span class="line">System.out.println(list.get(<span class="number">0</span>));</span><br></pre></td></tr></table></figure>

<h3 id="1-3-K、V-键值对（Key-Value）"><a href="#1-3-K、V-键值对（Key-Value）" class="headerlink" title="1.3 K、V 键值对（Key &#x2F; Value）"></a>1.3 K、V 键值对（Key &#x2F; Value）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 模拟 Map&lt;K,V&gt;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyMap</span>&lt;K,V&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> K key;</span><br><span class="line">    <span class="keyword">private</span> V value;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">put</span><span class="params">(K key, V value)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.key = key;</span><br><span class="line">        <span class="built_in">this</span>.value = value;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> K <span class="title function_">getKey</span><span class="params">()</span> &#123; <span class="keyword">return</span> key; &#125;</span><br><span class="line">    <span class="keyword">public</span> V <span class="title function_">getValue</span><span class="params">()</span> &#123; <span class="keyword">return</span> value; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">MyMap&lt;String, Integer&gt; map = <span class="keyword">new</span> <span class="title class_">MyMap</span>&lt;&gt;();</span><br><span class="line">map.put(<span class="string">&quot;年龄&quot;</span>, <span class="number">20</span>);</span><br><span class="line">System.out.println(map.getKey() + <span class="string">&quot;：&quot;</span> + map.getValue());</span><br></pre></td></tr></table></figure>

<h3 id="1-4-T、U-双泛型"><a href="#1-4-T、U-双泛型" class="headerlink" title="1.4 T、U 双泛型"></a>1.4 T、U 双泛型</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Pair</span>&lt;T,U&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T first;</span><br><span class="line">    <span class="keyword">private</span> U second;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Pair</span><span class="params">(T first, U second)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.first = first;</span><br><span class="line">        <span class="built_in">this</span>.second = second;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> T <span class="title function_">getFirst</span><span class="params">()</span> &#123; <span class="keyword">return</span> first; &#125;</span><br><span class="line">    <span class="keyword">public</span> U <span class="title function_">getSecond</span><span class="params">()</span> &#123; <span class="keyword">return</span> second; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">Pair&lt;String, Integer&gt; pair = <span class="keyword">new</span> <span class="title class_">Pair</span>&lt;&gt;(<span class="string">&quot;分数&quot;</span>, <span class="number">99</span>);</span><br><span class="line">System.out.println(pair.getFirst() + <span class="string">&quot; &quot;</span> + pair.getSecond());</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-通配符-全套"><a href="#2-通配符-全套" class="headerlink" title="2. 通配符 ? 全套"></a>2. 通配符 ? 全套</h2><h3 id="2-1-无界通配符"><a href="#2-1-无界通配符" class="headerlink" title="2.1 无界通配符 ?"></a>2.1 无界通配符 <code>?</code></h3><p>任意类型集合，<strong>只读</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WildcardTest</span> &#123;</span><br><span class="line">    <span class="comment">// List&lt;?&gt; 能接收任意类型List</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">printList</span><span class="params">(List&lt;?&gt; list)</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (Object o : list) &#123;</span><br><span class="line">            System.out.print(o + <span class="string">&quot; &quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        printList(Arrays.asList(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>));</span><br><span class="line">        printList(Arrays.asList(<span class="string">&quot;a&quot;</span>,<span class="string">&quot;b&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-2-上界通配符-extends-父类"><a href="#2-2-上界通配符-extends-父类" class="headerlink" title="2.2 上界通配符 ? extends 父类"></a>2.2 上界通配符 <code>? extends 父类</code></h3><p>只能是<strong>本类 + 子类</strong>，只读</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 只能 Number 及其子类：Integer Long Double</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">showNumber</span><span class="params">(List&lt;? extends Number&gt; list)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (Number n : list) &#123;</span><br><span class="line">        System.out.print(n + <span class="string">&quot; &quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line">showNumber(Arrays.asList(<span class="number">11</span>,<span class="number">22</span>,<span class="number">33</span>));</span><br><span class="line">showNumber(Arrays.asList(<span class="number">1.1</span>,<span class="number">2.2</span>));</span><br></pre></td></tr></table></figure>

<h3 id="2-3-下界通配符-super-子类"><a href="#2-3-下界通配符-super-子类" class="headerlink" title="2.3 下界通配符 ? super 子类"></a>2.3 下界通配符 <code>? super 子类</code></h3><p>只能是<strong>本类 + 父类</strong>，可写</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 只能 Integer 及其父类：Number、Object</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">addInt</span><span class="params">(List&lt;? <span class="built_in">super</span> Integer&gt; list)</span> &#123;</span><br><span class="line">    list.add(<span class="number">100</span>);</span><br><span class="line">    list.add(<span class="number">200</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line">List&lt;Number&gt; numList = Arrays.asList();</span><br><span class="line">addInt(numList);</span><br><span class="line">System.out.println(numList);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-泛型方法"><a href="#3-泛型方法" class="headerlink" title="3. 泛型方法 "></a>3. 泛型方法 <T></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 泛型静态方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; T <span class="title function_">getSame</span><span class="params">(T t)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line"><span class="type">String</span> <span class="variable">s</span> <span class="operator">=</span> getSame(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line"><span class="type">Integer</span> <span class="variable">n</span> <span class="operator">=</span> getSame(<span class="number">888</span>);</span><br><span class="line">System.out.println(s + <span class="string">&quot; &quot;</span> + n);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-泛型多接口限定"><a href="#4-泛型多接口限定" class="headerlink" title="4. 泛型多接口限定 &amp;"></a>4. 泛型多接口限定 <code>&amp;</code></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">A</span> &#123;&#125;</span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">B</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// T 必须同时实现 A 和 B</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span>&lt;T <span class="keyword">extends</span> <span class="title class_">A</span> &amp; B&gt; &#123;&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="速记口诀"><a href="#速记口诀" class="headerlink" title="速记口诀"></a>速记口诀</h1><ul>
<li><strong>T</strong> 通用任意类型</li>
<li><strong>E</strong> 集合元素</li>
<li><strong>K&#x2F;V</strong> 键值对</li>
<li><strong>U</strong> 第二个泛型</li>
<li><strong>?</strong> 任意未知类型</li>
<li><strong>? extends</strong> 上限（子类可读）</li>
<li><strong>? super</strong> 下限（父类可写）</li>
<li><code>&lt;T&gt;</code> 定义泛型方法&#x2F;类</li>
</ul>
<h1 id="接口统一返回类"><a href="#接口统一返回类" class="headerlink" title="接口统一返回类"></a>接口统一返回类</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">import lombok.Data;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 全局统一返回结果泛型</span><br><span class="line"> * @param &lt;T&gt; 响应数据泛型</span><br><span class="line"> */</span><br><span class="line">@Data</span><br><span class="line">public class Result&lt;T&gt; &#123;</span><br><span class="line"></span><br><span class="line">    // 响应码</span><br><span class="line">    private Integer code;</span><br><span class="line">    // 提示消息</span><br><span class="line">    private String msg;</span><br><span class="line">    // 泛型数据</span><br><span class="line">    private T data;</span><br><span class="line"></span><br><span class="line">    // 成功静态方法</span><br><span class="line">    public static &lt;T&gt; Result&lt;T&gt; success() &#123;</span><br><span class="line">        Result&lt;T&gt; r = new Result&lt;&gt;();</span><br><span class="line">        r.setCode(200);</span><br><span class="line">        r.setMsg(&quot;success&quot;);</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static &lt;T&gt; Result&lt;T&gt; success(T data) &#123;</span><br><span class="line">        Result&lt;T&gt; r = new Result&lt;&gt;();</span><br><span class="line">        r.setCode(200);</span><br><span class="line">        r.setMsg(&quot;success&quot;);</span><br><span class="line">        r.setData(data);</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 失败静态方法</span><br><span class="line">    public static &lt;T&gt; Result&lt;T&gt; fail() &#123;</span><br><span class="line">        Result&lt;T&gt; r = new Result&lt;&gt;();</span><br><span class="line">        r.setCode(500);</span><br><span class="line">        r.setMsg(&quot;fail&quot;);</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static &lt;T&gt; Result&lt;T&gt; fail(Integer code, String msg) &#123;</span><br><span class="line">        Result&lt;T&gt; r = new Result&lt;&gt;();</span><br><span class="line">        r.setCode(code);</span><br><span class="line">        r.setMsg(msg);</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis-Flex</title>
    <url>/2026/05/04/Java/MyBatis-Flex/</url>
    <content><![CDATA[<h1 id="添加依赖"><a href="#添加依赖" class="headerlink" title="添加依赖"></a>添加依赖</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">implementation &#x27;com.mybatis-flex:mybatis-flex-spring-boot4-starter:1.11.7&#x27;</span><br><span class="line">annotationProcessor &#x27;com.mybatis-flex:mybatis-flex-processor:1.11.7&#x27;</span><br></pre></td></tr></table></figure>

<h1 id="springboot-配置"><a href="#springboot-配置" class="headerlink" title="springboot 配置"></a>springboot 配置</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mybatis-flex:</span><br><span class="line">  mapper-locations: classpath*:org/my919/springboot/mapper/**/*.xml</span><br><span class="line">  type-aliases-package: org.my919.springboot.domain</span><br><span class="line">  configuration:</span><br><span class="line">    map-underscore-to-camel-case: true</span><br><span class="line">    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl</span><br></pre></td></tr></table></figure>

<h1 id="添加-MapperScan-“org-my919-springboot-mapper”"><a href="#添加-MapperScan-“org-my919-springboot-mapper”" class="headerlink" title="添加@MapperScan(“org.my919.springboot.mapper”)"></a>添加@MapperScan(“org.my919.springboot.mapper”)</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import org.mybatis.spring.annotation.MapperScan;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">@MapperScan(&quot;org.my919.springboot.mapper&quot;) // 推荐，使用此注解，不用每个mapper上加@Mapper</span><br><span class="line">public class MapperScanConfiguration &#123;&#125;</span><br></pre></td></tr></table></figure>

<h1 id="插件生成代码"><a href="#插件生成代码" class="headerlink" title="插件生成代码"></a>插件生成代码</h1><ul>
<li>idea 插件安装MyatisX-Generator</li>
<li>idea 登录数据库</li>
<li>在表上右键-MyatisX-Generator 自动生成CRUD代码</li>
</ul>
<h1 id="MyBatis-Flex-分页接口"><a href="#MyBatis-Flex-分页接口" class="headerlink" title="MyBatis-Flex 分页接口"></a>MyBatis-Flex 分页接口</h1><ul>
<li>UsersService</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Page&lt;Users&gt; page(long pageNum, long pageSize);</span><br></pre></td></tr></table></figure>
<ul>
<li>UsersServiceImpl</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"># 添加@MapperScan(&quot;org.my919.springboot.mapper&quot;) </span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>import org.mybatis.spring.annotation.MapperScan;<br>import org.springframework.context.annotation.Configuration;</p>
<p>@Configuration<br>@MapperScan(“org.my919.springboot.mapper”) &#x2F;&#x2F; 推荐，使用此注解，不用每个mapper上加@Mapper<br>public class MapperScanConfiguration {}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"># 添加分页插件配置</span><br><span class="line"></span><br><span class="line">```java</span><br><span class="line">import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;</span><br><span class="line">import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;</span><br><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">public class MybatisPlusPaginationConfig &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public MybatisPlusInterceptor mybatisPlusInterceptor() &#123;</span><br><span class="line">        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();</span><br><span class="line">        PaginationInnerInterceptor pagination = new PaginationInnerInterceptor();</span><br><span class="line">        pagination.setOverflow(true);</span><br><span class="line">        interceptor.addInnerInterceptor(pagination);</span><br><span class="line">        return interceptor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h1 id="插件生成代码-1"><a href="#插件生成代码-1" class="headerlink" title="插件生成代码"></a>插件生成代码</h1><ul>
<li>idea 插件安装MyatisX-Generator</li>
<li>idea 登录数据库</li>
<li>在表上右键-MyatisX-Generator 自动生成CRUD代码</li>
</ul>
<h1 id="MyBatis-Flex-分页接口-1"><a href="#MyBatis-Flex-分页接口-1" class="headerlink" title="MyBatis-Flex 分页接口"></a>MyBatis-Flex 分页接口</h1><ul>
<li>UsersService</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Page&lt;Users&gt; page(long pageNum, long pageSize);</span><br></pre></td></tr></table></figure>

<ul>
<li>UsersServiceImpl</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.mybatisflex.core.paginate.Page;</span><br><span class="line"><span class="keyword">import</span> com.mybatisflex.core.query.QueryWrapper;</span><br><span class="line"><span class="keyword">import</span> com.mybatisflex.spring.service.impl.ServiceImpl;</span><br><span class="line"><span class="keyword">import</span> org.my919.springboot.domain.Users;</span><br><span class="line"><span class="keyword">import</span> org.my919.springboot.mapper.UsersMapper;</span><br><span class="line"><span class="keyword">import</span> org.my919.springboot.service.UsersService;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UsersServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UsersMapper, Users&gt;</span><br><span class="line">    <span class="keyword">implements</span> <span class="title class_">UsersService</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Page&lt;Users&gt; <span class="title function_">page</span><span class="params">(<span class="type">long</span> pageNum, <span class="type">long</span> pageSize)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> page(Page.of(pageNum, pageSize), QueryWrapper.create());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="MyBatis-Flex-分页原理（通俗易懂-流程拆解-和-MP-对比）"><a href="#MyBatis-Flex-分页原理（通俗易懂-流程拆解-和-MP-对比）" class="headerlink" title="MyBatis-Flex 分页原理（通俗易懂 + 流程拆解 + 和 MP 对比）"></a>MyBatis-Flex 分页原理（通俗易懂 + 流程拆解 + 和 MP 对比）</h1><h2 id="一、核心结论"><a href="#一、核心结论" class="headerlink" title="一、核心结论"></a>一、核心结论</h2><p>MyBatis-Flex 底层<strong>也是基于 MyBatis 拦截器</strong>，原理和 MyBatis-Plus <strong>大体一样</strong>，但<strong>设计更轻、SQL 解析更强、自动 count 更智能</strong>。</p>
<p>核心逻辑：<br><strong>MyBatis 拦截器 → 拦截查询SQL → 自动生成 count 总数 → 原SQL 拼接 LIMIT → 封装分页结果</strong></p>
<hr>
<h2 id="二、核心组件"><a href="#二、核心组件" class="headerlink" title="二、核心组件"></a>二、核心组件</h2><ol>
<li><strong>MyBatisFlexInterceptor</strong><br>Flex 全局核心拦截器，替代 MP 的 <code>MybatisPlusInterceptor</code></li>
<li><strong>PageInterceptor</strong><br>专门负责<strong>分页拦截</strong>，Flex 分页核心逻辑都在这</li>
<li><strong>Page、IPage</strong><br>分页载体，和 MP 几乎一模一样：当前页、页大小、总条数、分页数据</li>
</ol>
<hr>
<h2 id="三、完整分页执行流程（流程图版）"><a href="#三、完整分页执行流程（流程图版）" class="headerlink" title="三、完整分页执行流程（流程图版）"></a>三、完整分页执行流程（流程图版）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">业务代码：new Page&lt;&gt;(pageNum, pageSize)</span><br><span class="line">          ↓</span><br><span class="line">调用 BaseMapper / Db 工具类 selectPage</span><br><span class="line">          ↓</span><br><span class="line">进入 MyBatis 执行SQL流程</span><br><span class="line">          ↓</span><br><span class="line">MyBatisFlexInterceptor 拦截</span><br><span class="line">          ↓</span><br><span class="line">匹配内部 PageInterceptor 分页拦截器</span><br><span class="line">          ↓</span><br><span class="line">检测方法参数是否有 Page/IPage 对象</span><br><span class="line">   ├─ 无 → 正常执行原SQL，不分页</span><br><span class="line">   └─ 有 → 进入Flex分页逻辑</span><br><span class="line">          ↓</span><br><span class="line">1. 解析原始查询 SQL</span><br><span class="line">          ↓</span><br><span class="line">2. 智能生成 count 统计SQL</span><br><span class="line">   （自动去 order by、优化子查询、避免重复排序）</span><br><span class="line">          ↓</span><br><span class="line">3. 执行 count SQL → 得到总记录数 total</span><br><span class="line">          ↓</span><br><span class="line">4. 给原SQL 自动拼接 LIMIT ?,?</span><br><span class="line">          ↓</span><br><span class="line">5. 执行分页SQL → 拿到当前页数据列表</span><br><span class="line">          ↓</span><br><span class="line">6. 计算总页数、上下页，封装到 Page 对象</span><br><span class="line">          ↓</span><br><span class="line">返回 IPage 分页结果</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、和-MyBatis-Plus-分页原理关键区别"><a href="#四、和-MyBatis-Plus-分页原理关键区别" class="headerlink" title="四、和 MyBatis-Plus 分页原理关键区别"></a>四、和 MyBatis-Plus 分页原理<strong>关键区别</strong></h2><table>
<thead>
<tr>
<th>对比点</th>
<th>MyBatis-Plus</th>
<th>MyBatis-Flex</th>
</tr>
</thead>
<tbody><tr>
<td>拦截器结构</td>
<td>插件嵌套式，较重</td>
<td>极简拦截器，更轻量</td>
</tr>
<tr>
<td>Count SQL 生成</td>
<td>简单直接，复杂SQL容易生成低效count</td>
<td><strong>智能优化</strong>：自动去掉 order by、优化嵌套SQL</td>
</tr>
<tr>
<td>多表&#x2F;复杂SQL</td>
<td>偶尔分页解析失败</td>
<td>解析器更强，兼容复杂联表、子查询</td>
</tr>
<tr>
<td>分页参数传递</td>
<td>必须 Mapper 方法带 Page 参数</td>
<td>支持 Mapper、也支持 Db 静态工具直接分页</td>
</tr>
<tr>
<td>配置</td>
<td>必须手动注册分页插件</td>
<td>自动配置，<strong>开箱即用</strong>，不用手动@Bean</td>
</tr>
</tbody></table>
<hr>
<h2 id="五、代码使用（和-MP-几乎一模一样）"><a href="#五、代码使用（和-MP-几乎一模一样）" class="headerlink" title="五、代码使用（和 MP 几乎一模一样）"></a>五、代码使用（和 MP 几乎一模一样）</h2><h3 id="1-无需手动配置分页插件"><a href="#1-无需手动配置分页插件" class="headerlink" title="1. 无需手动配置分页插件"></a>1. 无需手动配置分页插件</h3><p>Flex 自动自动注入分页拦截器，<strong>不用写任何 @Bean 配置</strong></p>
<h3 id="2-业务分页代码"><a href="#2-业务分页代码" class="headerlink" title="2. 业务分页代码"></a>2. 业务分页代码</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 当前页、每页条数</span></span><br><span class="line">Page&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">1</span>, <span class="number">10</span>);</span><br><span class="line"><span class="comment">// 分页查询</span></span><br><span class="line">Page&lt;User&gt; result = userMapper.selectPage(page, QueryWrapper.create());</span><br></pre></td></tr></table></figure>
<p>写法和 MP <strong>完全兼容，无缝切换</strong>。</p>
<hr>
<h2 id="六、底层关键亮点（Flex-比-MP-强的地方）"><a href="#六、底层关键亮点（Flex-比-MP-强的地方）" class="headerlink" title="六、底层关键亮点（Flex 比 MP 强的地方）"></a>六、底层关键亮点（Flex 比 MP 强的地方）</h2><ol>
<li><strong>自动优化 Count SQL</strong><br>原SQL 有 <code>order by</code>，Flex 会自动剔除，避免 count 也排序，提升性能。</li>
<li><strong>无需手动配置拦截器</strong><br>自动装配，少写配置类，少踩坑。</li>
<li><strong>支持更多数据库方言</strong><br>MySQL、PostgreSQL、Oracle、SQLite 等自动适配 LIMIT 语法。</li>
<li><strong>Lambda 分页更丝滑</strong><br>不用写 Wrapper，链式直接分页。</li>
</ol>
<hr>
<h2 id="七、一句话总结"><a href="#七、一句话总结" class="headerlink" title="七、一句话总结"></a>七、一句话总结</h2><p>MyBatis-Flex 分页原理：<br><strong>复用 MyBatis 拦截机制 → 拦截SQL → 智能生成count → 自动加LIMIT → 封装分页</strong><br>和 MP 原理同源，但<strong>解析更强、配置更少、性能更好、坑更少</strong>。</p>
<h1 id="MyBatis-Plus-迁移-MyBatis-Flex-极简步骤（5分钟搞定，无痛迁移）"><a href="#MyBatis-Plus-迁移-MyBatis-Flex-极简步骤（5分钟搞定，无痛迁移）" class="headerlink" title="MyBatis-Plus 迁移 MyBatis-Flex 极简步骤（5分钟搞定，无痛迁移）"></a>MyBatis-Plus 迁移 MyBatis-Flex 极简步骤（5分钟搞定，无痛迁移）</h1><h2 id="一、核心优势"><a href="#一、核心优势" class="headerlink" title="一、核心优势"></a>一、核心优势</h2><ul>
<li>实体、Mapper、分页、Wrapper <strong>写法几乎不变</strong></li>
<li>不用改业务代码、不用改SQL</li>
<li>自动配置，删掉 MP 拦截器配置即可</li>
</ul>
<hr>
<h1 id="第一步：移除-MyBatis-Plus-依赖"><a href="#第一步：移除-MyBatis-Plus-依赖" class="headerlink" title="第一步：移除 MyBatis-Plus 依赖"></a>第一步：移除 MyBatis-Plus 依赖</h1><p>pom.xml 删掉所有 MP 相关：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 删掉这两个 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- mybatis-plus-spring-boot4-starter --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 任何 mp 相关依赖全部删掉 --&gt;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="第二步：引入-MyBatis-Flex-依赖"><a href="#第二步：引入-MyBatis-Flex-依赖" class="headerlink" title="第二步：引入 MyBatis-Flex 依赖"></a>第二步：引入 MyBatis-Flex 依赖</h1><p>SpringBoot4 适配版：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">implementation &#x27;com.mybatis-flex:mybatis-flex-spring-boot4-starter:1.11.7&#x27;</span><br><span class="line">annotationProcessor &#x27;com.mybatis-flex:mybatis-flex-processor:1.11.7&#x27;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="第三步：删除-MP-专属配置-配置类"><a href="#第三步：删除-MP-专属配置-配置类" class="headerlink" title="第三步：删除 MP 专属配置 &amp; 配置类"></a>第三步：删除 MP 专属配置 &amp; 配置类</h1><ol>
<li><strong>删掉 MP 分页插件配置类</strong><br>删掉这个 <code>@Bean</code>：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 删掉！PaginationInnerInterceptor、MybatisPlusInterceptor 全部删掉</span></span><br></pre></td></tr></table></figure></li>
<li><strong>yml 里删掉 mybatis-plus 所有配置</strong><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 删掉</span></span><br><span class="line"><span class="attr">mybatis-plus:</span></span><br><span class="line">  <span class="attr">mapper-locations:</span> <span class="string">...</span></span><br><span class="line">  <span class="attr">configuration:</span> <span class="string">...</span></span><br></pre></td></tr></table></figure></li>
<li>换成 <strong>原生 MyBatis 配置</strong></li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">mybatis:</span></span><br><span class="line">  <span class="attr">mapper-locations:</span> <span class="string">classpath:/mapper/**/*.xml</span></span><br><span class="line">  <span class="attr">configuration:</span></span><br><span class="line">    <span class="attr">map-underscore-to-camel-case:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="第四步：实体类注解批量替换（最关键）"><a href="#第四步：实体类注解批量替换（最关键）" class="headerlink" title="第四步：实体类注解批量替换（最关键）"></a>第四步：实体类注解批量替换（最关键）</h1><table>
<thead>
<tr>
<th>MyBatis-Plus 注解</th>
<th>MyBatis-Flex 替换</th>
</tr>
</thead>
<tbody><tr>
<td><code>@TableName</code></td>
<td><code>@Table</code></td>
</tr>
<tr>
<td><code>@TableId</code></td>
<td><code>@Id</code></td>
</tr>
<tr>
<td><code>@TableField</code></td>
<td><code>@Column</code></td>
</tr>
<tr>
<td><code>@LogicDelete</code></td>
<td><code>@LogicDelete</code>（包变了，导新包）</td>
</tr>
<tr>
<td><code>@TableLogic</code></td>
<td><code>@LogicDelete</code></td>
</tr>
</tbody></table>
<p>示例改造前(MP)：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@TableName(&quot;t_user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="meta">@TableId(type = IdType.AUTO)</span></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableField(&quot;user_name&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String userName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>改造后(Flex)：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.mybatis-flex.annotation.Table;</span><br><span class="line"><span class="keyword">import</span> com.mybatis-flex.annotation.Id;</span><br><span class="line"><span class="keyword">import</span> com.mybatis-flex.annotation.Column;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Table(&quot;t_user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="meta">@Id(keyType = KeyType.Auto)</span></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Column(&quot;user_name&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String userName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="第五步：Mapper-接口替换"><a href="#第五步：Mapper-接口替换" class="headerlink" title="第五步：Mapper 接口替换"></a>第五步：Mapper 接口替换</h1><h3 id="MP-写法"><a href="#MP-写法" class="headerlink" title="MP 写法"></a>MP 写法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baomidou.mybatisplus.core.mapper.BaseMapper;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserMapper</span> <span class="keyword">extends</span> <span class="title class_">BaseMapper</span>&lt;User&gt; &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Flex-写法"><a href="#Flex-写法" class="headerlink" title="Flex 写法"></a>Flex 写法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.mybatis-flex.core.BaseMapper;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserMapper</span> <span class="keyword">extends</span> <span class="title class_">BaseMapper</span>&lt;User&gt; &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>方法名几乎一模一样</strong>：<br><code>selectPage</code>、<code>selectById</code>、<code>insert</code>、<code>update</code>、<code>delete</code> 全都不用改业务代码。</p>
<hr>
<h1 id="第六步：分页代码-不用改！"><a href="#第六步：分页代码-不用改！" class="headerlink" title="第六步：分页代码 不用改！"></a>第六步：分页代码 不用改！</h1><p>MP 原有分页代码 <strong>直接能用</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 完全不用改</span></span><br><span class="line">Page&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">1</span>,<span class="number">10</span>);</span><br><span class="line">IPage&lt;User&gt; pageData = userMapper.selectPage(page, wrapper);</span><br></pre></td></tr></table></figure>
<p>只需要<strong>换一下 Page、IPage 导入包</strong>就行。</p>
<hr>
<h1 id="第七步：-MapperScan-保留不动"><a href="#第七步：-MapperScan-保留不动" class="headerlink" title="第七步：@MapperScan 保留不动"></a>第七步：@MapperScan 保留不动</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@MapperScan(&quot;org.my919.springboot.mapper&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>完全兼容，不用改。</p>
<hr>
<h1 id="迁移总结一句话"><a href="#迁移总结一句话" class="headerlink" title="迁移总结一句话"></a>迁移总结一句话</h1><ol>
<li>删 MP 依赖 → 加 Flex 依赖</li>
<li>删 MP 配置类 + yml 配置</li>
<li>实体注解、BaseMapper 换包</li>
<li>业务 CRUD、分页代码 <strong>零改动</strong></li>
</ol>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis-Plus</title>
    <url>/2026/05/01/Java/MyBatis/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><h1 id="添加依赖"><a href="#添加依赖" class="headerlink" title="添加依赖"></a>添加依赖</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">implementation &#x27;com.baomidou:mybatis-plus-spring-boot4-starter:3.5.15&#x27;</span><br><span class="line">implementation &#x27;com.baomidou:mybatis-plus-jsqlparser:3.5.15&#x27;</span><br></pre></td></tr></table></figure>

<h1 id="springboot配置"><a href="#springboot配置" class="headerlink" title="springboot配置"></a>springboot配置</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mybatis-plus:</span><br><span class="line">  mapper-locations: classpath*:org/my919/springboot/mapper/**/*.xml</span><br><span class="line">  type-aliases-package: org.my919.springboot.domain</span><br><span class="line">  configuration:</span><br><span class="line">    map-underscore-to-camel-case: true</span><br><span class="line">    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl</span><br></pre></td></tr></table></figure>
<h1 id="添加-MapperScan-“org-my919-springboot-mapper”"><a href="#添加-MapperScan-“org-my919-springboot-mapper”" class="headerlink" title="添加@MapperScan(“org.my919.springboot.mapper”)"></a>添加@MapperScan(“org.my919.springboot.mapper”)</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import org.mybatis.spring.annotation.MapperScan;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">@MapperScan(&quot;org.my919.springboot.mapper&quot;) // 推荐，使用此注解，不用每个mapper上加@Mapper</span><br><span class="line">public class MapperScanConfiguration &#123;&#125;</span><br></pre></td></tr></table></figure>
<h1 id="添加分页插件配置"><a href="#添加分页插件配置" class="headerlink" title="添加分页插件配置"></a>添加分页插件配置</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;</span><br><span class="line">import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;</span><br><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line">@Configuration</span><br><span class="line">public class MybatisPlusPaginationConfig &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public MybatisPlusInterceptor mybatisPlusInterceptor() &#123;</span><br><span class="line">        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();</span><br><span class="line">        PaginationInnerInterceptor pagination = new PaginationInnerInterceptor();</span><br><span class="line">        pagination.setOverflow(true);</span><br><span class="line">        interceptor.addInnerInterceptor(pagination);</span><br><span class="line">        return interceptor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h1 id="插件生成代码"><a href="#插件生成代码" class="headerlink" title="插件生成代码"></a>插件生成代码</h1><ul>
<li>idea 插件安装MyatisX-Generator</li>
<li>idea 登录数据库</li>
<li>在表上右键-MyatisX-Generator 自动生成CRUD代码</li>
</ul>
<h1 id="MyBatis-Plus-分页接口"><a href="#MyBatis-Plus-分页接口" class="headerlink" title="MyBatis-Plus 分页接口"></a>MyBatis-Plus 分页接口</h1><ul>
<li>UsersService</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Page&lt;Users&gt; page(long pageNum, long pageSize);</span><br></pre></td></tr></table></figure>
<ul>
<li>UsersServiceImpl</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> public Page&lt;Users&gt; page(long pageNum, long pageSize) &#123;</span><br><span class="line">    Page&lt;Users&gt; p = new Page&lt;&gt;(pageNum, pageSize);</span><br><span class="line">    usersMapper.selectPage(p, Wrappers.emptyWrapper());</span><br><span class="line">    return p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="MyBatis-Plus-分页原理执行流程图"><a href="#MyBatis-Plus-分页原理执行流程图" class="headerlink" title="MyBatis-Plus 分页原理执行流程图"></a>MyBatis-Plus 分页原理执行流程图</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">【开发者业务代码】</span><br><span class="line">        ↓</span><br><span class="line">new Page&lt;&gt;(pageNum, pageSize)  // 传入分页参数</span><br><span class="line">        ↓</span><br><span class="line">userMapper.selectPage(Page, QueryWrapper)</span><br><span class="line">        ↓</span><br><span class="line">【MyBatis 拦截器入口】</span><br><span class="line">        ↓</span><br><span class="line">MybatisPlusInterceptor 全局拦截</span><br><span class="line">        ↓</span><br><span class="line">匹配内部插件 → PaginationInnerInterceptor（分页核心拦截器）</span><br><span class="line">        ↓</span><br><span class="line">判断：方法参数是否包含 IPage/Page 对象？</span><br><span class="line">   ├─ 否 → 正常执行原SQL，不分页</span><br><span class="line">   └─ 是 → 进入MP分页逻辑</span><br><span class="line">          ↓</span><br><span class="line">1. 自动解析原始查询 SQL</span><br><span class="line">          ↓</span><br><span class="line">2. 自动生成 Count 统计SQL（select count(*) ...）</span><br><span class="line">          ↓</span><br><span class="line">3. 执行 Count SQL → 获取【总记录数 total】</span><br><span class="line">          ↓</span><br><span class="line">4. 自动改写原始SQL → 末尾拼接 LIMIT ?,?</span><br><span class="line">          ↓</span><br><span class="line">5. 执行 分页Limit SQL → 获取当前页数据列表</span><br><span class="line">          ↓</span><br><span class="line">6. 自动计算：总页数、当前页、是否有上/下一页</span><br><span class="line">          ↓</span><br><span class="line">7. 把 数据列表+分页信息 封装进 IPage 对象</span><br><span class="line">          ↓</span><br><span class="line">【返回给 Service/Controller 层】</span><br></pre></td></tr></table></figure>

<h2 id="MyBatis-Plus-分页原理"><a href="#MyBatis-Plus-分页原理" class="headerlink" title="MyBatis-Plus 分页原理"></a>MyBatis-Plus 分页原理</h2><h3 id="一、核心一句话"><a href="#一、核心一句话" class="headerlink" title="一、核心一句话"></a>一、核心一句话</h3><p>MP 分页本质：<strong>MyBatis 拦截器 + 自动改写 SQL</strong></p>
<ol>
<li>你传入 <code>Page&lt;&gt;(当前页,每页条数)</code></li>
<li>MP 分页拦截器拦截 Mapper 查询方法</li>
<li><strong>自动帮你生成 count 总数SQL + 拼接 limit 分页SQL</strong></li>
<li>执行两条 SQL：<strong>查总数 → 查当前页数据</strong></li>
<li>把总数、页码、数据封装到 <code>IPage</code> 返回</li>
</ol>
<hr>
<h3 id="二、关键核心组件"><a href="#二、关键核心组件" class="headerlink" title="二、关键核心组件"></a>二、关键核心组件</h3><ol>
<li><strong>MybatisPlusInterceptor</strong><br>MP 核心插件容器，所有插件（分页、防全表更新、乐观锁）都往里注册。</li>
<li><strong>PaginationInnerInterceptor</strong><br><strong>MP 真正的分页拦截器</strong>，核心逻辑全在这。</li>
<li><strong>Page &#x2F; IPage</strong><br>分页参数载体，存：当前页、每页条数、总记录数、总页数、分页数据。</li>
</ol>
<hr>
<h3 id="三、完整执行流程（一步一步走）"><a href="#三、完整执行流程（一步一步走）" class="headerlink" title="三、完整执行流程（一步一步走）"></a>三、完整执行流程（一步一步走）</h3><h4 id="1-配置分页拦截器（必须配置，否则分页无效）"><a href="#1-配置分页拦截器（必须配置，否则分页无效）" class="headerlink" title="1. 配置分页拦截器（必须配置，否则分页无效）"></a>1. 配置分页拦截器（必须配置，否则分页无效）</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> MybatisPlusInterceptor <span class="title function_">mybatisPlusInterceptor</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">MybatisPlusInterceptor</span> <span class="variable">interceptor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MybatisPlusInterceptor</span>();</span><br><span class="line">    <span class="comment">// 加入分页拦截器，指定数据库方言</span></span><br><span class="line">    interceptor.addInnerInterceptor(<span class="keyword">new</span> <span class="title class_">PaginationInnerInterceptor</span>(DbType.MYSQL));</span><br><span class="line">    <span class="keyword">return</span> interceptor;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-业务代码使用"><a href="#2-业务代码使用" class="headerlink" title="2. 业务代码使用"></a>2. 业务代码使用</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第几页、每页几条</span></span><br><span class="line">IPage&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">1</span>, <span class="number">10</span>);</span><br><span class="line"><span class="comment">// MP 会自动分页</span></span><br><span class="line">IPage&lt;User&gt; resultPage = userMapper.selectPage(page, <span class="literal">null</span>);</span><br></pre></td></tr></table></figure>

<h4 id="3-底层执行步骤"><a href="#3-底层执行步骤" class="headerlink" title="3. 底层执行步骤"></a>3. 底层执行步骤</h4><ol>
<li><strong>进入 Mapper 方法前</strong><br><code>PaginationInnerInterceptor</code> 拦截到 <code>selectPage</code> 查询。</li>
<li><strong>判断是否是分页查询</strong><br>检测方法参数里有没有 <code>IPage/Page</code> 对象，<strong>有就走分页逻辑</strong>。</li>
<li><strong>自动生成 COUNT 语句</strong><br>原SQL：<code>select id,name from user</code><br>自动生成：<code>select count(*) from user</code> → 查询<strong>总记录数</strong></li>
<li><strong>改写原SQL 加 LIMIT</strong><br>原SQL 被改写为：<br><code>select id,name from user limit 0,10</code></li>
<li><strong>执行两条 SQL</strong></li>
</ol>
<ul>
<li>第一条：查总条数</li>
<li>第二条：查分页数据</li>
</ul>
<ol start="6">
<li><strong>封装分页信息</strong><br>把总条数、当前页、每页大小、总页数、数据列表 全部塞进 <code>IPage</code> 返回。</li>
</ol>
<hr>
<h3 id="四、为什么不用自己写-limit、不用自己写-count？"><a href="#四、为什么不用自己写-limit、不用自己写-count？" class="headerlink" title="四、为什么不用自己写 limit、不用自己写 count？"></a>四、为什么不用自己写 limit、不用自己写 count？</h3><p>MP 拦截器<strong>动态解析 SQL、动态拼接 limit、动态生成 count</strong>，完全无侵入。</p>
<hr>
<h3 id="五、分页两个重要特性"><a href="#五、分页两个重要特性" class="headerlink" title="五、分页两个重要特性"></a>五、分页两个重要特性</h3><h4 id="1-分页合理化-reasonable-true"><a href="#1-分页合理化-reasonable-true" class="headerlink" title="1. 分页合理化 reasonable=true"></a>1. 分页合理化 <code>reasonable=true</code></h4><ul>
<li>输入页码 <strong>&lt;&#x3D;0</strong> 自动变成第 1 页</li>
<li>输入页码 <strong>大于总页数</strong> 自动返回最后一页</li>
</ul>
<h4 id="2-溢出总页数后是否查最后一页"><a href="#2-溢出总页数后是否查最后一页" class="headerlink" title="2. 溢出总页数后是否查最后一页"></a>2. 溢出总页数后是否查最后一页</h4><p><code>PaginationInnerInterceptor</code> 可配置，防止页码乱输报错。</p>
<hr>
<h3 id="六、常见坑（原理衍生出来的）"><a href="#六、常见坑（原理衍生出来的）" class="headerlink" title="六、常见坑（原理衍生出来的）"></a>六、常见坑（原理衍生出来的）</h3><ol>
<li><strong>没配置 PaginationInnerInterceptor → 分页无效，查全部数据</strong></li>
<li><strong>只有 Mapper 层用 Page 参数才生效，Service 自己写 list 不分页</strong></li>
<li><strong>自定义 XML SQL 也能自动分页</strong>，MP 照样拦截加 limit、count</li>
<li><strong>多表联查、复杂 SQL 也支持</strong>，拦截器会解析原有 SQL 结构</li>
</ol>
<hr>
<h3 id="七、极简总结"><a href="#七、极简总结" class="headerlink" title="七、极简总结"></a>七、极简总结</h3><p>MP 分页 &#x3D; <strong>拦截器拦截SQL → 自动查总数 → 自动拼接LIMIT → 封装分页结果</strong><br>不用手写 count、不用手写 limit，全靠拦截器动态代理实现。</p>
<p>需要我给你画一张<strong>MP分页执行流程图</strong>，更直观看懂调用链路吗？</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>MyBatis-Plus</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Gradle 构建工具</title>
    <url>/2026/05/01/Java/gradle/</url>
    <content><![CDATA[<p>Gradle 是基于 JVM 的构建自动化工具，用 <strong><code>build.gradle</code></strong> &#x2F; <strong><code>build.gradle.kts</code></strong> 与 <strong><code>settings.gradle</code></strong> 描述依赖、插件与任务，支持增量编译与构建缓存，Android 与大型多模块项目常用。下面从官网、前置条件、Windows &#x2F; Rocky Linux 安装、环境变量到国内镜像加速，按步骤写清楚。</p>
<hr>
<h1 id="一、官网与发行包"><a href="#一、官网与发行包" class="headerlink" title="一、官网与发行包"></a>一、官网与发行包</h1><ul>
<li><strong>项目官网</strong>：<a href="https://gradle.org/">https://gradle.org/</a></li>
<li><strong>发行版列表与变更说明</strong>：<a href="https://gradle.org/releases/">https://gradle.org/releases/</a></li>
<li><strong>二进制包下载（<code>-bin.zip</code>，各平台通用）</strong>：<code>https://services.gradle.org/distributions/gradle-&lt;版本&gt;-bin.zip</code><br>例如：<code>https://services.gradle.org/distributions/gradle-9.5.0-bin.zip</code>（<strong><code>&lt;版本&gt;</code></strong> 请按官网当前稳定版替换）。</li>
</ul>
<p>安装前需已安装 <strong>JDK 8+</strong>（建议 JDK 17 或 21 LTS），终端可执行 <code>java -version</code> 验证。<br>Linux 解压 <strong><code>.zip</code></strong> 需要 <strong><code>unzip</code></strong>（Rocky 可 <code>sudo dnf install -y unzip</code>）。</p>
<hr>
<h1 id="二、Windows-安装"><a href="#二、Windows-安装" class="headerlink" title="二、Windows 安装"></a>二、Windows 安装</h1><h2 id="1-解压到固定目录"><a href="#1-解压到固定目录" class="headerlink" title="1. 解压到固定目录"></a>1. 解压到固定目录</h2><p>将下载的 <strong><code>gradle-*-bin.zip</code></strong> 解压到不含中文、无空格的路径。本文默认安装到 <strong><code>C:\app\</code></strong>，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C:\app\gradle-9.5.0</span><br></pre></td></tr></table></figure>

<p>解压后应能看到 <strong><code>bin</code></strong>、<strong><code>lib</code></strong>、<strong><code>init.d</code></strong> 等子目录。</p>
<h2 id="2-配置系统环境变量"><a href="#2-配置系统环境变量" class="headerlink" title="2. 配置系统环境变量"></a>2. 配置系统环境变量</h2><ol>
<li><p>打开 <strong>「系统属性」→「高级」→「环境变量」</strong>。</p>
</li>
<li><p><strong>新建系统变量</strong>（用户级也可，多用户建议系统级）：</p>
<ul>
<li>变量名：<code>GRADLE_HOME</code>  </li>
<li>变量值：<code>C:\app\gradle-9.5.0</code>（与解压路径一致，<strong>不要</strong>写到 <code>\bin</code>）</li>
</ul>
</li>
<li><p>编辑系统变量 <strong><code>Path</code></strong>，<strong>新建</strong>一行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">%GRADLE_HOME%\bin</span><br></pre></td></tr></table></figure>
</li>
<li><p>确定保存后，<strong>重新打开</strong> PowerShell 或 CMD。</p>
</li>
</ol>
<h2 id="3-验证"><a href="#3-验证" class="headerlink" title="3. 验证"></a>3. 验证</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">gradle <span class="literal">-v</span></span><br></pre></td></tr></table></figure>

<p>应输出 Gradle 版本、JVM 与安装路径等信息。</p>
<hr>
<h1 id="三、Rocky-Linux-安装"><a href="#三、Rocky-Linux-安装" class="headerlink" title="三、Rocky Linux 安装"></a>三、Rocky Linux 安装</h1><p>任选其一即可；需要固定版本或与官网一致时，优先用 <strong>官方压缩包</strong>。</p>
<h2 id="方式-A：软件包安装（简单）"><a href="#方式-A：软件包安装（简单）" class="headerlink" title="方式 A：软件包安装（简单）"></a>方式 A：软件包安装（简单）</h2><p>部分版本可通过仓库安装（包名与版本随发行版变化）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> dnf install -y unzip gradle</span><br><span class="line">gradle -v</span><br></pre></td></tr></table></figure>

<p>若提示找不到包或版本过旧，可改用方式 B。<br><strong>方式 A</strong> 的安装路径由发行版软件包决定；若约定统一放在 <strong><code>/usr/local/</code></strong>，请用方式 B。</p>
<h2 id="方式-B：官方二进制包（版本可控，默认-usr-local-）"><a href="#方式-B：官方二进制包（版本可控，默认-usr-local-）" class="headerlink" title="方式 B：官方二进制包（版本可控，默认 /usr/local/）"></a>方式 B：官方二进制包（版本可控，默认 <code>/usr/local/</code>）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例版本号请按官网 releases 页最新稳定版替换</span></span><br><span class="line">VER=9.5.0</span><br><span class="line"><span class="built_in">sudo</span> dnf install -y unzip</span><br><span class="line"><span class="built_in">cd</span> /usr/local</span><br><span class="line"><span class="built_in">sudo</span> wget <span class="string">&quot;https://services.gradle.org/distributions/gradle-<span class="variable">$&#123;VER&#125;</span>-bin.zip&quot;</span></span><br><span class="line"><span class="built_in">sudo</span> unzip -q <span class="string">&quot;gradle-<span class="variable">$&#123;VER&#125;</span>-bin.zip&quot;</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">ln</span> -sfn <span class="string">&quot;/usr/local/gradle-<span class="variable">$&#123;VER&#125;</span>&quot;</span> /usr/local/gradle</span><br></pre></td></tr></table></figure>

<p>下面「环境变量」一节中的路径为 <strong><code>/usr/local/gradle</code></strong>（即 <strong><code>GRADLE_HOME</code></strong> 默认值）。</p>
<hr>
<h1 id="四、Rocky-Linux-环境变量"><a href="#四、Rocky-Linux-环境变量" class="headerlink" title="四、Rocky Linux 环境变量"></a>四、Rocky Linux 环境变量</h1><h2 id="1-当前用户生效（推荐个人开发）"><a href="#1-当前用户生效（推荐个人开发）" class="headerlink" title="1. 当前用户生效（推荐个人开发）"></a>1. 当前用户生效（推荐个人开发）</h2><p>编辑 <code>~/.bashrc</code>（或 <code>~/.bash_profile</code>），末尾追加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> GRADLE_HOME=/usr/local/gradle</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$GRADLE_HOME</span>/bin:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>

<p>使配置生效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br><span class="line">gradle -v</span><br></pre></td></tr></table></figure>

<p>若实际解压路径不同，把 <strong><code>GRADLE_HOME</code></strong> 改成对应目录即可。</p>
<h2 id="2-全系统生效"><a href="#2-全系统生效" class="headerlink" title="2. 全系统生效"></a>2. 全系统生效</h2><p>新建 <strong><code>/etc/profile.d/gradle.sh</code></strong>（需 root），<strong><code>cat</code> + <code>tee</code></strong> 一键写入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">&#x27;EOF&#x27;</span> | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/profile.d/gradle.sh</span><br><span class="line"><span class="built_in">export</span> GRADLE_HOME=/usr/local/gradle</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$GRADLE_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line">EOF</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">chmod</span> +x /etc/profile.d/gradle.sh</span><br><span class="line"><span class="built_in">source</span> /etc/profile.d/gradle.sh</span><br><span class="line">gradle -v</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、阿里云镜像（加速依赖与插件解析）"><a href="#五、阿里云镜像（加速依赖与插件解析）" class="headerlink" title="五、阿里云镜像（加速依赖与插件解析）"></a>五、阿里云镜像（加速依赖与插件解析）</h1><p>Gradle 依赖仓库多在项目的 <strong><code>settings.gradle</code></strong> &#x2F; <strong><code>build.gradle</code></strong> 中声明；在 <strong><code>~/.gradle/</code></strong> 下放 <strong>初始化脚本</strong>，可对当前用户下执行的所有构建统一 prepend 国内源（适合本机开发）。在用户目录新建 <strong><code>~/.gradle/init.gradle</code></strong>：</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">allprojects</span> &#123;</span><br><span class="line">    <span class="keyword">repositories</span> &#123;</span><br><span class="line">        maven &#123; url <span class="string">&#x27;https://maven.aliyun.com/repository/google&#x27;</span> &#125;</span><br><span class="line">        maven &#123; url <span class="string">&#x27;https://maven.aliyun.com/repository/public&#x27;</span> &#125;</span><br><span class="line">        maven &#123; url <span class="string">&#x27;https://maven.aliyun.com/repository/gradle-plugin&#x27;</span> &#125;</span><br><span class="line">        google()</span><br><span class="line">        mavenCentral()</span><br><span class="line">        gradlePluginPortal()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><strong>阿里云 Maven 仓库指南</strong>（含 Gradle 相关说明）：<a href="https://developer.aliyun.com/mvn/guide">https://developer.aliyun.com/mvn/guide</a>  </li>
<li>若项目已在 <strong><code>settings.gradle</code></strong> 里使用 <strong><code>dependencyResolutionManagement { repositories { ... } }</code></strong> 且不允许继承，全局 <strong><code>init.gradle</code></strong> 可能不生效或需按项目规范改 <strong><code>settings.gradle</code></strong>。  </li>
<li><strong>Gradle Wrapper</strong> 下载 <strong><code>distributionUrl</code></strong> 若很慢，可在项目 <strong><code>gradle/wrapper/gradle-wrapper.properties</code></strong> 中把发行包地址改为国内镜像（如腾讯云 Gradle 镜像，路径与版本需与镜像站一致），与 <strong><code>init.gradle</code></strong> 是两件事：前者加速 <strong>Gradle 本体 zip</strong>，后者加速 <strong>依赖 jar</strong>。</li>
</ul>
<p>公司私服若已配置仓库，需与运维规范一致，避免与私服规则冲突。</p>
<hr>
<h1 id="六、常用命令速查"><a href="#六、常用命令速查" class="headerlink" title="六、常用命令速查"></a>六、常用命令速查</h1><p>在项目根目录（含 <strong><code>settings.gradle</code></strong> 或 <strong><code>settings.gradle.kts</code></strong>）执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gradle tasks              <span class="comment"># 列出可用任务</span></span><br><span class="line">gradle clean              <span class="comment"># 清理构建输出</span></span><br><span class="line">gradle build              <span class="comment"># 编译、测试并打包（依项目配置）</span></span><br><span class="line">gradle <span class="built_in">test</span>               <span class="comment"># 运行测试</span></span><br><span class="line">./gradlew build           <span class="comment"># 使用 Wrapper 时（需项目已生成 gradlew）</span></span><br></pre></td></tr></table></figure>

<p>首次执行会下载依赖与插件，配置镜像后一般会明显加快。团队项目推荐提交 <strong>Gradle Wrapper</strong>（<code>gradlew</code> &#x2F; <code>gradlew.bat</code>），保证大家使用同一 Gradle 版本。</p>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>官网</td>
<td><a href="https://gradle.org/">https://gradle.org/</a></td>
</tr>
<tr>
<td>Windows</td>
<td>默认解压到 **<code>C:\app\</code>**，配置 <code>GRADLE_HOME</code> 与 <code>Path</code> 中的 <code>%GRADLE_HOME%\bin</code></td>
</tr>
<tr>
<td>Rocky Linux</td>
<td><code>dnf install gradle</code>（路径随包）或官方 <strong><code>gradle-*-bin.zip</code></strong> 解压到 <strong><code>/usr/local/</code></strong> 并设 <code>GRADLE_HOME=/usr/local/gradle</code></td>
</tr>
<tr>
<td>加速</td>
<td><code>~/.gradle/init.gradle</code> 配置阿里云等仓库；Wrapper 本体另看 <strong><code>gradle-wrapper.properties</code></strong></td>
</tr>
<tr>
<td>验证</td>
<td><code>gradle -v</code></td>
</tr>
</tbody></table>
<p>完成以上步骤后，本机即可稳定使用 Gradle 构建 Java 等项目。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Gradle</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven 构建工具</title>
    <url>/2026/05/01/Java/maven/</url>
    <content><![CDATA[<p>Apache Maven 是 Java 项目常用的依赖管理与构建工具，用 <strong><code>pom.xml</code></strong> 描述坐标、依赖与生命周期，一条命令完成编译、测试、打包。下面从官网、前置条件、Windows &#x2F; Rocky Linux 安装、环境变量到国内镜像加速，按步骤写清楚。</p>
<hr>
<h1 id="一、官网与发行包"><a href="#一、官网与发行包" class="headerlink" title="一、官网与发行包"></a>一、官网与发行包</h1><ul>
<li><strong>项目官网</strong>：<a href="https://maven.apache.org/">https://maven.apache.org/</a></li>
<li><strong>下载页（Binary zip &#x2F; tar.gz）</strong>：<a href="https://maven.apache.org/download.cgi">https://maven.apache.org/download.cgi</a><br>选择当前稳定版的 <strong><code>apache-maven-*-bin.zip</code></strong>（Windows）或 <strong><code>apache-maven-*-bin.tar.gz</code></strong>（Linux）。</li>
</ul>
<p>安装前需已安装 <strong>JDK 8+</strong>（建议 JDK 17 或 21 LTS），终端可执行 <code>java -version</code> 验证。</p>
<hr>
<h1 id="二、Windows-安装"><a href="#二、Windows-安装" class="headerlink" title="二、Windows 安装"></a>二、Windows 安装</h1><h2 id="1-解压到固定目录"><a href="#1-解压到固定目录" class="headerlink" title="1. 解压到固定目录"></a>1. 解压到固定目录</h2><p>将下载的 <strong><code>apache-maven-*-bin.zip</code></strong> 解压到不含中文、无空格的路径。本文默认安装到 <strong><code>C:\app\</code></strong>，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C:\app\apache-maven-3.9.15</span><br></pre></td></tr></table></figure>

<p>解压后应能看到 <strong><code>bin</code></strong>、<strong><code>conf</code></strong>、<strong><code>lib</code></strong> 等子目录。</p>
<h2 id="2-配置系统环境变量"><a href="#2-配置系统环境变量" class="headerlink" title="2. 配置系统环境变量"></a>2. 配置系统环境变量</h2><ol>
<li><p>打开 <strong>「系统属性」→「高级」→「环境变量」</strong>。</p>
</li>
<li><p><strong>新建系统变量</strong>（用户级也可，多用户建议系统级）：</p>
<ul>
<li>变量名：<code>MAVEN_HOME</code>  </li>
<li>变量值：<code>C:\app\apache-maven-3.9.15</code>（与解压路径一致，<strong>不要</strong>写到 <code>\bin</code>）</li>
</ul>
</li>
<li><p>编辑系统变量 <strong><code>Path</code></strong>，<strong>新建</strong>一行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">%MAVEN_HOME%\bin</span><br></pre></td></tr></table></figure>
</li>
<li><p>确定保存后，<strong>重新打开</strong> PowerShell 或 CMD。</p>
</li>
</ol>
<h2 id="3-验证"><a href="#3-验证" class="headerlink" title="3. 验证"></a>3. 验证</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">mvn <span class="literal">-v</span></span><br></pre></td></tr></table></figure>

<p>应输出 Maven 版本、Java 版本与安装路径等信息。</p>
<hr>
<h1 id="三、Rocky-Linux-安装"><a href="#三、Rocky-Linux-安装" class="headerlink" title="三、Rocky Linux 安装"></a>三、Rocky Linux 安装</h1><p>任选其一即可；需要固定版本或与官网一致时，优先用 <strong>官方压缩包</strong>。</p>
<h2 id="方式-A：软件包安装（简单）"><a href="#方式-A：软件包安装（简单）" class="headerlink" title="方式 A：软件包安装（简单）"></a>方式 A：软件包安装（简单）</h2><p>Rocky Linux 8 &#x2F; 9 在启用默认仓库后，通常可直接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> dnf install -y maven</span><br><span class="line">mvn -v</span><br></pre></td></tr></table></figure>

<p>若提示找不到包，可先更新缓存并安装常用附加仓库说明见官方文档，或改用方式 B。<br><strong>方式 A</strong> 的安装路径由发行版软件包决定；若约定统一放在 <strong><code>/usr/local/</code></strong>，请用方式 B。</p>
<h2 id="方式-B：官方二进制包（版本可控，默认-usr-local-）"><a href="#方式-B：官方二进制包（版本可控，默认-usr-local-）" class="headerlink" title="方式 B：官方二进制包（版本可控，默认 /usr/local/）"></a>方式 B：官方二进制包（版本可控，默认 <code>/usr/local/</code>）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例版本号请按下载页最新稳定版替换</span></span><br><span class="line">VER=3.9.15</span><br><span class="line"><span class="built_in">cd</span> /usr/local</span><br><span class="line"><span class="built_in">sudo</span> wget https://dlcdn.apache.org/maven/maven-3/<span class="variable">$&#123;VER&#125;</span>/binaries/apache-maven-<span class="variable">$&#123;VER&#125;</span>-bin.tar.gz</span><br><span class="line"><span class="built_in">sudo</span> tar -xzf apache-maven-<span class="variable">$&#123;VER&#125;</span>-bin.tar.gz</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">ln</span> -sfn /usr/local/apache-maven-<span class="variable">$&#123;VER&#125;</span> /usr/local/maven</span><br></pre></td></tr></table></figure>

<p>下面「环境变量」一节中的路径为 <strong><code>/usr/local/maven</code></strong>（即 <strong><code>MAVEN_HOME</code></strong> 默认值）。</p>
<hr>
<h1 id="四、Rocky-Linux-环境变量"><a href="#四、Rocky-Linux-环境变量" class="headerlink" title="四、Rocky Linux 环境变量"></a>四、Rocky Linux 环境变量</h1><h2 id="1-当前用户生效（推荐个人开发）"><a href="#1-当前用户生效（推荐个人开发）" class="headerlink" title="1. 当前用户生效（推荐个人开发）"></a>1. 当前用户生效（推荐个人开发）</h2><p>编辑 <code>~/.bashrc</code>（或 <code>~/.bash_profile</code>），末尾追加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> MAVEN_HOME=/usr/local/maven</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$MAVEN_HOME</span>/bin:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>

<p>使配置生效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br><span class="line">mvn -v</span><br></pre></td></tr></table></figure>

<p>若实际解压路径不同，把 <strong><code>MAVEN_HOME</code></strong> 改成对应目录即可。</p>
<h2 id="2-全系统生效"><a href="#2-全系统生效" class="headerlink" title="2. 全系统生效"></a>2. 全系统生效</h2><p>新建 <strong><code>/etc/profile.d/maven.sh</code></strong>（需 root），<strong><code>cat</code> + <code>tee</code></strong> 一键写入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">&#x27;EOF&#x27;</span> | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/profile.d/maven.sh</span><br><span class="line"><span class="built_in">export</span> MAVEN_HOME=/usr/local/maven</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$MAVEN_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line">EOF</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">chmod</span> +x /etc/profile.d/maven.sh</span><br><span class="line"><span class="built_in">source</span> /etc/profile.d/maven.sh</span><br><span class="line">mvn -v</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、阿里云公共仓库镜像（加速依赖下载）"><a href="#五、阿里云公共仓库镜像（加速依赖下载）" class="headerlink" title="五、阿里云公共仓库镜像（加速依赖下载）"></a>五、阿里云公共仓库镜像（加速依赖下载）</h1><p>在用户目录下配置 <strong><code>~/.m2/settings.xml</code></strong>（没有则新建），在 <strong><code>&lt;mirrors&gt;</code></strong> 内增加阿里云公共仓库（聚合了 central 等常用仓库，适合个人与小型项目）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">settings</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/SETTINGS/1.2.0&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">mirrors</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mirror</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>aliyunmaven<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>*<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">name</span>&gt;</span>Aliyun Public<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">url</span>&gt;</span>https://maven.aliyun.com/repository/public<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">mirrors</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li>官方配置说明见 <strong>阿里云 Maven 仓库指南</strong>：<a href="https://developer.aliyun.com/mvn/guide">https://developer.aliyun.com/mvn/guide</a>  </li>
<li>若只需替换 Maven Central，可把 <strong><code>mirrorOf</code></strong> 改为 <strong><code>central</code></strong>，<strong><code>url</code></strong> 改为 <strong><code>https://maven.aliyun.com/repository/central</code></strong>。  </li>
<li>公司私服若已配置 <strong><code>mirror</code></strong>，需与运维规范一致，勿与私服规则冲突。</li>
</ul>
<hr>
<h1 id="六、常用命令速查"><a href="#六、常用命令速查" class="headerlink" title="六、常用命令速查"></a>六、常用命令速查</h1><p>在项目根目录（含 <strong><code>pom.xml</code></strong>）执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn clean          <span class="comment"># 清理 target</span></span><br><span class="line">mvn compile        <span class="comment"># 编译主代码</span></span><br><span class="line">mvn <span class="built_in">test</span>           <span class="comment"># 运行测试</span></span><br><span class="line">mvn package        <span class="comment"># 打包（jar/war 等）</span></span><br><span class="line">mvn clean install  <span class="comment"># 清理后编译、测试并安装到本地仓库</span></span><br></pre></td></tr></table></figure>

<p>首次执行会下载插件与依赖，配置镜像后一般会明显加快。</p>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>官网</td>
<td><a href="https://maven.apache.org/">https://maven.apache.org/</a></td>
</tr>
<tr>
<td>Windows</td>
<td>默认解压到 **<code>C:\app\</code>**，配置 <code>MAVEN_HOME</code> 与 <code>Path</code> 中的 <code>%MAVEN_HOME%\bin</code></td>
</tr>
<tr>
<td>Rocky Linux</td>
<td><code>dnf install maven</code>（路径随包）或官方包解压到 <strong><code>/usr/local/</code></strong> 并设 <code>MAVEN_HOME=/usr/local/maven</code></td>
</tr>
<tr>
<td>加速</td>
<td><code>~/.m2/settings.xml</code> 配置阿里云等镜像</td>
</tr>
<tr>
<td>验证</td>
<td><code>mvn -v</code></td>
</tr>
</tbody></table>
<p>完成以上步骤后，本机即可稳定使用 Maven 构建 Java 项目。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux</title>
    <url>/2026/05/01/Linux/Linux/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows 上 WSL（Rocky Linux）与 Docker Desktop</title>
    <url>/2026/05/01/Linux/WSL-Rocky/</url>
    <content><![CDATA[<p>在 <strong>Windows 10&#x2F;11</strong> 上通过 <strong>WSL2</strong> 运行 <strong>Rocky Linux</strong>，可与 <strong>Docker Desktop</strong> 共用同一套引擎：容器在 <strong>Docker Desktop</strong> 的 <strong>Linux</strong> 后端里跑，你在 <strong>Rocky Linux</strong> 终端里直接使用 <strong><code>docker</code></strong> 命令。下面按「准备 → 启用 <strong>WSL</strong> → 安装 <strong>Rocky Linux</strong> → <strong>Docker Desktop</strong> 集成 → 首次配置 → 日常维护」顺序说明；<strong>Rocky Linux</strong> 安装以官方文档为准，<strong>Docker</strong> 以 <strong>Docker Desktop</strong> 文档为准。</p>
<hr>
<h1 id="一、环境与版本说明"><a href="#一、环境与版本说明" class="headerlink" title="一、环境与版本说明"></a>一、环境与版本说明</h1><ol>
<li><strong>操作系统</strong>：<strong>Windows 10</strong>（建议 22H2 及以后）或 <strong>Windows 11</strong>。  </li>
<li><strong>体系结构</strong>：下载 <strong>Rocky Linux</strong> 镜像时选 <strong>x86_64</strong> 或 <strong>aarch64</strong>，与 <strong>Windows</strong>、<strong>Docker Desktop</strong> 安装包一致。  </li>
<li><strong>WSL</strong>：使用 <strong>WSL2</strong>；<strong>Docker Desktop</strong> 的 <strong>WSL 2</strong> 后端依赖此版本。  </li>
<li><strong>Rocky Linux</strong>：官方 <strong><code>.wsl</code></strong> 镜像（推荐）或 <strong><code>wsl --import</code></strong>。  </li>
<li><strong>权威文档</strong>：  <ul>
<li><strong>Rocky Linux</strong>（本文默认 <strong>10</strong>，文档内可切换版本）：<a href="https://docs.rockylinux.org/10/guides/interoperability/import_rocky_to_wsl/">Import Rocky Linux to WSL or WSL2</a>  </li>
<li><strong>Microsoft</strong>：<a href="https://learn.microsoft.com/zh-cn/windows/wsl/install">安装 WSL</a>  </li>
<li><strong>Docker</strong>：<a href="https://docs.docker.com/desktop/setup/install/windows-install/">Docker Desktop on Windows</a>、<a href="https://docs.docker.com/desktop/wsl/">WSL 与 Docker Desktop</a></li>
</ul>
</li>
</ol>
<hr>
<h1 id="二、硬件与-Windows-功能准备"><a href="#二、硬件与-Windows-功能准备" class="headerlink" title="二、硬件与 Windows 功能准备"></a>二、硬件与 Windows 功能准备</h1><h2 id="1-BIOS-UEFI"><a href="#1-BIOS-UEFI" class="headerlink" title="1. BIOS &#x2F; UEFI"></a>1. BIOS &#x2F; UEFI</h2><p>开启 <strong>CPU 虚拟化</strong>（<strong>Intel VT-x</strong>、<strong>AMD-V</strong>）。<strong>WSL2</strong> 与 <strong>Docker Desktop</strong> 均依赖。</p>
<h2 id="2-启用可选组件（图形界面）"><a href="#2-启用可选组件（图形界面）" class="headerlink" title="2. 启用可选组件（图形界面）"></a>2. 启用可选组件（图形界面）</h2><ol>
<li><strong>Win + R</strong> → <strong><code>optionalfeatures</code></strong> → 勾选 <strong>适用于 Linux 的 Windows 子系统</strong>、<strong>虚拟机平台</strong>。  </li>
<li>重启。</li>
</ol>
<h2 id="3-商店版-WSL（推荐）"><a href="#3-商店版-WSL（推荐）" class="headerlink" title="3. 商店版 WSL（推荐）"></a>3. 商店版 WSL（推荐）</h2><p>从 <strong>Microsoft Store</strong> 安装 <a href="https://apps.microsoft.com/detail/9p9tqf7mrm4r">Windows Subsystem for Linux</a>。</p>
<hr>
<h1 id="三、用-PowerShell-启用并更新-WSL"><a href="#三、用-PowerShell-启用并更新-WSL" class="headerlink" title="三、用 PowerShell 启用并更新 WSL"></a>三、用 PowerShell 启用并更新 WSL</h1><p>在 <strong>以管理员身份运行</strong> 的 <strong>PowerShell</strong> 中执行。</p>
<h2 id="1-安装-WSL-组件（若尚未安装）"><a href="#1-安装-WSL-组件（若尚未安装）" class="headerlink" title="1. 安装 WSL 组件（若尚未安装）"></a>1. 安装 WSL 组件（若尚未安装）</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--install</span>  <span class="comment"># 默认安装Ubuntu，这里不使用此命令，使用 安装 Rocky Linux 命令</span></span><br></pre></td></tr></table></figure>

<h2 id="2-默认使用-WSL2"><a href="#2-默认使用-WSL2" class="headerlink" title="2. 默认使用 WSL2"></a>2. 默认使用 WSL2</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--set-default-version</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>

<h2 id="3-更新-WSL"><a href="#3-更新-WSL" class="headerlink" title="3. 更新 WSL"></a>3. 更新 WSL</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--update</span></span><br></pre></td></tr></table></figure>

<h2 id="4-验证"><a href="#4-验证" class="headerlink" title="4. 验证"></a>4. 验证</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--version</span></span><br><span class="line">wsl <span class="literal">--status</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、安装-Rocky-Linux（推荐：官方-wsl-镜像）"><a href="#四、安装-Rocky-Linux（推荐：官方-wsl-镜像）" class="headerlink" title="四、安装 Rocky Linux（推荐：官方 .wsl 镜像）"></a>四、安装 Rocky Linux（推荐：官方 <code>.wsl</code> 镜像）</h1><h2 id="1-下载镜像（默认-Rocky-Linux-10）"><a href="#1-下载镜像（默认-Rocky-Linux-10）" class="headerlink" title="1. 下载镜像（默认 Rocky Linux 10）"></a>1. 下载镜像（默认 <strong>Rocky Linux 10</strong>）</h2><p><strong>Rocky Linux 10</strong>（<strong>x86_64</strong>）：<a href="https://dl.rockylinux.org/pub/rocky/10/images/x86_64/Rocky-10-WSL-Base.latest.x86_64.wsl">https://dl.rockylinux.org/pub/rocky/10/images/x86_64/Rocky-10-WSL-Base.latest.x86_64.wsl</a><br><strong>aarch64</strong>：<a href="https://dl.rockylinux.org/pub/rocky/10/images/aarch64/Rocky-10-WSL-Base.latest.aarch64.wsl">https://dl.rockylinux.org/pub/rocky/10/images/aarch64/Rocky-10-WSL-Base.latest.aarch64.wsl</a></p>
<h2 id="2-命令行安装"><a href="#2-命令行安装" class="headerlink" title="2. 命令行安装"></a>2. 命令行安装</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--install</span> <span class="literal">--from-file</span> <span class="string">&quot;D:\download\Rocky-10-WSL-Base.latest.x86_64.wsl&quot;</span> <span class="literal">--name</span> Rocky<span class="literal">-10</span></span><br></pre></td></tr></table></figure>

<p>将路径、<code>--name</code> 改成你的实际值。安装后 <strong>Windows Terminal</strong> 下拉里可出现 <strong>Rocky-10</strong>。</p>
<h2 id="3-双击安装（可选）"><a href="#3-双击安装（可选）" class="headerlink" title="3. 双击安装（可选）"></a>3. 双击安装（可选）</h2><p>资源管理器中双击 <strong><code>.wsl</code></strong> 按向导安装。</p>
<hr>
<h1 id="五、安装与配置-Docker-Desktop（配合-WSL2）"><a href="#五、安装与配置-Docker-Desktop（配合-WSL2）" class="headerlink" title="五、安装与配置 Docker Desktop（配合 WSL2）"></a>五、安装与配置 Docker Desktop（配合 WSL2）</h1><h2 id="1-下载并安装"><a href="#1-下载并安装" class="headerlink" title="1. 下载并安装"></a>1. 下载并安装</h2><ol>
<li>打开 <a href="https://www.docker.com/products/docker-desktop/">https://www.docker.com/products/docker-desktop/</a> ，下载 <strong>Docker Desktop for Windows</strong>。  </li>
<li>安装向导中保持 <strong>Use WSL 2 instead of Hyper-V</strong>（或等价选项，以当前安装界面为准）。  </li>
<li>安装结束若提示注销或重启，按提示操作。</li>
</ol>
<h2 id="2-确认使用-WSL-2-后端"><a href="#2-确认使用-WSL-2-后端" class="headerlink" title="2. 确认使用 WSL 2 后端"></a>2. 确认使用 WSL 2 后端</h2><p>打开 <strong>Docker Desktop</strong> → <strong>Settings</strong>（设置）→ <strong>General</strong>（常规）：</p>
<ul>
<li>勾选 <strong>Use the WSL 2 based engine</strong>（使用基于 <strong>WSL 2</strong> 的引擎）。</li>
</ul>
<p>应用后必要时 <strong>Restart Docker Desktop</strong>。</p>
<h2 id="3-与-Rocky-Linux-发行版集成（核心）"><a href="#3-与-Rocky-Linux-发行版集成（核心）" class="headerlink" title="3. 与 Rocky Linux 发行版集成（核心）"></a>3. 与 Rocky Linux 发行版集成（核心）</h2><p><strong>Settings</strong> → <strong>Resources</strong> → <strong>WSL integration</strong>（<strong>WSL</strong> 集成）：</p>
<ol>
<li>打开 <strong>Enable integration with my default Wsl distro</strong>（与默认 <strong>WSL</strong> 发行版集成）（按需）。  </li>
<li>在 <strong>Enable integration with additional distros</strong> 中，<strong>打开</strong>你的 <strong>Rocky Linux</strong> 名称（例如 <strong>Rocky-10</strong>）。  </li>
<li>点击 <strong>Apply &amp; restart</strong>。</li>
</ol>
<p>之后在 <strong><code>wsl -d Rocky-10</code></strong> 的 shell 中应能直接执行 <strong><code>docker</code></strong>、<strong><code>docker compose</code></strong>，实际引擎由 <strong>Docker Desktop</strong> 提供，无需在 <strong>Rocky</strong> 里再装 <strong><code>dockerd</code></strong> 或配置 <strong>rootful</strong> 独立引擎（避免与 <strong>Desktop</strong> 抢端口、重复占资源）。</p>
<h2 id="4-验证（在-Rocky-Linux-内）"><a href="#4-验证（在-Rocky-Linux-内）" class="headerlink" title="4. 验证（在 Rocky Linux 内）"></a>4. 验证（在 Rocky Linux 内）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>若提示找不到 <strong><code>docker</code></strong>：回到上一步检查该发行版集成开关，并执行 <strong><code>wsl --shutdown</code></strong> 后重新打开 <strong>Docker Desktop</strong> 与 <strong>WSL</strong> 终端。</p>
<h2 id="5-镜像加速与构建缓存（Docker-Engine）"><a href="#5-镜像加速与构建缓存（Docker-Engine）" class="headerlink" title="5. 镜像加速与构建缓存（Docker Engine）"></a>5. 镜像加速与构建缓存（Docker Engine）</h2><p>在 <strong>Docker Desktop</strong> → <strong>Settings</strong>（设置）→ <strong>Docker Engine</strong> 中编辑 <strong>JSON</strong>（与引擎配置同源，保存后 <strong>Apply &amp; restart</strong> 生效）。若已有其它键（如 <strong><code>insecure-registries</code></strong>），请把下面片段<strong>合并</strong>进现有对象，不要整段覆盖导致丢配置。</p>
<ul>
<li><strong><code>registry-mirrors</code></strong>：拉取镜像时优先走列表中的镜像站，减轻访问 <strong><code>docker.io</code></strong> 的延迟与失败（镜像站可用性与合规以你网络环境为准，可日后替换为团队提供的地址）。  </li>
<li><strong><code>builder.gc</code></strong>：为 <strong>BuildKit</strong> 构建缓存开启自动回收，<strong><code>defaultKeepStorage</code></strong> 限制默认保留量（示例 <strong>20GB</strong>），避免构建缓存占满磁盘。</li>
</ul>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;builder&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;gc&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;defaultKeepStorage&quot;</span><span class="punctuation">:</span> <span class="string">&quot;20GB&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;experimental&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;registry-mirrors&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;https://docker.1ms.run&quot;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>修改后点击 <strong>Apply &amp; restart</strong>；再在 <strong>Rocky</strong> 或 <strong>PowerShell</strong> 中执行 <strong><code>docker info</code></strong>，在输出里确认 <strong><code>Registry Mirrors</code></strong> 已包含上述地址。</p>
<h2 id="6-文件与性能建议"><a href="#6-文件与性能建议" class="headerlink" title="6. 文件与性能建议"></a>6. 文件与性能建议</h2><ul>
<li><p>项目代码放在 <strong>WSL 发行版自己的 Linux 文件系统里</strong>（例如在 <strong><code>wsl -d Rocky-10</code></strong> 里用 <strong><code>~/project</code></strong>，即 <strong><code>/home/你的用户/project</code></strong>），不要放在 <strong><code>/mnt/c/...</code></strong> 下做大量编译与 <strong>Docker</strong> 构建，可减少跨系统 <strong>I&#x2F;O</strong> 延迟。  </p>
</li>
<li><p><strong>Docker Desktop</strong> 的 <strong>Settings → Resources</strong> 可调 <strong>CPU</strong>、内存、磁盘镜像上限，与 <strong>Rocky</strong> 其它进程共用本机资源，按机器配置酌情调大。</p>
</li>
</ul>
<h2 id="7-与「在-Rocky-里单独装-Docker-Engine」的区别"><a href="#7-与「在-Rocky-里单独装-Docker-Engine」的区别" class="headerlink" title="7. 与「在 Rocky 里单独装 Docker Engine」的区别"></a>7. 与「在 Rocky 里单独装 Docker Engine」的区别</h2><table>
<thead>
<tr>
<th>方式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Docker Desktop + WSL 集成</strong>（本文推荐）</td>
<td>一个图形界面管理引擎、更新与证书；<strong>Rocky</strong> 内仅 <strong>CLI</strong> 连到 <strong>Desktop</strong></td>
</tr>
<tr>
<td><strong>Rocky 内 <code>dnf install docker-ce</code> 并跑 <code>dockerd</code></strong></td>
<td>与 <strong>Desktop</strong> 两套引擎易冲突，一般不叠加以 <strong>Desktop</strong> 为主场景</td>
</tr>
</tbody></table>
<p>若必须仅用发行版内 <strong>dockerd</strong>（无 <strong>Desktop</strong>），应关闭 <strong>Docker Desktop</strong> 的 <strong>WSL</strong> 集成或卸载 <strong>Desktop</strong>，并自行处理 <strong>iptables</strong>&#x2F;<strong>cgroup</strong> 等与 <strong>WSL</strong> 文档差异。</p>
<hr>
<h1 id="七、Rocky-Linux-首次进入与基础配置"><a href="#七、Rocky-Linux-首次进入与基础配置" class="headerlink" title="七、Rocky Linux 首次进入与基础配置"></a>七、Rocky Linux 首次进入与基础配置</h1><h2 id="1-进入发行版"><a href="#1-进入发行版" class="headerlink" title="1. 进入发行版"></a>1. 进入发行版</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">-d</span> Rocky<span class="literal">-10</span></span><br></pre></td></tr></table></figure>

<h2 id="2-是否需要「登录」、如何设置与验证-root-密码"><a href="#2-是否需要「登录」、如何设置与验证-root-密码" class="headerlink" title="2. 是否需要「登录」、如何设置与验证 root 密码"></a>2. 是否需要「登录」、如何设置与验证 <strong>root</strong> 密码</h2><p><strong>是否要登录</strong>：<strong>WSL</strong> 从 <strong>Windows Terminal</strong>、<strong><code>wsl -d Rocky-10</code></strong> 等方式进入时，一般是<strong>直接进入已配置的默认 Linux 用户的 shell</strong>，不会像 <strong>SSH</strong> 那样再在终端里提示输入一次用户名（除非你主动执行 <strong><code>login</code></strong>、<strong><code>su</code></strong> 等）。首次安装某些发行版时会交互式让你创建 <strong>UNIX</strong> 用户名与密码；<strong>Rocky</strong> 官方 <strong><code>.wsl</code></strong> 导入后的默认用户以该镜像说明为准（常见为 <strong><code>root</code></strong> 或需自行建普通用户，见下节）。</p>
<p><strong>root 密码是什么、要不要设</strong>：<strong>root</strong> 账户始终存在；若从未执行过 <strong><code>passwd</code></strong>，<strong>root</strong> 可能没有可用登录密码（或镜像预置了密码，以发行版文档为准）。日常维护建议用<strong>带 <code>sudo</code> 的普通用户</strong>；需要 <strong>root shell</strong> 时再切。</p>
<p><strong>设置 root 密码</strong>（在已能 <strong><code>sudo</code></strong> 的普通用户下）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> passwd root  <span class="comment"># pi=3.1415</span></span><br></pre></td></tr></table></figure>

<p>若当前 shell <strong>已是 root</strong>（提示符常为 <strong><code>#</code></strong> 且 <strong><code>whoami</code></strong> 输出 <strong><code>root</code></strong>），直接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">passwd</span><br></pre></td></tr></table></figure>

<p><strong>如何验证 root 密码是否生效</strong>：在同一发行版里执行 <strong><code>su -</code></strong>，提示 <strong><code>Password:</code></strong> 时输入刚设的 root 密码；成功则进入 root 的登录 shell（<strong><code>whoami</code></strong> 应输出 <strong><code>root</code></strong>）。用 <strong><code>exit</code></strong> 退出。注意 <strong><code>sudo -i</code></strong> 校验的通常是<strong>当前用户</strong>的 <strong><code>sudo</code></strong> 凭据，不是专门用来「试 root 密码」的命令。</p>
<p><strong>从 Windows 侧以 root 进入（忘记普通用户密码时）</strong>：在 <strong>PowerShell</strong> 中执行 <strong><code>wsl -d Rocky-10 -u root</code></strong>，一般无需先知道普通用户密码；进入后再 <strong><code>passwd youruser</code></strong> 重置普通用户，或 <strong><code>passwd root</code></strong> 重置 root。</p>
<h2 id="3-用户与-sudo"><a href="#3-用户与-sudo" class="headerlink" title="3. 用户与 sudo"></a>3. 用户与 sudo</h2><p>按需创建普通用户并加入 <strong><code>wheel</code></strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> useradd -m -G wheel youruser</span><br><span class="line"><span class="built_in">sudo</span> passwd youruser</span><br></pre></td></tr></table></figure>

<h2 id="4-系统更新"><a href="#4-系统更新" class="headerlink" title="4. 系统更新"></a>4. 系统更新</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> dnf -y update</span><br></pre></td></tr></table></figure>

<h2 id="5-设为默认-WSL-发行版（可选）"><a href="#5-设为默认-WSL-发行版（可选）" class="headerlink" title="5. 设为默认 WSL 发行版（可选）"></a>5. 设为默认 WSL 发行版（可选）</h2><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--set-default</span> Rocky<span class="literal">-10</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="八、日常维护与常用命令（Windows-侧）"><a href="#八、日常维护与常用命令（Windows-侧）" class="headerlink" title="八、日常维护与常用命令（Windows 侧）"></a>八、日常维护与常用命令（Windows 侧）</h1><table>
<thead>
<tr>
<th>目的</th>
<th>命令示例</th>
</tr>
</thead>
<tbody><tr>
<td>列出发行版及 <strong>WSL</strong> 版本</td>
<td><code>wsl -l -v</code></td>
</tr>
<tr>
<td>关闭所有 <strong>WSL</strong></td>
<td><code>wsl --shutdown</code></td>
</tr>
<tr>
<td>删除某发行版（<strong>数据清空</strong>）</td>
<td><code>wsl --unregister Rocky-10</code></td>
</tr>
<tr>
<td>导出备份</td>
<td><code>wsl --export Rocky-10 D:\backup\rocky-10.tar</code></td>
</tr>
</tbody></table>
<p><strong>Docker Desktop</strong> 升级、重置可在 <strong>Windows</strong>「应用和功能」或 <strong>Docker Desktop</strong> 自带 <strong>Troubleshoot</strong> 中处理；与 <strong>WSL</strong> 内核相关的问题优先 <strong><code>wsl --update</code></strong>。</p>
<hr>
<h1 id="九、小结"><a href="#九、小结" class="headerlink" title="九、小结"></a>九、小结</h1><table>
<thead>
<tr>
<th>步骤</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td>基础</td>
<td><strong>BIOS</strong> 虚拟化、<strong>WSL</strong> + <strong>虚拟机平台</strong>、<strong>WSL2</strong>、<strong><code>wsl --update</code></strong></td>
</tr>
<tr>
<td><strong>Rocky Linux</strong></td>
<td>默认 <strong>10</strong>：<strong><code>Rocky-10-WSL-Base.latest.*.wsl</code></strong> + <strong><code>--name Rocky-10</code></strong>；<strong>9</strong> 同理换包名与 <strong><code>--name</code></strong></td>
</tr>
<tr>
<td><strong>Docker Desktop</strong></td>
<td>安装时选 <strong>WSL 2</strong> 引擎；<strong>Settings → WSL integration</strong> 勾选 <strong>Rocky</strong> 发行版</td>
</tr>
<tr>
<td>镜像加速</td>
<td><strong>Settings → Docker Engine</strong>：<strong><code>registry-mirrors</code></strong> + <strong><code>builder.gc</code></strong>（见 <strong>五、5</strong>）</td>
</tr>
<tr>
<td>验证</td>
<td><strong>Rocky</strong> 内 <strong><code>docker run hello-world</code></strong></td>
</tr>
<tr>
<td>用户与 <strong>root</strong></td>
<td><strong>WSL</strong> 一般无二次「登录」；<strong><code>sudo passwd root</code></strong> 设密码，<strong><code>su -</code></strong> 验证；必要时 <strong><code>wsl -u root</code></strong>（见 <strong>七、2</strong>）</td>
</tr>
<tr>
<td>实践建议</td>
<td>代码放 <strong>Rocky 等发行版内 Linux 路径</strong>（如 <strong><code>~/project</code></strong>）；勿长期用 <strong><code>/mnt/c</code></strong> 做重编译；避免与发行版内独立 <strong><code>dockerd</code></strong> 混用</td>
</tr>
</tbody></table>
<p><strong>Rocky Linux 9 &#x2F; 10</strong> 切换请对照官方 <strong>WSL</strong> 文档对应版本页；<strong>Docker Desktop</strong> 与 <strong>Windows</strong> 大版本兼容性以 <strong>Docker</strong> 发行说明为准。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Desktop</tag>
        <tag>WSL</tag>
        <tag>Rocky Linux</tag>
        <tag>Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 Kafka</title>
    <url>/2026/05/01/MQ/Kafka-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>Apache Kafka</strong>（<strong>KRaft</strong> 模式，无需 <strong>ZooKeeper</strong>），适合本地开发、与 <strong>Compose</strong> 编排其它服务、或在 <strong>Kubernetes</strong> 中单节点验证。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；<strong><code>ADVERTISED_LISTENERS</code></strong> 与数据卷请按环境自行调整，勿把生产密钥提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、Kafka-镜像选择"><a href="#二、Kafka-镜像选择" class="headerlink" title="二、Kafka 镜像选择"></a>二、Kafka 镜像选择</h1><h2 id="1-常用镜像"><a href="#1-常用镜像" class="headerlink" title="1. 常用镜像"></a>1. 常用镜像</h2><table>
<thead>
<tr>
<th>镜像</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>apache/kafka</code></strong></td>
<td><strong>Apache</strong> 官方维护（<strong>Docker Hub</strong>：<a href="https://hub.docker.com/r/apache/kafka">https://hub.docker.com/r/apache/kafka</a>），参数前缀多为 <strong><code>KAFKA_</code></strong>，与发行包行为贴近。</td>
</tr>
<tr>
<td><strong><code>bitnami/kafka</code></strong></td>
<td><strong>Bitnami</strong> 打包（<a href="https://hub.docker.com/r/bitnami/kafka">https://hub.docker.com/r/bitnami/kafka</a>），参数前缀多为 <strong><code>KAFKA_CFG_</code></strong>，文档与示例丰富，下文默认采用。</td>
</tr>
</tbody></table>
<h2 id="2-版本与标签"><a href="#2-版本与标签" class="headerlink" title="2. 版本与标签"></a>2. 版本与标签</h2><ul>
<li><strong>开发&#x2F;学习</strong>：固定大版本标签（如 <strong><code>bitnami/kafka:3.7</code></strong>、<strong><code>apache/kafka:3.9.0</code></strong>），避免 <strong><code>latest</code></strong> 漂移。  </li>
<li><strong>生产</strong>：固定补丁级标签或 <strong>digest</strong>，并阅读对应镜像的发行说明与安全更新。</li>
</ul>
<h2 id="3-监听器与-ADVERTISED-LISTENERS"><a href="#3-监听器与-ADVERTISED-LISTENERS" class="headerlink" title="3. 监听器与 ADVERTISED_LISTENERS"></a>3. 监听器与 <code>ADVERTISED_LISTENERS</code></h2><p>客户端使用的 <strong><code>bootstrap.servers</code></strong> 必须落在 <strong><code>advertised.listeners</code></strong> 可解析的地址上：</p>
<ul>
<li><strong>仅本机</strong>连 <strong><code>localhost:9092</code></strong>：可把 <strong><code>KAFKA_CFG_ADVERTISED_LISTENERS</code></strong> 设为 <strong><code>PLAINTEXT://127.0.0.1:9092</code></strong>（与下文 <strong>「三」</strong> 单行示例一致）。  </li>
<li><strong>Compose 内其它服务</strong>连 <strong><code>kafka:9092</code></strong>，同时<strong>宿主机</strong>还要连：需<strong>双监听</strong>（下文 <strong>「四」</strong>），例如内部 <strong><code>PLAINTEXT://kafka:9092</code></strong>、外部 <strong><code>PLAINTEXT://localhost:9094</code></strong>，并映射两个端口。</li>
</ul>
<h2 id="4-资源与持久化"><a href="#4-资源与持久化" class="headerlink" title="4. 资源与持久化"></a>4. 资源与持久化</h2><p><strong>Kafka</strong> 依赖磁盘顺序写与充足 <strong>page cache</strong>；<strong>Bitnami</strong> 数据目录一般为 <strong><code>/bitnami/kafka</code></strong>。单机开发可适当降低 <strong><code>heap</code></strong>，生产请按吞吐与副本单独规划。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式跑 <strong>Apache Kafka</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>Kafka</strong> 一键部署：由面板管理镜像版本、数据卷、监听端口与 <strong>KRaft</strong> 相关环境变量（以当前上架镜像与模板为准）。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s<code>** 中的 **</code>KAFKA_CFG_ADVERTISED_LISTENERS<code>**、双监听、数据目录与 **</code>heap&#96;</strong> 对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等「快速起消息流」的场景。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>仅适合<strong>宿主机上的客户端</strong>访问（<strong><code>ADVERTISED_LISTENERS</code></strong> 指向 <strong><code>127.0.0.1</code></strong>）。先建数据目录（仅首次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./kafka-data</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（<strong>KRaft</strong> 单进程；请按需改端口与卷路径；示例仅供本地）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name kafka-dev -p 9092:9092 -e ALLOW_PLAINTEXT_LISTENER=<span class="built_in">yes</span> -e KAFKA_CFG_NODE_ID=0 -e KAFKA_CFG_PROCESS_ROLES=controller,broker -e KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093 -e KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092 -e KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER -e KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT -e KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@127.0.0.1:9093 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/kafka-data:/bitnami/kafka&quot;</span> docker.io/bitnami/kafka:3.7</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f kafka-dev</span><br><span class="line">docker stop kafka-dev &amp;&amp; docker <span class="built_in">rm</span> kafka-dev</span><br></pre></td></tr></table></figure>

<p>本机探测（需本机已安装 <strong><code>kafka</code></strong> 客户端或进入容器执行）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> kafka-dev kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.kafka.yml</code></strong>。<strong>双监听</strong>：<strong>Compose</strong> 网内用 <strong><code>kafka:9092</code></strong>，宿主机用 <strong><code>localhost:9094</code></strong>。</p>
<p><strong>一条命令</strong>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.kafka.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.kafka.yml</code></strong> 全文：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">kafka-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">kafka:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">docker.io/bitnami/kafka:3.7</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">kafka-3</span></span><br><span class="line">    <span class="attr">hostname:</span> <span class="string">kafka</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9092:9092&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9094:9094&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">ALLOW_PLAINTEXT_LISTENER:</span> <span class="string">&quot;yes&quot;</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_NODE_ID:</span> <span class="string">&quot;0&quot;</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_PROCESS_ROLES:</span> <span class="string">controller,broker</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_CONTROLLER_LISTENER_NAMES:</span> <span class="string">CONTROLLER</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP:</span> <span class="string">CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_LISTENERS:</span> <span class="string">PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_ADVERTISED_LISTENERS:</span> <span class="string">PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_CONTROLLER_QUORUM_VOTERS:</span> <span class="number">0</span><span class="string">@kafka:9093</span></span><br><span class="line">      <span class="attr">KAFKA_CFG_INTER_BROKER_LISTENER_NAME:</span> <span class="string">PLAINTEXT</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">kafka_data:/bitnami/kafka</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;kafka-broker-api-versions.sh --bootstrap-server kafka:9092 || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">12</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">60s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">kafka_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.kafka.yml ps</span><br><span class="line">docker compose -f docker-compose.kafka.yml logs -f kafka</span><br><span class="line">docker compose -f docker-compose.kafka.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留 <strong><code>kafka_data</code></strong>；清空数据使用 <strong><code>docker compose -f docker-compose.kafka.yml down -v</code></strong>（<strong>会删主题与消息</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>kafka-k8s.yaml</code></strong> 后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f kafka-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>以下为 <strong>单副本</strong> <strong>Deployment</strong> + <strong>PVC</strong> + <strong>Service</strong>（<strong>ClusterIP 9092</strong>）。<strong><code>KAFKA_CFG_ADVERTISED_LISTENERS</code></strong> 使用 <strong>Service DNS</strong>，供集群内 <strong>Pod</strong> 访问；集群外请 <strong><code>Ingress</code></strong>、<strong><code>NodePort</code></strong> 或 <strong><code>kubectl port-forward</code></strong> 另行暴露，并同步修改 <strong><code>ADVERTISED</code></strong> 与监听。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">kafka-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">20Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">kafka</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">kafka</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">kafka</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">kafka</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">hostname:</span> <span class="string">kafka</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">kafka</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">docker.io/bitnami/kafka:3.7</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9092</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">plaintext</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9093</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">controller</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">ALLOW_PLAINTEXT_LISTENER</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;yes&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_NODE_ID</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;0&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_PROCESS_ROLES</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">controller,broker</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_CONTROLLER_LISTENER_NAMES</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">CONTROLLER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_LISTENERS</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">PLAINTEXT://:9092,CONTROLLER://:9093</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_ADVERTISED_LISTENERS</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">PLAINTEXT://kafka.default.svc.cluster.local:9092</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_CONTROLLER_QUORUM_VOTERS</span></span><br><span class="line">              <span class="attr">value:</span> <span class="number">0</span><span class="string">@127.0.0.1:9093</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">KAFKA_CFG_INTER_BROKER_LISTENER_NAME</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">PLAINTEXT</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/bitnami/kafka</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">exec:</span></span><br><span class="line">              <span class="attr">command:</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">/bin/sh</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">kafka-broker-api-versions.sh</span> <span class="string">--bootstrap-server</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:9092</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">30</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">18</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">9092</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">90</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">20</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;500m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;2Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;2000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">kafka-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">kafka</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">kafka</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">kafka</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">plaintext</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9092</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9092</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">controller</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9093</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9093</span></span><br></pre></td></tr></table></figure>

<p>当前示例为 <strong>单副本 Deployment</strong> + <strong>ClusterIP</strong>，仅作<strong>集群内连通性验证</strong>；与 <strong>StatefulSet + Headless</strong> 的典型生产拓扑不同。查看资源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get deploy,svc,pvc -l app=kafka</span><br></pre></td></tr></table></figure>

<p>集群内 <strong>bootstrap</strong> 示例：<strong><code>kafka.default.svc.cluster.local:9092</code></strong>（与 <strong><code>KAFKA_CFG_ADVERTISED_LISTENERS</code></strong> 一致）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本同时挂载；<strong>多 broker、跨 AZ、滚动升级</strong> 请使用 <strong>Strimzi</strong>、<strong>Confluent for Kubernetes</strong> 等 <strong>Operator</strong> 或官方推荐的 <strong>StatefulSet</strong> 拓扑，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>文内默认 <strong><code>bitnami/kafka:3.7</code></strong>（<strong><code>KAFKA_CFG_*</code></strong>）；可选用 <strong><code>apache/kafka</code></strong>（<strong><code>KAFKA_*</code></strong>）</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong> 后一条 <strong><code>docker run ...</code></strong>（<strong><code>ADVERTISED</code></strong> 指向 <strong><code>127.0.0.1</code></strong>，仅本机客户端）</td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td><strong><code>docker-compose.kafka.yml</code></strong> 双监听：<strong><code>kafka:9092</code></strong> + <strong><code>localhost:9094</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td><strong><code>kafka-k8s.yaml</code></strong>：<strong>PVC + Deployment + Headless Service</strong>；<strong><code>ADVERTISED</code></strong> 与 <strong>Service</strong> 名一致</td>
</tr>
</tbody></table>
<p>生产环境请启用 <strong>SASL&#x2F;TLS</strong>、<strong>ACL</strong>、监控与备份；<strong>KRaft</strong> 与 <strong>ZooKeeper</strong> 迁移、<strong>Controller</strong> 仲裁数等以 <strong>Apache Kafka</strong> 官方文档为准。</p>
]]></content>
      <categories>
        <category>MQ</category>
      </categories>
      <tags>
        <tag>Kafka</tag>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 RocketMQ</title>
    <url>/2026/05/01/MQ/RocketMQ-Docker/</url>
    <content><![CDATA[<p><strong>Apache RocketMQ</strong> 在 <strong>Docker</strong> 中至少需要 <strong>NameServer</strong> 与 <strong>Broker</strong> 两个进程（常见为两个容器）。下文给出<strong>镜像选型</strong>、<strong>三条命令起最小栈</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；<strong>Broker</strong> 监听与 <strong><code>brokerIP1</code></strong> 等请按部署环境调整，勿把生产敏感配置提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、RocketMQ-镜像选择"><a href="#二、RocketMQ-镜像选择" class="headerlink" title="二、RocketMQ 镜像选择"></a>二、RocketMQ 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>apache/rocketmq</code></strong>（Docker Hub：<a href="https://hub.docker.com/r/apache/rocketmq/tags">https://hub.docker.com/r/apache/rocketmq/tags</a>），与 <strong>Apache RocketMQ</strong> 发行版本对应。  </li>
<li><strong>常用标签</strong>：如 <strong><code>apache/rocketmq:5.3.2</code></strong>（下文以此为例；升级时请同时核对容器内 <strong><code>ROCKETMQ_HOME</code></strong> 下 <strong><code>conf</code></strong> 路径是否随版本变化）。  </li>
<li><strong>生产建议</strong>：固定补丁级标签或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移。</li>
</ul>
<h2 id="2-角色与端口（开发常用）"><a href="#2-角色与端口（开发常用）" class="headerlink" title="2. 角色与端口（开发常用）"></a>2. 角色与端口（开发常用）</h2><table>
<thead>
<tr>
<th>组件</th>
<th>说明</th>
<th>常见映射端口（示例）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>NameServer</strong></td>
<td>路由注册与发现</td>
<td><strong>9876</strong></td>
</tr>
<tr>
<td><strong>Broker</strong></td>
<td>消息存储与转发</td>
<td><strong>10909</strong>、<strong>10911</strong>、<strong>10912</strong> 等（与 <strong><code>broker.conf</code></strong> &#x2F; 启动参数有关）</td>
</tr>
<tr>
<td><strong>Proxy</strong>（<strong>5.x</strong> 可选）</td>
<td><strong>gRPC</strong> 接入等（官方 <strong>Docker</strong> 快速入门常与 <strong><code>--enable-proxy</code></strong> 一并启用）</td>
<td><strong>8080</strong>、<strong>8081</strong> 等</td>
</tr>
</tbody></table>
<h2 id="3-与官方「Broker-Proxy」快速入门的关系"><a href="#3-与官方「Broker-Proxy」快速入门的关系" class="headerlink" title="3. 与官方「Broker + Proxy」快速入门的关系"></a>3. 与官方「Broker + Proxy」快速入门的关系</h2><p><strong>Apache</strong> 文档推荐挂载 <strong><code>broker.conf</code></strong> 并执行 <strong><code>mqbroker --enable-proxy</code></strong>（见 <a href="https://rocketmq.apache.org/docs/quickStart/02quickstartWithDocker/">Run RocketMQ in Docker</a>）。下文 <strong>Compose</strong> 提供<strong>带 Proxy</strong> 的完整示例；若仅需经典 <strong>Remoting</strong> 客户端，可将 <strong>Broker</strong> 的 <strong><code>command</code></strong> 改为 <strong><code>sh mqbroker -n &lt;namesrv&gt;:9876</code></strong> 并去掉 <strong>Proxy</strong> 相关端口（需自行验证与客户端版本匹配）。</p>
<h2 id="4-资源与持久化"><a href="#4-资源与持久化" class="headerlink" title="4. 资源与持久化"></a>4. 资源与持久化</h2><p><strong>Broker</strong> 磁盘与堆内存占用随主题、队列与流量增长较快；<strong>Compose</strong> 与 <strong>K8s</strong> 中为 <strong>Broker</strong> 数据目录挂载卷，并酌情提高 <strong><code>limits</code></strong>。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式跑 <strong>Apache RocketMQ</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>RocketMQ</strong> 一键部署：由面板编排 <strong>NameServer</strong>、<strong>Broker</strong>（及上架版本若含的 <strong>Proxy</strong>）与数据卷、端口。与下文 <strong><code>docker</code> &#x2F; Compose &#x2F; K8s<code>** 中的 **</code>broker.conf<code>**、**</code>NAMESRV_ADDR<code>**、**</code>brokerIP1&#96;</strong> 与端口映射对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等场景。</p>
<hr>
<h1 id="三、最少命令启动（NameServer-Broker）"><a href="#三、最少命令启动（NameServer-Broker）" class="headerlink" title="三、最少命令启动（NameServer + Broker）"></a>三、最少命令启动（NameServer + Broker）</h1><p><strong>RocketMQ</strong> 无单容器一体运行官方形态，下面用<strong>三条命令</strong>完成最小栈（与 <a href="https://rocketmq.apache.org/docs/quickStart/02quickstartWithDocker/">官方 Docker 快速入门</a> 思路一致：<strong>网络 → NameServer → Broker+Proxy</strong>）。</p>
<h2 id="1-准备网络与-broker-conf（当前目录）"><a href="#1-准备网络与-broker-conf（当前目录）" class="headerlink" title="1. 准备网络与 broker.conf（当前目录）"></a>1. 准备网络与 <code>broker.conf</code>（当前目录）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker network create rocketmq</span><br><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;%s\n&#x27;</span> <span class="string">&#x27;brokerClusterName=DefaultCluster&#x27;</span> <span class="string">&#x27;brokerName=broker-a&#x27;</span> <span class="string">&#x27;brokerId=0&#x27;</span> <span class="string">&#x27;deleteWhen=04&#x27;</span> <span class="string">&#x27;fileReservedTime=48&#x27;</span> <span class="string">&#x27;brokerRole=ASYNC_MASTER&#x27;</span> <span class="string">&#x27;flushDiskType=ASYNC_FLUSH&#x27;</span> <span class="string">&#x27;brokerIP1=127.0.0.1&#x27;</span> &gt; broker.conf</span><br></pre></td></tr></table></figure>

<p><strong><code>brokerIP1</code></strong>：仅从<strong>本机</strong>客户端访问映射端口时，常用 <strong><code>127.0.0.1</code></strong>；若客户端在其它机器或 <strong>K8s Pod</strong> 内，需改为可达的宿主机 <strong>IP</strong> 或 <strong>Service</strong> 地址，并同步 <strong>Broker</strong> 监听与防火墙策略。</p>
<h2 id="2-启动-NameServer"><a href="#2-启动-NameServer" class="headerlink" title="2. 启动 NameServer"></a>2. 启动 NameServer</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name rmqnamesrv -p 9876:9876 --network rocketmq apache/rocketmq:5.3.2 sh mqnamesrv</span><br></pre></td></tr></table></figure>

<h2 id="3-启动-Broker（含-Proxy，挂载-broker-conf）"><a href="#3-启动-Broker（含-Proxy，挂载-broker-conf）" class="headerlink" title="3. 启动 Broker（含 Proxy，挂载 broker.conf）"></a>3. 启动 Broker（含 Proxy，挂载 <code>broker.conf</code>）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name rmqbroker --network rocketmq -p 10912:10912 -p 10911:10911 -p 10909:10909 -p 8080:8080 -p 8081:8081 -e NAMESRV_ADDR=rmqnamesrv:9876 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/broker.conf:/home/rocketmq/rocketmq-5.3.2/conf/broker.conf&quot;</span> apache/rocketmq:5.3.2 sh mqbroker --enable-proxy -c /home/rocketmq/rocketmq-5.3.2/conf/broker.conf</span><br></pre></td></tr></table></figure>

<p>常用运维：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f rmqnamesrv</span><br><span class="line">docker logs -f rmqbroker</span><br><span class="line">docker stop rmqbroker rmqnamesrv &amp;&amp; docker <span class="built_in">rm</span> rmqbroker rmqnamesrv &amp;&amp; docker network <span class="built_in">rm</span> rocketmq</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面 <strong><code>broker.conf</code></strong> 内容保存为当前目录 <strong><code>broker.conf</code></strong>（与上节 <strong><code>printf</code></strong> 示例等价即可），再将 <strong>Compose</strong> 文件保存为 <strong><code>docker-compose.rocketmq.yml</code></strong>。</p>
<p><strong>一条命令</strong>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.rocketmq.yml up -d</span><br></pre></td></tr></table></figure>

<p><strong><code>docker-compose.rocketmq.yml</code></strong> 全文：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">rocketmq-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">rmqnamesrv:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.2</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">rmqnamesrv</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">sh</span> <span class="string">mqnamesrv</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9876:9876&quot;</span></span><br><span class="line">    <span class="attr">networks:</span> [<span class="string">rocketmq</span>]</span><br><span class="line"></span><br><span class="line">  <span class="attr">broker:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.2</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">rmqbroker</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">rmqnamesrv</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">NAMESRV_ADDR:</span> <span class="string">rmqnamesrv:9876</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">sh</span> <span class="string">mqbroker</span> <span class="string">--enable-proxy</span> <span class="string">-c</span> <span class="string">/home/rocketmq/rocketmq-5.3.2/conf/broker.conf</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10912:10912&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10911:10911&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10909:10909&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:8080&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8081:8081&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./broker.conf:/home/rocketmq/rocketmq-5.3.2/conf/broker.conf:ro</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">broker_store:/home/rocketmq/store</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">broker_logs:/home/rocketmq/logs</span></span><br><span class="line">    <span class="attr">networks:</span> [<span class="string">rocketmq</span>]</span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;bash -c &#x27;&lt;/dev/tcp/127.0.0.1/10911&#x27; || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">15s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">20</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">90s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">broker_store:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">broker_logs:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line"></span><br><span class="line"><span class="attr">networks:</span></span><br><span class="line">  <span class="attr">rocketmq:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">bridge</span></span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止（保留命名卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.rocketmq.yml ps</span><br><span class="line">docker compose -f docker-compose.rocketmq.yml logs -f broker</span><br><span class="line">docker compose -f docker-compose.rocketmq.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down -v</code></strong> 会删除 <strong><code>broker_store</code></strong> 等卷上的消息数据。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将 <strong><code>broker.conf</code></strong> 以 <strong><code>ConfigMap</code></strong> 挂载；<strong>NameServer</strong> 与 <strong>Broker</strong> 分 <strong>Deployment</strong>，<strong>Broker</strong> 使用 <strong>PVC</strong> 持久化 <strong><code>/home/rocketmq/store</code></strong>。将下面保存为 <strong><code>rocketmq-k8s.yaml</code></strong> 后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f rocketmq-k8s.yaml</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：为简化依赖，<strong>Broker</strong> 使用 <strong><code>mqbroker -n rocketmq-namesrv.default.svc.cluster.local:9876</code></strong>（<strong>未启用 Proxy</strong>）；若需与官方文档一致的 <strong>Proxy</strong>，请在镜像内 <strong><code>ROCKETMQ_HOME</code></strong> 下增加 <strong><code>broker.conf</code></strong> 与 <strong><code>--enable-proxy</code></strong> 等参数并开放对应端口。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ConfigMap</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-broker-conf</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="attr">broker.conf:</span> <span class="string">|</span></span><br><span class="line"><span class="string">    brokerClusterName=DefaultCluster</span></span><br><span class="line"><span class="string">    brokerName=broker-a</span></span><br><span class="line"><span class="string">    brokerId=0</span></span><br><span class="line"><span class="string">    deleteWhen=04</span></span><br><span class="line"><span class="string">    fileReservedTime=48</span></span><br><span class="line"><span class="string">    brokerRole=ASYNC_MASTER</span></span><br><span class="line"><span class="string">    flushDiskType=ASYNC_FLUSH</span></span><br><span class="line"><span class="string">    brokerIP1=0.0.0.0</span></span><br><span class="line"><span class="string"></span><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-broker-store</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">20Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">namesrv</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.2</span></span><br><span class="line">          <span class="attr">command:</span> [<span class="string">&quot;sh&quot;</span>, <span class="string">&quot;mqnamesrv&quot;</span>]</span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9876</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">namesrv</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">9876</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">9876</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">30</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">15</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;256Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;100m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;512Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;500m&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-namesrv</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">namesrv</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9876</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9876</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-broker</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-broker</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">rocketmq-broker</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">rocketmq-broker</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">initContainers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">wait-namesrv</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">busybox:1.36</span></span><br><span class="line">          <span class="attr">command:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">sh</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;until nc -z rocketmq-namesrv 9876; do echo waiting namesrv; sleep 2; done&quot;</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">broker</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.2</span></span><br><span class="line">          <span class="attr">command:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">sh</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">mqbroker</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">-n</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">rocketmq-namesrv.default.svc.cluster.local:9876</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">/home/rocketmq/rocketmq-5.3.2/conf/broker.conf</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">10909</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">vip</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">10911</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">main</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">10912</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">ha</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NAMESRV_ADDR</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">rocketmq-namesrv.default.svc.cluster.local:9876</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">broker-conf</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/rocketmq/rocketmq-5.3.2/conf/broker.conf</span></span><br><span class="line">              <span class="attr">subPath:</span> <span class="string">broker.conf</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">broker-store</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/rocketmq/store</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">10911</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">40</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">30</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">10911</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">120</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">20</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;500m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;2Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;2000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">broker-conf</span></span><br><span class="line">          <span class="attr">configMap:</span></span><br><span class="line">            <span class="attr">name:</span> <span class="string">rocketmq-broker-conf</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">broker-store</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">rocketmq-broker-store</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">rocketmq-broker</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-broker</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">rocketmq-broker</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">vip</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">10909</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">10909</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">main</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">10911</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">10911</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">ha</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">10912</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">10912</span></span><br></pre></td></tr></table></figure>

<p>查看资源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get deploy,svc,pvc</span><br></pre></td></tr></table></figure>

<p>集群内 <strong>NameServer</strong> 地址：<strong><code>rocketmq-namesrv.default.svc.cluster.local:9876</code></strong>；<strong>Broker</strong> 主端口：<strong><code>rocketmq-broker.default.svc.cluster.local:10911</code></strong>（按 <strong>namespace</strong> 调整）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本同时挂载；多 <strong>Master</strong>、<strong>DLedger</strong>、<strong>Controller</strong> 与运维自动化请使用 <strong>RocketMQ Operator</strong> 或官方推荐拓扑，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td><strong><code>apache/rocketmq:5.3.2</code></strong>；升级时核对 <strong><code>conf</code></strong> 路径与 <strong>Proxy</strong> 需求</td>
</tr>
<tr>
<td><strong>最小编排</strong></td>
<td><strong><code>docker network create</code></strong> → <strong><code>rmqnamesrv</code></strong> → <strong><code>rmqbroker</code></strong>（三条命令）</td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td><strong><code>broker.conf</code></strong> + <strong><code>docker-compose.rocketmq.yml</code></strong> → <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td><strong><code>rocketmq-k8s.yaml</code></strong>：<strong>ConfigMap + PVC + 两套 Deploy&#x2F;Service&#96;</strong></td>
</tr>
</tbody></table>
<p>生产环境请启用 <strong>ACL</strong>、<strong>TLS</strong>、监控与容量规划；<strong>5.x</strong> 新客户端与 <strong>Proxy</strong> 接入请参考 <strong>Apache RocketMQ</strong> 官方文档。</p>
]]></content>
      <categories>
        <category>MQ</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ</title>
    <url>/2026/05/01/MQ/RocketMQ/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>MQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka</title>
    <url>/2026/05/01/MQ/kafka/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>MQ</category>
      </categories>
      <tags>
        <tag>Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 ZooKeeper</title>
    <url>/2026/05/01/Midware/Zookeeper-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>Apache ZooKeeper</strong>，适合本地开发、为 <strong>Kafka</strong> 等组件提供协调服务、或在 <strong>Kubernetes</strong> 中单节点验证。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；数据卷与 <strong><code>ZOO_SERVERS</code></strong> 等集群参数请按环境自行调整，勿把生产敏感配置提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、ZooKeeper-镜像选择"><a href="#二、ZooKeeper-镜像选择" class="headerlink" title="二、ZooKeeper 镜像选择"></a>二、ZooKeeper 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>zookeeper</code></strong>（Docker Hub 官方库：<a href="https://hub.docker.com/_/zookeeper/tags">https://hub.docker.com/_/zookeeper/tags</a>）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>zookeeper:3.9</code></strong>：<strong>3.9.x</strong> 线，与多数依赖 <strong>3.5+ &#x2F; 3.6+</strong> 的组件兼容。  </li>
<li><strong><code>zookeeper:3.8</code></strong>：仍广泛使用的上一大版本线。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定小版本标签（如 <strong><code>zookeeper:3.9.2</code></strong>）或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>单机 &#x2F; 集群</strong></td>
<td>单机只需 <strong><code>ZOO_MY_ID</code></strong>（常为 <strong><code>1</code></strong>）；多节点需 <strong><code>ZOO_SERVERS</code></strong> 等与 <strong><code>myid</code></strong> 一致的集群配置（见 <a href="https://hub.docker.com/_/zookeeper">官方镜像说明</a>）。</td>
</tr>
<tr>
<td><strong>客户端端口</strong></td>
<td>默认 <strong><code>2181</code></strong>（<strong><code>ZOO_PORT</code></strong> 可改）。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>官方 <strong><code>zookeeper</code></strong> 多架构镜像一般可直接拉取；若某标签缺架构，换标签或查 Hub <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区有 <strong><code>bitnami/zookeeper</code></strong> 等，环境变量名与数据路径与官方不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-数据目录与事务日志"><a href="#3-数据目录与事务日志" class="headerlink" title="3. 数据目录与事务日志"></a>3. 数据目录与事务日志</h2><p>镜像默认 <strong><code>ZOO_DATA_DIR</code></strong>（默认 <strong><code>/data</code></strong>）存放数据快照等，<strong><code>ZOO_DATA_LOG_DIR</code></strong>（默认 <strong><code>/datalog</code></strong>）存放事务 <strong>log</strong>；持久化时建议<strong>分别挂载</strong>两目录到卷或绑定目录，便于备份与磁盘规划。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以 <strong>Docker</strong> 管理 <strong>ZooKeeper</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「容器」</strong> 中用 <strong>Compose</strong> 维护与 <strong># 四</strong> 一致或等价的编排（镜像选 <strong><code>zookeeper</code></strong> 官方标签、<strong><code>/data</code></strong> 与 <strong><code>/datalog</code></strong> 卷、<strong><code>ZOO_*</code></strong> 与 <strong><code>2181</code></strong>），启停与日志在界面查看；若在 <strong>「应用商店」</strong> 中能检索到独立 <strong>ZooKeeper</strong> 亦可一键安装（以面板当前上架为准）。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s&#96;</strong> 对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等场景。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>在<strong>当前目录</strong>先建数据与日志目录（仅首次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./zk-data ./zk-logs</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（请按需改端口与卷路径；示例仅供本地）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name zookeeper-dev -p 2181:2181 -e ZOO_MY_ID=1 -e ZOO_TICK_TIME=2000 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/zk-data:/data&quot;</span> -v <span class="string">&quot;<span class="subst">$(pwd)</span>/zk-logs:/datalog&quot;</span> zookeeper:3.9</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f zookeeper-dev</span><br><span class="line">docker stop zookeeper-dev &amp;&amp; docker <span class="built_in">rm</span> zookeeper-dev</span><br></pre></td></tr></table></figure>

<p>本机 <strong>四字命令</strong> 探测（返回 <strong><code>imok</code></strong> 表示进程正常）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> ruok | nc 127.0.0.1 2181</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.zookeeper.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。</p>
<p><strong>一条命令</strong>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.zookeeper.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.zookeeper.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">zookeeper-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">zookeeper:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">zookeeper:3.9</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">zookeeper-3</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;2181:2181&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">ZOO_MY_ID:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">      <span class="attr">ZOO_TICK_TIME:</span> <span class="string">&quot;2000&quot;</span></span><br><span class="line">      <span class="attr">ZOO_AUTOPURGE_PURGEINTERVAL:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">zk_data:/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">zk_logs:/datalog</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;echo ruok | nc -w 2 127.0.0.1 2181 | grep imok || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">6</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">30s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">zk_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">zk_logs:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.zookeeper.yml ps</span><br><span class="line">docker compose -f docker-compose.zookeeper.yml logs -f zookeeper</span><br><span class="line">docker compose -f docker-compose.zookeeper.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留 <strong><code>zk_data</code> &#x2F; <code>zk_logs</code></strong>；若需清空数据加 <strong><code>docker compose -f docker-compose.zookeeper.yml down -v</code></strong>（<strong>会删协调数据</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>zookeeper-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f zookeeper-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>PVC</strong>（数据与事务日志分卷）、<strong>Deployment</strong>、<strong>ClusterIP Service（2181）</strong>。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">zookeeper-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">10Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">zookeeper-datalog</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">10Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">zookeeper</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">zookeeper</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">zookeeper</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">zookeeper</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">zookeeper</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">zookeeper:3.9</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">2181</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">client</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">ZOO_MY_ID</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">ZOO_TICK_TIME</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;2000&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">ZOO_AUTOPURGE_PURGEINTERVAL</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/data</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">datalog</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/datalog</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">2181</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">12</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">2181</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">40</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">20</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;256Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;100m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;1000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">zookeeper-data</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">datalog</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">zookeeper-datalog</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">zookeeper</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">zookeeper</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">zookeeper</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">client</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">2181</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">2181</span></span><br></pre></td></tr></table></figure>

<p>查看资源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get deploy,svc,pvc -l app=zookeeper</span><br></pre></td></tr></table></figure>

<p>集群内连接串示例：<strong><code>zookeeper.default.svc.cluster.local:2181</code></strong>（按 <strong>namespace</strong> 调整）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；<strong>ZooKeeper</strong> 奇数节点仲裁、跨可用区与证书认证等请使用 <strong>StatefulSet</strong> 或专用 <strong>Operator</strong>，勿直接水平扩展上述 <strong>Deployment</strong> 当作集群使用。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先官方 <strong><code>zookeeper:3.9</code></strong> &#x2F; 固定补丁标签；单机 <strong><code>ZOO_MY_ID=1</code></strong></td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong> 后一条 <strong><code>docker run ...</code></strong>（挂载 <strong><code>/data</code></strong> 与 <strong><code>/datalog</code></strong>）</td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.zookeeper.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>zookeeper-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请配置 <strong>ACL &#x2F; TLS</strong>、监控与备份；与 <strong>Kafka</strong> 同栈时保持 <strong><code>zookeeper</code></strong> 版本与 <strong>Kafka</strong> 发行说明中的兼容性要求。</p>
]]></content>
      <categories>
        <category>Midware</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>ZooKeeper</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 MongoDB</title>
    <url>/2026/05/01/MongoDB/MongoDB-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>MongoDB</strong>，适合本地开发、与 <strong>Compose</strong> 编排其它服务、或在 <strong>Kubernetes</strong> 中单实例部署。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；密码与数据卷请按环境自行替换，勿把生产密钥提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、MongoDB-镜像选择"><a href="#二、MongoDB-镜像选择" class="headerlink" title="二、MongoDB 镜像选择"></a>二、MongoDB 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>mongo</code></strong>（Docker Hub 官方库，由 <strong>MongoDB Inc.</strong> 维护）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>mongo:7.0</code></strong>：<strong>7.x</strong> 稳定线，驱动与工具链成熟。  </li>
<li><strong><code>mongo:8.0</code></strong> 或 <strong><code>mongo:latest</code></strong>：较新大版本（以 <a href="https://hub.docker.com/_/mongo/tags">Docker Hub mongo tags</a> 为准）。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定小版本标签（如 <strong><code>mongo:7.0.15</code></strong>）或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>认证与 root</strong></td>
<td>同时设置 <strong><code>MONGO_INITDB_ROOT_USERNAME</code></strong>、<strong><code>MONGO_INITDB_ROOT_PASSWORD</code></strong> 时启用<strong>超级用户</strong>；连接串中 <strong><code>authSource=admin</code></strong> 常指向管理库。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>官方 <strong><code>mongo</code></strong> 多架构镜像一般可直接拉取；若某标签缺架构，换标签或查 Hub <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区有 <strong><code>bitnami/mongodb</code></strong> 等，环境变量与数据路径与官方不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-初始化脚本"><a href="#3-初始化脚本" class="headerlink" title="3. 初始化脚本"></a>3. 初始化脚本</h2><p><strong><code>/docker-entrypoint-initdb.d/</code></strong> 下 <strong><code>*.js</code></strong>、<strong><code>*.sh</code></strong> 仅在<strong>首次初始化空数据目录</strong>时执行；已有数据卷时不会重复执行。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式跑 <strong>MongoDB</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>MongoDB</strong> 一键部署：由面板管理镜像版本、数据持久化、<strong><code>27017</code></strong> 端口、<strong><code>MONGO_INITDB_ROOT_*</code></strong> 与（可选）备份，启停与日志可在界面查看。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s<code>** 的环境变量、**</code>&#x2F;data&#x2F;db<code>** 与 **</code>&#x2F;docker-entrypoint-initdb.d&#96;</strong> 语义对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等「一台机子上稳态跑库」的场景。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>在<strong>当前目录</strong>先建数据与初始化目录（仅首次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./mongo-data ./mongo-init</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（请按需改账号、密码、端口、卷路径；示例仅供本地）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name mongodb-dev -e MONGO_INITDB_ROOT_USERNAME=root -e MONGO_INITDB_ROOT_PASSWORD=dev-root-change-me -e MONGO_INITDB_DATABASE=appdb -p 27017:27017 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/mongo-data:/data/db&quot;</span> -v <span class="string">&quot;<span class="subst">$(pwd)</span>/mongo-init:/docker-entrypoint-initdb.d:ro&quot;</span> mongo:7.0</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f mongodb-dev</span><br><span class="line">docker stop mongodb-dev &amp;&amp; docker <span class="built_in">rm</span> mongodb-dev</span><br></pre></td></tr></table></figure>

<p>本机连接示例（<strong><code>mongosh</code></strong> 与镜像主版本一致即可）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mongosh <span class="string">&quot;mongodb://root:dev-root-change-me@127.0.0.1:27017/appdb?authSource=admin&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.mongo.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。敏感项建议放在同目录 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>），勿长期写死在仓库。</p>
<p><strong>一条命令</strong>启动全栈（在 <strong><code>docker-compose.mongo.yml</code></strong> 所在目录执行）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.mongo.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.mongo.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">mongo-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mongodb:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mongo:7.0</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">mongo-7</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MONGO_INITDB_ROOT_USERNAME:</span> <span class="string">$&#123;MONGO_INITDB_ROOT_USERNAME:-root&#125;</span></span><br><span class="line">      <span class="attr">MONGO_INITDB_ROOT_PASSWORD:</span> <span class="string">$&#123;MONGO_INITDB_ROOT_PASSWORD:-dev-root-change-me&#125;</span></span><br><span class="line">      <span class="attr">MONGO_INITDB_DATABASE:</span> <span class="string">$&#123;MONGO_INITDB_DATABASE:-appdb&#125;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;27017:27017&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mongo_data:/data/db</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./mongo-init:/docker-entrypoint-initdb.d:ro</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;mongosh -u \&quot;$$MONGO_INITDB_ROOT_USERNAME\&quot; -p \&quot;$$MONGO_INITDB_ROOT_PASSWORD\&quot; --authenticationDatabase admin --quiet --eval &#x27;db.adminCommand(&#123;ping:1&#125;)&#x27; || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">12</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">40s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">mongo_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>同目录可选 <strong><code>.env</code></strong>（勿提交）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MONGO_INITDB_ROOT_USERNAME=root</span><br><span class="line">MONGO_INITDB_ROOT_PASSWORD=your-strong-root-password</span><br><span class="line">MONGO_INITDB_DATABASE=appdb</span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.mongo.yml ps</span><br><span class="line">docker compose -f docker-compose.mongo.yml logs -f mongodb</span><br><span class="line">docker compose -f docker-compose.mongo.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留命名卷 <strong><code>mongo_data</code></strong>；若需清空数据加 <strong><code>docker compose ... down -v</code></strong>（<strong>会删库</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>mongo-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f mongo-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>Secret</strong>（<strong>root</strong> 账号与初始库名）、<strong>PVC</strong>、<strong>Deployment</strong>、<strong>ClusterIP Service</strong>。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mongo-secret</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">MONGO_INITDB_ROOT_USERNAME:</span> <span class="string">&quot;root&quot;</span></span><br><span class="line">  <span class="attr">MONGO_INITDB_ROOT_PASSWORD:</span> <span class="string">&quot;请替换为强密码&quot;</span></span><br><span class="line">  <span class="attr">MONGO_INITDB_DATABASE:</span> <span class="string">&quot;appdb&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mongo-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">20Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mongodb</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mongodb</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">mongodb</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">mongodb</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mongodb</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">mongo:7.0</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">27017</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">mongodb</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MONGO_INITDB_ROOT_USERNAME</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mongo-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MONGO_INITDB_ROOT_USERNAME</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MONGO_INITDB_ROOT_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mongo-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MONGO_INITDB_ROOT_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MONGO_INITDB_DATABASE</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mongo-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MONGO_INITDB_DATABASE</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;Asia/Shanghai&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/data/db</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">exec:</span></span><br><span class="line">              <span class="attr">command:</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">/bin/sh</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">&#x27;mongosh --quiet -u &quot;$MONGO_INITDB_ROOT_USERNAME&quot; -p &quot;$MONGO_INITDB_ROOT_PASSWORD&quot; --authenticationDatabase admin --eval &quot;db.adminCommand(&#123; ping: 1 &#125;)&quot;&#x27;</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">12</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">27017</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">60</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;512Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;250m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;1000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">mongo-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mongodb</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mongodb</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mongodb</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mongodb</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">27017</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">27017</span></span><br></pre></td></tr></table></figure>

<p>查看资源与连接前检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get pods,svc,pvc -l app=mongodb</span><br></pre></td></tr></table></figure>

<p>集群内连接串示例（按 <strong>namespace</strong> 与 <strong>Secret</strong> 中密码替换）：<strong><code>mongodb://root:请替换@mongodb.default.svc.cluster.local:27017/appdb?authSource=admin</code></strong>。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；副本集、分片、运维自动化请使用 <strong>MongoDB Kubernetes Operator</strong> 等方案，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先官方 <strong><code>mongo:7.0</code></strong> &#x2F; 固定补丁标签；<strong><code>mongosh</code></strong> 与镜像主版本对齐</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong> 后一条 <strong><code>docker run ...</code></strong></td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.mongo.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>mongo-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请配合备份、监控、网络策略与 <strong>TLS</strong>；<strong>Secret</strong> 建议对接 <strong>External Secrets</strong> &#x2F; <strong>Sealed Secrets</strong> 等管控流程。</p>
]]></content>
      <categories>
        <category>MongoDB</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>MongoDB</tag>
      </tags>
  </entry>
  <entry>
    <title>MongoDB</title>
    <url>/2026/05/01/MongoDB/MongoDB/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>MongoDB</category>
      </categories>
      <tags>
        <tag>MongoDB</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 MySQL</title>
    <url>/2026/05/01/MySQL/MySQL-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>MySQL</strong>，适合本地开发、与 <strong>Compose</strong> 编排其它服务、或在 <strong>Kubernetes</strong> 中单实例部署。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；密码与数据卷请按环境自行替换，勿把生产密钥提交进仓库。多行命令：<strong>Linux &#x2F; macOS &#x2F; Git Bash</strong> 用行末 <strong><code>\</code>** 续行；**Windows CMD** 用行末 **<code>^</code>** 续行（**<code>^</code> 后不能有空格</strong>）。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、MySQL-镜像选择"><a href="#二、MySQL-镜像选择" class="headerlink" title="二、MySQL 镜像选择"></a>二、MySQL 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>mysql</code></strong>（Docker Hub 官方库，由 <strong>Oracle</strong> &#x2F; <strong>MySQL</strong> 团队维护）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>mysql:8.0</code></strong>：<strong>8.0</strong> 长期维护线，生态兼容面广（<strong>WordPress</strong>、旧驱动等多以 <strong>8.0</strong> 为基准）。  </li>
<li><strong><code>mysql:8.4</code></strong> 或 <strong><code>mysql:lts</code></strong>：较新的 <strong>LTS</strong> 线（以 <a href="https://hub.docker.com/_/mysql/tags">Docker Hub mysql tags</a> 为准）。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定小版本标签（如 <strong><code>mysql:8.0.40</code></strong>）或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>默认认证插件</strong></td>
<td><strong>8.0</strong> 起默认 <strong><code>caching_sha2_password</code></strong>；老客户端不支持时需调整 <strong><code>--default-authentication-plugin</code></strong> 或升级客户端（<strong>MySQL 8.4</strong> 已移除 <strong><code>mysql_native_password</code></strong>，迁移前查发行说明）。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>官方 <strong><code>mysql</code></strong> 多架构镜像一般可直接拉取；若某标签缺架构，换标签或查 Hub <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区有 <strong><code>bitnami/mysql</code></strong> 等，参数与路径与官方不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-字符集与排序规则"><a href="#3-字符集与排序规则" class="headerlink" title="3. 字符集与排序规则"></a>3. 字符集与排序规则</h2><p>容器内建议在 <strong><code>command</code></strong> 或 <strong><code>my.cnf</code></strong> 中显式指定 <strong><code>utf8mb4</code></strong>，与业务库一致，避免默认与连接串不一致。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式运行 <strong>MySQL</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>MySQL</strong> 一键部署：由面板负责镜像版本、数据持久化、端口映射与（可选）计划备份，启停与日志可在界面查看。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s</strong> 中的环境变量、卷与字符集语义对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等「一台机子上稳态跑库」的场景。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p><strong>一条命令</strong>启动（与下述参数一致；<strong>命名卷</strong> <strong><code>mysql-data</code></strong>、<strong><code>mysql-init</code></strong> 在首次运行时会由 <strong>Docker</strong> 自动创建，无需在宿主机 <strong><code>mkdir</code></strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d   --name mysql-dev   -e MYSQL_ROOT_PASSWORD=<span class="string">&quot;pi=3.1415&quot;</span>   -e MYSQL_DATABASE=<span class="string">&quot;app&quot;</span>   -e MYSQL_USER=<span class="string">&quot;admin&quot;</span>   -e MYSQL_PASSWORD=<span class="string">&quot;pi=3.1415&quot;</span>   -p 3306:3306   -v mysql-data:/var/lib/mysql   -v mysql-init:/docker-entrypoint-initdb.d:ro   mysql:8.0   --character-set-server=utf8mb4   --collation-server=utf8mb4_unicode_ci</span><br></pre></td></tr></table></figure>

<p>多行可读（与上条等价；按终端选一种）：</p>
<h2 id="Linux-macOS-Git-Bash（-续行）"><a href="#Linux-macOS-Git-Bash（-续行）" class="headerlink" title="Linux &#x2F; macOS &#x2F; Git Bash（\ 续行）"></a>Linux &#x2F; macOS &#x2F; Git Bash（<code>\</code> 续行）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d\</span><br><span class="line">  --name mysql-dev\</span><br><span class="line">  -e MYSQL_ROOT_PASSWORD=<span class="string">&quot;pi=3.1415&quot;</span>\</span><br><span class="line">  -e MYSQL_DATABASE=<span class="string">&quot;app&quot;</span>\</span><br><span class="line">  -e MYSQL_USER=<span class="string">&quot;admin&quot;</span>\</span><br><span class="line">  -e MYSQL_PASSWORD=<span class="string">&quot;pi=3.1415&quot;</span>\</span><br><span class="line">  -p 3306:3306\</span><br><span class="line">  -v mysql-data:/var/lib/mysql\</span><br><span class="line">  -v mysql-init:/docker-entrypoint-initdb.d:ro\</span><br><span class="line">  mysql:8.0\</span><br><span class="line">  --character-set-server=utf8mb4\</span><br><span class="line">  --collation-server=utf8mb4_unicode_ci</span><br></pre></td></tr></table></figure>

<h2 id="Windows-CMD（-续行）"><a href="#Windows-CMD（-续行）" class="headerlink" title="Windows CMD（^ 续行）"></a>Windows CMD（<code>^</code> 续行）</h2><figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">docker run -d^</span><br><span class="line">  --name mysql-dev^</span><br><span class="line">  -e MYSQL_ROOT_PASSWORD=&quot;pi=<span class="number">3</span>.<span class="number">1415</span>&quot;^</span><br><span class="line">  -e MYSQL_DATABASE=&quot;app&quot;^</span><br><span class="line">  -e MYSQL_USER=&quot;admin&quot;^</span><br><span class="line">  -e MYSQL_PASSWORD=&quot;pi=<span class="number">3</span>.<span class="number">1415</span>&quot;^</span><br><span class="line">  -p <span class="number">3306</span>:<span class="number">3306</span>^</span><br><span class="line">  -v mysql-data:/var/lib/mysql^</span><br><span class="line">  -v mysql-init:/docker-entrypoint-initdb.d:ro^</span><br><span class="line"><span class="function">  mysql:8.0^</span></span><br><span class="line"><span class="function">  --<span class="title">character</span>-<span class="title">set</span>-<span class="title">server</span>=<span class="title">utf8mb4</span>^</span></span><br><span class="line"><span class="function">  --<span class="title">collation</span>-<span class="title">server</span>=<span class="title">utf8mb4_unicode_ci</span></span></span><br></pre></td></tr></table></figure>

<p><strong>Windows 说明</strong>：<strong>Docker Desktop</strong> 下命名卷写法与 <strong>Linux</strong> 一致；若在 <strong>CMD</strong> 中路径或引号报错，可改用 <strong>PowerShell</strong>（行末续行符为 <strong><code>`</code></strong>）或仍用<strong>一条命令</strong>单行复制。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>-e MYSQL_*</code></strong></td>
<td><strong>root</strong> 与应用库 <strong><code>app</code></strong>、用户 <strong><code>admin</code></strong> 的密码（示例为 <strong><code>pi=3.1415</code></strong>，含 <strong><code>=</code></strong> 故用引号包住）</td>
</tr>
<tr>
<td><strong><code>-v mysql-data:</code></strong></td>
<td>数据目录持久化到 <strong>Docker 命名卷</strong> <strong><code>mysql-data</code></strong></td>
</tr>
<tr>
<td><strong><code>-v mysql-init:</code></strong></td>
<td>初始化脚本目录挂载为<strong>只读</strong>命名卷 <strong><code>mysql-init</code></strong>；<strong><code>.sql</code> &#x2F; <code>.sh</code></strong> 仅在<strong>首次空数据目录</strong>时执行</td>
</tr>
<tr>
<td>末尾 <strong><code>--character-*</code></strong></td>
<td>传给 <strong><code>mysqld</code></strong>：服务端 <strong>utf8mb4</strong> &#x2F; <strong><code>utf8mb4_unicode_ci</code></strong></td>
</tr>
</tbody></table>
<p>若要把 <strong><code>.sql</code></strong> 放进命名卷 <strong><code>mysql-init</code></strong>，可任选其一：<strong>①</strong> 临时容器拷入 <strong><code>docker run --rm -v mysql-init:/init ...</code></strong>；<strong>②</strong> 改用绑定挂载 <strong><code>-v /宿主机路径/mysql-init:/docker-entrypoint-initdb.d:ro</code></strong>，便于本机直接放文件。</p>
<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f mysql-dev</span><br><span class="line">docker stop mysql-dev &amp;&amp; docker <span class="built_in">rm</span> mysql-dev</span><br></pre></td></tr></table></figure>

<p>已有数据卷时<strong>不会</strong>再次执行 <strong><code>docker-entrypoint-initdb.d</code></strong> 内脚本；清空数据需删卷：<strong><code>docker volume rm mysql-data</code></strong>（<strong>会删库</strong>，且须先删容器）。</p>
<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.mysql.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。敏感项建议放在同目录 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>），勿长期写死在仓库。</p>
<p><strong>一条命令</strong>启动全栈（在 <strong><code>docker-compose.mysql.yml</code></strong> 所在目录执行）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.mysql.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.mysql.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>、<strong>字符集参数</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">mysql-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">mysql-dev</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">$&#123;MYSQL_ROOT_PASSWORD:-pi=3.1415&#125;</span></span><br><span class="line">      <span class="attr">MYSQL_DATABASE:</span> <span class="string">$&#123;MYSQL_DATABASE:-app&#125;</span></span><br><span class="line">      <span class="attr">MYSQL_USER:</span> <span class="string">$&#123;MYSQL_USER:-admin&#125;</span></span><br><span class="line">      <span class="attr">MYSQL_PASSWORD:</span> <span class="string">$&#123;MYSQL_PASSWORD:-pi=3.1415&#125;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;3306:3306&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql-data:/var/lib/mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql-init:/docker-entrypoint-initdb.d:ro</span></span><br><span class="line">    <span class="attr">command:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;--character-set-server=utf8mb4&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;--collation-server=utf8mb4_unicode_ci&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;--default-authentication-plugin=caching_sha2_password&quot;</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;mysqladmin ping -h 127.0.0.1 -uroot -p\&quot;$$MYSQL_ROOT_PASSWORD\&quot; || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">30</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">40s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">mysql-data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">mysql-init:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>同目录可选 <strong><code>.env</code></strong>（勿提交；与 <strong><code># 三</code></strong> 一条 <strong><code>docker run</code></strong> 对齐）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MYSQL_ROOT_PASSWORD=pi=3.1415</span><br><span class="line">MYSQL_DATABASE=app</span><br><span class="line">MYSQL_USER=admin</span><br><span class="line">MYSQL_PASSWORD=pi=3.1415</span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.mysql.yml ps</span><br><span class="line">docker compose -f docker-compose.mysql.yml logs -f mysql</span><br><span class="line">docker compose -f docker-compose.mysql.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留命名卷 <strong><code>mysql-data</code></strong>、<strong><code>mysql-init</code></strong>；若需清空数据加 <strong><code>docker compose ... down -v</code></strong>（<strong>会删库</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>mysql-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f mysql-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>Secret</strong>（<strong>root</strong> 与应用账号）、<strong>PVC</strong>、<strong>Deployment</strong>、<strong>ClusterIP Service</strong>。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql-secret</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">&quot;pi=3.1415&quot;</span></span><br><span class="line">  <span class="attr">MYSQL_DATABASE:</span> <span class="string">&quot;app&quot;</span></span><br><span class="line">  <span class="attr">MYSQL_USER:</span> <span class="string">&quot;admin&quot;</span></span><br><span class="line">  <span class="attr">MYSQL_PASSWORD:</span> <span class="string">&quot;pi=3.1415&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">20Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">3306</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_ROOT_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mysql-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_ROOT_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_DATABASE</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mysql-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_DATABASE</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_USER</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mysql-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_USER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">mysql-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;Asia/Shanghai&quot;</span></span><br><span class="line">          <span class="attr">args:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;--character-set-server=utf8mb4&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;--collation-server=utf8mb4_unicode_ci&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;--default-authentication-plugin=caching_sha2_password&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/var/lib/mysql</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">3306</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">30</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">3306</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">60</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;512Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;250m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;1000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">mysql-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">3306</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">3306</span></span><br></pre></td></tr></table></figure>

<p>查看资源与连接前检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get pods,svc,pvc -l app=mysql</span><br></pre></td></tr></table></figure>

<p>集群内连接串示例（按 <strong>namespace</strong> 调整）：<strong><code>admin:pi=3.1415@tcp(mysql.default.svc.cluster.local:3306)/app?parseTime=true&amp;charset=utf8mb4</code></strong>（密码含 <strong><code>=</code></strong> 时 <strong>URL</strong> 内需 <strong><code>encodeURIComponent</code></strong> 或改用 <strong>Secret</strong> 挂载 <strong><code>my.cnf</code></strong>，勿裸贴生产密钥）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；<strong>MySQL</strong> 主从、读写分离请用 <strong>Operator</strong> 或 <strong>StatefulSet</strong> 方案，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先官方 <strong><code>mysql:8.0</code></strong> &#x2F; 固定补丁标签；注意认证插件与客户端版本</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>docker run</code></strong>：<strong><code>mysql-dev</code></strong>、命名卷 <strong><code>mysql-data</code></strong> &#x2F; <strong><code>mysql-init</code></strong>、库 <strong><code>app</code></strong>、用户 <strong><code>admin</code></strong>、<strong><code>utf8mb4</code></strong>（见 <strong># 三</strong>）</td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.mysql.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>mysql-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请配合备份、监控、网络策略与 <strong>TLS</strong>；<strong>Secret</strong> 建议对接 <strong>External Secrets</strong> &#x2F; <strong>Sealed Secrets</strong> 等管控流程。</p>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL DDL 语句</title>
    <url>/2026/05/01/MySQL/MySQL/</url>
    <content><![CDATA[<h1 id="MySQL-DDL-语句"><a href="#MySQL-DDL-语句" class="headerlink" title="MySQL DDL 语句"></a>MySQL DDL 语句</h1><p><strong>DDL</strong>：Data Definition Language 数据定义语言<br>作用：<strong>定义&#x2F;修改&#x2F;删除 数据库、表、字段结构</strong>，不操作表里数据，只管「结构」</p>
<h2 id="DDL-主要分-3-大类"><a href="#DDL-主要分-3-大类" class="headerlink" title="DDL 主要分 3 大类"></a>DDL 主要分 3 大类</h2><ol>
<li><strong>库操作</strong>：CREATE &#x2F; ALTER &#x2F; DROP 数据库  </li>
<li><strong>表操作</strong>：CREATE &#x2F; ALTER &#x2F; DROP 数据表  </li>
<li><strong>其他</strong>（仍是 <strong>DDL</strong>：定义「对象结构」，不直接改表内业务行）  <ul>
<li><strong>视图（VIEW）</strong>：<strong><code>CREATE VIEW</code> &#x2F; <code>ALTER VIEW</code> &#x2F; <code>DROP VIEW</code></strong>。把<strong>一段查询</strong>保存成<strong>虚拟表</strong>名，查询时动态算出结果；本身<strong>不单独存一份业务数据</strong>，常用于简化复杂 <strong>SQL</strong>、封装列权限。  </li>
<li><strong>索引（INDEX）</strong>：<strong><code>CREATE INDEX</code></strong>，或 <strong><code>ALTER TABLE ... ADD INDEX</code></strong>、<strong><code>DROP INDEX</code></strong> 等。在列（或列组合）上维护<strong>辅助查找结构</strong>，加快 <strong>SELECT</strong>，但会占磁盘、拖慢部分 <strong>INSERT&#x2F;UPDATE&#x2F;DELETE</strong>；与<strong>表</strong>强相关，属结构的延伸。<br><strong>存储过程、触发器</strong>等也可由 <strong>DDL</strong> 创建&#x2F;删除对象。</li>
</ul>
</li>
</ol>
<hr>
<h1 id="一、数据库-DDL-操作"><a href="#一、数据库-DDL-操作" class="headerlink" title="一、数据库 DDL 操作"></a>一、数据库 DDL 操作</h1><h3 id="1-创建数据库"><a href="#1-创建数据库" class="headerlink" title="1. 创建数据库"></a>1. 创建数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 基础创建</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE db1;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 带字符集（推荐写法）</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE db2 <span class="keyword">DEFAULT</span> <span class="keyword">CHARACTER SET</span> utf8mb4 <span class="keyword">COLLATE</span> utf8mb4_unicode_ci;</span><br></pre></td></tr></table></figure>

<h3 id="2-查看数据库"><a href="#2-查看数据库" class="headerlink" title="2. 查看数据库"></a>2. 查看数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看所有库</span></span><br><span class="line"><span class="keyword">SHOW</span> DATABASES;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看建库语句</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE</span> DATABASE db2;</span><br></pre></td></tr></table></figure>

<h3 id="3-使用-切换数据库"><a href="#3-使用-切换数据库" class="headerlink" title="3. 使用&#x2F;切换数据库"></a>3. 使用&#x2F;切换数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">USE db2;</span><br></pre></td></tr></table></figure>

<h3 id="4-修改数据库字符集"><a href="#4-修改数据库字符集" class="headerlink" title="4. 修改数据库字符集"></a>4. 修改数据库字符集</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> DATABASE db2 <span class="keyword">DEFAULT</span> <span class="keyword">CHARACTER SET</span> utf8mb4;</span><br></pre></td></tr></table></figure>

<h3 id="5-删除数据库"><a href="#5-删除数据库" class="headerlink" title="5. 删除数据库"></a>5. 删除数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> DATABASE IF <span class="keyword">EXISTS</span> db1;</span><br></pre></td></tr></table></figure>

<p><code>IF EXISTS</code>：防止库不存在报错，<strong>新手必加</strong></p>
<hr>
<h1 id="二、数据表-DDL-操作（重点）"><a href="#二、数据表-DDL-操作（重点）" class="headerlink" title="二、数据表 DDL 操作（重点）"></a>二、数据表 DDL 操作（重点）</h1><h2 id="1-创建表-CREATE-TABLE"><a href="#1-创建表-CREATE-TABLE" class="headerlink" title="1. 创建表 CREATE TABLE"></a>1. 创建表 CREATE TABLE</h2><p>语法：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> 表名(</span><br><span class="line">    字段名 类型 [约束],</span><br><span class="line">    字段名 类型 [约束],</span><br><span class="line">    ...</span><br><span class="line">) [字符集];</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">USE db2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> student (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    age <span class="type">INT</span>,</span><br><span class="line">    gender <span class="type">CHAR</span>(<span class="number">2</span>),</span><br><span class="line">    create_time DATETIME</span><br><span class="line">) <span class="keyword">DEFAULT</span> <span class="keyword">CHARACTER SET</span> utf8mb4;</span><br></pre></td></tr></table></figure>

<p>常用字段类型：</p>
<ul>
<li><code>INT</code> 整型  </li>
<li><code>VARCHAR(n)</code> 可变字符串  </li>
<li><code>CHAR(n)</code> 固定字符串  </li>
<li><code>DATETIME</code> 日期时间</li>
</ul>
<p>常用约束：</p>
<ul>
<li><code>PRIMARY KEY</code> 主键  </li>
<li><code>AUTO_INCREMENT</code> 自增  </li>
<li><code>NOT NULL</code> 非空  </li>
<li><code>DEFAULT 值</code> 默认值  </li>
<li><code>UNIQUE</code> 唯一约束（列或列组合值不重复；<strong><code>NULL</code></strong> 在唯一索引中的处理见下）  </li>
<li><code>FOREIGN KEY</code> 外键（子表列引用父表主键或<strong>唯一键</strong>，由数据库维护引用完整性）</li>
</ul>
<h3 id="UNIQUE（唯一约束）"><a href="#UNIQUE（唯一约束）" class="headerlink" title="UNIQUE（唯一约束）"></a>UNIQUE（唯一约束）</h3><ul>
<li><strong>作用</strong>：保证列（或列组合）取值在表内<strong>不重复</strong>，常用于业务号、邮箱、手机号等。  </li>
<li><strong>与主键</strong>：一张表只有一个 <strong><code>PRIMARY KEY</code></strong>，可有多个 <strong><code>UNIQUE</code></strong>。  </li>
<li><strong><code>NULL</code></strong>：<strong>MySQL InnoDB</strong> 下唯一索引<strong>允许多行</strong>取 <strong><code>NULL</code></strong>（不参与互斥比较）；若业务上「只能一行未填」，需配合 <strong><code>NOT NULL</code></strong> 或应用层校验。</li>
</ul>
<p>建表示例（列级简写 + 命名唯一索引）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">USE db2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_user (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    username <span class="type">VARCHAR</span>(<span class="number">32</span>) <span class="keyword">NOT NULL</span> <span class="keyword">UNIQUE</span>,</span><br><span class="line">    email <span class="type">VARCHAR</span>(<span class="number">64</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">UNIQUE</span> KEY uk_email (email)</span><br><span class="line">) <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br></pre></td></tr></table></figure>

<p>联合唯一（例如「同部门内工号不重复」）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_staff (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    dept_id <span class="type">INT</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    emp_no <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">UNIQUE</span> KEY uk_dept_emp (dept_id, emp_no)</span><br><span class="line">) <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br></pre></td></tr></table></figure>

<h3 id="FOREIGN-KEY（外键约束）"><a href="#FOREIGN-KEY（外键约束）" class="headerlink" title="FOREIGN KEY（外键约束）"></a>FOREIGN KEY（外键约束）</h3><ul>
<li><strong>作用</strong>：子表某列（或列组合）只能取<strong>父表已存在</strong>的主键值（或父表上被引用的 <strong><code>UNIQUE</code></strong> 列），防止「悬挂引用」。  </li>
<li><strong>要求</strong>：父表被引用列必须是 <strong><code>PRIMARY KEY</code></strong> 或 <strong><code>UNIQUE</code></strong>；类型、长度、字符集等应一致或兼容。  </li>
<li><strong><code>ON DELETE</code> &#x2F; <code>ON UPDATE</code></strong>：父行删除或主键更新时，对子行的动作，常用 <strong><code>RESTRICT</code></strong>（禁止若仍有子行）、<strong><code>CASCADE</code></strong>（级联删&#x2F;改）、<strong><code>SET NULL</code></strong>（子列须可空）。</li>
</ul>
<p>建表示例（先父后子）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">USE db2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_dept (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span></span><br><span class="line">) <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_emp (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    dept_id <span class="type">INT</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_emp_dept <span class="keyword">FOREIGN KEY</span> (dept_id) <span class="keyword">REFERENCES</span> demo_dept (id)</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">UPDATE</span> CASCADE</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">DELETE</span> RESTRICT</span><br><span class="line">) <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：插入 <strong><code>demo_emp</code></strong> 前，<strong><code>dept_id</code></strong> 必须在 <strong><code>demo_dept</code></strong> 中已存在；删除父部门时若仍有员工引用，<strong><code>ON DELETE RESTRICT</code></strong> 会报错阻止删除。</p>
<h2 id="2-查看表结构"><a href="#2-查看表结构" class="headerlink" title="2. 查看表结构"></a>2. 查看表结构</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看当前库所有表</span></span><br><span class="line"><span class="keyword">SHOW</span> TABLES;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看表字段详情</span></span><br><span class="line"><span class="keyword">DESC</span> student;</span><br><span class="line"><span class="comment">-- 等价</span></span><br><span class="line"><span class="keyword">DESCRIBE</span> student;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看建表语句</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE TABLE</span> student;</span><br></pre></td></tr></table></figure>

<h2 id="3-修改表-ALTER-TABLE（超常用）"><a href="#3-修改表-ALTER-TABLE（超常用）" class="headerlink" title="3. 修改表 ALTER TABLE（超常用）"></a>3. 修改表 ALTER TABLE（超常用）</h2><h3 id="3-1-新增字段"><a href="#3-1-新增字段" class="headerlink" title="3.1 新增字段"></a>3.1 新增字段</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 末尾加字段</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> student <span class="keyword">ADD</span> email <span class="type">VARCHAR</span>(<span class="number">50</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 在 name 后面加字段</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> student <span class="keyword">ADD</span> phone <span class="type">VARCHAR</span>(<span class="number">11</span>) AFTER name;</span><br></pre></td></tr></table></figure>

<h3 id="3-2-修改字段类型-约束"><a href="#3-2-修改字段类型-约束" class="headerlink" title="3.2 修改字段类型&#x2F;约束"></a>3.2 修改字段类型&#x2F;约束</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student MODIFY age TINYINT <span class="keyword">NOT NULL</span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-重命名字段"><a href="#3-3-重命名字段" class="headerlink" title="3.3 重命名字段"></a>3.3 重命名字段</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student RENAME <span class="keyword">COLUMN</span> phone <span class="keyword">TO</span> tel;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：<code>RENAME COLUMN</code> 需 <strong>MySQL 8.0+</strong>；更旧版本可用 <strong><code>CHANGE COLUMN phone tel VARCHAR(11)</code></strong> 等形式（需重复写出类型）。</p>
<h3 id="3-4-删除字段"><a href="#3-4-删除字段" class="headerlink" title="3.4 删除字段"></a>3.4 删除字段</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student <span class="keyword">DROP</span> <span class="keyword">COLUMN</span> email;</span><br></pre></td></tr></table></figure>

<h3 id="3-5-重命名表"><a href="#3-5-重命名表" class="headerlink" title="3.5 重命名表"></a>3.5 重命名表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student RENAME <span class="keyword">TO</span> stu;</span><br></pre></td></tr></table></figure>

<h3 id="3-6-增加-删除-UNIQUE-与-FOREIGN-KEY"><a href="#3-6-增加-删除-UNIQUE-与-FOREIGN-KEY" class="headerlink" title="3.6 增加 &#x2F; 删除 UNIQUE 与 FOREIGN KEY"></a>3.6 增加 &#x2F; 删除 UNIQUE 与 FOREIGN KEY</h3><p><strong>增加唯一索引（效果同唯一约束）</strong>（列不存在时先加列）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> demo_user</span><br><span class="line">    <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> mobile <span class="type">VARCHAR</span>(<span class="number">11</span>) <span class="keyword">NULL</span>,</span><br><span class="line">    <span class="keyword">ADD</span> <span class="keyword">UNIQUE</span> KEY uk_mobile (mobile);</span><br></pre></td></tr></table></figure>

<p><strong>删除外键</strong>（约束名以 <strong><code>SHOW CREATE TABLE 子表名</code></strong> 为准，或建表时写在 <strong><code>CONSTRAINT 名字</code></strong> 里）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> demo_emp <span class="keyword">DROP</span> <span class="keyword">FOREIGN KEY</span> fk_emp_dept;</span><br></pre></td></tr></table></figure>

<p><strong>增加外键</strong>（子表数据须已满足引用，否则会失败）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> demo_emp</span><br><span class="line">    <span class="keyword">ADD CONSTRAINT</span> fk_emp_dept <span class="keyword">FOREIGN KEY</span> (dept_id) <span class="keyword">REFERENCES</span> demo_dept (id)</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">UPDATE</span> CASCADE</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">DELETE</span> RESTRICT;</span><br></pre></td></tr></table></figure>

<h2 id="4-删除表"><a href="#4-删除表" class="headerlink" title="4. 删除表"></a>4. 删除表</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 普通删除</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> stu;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 安全删除（不存在不报错）</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> stu;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="三、DDL-和-DML-区别（必记）"><a href="#三、DDL-和-DML-区别（必记）" class="headerlink" title="三、DDL 和 DML 区别（必记）"></a>三、DDL 和 DML 区别（必记）</h1><table>
<thead>
<tr>
<th>分类</th>
<th>全称</th>
<th>作用</th>
<th>常用语句</th>
</tr>
</thead>
<tbody><tr>
<td>DDL</td>
<td>数据定义语言</td>
<td>管<strong>库、表、结构</strong></td>
<td>CREATE、ALTER、DROP</td>
</tr>
<tr>
<td>DML</td>
<td>数据操作语言</td>
<td>管<strong>表里数据</strong></td>
<td>INSERT、UPDATE、DELETE、SELECT</td>
</tr>
</tbody></table>
<p>简单记：</p>
<ul>
<li><strong>DDL 改架子</strong>（库、表、字段）  </li>
<li><strong>DML 改数据</strong>（增删改查记录）</li>
</ul>
<hr>
<h1 id="四、入门必背-DDL-极简清单"><a href="#四、入门必背-DDL-极简清单" class="headerlink" title="四、入门必背 DDL 极简清单"></a>四、入门必背 DDL 极简清单</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE <span class="operator">/</span> <span class="keyword">SHOW</span> DATABASES <span class="operator">/</span> USE <span class="operator">/</span> <span class="keyword">ALTER</span> DATABASE <span class="operator">/</span> <span class="keyword">DROP</span> DATABASE</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 表</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> <span class="operator">/</span> <span class="keyword">SHOW</span> TABLES <span class="operator">/</span> <span class="keyword">DESC</span> <span class="operator">/</span> <span class="keyword">ALTER TABLE</span> <span class="operator">/</span> <span class="keyword">DROP</span> <span class="keyword">TABLE</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 约束（常与 CREATE / ALTER 搭配）</span></span><br><span class="line"><span class="keyword">UNIQUE</span> <span class="operator">/</span> <span class="keyword">FOREIGN KEY</span> <span class="operator">/</span> <span class="keyword">CONSTRAINT</span> … <span class="keyword">REFERENCES</span> … <span class="operator">/</span> <span class="keyword">ON</span> <span class="keyword">DELETE</span> <span class="operator">/</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span></span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>DDL</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 存储引擎概览</title>
    <url>/2026/05/03/MySQL/MySQL%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<p><strong>MySQL</strong> 在<strong>服务器层</strong>之下，通过<strong>可插拔存储引擎</strong>把「行怎么存、事务与锁如何实现」交给不同实现。建表时可指定 <strong><code>ENGINE=…</code></strong>（<strong>8.0</strong> 起默认 <strong>InnoDB</strong>）。姊妹文：<strong><code>MySQL-Docker.md</code></strong>（部署）、<strong><code>MySQL锁详解.md</code></strong>（<strong>InnoDB</strong> 锁）、<strong><code>MySQL索引与性能优化.md</code></strong>。</p>
<hr>
<h2 id="一、如何查看支持的引擎与默认值"><a href="#一、如何查看支持的引擎与默认值" class="headerlink" title="一、如何查看支持的引擎与默认值"></a>一、如何查看支持的引擎与默认值</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> ENGINES;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;default_storage_engine&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>建表未写 <strong><code>ENGINE</code></strong> 时使用 <strong><code>default_storage_engine</code></strong>（一般为 <strong><code>InnoDB</code></strong>）。</p>
<hr>
<h2 id="二、InnoDB（事实上的唯一首选）"><a href="#二、InnoDB（事实上的唯一首选）" class="headerlink" title="二、InnoDB（事实上的唯一首选）"></a>二、InnoDB（事实上的唯一首选）</h2><table>
<thead>
<tr>
<th>维度</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>事务</strong></td>
<td><strong>ACID</strong>；<strong><code>COMMIT</code> &#x2F; <code>ROLLBACK</code></strong>、崩溃恢复（<strong>redo &#x2F; undo</strong>）。</td>
</tr>
<tr>
<td><strong>锁与并发</strong></td>
<td>以<strong>行锁</strong>为主；隔离级别与<strong>间隙锁</strong>等见 <strong><code>MySQL锁详解.md</code></strong>。</td>
</tr>
<tr>
<td><strong>索引与数据</strong></td>
<td><strong>聚簇索引</strong>即主键 <strong>B+ 树</strong>，二级索引叶子存<strong>主键</strong>；详见 <strong><code>MySQL索引与性能优化.md</code></strong>。</td>
</tr>
<tr>
<td><strong>外键</strong></td>
<td>支持 <strong><code>FOREIGN KEY</code></strong>（与 <strong><code>MySQL.md</code></strong> 中 DDL 一致）。</td>
</tr>
<tr>
<td><strong>适用</strong></td>
<td><strong>OLTP</strong>、绝大多数业务库；<strong>官方默认与推荐</strong>。</td>
</tr>
</tbody></table>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> t_order (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    user_id <span class="type">BIGINT</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    amount <span class="type">DECIMAL</span>(<span class="number">10</span>,<span class="number">2</span>) <span class="keyword">NOT NULL</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="三、MyISAM（-legacy，新业务避免）"><a href="#三、MyISAM（-legacy，新业务避免）" class="headerlink" title="三、MyISAM（ legacy，新业务避免）"></a>三、MyISAM（ legacy，新业务避免）</h2><table>
<thead>
<tr>
<th>维度</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>事务</strong></td>
<td><strong>不支持</strong>事务与崩溃安全意义上的 <strong>InnoDB</strong> 同级别恢复。</td>
</tr>
<tr>
<td><strong>锁</strong></td>
<td><strong>表级锁</strong>，写多读少场景易成为瓶颈。</td>
</tr>
<tr>
<td><strong>索引</strong></td>
<td>数据与索引布局与 <strong>InnoDB</strong> 不同；<strong>无聚簇</strong>主键同义实现。</td>
</tr>
<tr>
<td><strong>典型遗留场景</strong></td>
<td>老系统、只读库、部分空间索引历史用法；<strong>新表请勿选用</strong>。</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、MEMORY（内存表，谨慎）"><a href="#四、MEMORY（内存表，谨慎）" class="headerlink" title="四、MEMORY（内存表，谨慎）"></a>四、MEMORY（内存表，谨慎）</h2><ul>
<li>数据驻<strong>内存</strong>，进程崩溃或重启后<strong>易失</strong>；容量受 <strong><code>max_heap_table_size</code></strong> 等限制。  </li>
<li>曾用于临时高速缓存；现代实践更常见 <strong>Redis</strong> 或 <strong>InnoDB</strong> + 足够内存 + 合理索引。</li>
</ul>
<hr>
<h2 id="五、其它引擎（知道名字即可）"><a href="#五、其它引擎（知道名字即可）" class="headerlink" title="五、其它引擎（知道名字即可）"></a>五、其它引擎（知道名字即可）</h2><table>
<thead>
<tr>
<th>引擎</th>
<th>一句话</th>
</tr>
</thead>
<tbody><tr>
<td><strong>ARCHIVE</strong></td>
<td>高压缩追加存储，适合冷归档、很少更新。</td>
</tr>
<tr>
<td><strong>CSV</strong></td>
<td>以 CSV 形式存文件，多用于导入导出管道，非通用 OLTP。</td>
</tr>
<tr>
<td><strong>BLACKHOLE</strong></td>
<td>写入即丢弃，有时用于复制拓扑中的「中继」等特殊场景。</td>
</tr>
<tr>
<td><strong>NDB</strong>（<strong>MySQL Cluster</strong>）</td>
<td>分布式集群专用，与普通单机 <strong>InnoDB</strong> 运维模型不同。</td>
</tr>
</tbody></table>
<p>具体是否编译进你的 <strong>mysqld</strong>，以 <strong><code>SHOW ENGINES</code></strong> 为准。</p>
<hr>
<h2 id="六、选型与迁移建议"><a href="#六、选型与迁移建议" class="headerlink" title="六、选型与迁移建议"></a>六、选型与迁移建议</h2><ol>
<li><strong>新表一律 <code>ENGINE=InnoDB</code></strong>，并显式 <strong><code>DEFAULT CHARSET=utf8mb4</code></strong>（与 <strong><code>MySQL.md</code></strong> 一致）。  </li>
<li>发现 <strong><code>MyISAM</code></strong> 老表：评估后 <strong><code>ALTER TABLE … ENGINE=InnoDB</code></strong>（需在低峰窗口做；大表走 <strong>gh-ost</strong> &#x2F; <strong>pt-online-schema-change</strong> 等在线 DDL 工具，属运维专题）。  </li>
<li>不要用 <strong><code>ENGINE</code></strong> 解决「性能问题」：优先<strong>索引、SQL、连接与事务设计</strong>。</li>
</ol>
<hr>
<h2 id="七、与姊妹文的关系"><a href="#七、与姊妹文的关系" class="headerlink" title="七、与姊妹文的关系"></a>七、与姊妹文的关系</h2><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>MySQL锁详解.md</code></strong></td>
<td><strong>InnoDB</strong> 行锁、间隙锁、死锁与实验</td>
</tr>
<tr>
<td><strong><code>MySQL索引与性能优化.md</code></strong></td>
<td><strong>B+ 树直觉、<code>EXPLAIN</code>、事务隔离配置</strong></td>
</tr>
<tr>
<td><strong><code>MySQL.md</code></strong></td>
<td><strong>DDL</strong>、<strong><code>FOREIGN KEY</code></strong></td>
</tr>
<tr>
<td><strong><code>MySQL-Docker.md</code></strong></td>
<td>容器内 <strong>InnoDB</strong> 数据目录与备份注意点</td>
</tr>
</tbody></table>
<p><strong>MySQL 8.0</strong> 已移除 <strong><code>PERFORMANCE_SCHEMA</code></strong> 之外的不少历史特性；引擎相关选项以当前版本官方文档为准。</p>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>InnoDB</tag>
        <tag>存储引擎</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 索引与性能优化</title>
    <url>/2026/05/03/MySQL/MySQL%E7%B4%A2%E5%BC%95%E4%B8%8E%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<p>面向<strong>业务开发与日常运维</strong>：能看懂<strong>执行计划</strong>、会选<strong>隔离级别</strong>、会开<strong>慢日志</strong>、知道<strong>备份与按时间点恢复</strong>在说什么。与 <strong>《MySQL锁详解》</strong>（<strong><code>MySQL锁详解.md</code></strong>，偏 <strong>InnoDB</strong> 锁原理与双会话实验）互补；容器内备份与卷持久化可对照 <strong>《Docker 部署 MySQL》</strong>（<strong><code>MySQL-Docker.md</code></strong>）中的数据卷与实例启停。</p>
<hr>
<h2 id="一、索引在干什么：B-树直觉（够用版）"><a href="#一、索引在干什么：B-树直觉（够用版）" class="headerlink" title="一、索引在干什么：B+ 树直觉（够用版）"></a>一、索引在干什么：B+ 树直觉（够用版）</h2><p><strong>InnoDB</strong> 里，表数据按<strong>主键</strong>组织成一棵 <strong>B+ 树</strong>（<strong>聚簇索引</strong>）：叶子节点存「主键 + 整行数据」。<br><strong>二级索引</strong>（你建的 <strong><code>INDEX</code> &#x2F; <code>UNIQUE</code></strong>）叶子节点存「索引列 + <strong>主键</strong>」，需要整行时往往要<strong>再回表</strong>到聚簇索引取数据。</p>
<p>下图便于建立直觉（<strong>示意</strong>：键值与层数为教学简化，与真实页大小、填充率无关）。</p>
<p><img src="/images/mysql/innodb-bplus-tree.png" alt="InnoDB B+ 树：聚簇索引与二级索引、叶子链表与回表（示意）"></p>
<p><strong>读图要点</strong>：上层结点只存<strong>分界键</strong>与<strong>子树指针</strong>；<strong>最底层叶子</strong>由<strong>双向链表</strong>串起，方便<strong>范围扫描</strong>；<strong>聚簇索引</strong>叶子是<strong>主键 + 行数据</strong>；<strong>二级索引</strong>叶子多一步<strong>按主键回表</strong>读完整行。</p>
<p>直觉：</p>
<ul>
<li><strong>等值 &#x2F; 范围</strong>命中索引列 → 树高度低、少读页 → <strong>随机 I&#x2F;O</strong> 变少。  </li>
<li><strong>全表扫</strong> → 叶子链顺序读，数据量一大就慢。  </li>
<li>索引<strong>不是越多越好</strong>：每次 <strong><code>INSERT/UPDATE/DELETE</code></strong> 都可能维护多棵索引树。</li>
</ul>
<hr>
<h2 id="二、联合索引与最左前缀"><a href="#二、联合索引与最左前缀" class="headerlink" title="二、联合索引与最左前缀"></a>二、联合索引与最左前缀</h2><p>联合索引 <strong><code>KEY idx_abc (a, b, c)</code></strong> 的排序语义是：先按 <strong><code>a</code></strong>，再 <strong><code>a</code> 相同</strong>时按 <strong><code>b</code></strong>，再按 <strong><code>c</code></strong>。</p>
<p><strong>能较好利用该索引</strong>的典型条件（示意）：</p>
<ul>
<li><strong><code>WHERE a = ?</code></strong>  </li>
<li><strong><code>WHERE a = ? AND b = ?</code></strong>  </li>
<li><strong><code>WHERE a = ? AND b BETWEEN ? AND ?</code></strong></li>
</ul>
<p><strong>容易用不上或只用一截</strong>的情况：</p>
<ul>
<li><strong><code>WHERE b = ? AND c = ?</code></strong>（缺 <strong><code>a</code></strong>，与「最左」不一致）  </li>
<li><strong><code>WHERE a = ? OR d = ?</code></strong>（<strong><code>OR</code></strong> 另一侧常走另一条路径，易优化成 <strong>UNION</strong> 或改写法）  </li>
<li>对 <strong><code>a</code></strong> 做函数&#x2F;隐式类型转换导致<strong>无法走树</strong>（例如 <strong><code>WHERE DATE(a_col) = ...</code></strong>）。</li>
</ul>
<p>设计经验：把<strong>区分度高、过滤性大</strong>的列尽量放左侧；把<strong>范围查询</strong>列放联合索引<strong>右侧</strong>更常见（仍要结合 <strong><code>EXPLAIN</code></strong> 验证）。</p>
<hr>
<h2 id="三、覆盖索引（Using-index）"><a href="#三、覆盖索引（Using-index）" class="headerlink" title="三、覆盖索引（Using index）"></a>三、覆盖索引（<strong><code>Using index</code></strong>）</h2><p>若 <strong><code>SELECT</code></strong> 里出现的列都能从<strong>某条二级索引</strong>的叶子直接凑齐（含 <strong><code>WHERE</code></strong> 用到的索引列），<strong>InnoDB</strong> 可能<strong>只扫索引、不回表</strong>，<strong><code>EXPLAIN</code></strong> 的 <strong><code>Extra</code></strong> 常见 <strong><code>Using index</code></strong>。</p>
<p>典型收益：大宽表上只查少数列 + 条件列恰好在同一联合索引上，<strong>I&#x2F;O</strong> 明显下降。</p>
<hr>
<h2 id="四、索引条件下推-ICP（一眼带过）"><a href="#四、索引条件下推-ICP（一眼带过）" class="headerlink" title="四、索引条件下推 ICP（一眼带过）"></a>四、索引条件下推 <strong>ICP</strong>（一眼带过）</h2><p><strong>Index Condition Pushdown</strong>：把部分 <strong><code>WHERE</code></strong> 条件下推到存储引擎层，在<strong>索引扫描过程中</strong>就过滤，少回表。<br>在 <strong><code>EXPLAIN</code></strong> 里常见 <strong><code>Using index condition</code></strong>（与 <strong><code>Using index</code></strong> 不同：后者多为覆盖索引）。不必一次讲透优化器细节，看到术语知道「在索引层多干了一点过滤」即可。</p>
<hr>
<h2 id="五、怎么看-EXPLAIN（执行计划）"><a href="#五、怎么看-EXPLAIN（执行计划）" class="headerlink" title="五、怎么看 EXPLAIN（执行计划）"></a>五、怎么看 <strong><code>EXPLAIN</code></strong>（执行计划）</h2><h3 id="1-基本用法"><a href="#1-基本用法" class="headerlink" title="1. 基本用法"></a>1. 基本用法</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> your_table <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- MySQL 8.0+ 可看优化器树（可选）</span></span><br><span class="line"><span class="comment">-- EXPLAIN FORMAT=TREE SELECT ...</span></span><br></pre></td></tr></table></figure>

<h3 id="2-建议优先盯的列（入门够用）"><a href="#2-建议优先盯的列（入门够用）" class="headerlink" title="2. 建议优先盯的列（入门够用）"></a>2. 建议优先盯的列（入门够用）</h3><table>
<thead>
<tr>
<th>列 &#x2F; 信息</th>
<th>大致含义（入门）</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>type</code></strong></td>
<td>访问类型，从差到好常见：<strong><code>ALL</code></strong>（全表扫）→ <strong><code>index</code></strong>（扫整棵二级索引树）→ <strong><code>range</code></strong> → <strong><code>ref</code></strong> → <strong><code>eq_ref</code></strong> → <strong><code>const</code></strong>。出现 <strong><code>ALL</code></strong> 且行很大时要警惕。</td>
</tr>
<tr>
<td><strong><code>key</code></strong></td>
<td>实际选用的索引名；<strong><code>NULL</code></strong> 表示未用索引。</td>
</tr>
<tr>
<td><strong><code>rows</code></strong></td>
<td>预估扫描行数（越小通常越好，<strong>预估值</strong>不等于真实值）。</td>
</tr>
<tr>
<td><strong><code>Extra</code></strong></td>
<td><strong><code>Using index</code></strong> 覆盖索引；<strong><code>Using where</code></strong> 在引擎返回后再过滤；<strong><code>Using filesort</code></strong> &#x2F; <strong><code>Using temporary</code></strong> 常与排序、分组有关，数据大时要关注。</td>
</tr>
</tbody></table>
<h3 id="3-实操习惯"><a href="#3-实操习惯" class="headerlink" title="3. 实操习惯"></a>3. 实操习惯</h3><ol>
<li>对<strong>慢 SQL</strong>先 <strong><code>EXPLAIN</code></strong>，确认是否走了预期 <strong><code>key</code></strong>。  </li>
<li><strong><code>SELECT *</code></strong> 易阻碍覆盖索引；列表接口尽量<strong>只查需要的列</strong>。  </li>
<li>改完索引或统计信息后，可 <strong><code>ANALYZE TABLE tbl;</code></strong>（视版本与权限）帮助优化器更新统计信息。</li>
</ol>
<hr>
<h2 id="六、事务与隔离级别（写法与选型）"><a href="#六、事务与隔离级别（写法与选型）" class="headerlink" title="六、事务与隔离级别（写法与选型）"></a>六、事务与隔离级别（写法与选型）</h2><h3 id="1-autocommit-与显式事务"><a href="#1-autocommit-与显式事务" class="headerlink" title="1. autocommit 与显式事务"></a>1. <strong><code>autocommit</code> 与显式事务</strong></h3><ul>
<li>默认 <strong><code>autocommit = 1</code></strong>：每条语句单独一事务，执行完即提交。  </li>
<li>显式事务：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SET</span> autocommit <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">START</span> TRANSACTION;  <span class="comment">-- 或 BEGIN;</span></span><br><span class="line"><span class="comment">-- 多条 DML</span></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"><span class="comment">-- 或 ROLLBACK;</span></span><br><span class="line"><span class="keyword">SET</span> autocommit <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>业务代码里常用连接池 + <strong>ORM</strong>：仍要清楚「一次请求里是否包在一个事务里」，避免<strong>长事务</strong>占锁（与 <strong><code>MySQL锁详解.md</code></strong> 中的死锁、锁等待分析衔接）。</p>
<h3 id="2-READ-COMMITTED（RC）-与-REPEATABLE-READ（RR）"><a href="#2-READ-COMMITTED（RC）-与-REPEATABLE-READ（RR）" class="headerlink" title="2. READ COMMITTED（RC） 与 REPEATABLE READ（RR）"></a>2. <strong><code>READ COMMITTED</code>（RC）</strong> 与 <strong><code>REPEATABLE READ</code>（RR）</strong></h3><table>
<thead>
<tr>
<th>维度</th>
<th><strong>RC</strong></th>
<th><strong>RR</strong>（<strong>InnoDB</strong> 默认）</th>
</tr>
</thead>
<tbody><tr>
<td>读行为</td>
<td>每次读可见<strong>已提交</strong>最新数据</td>
<td>同一事务内<strong>可重复读</strong>快照（简化理解）</td>
</tr>
<tr>
<td>锁与幻读</td>
<td>间隙锁较少场景（仍与语句、索引有关）</td>
<td>更易出现<strong>间隙锁 &#x2F; 临键锁</strong>相关阻塞，利于抑制幻读</td>
</tr>
<tr>
<td>常见选型</td>
<td>报表、强一致读最新、部分<strong>Oracle</strong> 迁移习惯</td>
<td><strong>MySQL</strong> 默认；与多数 ORM 默认一致</td>
</tr>
</tbody></table>
<p><strong>选型建议（粗线条）</strong>：无特殊要求可先保持 <strong>RR</strong>；若已确认<strong>锁竞争 &#x2F; 插入阻塞</strong>与隔离级别强相关，再在测试环境评估 <strong>RC</strong>，并同步应用是否接受<strong>不可重复读 &#x2F; 幻读</strong>语义。<strong>锁细节、实验复现</strong>见 <strong><code>MySQL锁详解.md</code></strong>。</p>
<h3 id="3-会话级修改（示例）"><a href="#3-会话级修改（示例）" class="headerlink" title="3. 会话级修改（示例）"></a>3. 会话级修改（示例）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 仅当前会话（连接断开即失效，具体以版本与权限为准）</span></span><br><span class="line"><span class="keyword">SET</span> SESSION transaction_isolation <span class="operator">=</span> <span class="string">&#x27;READ-COMMITTED&#x27;</span>;</span><br><span class="line"><span class="comment">-- 或</span></span><br><span class="line"><span class="keyword">SET</span> SESSION transaction_isolation <span class="operator">=</span> <span class="string">&#x27;REPEATABLE-READ&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>全局默认值在 <strong><code>my.cnf</code></strong> &#x2F; <strong><code>mysqld</code></strong> 启动参数中配置；生产改全局需评估全业务影响。</p>
<hr>
<h2 id="七、慢查询与运维「三板斧」（点到为止）"><a href="#七、慢查询与运维「三板斧」（点到为止）" class="headerlink" title="七、慢查询与运维「三板斧」（点到为止）"></a>七、慢查询与运维「三板斧」（点到为止）</h2><h3 id="1-打开慢查询日志"><a href="#1-打开慢查询日志" class="headerlink" title="1. 打开慢查询日志"></a>1. 打开慢查询日志</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SET</span> <span class="keyword">GLOBAL</span> slow_query_log <span class="operator">=</span> <span class="keyword">ON</span>;</span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">GLOBAL</span> long_query_time <span class="operator">=</span> <span class="number">1</span>;       <span class="comment">-- 秒，按环境调</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">GLOBAL</span> log_queries_not_using_indexes <span class="operator">=</span> OFF;  <span class="comment">-- 慎用，易刷爆磁盘</span></span><br></pre></td></tr></table></figure>

<p>持久化应写配置文件并重启或通过运维平台下发；<strong><code>SET GLOBAL</code></strong> 需相应权限。</p>
<h3 id="2-mysqldumpslow（随-MySQL-客户端工具分发）"><a href="#2-mysqldumpslow（随-MySQL-客户端工具分发）" class="headerlink" title="2. mysqldumpslow（随 MySQL 客户端工具分发）"></a>2. <strong><code>mysqldumpslow</code></strong>（随 <strong>MySQL</strong> 客户端工具分发）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysqldumpslow -s t -t 10 /path/to/slow.log</span><br></pre></td></tr></table></figure>

<p>常见含义：<strong><code>-s t</code></strong> 按累计总时间排序，<strong><code>-t 10</code></strong> 看前 <strong>10</strong> 条。各发行版路径、包名略有差异。</p>
<h3 id="3-pt-query-digest（Percona-Toolkit）"><a href="#3-pt-query-digest（Percona-Toolkit）" class="headerlink" title="3. pt-query-digest（Percona Toolkit）"></a>3. <strong><code>pt-query-digest</code></strong>（<strong>Percona Toolkit</strong>）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pt-query-digest /path/to/slow.log &gt; report.txt</span><br></pre></td></tr></table></figure>

<p>适合日志量大时做<strong>聚合报告</strong>；安装与权限属运维范畴，此处不展开。</p>
<p><strong>落地顺序</strong>：开慢日志 → 用工具抓 <strong>Top SQL</strong> → <strong><code>EXPLAIN</code></strong> → 改索引或改写法 → 再对比慢日志。</p>
<hr>
<h2 id="八、备份恢复与-PITR-概念"><a href="#八、备份恢复与-PITR-概念" class="headerlink" title="八、备份恢复与 PITR 概念"></a>八、备份恢复与 <strong>PITR</strong> 概念</h2><h3 id="1-逻辑备份（mysqldump）"><a href="#1-逻辑备份（mysqldump）" class="headerlink" title="1. 逻辑备份（mysqldump）"></a>1. 逻辑备份（<strong><code>mysqldump</code></strong>）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysqldump -h127.0.0.1 -uroot -p --single-transaction --routines --triggers \</span><br><span class="line">  --databases your_db &gt; backup.sql</span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>--single-transaction</code></strong>：<strong>InnoDB</strong> 下用一致性读做导出，<strong>尽量避免 <code>FLUSH TABLES WITH READ LOCK</code> 式全库只读</strong>（与全局锁备份对比，见 <strong><code>MySQL锁详解.md</code></strong> 中「全局锁 + <strong><code>mysqldump --single-transaction</code></strong>」相关说明）。  </li>
<li>恢复：<code>mysql -uroot -p &lt; backup.sql</code>（注意字符集与目标实例版本）。</li>
</ul>
<h3 id="2-物理备份与快照"><a href="#2-物理备份与快照" class="headerlink" title="2. 物理备份与快照"></a>2. 物理备份与快照</h3><p>对大数据量，常用<strong>物理热备</strong>（厂商工具或存储快照）+ <strong>binlog</strong>；与 <strong>Docker</strong> 场景结合时，<strong>数据在卷上</strong>，备份策略要在「卷快照 &#x2F; 容器外 <strong><code>mysqldump</code></strong>」之间选一种或组合，并做<strong>恢复演练</strong>（参见 <strong><code>MySQL-Docker.md</code></strong> 中数据卷与持久化思路）。</p>
<h3 id="3-PITR（Point-in-Time-Recovery，按时间点恢复）"><a href="#3-PITR（Point-in-Time-Recovery，按时间点恢复）" class="headerlink" title="3. PITR（Point-in-Time Recovery，按时间点恢复）"></a>3. <strong>PITR（Point-in-Time Recovery，按时间点恢复）</strong></h3><p>含义：不只恢复到「昨晚整库备份」，而是恢复到<strong>某事故前一秒</strong>。通常需要：</p>
<ol>
<li><strong>周期全量 &#x2F; 增量</strong>（逻辑或物理）；  </li>
<li><strong>连续归档的 binlog<code>**（**</code>log_bin&#96;</strong> 打开，并安全保存 <strong>binlog</strong> 文件）；  </li>
<li>恢复时：先还原备份到某一时刻，再<strong>重放 binlog</strong> 到目标时间点（具体命令与工具因版本与平台而异，属 DBA 操作手册级）。</li>
</ol>
<p><strong>可靠性</strong>＝备份自动化 + <strong>定期恢复演练</strong> + <strong>binlog</strong> 留存策略；开发至少要懂「为什么不能只靠一台机子从不备份」。</p>
<hr>
<h2 id="九、本篇与姊妹文关系（速记）"><a href="#九、本篇与姊妹文关系（速记）" class="headerlink" title="九、本篇与姊妹文关系（速记）"></a>九、本篇与姊妹文关系（速记）</h2><table>
<thead>
<tr>
<th>主题</th>
<th>本文</th>
<th>延伸阅读</th>
</tr>
</thead>
<tbody><tr>
<td>锁、间隙锁、死锁实验</td>
<td>点到为止</td>
<td><strong><code>MySQL锁详解.md</code></strong></td>
</tr>
<tr>
<td>容器卷、实例启停</td>
<td>备份落地要结合环境</td>
<td><strong><code>MySQL-Docker.md</code></strong></td>
</tr>
<tr>
<td>表结构、索引 <strong>DDL</strong></td>
<td>建索引语句</td>
<td><strong><code>MySQL.md</code></strong></td>
</tr>
<tr>
<td>复杂 <strong>SELECT</strong></td>
<td>写法与窗口函数等</td>
<td><strong><code>MySQL高级查询.md</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请叠加：<strong>监控告警</strong>、<strong>变更窗口</strong>、<strong>只读副本延迟</strong>、<strong>连接数与超时</strong>等，本篇不替代完整 <strong>DBA</strong> 规范。</p>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
        <tag>EXPLAIN</tag>
        <tag>性能优化</tag>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL锁详解</title>
    <url>/2026/05/03/MySQL/MySQL%E9%94%81%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p><strong>结构清晰、实战导向</strong>（基于 <strong>InnoDB</strong>，<strong>MySQL 5.7 &#x2F; 8.0</strong>）。</p>
<hr>
<h2 id="一、锁的核心目的"><a href="#一、锁的核心目的" class="headerlink" title="一、锁的核心目的"></a>一、锁的核心目的</h2><p>锁 &#x3D; <strong>并发控制</strong>，解决：</p>
<ul>
<li>脏读、不可重复读、幻读</li>
<li>防止多个事务同时修改同一数据导致不一致</li>
<li>平衡 <strong>数据一致性 vs 并发性能</strong></li>
</ul>
<hr>
<h2 id="二、按粒度分类（最核心）"><a href="#二、按粒度分类（最核心）" class="headerlink" title="二、按粒度分类（最核心）"></a>二、按粒度分类（最核心）</h2><h3 id="1）全局锁（Global-Lock）"><a href="#1）全局锁（Global-Lock）" class="headerlink" title="1）全局锁（Global Lock）"></a>1）全局锁（Global Lock）</h3><ul>
<li>粒度：<strong>整个数据库实例</strong></li>
<li>命令：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">FLUSH TABLES <span class="keyword">WITH</span> READ LOCK; <span class="comment">-- 全库只读</span></span><br><span class="line">UNLOCK TABLES;</span><br></pre></td></tr></table></figure>
<ul>
<li>特点：<ul>
<li>所有库所有表<strong>只能读、不能写</strong></li>
<li>阻塞 DML、DDL</li>
</ul>
</li>
<li>用途：<strong>全库逻辑备份</strong></li>
<li>替代方案（InnoDB）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysqldump --single-transaction  <span class="comment"># 利用 MVCC 快照，不加锁</span></span><br></pre></td></tr></table></figure>

<h3 id="2）表级锁（Table-level-Lock）"><a href="#2）表级锁（Table-level-Lock）" class="headerlink" title="2）表级锁（Table-level Lock）"></a>2）表级锁（Table-level Lock）</h3><ul>
<li>粒度：<strong>整张表</strong></li>
<li>MyISAM 只支持表锁；InnoDB 也有表锁（如 MDL）</li>
<li>常用：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">LOCK <span class="keyword">TABLE</span> t1 READ;   <span class="comment">-- 读锁：别人可读不可写</span></span><br><span class="line">LOCK <span class="keyword">TABLE</span> t1 WRITE;  <span class="comment">-- 写锁：别人读写都阻塞</span></span><br><span class="line">UNLOCK TABLES;</span><br></pre></td></tr></table></figure>
<ul>
<li>元数据锁（MDL）：<ul>
<li>访问表时自动加，防止“查询中表被删&#x2F;改结构”</li>
<li>长事务 + DDL 极易导致 MDL 锁等待</li>
</ul>
</li>
</ul>
<h3 id="3）行级锁（Row-level-Lock，InnoDB-核心）"><a href="#3）行级锁（Row-level-Lock，InnoDB-核心）" class="headerlink" title="3）行级锁（Row-level Lock，InnoDB 核心）"></a>3）行级锁（Row-level Lock，InnoDB 核心）</h3><ul>
<li>粒度：<strong>单行或多行索引记录</strong></li>
<li>优点：<strong>并发高、冲突小</strong></li>
<li>缺点：开销大、依赖索引</li>
<li>重点：<strong>行锁是锁索引，不是锁数据行</strong><br>无索引&#x2F;索引失效 → <strong>行锁退化为表锁</strong>（灾难）</li>
</ul>
<hr>
<h2 id="三、按读写类型分类（S-X-锁）"><a href="#三、按读写类型分类（S-X-锁）" class="headerlink" title="三、按读写类型分类（S&#x2F;X 锁）"></a>三、按读写类型分类（S&#x2F;X 锁）</h2><h3 id="1）共享锁（S-锁-读锁）"><a href="#1）共享锁（S-锁-读锁）" class="headerlink" title="1）共享锁（S 锁 &#x2F; 读锁）"></a>1）共享锁（S 锁 &#x2F; 读锁）</h3><ul>
<li>特点：<strong>读读兼容、读写互斥</strong></li>
<li>加锁：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> t <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span> LOCK <span class="keyword">IN</span> SHARE MODE;</span><br></pre></td></tr></table></figure>
<ul>
<li>场景：要保证数据不被修改，同时允许别人读</li>
</ul>
<h3 id="2）排他锁（X-锁-写锁）"><a href="#2）排他锁（X-锁-写锁）" class="headerlink" title="2）排他锁（X 锁 &#x2F; 写锁）"></a>2）排他锁（X 锁 &#x2F; 写锁）</h3><ul>
<li>特点：<strong>写写互斥、读写互斥</strong>（独占）</li>
<li>自动加：<code>UPDATE / DELETE / INSERT</code></li>
<li>手动加：</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> t <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>场景：修改数据，禁止别人读或写</li>
</ul>
<h3 id="兼容性矩阵（必背）"><a href="#兼容性矩阵（必背）" class="headerlink" title="兼容性矩阵（必背）"></a>兼容性矩阵（必背）</h3><table>
<thead>
<tr>
<th>当前锁</th>
<th>加 S 锁</th>
<th>加 X 锁</th>
</tr>
</thead>
<tbody><tr>
<td>无锁</td>
<td>✅</td>
<td>✅</td>
</tr>
<tr>
<td>S 锁</td>
<td>✅</td>
<td>❌</td>
</tr>
<tr>
<td>X 锁</td>
<td>❌</td>
<td>❌</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、意向锁（IS-IX，表级）"><a href="#四、意向锁（IS-IX，表级）" class="headerlink" title="四、意向锁（IS &#x2F; IX，表级）"></a>四、意向锁（IS &#x2F; IX，表级）</h2><p>作用：<strong>快速判断表内有没有行锁，避免全表扫行锁</strong></p>
<ul>
<li>IS：意向共享锁（要加 S 行锁）</li>
<li>IX：意向排他锁（要加 X 行锁）</li>
</ul>
<p>规则：</p>
<ol>
<li>事务加行锁前，必须先加对应意向锁</li>
<li>意向锁之间<strong>完全兼容</strong></li>
<li>意向锁只和<strong>表级 S&#x2F;X 锁</strong>互斥</li>
</ol>
<hr>
<h2 id="五、InnoDB-行锁三大算法（RR-隔离级别默认）"><a href="#五、InnoDB-行锁三大算法（RR-隔离级别默认）" class="headerlink" title="五、InnoDB 行锁三大算法（RR 隔离级别默认）"></a>五、InnoDB 行锁三大算法（RR 隔离级别默认）</h2><h3 id="1）记录锁（Record-Lock）"><a href="#1）记录锁（Record-Lock）" class="headerlink" title="1）记录锁（Record Lock）"></a>1）记录锁（Record Lock）</h3><ul>
<li>锁：<strong>单个索引记录</strong></li>
<li>触发：<strong>唯一索引 + 精准等值查询（命中）</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">10</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>; <span class="comment">-- 只锁 id=10</span></span><br></pre></td></tr></table></figure>

<h3 id="2）间隙锁（Gap-Lock）"><a href="#2）间隙锁（Gap-Lock）" class="headerlink" title="2）间隙锁（Gap Lock）"></a>2）间隙锁（Gap Lock）</h3><ul>
<li>锁：<strong>索引之间的间隙（不包含记录）</strong></li>
<li>目的：<strong>防止幻读</strong>（禁止区间插入）</li>
<li>示例：表有 id&#x3D;1,3,5</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id <span class="keyword">BETWEEN</span> <span class="number">1</span> <span class="keyword">AND</span> <span class="number">5</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>;</span><br><span class="line"><span class="comment">-- 锁间隙：(1,3)、(3,5)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>特点：间隙锁之间<strong>兼容</strong>，只与<strong>插入意向锁</strong>互斥</li>
</ul>
<h3 id="3）临键锁（Next-Key-Lock）-记录锁-间隙锁"><a href="#3）临键锁（Next-Key-Lock）-记录锁-间隙锁" class="headerlink" title="3）临键锁（Next-Key Lock）&#x3D; 记录锁 + 间隙锁"></a>3）临键锁（Next-Key Lock）&#x3D; 记录锁 + 间隙锁</h3><ul>
<li>锁：<strong>左开右闭区间 (min, max]</strong></li>
<li>InnoDB <strong>RR 默认</strong>，解决幻读</li>
<li>示例：id&#x3D;1,3,5<ul>
<li>查 id&#x3D;3 → 锁 (1,3]</li>
<li>查 id&#x3D;4 → 锁 (3,5]</li>
</ul>
</li>
</ul>
<hr>
<h2 id="六、死锁（Deadlock）"><a href="#六、死锁（Deadlock）" class="headerlink" title="六、死锁（Deadlock）"></a>六、死锁（Deadlock）</h2><h3 id="1）必要条件（四个同时满足）"><a href="#1）必要条件（四个同时满足）" class="headerlink" title="1）必要条件（四个同时满足）"></a>1）必要条件（四个同时满足）</h3><ul>
<li>互斥：资源独占</li>
<li>持有并等待：拿着锁等别人的锁</li>
<li>不可剥夺：不能强行抢锁</li>
<li>循环等待：A 等 B，B 等 A</li>
</ul>
<h3 id="2）典型死锁示例"><a href="#2）典型死锁示例" class="headerlink" title="2）典型死锁示例"></a>2）典型死锁示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">事务1：BEGIN; UPDATE t SET a=1 WHERE id=1;</span><br><span class="line">事务2：BEGIN; UPDATE t SET a=1 WHERE id=2;</span><br><span class="line">事务1：UPDATE t SET a=1 WHERE id=2; -- 等事务2</span><br><span class="line">事务2：UPDATE t SET a=1 WHERE id=1; -- 等事务1 → 死锁</span><br></pre></td></tr></table></figure>

<h3 id="3）如何避免"><a href="#3）如何避免" class="headerlink" title="3）如何避免"></a>3）如何避免</h3><ul>
<li>按<strong>固定顺序</strong>访问资源（先 id&#x3D;1 再 id&#x3D;2）</li>
<li>控制<strong>事务大小</strong>，避免长事务</li>
<li>合理设置<strong>超时</strong>：<code>innodb_lock_wait_timeout=50</code></li>
<li>死锁检测：<code>SHOW ENGINE INNODB STATUS;</code></li>
</ul>
<hr>
<h2 id="七、实战高频坑（必避）"><a href="#七、实战高频坑（必避）" class="headerlink" title="七、实战高频坑（必避）"></a>七、实战高频坑（必避）</h2><ol>
<li><strong>无索引 → 行锁变表锁</strong>：<code>UPDATE t SET name=&#39;a&#39; WHERE age=20;</code>（age 无索引）</li>
<li><strong>长事务 + 慢查询 → 锁等待堆积</strong></li>
<li><strong>RR 间隙锁导致插入阻塞</strong>：范围查询容易触发</li>
<li><strong>忘记提交&#x2F;回滚</strong>：锁一直不释放</li>
</ol>
<hr>
<h2 id="八、总结（一句话记）"><a href="#八、总结（一句话记）" class="headerlink" title="八、总结（一句话记）"></a>八、总结（一句话记）</h2><ul>
<li><strong>粒度</strong>：全局 &gt; 表 &gt; 行（粒度越小，并发越高、开销越大）</li>
<li><strong>读写</strong>：S 锁共享，X 锁独占</li>
<li><strong>行锁三形态</strong>：记录锁、间隙锁、临键锁（RR 防幻读）</li>
<li><strong>死锁四条件</strong>：互斥、持有等待、不可剥夺、循环等待</li>
</ul>
<hr>
<h2 id="九、分步-SQL-脚本（双会话复现）"><a href="#九、分步-SQL-脚本（双会话复现）" class="headerlink" title="九、分步 SQL 脚本（双会话复现）"></a>九、分步 SQL 脚本（双会话复现）</h2><p>环境要求：<strong>InnoDB</strong>、默认 <strong>RR（可重复读）</strong>。请开<strong>两个会话</strong>（两个终端或客户端连接），按小节顺序分别执行，可观察阻塞、间隙锁与死锁。</p>
<h3 id="9-1-前置准备：建库建表与测试数据"><a href="#9-1-前置准备：建库建表与测试数据" class="headerlink" title="9.1 前置准备：建库建表与测试数据"></a>9.1 前置准备：建库建表与测试数据</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 1. 创建数据库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> lock_demo <span class="keyword">DEFAULT</span> <span class="keyword">CHARACTER SET</span> utf8mb4;</span><br><span class="line">USE lock_demo;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 创建测试表（InnoDB + 主键索引）</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> <span class="keyword">user</span>;</span><br><span class="line"><span class="keyword">CREATE TABLE</span> <span class="keyword">user</span> (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    age <span class="type">INT</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. 插入测试数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> <span class="keyword">user</span>(name,age) <span class="keyword">VALUES</span> </span><br><span class="line">(<span class="string">&#x27;张三&#x27;</span>,<span class="number">20</span>),</span><br><span class="line">(<span class="string">&#x27;李四&#x27;</span>,<span class="number">25</span>),</span><br><span class="line">(<span class="string">&#x27;王五&#x27;</span>,<span class="number">30</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看数据</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="9-2-行排他锁-FOR-UPDATE-阻塞演示"><a href="#9-2-行排他锁-FOR-UPDATE-阻塞演示" class="headerlink" title="9.2 行排他锁 FOR UPDATE 阻塞演示"></a>9.2 行排他锁 FOR UPDATE 阻塞演示</h3><p><strong>会话 1（先执行，不提交）</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="comment">-- 加行级排他锁</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>;</span><br><span class="line"><span class="comment">-- 故意不 COMMIT，锁一直持有</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 2（马上执行，会被阻塞）</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="comment">-- 尝试修改同一条 id=1，被行锁阻塞</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">21</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p><strong>收尾：会话 1 提交释放锁</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">COMMIT</span>;</span><br></pre></td></tr></table></figure>
<p>👉 现象：会话2立刻执行成功，<strong>行锁只锁 id&#x3D;1，不锁其他行</strong></p>
<hr>
<h3 id="9-3-无索引导致行锁退化为表锁"><a href="#9-3-无索引导致行锁退化为表锁" class="headerlink" title="9.3 无索引导致行锁退化为表锁"></a>9.3 无索引导致行锁退化为表锁</h3><p><strong>场景</strong>：<strong><code>age</code></strong> 无索引，按 <strong><code>age</code></strong> 更新。</p>
<p><strong>会话 1</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> name<span class="operator">=</span><span class="string">&#x27;张小三&#x27;</span> <span class="keyword">WHERE</span> age <span class="operator">=</span> <span class="number">20</span>;</span><br><span class="line"><span class="comment">-- 不提交</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 2</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="comment">-- 更新 id=2 无关数据，居然也被阻塞（行锁变表锁）</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> name<span class="operator">=</span><span class="string">&#x27;李小四&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure>
<p>👉 结论：<strong>查询条件无索引 → InnoDB 行锁退化成整张表锁，并发直接废掉</strong></p>
<hr>
<h3 id="9-4-共享锁与读写互斥演示"><a href="#9-4-共享锁与读写互斥演示" class="headerlink" title="9.4 共享锁与读写互斥演示"></a>9.4 共享锁与读写互斥演示</h3><p><strong>会话 1：加共享锁</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span> LOCK <span class="keyword">IN</span> SHARE MODE;</span><br></pre></td></tr></table></figure>

<p><strong>会话 2：普通读</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span>; <span class="comment">-- 正常查询</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 2：尝试写（被阻塞）</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">22</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span>; <span class="comment">-- 阻塞</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 1：提交释放</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">COMMIT</span>;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="9-5-间隙锁与临键锁阻塞插入（RR）"><a href="#9-5-间隙锁与临键锁阻塞插入（RR）" class="headerlink" title="9.5 间隙锁与临键锁阻塞插入（RR）"></a>9.5 间隙锁与临键锁阻塞插入（RR）</h3><p>表内现有数据：<strong><code>id</code></strong> 为 <strong>1、2、3</strong>（与 <strong>9.1</strong> 插入一致）。</p>
<p><strong>会话 1：范围查询加锁</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id <span class="keyword">BETWEEN</span> <span class="number">1</span> <span class="keyword">AND</span> <span class="number">3</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>;</span><br><span class="line"><span class="comment">-- 不提交</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 2：插入新行（被阻塞）</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">INSERT INTO</span> <span class="keyword">user</span>(id,name,age) <span class="keyword">VALUES</span>(<span class="number">4</span>,<span class="string">&#x27;赵六&#x27;</span>,<span class="number">28</span>); <span class="comment">-- 直接阻塞</span></span><br></pre></td></tr></table></figure>
<p>👉 原理：RR 级别临键锁+间隙锁，<strong>防止幻读，锁住区间不让插入</strong></p>
<hr>
<h3 id="9-6-经典死锁完整复现"><a href="#9-6-经典死锁完整复现" class="headerlink" title="9.6 经典死锁完整复现"></a>9.6 经典死锁完整复现</h3><p><strong>会话 1：第一步</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 事务1</span></span><br><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">21</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span>;</span><br><span class="line"><span class="comment">-- 先持有 id=1 行锁</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 2：第一步</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 事务2</span></span><br><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">26</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">2</span>;</span><br><span class="line"><span class="comment">-- 先持有 id=2 行锁</span></span><br></pre></td></tr></table></figure>

<p><strong>会话 1：第二步</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 事务1 尝试拿 id=2 锁，等待事务2</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">22</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">2</span>;</span><br></pre></td></tr></table></figure>

<p><strong>会话 2：第二步</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 事务2 尝试拿 id=1 锁，循环等待 → 立刻触发死锁</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">user</span> <span class="keyword">SET</span> age<span class="operator">=</span><span class="number">27</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>👉 现象：MySQL 自动检测死锁，<strong>直接回滚其中一个事务</strong>，报死锁错误。</p>
<hr>
<h3 id="9-7-查看锁状态与死锁日志"><a href="#9-7-查看锁状态与死锁日志" class="headerlink" title="9.7 查看锁状态与死锁日志"></a>9.7 查看锁状态与死锁日志</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看 InnoDB 锁、死锁详情</span></span><br><span class="line"><span class="keyword">SHOW</span> ENGINE INNODB STATUS;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看当前事务、锁等待</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> information_schema.innodb_trx;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> information_schema.innodb_locks;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> information_schema.innodb_lock_waits;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：<strong>MySQL 8.0</strong> 起 <strong><code>information_schema.innodb_locks</code></strong>、<strong><code>innodb_lock_waits</code></strong> 已移除；<strong>8.0</strong> 请优先看 <strong><code>SHOW ENGINE INNODB STATUS</code></strong>，并结合 <strong><code>performance_schema</code></strong> 中 <strong><code>data_locks</code></strong> &#x2F; <strong><code>data_lock_waits</code></strong> 等表（与版本文档为准）。</p>
<hr>
<h3 id="9-8-脚本实验小结（看完九必记）"><a href="#9-8-脚本实验小结（看完九必记）" class="headerlink" title="9.8 脚本实验小结（看完九必记）"></a>9.8 脚本实验小结（看完九必记）</h3><ol>
<li>行锁<strong>依赖索引</strong>，无索引直接降级表锁  </li>
<li><strong><code>FOR UPDATE</code></strong> 排他锁、<strong><code>LOCK IN SHARE MODE</code></strong> 共享锁  </li>
<li><strong>RR</strong> 隔离级别有<strong>临键锁 + 间隙锁</strong>，防幻读但会阻塞插入  </li>
<li>多事务<strong>交叉更新不同行</strong>极易死锁，统一更新顺序可避免  </li>
<li>长事务不提交 &#x3D; 锁不释放 &#x3D; 业务阻塞雪崩</li>
</ol>
<hr>
<h2 id="十、五大实验：现象、原理与面试标准答"><a href="#十、五大实验：现象、原理与面试标准答" class="headerlink" title="十、五大实验：现象、原理与面试标准答"></a>十、五大实验：现象、原理与面试标准答</h2><p>与<strong>第九节</strong>中五个脚本场景一一对应：<strong>现象 → 底层原理 → 面试可直接背诵的表述</strong>。</p>
<p><strong>统一前提</strong>：</p>
<ul>
<li>引擎：<strong>InnoDB</strong></li>
<li>隔离级别：<strong>RR 可重复读（默认）</strong></li>
<li>锁粒度：行锁依赖<strong>索引</strong>，无索引降级为表锁</li>
</ul>
<hr>
<h3 id="实验一：行排他锁-FOR-UPDATE-阻塞演示"><a href="#实验一：行排他锁-FOR-UPDATE-阻塞演示" class="headerlink" title="实验一：行排他锁 FOR UPDATE 阻塞演示"></a>实验一：行排他锁 FOR UPDATE 阻塞演示</h3><h4 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h4><p>会话1：开启事务 → <code>id=1</code> 加 <code>FOR UPDATE</code> 不提交<br>会话2：修改 <code>id=1</code> 被阻塞；修改 <code>id=2</code> 正常执行</p>
<h4 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h4><ol>
<li>同一条主键记录：<strong>写操作被阻塞</strong></li>
<li>不同主键记录：<strong>完全不受影响</strong></li>
<li>会话1 <code>commit</code> 后，会话2立刻执行成功</li>
</ol>
<h4 id="底层原理"><a href="#底层原理" class="headerlink" title="底层原理"></a>底层原理</h4><ol>
<li><code>FOR UPDATE</code> 加 <strong>行级排他锁（X锁）</strong></li>
<li>排他锁规则：<strong>写写互斥、读写互斥</strong></li>
<li>InnoDB 行锁<strong>只锁索引命中的单行</strong>，不影响其他行</li>
</ol>
<h4 id="面试标准答案"><a href="#面试标准答案" class="headerlink" title="面试标准答案"></a>面试标准答案</h4><blockquote>
<p>InnoDB 中通过主键&#x2F;唯一索引精准查询加 <code>FOR UPDATE</code>，会加<strong>行级排他锁</strong>；只锁定当前命中的单行记录，其他行不受影响，能保证高并发。排他锁与任何读写操作都互斥，事务提交后锁自动释放。</p>
</blockquote>
<hr>
<h3 id="实验二：无索引导致行锁退化为表锁"><a href="#实验二：无索引导致行锁退化为表锁" class="headerlink" title="实验二：无索引导致行锁退化为表锁"></a>实验二：无索引导致行锁退化为表锁</h3><h4 id="执行流程-1"><a href="#执行流程-1" class="headerlink" title="执行流程"></a>执行流程</h4><p>按无索引字段 <code>age=20</code> 更新，会话1不提交，会话2更新<strong>完全无关的 id&#x3D;2</strong> 也被阻塞</p>
<h4 id="现象-1"><a href="#现象-1" class="headerlink" title="现象"></a>现象</h4><p>明明更新不同行，却<strong>整张表都被锁住</strong>，所有写操作全部阻塞</p>
<h4 id="底层原理-1"><a href="#底层原理-1" class="headerlink" title="底层原理"></a>底层原理</h4><ol>
<li>InnoDB <strong>行锁是锁索引，不是锁数据行</strong></li>
<li>条件字段<strong>无索引</strong>，MySQL 无法定位具体行</li>
<li>只能逐行加行锁扫描全表，最终<strong>退化成表级锁</strong></li>
</ol>
<h4 id="面试标准答案-1"><a href="#面试标准答案-1" class="headerlink" title="面试标准答案"></a>面试标准答案</h4><blockquote>
<p>InnoDB 行锁<strong>必须依赖索引</strong>。如果更新&#x2F;删除条件没有索引或索引失效，MySQL 无法精确定位行记录，会扫描全表并逐行加锁，最终<strong>行锁降级为表锁</strong>，严重降低并发性能，开发中必须保证更新条件走索引。</p>
</blockquote>
<hr>
<h3 id="实验三：共享锁与读写互斥演示"><a href="#实验三：共享锁与读写互斥演示" class="headerlink" title="实验三：共享锁与读写互斥演示"></a>实验三：共享锁与读写互斥演示</h3><h4 id="执行流程-2"><a href="#执行流程-2" class="headerlink" title="执行流程"></a>执行流程</h4><p>会话1：<code>LOCK IN SHARE MODE</code> 加共享锁<br>会话2：普通<code>SELECT</code>能查，<code>UPDATE/DELETE</code>被阻塞</p>
<h4 id="现象-2"><a href="#现象-2" class="headerlink" title="现象"></a>现象</h4><ol>
<li><strong>多个事务可以同时加共享锁</strong>（读读兼容）</li>
<li>加了共享锁后，其他事务<strong>不能加排他锁、不能修改数据</strong></li>
<li>事务提交后，写操作立刻放行</li>
</ol>
<h4 id="底层原理-2"><a href="#底层原理-2" class="headerlink" title="底层原理"></a>底层原理</h4><ul>
<li>共享锁（S锁）：<strong>读读兼容，读写互斥</strong></li>
<li>意向锁辅助，S锁阻塞所有写操作，保证数据当前快照不被修改</li>
</ul>
<h4 id="面试标准答案-2"><a href="#面试标准答案-2" class="headerlink" title="面试标准答案"></a>面试标准答案</h4><blockquote>
<p>共享锁（S锁）又叫读锁，使用 <code>LOCK IN SHARE MODE</code> 加锁。特性是<strong>读读可以并发，读写、写写互斥</strong>；适合需要强一致性、临时禁止别人修改数据的场景，事务提交自动释放锁。</p>
</blockquote>
<hr>
<h3 id="实验四：间隙锁与临键锁阻塞插入（RR-与幻读）"><a href="#实验四：间隙锁与临键锁阻塞插入（RR-与幻读）" class="headerlink" title="实验四：间隙锁与临键锁阻塞插入（RR 与幻读）"></a>实验四：间隙锁与临键锁阻塞插入（RR 与幻读）</h3><h4 id="执行流程-3"><a href="#执行流程-3" class="headerlink" title="执行流程"></a>执行流程</h4><p>会话1：范围查询 <code>between 1 and 3 for update</code> 不提交<br>会话2：插入区间内新 ID，直接被阻塞</p>
<h4 id="现象-3"><a href="#现象-3" class="headerlink" title="现象"></a>现象</h4><p>没有任何行被修改，<strong>只是范围加锁</strong>，却无法往区间里插入新数据</p>
<h4 id="底层原理-3"><a href="#底层原理-3" class="headerlink" title="底层原理"></a>底层原理</h4><ol>
<li>MySQL RR 隔离级别默认使用 <strong>Next-Key Lock 临键锁</strong></li>
<li>临键锁 &#x3D; 记录锁 + 间隙锁</li>
<li>范围查询会锁住<strong>索引区间间隙</strong>，禁止其他事务插入新记录，<strong>解决幻读</strong></li>
<li>间隙锁只在 <strong>RR</strong> 存在，<strong>RC 隔离级别没有间隙锁</strong></li>
</ol>
<h4 id="面试标准答案-3"><a href="#面试标准答案-3" class="headerlink" title="面试标准答案"></a>面试标准答案</h4><blockquote>
<p>InnoDB 在 RR 可重复读隔离级别下，采用<strong>临键锁（Next-Key Lock）</strong> 解决幻读。临键锁由记录锁+间隙锁组成，范围查询会锁定索引左右间隙，禁止其他事务在区间内插入数据；RC 读已提交级别会关闭间隙锁，因此无法彻底避免幻读，但并发更高。</p>
</blockquote>
<hr>
<h3 id="实验五：经典死锁复现"><a href="#实验五：经典死锁复现" class="headerlink" title="实验五：经典死锁复现"></a>实验五：经典死锁复现</h3><h4 id="执行流程-4"><a href="#执行流程-4" class="headerlink" title="执行流程"></a>执行流程</h4><ul>
<li>事务1：先锁 id&#x3D;1 → 再请求锁 id&#x3D;2</li>
<li>事务2：先锁 id&#x3D;2 → 再请求锁 id&#x3D;1</li>
<li>互相等待，立刻触发死锁，MySQL 自动回滚一个事务</li>
</ul>
<h4 id="现象-4"><a href="#现象-4" class="headerlink" title="现象"></a>现象</h4><ol>
<li>互相持有对方需要的锁，循环等待</li>
<li>InnoDB <strong>自动死锁检测</strong>，回滚代价小的事务，抛出死锁异常</li>
<li>不会无限阻塞</li>
</ol>
<h4 id="底层原理-4"><a href="#底层原理-4" class="headerlink" title="底层原理"></a>底层原理</h4><p>死锁产生<strong>四个必要条件</strong>同时满足：</p>
<ol>
<li>互斥条件：锁资源独占</li>
<li>持有并等待：自己拿着锁，还等别人的锁</li>
<li>不可剥夺：锁不能被强行抢占</li>
<li>循环等待：事务形成等待环路</li>
</ol>
<h4 id="面试标准答案（必背）"><a href="#面试标准答案（必背）" class="headerlink" title="面试标准答案（必背）"></a>面试标准答案（必背）</h4><blockquote>
<p>死锁产生必须同时满足四个条件：<strong>互斥、持有并等待、不可剥夺、循环等待</strong>。<br>规避方案：</p>
<ol>
<li>统一多事务<strong>访问资源的顺序</strong>；</li>
<li>拆分长事务，尽早提交；</li>
<li>避免在事务中做无关耗时操作；</li>
<li>合理设置锁等待超时时间。<br>InnoDB 自带<strong>死锁检测机制</strong>，发现循环等待会自动回滚一个事务，避免程序卡死。</li>
</ol>
</blockquote>
<hr>
<h2 id="十一、面试高频一句话速记"><a href="#十一、面试高频一句话速记" class="headerlink" title="十一、面试高频一句话速记"></a>十一、面试高频一句话速记</h2><ol>
<li>行锁<strong>靠索引</strong>，无索引变表锁  </li>
<li><strong>S</strong> 锁读读兼容，<strong>X</strong> 锁完全独占  </li>
<li><strong>RR</strong> 有间隙锁防幻读，<strong>RC</strong> 没有  </li>
<li>交叉改不同行、顺序乱写易出死锁</li>
</ol>
<hr>
<h2 id="十二、二十道高频面试题与标准答"><a href="#十二、二十道高频面试题与标准答" class="headerlink" title="十二、二十道高频面试题与标准答"></a>十二、二十道高频面试题与标准答</h2><p>全部<strong>面试可直接背诵</strong>，基于 <strong>InnoDB</strong>、<strong>RR</strong> 隔离级别，精简不废话。</p>
<h3 id="1-MySQL-有哪些锁分类？"><a href="#1-MySQL-有哪些锁分类？" class="headerlink" title="1. MySQL 有哪些锁分类？"></a>1. MySQL 有哪些锁分类？</h3><p><strong>答案</strong></p>
<ol>
<li>按粒度：<strong>全局锁、表级锁、行级锁</strong></li>
<li>按读写：<strong>共享锁S、排他锁X</strong></li>
<li>InnoDB 特有：<strong>意向锁、记录锁、间隙锁、临键锁</strong></li>
</ol>
<h3 id="2-全局锁作用是什么？怎么加？"><a href="#2-全局锁作用是什么？怎么加？" class="headerlink" title="2. 全局锁作用是什么？怎么加？"></a>2. 全局锁作用是什么？怎么加？</h3><p><strong>答案</strong><br>整库只读，所有库表只能读不能写、阻塞 DDL&#x2F;DML。<br>加锁：<code>FLUSH TABLES WITH READ LOCK;</code><br>解锁：<code>UNLOCK TABLES;</code><br>多用于全库逻辑备份；生产优先用 <code>mysqldump --single-transaction</code> 快照备份，不加锁。</p>
<h3 id="3-什么是-MDL-元数据锁？"><a href="#3-什么是-MDL-元数据锁？" class="headerlink" title="3. 什么是 MDL 元数据锁？"></a>3. 什么是 MDL 元数据锁？</h3><p><strong>答案</strong><br>访问表时<strong>自动加</strong>，保护表结构不被中途修改&#x2F;删除。<br>长事务不提交 + 执行 DDL 改表结构，会造成 MDL 锁等待阻塞，线上高发故障点。</p>
<h3 id="4-表锁和行锁区别？"><a href="#4-表锁和行锁区别？" class="headerlink" title="4. 表锁和行锁区别？"></a>4. 表锁和行锁区别？</h3><p><strong>答案</strong></p>
<ul>
<li>表锁：粒度大、开销小、并发低、MyISAM 默认</li>
<li>行锁：粒度小、开销大、并发高、InnoDB 支持<br>InnoDB 优先用行锁提升并发。</li>
</ul>
<h3 id="5-共享锁S、排他锁X-特性？"><a href="#5-共享锁S、排他锁X-特性？" class="headerlink" title="5. 共享锁S、排他锁X 特性？"></a>5. 共享锁S、排他锁X 特性？</h3><p><strong>答案</strong></p>
<ul>
<li>共享锁S：<strong>读读兼容，读写&#x2F;写写互斥</strong></li>
<li>排他锁X：<strong>读写、写写全部互斥，独占资源</strong><br>加锁：<br>S：<code>select ... lock in share mode</code><br>X：<code>select ... for update</code></li>
</ul>
<h3 id="6-意向锁-IS-IX-作用？"><a href="#6-意向锁-IS-IX-作用？" class="headerlink" title="6. 意向锁 IS&#x2F;IX 作用？"></a>6. 意向锁 IS&#x2F;IX 作用？</h3><p><strong>答案</strong><br>意向锁是<strong>表级锁</strong>，事务加行锁前必须先加意向锁。<br>作用：快速判断表内是否有行锁，不用遍历所有行锁，提升加锁效率。</p>
<ul>
<li>IS：要加 S 行锁</li>
<li>IX：要加 X 行锁<br>意向锁之间互相兼容，只和表级 S&#x2F;X 锁互斥。</li>
</ul>
<h3 id="7-InnoDB-行锁本质是什么？"><a href="#7-InnoDB-行锁本质是什么？" class="headerlink" title="7. InnoDB 行锁本质是什么？"></a>7. InnoDB 行锁本质是什么？</h3><p><strong>答案</strong><br><strong>行锁锁的是索引，不是物理数据行</strong>。<br>查询条件<strong>无索引、索引失效</strong>，行锁会<strong>退化成表锁</strong>，并发骤降。</p>
<h3 id="8-什么情况下行锁会降级为表锁？"><a href="#8-什么情况下行锁会降级为表锁？" class="headerlink" title="8. 什么情况下行锁会降级为表锁？"></a>8. 什么情况下行锁会降级为表锁？</h3><p><strong>答案</strong></p>
<ol>
<li>where 条件<strong>无索引字段</strong></li>
<li>索引失效（函数、隐式类型转换、like 前缀模糊等）</li>
<li>没有命中任何记录，全表扫描加锁</li>
</ol>
<h3 id="9-InnoDB-三种行锁算法？"><a href="#9-InnoDB-三种行锁算法？" class="headerlink" title="9. InnoDB 三种行锁算法？"></a>9. InnoDB 三种行锁算法？</h3><p><strong>答案</strong></p>
<ol>
<li><strong>记录锁</strong>：锁单行索引记录</li>
<li><strong>间隙锁 Gap Lock</strong>：锁索引间隙，不锁记录，防止幻读插入</li>
<li><strong>临键锁 Next-Key Lock</strong> &#x3D; 记录锁+间隙锁，左开右闭区间，RR 默认</li>
</ol>
<h3 id="10-间隙锁什么时候生效？什么时候关闭？"><a href="#10-间隙锁什么时候生效？什么时候关闭？" class="headerlink" title="10. 间隙锁什么时候生效？什么时候关闭？"></a>10. 间隙锁什么时候生效？什么时候关闭？</h3><p><strong>答案</strong></p>
<ul>
<li>生效：<strong>RR 可重复读</strong>隔离级别、范围查询&#x2F;不走唯一索引等值查询</li>
<li>关闭：<strong>RC 读已提交</strong> 没有间隙锁，并发更高，但会有幻读</li>
</ul>
<h3 id="11-临键锁解决了什么问题？"><a href="#11-临键锁解决了什么问题？" class="headerlink" title="11. 临键锁解决了什么问题？"></a>11. 临键锁解决了什么问题？</h3><p><strong>答案</strong><br>在 RR 隔离级别下，通过<strong>临键锁+间隙锁</strong>彻底<strong>解决幻读</strong>，禁止其他事务在索引区间插入新数据。</p>
<h3 id="12-什么是幻读？怎么解决？"><a href="#12-什么是幻读？怎么解决？" class="headerlink" title="12. 什么是幻读？怎么解决？"></a>12. 什么是幻读？怎么解决？</h3><p><strong>答案</strong><br>同一事务内，前后两次范围查询，查到别人新插入的数据。<br>解决：</p>
<ul>
<li>数据库层：InnoDB RR + 临键锁</li>
<li>业务层：悲观锁、分布式锁限制插入</li>
</ul>
<h3 id="13-死锁产生的四个必要条件？"><a href="#13-死锁产生的四个必要条件？" class="headerlink" title="13. 死锁产生的四个必要条件？"></a>13. 死锁产生的四个必要条件？</h3><p><strong>答案</strong></p>
<ol>
<li>互斥：锁资源独占</li>
<li>持有并等待：拿着自己的锁，等待别人的锁</li>
<li>不可剥夺：锁不能被强行抢占</li>
<li>循环等待：事务互相等待形成环路</li>
</ol>
<h3 id="14-MySQL-怎么处理死锁？"><a href="#14-MySQL-怎么处理死锁？" class="headerlink" title="14. MySQL 怎么处理死锁？"></a>14. MySQL 怎么处理死锁？</h3><p><strong>答案</strong><br>InnoDB 有<strong>自动死锁检测</strong>，检测到循环等待后，<strong>自动回滚代价更小的一个事务</strong>，抛出死锁异常，避免无限阻塞。</p>
<h3 id="15-如何避免-MySQL-死锁？"><a href="#15-如何避免-MySQL-死锁？" class="headerlink" title="15. 如何避免 MySQL 死锁？"></a>15. 如何避免 MySQL 死锁？</h3><p><strong>答案</strong></p>
<ol>
<li>统一所有事务<strong>访问资源&#x2F;更新行的顺序</strong></li>
<li>精简事务，<strong>避免长事务</strong>，尽早 commit</li>
<li>事务内不做耗时网络&#x2F;IO 操作</li>
<li>合理设置锁等待超时 <code>innodb_lock_wait_timeout</code></li>
</ol>
<h3 id="16-for-update-和-lock-in-share-mode-区别？"><a href="#16-for-update-和-lock-in-share-mode-区别？" class="headerlink" title="16. for update 和 lock in share mode 区别？"></a>16. for update 和 lock in share mode 区别？</h3><p><strong>答案</strong></p>
<ul>
<li><code>for update</code>：加<strong>排他锁X</strong>，别人不能读也不能改</li>
<li><code>lock in share mode</code>：加<strong>共享锁S</strong>，别人可以读、不能改</li>
</ul>
<h3 id="17-RR-和-RC-在锁层面最大区别？"><a href="#17-RR-和-RC-在锁层面最大区别？" class="headerlink" title="17. RR 和 RC 在锁层面最大区别？"></a>17. RR 和 RC 在锁层面最大区别？</h3><p><strong>答案</strong></p>
<ol>
<li>RR 有<strong>间隙锁&#x2F;临键锁</strong>，防幻读；RC 没有</li>
<li>RC 行锁范围更小、并发更高，但存在幻读</li>
<li>RC 锁释放更早，阻塞更少</li>
</ol>
<h3 id="18-长事务有什么锁风险？"><a href="#18-长事务有什么锁风险？" class="headerlink" title="18. 长事务有什么锁风险？"></a>18. 长事务有什么锁风险？</h3><p><strong>答案</strong><br>长事务不提交：</p>
<ol>
<li>行锁、MDL 锁长期不释放</li>
<li>造成大量锁等待、业务阻塞</li>
<li>undo log 不能清理，占用磁盘</li>
<li>容易触发死锁、数据库性能雪崩</li>
</ol>
<h3 id="19-如何查看-MySQL-当前锁和死锁？"><a href="#19-如何查看-MySQL-当前锁和死锁？" class="headerlink" title="19. 如何查看 MySQL 当前锁和死锁？"></a>19. 如何查看 MySQL 当前锁和死锁？</h3><p><strong>答案</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看最近死锁、锁详情</span></span><br><span class="line"><span class="keyword">SHOW</span> ENGINE INNODB STATUS;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看事务、锁、锁等待</span></span><br><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> information_schema.innodb_trx;</span><br><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> information_schema.innodb_locks;</span><br><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> information_schema.innodb_lock_waits;</span><br></pre></td></tr></table></figure>

<h3 id="20-线上开发写-UPDATE-防锁坑要注意什么？"><a href="#20-线上开发写-UPDATE-防锁坑要注意什么？" class="headerlink" title="20. 线上开发写 UPDATE 防锁坑要注意什么？"></a>20. 线上开发写 UPDATE 防锁坑要注意什么？</h3><p><strong>答案</strong></p>
<ol>
<li>更新条件<strong>必须走索引</strong>，防止行锁变表锁</li>
<li>尽量用<strong>主键&#x2F;唯一索引</strong>精准等值，少用范围查询</li>
<li>事务尽量短小，及时提交</li>
<li>禁止大事务批量更新、避免范围锁区间过大</li>
<li>统一更新顺序，避免交叉更新产生死锁</li>
</ol>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 高级查询示例</title>
    <url>/2026/05/03/MySQL/MySQL%E9%AB%98%E7%BA%A7%E6%9F%A5%E8%AF%A2/</url>
    <content><![CDATA[<p>面向 <strong>SELECT</strong> 侧的常用进阶写法；示例基于 <strong>InnoDB</strong>，<strong><code>utf8mb4</code></strong>。<strong>公共表表达式（WITH）</strong> 与 <strong>窗口函数</strong> 需 <strong>MySQL 8.0+</strong>（文中已标注）。</p>
<hr>
<h2 id="示例数据（一键建表）"><a href="#示例数据（一键建表）" class="headerlink" title="示例数据（一键建表）"></a>示例数据（一键建表）</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> adv_demo <span class="keyword">DEFAULT</span> <span class="keyword">CHARACTER SET</span> utf8mb4 <span class="keyword">COLLATE</span> utf8mb4_unicode_ci;</span><br><span class="line">USE adv_demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> emp;</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> dept;</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> orders;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> dept (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> emp (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    dept_id <span class="type">INT</span>,</span><br><span class="line">    salary <span class="type">DECIMAL</span>(<span class="number">10</span>,<span class="number">2</span>),</span><br><span class="line">    hire_date <span class="type">DATE</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_emp_dept <span class="keyword">FOREIGN KEY</span> (dept_id) <span class="keyword">REFERENCES</span> dept(id)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> orders (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT,</span><br><span class="line">    emp_id <span class="type">INT</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    amount <span class="type">DECIMAL</span>(<span class="number">10</span>,<span class="number">2</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    order_date <span class="type">DATE</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_ord_emp <span class="keyword">FOREIGN KEY</span> (emp_id) <span class="keyword">REFERENCES</span> emp(id)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4;</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> dept (id, name) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="string">&#x27;研发&#x27;</span>), (<span class="number">2</span>, <span class="string">&#x27;市场&#x27;</span>), (<span class="number">3</span>, <span class="string">&#x27;财务&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> emp (id, name, dept_id, salary, hire_date) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="string">&#x27;张三&#x27;</span>, <span class="number">1</span>, <span class="number">15000.00</span>, <span class="string">&#x27;2020-03-01&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="string">&#x27;李四&#x27;</span>, <span class="number">1</span>, <span class="number">18000.00</span>, <span class="string">&#x27;2019-07-15&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="string">&#x27;王五&#x27;</span>, <span class="number">2</span>, <span class="number">12000.00</span>, <span class="string">&#x27;2021-01-10&#x27;</span>),</span><br><span class="line">(<span class="number">4</span>, <span class="string">&#x27;赵六&#x27;</span>, <span class="number">2</span>, <span class="keyword">NULL</span>, <span class="string">&#x27;2022-05-20&#x27;</span>),</span><br><span class="line">(<span class="number">5</span>, <span class="string">&#x27;钱七&#x27;</span>, <span class="keyword">NULL</span>, <span class="number">9000.00</span>, <span class="string">&#x27;2023-11-01&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> orders (emp_id, amount, order_date) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="number">100.00</span>, <span class="string">&#x27;2024-06-01&#x27;</span>),</span><br><span class="line">(<span class="number">1</span>, <span class="number">200.50</span>, <span class="string">&#x27;2024-06-10&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="number">300.00</span>, <span class="string">&#x27;2024-06-12&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="number">50.00</span>, <span class="string">&#x27;2024-06-15&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（仅展示插入后的基表；<strong><code>orders.id</code></strong> 由自增生成，下列 <strong><code>orders</code></strong> 行为常见顺序）：</p>
<p><strong><code>dept</code></strong></p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>研发</td>
</tr>
<tr>
<td>2</td>
<td>市场</td>
</tr>
<tr>
<td>3</td>
<td>财务</td>
</tr>
</tbody></table>
<p><strong><code>emp</code></strong></p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>dept_id</th>
<th>salary</th>
<th>hire_date</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>张三</td>
<td>1</td>
<td>15000.00</td>
<td>2020-03-01</td>
</tr>
<tr>
<td>2</td>
<td>李四</td>
<td>1</td>
<td>18000.00</td>
<td>2019-07-15</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>2</td>
<td>12000.00</td>
<td>2021-01-10</td>
</tr>
<tr>
<td>4</td>
<td>赵六</td>
<td>2</td>
<td>NULL</td>
<td>2022-05-20</td>
</tr>
<tr>
<td>5</td>
<td>钱七</td>
<td>NULL</td>
<td>9000.00</td>
<td>2023-11-01</td>
</tr>
</tbody></table>
<p><strong><code>orders</code></strong>（<strong><code>id</code></strong> 以实际自增值为准，下表按 <strong><code>id</code> 1～4</strong> 示意）</p>
<table>
<thead>
<tr>
<th>id</th>
<th>emp_id</th>
<th>amount</th>
<th>order_date</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>1</td>
<td>100.00</td>
<td>2024-06-01</td>
</tr>
<tr>
<td>2</td>
<td>1</td>
<td>200.50</td>
<td>2024-06-10</td>
</tr>
<tr>
<td>3</td>
<td>2</td>
<td>300.00</td>
<td>2024-06-12</td>
</tr>
<tr>
<td>4</td>
<td>3</td>
<td>50.00</td>
<td>2024-06-15</td>
</tr>
</tbody></table>
<p>下文默认 <strong><code>USE adv_demo;</code></strong>。各小节 <strong>「示例结果」</strong> 均按上述数据推算（与本地执行相比，仅可能出现行顺序差异）。</p>
<hr>
<h2 id="一、多表连接-JOIN"><a href="#一、多表连接-JOIN" class="headerlink" title="一、多表连接 JOIN"></a>一、多表连接 JOIN</h2><h3 id="1-内连接-INNER-JOIN（两表都有匹配才出现）"><a href="#1-内连接-INNER-JOIN（两表都有匹配才出现）" class="headerlink" title="1. 内连接 INNER JOIN（两表都有匹配才出现）"></a>1. 内连接 INNER JOIN（两表都有匹配才出现）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.id, e.name, d.name <span class="keyword">AS</span> dept_name, e.salary</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（无部门者不出现在内连接中）：</p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>dept_name</th>
<th>salary</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>张三</td>
<td>研发</td>
<td>15000.00</td>
</tr>
<tr>
<td>2</td>
<td>李四</td>
<td>研发</td>
<td>18000.00</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>市场</td>
<td>12000.00</td>
</tr>
<tr>
<td>4</td>
<td>赵六</td>
<td>市场</td>
<td>NULL</td>
</tr>
</tbody></table>
<h3 id="2-左外连接-LEFT-JOIN（保留左表全部行，右表无匹配则右列为-NULL）"><a href="#2-左外连接-LEFT-JOIN（保留左表全部行，右表无匹配则右列为-NULL）" class="headerlink" title="2. 左外连接 LEFT JOIN（保留左表全部行，右表无匹配则右列为 NULL）"></a>2. 左外连接 LEFT JOIN（保留左表全部行，右表无匹配则右列为 NULL）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name, d.name <span class="keyword">AS</span> dept_name</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>dept_name</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>研发</td>
</tr>
<tr>
<td>李四</td>
<td>研发</td>
</tr>
<tr>
<td>王五</td>
<td>市场</td>
</tr>
<tr>
<td>赵六</td>
<td>市场</td>
</tr>
<tr>
<td>钱七</td>
<td>NULL</td>
</tr>
</tbody></table>
<h3 id="3-右外连接-RIGHT-JOIN"><a href="#3-右外连接-RIGHT-JOIN" class="headerlink" title="3. 右外连接 RIGHT JOIN"></a>3. 右外连接 RIGHT JOIN</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name, d.name <span class="keyword">AS</span> dept_name</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（右表 <strong><code>dept</code></strong> 全保留；无员工部门左表为 <strong>NULL</strong>；行顺序依优化器可能不同）：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>dept_name</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>研发</td>
</tr>
<tr>
<td>李四</td>
<td>研发</td>
</tr>
<tr>
<td>王五</td>
<td>市场</td>
</tr>
<tr>
<td>赵六</td>
<td>市场</td>
</tr>
<tr>
<td>NULL</td>
<td>财务</td>
</tr>
</tbody></table>
<h3 id="4-多表连接（员工-部门-订单汇总）"><a href="#4-多表连接（员工-部门-订单汇总）" class="headerlink" title="4. 多表连接（员工 + 部门 + 订单汇总）"></a>4. 多表连接（员工 + 部门 + 订单汇总）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name,</span><br><span class="line">       d.name <span class="keyword">AS</span> dept_name,</span><br><span class="line">       <span class="built_in">COUNT</span>(o.id) <span class="keyword">AS</span> order_cnt,</span><br><span class="line">       IFNULL(<span class="built_in">SUM</span>(o.amount), <span class="number">0</span>) <span class="keyword">AS</span> total_amount</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> orders o <span class="keyword">ON</span> o.emp_id <span class="operator">=</span> e.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> e.id, e.name, d.name;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>order_cnt</code></strong> &#x2F; <strong><code>total_amount</code></strong> 按 <strong><code>orders</code></strong> 示意 <strong><code>id</code></strong> 推算）：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>dept_name</th>
<th>order_cnt</th>
<th>total_amount</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>研发</td>
<td>2</td>
<td>300.50</td>
</tr>
<tr>
<td>李四</td>
<td>研发</td>
<td>1</td>
<td>300.00</td>
</tr>
<tr>
<td>王五</td>
<td>市场</td>
<td>1</td>
<td>50.00</td>
</tr>
<tr>
<td>赵六</td>
<td>市场</td>
<td>0</td>
<td>0</td>
</tr>
<tr>
<td>钱七</td>
<td>NULL</td>
<td>0</td>
<td>0</td>
</tr>
</tbody></table>
<hr>
<h2 id="二、子查询"><a href="#二、子查询" class="headerlink" title="二、子查询"></a>二、子查询</h2><h3 id="1-标量子查询（返回单行单列，可当表达式）"><a href="#1-标量子查询（返回单行单列，可当表达式）" class="headerlink" title="1. 标量子查询（返回单行单列，可当表达式）"></a>1. 标量子查询（返回单行单列，可当表达式）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, salary,</span><br><span class="line">       (<span class="keyword">SELECT</span> <span class="built_in">AVG</span>(salary) <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> salary <span class="keyword">IS</span> <span class="keyword">NOT NULL</span>) <span class="keyword">AS</span> company_avg_salary</span><br><span class="line"><span class="keyword">FROM</span> emp;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（全表平均薪 <strong>(15000+18000+12000+9000)&#x2F;4 &#x3D; 13500</strong>；<strong><code>NULL</code></strong> 薪资不参与子查询平均）：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>salary</th>
<th>company_avg_salary</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>15000.00</td>
<td>13500.000000</td>
</tr>
<tr>
<td>李四</td>
<td>18000.00</td>
<td>13500.000000</td>
</tr>
<tr>
<td>王五</td>
<td>12000.00</td>
<td>13500.000000</td>
</tr>
<tr>
<td>赵六</td>
<td>NULL</td>
<td>13500.000000</td>
</tr>
<tr>
<td>钱七</td>
<td>9000.00</td>
<td>13500.000000</td>
</tr>
</tbody></table>
<h3 id="2-IN-NOT-IN（集合成员判断）"><a href="#2-IN-NOT-IN（集合成员判断）" class="headerlink" title="2. IN &#x2F; NOT IN（集合成员判断）"></a>2. IN &#x2F; NOT IN（集合成员判断）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name <span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">WHERE</span> dept_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name <span class="keyword">IN</span> (<span class="string">&#x27;研发&#x27;</span>, <span class="string">&#x27;市场&#x27;</span>));</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：</p>
<table>
<thead>
<tr>
<th>name</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
</tr>
<tr>
<td>李四</td>
</tr>
<tr>
<td>王五</td>
</tr>
<tr>
<td>赵六</td>
</tr>
</tbody></table>
<h3 id="3-EXISTS-NOT-EXISTS（关心「是否存在」，常比大-IN-更省内存）"><a href="#3-EXISTS-NOT-EXISTS（关心「是否存在」，常比大-IN-更省内存）" class="headerlink" title="3. EXISTS &#x2F; NOT EXISTS（关心「是否存在」，常比大 IN 更省内存）"></a>3. EXISTS &#x2F; NOT EXISTS（关心「是否存在」，常比大 <strong>IN</strong> 更省内存）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">EXISTS</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> orders o <span class="keyword">WHERE</span> o.emp_id <span class="operator">=</span> e.id</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（在 <strong><code>orders</code></strong> 中出现过 <strong><code>emp_id</code></strong> 的员工）：</p>
<table>
<thead>
<tr>
<th>name</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
</tr>
<tr>
<td>李四</td>
</tr>
<tr>
<td>王五</td>
</tr>
</tbody></table>
<h3 id="4-派生表（FROM-子查询，须有别名）"><a href="#4-派生表（FROM-子查询，须有别名）" class="headerlink" title="4. 派生表（FROM 子查询，须有别名）"></a>4. 派生表（FROM 子查询，须有别名）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> t.dept_name, t.cnt</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> d.name <span class="keyword">AS</span> dept_name, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> cnt</span><br><span class="line">    <span class="keyword">FROM</span> emp e</span><br><span class="line">    <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> d.id, d.name</span><br><span class="line">) <span class="keyword">AS</span> t</span><br><span class="line"><span class="keyword">WHERE</span> t.cnt <span class="operator">&gt;=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：</p>
<table>
<thead>
<tr>
<th>dept_name</th>
<th>cnt</th>
</tr>
</thead>
<tbody><tr>
<td>研发</td>
<td>2</td>
</tr>
<tr>
<td>市场</td>
<td>2</td>
</tr>
</tbody></table>
<hr>
<h2 id="三、UNION-与-UNION-ALL"><a href="#三、UNION-与-UNION-ALL" class="headerlink" title="三、UNION 与 UNION ALL"></a>三、UNION 与 UNION ALL</h2><p><strong><code>UNION</code></strong>：合并结果集并<strong>去重</strong>；<strong><code>UNION ALL</code></strong>：不去重，性能通常更好。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, <span class="string">&#x27;emp&#x27;</span> <span class="keyword">AS</span> src <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> <span class="number">1</span></span><br><span class="line"><span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line"><span class="keyword">SELECT</span> name, <span class="string">&#x27;dept&#x27;</span> <span class="keyword">AS</span> src <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>UNION ALL</code></strong> 保留上下两段所有行）：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>src</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>emp</td>
</tr>
<tr>
<td>李四</td>
<td>emp</td>
</tr>
<tr>
<td>研发</td>
<td>dept</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、公共表表达式-WITH（CTE）（MySQL-8-0-）"><a href="#四、公共表表达式-WITH（CTE）（MySQL-8-0-）" class="headerlink" title="四、公共表表达式 WITH（CTE）（MySQL 8.0+）"></a>四、公共表表达式 WITH（CTE）（MySQL 8.0+）</h2><p>把复杂查询拆成可读的多段「临时结果」。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WITH</span> dept_stat <span class="keyword">AS</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> dept_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> emp_cnt, <span class="built_in">AVG</span>(salary) <span class="keyword">AS</span> avg_sal</span><br><span class="line">    <span class="keyword">FROM</span> emp</span><br><span class="line">    <span class="keyword">WHERE</span> salary <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> dept_id</span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> d.name, ds.emp_cnt, ROUND(ds.avg_sal, <span class="number">2</span>) <span class="keyword">AS</span> avg_salary</span><br><span class="line"><span class="keyword">FROM</span> dept_stat ds</span><br><span class="line"><span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> d.id <span class="operator">=</span> ds.dept_id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>dept_stat</code></strong> 中 <strong><code>dept_id</code></strong> 为 <strong>NULL</strong> 的分组与 <strong><code>dept</code></strong> 内连接时被过滤；<strong><code>AVG(salary)</code></strong> 在部门 <strong>2</strong> 仅统计非 <strong>NULL</strong> 薪资）：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>emp_cnt</th>
<th>avg_salary</th>
</tr>
</thead>
<tbody><tr>
<td>研发</td>
<td>2</td>
<td>16500.00</td>
</tr>
<tr>
<td>市场</td>
<td>1</td>
<td>12000.00</td>
</tr>
</tbody></table>
<p><strong>递归 CTE</strong>（如组织架构、树路径）亦在 <strong>8.0+</strong> 支持，需 <strong><code>WITH RECURSIVE</code></strong>，此处从略。</p>
<hr>
<h2 id="五、窗口函数（MySQL-8-0-）"><a href="#五、窗口函数（MySQL-8-0-）" class="headerlink" title="五、窗口函数（MySQL 8.0+）"></a>五、窗口函数（MySQL 8.0+）</h2><p>在<strong>不减少结果行数</strong>的前提下，对分区内的行做排序、累计等。</p>
<h3 id="1-分区排序：ROW-NUMBER-RANK-DENSE-RANK"><a href="#1-分区排序：ROW-NUMBER-RANK-DENSE-RANK" class="headerlink" title="1. 分区排序：ROW_NUMBER &#x2F; RANK &#x2F; DENSE_RANK"></a>1. 分区排序：ROW_NUMBER &#x2F; RANK &#x2F; DENSE_RANK</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, name, dept_id, salary,</span><br><span class="line">       <span class="built_in">ROW_NUMBER</span>() <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> dept_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> salary <span class="keyword">DESC</span>) <span class="keyword">AS</span> rn_in_dept,</span><br><span class="line">       <span class="built_in">RANK</span>()       <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> dept_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> salary <span class="keyword">DESC</span>) <span class="keyword">AS</span> rk,</span><br><span class="line">       <span class="built_in">DENSE_RANK</span>() <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> dept_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> salary <span class="keyword">DESC</span>) <span class="keyword">AS</span> drk</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">WHERE</span> dept_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>dept_id = 1</code></strong>：薪资降序 <strong>李四 → 张三</strong>；<strong><code>dept_id = 2</code></strong>：<strong>王五</strong> 在前、<strong><code>NULL</code></strong> 薪资在后；<strong><code>rk</code> &#x2F; <code>drk</code></strong> 在无并列时与 <strong><code>rn_in_dept</code></strong> 一致）：</p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>dept_id</th>
<th>salary</th>
<th>rn_in_dept</th>
<th>rk</th>
<th>drk</th>
</tr>
</thead>
<tbody><tr>
<td>2</td>
<td>李四</td>
<td>1</td>
<td>18000.00</td>
<td>1</td>
<td>1</td>
<td>1</td>
</tr>
<tr>
<td>1</td>
<td>张三</td>
<td>1</td>
<td>15000.00</td>
<td>2</td>
<td>2</td>
<td>2</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>2</td>
<td>12000.00</td>
<td>1</td>
<td>1</td>
<td>1</td>
</tr>
<tr>
<td>4</td>
<td>赵六</td>
<td>2</td>
<td>NULL</td>
<td>2</td>
<td>2</td>
<td>2</td>
</tr>
</tbody></table>
<h3 id="2-累计与滑动：SUM-AVG-OVER"><a href="#2-累计与滑动：SUM-AVG-OVER" class="headerlink" title="2. 累计与滑动：SUM &#x2F; AVG OVER"></a>2. 累计与滑动：SUM &#x2F; AVG OVER</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> emp_id, order_date, amount,</span><br><span class="line">       <span class="built_in">SUM</span>(amount) <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> emp_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> order_date</span><br><span class="line">                           <span class="keyword">ROWS</span> <span class="keyword">BETWEEN</span> UNBOUNDED PRECEDING <span class="keyword">AND</span> <span class="keyword">CURRENT</span> <span class="type">ROW</span>) <span class="keyword">AS</span> running_sum</span><br><span class="line"><span class="keyword">FROM</span> orders;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>running_sum</code></strong> 按 <strong><code>emp_id</code> + <code>order_date</code></strong> 分区累计；行顺序与存储顺序一致时如下）：</p>
<table>
<thead>
<tr>
<th>emp_id</th>
<th>order_date</th>
<th>amount</th>
<th>running_sum</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>2024-06-01</td>
<td>100.00</td>
<td>100.00</td>
</tr>
<tr>
<td>1</td>
<td>2024-06-10</td>
<td>200.50</td>
<td>300.50</td>
</tr>
<tr>
<td>2</td>
<td>2024-06-12</td>
<td>300.00</td>
<td>300.00</td>
</tr>
<tr>
<td>3</td>
<td>2024-06-15</td>
<td>50.00</td>
<td>50.00</td>
</tr>
</tbody></table>
<h3 id="3-取每组-Top-N-的常见写法（例如每部门工资前两名）"><a href="#3-取每组-Top-N-的常见写法（例如每部门工资前两名）" class="headerlink" title="3. 取每组 Top N 的常见写法（例如每部门工资前两名）"></a>3. 取每组 Top N 的常见写法（例如每部门工资前两名）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> id, name, dept_id, salary,</span><br><span class="line">           <span class="built_in">ROW_NUMBER</span>() <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> dept_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> salary <span class="keyword">DESC</span>) <span class="keyword">AS</span> rn</span><br><span class="line">    <span class="keyword">FROM</span> emp</span><br><span class="line">    <span class="keyword">WHERE</span> dept_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line">) x</span><br><span class="line"><span class="keyword">WHERE</span> x.rn <span class="operator">&lt;=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（每部门 <strong><code>rn &lt;= 2</code></strong>，即薪资前两行；<strong><code>dept_id = 2</code></strong> 含 <strong><code>NULL</code></strong> 薪资）：</p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>dept_id</th>
<th>salary</th>
<th>rn</th>
</tr>
</thead>
<tbody><tr>
<td>2</td>
<td>李四</td>
<td>1</td>
<td>18000.00</td>
<td>1</td>
</tr>
<tr>
<td>1</td>
<td>张三</td>
<td>1</td>
<td>15000.00</td>
<td>2</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>2</td>
<td>12000.00</td>
<td>1</td>
</tr>
<tr>
<td>4</td>
<td>赵六</td>
<td>2</td>
<td>NULL</td>
<td>2</td>
</tr>
</tbody></table>
<hr>
<h2 id="六、分组与条件聚合-GROUP-BY-HAVING"><a href="#六、分组与条件聚合-GROUP-BY-HAVING" class="headerlink" title="六、分组与条件聚合 GROUP BY &#x2F; HAVING"></a>六、分组与条件聚合 GROUP BY &#x2F; HAVING</h2><p><strong><code>WHERE</code></strong> 在分组前过滤行；<strong><code>HAVING</code></strong> 在分组后过滤聚合结果。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> d.name <span class="keyword">AS</span> dept_name,</span><br><span class="line">       <span class="built_in">COUNT</span>(e.id) <span class="keyword">AS</span> emp_cnt,</span><br><span class="line">       <span class="built_in">MAX</span>(e.salary) <span class="keyword">AS</span> max_salary</span><br><span class="line"><span class="keyword">FROM</span> dept d</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> emp e <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> d.id, d.name</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">COUNT</span>(e.id) <span class="operator">&gt;</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>HAVING</code></strong> 去掉无员工的 <strong>财务</strong>）：</p>
<table>
<thead>
<tr>
<th>dept_name</th>
<th>emp_cnt</th>
<th>max_salary</th>
</tr>
</thead>
<tbody><tr>
<td>研发</td>
<td>2</td>
<td>18000.00</td>
</tr>
<tr>
<td>市场</td>
<td>2</td>
<td>12000.00</td>
</tr>
</tbody></table>
<p><strong><code>COUNT(*)</code></strong> 与 <strong><code>COUNT(列)</code></strong> 区别：对 <strong><code>salary</code></strong> 统计时 <strong><code>NULL</code></strong> 不计入 <strong><code>COUNT(salary)</code></strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> dept_id,</span><br><span class="line">       <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> rows_cnt,</span><br><span class="line">       <span class="built_in">COUNT</span>(salary) <span class="keyword">AS</span> non_null_salary_cnt</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> dept_id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>dept_id = 2</code></strong> 两行里 <strong><code>salary</code></strong> 仅一行非 <strong>NULL</strong>，故 <strong><code>COUNT(salary)=1</code></strong>）：</p>
<table>
<thead>
<tr>
<th>dept_id</th>
<th>rows_cnt</th>
<th>non_null_salary_cnt</th>
</tr>
</thead>
<tbody><tr>
<td>NULL</td>
<td>1</td>
<td>1</td>
</tr>
<tr>
<td>1</td>
<td>2</td>
<td>2</td>
</tr>
<tr>
<td>2</td>
<td>2</td>
<td>1</td>
</tr>
</tbody></table>
<hr>
<h2 id="七、CASE-表达式与条件计数"><a href="#七、CASE-表达式与条件计数" class="headerlink" title="七、CASE 表达式与条件计数"></a>七、CASE 表达式与条件计数</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, salary,</span><br><span class="line">       <span class="keyword">CASE</span></span><br><span class="line">           <span class="keyword">WHEN</span> salary <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">THEN</span> <span class="string">&#x27;未填&#x27;</span></span><br><span class="line">           <span class="keyword">WHEN</span> salary <span class="operator">&gt;=</span> <span class="number">15000</span> <span class="keyword">THEN</span> <span class="string">&#x27;高&#x27;</span></span><br><span class="line">           <span class="keyword">WHEN</span> salary <span class="operator">&gt;=</span> <span class="number">10000</span> <span class="keyword">THEN</span> <span class="string">&#x27;中&#x27;</span></span><br><span class="line">           <span class="keyword">ELSE</span> <span class="string">&#x27;低&#x27;</span></span><br><span class="line">       <span class="keyword">END</span> <span class="keyword">AS</span> salary_level</span><br><span class="line"><span class="keyword">FROM</span> emp;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：</p>
<table>
<thead>
<tr>
<th>name</th>
<th>salary</th>
<th>salary_level</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>15000.00</td>
<td>高</td>
</tr>
<tr>
<td>李四</td>
<td>18000.00</td>
<td>高</td>
</tr>
<tr>
<td>王五</td>
<td>12000.00</td>
<td>中</td>
</tr>
<tr>
<td>赵六</td>
<td>NULL</td>
<td>未填</td>
</tr>
<tr>
<td>钱七</td>
<td>9000.00</td>
<td>低</td>
</tr>
</tbody></table>
<p><strong>条件聚合</strong>（列转行统计）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> dept_id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> dept1_cnt,</span><br><span class="line">    <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> dept_id <span class="operator">=</span> <span class="number">2</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> dept2_cnt</span><br><span class="line"><span class="keyword">FROM</span> emp;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（单行汇总）：</p>
<table>
<thead>
<tr>
<th>dept1_cnt</th>
<th>dept2_cnt</th>
</tr>
</thead>
<tbody><tr>
<td>2</td>
<td>2</td>
</tr>
</tbody></table>
<hr>
<h2 id="八、分组字符串拼接-GROUP-CONCAT"><a href="#八、分组字符串拼接-GROUP-CONCAT" class="headerlink" title="八、分组字符串拼接 GROUP_CONCAT"></a>八、分组字符串拼接 GROUP_CONCAT</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> d.name <span class="keyword">AS</span> dept_name,</span><br><span class="line">       GROUP_CONCAT(e.name <span class="keyword">ORDER</span> <span class="keyword">BY</span> e.salary <span class="keyword">DESC</span> SEPARATOR <span class="string">&#x27; | &#x27;</span>) <span class="keyword">AS</span> members</span><br><span class="line"><span class="keyword">FROM</span> dept d</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> emp e <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> d.id, d.name;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>members</code></strong> 按薪资降序拼接；无员工部门 <strong><code>GROUP_CONCAT</code></strong> 常为 <strong>NULL</strong>；表中用 <strong>→</strong> 表示 <strong><code>SEPARATOR &#39; | &#39;</code></strong> 的实际竖线拼接顺序）：</p>
<table>
<thead>
<tr>
<th>dept_name</th>
<th>members</th>
</tr>
</thead>
<tbody><tr>
<td>研发</td>
<td>李四 → 张三</td>
</tr>
<tr>
<td>市场</td>
<td>王五 → 赵六</td>
</tr>
<tr>
<td>财务</td>
<td>NULL</td>
</tr>
</tbody></table>
<p><strong>注意</strong>：默认 <strong><code>group_concat_max_len</code></strong> 有限，大结果需调大会话或全局参数。</p>
<hr>
<h2 id="九、分页与稳定排序"><a href="#九、分页与稳定排序" class="headerlink" title="九、分页与稳定排序"></a>九、分页与稳定排序</h2><p><strong><code>LIMIT offset, row_count</code></strong> 在大 <strong>offset</strong> 时变慢，生产常配合「覆盖索引 + 上一页最大值」做<strong>键集分页</strong>；入门可先掌握 <strong><code>LIMIT</code></strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, name, hire_date</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> hire_date <span class="keyword">DESC</span>, id <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span> <span class="keyword">OFFSET</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>LIMIT 10</code></strong> 在本数据上等于取全表；<strong><code>hire_date</code></strong> 降序）：</p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>hire_date</th>
</tr>
</thead>
<tbody><tr>
<td>5</td>
<td>钱七</td>
<td>2023-11-01</td>
</tr>
<tr>
<td>4</td>
<td>赵六</td>
<td>2022-05-20</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>2021-01-10</td>
</tr>
<tr>
<td>1</td>
<td>张三</td>
<td>2020-03-01</td>
</tr>
<tr>
<td>2</td>
<td>李四</td>
<td>2019-07-15</td>
</tr>
</tbody></table>
<p><strong>MySQL 8.0+</strong> 可与窗口函数结合做「第 <strong>N</strong> 页」（下例取 <strong><code>rn</code></strong> 在 <strong>2～4</strong> 的行，避免演示数据过少时中间页为空）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> id, name, salary,</span><br><span class="line">           <span class="built_in">ROW_NUMBER</span>() <span class="keyword">OVER</span> (<span class="keyword">ORDER</span> <span class="keyword">BY</span> (salary <span class="keyword">IS</span> <span class="keyword">NULL</span>), salary <span class="keyword">DESC</span>, id) <span class="keyword">AS</span> rn</span><br><span class="line">    <span class="keyword">FROM</span> emp</span><br><span class="line">) t</span><br><span class="line"><span class="keyword">WHERE</span> t.rn <span class="keyword">BETWEEN</span> <span class="number">2</span> <span class="keyword">AND</span> <span class="number">4</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>rn=1</code></strong> 为 <strong>李四</strong>；本例为 <strong><code>rn</code> 2～4</strong>）：</p>
<table>
<thead>
<tr>
<th>id</th>
<th>name</th>
<th>salary</th>
<th>rn</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>张三</td>
<td>15000.00</td>
<td>2</td>
</tr>
<tr>
<td>3</td>
<td>王五</td>
<td>12000.00</td>
<td>3</td>
</tr>
<tr>
<td>5</td>
<td>钱七</td>
<td>9000.00</td>
<td>4</td>
</tr>
</tbody></table>
<p><strong><code>ORDER BY (salary IS NULL), salary DESC</code></strong>：先排「非 <strong>NULL</strong> 薪资」在前，<strong><code>NULL</code></strong> 在后，兼容各 <strong>8.0</strong> 小版本。</p>
<hr>
<h2 id="十、常用补充：DISTINCT、COALESCE、正则"><a href="#十、常用补充：DISTINCT、COALESCE、正则" class="headerlink" title="十、常用补充：DISTINCT、COALESCE、正则"></a>十、常用补充：DISTINCT、COALESCE、正则</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 去重部门编号</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> dept_id <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- NULL 与默认值</span></span><br><span class="line"><span class="keyword">SELECT</span> name, <span class="built_in">COALESCE</span>(salary, <span class="number">0</span>) <span class="keyword">AS</span> salary_or_zero <span class="keyword">FROM</span> emp;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 正则筛选（简单示例）</span></span><br><span class="line"><span class="keyword">SELECT</span> name <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name REGEXP <span class="string">&#x27;^[张李王].*&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong></p>
<p><strong><code>DISTINCT dept_id</code></strong></p>
<table>
<thead>
<tr>
<th>dept_id</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
</tr>
<tr>
<td>2</td>
</tr>
</tbody></table>
<p><strong><code>COALESCE(salary, 0)</code></strong></p>
<table>
<thead>
<tr>
<th>name</th>
<th>salary_or_zero</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>15000.00</td>
</tr>
<tr>
<td>李四</td>
<td>18000.00</td>
</tr>
<tr>
<td>王五</td>
<td>12000.00</td>
</tr>
<tr>
<td>赵六</td>
<td>0</td>
</tr>
<tr>
<td>钱七</td>
<td>9000.00</td>
</tr>
</tbody></table>
<p><strong><code>REGEXP &#39;^[张李王].*&#39;</code></strong>（姓为 <strong>张 &#x2F; 李 &#x2F; 王</strong> 之一开头）</p>
<table>
<thead>
<tr>
<th>name</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
</tr>
<tr>
<td>李四</td>
</tr>
<tr>
<td>王五</td>
</tr>
</tbody></table>
<hr>
<h2 id="十一、速查小结"><a href="#十一、速查小结" class="headerlink" title="十一、速查小结"></a>十一、速查小结</h2><table>
<thead>
<tr>
<th>场景</th>
<th>典型写法</th>
</tr>
</thead>
<tbody><tr>
<td>多表关联</td>
<td><strong><code>INNER JOIN</code> &#x2F; <code>LEFT JOIN</code></strong> + <strong><code>ON</code></strong></td>
</tr>
<tr>
<td>是否存在</td>
<td><strong><code>EXISTS (SELECT 1 …)</code></strong></td>
</tr>
<tr>
<td>合并结果</td>
<td><strong><code>UNION ALL</code></strong>（优先）&#x2F; <strong><code>UNION</code></strong></td>
</tr>
<tr>
<td>可读拆分（8.0+）</td>
<td><strong><code>WITH cte AS (...)</code></strong></td>
</tr>
<tr>
<td>组内排名、累计（8.0+）</td>
<td><strong><code>ROW_NUMBER()</code>、<code>SUM() OVER (...)</code></strong></td>
</tr>
<tr>
<td>聚合后过滤</td>
<td><strong><code>GROUP BY</code> + <code>HAVING</code></strong></td>
</tr>
<tr>
<td>分支与透视列</td>
<td><strong><code>CASE WHEN</code></strong></td>
</tr>
<tr>
<td>组内拼字符串</td>
<td><strong><code>GROUP_CONCAT</code></strong></td>
</tr>
<tr>
<td>分页</td>
<td><strong><code>ORDER BY</code> + <code>LIMIT</code></strong>；大表慎用深 <strong><code>OFFSET</code></strong></td>
</tr>
</tbody></table>
<p>练习时可在测试库执行<strong>文首「示例数据」建表脚本</strong>，再逐段复制运行，对照各小节 <strong>「示例结果」</strong> 表格验算。</p>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>SQL</tag>
        <tag>高级查询</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty</title>
    <url>/2026/05/01/Netty/Netty/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Netty</category>
      </categories>
      <tags>
        <tag>Netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty 多路复用</title>
    <url>/2026/05/01/Netty/Netty%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8/</url>
    <content><![CDATA[<p><strong>I&#x2F;O 多路复用</strong>指用<strong>少量线程</strong>通过 <strong><code>Selector</code>（Linux 上常为 Epoll）</strong> 同时<strong>监视大量 <code>Channel</code></strong>，只在<strong>有就绪事件</strong>时读写，而不是「一线程一连接」。<strong>Netty</strong> 把 <strong>Java NIO</strong> 封装成 <strong><code>EventLoop</code> + <code>Pipeline</code></strong> 的 <strong>Reactor</strong> 风格，便于高并发网络程序。下面先给 <strong>Maven &#x2F; Gradle</strong> 依赖，再讲概念、<strong>Boss &#x2F; Worker</strong> 线程模型、<strong>Echo</strong> 示例与系统侧注意点。</p>
<hr>
<h1 id="一、先把三个概念说清楚"><a href="#一、先把三个概念说清楚" class="headerlink" title="一、先把三个概念说清楚"></a>一、先把三个概念说清楚</h1><h2 id="1-「很多连接」≠「每条连接占满一条线程」"><a href="#1-「很多连接」≠「每条连接占满一条线程」" class="headerlink" title="1. 「很多连接」≠「每条连接占满一条线程」"></a>1. 「很多连接」≠「每条连接占满一条线程」</h2><ul>
<li><strong>大量长连接</strong>：例如推送、网关、<strong>IM</strong> 在线，许多连接<strong>长时间无读写</strong>，适合交给<strong>一个 <code>Selector</code></strong> 统一等就绪。  </li>
<li><strong>高 QPS 短连接</strong>：瓶颈常在 <strong>CPU</strong>、<strong>GC</strong>、<strong>协议栈</strong>，与「单 <code>Selector</code> 挂很多空闲连接」不是同一维度的问题。</li>
</ul>
<h2 id="2-NIO-多路复用在干什么"><a href="#2-NIO-多路复用在干什么" class="headerlink" title="2. NIO 多路复用在干什么"></a>2. <strong>NIO</strong> 多路复用在干什么</h2><p>一条线程调用 <strong><code>Selector.select()</code></strong> 阻塞等待，直到<strong>一批</strong> <strong>Channel</strong> 上<strong>有就绪的 I&#x2F;O 事件</strong>（可读、可写等），再<strong>依次分发</strong>处理。这样<strong>一条线程可以注册并监视成千上万个连接</strong>，而不是为每个连接各开一条阻塞线程。</p>
<h2 id="3-Netty-扮演的角色"><a href="#3-Netty-扮演的角色" class="headerlink" title="3. Netty 扮演的角色"></a>3. <strong>Netty</strong> 扮演的角色</h2><p><strong>Netty</strong> 在 <strong>Java NIO &#x2F; Epoll（Linux）</strong> 之上封装了 <strong>Reactor</strong> 风格的 <strong><code>EventLoop</code></strong>、<strong><code>Pipeline</code></strong>、内存与缓冲策略，让你用<strong>少量 <code>EventLoop</code> 线程</strong>管理<strong>大量 <code>Channel</code></strong>，并把编解码、业务入 <strong><code>ChannelPipeline</code></strong>。</p>
<hr>
<h1 id="二、依赖示例（Maven-Gradle）"><a href="#二、依赖示例（Maven-Gradle）" class="headerlink" title="二、依赖示例（Maven &#x2F; Gradle）"></a>二、依赖示例（Maven &#x2F; Gradle）</h1><p><strong><code>&lt;version&gt;</code> &#x2F; 版本号</strong> 请换成当前 <strong>4.1.x</strong> 稳定版，与团队对齐。</p>
<h2 id="1-Maven（pom-xml）"><a href="#1-Maven（pom-xml）" class="headerlink" title="1. Maven（pom.xml）"></a>1. Maven（<code>pom.xml</code>）</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.netty<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>netty-all<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.1.115.Final<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="2-Gradle（Groovy，build-gradle）"><a href="#2-Gradle（Groovy，build-gradle）" class="headerlink" title="2. Gradle（Groovy，build.gradle）"></a>2. Gradle（Groovy，<code>build.gradle</code>）</h2><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">dependencies</span> &#123;</span><br><span class="line">    implementation <span class="string">&#x27;io.netty:netty-all:4.1.115.Final&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-Gradle（Kotlin-DSL，build-gradle-kts）"><a href="#3-Gradle（Kotlin-DSL，build-gradle-kts）" class="headerlink" title="3. Gradle（Kotlin DSL，build.gradle.kts）"></a>3. Gradle（Kotlin DSL，<code>build.gradle.kts</code>）</h2><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;io.netty:netty-all:4.1.115.Final&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>官方仓库与版本列表：<strong><a href="https://netty.io/">https://netty.io/</a></strong> · <strong><a href="https://github.com/netty/netty">https://github.com/netty/netty</a></strong></p>
<hr>
<h1 id="三、Netty-常见线程模型（Boss-Worker）"><a href="#三、Netty-常见线程模型（Boss-Worker）" class="headerlink" title="三、Netty 常见线程模型（Boss &#x2F; Worker）"></a>三、Netty 常见线程模型（Boss &#x2F; Worker）</h1><h2 id="1-典型服务端写法（概念）"><a href="#1-典型服务端写法（概念）" class="headerlink" title="1. 典型服务端写法（概念）"></a>1. 典型服务端写法（概念）</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">EventLoopGroup</span> <span class="variable">bossGroup</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NioEventLoopGroup</span>(<span class="number">1</span>);   <span class="comment">// 常 1：只负责接入</span></span><br><span class="line"><span class="type">EventLoopGroup</span> <span class="variable">workerGroup</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NioEventLoopGroup</span>(); <span class="comment">// 默认：CPU×2 等，负责已连接通道的 I/O</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>bossGroup</code></strong>：线程数常为 <strong>1</strong>（也可更多），负责<strong>接受新连接</strong>并把 <strong><code>SocketChannel</code></strong> 注册进 <strong><code>workerGroup</code></strong> 中某个 <strong><code>EventLoop</code></strong> 的 <strong><code>Selector</code></strong>。  </li>
<li><strong><code>workerGroup</code></strong>：每个 <strong><code>NioEventLoop</code></strong> 内<strong>一条线程 + 一个 <code>Selector</code></strong>，<strong>多路复用</strong>其上的所有 <strong><code>Channel</code></strong> 的读&#x2F;写就绪事件。整体是<strong>少量线程 + 大量连接</strong>，业务逻辑仍在 <strong><code>Pipeline</code></strong> 里按事件触发执行。</li>
</ul>
<h2 id="2-一张逻辑关系（文字版）"><a href="#2-一张逻辑关系（文字版）" class="headerlink" title="2. 一张逻辑关系（文字版）"></a>2. 一张逻辑关系（文字版）</h2><table>
<thead>
<tr>
<th>角色</th>
<th>大致职责</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Boss <code>EventLoop</code></strong></td>
<td><strong><code>accept</code></strong>，建 <strong><code>Channel</code></strong>，注册到某个 <strong>Worker</strong></td>
</tr>
<tr>
<td><strong>Worker <code>EventLoop</code></strong></td>
<td>已连接 <strong><code>Channel</code></strong> 的读&#x2F;写就绪事件、入站出站 <strong>Pipeline</strong></td>
</tr>
</tbody></table>
<p>业务若<strong>阻塞</strong>（重度 <strong>CPU</strong>、同步 <strong>JDBC</strong>、大 <strong><code>Thread.sleep</code></strong>），会占满该 <strong><code>EventLoop</code></strong> 上的所有 <strong><code>Channel</code></strong>，拖慢整组连接——因此<strong>阻塞操作</strong>应丢到<strong>业务线程池</strong>或异步组件，这是多路复用能发挥作用的<strong>前提</strong>。</p>
<hr>
<h1 id="四、示例代码：最小-Echo-服务端（可运行）"><a href="#四、示例代码：最小-Echo-服务端（可运行）" class="headerlink" title="四、示例代码：最小 Echo 服务端（可运行）"></a>四、示例代码：最小 Echo 服务端（可运行）</h1><p>下面是一个<strong>单文件可跑</strong>的 <strong>Echo</strong>：客户端发来多少字节，服务端原样写回（<strong>Boss&#x3D;1</strong>、<strong>Worker</strong> 默认线程数由 <strong>Netty</strong> 按 <strong>CPU</strong> 推算）。包名可按你的工程修改；需已引入 <strong><code>netty-all</code></strong>（<strong>Maven</strong> 见 <strong><code>pom.xml</code></strong>，<strong>Gradle</strong> 见 <strong><code>build.gradle</code> &#x2F; <code>build.gradle.kts</code></strong>，均见上文 <strong>二、依赖示例</strong>）。</p>
<h2 id="1-EchoServer-java"><a href="#1-EchoServer-java" class="headerlink" title="1. EchoServer.java"></a>1. <code>EchoServer.java</code></h2><p>将类放到任意 <strong><code>src/main/java</code></strong> 包路径下（示例包名 <strong><code>example.netty</code></strong>），与 <strong><code>pom.xml</code></strong> 中 <strong><code>mainClass</code></strong> 或 <strong>IDE</strong> 运行入口一致即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> example.netty;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> io.netty.bootstrap.ServerBootstrap;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.*;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.nio.NioEventLoopGroup;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.socket.SocketChannel;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.socket.nio.NioServerSocketChannel;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 最小 Echo：读入的字节原样写回。默认端口 8000，也可传入参数：8080。</span></span><br><span class="line"><span class="comment"> * 验证：nc 127.0.0.1 8000</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">EchoServer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">port</span> <span class="operator">=</span> (args.length &gt; <span class="number">0</span>) ? Integer.parseInt(args[<span class="number">0</span>]) : <span class="number">8000</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">EventLoopGroup</span> <span class="variable">bossGroup</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NioEventLoopGroup</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="type">EventLoopGroup</span> <span class="variable">workerGroup</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NioEventLoopGroup</span>();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">ServerBootstrap</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServerBootstrap</span>();</span><br><span class="line">            b.group(bossGroup, workerGroup)</span><br><span class="line">                    .channel(NioServerSocketChannel.class)</span><br><span class="line">                    .childHandler(<span class="keyword">new</span> <span class="title class_">ChannelInitializer</span>&lt;SocketChannel&gt;() &#123;</span><br><span class="line">                        <span class="meta">@Override</span></span><br><span class="line">                        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initChannel</span><span class="params">(SocketChannel ch)</span> &#123;</span><br><span class="line">                            ch.pipeline().addLast(<span class="keyword">new</span> <span class="title class_">ChannelInboundHandlerAdapter</span>() &#123;</span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> &#123;</span><br><span class="line">                                    ctx.write(msg);</span><br><span class="line">                                &#125;</span><br><span class="line"></span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">channelReadComplete</span><span class="params">(ChannelHandlerContext ctx)</span> &#123;</span><br><span class="line">                                    ctx.flush();</span><br><span class="line">                                &#125;</span><br><span class="line"></span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">exceptionCaught</span><span class="params">(ChannelHandlerContext ctx, Throwable cause)</span> &#123;</span><br><span class="line">                                    cause.printStackTrace();</span><br><span class="line">                                    ctx.close();</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;);</span><br><span class="line"></span><br><span class="line">            <span class="type">ChannelFuture</span> <span class="variable">f</span> <span class="operator">=</span> b.bind(port).sync();</span><br><span class="line">            System.out.println(<span class="string">&quot;Echo 监听端口 &quot;</span> + port + <span class="string">&quot;，终端执行: nc 127.0.0.1 &quot;</span> + port);</span><br><span class="line">            f.channel().closeFuture().sync();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            bossGroup.shutdownGracefully();</span><br><span class="line">            workerGroup.shutdownGracefully();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-用-nc-或-Netty-客户端自测"><a href="#2-用-nc-或-Netty-客户端自测" class="headerlink" title="2. 用 nc 或 Netty 客户端自测"></a>2. 用 <code>nc</code> 或 <strong>Netty</strong> 客户端自测</h2><p>另开终端：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nc 127.0.0.1 8000</span><br></pre></td></tr></table></figure>

<p>随意输入几行文字；服务端把同一字节流写回，<strong><code>nc</code></strong> 侧应看到<strong>回显</strong>。按 <strong><code>Ctrl+C</code></strong> 结束 <strong><code>nc</code></strong>；停止服务端在运行 <strong>Echo</strong> 的终端 <strong><code>Ctrl+C</code></strong> 或结束进程。</p>
<h2 id="3-可选：EchoClient-java（本机连本机）"><a href="#3-可选：EchoClient-java（本机连本机）" class="headerlink" title="3. 可选：EchoClient.java（本机连本机）"></a>3. 可选：<code>EchoClient.java</code>（本机连本机）</h2><p>与上节同一 <strong><code>package</code></strong> 下再加一类，先启动 <strong><code>EchoServer</code></strong>，再运行 <strong><code>EchoClient</code></strong>；客户端连上后发送一行 <strong><code>hello</code></strong>，控制台打印读到的字节数（即回显长度）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> example.netty;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> io.netty.bootstrap.Bootstrap;</span><br><span class="line"><span class="keyword">import</span> io.netty.buffer.ByteBuf;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.*;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.nio.NioEventLoopGroup;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.socket.SocketChannel;</span><br><span class="line"><span class="keyword">import</span> io.netty.channel.socket.nio.NioSocketChannel;</span><br><span class="line"><span class="keyword">import</span> io.netty.util.CharsetUtil;</span><br><span class="line"><span class="keyword">import</span> io.netty.util.ReferenceCountUtil;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">EchoClient</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">host</span> <span class="operator">=</span> (args.length &gt; <span class="number">0</span>) ? args[<span class="number">0</span>] : <span class="string">&quot;127.0.0.1&quot;</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">port</span> <span class="operator">=</span> (args.length &gt; <span class="number">1</span>) ? Integer.parseInt(args[<span class="number">1</span>]) : <span class="number">8000</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">EventLoopGroup</span> <span class="variable">group</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NioEventLoopGroup</span>();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">Bootstrap</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Bootstrap</span>();</span><br><span class="line">            b.group(group)</span><br><span class="line">                    .channel(NioSocketChannel.class)</span><br><span class="line">                    .handler(<span class="keyword">new</span> <span class="title class_">ChannelInitializer</span>&lt;SocketChannel&gt;() &#123;</span><br><span class="line">                        <span class="meta">@Override</span></span><br><span class="line">                        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initChannel</span><span class="params">(SocketChannel ch)</span> &#123;</span><br><span class="line">                            ch.pipeline().addLast(<span class="keyword">new</span> <span class="title class_">ChannelInboundHandlerAdapter</span>() &#123;</span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">channelActive</span><span class="params">(ChannelHandlerContext ctx)</span> &#123;</span><br><span class="line">                                    <span class="type">ByteBuf</span> <span class="variable">buf</span> <span class="operator">=</span> ctx.alloc().buffer();</span><br><span class="line">                                    buf.writeBytes(<span class="string">&quot;hello\n&quot;</span>.getBytes(CharsetUtil.UTF_8));</span><br><span class="line">                                    ctx.writeAndFlush(buf);</span><br><span class="line">                                &#125;</span><br><span class="line"></span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> &#123;</span><br><span class="line">                                    <span class="type">ByteBuf</span> <span class="variable">in</span> <span class="operator">=</span> (ByteBuf) msg;</span><br><span class="line">                                    <span class="keyword">try</span> &#123;</span><br><span class="line">                                        System.out.println(<span class="string">&quot;收到回显，可读字节: &quot;</span> + in.readableBytes());</span><br><span class="line">                                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                                        ReferenceCountUtil.release(msg);</span><br><span class="line">                                    &#125;</span><br><span class="line">                                &#125;</span><br><span class="line"></span><br><span class="line">                                <span class="meta">@Override</span></span><br><span class="line">                                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">exceptionCaught</span><span class="params">(ChannelHandlerContext ctx, Throwable cause)</span> &#123;</span><br><span class="line">                                    cause.printStackTrace();</span><br><span class="line">                                    ctx.close();</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;);</span><br><span class="line"></span><br><span class="line">            <span class="type">ChannelFuture</span> <span class="variable">f</span> <span class="operator">=</span> b.connect(host, port).sync();</span><br><span class="line">            f.channel().closeFuture().sync();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            group.shutdownGracefully();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：真实协议一般要在 <strong><code>Pipeline</code></strong> 里加<strong>粘包&#x2F;拆包</strong>（如 <strong><code>LineBasedFrameDecoder</code></strong>、长度域），这里刻意最简，只对应「线程模型 + 读写回显」演示。</p>
<hr>
<h1 id="五、大量连接下的系统侧（简述）"><a href="#五、大量连接下的系统侧（简述）" class="headerlink" title="五、大量连接下的系统侧（简述）"></a>五、大量连接下的系统侧（简述）</h1><p>要把<strong>多路复用</strong>撑到<strong>很大连接数</strong>，通常还要：</p>
<ol>
<li><strong>进程最大打开文件数</strong>（<strong><code>ulimit -n</code></strong>）调到足够大。  </li>
<li><strong>Linux</strong> 上 <strong>TCP</strong>、<strong>端口范围</strong>、<strong><code>somaxconn</code></strong> 等与接入能力相关的内核参数按运维规范调整。  </li>
<li><strong>堆外内存</strong>、<strong>直接内存</strong>、<strong><code>-Xmx</code></strong> 与 <strong>Netty</strong> 的 <strong>ByteBuf</strong> 分配策略匹配业务量，避免 <strong>OOM</strong>。</li>
</ol>
<p>这些属于<strong>容量规划</strong>与<strong>运维</strong>，单靠应用层多路复用无法替代。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>说法</th>
<th>更稳妥的理解</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Maven &#x2F; Gradle</strong></td>
<td><strong>二</strong>：坐标 <strong><code>io.netty:netty-all</code></strong>；<strong>Maven</strong> 用 <strong><code>&lt;dependency&gt;</code></strong>；<strong>Gradle</strong> 用 <strong><code>implementation</code></strong>（<strong>Groovy</strong> 或 <strong>Kotlin DSL</strong>）。版本与团队对齐。</td>
</tr>
<tr>
<td><strong>I&#x2F;O 多路复用</strong></td>
<td><strong><code>Selector</code></strong> 一批 <strong><code>Channel</code></strong> 上就绪事件再处理；<strong>Netty</strong> 用 <strong><code>EventLoop</code></strong> 封装这一模型。</td>
</tr>
<tr>
<td><strong>Netty</strong> 与线程</td>
<td><strong>Boss</strong> 接入、<strong>Worker</strong> 做已连通道的 <strong>I&#x2F;O</strong> 与 <strong><code>Pipeline</code></strong>；<strong>少量线程</strong>对应<strong>大量连接</strong>（连接多空闲、业务非阻塞时更匹配）。</td>
</tr>
<tr>
<td>示例代码</td>
<td><strong>四</strong>：<strong><code>EchoServer</code></strong> + <strong><code>nc</code></strong>；可选 <strong><code>EchoClient</code></strong>。依赖声明见 <strong>二</strong>。</td>
</tr>
<tr>
<td>要上量</td>
<td><strong>连接模型</strong>、<strong>业务是否阻塞</strong>、<strong>FD &#x2F; 内核 &#x2F; 内存</strong> 一起评估。</td>
</tr>
</tbody></table>
<p>进一步阅读：<strong>《Netty 实战》</strong>、官方 <strong>User guide</strong>；进阶可看 <strong>Epoll</strong>、<strong>零拷贝（<code>FileRegion</code> &#x2F; <code>sendfile</code>）</strong>、<strong>内存池</strong>与 <strong><code>PooledByteBufAllocator</code></strong>。</p>
]]></content>
      <categories>
        <category>Netty</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx</title>
    <url>/2026/05/01/Nginx/Nginx/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Nginx</category>
      </categories>
      <tags>
        <tag>Nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>Node的nvm工具</title>
    <url>/2026/04/30/Node/Node-nvm/</url>
    <content><![CDATA[<p><strong>nvm</strong>（Node Version Manager）是用于在<strong>同一台机器上安装、切换多份 Node.js</strong> 的 Shell 脚本工具，适合不同项目依赖不同 Node 版本时使用。下面按安装 nvm、安装并固定 <strong>Node v22.22.2</strong>、项目级版本、常用命令说明；<strong>macOS &#x2F; Linux</strong> 以官方 <a href="https://github.com/nvm-sh/nvm">nvm-sh&#x2F;nvm</a> 为准，<strong>Windows</strong> 见文末补充。</p>
<hr>
<h1 id="一、nvm-是什么"><a href="#一、nvm-是什么" class="headerlink" title="一、nvm 是什么"></a>一、nvm 是什么</h1><ul>
<li><strong>作用</strong>：从源码或官方预编译包拉取 Node，把各版本隔离在用户目录下，通过 <code>nvm use</code> 切换当前终端使用的 <code>node</code> &#x2F; <code>npm</code>。</li>
<li><strong>与全局安装的区别</strong>：不会把某个 Node 强行装进系统目录；切换版本只影响当前 Shell（及子进程）。</li>
<li><strong>本文示例版本</strong>：统一以 <strong>Node v22.22.2</strong>（Current&#x2F;LTS 以你安装时官方为准）演示命令。</li>
</ul>
<hr>
<h1 id="二、安装-nvm（macOS-Linux）"><a href="#二、安装-nvm（macOS-Linux）" class="headerlink" title="二、安装 nvm（macOS &#x2F; Linux）"></a>二、安装 nvm（macOS &#x2F; Linux）</h1><h2 id="1-使用官方安装脚本"><a href="#1-使用官方安装脚本" class="headerlink" title="1. 使用官方安装脚本"></a>1. 使用官方安装脚本</h2><p>在终端执行（脚本版本号可到 <a href="https://github.com/nvm-sh/nvm/releases">nvm Releases</a> 核对后替换 URL 中的版本）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.4/install.sh | bash</span><br></pre></td></tr></table></figure>

<p>若无 <code>curl</code>，可用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.4/install.sh | bash</span><br></pre></td></tr></table></figure>

<h2 id="2-使-nvm-在当前终端生效"><a href="#2-使-nvm-在当前终端生效" class="headerlink" title="2. 使 nvm 在当前终端生效"></a>2. 使 nvm 在当前终端生效</h2><p>安装脚本会把 nvm 装到 <strong><code>$HOME/.nvm</code></strong>。新开终端一般会自动加载；当前已打开的终端可手动执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> NVM_DIR=<span class="string">&quot;<span class="variable">$HOME</span>/.nvm&quot;</span></span><br><span class="line">[ -s <span class="string">&quot;<span class="variable">$NVM_DIR</span>/nvm.sh&quot;</span> ] &amp;&amp; \. <span class="string">&quot;<span class="variable">$NVM_DIR</span>/nvm.sh&quot;</span></span><br><span class="line">[ -s <span class="string">&quot;<span class="variable">$NVM_DIR</span>/bash_completion&quot;</span> ] &amp;&amp; \. <span class="string">&quot;<span class="variable">$NVM_DIR</span>/bash_completion&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-确认安装成功"><a href="#3-确认安装成功" class="headerlink" title="3. 确认安装成功"></a>3. 确认安装成功</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">command</span> -v nvm &amp;&amp; nvm --version</span><br></pre></td></tr></table></figure>

<p>能输出版本号即表示 nvm 已可用。</p>
<hr>
<h1 id="三、安装并使用-Node-js-v22-22-2"><a href="#三、安装并使用-Node-js-v22-22-2" class="headerlink" title="三、安装并使用 Node.js v22.22.2"></a>三、安装并使用 Node.js v22.22.2</h1><h2 id="1-安装指定版本"><a href="#1-安装指定版本" class="headerlink" title="1. 安装指定版本"></a>1. 安装指定版本</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm install v22.22.2</span><br></pre></td></tr></table></figure>

<p><code>nvm</code> 会下载并编译或解压对应 Node；国内若下载慢，可结合代理或镜像（见 nvm 文档中的 <code>NVM_NODEJS_ORG_MIRROR</code> 等环境变量）。</p>
<h2 id="2-在当前终端切换到-v22-22-2"><a href="#2-在当前终端切换到-v22-22-2" class="headerlink" title="2. 在当前终端切换到 v22.22.2"></a>2. 在当前终端切换到 v22.22.2</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm use v22.22.2</span><br></pre></td></tr></table></figure>

<h2 id="3-设为默认版本（新开终端自动使用）"><a href="#3-设为默认版本（新开终端自动使用）" class="headerlink" title="3. 设为默认版本（新开终端自动使用）"></a>3. 设为默认版本（新开终端自动使用）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm <span class="built_in">alias</span> default v22.22.2</span><br></pre></td></tr></table></figure>

<p>之后重新打开终端时，默认即为 <strong>v22.22.2</strong>（除非你又在配置里改了 <code>nvm use</code>）。</p>
<h2 id="4-为单个项目固定-Node-版本（-nvmrc）"><a href="#4-为单个项目固定-Node-版本（-nvmrc）" class="headerlink" title="4. 为单个项目固定 Node 版本（.nvmrc）"></a>4. 为单个项目固定 Node 版本（<code>.nvmrc</code>）</h2><p>在项目根目录创建 <strong><code>.nvmrc</code></strong>，只写一行版本号：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/your/project</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;v22.22.2&quot;</span> &gt; .nvmrc</span><br><span class="line">nvm use</span><br></pre></td></tr></table></figure>

<p>进入该目录后执行 <code>nvm use</code>（无参数），nvm 会读取 <code>.nvmrc</code> 并切换到 <strong>v22.22.2</strong>。可与团队约定：克隆仓库后在该目录执行一次 <code>nvm use</code>。</p>
<h2 id="5-验证-node-与-npm"><a href="#5-验证-node-与-npm" class="headerlink" title="5. 验证 node 与 npm"></a>5. 验证 node 与 npm</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v   <span class="comment"># 应输出 v22.22.2</span></span><br><span class="line">npm -v</span><br><span class="line"><span class="built_in">which</span> node</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、常用命令速查"><a href="#四、常用命令速查" class="headerlink" title="四、常用命令速查"></a>四、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>示例命令</th>
</tr>
</thead>
<tbody><tr>
<td>查看已安装版本</td>
<td><code>nvm list</code></td>
</tr>
<tr>
<td>查看远程可装版本</td>
<td><code>nvm ls-remote</code>，或 <code>nvm ls-remote 22</code> 缩小列表</td>
</tr>
<tr>
<td>安装某版本</td>
<td><code>nvm install v22.22.2</code></td>
</tr>
<tr>
<td>当前 Shell 使用某版本</td>
<td><code>nvm use v22.22.2</code></td>
</tr>
<tr>
<td>默认版本</td>
<td><code>nvm alias default v22.22.2</code></td>
</tr>
<tr>
<td>按项目 <code>.nvmrc</code> 切换</td>
<td>在含 <code>.nvmrc</code> 的目录执行 <code>nvm use</code></td>
</tr>
<tr>
<td>卸载某版本</td>
<td><code>nvm uninstall v22.22.2</code></td>
</tr>
</tbody></table>
<p>更完整的子命令见：<code>nvm --help</code> 或 <a href="https://github.com/nvm-sh/nvm/blob/master/README.md">nvm 仓库 README</a>。</p>
<hr>
<h1 id="五、Windows-与补充说明"><a href="#五、Windows-与补充说明" class="headerlink" title="五、Windows 与补充说明"></a>五、Windows 与补充说明</h1><ul>
<li><strong>经典 nvm（nvm-sh）不支持原生 Windows</strong>。若在 Windows 上开发，常见做法有：  <ol>
<li>使用 <strong><a href="https://github.com/coreybutler/nvm-windows">nvm-windows</a></strong>（命令与 nvm-sh 接近但不完全相同，安装包在 Releases 下载）；  </li>
<li>使用 <strong>WSL</strong>，在 Linux 子系统里按本文 <strong>二、三</strong> 安装 nvm-sh；  </li>
<li>使用 <strong>fnm</strong>、<strong>Volta</strong> 等跨平台工具（非 nvm，但可管理多版本 Node）。</li>
</ol>
</li>
<li><strong>国内镜像</strong>：可在安装 Node 前设置 <code>NVM_NODEJS_ORG_MIRROR</code>（具体变量名与地址以当前 nvm 文档为准），以加速 <code>nvm install</code>。</li>
<li><strong>与系统自带 node 冲突</strong>：若曾用包管理器安装过 <code>node</code>，<code>which node</code> 可能仍指向旧路径；确保 <code>nvm use</code> 后 <code>PATH</code> 中 nvm 的 shim 在前，或卸载系统级 Node 后再试。</li>
</ul>
<p>官方文档：<a href="https://github.com/nvm-sh/nvm">nvm-sh&#x2F;nvm</a>。</p>
]]></content>
      <categories>
        <category>Node</category>
      </categories>
      <tags>
        <tag>nvm</tag>
        <tag>Node</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript</title>
    <url>/2026/05/01/Node/TypeScript/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Node</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV</title>
    <url>/2026/05/01/OpenCV/OpenCV/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenSSL</title>
    <url>/2026/05/01/OpenSSL/OpenSSL/</url>
    <content><![CDATA[<h1 id="OpenSSL-证书与密钥-完整指南"><a href="#OpenSSL-证书与密钥-完整指南" class="headerlink" title="OpenSSL 证书与密钥 完整指南"></a>OpenSSL 证书与密钥 完整指南</h1><h2 id="OpenSSL-是生成、管理非对称密钥和数字证书的标准工具，广泛用于-HTTPS、SSL-TLS、身份认证、加密通信等场景。"><a href="#OpenSSL-是生成、管理非对称密钥和数字证书的标准工具，广泛用于-HTTPS、SSL-TLS、身份认证、加密通信等场景。" class="headerlink" title="OpenSSL 是生成、管理非对称密钥和数字证书的标准工具，广泛用于 HTTPS、SSL&#x2F;TLS、身份认证、加密通信等场景。"></a>OpenSSL 是生成、管理<strong>非对称密钥</strong>和<strong>数字证书</strong>的标准工具，广泛用于 HTTPS、SSL&#x2F;TLS、身份认证、加密通信等场景。</h2><h2 id="一、核心概念（快速看懂）"><a href="#一、核心概念（快速看懂）" class="headerlink" title="一、核心概念（快速看懂）"></a>一、核心概念（快速看懂）</h2><ol>
<li><strong>私钥（private key）</strong>：.key &#x2F; .pem，必须保密，用于解密、签名</li>
<li><strong>公钥（public key）</strong>：从私钥导出，公开分发，用于加密、验签</li>
<li><strong>证书（certificate）</strong>：.crt &#x2F; .pem，包含公钥 + 身份信息 + 签名，证明公钥合法</li>
<li><strong>CSR（证书签名请求）</strong>：.csr，向 CA 机构申请正式证书的文件</li>
</ol>
<hr>
<h2 id="二、最常用命令（直接用）"><a href="#二、最常用命令（直接用）" class="headerlink" title="二、最常用命令（直接用）"></a>二、最常用命令（直接用）</h2><h3 id="1-生成-RSA-私钥（无密码）"><a href="#1-生成-RSA-私钥（无密码）" class="headerlink" title="1. 生成 RSA 私钥（无密码）"></a>1. 生成 RSA 私钥（无密码）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out private.key 2048</span><br></pre></td></tr></table></figure>
<ul>
<li>2048 是密钥长度（安全推荐）</li>
<li>生成后<strong>千万不要泄露</strong></li>
</ul>
<h3 id="2-生成带密码保护的私钥（更安全）"><a href="#2-生成带密码保护的私钥（更安全）" class="headerlink" title="2. 生成带密码保护的私钥（更安全）"></a>2. 生成带密码保护的私钥（更安全）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -aes256 -out private.key 2048</span><br></pre></td></tr></table></figure>
<p>每次使用会要求输入密码。</p>
<h3 id="3-从私钥导出公钥"><a href="#3-从私钥导出公钥" class="headerlink" title="3. 从私钥导出公钥"></a>3. 从私钥导出公钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl rsa -<span class="keyword">in</span> private.key -pubout -out public.key</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="三、生成自签名证书（开发-测试必备）"><a href="#三、生成自签名证书（开发-测试必备）" class="headerlink" title="三、生成自签名证书（开发&#x2F;测试必备）"></a>三、生成自签名证书（开发&#x2F;测试必备）</h2><p><strong>自签名证书 &#x3D; 私钥 + 自己签发的证书</strong><br>适合本地开发、内网测试，不能用于公网生产环境。</p>
<h3 id="一步生成-私钥-证书（有效期-10-年）"><a href="#一步生成-私钥-证书（有效期-10-年）" class="headerlink" title="一步生成 私钥 + 证书（有效期 10 年）"></a>一步生成 私钥 + 证书（有效期 10 年）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -x509 -newkey rsa:2048 -keyout private.key -out cert.crt -days 3650 -nodes</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-nodes</code>：私钥不加密</li>
<li><code>-days 3650</code>：有效期 10 年</li>
<li>执行后按提示填信息（可以全部回车）</li>
</ul>
<p>最终得到两个文件：</p>
<ul>
<li><code>private.key</code>：私钥</li>
<li><code>cert.crt</code>：自签名证书</li>
</ul>
<hr>
<h2 id="四、生成-CSR（向-CA-申请正式证书）"><a href="#四、生成-CSR（向-CA-申请正式证书）" class="headerlink" title="四、生成 CSR（向 CA 申请正式证书）"></a>四、生成 CSR（向 CA 申请正式证书）</h2><p>如果你要申请<strong>阿里云&#x2F;腾讯云&#x2F;Let’s Encrypt</strong> 正规证书，必须先生成 CSR。</p>
<h3 id="1-生成私钥"><a href="#1-生成私钥" class="headerlink" title="1. 生成私钥"></a>1. 生成私钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out private.key 2048</span><br></pre></td></tr></table></figure>

<h3 id="2-生成-CSR-请求文件"><a href="#2-生成-CSR-请求文件" class="headerlink" title="2. 生成 CSR 请求文件"></a>2. 生成 CSR 请求文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -new -key private.key -out cert.csr</span><br></pre></td></tr></table></figure>
<p>提交 <code>cert.csr</code> 给 CA 机构，就能拿到正式证书。</p>
<hr>
<h2 id="五、证书-密钥查看命令"><a href="#五、证书-密钥查看命令" class="headerlink" title="五、证书&#x2F;密钥查看命令"></a>五、证书&#x2F;密钥查看命令</h2><h3 id="1-查看私钥信息"><a href="#1-查看私钥信息" class="headerlink" title="1. 查看私钥信息"></a>1. 查看私钥信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl rsa -<span class="keyword">in</span> private.key -text -noout</span><br></pre></td></tr></table></figure>

<h3 id="2-查看证书信息（最常用）"><a href="#2-查看证书信息（最常用）" class="headerlink" title="2. 查看证书信息（最常用）"></a>2. 查看证书信息（最常用）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> cert.crt -text -noout</span><br></pre></td></tr></table></figure>

<h3 id="3-查看-CSR-信息"><a href="#3-查看-CSR-信息" class="headerlink" title="3. 查看 CSR 信息"></a>3. 查看 CSR 信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -<span class="keyword">in</span> cert.csr -text -noout</span><br></pre></td></tr></table></figure>

<h3 id="4-验证-私钥-和-证书-是否匹配"><a href="#4-验证-私钥-和-证书-是否匹配" class="headerlink" title="4. 验证 私钥 和 证书 是否匹配"></a>4. 验证 私钥 和 证书 是否匹配</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl rsa -modulus -<span class="keyword">in</span> private.key -noout | <span class="built_in">md5sum</span></span><br><span class="line">openssl x509 -modulus -<span class="keyword">in</span> cert.crt -noout | <span class="built_in">md5sum</span></span><br></pre></td></tr></table></figure>
<p>两个 MD5 值<strong>完全相同</strong> &#x3D; 匹配</p>
<hr>
<h2 id="六、格式转换（最常见需求）"><a href="#六、格式转换（最常见需求）" class="headerlink" title="六、格式转换（最常见需求）"></a>六、格式转换（最常见需求）</h2><h3 id="1-PEM-转-PKCS-12（-pfx-p12，Windows-IIS-用）"><a href="#1-PEM-转-PKCS-12（-pfx-p12，Windows-IIS-用）" class="headerlink" title="1. PEM 转 PKCS#12（.pfx &#x2F; .p12，Windows&#x2F;IIS 用）"></a>1. PEM 转 PKCS#12（.pfx &#x2F; .p12，Windows&#x2F;IIS 用）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl pkcs12 -<span class="built_in">export</span> -out cert.pfx -inkey private.key -<span class="keyword">in</span> cert.crt</span><br></pre></td></tr></table></figure>

<h3 id="2-从-PFX-提取-私钥-证书"><a href="#2-从-PFX-提取-私钥-证书" class="headerlink" title="2. 从 PFX 提取 私钥 + 证书"></a>2. 从 PFX 提取 私钥 + 证书</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 提取私钥</span></span><br><span class="line">openssl pkcs12 -<span class="keyword">in</span> cert.pfx -nocerts -out private.key</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提取证书</span></span><br><span class="line">openssl pkcs12 -<span class="keyword">in</span> cert.pfx -clcerts -nokeys -out cert.crt</span><br></pre></td></tr></table></figure>

<h3 id="3-DER-转-PEM"><a href="#3-DER-转-PEM" class="headerlink" title="3. DER 转 PEM"></a>3. DER 转 PEM</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -inform der -<span class="keyword">in</span> cert.der -out cert.crt</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="七、生产环境标准流程"><a href="#七、生产环境标准流程" class="headerlink" title="七、生产环境标准流程"></a>七、生产环境标准流程</h2><ol>
<li><strong>生成安全私钥</strong>（4096 位，带密码）<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -aes256 -out private.key 4096</span><br></pre></td></tr></table></figure></li>
<li><strong>生成 CSR</strong><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -new -key private.key -out cert.csr</span><br></pre></td></tr></table></figure></li>
<li>提交 CSR 给 CA → 获取<strong>正式证书</strong></li>
<li><strong>部署证书</strong>（Nginx&#x2F;Apache&#x2F;IIS）</li>
</ol>
<hr>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol>
<li>开发用：<strong>自签名证书</strong>（私钥 + crt）</li>
<li>生产用：<strong>CSR 申请正规证书</strong></li>
<li>必备命令：生成密钥、查看证书、格式转换、验证匹配</li>
<li>私钥<strong>绝对不能泄露</strong>，泄露等于证书失效</li>
</ol>
]]></content>
      <categories>
        <category>OpenSSL</category>
      </categories>
      <tags>
        <tag>OpenSSL</tag>
      </tags>
  </entry>
  <entry>
    <title>Oracle</title>
    <url>/2026/05/01/Oracle/Oracle/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Oracle</category>
      </categories>
      <tags>
        <tag>Oracle</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 PostgreSQL</title>
    <url>/2026/05/01/PostgreSQL/PostgreSQL-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>PostgreSQL</strong>，适合本地开发、与 <strong>Compose</strong> 编排其它服务、或在 <strong>Kubernetes</strong> 中单实例部署。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；密码与数据卷请按环境自行替换，勿把生产密钥提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、PostgreSQL-镜像选择"><a href="#二、PostgreSQL-镜像选择" class="headerlink" title="二、PostgreSQL 镜像选择"></a>二、PostgreSQL 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>postgres</code></strong>（Docker Hub 官方库，由 <strong>PostgreSQL</strong> &#x2F; <strong>Docker Official Images</strong> 维护）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>postgres:16</code></strong>：<strong>16.x</strong> 当前常用大版本。  </li>
<li><strong><code>postgres:17</code></strong> 或 <strong><code>postgres:latest</code></strong>：较新线（以 <a href="https://hub.docker.com/_/postgres/tags">Docker Hub postgres tags</a> 为准）。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定小版本标签（如 <strong><code>postgres:16.6</code></strong>）或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>超级用户与库</strong></td>
<td><strong><code>POSTGRES_USER</code></strong> 为数据库超级用户名（默认 <strong><code>postgres</code></strong>）；<strong><code>POSTGRES_PASSWORD</code></strong> 为其密码；<strong><code>POSTGRES_DB</code></strong> 为首次创建的库名（未设置时默认与 <strong><code>POSTGRES_USER</code></strong> 同名）。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>官方 <strong><code>postgres</code></strong> 多架构镜像一般可直接拉取；若某标签缺架构，换标签或查 Hub <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区有 <strong><code>bitnami/postgresql</code></strong> 等，环境变量与数据路径与官方不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-编码与本地化"><a href="#3-编码与本地化" class="headerlink" title="3. 编码与本地化"></a>3. 编码与本地化</h2><p>需要固定库编码、排序规则时，可通过 <strong><code>POSTGRES_INITDB_ARGS</code></strong> 传入 <strong><code>initdb</code></strong> 参数（如 <strong><code>--encoding=UTF8 --locale=C</code></strong>），与业务 <strong><code>client_encoding</code></strong>、迁移脚本约定一致。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式跑 <strong>PostgreSQL</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>PostgreSQL</strong> 一键部署：由面板管理镜像版本、数据持久化、端口与备份等。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s&#96;</strong> 的环境变量、卷路径对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>在<strong>当前目录</strong>先建数据与初始化目录（仅首次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./pg-data ./pg-init</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（请按需改账号、密码、库名、端口、卷路径；示例仅供本地）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name postgres-dev -e POSTGRES_USER=app -e POSTGRES_PASSWORD=dev-change-me -e POSTGRES_DB=appdb -p 5432:5432 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/pg-data:/var/lib/postgresql/data&quot;</span> -v <span class="string">&quot;<span class="subst">$(pwd)</span>/pg-init:/docker-entrypoint-initdb.d:ro&quot;</span> postgres:16</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f postgres-dev</span><br><span class="line">docker stop postgres-dev &amp;&amp; docker <span class="built_in">rm</span> postgres-dev</span><br></pre></td></tr></table></figure>

<p>本机连接示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">psql <span class="string">&quot;postgresql://app:dev-change-me@127.0.0.1:5432/appdb&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong><code>./pg-init</code></strong> 下 <strong><code>.sql</code> &#x2F; <code>.sh</code></strong> 仅在<strong>首次初始化空数据目录</strong>时执行；已有数据卷时不会重复执行。</p>
<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.postgres.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。敏感项建议放在同目录 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>），勿长期写死在仓库。</p>
<p><strong>一条命令</strong>启动全栈（在 <strong><code>docker-compose.postgres.yml</code></strong> 所在目录执行）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.postgres.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.postgres.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">postgres-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">postgres:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">postgres:16</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">postgres-16</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">POSTGRES_USER:</span> <span class="string">$&#123;POSTGRES_USER:-app&#125;</span></span><br><span class="line">      <span class="attr">POSTGRES_PASSWORD:</span> <span class="string">$&#123;POSTGRES_PASSWORD:-dev-change-me&#125;</span></span><br><span class="line">      <span class="attr">POSTGRES_DB:</span> <span class="string">$&#123;POSTGRES_DB:-appdb&#125;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;5432:5432&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">pg_data:/var/lib/postgresql/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./pg-init:/docker-entrypoint-initdb.d:ro</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;pg_isready -U \&quot;$$POSTGRES_USER\&quot; -d \&quot;$$POSTGRES_DB\&quot; || exit 1&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">30</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">30s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">pg_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>同目录可选 <strong><code>.env</code></strong>（勿提交）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">POSTGRES_USER=app</span><br><span class="line">POSTGRES_PASSWORD=your-strong-password</span><br><span class="line">POSTGRES_DB=appdb</span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.postgres.yml ps</span><br><span class="line">docker compose -f docker-compose.postgres.yml logs -f postgres</span><br><span class="line">docker compose -f docker-compose.postgres.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留命名卷 <strong><code>pg_data</code></strong>；若需清空数据加 <strong><code>docker compose ... down -v</code></strong>（<strong>会删库</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>postgres-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f postgres-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>Secret</strong>（超级用户与库名）、<strong>PVC</strong>、<strong>Deployment</strong>、<strong>ClusterIP Service</strong>。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">postgres-secret</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">POSTGRES_USER:</span> <span class="string">&quot;app&quot;</span></span><br><span class="line">  <span class="attr">POSTGRES_PASSWORD:</span> <span class="string">&quot;请替换为强密码&quot;</span></span><br><span class="line">  <span class="attr">POSTGRES_DB:</span> <span class="string">&quot;appdb&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">postgres-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">20Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">postgres</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">postgres</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">postgres</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">postgres</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">postgres</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">postgres:16</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">5432</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">postgres</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">POSTGRES_USER</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">postgres-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">POSTGRES_USER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">POSTGRES_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">postgres-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">POSTGRES_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">POSTGRES_DB</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">postgres-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">POSTGRES_DB</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">TZ</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;Asia/Shanghai&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/var/lib/postgresql/data</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">exec:</span></span><br><span class="line">              <span class="attr">command:</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">/bin/sh</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">&#x27;pg_isready -U &quot;$POSTGRES_USER&quot; -d &quot;$POSTGRES_DB&quot;&#x27;</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">24</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">5432</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">60</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;256Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;250m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;1Gi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;1000m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">postgres-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">postgres</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">postgres</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">postgres</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">postgres</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">5432</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">5432</span></span><br></pre></td></tr></table></figure>

<p>查看资源与连接前检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get pods,svc,pvc -l app=postgres</span><br></pre></td></tr></table></figure>

<p>集群内连接串示例（按 <strong>namespace</strong> 与 <strong>Secret</strong> 中密码替换）：<strong><code>postgresql://app:请替换@postgres.default.svc.cluster.local:5432/appdb?sslmode=disable</code></strong>（生产请改为 <strong><code>sslmode=require</code></strong> 并配置 <strong>TLS</strong>）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；高可用、复制、自动故障转移请使用 <strong>Patroni</strong>、<strong>CloudNativePG</strong> 等 <strong>Operator</strong> 或 <strong>StatefulSet</strong> 方案，勿直接水平扩展上述 <strong>Deployment</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先官方 <strong><code>postgres:16</code></strong> &#x2F; 固定补丁标签；核对 <strong><code>POSTGRES_*</code></strong> 与客户端驱动版本</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong> 后一条 <strong><code>docker run ...</code></strong></td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.postgres.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>postgres-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>同目录可参考：<strong><code>PostgreSQL.md</code></strong>（<strong>DDL</strong>）、<strong><code>PostgreSQL高级查询.md</code></strong>、<strong><code>PostgreSQL索引与性能优化.md</code></strong>、<strong><code>PostgreSQL并发与MVCC入门.md</code></strong>。</p>
<p>生产环境请配合备份、监控、网络策略与 <strong>TLS</strong>；<strong>Secret</strong> 建议对接 <strong>External Secrets</strong> &#x2F; <strong>Sealed Secrets</strong> 等管控流程。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL DDL 语句</title>
    <url>/2026/05/01/PostgreSQL/PostgreSQL/</url>
    <content><![CDATA[<h1 id="PostgreSQL-DDL-语句"><a href="#PostgreSQL-DDL-语句" class="headerlink" title="PostgreSQL DDL 语句"></a>PostgreSQL DDL 语句</h1><p><strong>DDL</strong>：Data Definition Language 数据定义语言<br>作用：<strong>定义&#x2F;修改&#x2F;删除 数据库、模式（</strong><code>SCHEMA</code><strong>）、表、字段、索引等对象结构</strong>，不直接操作「行级业务数据」本身（数据由 <strong>DML</strong> 写入）。</p>
<h2 id="DDL-主要分-3-大类"><a href="#DDL-主要分-3-大类" class="headerlink" title="DDL 主要分 3 大类"></a>DDL 主要分 3 大类</h2><ol>
<li><strong>库 &#x2F; 模式</strong>：<strong><code>CREATE</code> &#x2F; <code>ALTER</code> &#x2F; <code>DROP DATABASE</code></strong>；<strong><code>CREATE SCHEMA</code></strong> 等（<strong><code>SCHEMA</code></strong> 类似命名空间，用于组织表与权限）。  </li>
<li><strong>表与列</strong>：<strong><code>CREATE</code> &#x2F; <code>ALTER</code> &#x2F; <code>DROP TABLE</code></strong>；约束、默认值、自增（<strong><code>IDENTITY</code></strong> &#x2F; <strong><code>SERIAL</code></strong>）。  </li>
<li><strong>其他对象</strong>：<strong><code>VIEW</code></strong>、<strong><code>INDEX</code></strong>、<strong><code>SEQUENCE</code></strong>、<strong><code>EXTENSION</code></strong> 等（入门可先把 <strong>表 + 索引</strong> 吃透）。</li>
</ol>
<p>与 <strong>Docker 部署</strong>、<strong>高级查询</strong>、<strong>索引与性能</strong>、<strong>并发与 MVCC</strong> 等姊妹文同目录，可按需互链（<strong><code>PostgreSQL-Docker.md</code></strong> 等）。</p>
<hr>
<h1 id="一、数据库与模式"><a href="#一、数据库与模式" class="headerlink" title="一、数据库与模式"></a>一、数据库与模式</h1><h3 id="1-创建数据库"><a href="#1-创建数据库" class="headerlink" title="1. 创建数据库"></a>1. 创建数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 基础创建（编码与排序规则可用模板或参数显式指定）</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE db1;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE db2</span><br><span class="line">    <span class="keyword">WITH</span> ENCODING <span class="string">&#x27;UTF8&#x27;</span></span><br><span class="line">    LC_COLLATE <span class="operator">=</span> <span class="string">&#x27;en_US.UTF-8&#x27;</span></span><br><span class="line">    LC_CTYPE   <span class="operator">=</span> <span class="string">&#x27;en_US.UTF-8&#x27;</span></span><br><span class="line">    TEMPLATE   <span class="operator">=</span> template0;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：<strong><code>LC_*</code></strong> 以操作系统可用区域为准；中文环境常见 <strong><code>zh_CN.UTF-8</code></strong> 等，需与运维&#x2F;宿主镜像约定一致。</p>
<h3 id="2-列出与切换当前数据库"><a href="#2-列出与切换当前数据库" class="headerlink" title="2. 列出与切换当前数据库"></a>2. 列出与切换当前数据库</h3><p><strong><code>psql</code></strong> 元命令（客户端内）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\l              -- 列出数据库</span><br><span class="line">\c db2          -- 连接到 db2</span><br></pre></td></tr></table></figure>

<p><strong>SQL</strong> 层可查 <strong><code>pg_database</code></strong>（节选）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> datname <span class="keyword">FROM</span> pg_database <span class="keyword">WHERE</span> datistemplate <span class="operator">=</span> <span class="literal">false</span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-模式-SCHEMA（强烈建议了解）"><a href="#3-模式-SCHEMA（强烈建议了解）" class="headerlink" title="3. 模式 SCHEMA（强烈建议了解）"></a>3. 模式 SCHEMA（强烈建议了解）</h3><p>表可建在 <strong><code>public</code></strong> 以外的模式中，便于多租户或模块隔离：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> SCHEMA IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> app <span class="keyword">AUTHORIZATION</span> <span class="built_in">CURRENT_USER</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SET</span> search_path <span class="keyword">TO</span> app, public;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> app.hello (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    msg text <span class="keyword">NOT NULL</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h3 id="4-删除数据库"><a href="#4-删除数据库" class="headerlink" title="4. 删除数据库"></a>4. 删除数据库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> DATABASE IF <span class="keyword">EXISTS</span> db1;</span><br></pre></td></tr></table></figure>

<p><strong><code>IF EXISTS</code></strong>：避免库不存在时报错，<strong>新手必加</strong>。删除前须<strong>无活跃连接</strong>占用该库（必要时 <strong><code>pg_terminate_backend</code></strong> 或先停应用）。</p>
<hr>
<h1 id="二、数据表-DDL-操作（重点）"><a href="#二、数据表-DDL-操作（重点）" class="headerlink" title="二、数据表 DDL 操作（重点）"></a>二、数据表 DDL 操作（重点）</h1><h2 id="1-创建表-CREATE-TABLE"><a href="#1-创建表-CREATE-TABLE" class="headerlink" title="1. 创建表 CREATE TABLE"></a>1. 创建表 CREATE TABLE</h2><p>语法骨架：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> 表名 (</span><br><span class="line">    列名 类型 [约束],</span><br><span class="line">    ...</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>示例（<strong><code>IDENTITY</code></strong> 自增主键；时间列用 <strong><code>timestamptz</code></strong> 更利于多时区）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SET</span> search_path <span class="keyword">TO</span> public;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> student (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    name <span class="type">varchar</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    age <span class="type">smallint</span>,</span><br><span class="line">    gender <span class="type">char</span>(<span class="number">2</span>),</span><br><span class="line">    create_time timestamptz <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> now()</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>常用类型（节选）：</p>
<ul>
<li><strong><code>integer</code> &#x2F; <code>bigint</code></strong>：整型  </li>
<li><strong><code>varchar(n)</code> &#x2F; <code>text</code></strong>：变长字符串（<strong><code>text</code></strong> 无长度上限，多数场景可优先 <strong><code>text</code> + CHECK</strong>）  </li>
<li><strong><code>boolean</code></strong>：布尔  </li>
<li><strong><code>date</code> &#x2F; <code>timestamp</code> &#x2F; <code>timestamptz</code></strong>：日期与时间（推荐 <strong><code>timestamptz</code></strong> 存绝对时刻）</li>
</ul>
<p>常用约束：</p>
<ul>
<li><strong><code>PRIMARY KEY</code></strong>  </li>
<li><strong><code>GENERATED { ALWAYS | BY DEFAULT } AS IDENTITY</code></strong>（推荐）或 <strong><code>SERIAL</code> &#x2F; <code>BIGSERIAL</code></strong>（底层仍是序列，语义略宽）  </li>
<li><strong><code>NOT NULL</code></strong>、<strong><code>DEFAULT</code></strong>  </li>
<li><strong><code>UNIQUE</code></strong>、<strong><code>REFERENCES</code></strong>（外键）</li>
</ul>
<h3 id="UNIQUE（唯一约束）"><a href="#UNIQUE（唯一约束）" class="headerlink" title="UNIQUE（唯一约束）"></a>UNIQUE（唯一约束）</h3><ul>
<li><strong>与主键</strong>：每表一个 <strong><code>PRIMARY KEY</code></strong>，可有多个 <strong><code>UNIQUE</code></strong>。  </li>
<li><strong><code>NULL</code></strong>：<strong>PostgreSQL</strong> 里 <strong><code>UNIQUE</code></strong> 认为 <strong><code>NULL</code> 与 <code>NULL</code> 不相等</strong>，故<strong>多行</strong>可取 <strong><code>NULL</code></strong>（与 <strong>SQL 标准</strong>一致）；若业务只允许一行未填，需 <strong><code>NOT NULL</code></strong> 或部分唯一索引（<strong><code>UNIQUE … WHERE col IS NOT NULL</code></strong>）等。</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_user (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    username <span class="type">varchar</span>(<span class="number">32</span>) <span class="keyword">NOT NULL</span> <span class="keyword">UNIQUE</span>,</span><br><span class="line">    email <span class="type">varchar</span>(<span class="number">64</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> uk_email <span class="keyword">UNIQUE</span> (email)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_staff (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    dept_id <span class="type">integer</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    emp_no <span class="type">varchar</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> uk_dept_emp <span class="keyword">UNIQUE</span> (dept_id, emp_no)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h3 id="FOREIGN-KEY（外键）"><a href="#FOREIGN-KEY（外键）" class="headerlink" title="FOREIGN KEY（外键）"></a>FOREIGN KEY（外键）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_dept (</span><br><span class="line">    id <span class="type">integer</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> demo_emp (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    dept_id <span class="type">integer</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    name <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_emp_dept <span class="keyword">FOREIGN KEY</span> (dept_id)</span><br><span class="line">        <span class="keyword">REFERENCES</span> demo_dept (id)</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">UPDATE</span> CASCADE</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">DELETE</span> RESTRICT</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-查看表结构"><a href="#2-查看表结构" class="headerlink" title="2. 查看表结构"></a>2. 查看表结构</h2><p><strong><code>psql</code></strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\dt                 -- 当前 search_path 下表</span><br><span class="line">\d student          -- 列、约束、索引摘要</span><br><span class="line">\d+ student         -- 更详细（存储参数等）</span><br></pre></td></tr></table></figure>

<p><strong><code>SQL</code></strong>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> column_name, data_type, is_nullable, column_default</span><br><span class="line"><span class="keyword">FROM</span> information_schema.columns</span><br><span class="line"><span class="keyword">WHERE</span> table_schema <span class="operator">=</span> <span class="string">&#x27;public&#x27;</span> <span class="keyword">AND</span> table_name <span class="operator">=</span> <span class="string">&#x27;student&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> ordinal_position;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-修改表-ALTER-TABLE"><a href="#3-修改表-ALTER-TABLE" class="headerlink" title="3. 修改表 ALTER TABLE"></a>3. 修改表 ALTER TABLE</h2><h3 id="3-1-新增列"><a href="#3-1-新增列" class="headerlink" title="3.1 新增列"></a>3.1 新增列</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> email <span class="type">varchar</span>(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER TABLE</span> student</span><br><span class="line">    <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> mobile <span class="type">varchar</span>(<span class="number">20</span>),</span><br><span class="line">    <span class="keyword">ADD CONSTRAINT</span> uk_student_mobile <span class="keyword">UNIQUE</span> (mobile);</span><br></pre></td></tr></table></figure>

<p>（<strong>PostgreSQL</strong> 无 <strong><code>AFTER 某列</code></strong> 语法；列顺序一般不作为设计重点。）</p>
<h3 id="3-2-修改类型-默认值-非空"><a href="#3-2-修改类型-默认值-非空" class="headerlink" title="3.2 修改类型 &#x2F; 默认值 &#x2F; 非空"></a>3.2 修改类型 &#x2F; 默认值 &#x2F; 非空</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student</span><br><span class="line">    <span class="keyword">ALTER</span> <span class="keyword">COLUMN</span> age TYPE <span class="type">smallint</span>,</span><br><span class="line">    <span class="keyword">ALTER</span> <span class="keyword">COLUMN</span> name <span class="keyword">SET</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;匿名&#x27;</span>,</span><br><span class="line">    <span class="keyword">ALTER</span> <span class="keyword">COLUMN</span> gender <span class="keyword">SET</span> <span class="keyword">NOT NULL</span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-重命名列-表"><a href="#3-3-重命名列-表" class="headerlink" title="3.3 重命名列 &#x2F; 表"></a>3.3 重命名列 &#x2F; 表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> student RENAME <span class="keyword">COLUMN</span> email <span class="keyword">TO</span> email_primary;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> student RENAME <span class="keyword">TO</span> stu;</span><br></pre></td></tr></table></figure>

<h3 id="3-4-删除列-约束"><a href="#3-4-删除列-约束" class="headerlink" title="3.4 删除列 &#x2F; 约束"></a>3.4 删除列 &#x2F; 约束</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> stu <span class="keyword">DROP</span> <span class="keyword">COLUMN</span> IF <span class="keyword">EXISTS</span> mobile;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> stu <span class="keyword">DROP</span> <span class="keyword">CONSTRAINT</span> IF <span class="keyword">EXISTS</span> uk_student_mobile;</span><br></pre></td></tr></table></figure>

<h3 id="3-5-删除外键-增加外键（示例）"><a href="#3-5-删除外键-增加外键（示例）" class="headerlink" title="3.5 删除外键 &#x2F; 增加外键（示例）"></a>3.5 删除外键 &#x2F; 增加外键（示例）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> demo_emp <span class="keyword">DROP</span> <span class="keyword">CONSTRAINT</span> IF <span class="keyword">EXISTS</span> fk_emp_dept;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER TABLE</span> demo_emp</span><br><span class="line">    <span class="keyword">ADD CONSTRAINT</span> fk_emp_dept <span class="keyword">FOREIGN KEY</span> (dept_id)</span><br><span class="line">        <span class="keyword">REFERENCES</span> demo_dept (id)</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">UPDATE</span> CASCADE</span><br><span class="line">        <span class="keyword">ON</span> <span class="keyword">DELETE</span> RESTRICT;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-删除表"><a href="#4-删除表" class="headerlink" title="4. 删除表"></a>4. 删除表</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> stu;</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> demo_emp, demo_staff, demo_user, demo_dept;</span><br></pre></td></tr></table></figure>

<p>依赖较多时慎用 <strong><code>CASCADE</code></strong>（会级联删依赖对象），<strong>生产务必先确认影响面</strong>。</p>
<hr>
<h1 id="三、DDL-与-DML-区别（必记）"><a href="#三、DDL-与-DML-区别（必记）" class="headerlink" title="三、DDL 与 DML 区别（必记）"></a>三、DDL 与 DML 区别（必记）</h1><table>
<thead>
<tr>
<th>分类</th>
<th>全称</th>
<th>作用</th>
<th>常用语句</th>
</tr>
</thead>
<tbody><tr>
<td>DDL</td>
<td>数据定义语言</td>
<td>管<strong>库、模式、表、索引、视图等结构</strong></td>
<td><strong><code>CREATE</code> &#x2F; <code>ALTER</code> &#x2F; <code>DROP</code></strong></td>
</tr>
<tr>
<td>DML</td>
<td>数据操作语言</td>
<td>管<strong>表内行数据</strong></td>
<td><strong><code>INSERT</code> &#x2F; <code>UPDATE</code> &#x2F; <code>DELETE</code> &#x2F; <code>SELECT</code></strong></td>
</tr>
</tbody></table>
<p>简单记：<strong>DDL 改架子</strong>；<strong>DML 改数据</strong>。</p>
<hr>
<h1 id="四、入门必背-DDL-极简清单"><a href="#四、入门必背-DDL-极简清单" class="headerlink" title="四、入门必背 DDL 极简清单"></a>四、入门必背 DDL 极简清单</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 库 / 模式</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE <span class="operator">/</span> <span class="keyword">DROP</span> DATABASE IF <span class="keyword">EXISTS</span></span><br><span class="line"><span class="keyword">CREATE</span> SCHEMA <span class="operator">/</span> <span class="keyword">SET</span> search_path</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 表</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> <span class="operator">/</span> <span class="keyword">ALTER TABLE</span> <span class="operator">/</span> <span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span></span><br><span class="line"><span class="keyword">PRIMARY KEY</span> <span class="operator">/</span> GENERATED … <span class="keyword">AS</span> <span class="keyword">IDENTITY</span> <span class="operator">/</span> <span class="keyword">UNIQUE</span> <span class="operator">/</span> <span class="keyword">REFERENCES</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看</span></span><br><span class="line">psql: \l \dt \d</span><br><span class="line"><span class="keyword">SQL</span>: information_schema <span class="operator">/</span> pg_catalog</span><br></pre></td></tr></table></figure>

<p>进阶阅读：<strong><code>PostgreSQL-Docker.md</code></strong>（部署）、<strong><code>PostgreSQL高级查询.md</code></strong>（<strong>SELECT</strong> 进阶）、<strong><code>PostgreSQL索引与性能优化.md</code></strong>、<strong><code>PostgreSQL并发与MVCC入门.md</code></strong>。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>DDL</tag>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL 并发与 MVCC 入门</title>
    <url>/2026/05/03/PostgreSQL/PostgreSQL%E5%B9%B6%E5%8F%91%E4%B8%8EMVCC%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p><strong>PostgreSQL</strong> 以 <strong>MVCC（多版本并发控制）</strong> 为主：读写通常不互相阻塞读路径；写与写仍可能因<strong>行级锁</strong>、<strong>唯一约束</strong>等冲突。本文偏<strong>用法与现象</strong>，与 <strong><code>PostgreSQL索引与性能优化.md</code></strong>（<strong><code>EXPLAIN</code>、隔离级别配置</strong>）互补。若你熟悉 <strong>《MySQL锁详解》</strong>（<strong><code>MySQL锁详解.md</code></strong>），可对照：<strong>InnoDB RR + 间隙锁</strong> 与 <strong>PostgreSQL</strong> 的<strong>快照与谓词锁（SSI）</strong> 并非一一对应，勿硬套术语。</p>
<hr>
<h2 id="一、快照与默认隔离级别"><a href="#一、快照与默认隔离级别" class="headerlink" title="一、快照与默认隔离级别"></a>一、快照与默认隔离级别</h2><ul>
<li>默认 <strong><code>READ COMMITTED</code></strong>：单条 <strong>SQL</strong> 内看到<strong>语句开始时</strong>已提交的数据版本。  </li>
<li><strong><code>REPEATABLE READ</code></strong>：事务内<strong>首条 SQL</strong> 建立快照，后续读一致。  </li>
<li><strong><code>SERIALIZABLE</code></strong>：可检测读写依赖，失败时返回 <strong><code>40001 serialization_failure</code></strong>，应用应<strong>重试</strong>。</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> transaction_isolation;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="二、显式行锁：FOR-UPDATE-FOR-SHARE"><a href="#二、显式行锁：FOR-UPDATE-FOR-SHARE" class="headerlink" title="二、显式行锁：FOR UPDATE &#x2F; FOR SHARE"></a>二、显式行锁：<code>FOR UPDATE</code> &#x2F; <code>FOR SHARE</code></h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> accounts <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">FOR</span> <span class="keyword">UPDATE</span>;</span><br><span class="line"><span class="comment">-- 其他事务对同一行的 FOR UPDATE / 写操作会阻塞或失败（视 NOWAIT）</span></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>FOR UPDATE</code></strong>：排他行锁，用于「先读后改」的临界区。  </li>
<li><strong><code>FOR SHARE</code></strong>：共享行锁，允许他人<strong>并发读</strong>，阻塞<strong>写</strong>。</li>
</ul>
<hr>
<h2 id="三、SKIP-LOCKED-与-NOWAIT（队列与抢锁）"><a href="#三、SKIP-LOCKED-与-NOWAIT（队列与抢锁）" class="headerlink" title="三、SKIP LOCKED 与 NOWAIT（队列与抢锁）"></a>三、<code>SKIP LOCKED</code> 与 <code>NOWAIT</code>（队列与抢锁）</h2><p><strong><code>SKIP LOCKED</code></strong>：跳过已被其他事务锁定的行，取下一批可用行——常用于<strong>任务队列</strong>、<strong>公平抢单</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> jobs</span><br><span class="line"><span class="keyword">WHERE</span> status <span class="operator">=</span> <span class="string">&#x27;pending&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> id</span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">UPDATE</span> <span class="keyword">SKIP</span> LOCKED</span><br><span class="line">LIMIT <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- 抢到则更新为 processing，再 COMMIT</span></span><br></pre></td></tr></table></figure>

<p><strong><code>NOWAIT</code></strong>：若不能立即加锁则<strong>立刻报错</strong>，适合避免长时间等待（由应用捕获错误后重试或放弃）。</p>
<hr>
<h2 id="四、死锁（deadlock-detected）"><a href="#四、死锁（deadlock-detected）" class="headerlink" title="四、死锁（deadlock detected）"></a>四、死锁（<code>deadlock detected</code>）</h2><p><strong>PostgreSQL</strong> 会自动检测<strong>事务间循环等待</strong>，回滚<strong>其中一个事务</strong>（报错 <strong><code>40P01 deadlock_detected</code></strong>）。<br>规避思路与 <strong>MySQL</strong> 类似：<strong>固定加锁顺序</strong>、<strong>缩短事务</strong>、<strong>避免无关长耗时</strong>在事务内执行。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 排障时常看（需权限）</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> pg_locks <span class="keyword">WHERE</span> <span class="keyword">NOT</span> granted;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="五、与-MySQL-InnoDB-的粗对照（防混）"><a href="#五、与-MySQL-InnoDB-的粗对照（防混）" class="headerlink" title="五、与 MySQL InnoDB 的粗对照（防混）"></a>五、与 <strong>MySQL InnoDB</strong> 的粗对照（防混）</h2><table>
<thead>
<tr>
<th>话题</th>
<th>MySQL InnoDB（见 <strong><code>MySQL锁详解.md</code></strong>）</th>
<th>PostgreSQL（入门记法）</th>
</tr>
</thead>
<tbody><tr>
<td>默认隔离</td>
<td>常为 <strong>RR</strong></td>
<td><strong>RC</strong></td>
</tr>
<tr>
<td>幻读与范围锁</td>
<td><strong>间隙锁 &#x2F; Next-Key</strong> 叙述多</td>
<td><strong>SSI &#x2F; 谓词锁</strong> 等在 <strong>SERIALIZABLE</strong> 下讨论多</td>
</tr>
<tr>
<td>队列抢任务</td>
<td><strong><code>FOR UPDATE</code></strong> + 设计索引</td>
<td><strong><code>FOR UPDATE SKIP LOCKED</code></strong> 一等公民</td>
</tr>
</tbody></table>
<hr>
<h2 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h2><ul>
<li><strong><code>PostgreSQL索引与性能优化.md</code></strong>：<code>EXPLAIN</code>、<strong><code>VACUUM</code></strong>、慢语句。  </li>
<li><strong><code>PostgreSQL-Docker.md</code></strong>：实例与卷、连接串。  </li>
<li><strong><code>PostgreSQL高级查询.md</code></strong>：复杂只读查询。</li>
</ul>
<p>线上问题请结合 <strong><code>pg_stat_activity</code></strong>、<strong><code>pg_locks</code></strong>、日志与监控，本文不替代完整 <strong>DBA Runbook</strong>。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>事务</tag>
        <tag>PostgreSQL</tag>
        <tag>MVCC</tag>
        <tag>锁</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL 索引与性能优化</title>
    <url>/2026/05/03/PostgreSQL/PostgreSQL%E7%B4%A2%E5%BC%95%E4%B8%8E%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<p>面向<strong>业务开发与日常运维</strong>：能读 <strong><code>EXPLAIN</code></strong>、理解<strong>默认隔离级别</strong>、会开<strong>慢语句日志</strong>、知道<strong>逻辑备份与 WAL &#x2F; PITR</strong>在说什么。与 <strong><code>PostgreSQL并发与MVCC入门.md</code></strong>（行锁、<strong><code>SKIP LOCKED</code></strong>、死锁与快照）互补；部署与数据卷见 <strong><code>PostgreSQL-Docker.md</code></strong>。</p>
<hr>
<h2 id="一、索引在干什么：B-Tree-直觉（默认）"><a href="#一、索引在干什么：B-Tree-直觉（默认）" class="headerlink" title="一、索引在干什么：B-Tree 直觉（默认）"></a>一、索引在干什么：B-Tree 直觉（默认）</h2><p><strong>PostgreSQL</strong> 默认 <strong><code>CREATE INDEX</code></strong> 多为 <strong>B-Tree</strong>（<strong><code>USING btree</code></strong>，可省略）。<br>直觉与 <strong>MySQL InnoDB</strong> 二级索引类似：以<strong>平衡树</strong>组织键，<strong>等值 &#x2F; 范围</strong>查询减少随机读；<strong>主键 &#x2F; 唯一</strong>约束底层也会建索引。</p>
<p>其他索引类型（<strong>点到为止</strong>）：<strong><code>GIN</code></strong>（全文、<strong><code>jsonb</code></strong> 含键）、<strong><code>GiST</code></strong>（地理、范围类型）、<strong><code>BRIN</code></strong>（超大顺序相关表）等，选型需读官方文档与压测。</p>
<hr>
<h2 id="二、联合索引与最左前缀"><a href="#二、联合索引与最左前缀" class="headerlink" title="二、联合索引与最左前缀"></a>二、联合索引与最左前缀</h2><p>联合索引 <strong><code>CREATE INDEX idx_abc ON t (a, b, c);</code></strong> 的排序语义仍是<strong>先 <code>a</code> 再 <code>b</code> 再 <code>c</code></strong>。<br><strong><code>WHERE a = ? AND b = ?</code></strong> 易命中；仅 <strong><code>WHERE b = ?</code></strong> 往往<strong>用不上</strong>该索引（与 <strong>MySQL</strong> 同款直觉）。<br><strong>函数包裹列</strong>（如 <strong><code>WHERE lower(email) = ?</code></strong>）常导致<strong>无法走 btree</strong>，需<strong>表达式索引</strong>（<strong><code>CREATE INDEX ON t ((lower(email)))</code></strong>）或改写法。</p>
<hr>
<h2 id="三、仅索引扫描（Index-Only-Scan）"><a href="#三、仅索引扫描（Index-Only-Scan）" class="headerlink" title="三、仅索引扫描（Index Only Scan）"></a>三、仅索引扫描（Index Only Scan）</h2><p>若查询列均可由<strong>某索引</strong>覆盖，且<strong>可见性映射（VM）<strong>允许优化器认为</strong>堆页全可见</strong>，<strong><code>EXPLAIN</code></strong> 可能出现 <strong><code>Index Only Scan</code></strong>。<br>与 <strong>MySQL</strong> 的 <strong><code>Using index</code></strong> 类似但不等价；仍以 <strong><code>EXPLAIN (ANALYZE, BUFFERS)</code></strong> 实测为准。</p>
<hr>
<h2 id="四、怎么看执行计划：EXPLAIN-EXPLAIN-ANALYZE"><a href="#四、怎么看执行计划：EXPLAIN-EXPLAIN-ANALYZE" class="headerlink" title="四、怎么看执行计划：EXPLAIN &#x2F; EXPLAIN ANALYZE"></a>四、怎么看执行计划：<code>EXPLAIN</code> &#x2F; <code>EXPLAIN ANALYZE</code></h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">EXPLAIN (ANALYZE, BUFFERS, VERBOSE)</span><br><span class="line"><span class="keyword">SELECT</span> e.name, <span class="built_in">SUM</span>(o.amount)</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">JOIN</span> orders o <span class="keyword">ON</span> o.emp_id <span class="operator">=</span> e.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> e.id, e.name;</span><br></pre></td></tr></table></figure>

<p>入门优先看：</p>
<table>
<thead>
<tr>
<th>字段 &#x2F; 标记</th>
<th>含义（入门）</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>Seq Scan</code></strong></td>
<td>顺序扫表，大表 + 高 <strong><code>rows</code></strong> 要警惕</td>
</tr>
<tr>
<td><strong><code>Index Scan</code> &#x2F; <code>Index Only Scan</code></strong></td>
<td>走索引访问</td>
</tr>
<tr>
<td><strong><code>Nested Loop</code> &#x2F; <code>Hash Join</code> &#x2F; <code>Merge Join</code></strong></td>
<td>连接策略</td>
</tr>
<tr>
<td><strong><code>ANALYZE</code></strong> 的 <strong><code>actual time</code></strong>、<strong><code>rows</code></strong></td>
<td>真实耗时与行数（与<strong>统计信息</strong>、缓存有关）</td>
</tr>
<tr>
<td><strong><code>BUFFERS</code></strong></td>
<td>读了多少<strong>共享缓冲区</strong>（判断是否 I&#x2F;O 重）</td>
</tr>
</tbody></table>
<p>统计信息维护：<strong><code>ANALYZE tbl;</code></strong>（<strong><code>VACUUM ANALYZE</code></strong> 亦可），大变更后应跑。</p>
<hr>
<h2 id="五、事务与隔离级别（用法向）"><a href="#五、事务与隔离级别（用法向）" class="headerlink" title="五、事务与隔离级别（用法向）"></a>五、事务与隔离级别（用法向）</h2><h3 id="1-基本写法"><a href="#1-基本写法" class="headerlink" title="1. 基本写法"></a>1. 基本写法</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line"><span class="comment">-- DML 多条</span></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"><span class="comment">-- 或 ROLLBACK;</span></span><br></pre></td></tr></table></figure>

<p>默认 <strong><code>READ COMMITTED</code></strong>：每条语句看到<strong>语句开始时</strong>已提交的快照（简化理解）。<br><strong><code>REPEATABLE READ</code></strong>：事务内<strong>第一条语句</strong>建立快照，读一致性更强。<br><strong><code>SERIALIZABLE</code></strong>：最强，可能出现 <strong><code>serialization_failure</code></strong>，应用需<strong>重试</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> transaction_isolation;</span><br><span class="line"><span class="keyword">SET</span> SESSION CHARACTERISTICS <span class="keyword">AS</span> TRANSACTION ISOLATION LEVEL REPEATABLE READ;</span><br></pre></td></tr></table></figure>

<p>与 <strong>MySQL InnoDB 默认 RR</strong> 不同：<strong>PostgreSQL 默认 RC</strong>；跨库迁移时注意语义差异。行级锁、<strong><code>FOR UPDATE</code></strong>、死锁见 <strong><code>PostgreSQL并发与MVCC入门.md</code></strong>。</p>
<hr>
<h2 id="六、慢语句与观测「三板斧」（点到为止）"><a href="#六、慢语句与观测「三板斧」（点到为止）" class="headerlink" title="六、慢语句与观测「三板斧」（点到为止）"></a>六、慢语句与观测「三板斧」（点到为止）</h2><h3 id="1-日志：log-min-duration-statement"><a href="#1-日志：log-min-duration-statement" class="headerlink" title="1. 日志：log_min_duration_statement"></a>1. 日志：<code>log_min_duration_statement</code></h3><p>在 <strong><code>postgresql.conf</code></strong> 或 <strong><code>ALTER SYSTEM</code></strong>（需 reload）中设置，例如记录超过 <strong>200ms</strong> 的语句（数值按环境调）。</p>
<h3 id="2-pg-stat-statements（扩展）"><a href="#2-pg-stat-statements（扩展）" class="headerlink" title="2. pg_stat_statements（扩展）"></a>2. <strong><code>pg_stat_statements</code></strong>（扩展）</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> EXTENSION IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> pg_stat_statements;</span><br><span class="line"><span class="keyword">SELECT</span> query, calls, mean_exec_time</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> mean_exec_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<p>需 <strong><code>shared_preload_libraries</code></strong> 预加载（运维配置），此处不展开。</p>
<h3 id="3-auto-explain（扩展）"><a href="#3-auto-explain（扩展）" class="headerlink" title="3. auto_explain（扩展）"></a>3. <strong><code>auto_explain</code></strong>（扩展）</h3><p>自动对超时语句打计划，适合测试环境；生产按规范启用。</p>
<p><strong>落地顺序</strong>：先<strong>慢日志</strong>或 <strong><code>pg_stat_statements</code></strong> 抓 <strong>Top SQL</strong> → <strong><code>EXPLAIN (ANALYZE, BUFFERS)</code></strong> → 改索引或改写法 → <strong><code>VACUUM ANALYZE</code></strong>。</p>
<hr>
<h2 id="七、VACUUM-与膨胀（可靠性常识）"><a href="#七、VACUUM-与膨胀（可靠性常识）" class="headerlink" title="七、VACUUM 与膨胀（可靠性常识）"></a>七、<code>VACUUM</code> 与<strong>膨胀</strong>（可靠性常识）</h2><p><strong>PostgreSQL</strong> 的 <strong>MVCC</strong> 会使更新&#x2F;删除留下<strong>死元组</strong>；<strong><code>VACUUM</code></strong>（通常由 <strong>autovacuum</strong> 自动跑）回收空间、更新统计信息。<strong>长期不 vacuum</strong> 会导致<strong>表膨胀</strong>、性能变差。<br>日常开发：大批量导入&#x2F;更新后，测试库可手动 <strong><code>VACUUM ANALYZE</code></strong>；生产依赖 <strong>autovacuum</strong> 参数与监控。</p>
<hr>
<h2 id="八、备份恢复与-PITR-概念"><a href="#八、备份恢复与-PITR-概念" class="headerlink" title="八、备份恢复与 PITR 概念"></a>八、备份恢复与 <strong>PITR</strong> 概念</h2><h3 id="1-逻辑备份-pg-dump-pg-restore"><a href="#1-逻辑备份-pg-dump-pg-restore" class="headerlink" title="1. 逻辑备份 pg_dump &#x2F; pg_restore"></a>1. 逻辑备份 <strong><code>pg_dump</code> &#x2F; <code>pg_restore</code></strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pg_dump -h 127.0.0.1 -U app -d appdb -Fc -f appdb.dump</span><br><span class="line">pg_restore -h 127.0.0.1 -U app -d appdb_new -Fc appdb.dump</span><br></pre></td></tr></table></figure>

<p><strong><code>-Fc</code></strong> 为自定义目录格式，便于并行恢复；纯 <strong>SQL</strong> 可用 <strong><code>-Fp</code></strong>。</p>
<h3 id="2-物理与-WAL"><a href="#2-物理与-WAL" class="headerlink" title="2. 物理与 WAL"></a>2. 物理与 <strong>WAL</strong></h3><p><strong>PITR</strong> 依赖：<strong>基础备份</strong>（文件系统级或 <strong><code>pg_basebackup</code></strong>）+ <strong>连续归档的 WAL</strong> + <strong>恢复目标时间</strong>。与 <strong>MySQL binlog</strong> 地位类似的是 <strong>PostgreSQL WAL</strong>；具体恢复命令属 <strong>DBA</strong> 手册级。</p>
<h3 id="3-与-Docker-的关系"><a href="#3-与-Docker-的关系" class="headerlink" title="3. 与 Docker 的关系"></a>3. 与 <strong>Docker</strong> 的关系</h3><p>容器内 <strong><code>$PGDATA</code></strong> 一般在卷上（见 <strong><code>PostgreSQL-Docker.md</code></strong>）；备份策略要约定「从宿主机 <strong><code>pg_dump</code></strong> 连容器端口」还是「卷快照 + <strong>WAL</strong> 归档」，并做<strong>恢复演练</strong>。</p>
<hr>
<h2 id="九、姊妹文索引"><a href="#九、姊妹文索引" class="headerlink" title="九、姊妹文索引"></a>九、姊妹文索引</h2><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>PostgreSQL.md</code></strong></td>
<td><strong>DDL</strong>、<strong><code>SCHEMA</code></strong>、约束</td>
</tr>
<tr>
<td><strong><code>PostgreSQL高级查询.md</code></strong></td>
<td><strong>CTE</strong>、窗口、<strong><code>DISTINCT ON</code></strong>、<strong><code>FILTER</code></strong></td>
</tr>
<tr>
<td><strong><code>PostgreSQL并发与MVCC入门.md</code></strong></td>
<td><strong><code>FOR UPDATE</code></strong>、死锁、快照</td>
</tr>
<tr>
<td><strong><code>PostgreSQL-Docker.md</code></strong></td>
<td><strong>Docker &#x2F; Compose &#x2F; K8s</strong> 部署</td>
</tr>
</tbody></table>
<p>生产请叠加：<strong>连接池</strong>、<strong>监控</strong>、<strong>TLS</strong>、<strong>只读副本延迟</strong>与<strong>变更规范</strong>。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>索引</tag>
        <tag>EXPLAIN</tag>
        <tag>性能优化</tag>
        <tag>事务</tag>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL 存储与执行模型概览</title>
    <url>/2026/05/03/PostgreSQL/PostgreSQL%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<p><strong>PostgreSQL</strong> <strong>没有</strong>与 <strong>MySQL</strong> 建表子句 <strong><code>ENGINE=InnoDB / MyISAM</code></strong> 等价的「多存储引擎二选一」模型：进程内是一套统一的<strong>后台执行与存储子系统</strong>（常说的 <strong>PostgreSQL 引擎</strong>多指<strong>整个数据库内核版本</strong>，而非可选插件式表引擎）。若需对照 <strong>MySQL 存储引擎</strong>，请读同目录 <strong><code>MySQL引擎.md</code></strong>。</p>
<p>姊妹文：<strong><code>PostgreSQL并发与MVCC入门.md</code></strong>（快照与行锁）、<strong><code>PostgreSQL索引与性能优化.md</code></strong>（<strong><code>EXPLAIN</code>、<code>VACUUM</code></strong>）、<strong><code>PostgreSQL-Docker.md</code></strong>（<strong><code>$PGDATA</code></strong> 与部署）。</p>
<hr>
<h2 id="一、表数据默认怎么存：Heap（堆表）"><a href="#一、表数据默认怎么存：Heap（堆表）" class="headerlink" title="一、表数据默认怎么存：Heap（堆表）"></a>一、表数据默认怎么存：Heap（堆表）</h2><ul>
<li>普通 <strong><code>CREATE TABLE</code></strong> 默认即 <strong>heap</strong> 表：行以<strong>堆页</strong>存放，通过 <strong>tid（块号 + 行号）</strong> 定位。  </li>
<li><strong>MVCC</strong> 在元组头维护版本信息（<strong><code>xmin</code> &#x2F; <code>xmax</code></strong> 等），更新与删除产生<strong>新版本&#x2F;死元组</strong>，依赖 <strong><code>VACUUM</code></strong> 回收（见 <strong><code>PostgreSQL索引与性能优化.md</code></strong>）。  </li>
<li>与 <strong>InnoDB 聚簇索引即整表</strong>不同：<strong>PostgreSQL</strong> 主键索引是<strong>二级索引</strong>，<strong>堆上</strong>存行，主键查找通常「<strong>索引 → 回表 tid</strong>」。</li>
</ul>
<hr>
<h2 id="二、表访问方法（Table-Access-Method）"><a href="#二、表访问方法（Table-Access-Method）" class="headerlink" title="二、表访问方法（Table Access Method）"></a>二、表访问方法（Table Access Method）</h2><p><strong>PostgreSQL 12+</strong> 起，表可指定 <strong><code>USING heap</code></strong>（默认）；理论上可挂<strong>自定义表访问方法</strong>（需扩展提供）。<br>生产常见仍是 <strong>heap</strong>；列存、混存等多在<strong>扩展或外部产品</strong>（如某些分析型方案）中实现，<strong>不等价于 MySQL 换一个 <code>ENGINE</code> 字符串</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 默认即 heap，一般无需写出</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> t (id <span class="type">int</span> <span class="keyword">PRIMARY KEY</span>, v text);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="三、与「引擎」易混的几个概念"><a href="#三、与「引擎」易混的几个概念" class="headerlink" title="三、与「引擎」易混的几个概念"></a>三、与「引擎」易混的几个概念</h2><table>
<thead>
<tr>
<th>概念</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>TABLESPACE</code></strong></td>
<td>表&#x2F;索引的<strong>文件放置位置</strong>（如 <strong><code>pg_default</code></strong>），不是另一种行存储引擎。</td>
</tr>
<tr>
<td><strong>TOAST</strong></td>
<td>大字段<strong>行外存储</strong>机制，对用户基本透明。</td>
</tr>
<tr>
<td><strong>Foreign Data Wrapper（FDW）</strong></td>
<td><strong>外部表</strong>：数据在远程；语法像本地表，<strong>语义与本地 heap 完全不同</strong>，不是换存储引擎。</td>
</tr>
<tr>
<td><strong>内置索引类型</strong></td>
<td><strong>B-Tree、Hash、GiST、GIN、BRIN</strong> 等是<strong>索引访问方法</strong>，不是替换 heap 的「表引擎」。</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、和-MySQL-InnoDB-的粗对照（选型沟通用）"><a href="#四、和-MySQL-InnoDB-的粗对照（选型沟通用）" class="headerlink" title="四、和 MySQL InnoDB 的粗对照（选型沟通用）"></a>四、和 MySQL InnoDB 的粗对照（选型沟通用）</h2><table>
<thead>
<tr>
<th>话题</th>
<th>MySQL（<strong><code>MySQL引擎.md</code></strong>）</th>
<th>PostgreSQL（本文）</th>
</tr>
</thead>
<tbody><tr>
<td>建表选「引擎」</td>
<td><strong><code>ENGINE=InnoDB</code></strong> 等</td>
<td><strong>无</strong>；默认 <strong>heap</strong> + 统一内核</td>
</tr>
<tr>
<td>行存与主键</td>
<td><strong>InnoDB</strong> 聚簇主键与数据同树</td>
<td><strong>堆行 + 主键索引</strong></td>
</tr>
<tr>
<td>并发读写的核心</td>
<td><strong>InnoDB MVCC + 锁</strong></td>
<td><strong>MVCC + 行锁</strong>（见 <strong><code>PostgreSQL并发与MVCC入门.md</code></strong>）</td>
</tr>
</tbody></table>
<hr>
<h2 id="五、实务建议"><a href="#五、实务建议" class="headerlink" title="五、实务建议"></a>五、实务建议</h2><ol>
<li>学习 <strong>PostgreSQL</strong> 时少问「用哪个引擎」，多问<strong>索引类型、执行计划、<code>VACUUM</code>、隔离级别与锁</strong>。  </li>
<li>需要列存、分片、多副本高可用时，在<strong>架构层</strong>选专用扩展或产品（如 <strong>Citus</strong>、云厂商数仓、读写分离），而不是在 <strong><code>CREATE TABLE</code></strong> 里找一个 <strong><code>ENGINE</code></strong> 开关。  </li>
<li>版本升级（<strong><code>14 → 16 → 17</code></strong>）会带执行器与优化器演进，「升级引擎」口语上常指<strong>升级主版本</strong>，与 <strong>MySQL</strong> 的 <strong>ENGINE</strong> 不是同一概念。</li>
</ol>
<hr>
<h2 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h2><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>PostgreSQL.md</code></strong></td>
<td><strong>DDL</strong>、<strong><code>SCHEMA</code></strong></td>
</tr>
<tr>
<td><strong><code>PostgreSQL高级查询.md</code></strong></td>
<td>复杂 <strong>SQL</strong></td>
</tr>
<tr>
<td><strong><code>PostgreSQL索引与性能优化.md</code></strong></td>
<td><strong><code>EXPLAIN</code>、备份与 WAL 概念</strong></td>
</tr>
<tr>
<td><strong><code>PostgreSQL-Docker.md</code></strong></td>
<td>镜像、卷、<strong><code>POSTGRES_*</code></strong></td>
</tr>
</tbody></table>
<p>以上描述以官方文档与当前大版本行为为准；内核细节以源码与 <strong>Wiki</strong> 为最终解释。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>PostgreSQL</tag>
        <tag>MVCC</tag>
        <tag>存储</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL 高级查询示例</title>
    <url>/2026/05/03/PostgreSQL/PostgreSQL%E9%AB%98%E7%BA%A7%E6%9F%A5%E8%AF%A2/</url>
    <content><![CDATA[<p>面向 <strong><code>SELECT</code></strong> 与常用分析写法；示例可在任意库执行（下文用 <strong><code>pg_demo</code></strong> 模式对象，<strong><code>DROP … IF EXISTS</code></strong> 便于重复跑）。</p>
<p>姊妹文：<strong><code>PostgreSQL.md</code></strong>（<strong>DDL</strong>）、<strong><code>PostgreSQL索引与性能优化.md</code></strong>（<strong><code>EXPLAIN</code></strong>）、<strong><code>PostgreSQL并发与MVCC入门.md</code></strong>（锁与快照）。</p>
<hr>
<h2 id="示例数据（一键建表）"><a href="#示例数据（一键建表）" class="headerlink" title="示例数据（一键建表）"></a>示例数据（一键建表）</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> orders, emp, dept CASCADE;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> dept (</span><br><span class="line">    id <span class="type">integer</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> emp (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    name <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    dept_id <span class="type">integer</span> <span class="keyword">REFERENCES</span> dept (id),</span><br><span class="line">    salary <span class="type">numeric</span>(<span class="number">10</span>,<span class="number">2</span>),</span><br><span class="line">    hire_date <span class="type">date</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> orders (</span><br><span class="line">    id <span class="type">bigint</span> <span class="keyword">PRIMARY KEY</span> GENERATED ALWAYS <span class="keyword">AS</span> <span class="keyword">IDENTITY</span>,</span><br><span class="line">    emp_id <span class="type">bigint</span> <span class="keyword">NOT NULL</span> <span class="keyword">REFERENCES</span> emp (id),</span><br><span class="line">    amount <span class="type">numeric</span>(<span class="number">10</span>,<span class="number">2</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    order_date <span class="type">date</span> <span class="keyword">NOT NULL</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> dept (id, name) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="string">&#x27;研发&#x27;</span>), (<span class="number">2</span>, <span class="string">&#x27;市场&#x27;</span>), (<span class="number">3</span>, <span class="string">&#x27;财务&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> emp (name, dept_id, salary, hire_date) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="string">&#x27;张三&#x27;</span>, <span class="number">1</span>, <span class="number">15000</span>, <span class="string">&#x27;2020-03-01&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;李四&#x27;</span>, <span class="number">1</span>, <span class="number">18000</span>, <span class="string">&#x27;2019-07-15&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;王五&#x27;</span>, <span class="number">2</span>, <span class="number">12000</span>, <span class="string">&#x27;2021-01-10&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;赵六&#x27;</span>, <span class="number">2</span>, <span class="keyword">NULL</span>, <span class="string">&#x27;2022-05-20&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;钱七&#x27;</span>, <span class="keyword">NULL</span>, <span class="number">9000</span>, <span class="string">&#x27;2023-11-01&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT INTO</span> orders (emp_id, amount, order_date) <span class="keyword">VALUES</span></span><br><span class="line">((<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span> LIMIT <span class="number">1</span>), <span class="number">100.00</span>, <span class="string">&#x27;2024-06-01&#x27;</span>),</span><br><span class="line">((<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span> LIMIT <span class="number">1</span>), <span class="number">200.50</span>, <span class="string">&#x27;2024-06-10&#x27;</span>),</span><br><span class="line">((<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;李四&#x27;</span> LIMIT <span class="number">1</span>), <span class="number">300.00</span>, <span class="string">&#x27;2024-06-12&#x27;</span>),</span><br><span class="line">((<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;王五&#x27;</span> LIMIT <span class="number">1</span>), <span class="number">50.00</span>, <span class="string">&#x27;2024-06-15&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>示例基表</strong>（<strong><code>emp.id</code></strong>、<strong><code>orders.id</code></strong> 为自增，你机器上具体数字可能与下表不同；<strong>下列「示例结果」按「姓名 &#x2F; 部门名」语义推算</strong>，与 <strong><code>EXPLAIN</code></strong> 对照时以你库中 <strong><code>id</code></strong> 为准）。</p>
<table>
<thead>
<tr>
<th>表</th>
<th>要点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>dept</strong></td>
<td><strong>3</strong> 行固定</td>
</tr>
<tr>
<td><strong>emp</strong></td>
<td><strong>5</strong> 行；<strong>钱七</strong> 无 <strong><code>dept_id</code></strong>；<strong>赵六</strong> 无 <strong><code>salary</code></strong></td>
</tr>
<tr>
<td><strong>orders</strong></td>
<td><strong>张三</strong> 两行、<strong>李四</strong> 一行、<strong>王五</strong> 一行</td>
</tr>
</tbody></table>
<hr>
<h2 id="一、多表连接-JOIN"><a href="#一、多表连接-JOIN" class="headerlink" title="一、多表连接 JOIN"></a>一、多表连接 JOIN</h2><h3 id="1-INNER-JOIN"><a href="#1-INNER-JOIN" class="headerlink" title="1. INNER JOIN"></a>1. INNER JOIN</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.id, e.name, d.name <span class="keyword">AS</span> dept_name, e.salary</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：</p>
<table>
<thead>
<tr>
<th>id（示意）</th>
<th>name</th>
<th>dept_name</th>
<th>salary</th>
</tr>
</thead>
<tbody><tr>
<td>…</td>
<td>张三</td>
<td>研发</td>
<td>15000</td>
</tr>
<tr>
<td>…</td>
<td>李四</td>
<td>研发</td>
<td>18000</td>
</tr>
<tr>
<td>…</td>
<td>王五</td>
<td>市场</td>
<td>12000</td>
</tr>
<tr>
<td>…</td>
<td>赵六</td>
<td>市场</td>
<td>NULL</td>
</tr>
</tbody></table>
<h3 id="2-LEFT-JOIN"><a href="#2-LEFT-JOIN" class="headerlink" title="2. LEFT JOIN"></a>2. LEFT JOIN</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name, d.name <span class="keyword">AS</span> dept_name</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：多一行 <strong>钱七 &#x2F; NULL</strong>。</p>
<h3 id="3-多表-聚合"><a href="#3-多表-聚合" class="headerlink" title="3. 多表 + 聚合"></a>3. 多表 + 聚合</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name,</span><br><span class="line">       d.name <span class="keyword">AS</span> dept_name,</span><br><span class="line">       <span class="built_in">COUNT</span>(o.id) <span class="keyword">AS</span> order_cnt,</span><br><span class="line">       <span class="built_in">COALESCE</span>(<span class="built_in">SUM</span>(o.amount), <span class="number">0</span>) <span class="keyword">AS</span> total_amount</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> orders o <span class="keyword">ON</span> o.emp_id <span class="operator">=</span> e.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> e.id, e.name, d.name;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（与 <strong>MySQL</strong> 版语义一致）：张三 <strong>2</strong> 笔合计 <strong>300.50</strong>；李四 <strong>300</strong>；王五 <strong>50</strong>；赵六、钱七 <strong>0</strong>。</p>
<hr>
<h2 id="二、子查询与-EXISTS"><a href="#二、子查询与-EXISTS" class="headerlink" title="二、子查询与 EXISTS"></a>二、子查询与 EXISTS</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name <span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">WHERE</span> dept_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name <span class="keyword">IN</span> (<span class="string">&#x27;研发&#x27;</span>, <span class="string">&#x27;市场&#x27;</span>));</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e.name</span><br><span class="line"><span class="keyword">FROM</span> emp e</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">EXISTS</span> (<span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> orders o <span class="keyword">WHERE</span> o.emp_id <span class="operator">=</span> e.id);</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：<strong><code>IN</code></strong> 列出 <strong>张三、李四、王五、赵六<code>**；**</code>EXISTS&#96;</strong> 仅 <strong>张三、李四、王五</strong>。</p>
<hr>
<h2 id="三、UNION-ALL"><a href="#三、UNION-ALL" class="headerlink" title="三、UNION ALL"></a>三、UNION ALL</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, <span class="string">&#x27;emp&#x27;</span>::text <span class="keyword">AS</span> src <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> <span class="number">1</span></span><br><span class="line"><span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line"><span class="keyword">SELECT</span> name, <span class="string">&#x27;dept&#x27;</span> <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：<strong>张三、李四、研发</strong> 三行。</p>
<hr>
<h2 id="四、公共表表达式-WITH（CTE）"><a href="#四、公共表表达式-WITH（CTE）" class="headerlink" title="四、公共表表达式 WITH（CTE）"></a>四、公共表表达式 WITH（CTE）</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WITH</span> dept_stat <span class="keyword">AS</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> dept_id,</span><br><span class="line">           <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> emp_cnt,</span><br><span class="line">           <span class="built_in">AVG</span>(salary) <span class="keyword">AS</span> avg_sal</span><br><span class="line">    <span class="keyword">FROM</span> emp</span><br><span class="line">    <span class="keyword">WHERE</span> salary <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> dept_id</span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> d.name, ds.emp_cnt, ROUND(ds.avg_sal::<span class="type">numeric</span>, <span class="number">2</span>) <span class="keyword">AS</span> avg_salary</span><br><span class="line"><span class="keyword">FROM</span> dept_stat ds</span><br><span class="line"><span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> d.id <span class="operator">=</span> ds.dept_id;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：<strong>研发 2 &#x2F; 16500</strong>；<strong>市场 1 &#x2F; 12000</strong>（<strong><code>dept_id</code> 为 NULL</strong> 的分组不与 <strong><code>dept</code></strong> 连接）。</p>
<hr>
<h2 id="五、窗口函数"><a href="#五、窗口函数" class="headerlink" title="五、窗口函数"></a>五、窗口函数</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, name, dept_id, salary,</span><br><span class="line">       <span class="built_in">row_number</span>() <span class="keyword">OVER</span> (<span class="keyword">PARTITION</span> <span class="keyword">BY</span> dept_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> salary <span class="keyword">DESC</span> <span class="keyword">NULLS LAST</span>) <span class="keyword">AS</span> rn_in_dept</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">WHERE</span> dept_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span>;</span><br></pre></td></tr></table></figure>

<p><strong><code>running_sum</code></strong>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> emp_id, order_date, amount,</span><br><span class="line">       <span class="built_in">SUM</span>(amount) <span class="keyword">OVER</span> (</span><br><span class="line">           <span class="keyword">PARTITION</span> <span class="keyword">BY</span> emp_id <span class="keyword">ORDER</span> <span class="keyword">BY</span> order_date</span><br><span class="line">           <span class="keyword">ROWS</span> <span class="keyword">BETWEEN</span> UNBOUNDED PRECEDING <span class="keyword">AND</span> <span class="keyword">CURRENT</span> <span class="type">ROW</span></span><br><span class="line">       ) <span class="keyword">AS</span> running_sum</span><br><span class="line"><span class="keyword">FROM</span> orders;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="六、DISTINCT-ON（PostgreSQL-特色）"><a href="#六、DISTINCT-ON（PostgreSQL-特色）" class="headerlink" title="六、DISTINCT ON（PostgreSQL 特色）"></a>六、DISTINCT ON（PostgreSQL 特色）</h2><p>每个 <strong><code>(dept_id)</code></strong> 分区取 <strong><code>ORDER BY</code></strong> 的第一行（常用「每组最新一条」）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> <span class="keyword">ON</span> (dept_id) dept_id, name, hire_date</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">WHERE</span> dept_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> dept_id, hire_date <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>ORDER BY dept_id, hire_date DESC</code></strong>：每个 <strong><code>dept_id</code></strong> 取入职日期<strong>最新</strong>的一行）：</p>
<table>
<thead>
<tr>
<th>dept_id</th>
<th>name</th>
<th>hire_date</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>张三</td>
<td>2020-03-01</td>
</tr>
<tr>
<td>2</td>
<td>赵六</td>
<td>2022-05-20</td>
</tr>
</tbody></table>
<hr>
<h2 id="七、聚合-FILTER（PostgreSQL）"><a href="#七、聚合-FILTER（PostgreSQL）" class="headerlink" title="七、聚合 FILTER（PostgreSQL）"></a>七、聚合 FILTER（PostgreSQL）</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FILTER</span> (<span class="keyword">WHERE</span> dept_id <span class="operator">=</span> <span class="number">1</span>) <span class="keyword">AS</span> dept1_cnt,</span><br><span class="line">    <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FILTER</span> (<span class="keyword">WHERE</span> dept_id <span class="operator">=</span> <span class="number">2</span>) <span class="keyword">AS</span> dept2_cnt</span><br><span class="line"><span class="keyword">FROM</span> emp;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>：<strong>2 &#x2F; 2</strong>。</p>
<hr>
<h2 id="八、字符串聚合-string-agg"><a href="#八、字符串聚合-string-agg" class="headerlink" title="八、字符串聚合 string_agg"></a>八、字符串聚合 string_agg</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> d.name <span class="keyword">AS</span> dept_name,</span><br><span class="line">       string_agg(e.name, <span class="string">&#x27; | &#x27;</span> <span class="keyword">ORDER</span> <span class="keyword">BY</span> e.salary <span class="keyword">DESC</span> <span class="keyword">NULLS LAST</span>) <span class="keyword">AS</span> members</span><br><span class="line"><span class="keyword">FROM</span> dept d</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> emp e <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> d.id, d.name;</span><br></pre></td></tr></table></figure>

<p><strong>示例结果</strong>（<strong><code>ORDER BY salary DESC NULLS LAST</code></strong>；箭头表示 <strong><code>string_agg</code></strong> 用 <strong><code>&#39; | &#39;</code></strong> 拼接顺序）：<strong>研发</strong> → <strong>李四 → 张三</strong>；<strong>市场</strong> → <strong>王五 → 赵六</strong>；<strong>财务</strong> → <strong>NULL</strong>。</p>
<hr>
<h2 id="九、分页-LIMIT-OFFSET"><a href="#九、分页-LIMIT-OFFSET" class="headerlink" title="九、分页 LIMIT &#x2F; OFFSET"></a>九、分页 LIMIT &#x2F; OFFSET</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, name, hire_date</span><br><span class="line"><span class="keyword">FROM</span> emp</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> hire_date <span class="keyword">DESC</span> <span class="keyword">NULLS LAST</span>, id <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span> <span class="keyword">OFFSET</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>深 <strong><code>OFFSET</code></strong> 大表性能差，生产常用<strong>键集分页</strong>（<strong><code>WHERE (hire_date, id) &lt; ($1,$2)</code></strong> 一类）。</p>
<hr>
<h2 id="十、RETURNING（与-DML-搭配，查询向补充）"><a href="#十、RETURNING（与-DML-搭配，查询向补充）" class="headerlink" title="十、RETURNING（与 DML 搭配，查询向补充）"></a>十、RETURNING（与 DML 搭配，查询向补充）</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 示例：插入并返回生成主键（需与业务事务搭配）</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> dept (id, name) <span class="keyword">VALUES</span> (<span class="number">9</span>, <span class="string">&#x27;法务&#x27;</span>)</span><br><span class="line"><span class="keyword">ON</span> CONFLICT (id) DO NOTHING</span><br><span class="line">RETURNING id, name;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="十一、速查小结"><a href="#十一、速查小结" class="headerlink" title="十一、速查小结"></a>十一、速查小结</h2><table>
<thead>
<tr>
<th>场景</th>
<th>PostgreSQL 典型写法</th>
</tr>
</thead>
<tbody><tr>
<td>多表</td>
<td><strong><code>JOIN</code> &#x2F; <code>LEFT JOIN</code></strong></td>
</tr>
<tr>
<td>去重取每组一条</td>
<td><strong><code>DISTINCT ON (…) … ORDER BY</code></strong></td>
</tr>
<tr>
<td>条件计数</td>
<td><strong><code>COUNT(*) FILTER (WHERE …)</code></strong></td>
</tr>
<tr>
<td>拼串</td>
<td><strong><code>string_agg(expr, sep ORDER BY …)</code></strong></td>
</tr>
<tr>
<td>可读拆分</td>
<td><strong><code>WITH</code></strong></td>
</tr>
<tr>
<td>组内排序编号</td>
<td><strong><code>row_number() OVER (PARTITION BY …)</code></strong></td>
</tr>
</tbody></table>
<p>更多执行计划与索引：<strong><code>PostgreSQL索引与性能优化.md</code></strong>。</p>
]]></content>
      <categories>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>SQL</tag>
        <tag>高级查询</tag>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>DNS 详解：解析原理、记录类型与命令/代码示例</title>
    <url>/2026/05/01/Protocol/DNS/</url>
    <content><![CDATA[<p><strong>DNS</strong>（<strong>Domain Name System</strong>）是分布式数据库，把<strong>域名</strong>映射为 <a href="/2026/05/01/IP/">IP</a> 地址、邮件交换子域等 <strong>RR（Resource Record）</strong>；应用访问 <a href="/2026/05/01/HTTP/">HTTP</a> 前几乎总要先解析域名。下面从「<strong>分层与解析原理</strong> → <strong>报文与传输（UDP&#x2F;TCP）</strong> → <strong>常见记录类型</strong> → <strong>加密 DNS</strong> → <strong><code>dig</code> &#x2F; Python 示例</strong>」顺序写清楚。</p>
<hr>
<h1 id="一、分层与解析原理"><a href="#一、分层与解析原理" class="headerlink" title="一、分层与解析原理"></a>一、分层与解析原理</h1><h2 id="1-角色"><a href="#1-角色" class="headerlink" title="1. 角色"></a>1. 角色</h2><table>
<thead>
<tr>
<th>角色</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Stub resolver</strong></td>
<td>运行在终端或系统库内，把查询发给<strong>递归解析器</strong></td>
</tr>
<tr>
<td><strong>Recursive resolver</strong></td>
<td>代客户端迭代问根、<strong>TLD</strong>、<strong>权威</strong> 直到拿到答案并<strong>缓存 TTL</strong></td>
</tr>
<tr>
<td><strong>Authoritative server</strong></td>
<td>对某区（<strong>zone</strong>）拥有权威数据（<strong>NS</strong> 委派链终点）</td>
</tr>
</tbody></table>
<h2 id="2-递归解析过程（概念）"><a href="#2-递归解析过程（概念）" class="headerlink" title="2. 递归解析过程（概念）"></a>2. 递归解析过程（概念）</h2><p>查询 <strong><code>www.example.com.</code></strong>（末尾点表示根）时，递归器从<strong>根</strong>得到 <strong><code>.com</code></strong> 的 <strong>NS</strong>，再问 <strong><code>.com</code></strong> 得到 <strong><code>example.com</code></strong> 的 <strong>NS</strong>，最后问 <strong><code>example.com</code></strong> 权威得到 <strong><code>www</code></strong> 的 <strong>A &#x2F; AAAA</strong>。结果按 <strong>TTL</strong> 缓存，命中则直接返回。</p>
<hr>
<h1 id="二、报文与传输（原理）"><a href="#二、报文与传输（原理）" class="headerlink" title="二、报文与传输（原理）"></a>二、报文与传输（原理）</h1><ul>
<li><strong>传统 53 端口</strong>：多数查询用 <a href="/2026/05/01/UDP/">UDP</a>，报文小、往返少。  </li>
<li><strong>响应过大或区域传送等</strong>：回退 <a href="/2026/05/01/TCP/">TCP</a>（<strong>TCP&#x2F;53</strong>）。  </li>
<li><strong>DNS 消息结构</strong>（概念）：<strong>Header</strong>（<strong>ID</strong>、<strong>flags</strong>、各计数器）+ <strong>Question</strong> + <strong>Answer &#x2F; Authority &#x2F; Additional</strong>。</li>
</ul>
<p><strong>ID</strong> 用于匹配请求与响应；客户端应随机化 <strong>ID</strong> 与源端口以降低<strong>投毒</strong>风险（实现依赖系统解析器）。</p>
<hr>
<h1 id="三、常见记录类型（作用）"><a href="#三、常见记录类型（作用）" class="headerlink" title="三、常见记录类型（作用）"></a>三、常见记录类型（作用）</h1><table>
<thead>
<tr>
<th>类型</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>A</strong></td>
<td>主机名 → <strong>IPv4</strong></td>
</tr>
<tr>
<td><strong>AAAA</strong></td>
<td>主机名 → <strong>IPv6</strong></td>
</tr>
<tr>
<td><strong>CNAME</strong></td>
<td>别名到另一域名（继续解析目标名）</td>
</tr>
<tr>
<td><strong>MX</strong></td>
<td>邮件交换：优先级 + 目标主机</td>
</tr>
<tr>
<td><strong>NS</strong></td>
<td>某区的权威服务器</td>
</tr>
<tr>
<td><strong>TXT</strong></td>
<td>任意文本（<strong>SPF</strong>、<strong>DKIM</strong>、域名验证等）</td>
</tr>
<tr>
<td><strong>PTR</strong></td>
<td><strong>IP</strong> → 主机名（反向解析）</td>
</tr>
</tbody></table>
<hr>
<h1 id="四、DoT-DoH（加密与传输）"><a href="#四、DoT-DoH（加密与传输）" class="headerlink" title="四、DoT &#x2F; DoH（加密与传输）"></a>四、DoT &#x2F; DoH（加密与传输）</h1><ul>
<li><strong>DoT</strong>（<strong>DNS over TLS</strong>）：<strong>853&#x2F;tcp</strong>，整流加密。  </li>
<li><strong>DoH</strong>（<strong>DNS over HTTPS</strong>）：封装在 <a href="/2026/05/01/HTTPS/">HTTPS</a> 内，常走 <strong>443</strong>，与网页流量形态相近。</li>
</ul>
<p>二者可防<strong>链路窃听</strong>，但递归器仍能看到查询内容；信任模型与日志策略需单独评审。</p>
<hr>
<h1 id="五、命令示例：dig"><a href="#五、命令示例：dig" class="headerlink" title="五、命令示例：dig"></a>五、命令示例：<code>dig</code></h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dig +short example.com A</span><br><span class="line">dig +short example.com AAAA</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dig +trace example.com A</span><br></pre></td></tr></table></figure>

<p>查看 <strong>DNSSEC</strong>（若部署）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dig +dnssec example.com A</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、代码示例：Python-发-UDP-查询（教学向极简）"><a href="#六、代码示例：Python-发-UDP-查询（教学向极简）" class="headerlink" title="六、代码示例：Python 发 UDP 查询（教学向极简）"></a>六、代码示例：<strong>Python</strong> 发 <strong>UDP</strong> 查询（教学向极简）</h1><p>下列示例<strong>仅用于理解「自己拼 DNS 问包」的思路</strong>；生产请用 <strong><code>dnspython</code></strong> 或系统 <strong><code>getaddrinfo</code></strong>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> random</span><br><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">build_query_qname</span>(<span class="params">hostname: <span class="built_in">str</span></span>) -&gt; <span class="built_in">bytes</span>:</span><br><span class="line">    parts = hostname.rstrip(<span class="string">&quot;.&quot;</span>).split(<span class="string">&quot;.&quot;</span>)</span><br><span class="line">    qname = <span class="string">b&quot;&quot;</span>.join(<span class="built_in">bytes</span>([<span class="built_in">len</span>(p)]) + p.encode() <span class="keyword">for</span> p <span class="keyword">in</span> parts) + <span class="string">b&quot;\x00&quot;</span></span><br><span class="line">    tid = random.randint(<span class="number">0</span>, <span class="number">0xFFFF</span>)</span><br><span class="line">    flags = <span class="number">0x0100</span>  <span class="comment"># 标准查询，递归期望</span></span><br><span class="line">    header = tid.to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>) + flags.to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)</span><br><span class="line">    header += (<span class="number">1</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)  <span class="comment"># QDCOUNT</span></span><br><span class="line">    header += (<span class="number">0</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)  <span class="comment"># ANCOUNT</span></span><br><span class="line">    header += (<span class="number">0</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)  <span class="comment"># NSCOUNT</span></span><br><span class="line">    header += (<span class="number">0</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)  <span class="comment"># ARCOUNT</span></span><br><span class="line">    qtype = (<span class="number">1</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)   <span class="comment"># A</span></span><br><span class="line">    qclass = (<span class="number">1</span>).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>)  <span class="comment"># IN</span></span><br><span class="line">    <span class="keyword">return</span> header + qname + qtype + qclass</span><br><span class="line"></span><br><span class="line">sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)</span><br><span class="line">sock.settimeout(<span class="number">3</span>)</span><br><span class="line">msg = build_query_qname(<span class="string">&quot;example.com&quot;</span>)</span><br><span class="line">sock.sendto(msg, (<span class="string">&quot;8.8.8.8&quot;</span>, <span class="number">53</span>))</span><br><span class="line">resp, _ = sock.recvfrom(<span class="number">512</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;response length&quot;</span>, <span class="built_in">len</span>(resp))</span><br></pre></td></tr></table></figure>

<p>解析 <strong>Answer</strong> 需按 <strong>RFC 1035</strong> 继续偏移指针，教学上更推荐：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="built_in">print</span>(socket.getaddrinfo(<span class="string">&quot;example.com&quot;</span>, <span class="number">443</span>, <span class="built_in">type</span>=socket.SOCK_STREAM))</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td>分层委派 + 缓存 <strong>TTL</strong>；<strong>UDP&#x2F;TCP 53</strong></td>
</tr>
<tr>
<td>作用</td>
<td>名字 ↔ <strong>RR</strong>；支撑几乎所有 <strong>Web</strong> 访问</td>
</tr>
<tr>
<td>命令</td>
<td><strong><code>dig</code></strong>、<code>**+trace**</code></td>
</tr>
<tr>
<td>代码</td>
<td><strong><code>getaddrinfo</code></strong> 或 <strong><code>dnspython</code></strong></td>
</tr>
</tbody></table>
<p>生产关注：<strong>权威与递归分离</strong>、<strong>防缓存投毒</strong>、<strong>限速</strong>、<strong>DNSSEC</strong> 策略与 <strong>TLS</strong> 证书透明度。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>DNS</tag>
        <tag>网络安全</tag>
      </tags>
  </entry>
  <entry>
    <title>FTP 详解：双连接模型、主动/被动与 lftp 示例</title>
    <url>/2026/05/01/Protocol/FTP/</url>
    <content><![CDATA[<p><strong>FTP</strong>（<strong>File Transfer Protocol</strong>，<strong>RFC 959</strong>）使用<strong>两条独立的 TCP 连接</strong>：<strong>控制连接</strong>（默认 <strong>21</strong>）传输命令与应答，<strong>数据连接</strong>传输文件与目录列表。下面从「<strong>设计动机</strong> → <strong>控制与数据通道原理</strong> → <strong>PORT 与 PASV</strong> → <strong>明文风险</strong> → <strong><code>lftp</code> 命令示例</strong> → <strong>与 SFTP 取舍</strong>」顺序写清楚；安全替代见 <a href="/2026/05/01/SFTP/">SFTP</a>。</p>
<hr>
<h1 id="一、设计动机（历史作用）"><a href="#一、设计动机（历史作用）" class="headerlink" title="一、设计动机（历史作用）"></a>一、设计动机（历史作用）</h1><p>在 <strong>TCP&#x2F;IP</strong> 早期提供<strong>跨异构系统</strong>的文件列目录、上传、下载与断点续传（由扩展命令支持程度决定）；命令为<strong>可读文本</strong>（如 <strong><code>RETR</code></strong>、<strong><code>STOR</code></strong>、<strong><code>LIST</code></strong>），便于人机调试。</p>
<hr>
<h1 id="二、双连接模型（原理）"><a href="#二、双连接模型（原理）" class="headerlink" title="二、双连接模型（原理）"></a>二、双连接模型（原理）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端                                服务端</span><br><span class="line">  |  TCP 21 控制连接（命令/响应）        |</span><br><span class="line">  |&lt;==================================&gt;|</span><br><span class="line">  |                                    |</span><br><span class="line">  |  数据连接（文件内容 / LIST 输出）    |</span><br><span class="line">  |&lt;==================================&gt;|  端口依 PORT/PASV 而定</span><br></pre></td></tr></table></figure>

<p><strong>控制连接</strong>全程保持；每条数据传送动态建立<strong>数据连接</strong>。</p>
<hr>
<h1 id="三、主动-PORT-与被动-PASV"><a href="#三、主动-PORT-与被动-PASV" class="headerlink" title="三、主动 PORT 与被动 PASV"></a>三、主动 PORT 与被动 PASV</h1><h2 id="1-主动（PORT）"><a href="#1-主动（PORT）" class="headerlink" title="1. 主动（PORT）"></a>1. 主动（PORT）</h2><p>服务端从 <strong>20</strong> 端口向客户端在 <strong><code>PORT</code></strong> 命令中声明的 <strong>IP:端口</strong> 主动连出。<strong>客户端在 NAT 后</strong>时常失败（内网端口对服务端不可达）。</p>
<h2 id="2-被动（PASV）（主流）"><a href="#2-被动（PASV）（主流）" class="headerlink" title="2. 被动（PASV）（主流）"></a>2. 被动（PASV）（主流）</h2><p>服务端在应答中给出<strong>监听 IP 与端口</strong>，客户端主动连入。<strong>防火墙</strong>须在服务端放行<strong>被动端口范围</strong>；<strong>NAT</strong> 下常需配置<strong>对外通告地址</strong>（如 <strong><code>masquerade_address</code></strong>）。</p>
<hr>
<h1 id="四、明文与认证"><a href="#四、明文与认证" class="headerlink" title="四、明文与认证"></a>四、明文与认证</h1><p>传统 <strong>FTP</strong> 用户名、密码与数据多为<strong>明文</strong>；<strong>FTPES</strong> 在 <strong>TLS</strong> 上加密，但仍保留双连接复杂性。公网新系统优先 <a href="/2026/05/01/SFTP/">SFTP</a> 或对象存储 <strong>HTTPS</strong> 上传。</p>
<hr>
<h1 id="五、命令示例：lftp（若已安装）"><a href="#五、命令示例：lftp（若已安装）" class="headerlink" title="五、命令示例：lftp（若已安装）"></a>五、命令示例：<code>lftp</code>（若已安装）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lftp -u anonymous, ftp://speedtest.tele2.net</span><br></pre></td></tr></table></figure>

<p>交互内：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ls</span><br><span class="line">get 1KB.zip</span><br><span class="line">bye</span><br></pre></td></tr></table></figure>

<p>脚本化：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lftp -c <span class="string">&quot;open -u user,pass ftp://example.com; ls; get remote.bin -o local.bin&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、原始-FTP-控制序列（telnet-nc-教学向）"><a href="#六、原始-FTP-控制序列（telnet-nc-教学向）" class="headerlink" title="六、原始 FTP 控制序列（telnet&#x2F;nc 教学向）"></a>六、原始 <strong>FTP</strong> 控制序列（<code>telnet</code>&#x2F;<code>nc</code> 教学向）</h1><p><strong>仅本地实验</strong>，手动发 <strong><code>USER</code></strong> &#x2F; <strong><code>PASS</code></strong>（密码进历史，勿在生产使用）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;USER anonymous\r\nPASS guest@\r\nQUIT\r\n&#x27;</span> | nc -v ftp.example.com 21</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>控制 21</strong> + <strong>动态数据连接</strong></td>
</tr>
<tr>
<td>作用</td>
<td>历史最广泛的文件交换协议之一</td>
</tr>
<tr>
<td>工具</td>
<td><strong><code>lftp</code></strong>、<strong><code>ftp</code></strong>、<strong><code>nc</code></strong></td>
</tr>
<tr>
<td>安全</td>
<td>优先 <a href="/2026/05/01/SFTP/">SFTP</a> &#x2F; <strong>HTTPS</strong></td>
</tr>
</tbody></table>
<hr>
<h1 id="八、与-SFTP-对比"><a href="#八、与-SFTP-对比" class="headerlink" title="八、与 SFTP 对比"></a>八、与 <a href="/2026/05/01/SFTP/">SFTP</a> 对比</h1><table>
<thead>
<tr>
<th></th>
<th><strong>FTP</strong></th>
<th><strong>SFTP</strong></th>
</tr>
</thead>
<tbody><tr>
<td>连接数</td>
<td>双连接</td>
<td>单 <a href="/2026/05/01/SSH/">SSH</a> 连接</td>
</tr>
<tr>
<td>加密</td>
<td>默认无</td>
<td>强加密</td>
</tr>
<tr>
<td>防火墙</td>
<td><strong>PASV</strong> 端口范围</td>
<td>通常单端口 <strong>22</strong></td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
      </tags>
  </entry>
  <entry>
    <title>GRPC 详解：Protobuf、HTTP/2 与 grpcurl / Python 示例</title>
    <url>/2026/05/01/Protocol/GRPC/</url>
    <content><![CDATA[<p><strong>gRPC</strong> 是跨语言 <strong>RPC</strong> 框架：接口用 <strong>Protocol Buffers（protobuf）</strong> 描述，默认基于 <a href="/2026/05/01/HTTP/">HTTP&#x2F;2</a> 帧传输二进制消息，支持一元与多种流式调用。下面从「<strong>与泛型 <a href="/2026/05/01/RPC/">RPC</a> 的关系</strong> → <strong><code>.proto</code> 与代码生成原理</strong> → <strong>传输与安全</strong> → <strong><code>grpcurl</code> 无代码调用</strong> → <strong>Python 最小客户端思路</strong>」顺序写清楚；磁盘文件名为 <strong><code>GRPC.md</code></strong>，正文统一使用官方拼写 <strong>gRPC</strong>。</p>
<hr>
<h1 id="一、在协议栈中的位置（作用）"><a href="#一、在协议栈中的位置（作用）" class="headerlink" title="一、在协议栈中的位置（作用）"></a>一、在协议栈中的位置（作用）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[ 应用：gRPC 生成的 Stub / Server 接口 ]</span><br><span class="line">            ↓</span><br><span class="line">[ gRPC：HTTP/2 帧 + protobuf 消息体 ]</span><br><span class="line">            ↓</span><br><span class="line">[ TLS（生产推荐）]</span><br><span class="line">            ↓</span><br><span class="line">[ TCP → IP ]</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、-proto-与代码生成（原理）"><a href="#二、-proto-与代码生成（原理）" class="headerlink" title="二、.proto 与代码生成（原理）"></a>二、<code>.proto</code> 与代码生成（原理）</h1><p><strong><code>helloworld.proto</code></strong>（节选）：</p>
<figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">&quot;proto3&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">service </span><span class="title class_">Greeter</span> &#123;</span><br><span class="line">  <span class="function"><span class="keyword">rpc</span> SayHello (HelloRequest) <span class="keyword">returns</span> (HelloReply)</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">message </span><span class="title class_">HelloRequest</span> &#123; <span class="type">string</span> name = <span class="number">1</span>; &#125;</span><br><span class="line"><span class="keyword">message </span><span class="title class_">HelloReply</span> &#123; <span class="type">string</span> message = <span class="number">1</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>使用 <strong><code>protoc</code></strong> 与 <strong>Go&#x2F;Java&#x2F;Python</strong> 等插件生成 <strong>stub</strong> 与 <strong>service</strong> 骨架；<strong><code>protoc</code></strong> 将 <strong><code>.proto</code></strong> 编译为各语言类型与序列化&#x2F;反序列化代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install grpcio grpcio-tools</span><br><span class="line">python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. helloworld.proto</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="三、四种调用类型（作用）"><a href="#三、四种调用类型（作用）" class="headerlink" title="三、四种调用类型（作用）"></a>三、四种调用类型（作用）</h1><table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Unary</strong></td>
<td>一问一答</td>
</tr>
<tr>
<td><strong>Server streaming</strong></td>
<td>一个请求，多个响应消息</td>
</tr>
<tr>
<td><strong>Client streaming</strong></td>
<td>多个请求消息，一个响应</td>
</tr>
<tr>
<td><strong>Bidirectional streaming</strong></td>
<td>双向流</td>
</tr>
</tbody></table>
<hr>
<h1 id="四、安全：TLS-与元数据"><a href="#四、安全：TLS-与元数据" class="headerlink" title="四、安全：TLS 与元数据"></a>四、安全：TLS 与元数据</h1><p>生产应对外 <strong><code>grpcs://</code></strong> 或等价 <strong>TLS</strong> 配置；<strong>Metadata</strong> 类似 <a href="/2026/05/01/HTTP/">HTTP</a> 首部，用于传递 <strong>Token</strong>、<strong>trace id</strong> 等（注意大小写规则以文档为准）。</p>
<hr>
<h1 id="五、grpcurl-示例（需单独安装）"><a href="#五、grpcurl-示例（需单独安装）" class="headerlink" title="五、grpcurl 示例（需单独安装）"></a>五、<code>grpcurl</code> 示例（需单独安装）</h1><p>列出服务（反射开启时）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grpcurl -plaintext localhost:50051 list</span><br></pre></td></tr></table></figure>

<p><strong>Unary</strong> 调用（示例服务名与方法名按你的 <strong>proto</strong> 替换）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grpcurl -plaintext -d <span class="string">&#x27;&#123;&quot;name&quot;:&quot;world&quot;&#125;&#x27;</span> localhost:50051 demo.Greeter/SayHello</span><br></pre></td></tr></table></figure>

<p><strong><code>-plaintext</code></strong> 表示不使用 <strong>TLS</strong>，仅本地调试。</p>
<hr>
<h1 id="六、Python-调用骨架（protoc-生成-helloworld-pb2-py-之后）"><a href="#六、Python-调用骨架（protoc-生成-helloworld-pb2-py-之后）" class="headerlink" title="六、Python 调用骨架（protoc 生成 helloworld_pb2*.py 之后）"></a>六、Python 调用骨架（<strong><code>protoc</code> 生成 <code>helloworld_pb2*.py</code> 之后</strong>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install grpcio grpcio-tools</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> grpc</span><br><span class="line"><span class="keyword">import</span> helloworld_pb2 <span class="keyword">as</span> pb</span><br><span class="line"><span class="keyword">import</span> helloworld_pb2_grpc <span class="keyword">as</span> pbg</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> grpc.insecure_channel(<span class="string">&quot;127.0.0.1:50051&quot;</span>) <span class="keyword">as</span> ch:</span><br><span class="line">    stub = pbg.GreeterStub(ch)</span><br><span class="line">    resp = stub.SayHello(pb.HelloRequest(name=<span class="string">&quot;world&quot;</span>))</span><br><span class="line">    <span class="built_in">print</span>(resp.message)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、与-REST-风格-HTTP-JSON-对比"><a href="#七、与-REST-风格-HTTP-JSON-对比" class="headerlink" title="七、与 REST 风格 HTTP+JSON 对比"></a>七、与 <a href="/2026/05/01/HTTP/">REST</a> 风格 <strong>HTTP+JSON</strong> 对比</h1><table>
<thead>
<tr>
<th></th>
<th><strong>gRPC</strong></th>
<th><strong>REST+JSON</strong></th>
</tr>
</thead>
<tbody><tr>
<td>契约</td>
<td><strong>protobuf</strong> 强类型</td>
<td><strong>OpenAPI</strong> 等</td>
</tr>
<tr>
<td>浏览器</td>
<td>受限，常需 <strong>gRPC-Web</strong> 或网关</td>
<td>原生友好</td>
</tr>
<tr>
<td>流式</td>
<td>一等公民</td>
<td><strong>SSE&#x2F;WebSocket</strong> 等组合</td>
</tr>
</tbody></table>
<hr>
<h1 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>HTTP&#x2F;2</strong> 承载 <strong>protobuf</strong> <strong>RPC</strong></td>
</tr>
<tr>
<td>作用</td>
<td>高性能多语言微服务内部调用</td>
</tr>
<tr>
<td>工具</td>
<td><strong><code>protoc</code></strong>、<strong><code>grpcurl</code></strong></td>
</tr>
<tr>
<td>范式</td>
<td><a href="/2026/05/01/RPC/">RPC</a> 的一种实现；同类见 <a href="/2026/05/01/Dubbo/">Dubbo</a></td>
</tr>
</tbody></table>
<p>版本兼容与 <strong>Breaking Change</strong> 管理以 <strong>protobuf</strong> 字段编号与 <strong>API 版本</strong>策略为准。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>GRPC</tag>
        <tag>HTTP/2</tag>
        <tag>Protobuf</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 详解：报文、方法、版本与 curl/nc 示例</title>
    <url>/2026/05/01/Protocol/HTTP/</url>
    <content><![CDATA[<p><strong>HTTP</strong>（<strong>Hypertext Transfer Protocol</strong>）是 <strong>Web</strong> 的应用层协议：基于<strong>请求 &#x2F; 响应</strong>模型，在语义层使用<strong>方法、URI、首部与可选正文</strong>表达操作。下面从「<strong>无状态与资源模型</strong> → <strong>报文结构原理</strong> → <strong>方法与幂等</strong> → <strong>状态码</strong> → <strong>HTTP&#x2F;1.1 与 HTTP&#x2F;2</strong> → <strong>curl 与原始报文示例</strong>」顺序写清楚；传输层默认依赖 <a href="/2026/05/01/TCP/">TCP</a>，安全传输见 <a href="/2026/05/01/HTTPS/">HTTPS</a>，全双工见 <a href="/2026/05/01/WebSocket/">WebSocket</a>，<strong>HTTP&#x2F;3</strong> 见 <a href="/2026/05/01/QUIC/">QUIC</a>。</p>
<hr>
<h1 id="一、无状态与资源模型（原理）"><a href="#一、无状态与资源模型（原理）" class="headerlink" title="一、无状态与资源模型（原理）"></a>一、无状态与资源模型（原理）</h1><p><strong>HTTP</strong> 本身<strong>无状态</strong>：服务器默认不记住「上一次请求」；会话常靠 <strong>Cookie</strong>、<strong>Token</strong>、服务端会话存储实现。<strong>REST</strong> 风格强调用 <strong>URI</strong> 表示<strong>资源</strong>，用 <strong>HTTP 方法</strong>表达操作（与 <a href="/2026/05/01/RPC/">RPC</a> 风格对照）。</p>
<hr>
<h1 id="二、报文结构（作用）"><a href="#二、报文结构（作用）" class="headerlink" title="二、报文结构（作用）"></a>二、报文结构（作用）</h1><h2 id="1-请求"><a href="#1-请求" class="headerlink" title="1. 请求"></a>1. 请求</h2><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GET</span> <span class="string">/path</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Host</span><span class="punctuation">: </span>example.com</span><br><span class="line"><span class="attribute">User-Agent</span><span class="punctuation">: </span>demo</span><br><span class="line"><span class="attribute">Accept</span><span class="punctuation">: </span>*/*</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li><strong>请求行</strong>：方法、<strong>请求目标</strong>（路径或绝对 <strong>URI</strong>）、版本。  </li>
<li><strong>首部</strong>：<strong><code>Host</code></strong> 在 <strong>HTTP&#x2F;1.1</strong> 中语义必填；<strong><code>Content-Length</code></strong> &#x2F; <strong><code>Transfer-Encoding: chunked</code></strong> 控制正文边界。</li>
</ul>
<h2 id="2-响应"><a href="#2-响应" class="headerlink" title="2. 响应"></a>2. 响应</h2><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">200</span> OK</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>text/plain; charset=utf-8</span><br><span class="line"><span class="attribute">Content-Length</span><span class="punctuation">: </span>5</span><br><span class="line"></span><br><span class="line"><span class="language-ebnf"><span class="attribute">hello</span></span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="三、常用方法与幂等（原理）"><a href="#三、常用方法与幂等（原理）" class="headerlink" title="三、常用方法与幂等（原理）"></a>三、常用方法与幂等（原理）</h1><table>
<thead>
<tr>
<th>方法</th>
<th>典型语义</th>
<th>幂等</th>
</tr>
</thead>
<tbody><tr>
<td><strong>GET</strong></td>
<td>读取资源</td>
<td>是</td>
</tr>
<tr>
<td><strong>POST</strong></td>
<td>创建 &#x2F; 提交</td>
<td>否</td>
</tr>
<tr>
<td><strong>PUT</strong></td>
<td>整体替换</td>
<td>是</td>
</tr>
<tr>
<td><strong>PATCH</strong></td>
<td>部分更新</td>
<td>视实现</td>
</tr>
<tr>
<td><strong>DELETE</strong></td>
<td>删除</td>
<td>是</td>
</tr>
<tr>
<td><strong>HEAD</strong></td>
<td>同 <strong>GET</strong> 无正文</td>
<td>是</td>
</tr>
<tr>
<td><strong>OPTIONS</strong></td>
<td>探测支持的方法</td>
<td>是</td>
</tr>
</tbody></table>
<p><strong>幂等</strong>：多次执行相同请求<strong>不应</strong>带来额外副作用（仍可能每次返回相同结果）。</p>
<hr>
<h1 id="四、状态码（分类）"><a href="#四、状态码（分类）" class="headerlink" title="四、状态码（分类）"></a>四、状态码（分类）</h1><table>
<thead>
<tr>
<th>范围</th>
<th>含义</th>
<th>例</th>
</tr>
</thead>
<tbody><tr>
<td><strong>2xx</strong></td>
<td>成功</td>
<td><strong>200</strong>、<strong>201</strong>、<strong>204</strong></td>
</tr>
<tr>
<td><strong>3xx</strong></td>
<td>重定向</td>
<td><strong>301</strong>、<strong>302</strong>、<strong>304</strong></td>
</tr>
<tr>
<td><strong>4xx</strong></td>
<td>客户端错误</td>
<td><strong>400</strong>、<strong>401</strong>、<strong>403</strong>、<strong>404</strong></td>
</tr>
<tr>
<td><strong>5xx</strong></td>
<td>服务器错误</td>
<td><strong>500</strong>、<strong>502</strong>、<strong>503</strong></td>
</tr>
</tbody></table>
<hr>
<h1 id="五、HTTP-1-1-与-HTTP-2（演进）"><a href="#五、HTTP-1-1-与-HTTP-2（演进）" class="headerlink" title="五、HTTP&#x2F;1.1 与 HTTP&#x2F;2（演进）"></a>五、HTTP&#x2F;1.1 与 HTTP&#x2F;2（演进）</h1><ul>
<li><strong>HTTP&#x2F;1.1</strong>（<strong>RFC 9112</strong>）：文本形态；<strong>Keep-Alive</strong> 复用连接；<strong>队头阻塞</strong>限制并发效率。  </li>
<li><strong>HTTP&#x2F;2</strong>（<strong>RFC 9113</strong>）：二进制<strong>帧</strong>；<strong>单连接多路复用</strong>；<strong>HPACK</strong> 压缩首部；浏览器场景几乎总与 <strong>TLS</strong> 同用。  </li>
<li><strong>HTTP&#x2F;3</strong>（<strong>RFC 9114</strong>）：基于 <a href="/2026/05/01/QUIC/">QUIC</a>（<a href="/2026/05/01/UDP/">UDP</a>）。</li>
</ul>
<hr>
<h1 id="六、示例：curl-观察报文"><a href="#六、示例：curl-观察报文" class="headerlink" title="六、示例：curl 观察报文"></a>六、示例：<code>curl</code> 观察报文</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v --http1.1 https://example.com/ -o /dev/null</span><br></pre></td></tr></table></figure>

<p>查看 <strong>TLS</strong> 与 <strong>HTTP&#x2F;2</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v --http2 https://example.com/ -o /dev/null</span><br></pre></td></tr></table></figure>

<p>仅看响应头：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sI https://example.com/</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、示例：用-nc-发送最小-HTTP-1-1-请求（明文）"><a href="#七、示例：用-nc-发送最小-HTTP-1-1-请求（明文）" class="headerlink" title="七、示例：用 nc 发送最小 HTTP&#x2F;1.1 请求（明文）"></a>七、示例：用 <strong>nc</strong> 发送最小 <strong>HTTP&#x2F;1.1</strong> 请求（明文）</h1><p><strong>仅用于本地实验</strong>（对端须是明文 <strong>HTTP</strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;GET / HTTP/1.1\r\nHost: example.com\r\nConnection: close\r\n\r\n&#x27;</span> | nc example.com 80 | <span class="built_in">head</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="八、示例：Python-3-最小-HTTP-客户端（标准库）"><a href="#八、示例：Python-3-最小-HTTP-客户端（标准库）" class="headerlink" title="八、示例：Python 3 最小 HTTP 客户端（标准库）"></a>八、示例：<strong>Python 3</strong> 最小 <strong>HTTP</strong> 客户端（标准库）</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> urllib.request</span><br><span class="line"></span><br><span class="line">req = urllib.request.Request(<span class="string">&quot;https://example.com/&quot;</span>, headers=&#123;<span class="string">&quot;User-Agent&quot;</span>: <span class="string">&quot;demo&quot;</span>&#125;)</span><br><span class="line"><span class="keyword">with</span> urllib.request.urlopen(req, timeout=<span class="number">10</span>) <span class="keyword">as</span> resp:</span><br><span class="line">    <span class="built_in">print</span>(resp.status, resp.getheader(<span class="string">&quot;Content-Type&quot;</span>))</span><br><span class="line">    <span class="built_in">print</span>(resp.read(<span class="number">200</span>))</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="九、小结"><a href="#九、小结" class="headerlink" title="九、小结"></a>九、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>请求&#x2F;响应</strong>、<strong>无状态</strong>、<strong>URI + 方法</strong></td>
</tr>
<tr>
<td>作用</td>
<td><strong>Web</strong> 与 <strong>API</strong> 的基础语义层</td>
</tr>
<tr>
<td>工具</td>
<td><strong><code>curl -v</code></strong>、<strong><code>nc</code></strong>、<strong><code>urllib</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/HTTPS/">HTTPS</a> · <a href="/2026/05/01/WebSocket/">WebSocket</a> · <a href="/2026/05/01/QUIC/">QUIC</a></td>
</tr>
</tbody></table>
<p>缓存与条件请求（<strong><code>ETag</code></strong>、<strong><code>If-None-Match</code></strong>）见 <strong>RFC 9110</strong> 缓存章节。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP3 详解：报文、方法、版本与 curl/nc 示例</title>
    <url>/2026/05/01/Protocol/HTTP3/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTPS 详解：TLS 握手、证书与 openssl/curl 示例</title>
    <url>/2026/05/01/Protocol/HTTPS/</url>
    <content><![CDATA[<p><strong>HTTPS</strong> &#x3D; <a href="/2026/05/01/HTTP/">HTTP</a> + <strong>TLS</strong>（<strong>Transport Layer Security</strong>）：在 <strong>TCP</strong> 连接建立后先做 <strong>TLS</strong> 握手，再在加密记录层上传输 <strong>HTTP</strong> 语义。默认端口 <strong>443</strong>。下面从「<strong>作用与威胁模型</strong> → <strong>TLS 握手与密钥派生（原理）</strong> → <strong>证书链与主机名校验</strong> → <strong>ALPN 与 HTTP&#x2F;2、HTTP&#x2F;3</strong> → <strong>openssl &#x2F; curl 示例</strong>」顺序写清楚；<strong>HTTP&#x2F;3</strong> 传输见 <a href="/2026/05/01/QUIC/">QUIC</a>。</p>
<hr>
<h1 id="一、作用与威胁模型"><a href="#一、作用与威胁模型" class="headerlink" title="一、作用与威胁模型"></a>一、作用与威胁模型</h1><h2 id="1-TLS-提供什么"><a href="#1-TLS-提供什么" class="headerlink" title="1. TLS 提供什么"></a>1. <strong>TLS</strong> 提供什么</h2><ul>
<li><strong>机密性</strong>：对称加密载荷，防窃听。  </li>
<li><strong>完整性</strong>：<strong>MAC</strong> &#x2F; <strong>AEAD</strong> 防篡改。  </li>
<li><strong>身份认证</strong>：服务端（可选客户端）<strong>X.509</strong> 证书链。</li>
</ul>
<h2 id="2-不解决什么"><a href="#2-不解决什么" class="headerlink" title="2. 不解决什么"></a>2. 不解决什么</h2><p><strong>TLS</strong> 不能替代应用层鉴权与输入校验；<strong><code>https://</code></strong> 也不等于站点无 <strong>XSS&#x2F;CSRF</strong> 等业务漏洞。</p>
<hr>
<h1 id="二、TLS-1-2-1-3-握手（原理简述）"><a href="#二、TLS-1-2-1-3-握手（原理简述）" class="headerlink" title="二、TLS 1.2 &#x2F; 1.3 握手（原理简述）"></a>二、TLS 1.2 &#x2F; 1.3 握手（原理简述）</h1><h2 id="1-共同目标"><a href="#1-共同目标" class="headerlink" title="1. 共同目标"></a>1. 共同目标</h2><p>客户端与服务端协商出<strong>对称会话密钥</strong>，后续 <strong>HTTP</strong> 记录用对称算法加密以获得性能。</p>
<h2 id="2-TLS-1-3-特点（相对-1-2）"><a href="#2-TLS-1-3-特点（相对-1-2）" class="headerlink" title="2. TLS 1.3 特点（相对 1.2）"></a>2. <strong>TLS 1.3</strong> 特点（相对 1.2）</h2><ul>
<li>常 <strong>1-RTT</strong> 完成握手（<strong>0-RTT</strong> 可选，有<strong>重放</strong>风险，敏感操作应禁用或二次校验）。  </li>
<li>废弃一批不安全算法；<strong>Hello</strong> 中即携带密钥共享材料。</li>
</ul>
<hr>
<h1 id="三、证书链与主机名（作用）"><a href="#三、证书链与主机名（作用）" class="headerlink" title="三、证书链与主机名（作用）"></a>三、证书链与主机名（作用）</h1><p>浏览器&#x2F;客户端会校验：</p>
<ol>
<li>证书是否由受信任 <strong>CA</strong> 签发，链是否完整。  </li>
<li><strong>Subject Alternative Name（SAN）</strong> 或兼容字段是否覆盖当前访问主机名。  </li>
<li>有效期、吊销信息（<strong>OCSP Stapling</strong> 等）。</li>
</ol>
<p>自签名证书仅适合内网或开发，公网须使用可信 <strong>CA</strong> 或组织内 <strong>PKI</strong>。</p>
<hr>
<h1 id="四、ALPN"><a href="#四、ALPN" class="headerlink" title="四、ALPN"></a>四、ALPN</h1><p>在 <strong>TLS</strong> 握手中通过 <strong>ALPN</strong> 协商应用层协议：<strong><code>h2</code></strong> 表示后续走 <strong>HTTP&#x2F;2</strong> 帧；<strong><code>h3</code></strong> 与 <a href="/2026/05/01/QUIC/">QUIC</a> &#x2F; <strong>HTTP&#x2F;3</strong> 相关（<strong>QUIC</strong> 内含 <strong>TLS 1.3</strong> 集成握手）。</p>
<hr>
<h1 id="五、命令示例：openssl-s-client"><a href="#五、命令示例：openssl-s-client" class="headerlink" title="五、命令示例：openssl s_client"></a>五、命令示例：<code>openssl s_client</code></h1><p>查看证书链与协商结果：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_client -connect example.com:443 -servername example.com &lt;/dev/null 2&gt;/dev/null | openssl x509 -noout -dates -subject -issuer</span><br></pre></td></tr></table></figure>

<p>交互式观察握手（输入 <strong><code>Q</code></strong> 退出）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_client -connect example.com:443 -servername example.com</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、命令示例：curl-与-TLS-版本"><a href="#六、命令示例：curl-与-TLS-版本" class="headerlink" title="六、命令示例：curl 与 TLS 版本"></a>六、命令示例：<code>curl</code> 与 <strong>TLS</strong> 版本</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v --tlsv1.3 https://example.com/ -o /dev/null</span><br></pre></td></tr></table></figure>

<p>指定 <strong>cipher</strong>（调试用）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v https://example.com/ --tls-max 1.2 -o /dev/null</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、示例：Python-发起-HTTPS-并校验证书（默认校验）"><a href="#七、示例：Python-发起-HTTPS-并校验证书（默认校验）" class="headerlink" title="七、示例：Python 发起 HTTPS 并校验证书（默认校验）"></a>七、示例：<strong>Python</strong> 发起 <strong>HTTPS</strong> 并校验证书（默认校验）</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ssl</span><br><span class="line"><span class="keyword">import</span> urllib.request</span><br><span class="line"></span><br><span class="line">ctx = ssl.create_default_context()</span><br><span class="line">req = urllib.request.Request(<span class="string">&quot;https://example.com/&quot;</span>)</span><br><span class="line"><span class="keyword">with</span> urllib.request.urlopen(req, context=ctx, timeout=<span class="number">10</span>) <span class="keyword">as</span> r:</span><br><span class="line">    <span class="built_in">print</span>(r.status, r.getpeercert()[<span class="string">&quot;subject&quot;</span>])</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>TLS</strong> 握手 → 对称加密传输 <a href="/2026/05/01/HTTP/">HTTP</a></td>
</tr>
<tr>
<td>作用</td>
<td>机密性、完整性、服务端身份</td>
</tr>
<tr>
<td>工具</td>
<td><strong><code>openssl s_client</code></strong>、<strong><code>curl -v</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><strong>HSTS</strong>、<strong>OCSP</strong>、<a href="/2026/05/01/QUIC/">QUIC</a> <strong>HTTP&#x2F;3</strong></td>
</tr>
</tbody></table>
<p>生产部署关注：<strong>证书续期</strong>、<strong>链完整性</strong>、<strong>中间件 SNI</strong>、<strong>TLS 终止位置</strong>（网关 vs 应用）。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
  <entry>
    <title>IP 协议详解：原理、编址与排障示例</title>
    <url>/2026/05/01/Protocol/IP/</url>
    <content><![CDATA[<p><strong>IP</strong>（<strong>Internet Protocol</strong>，互联网协议）工作在 <strong>OSI</strong> 第三层（网络层），向上为 <a href="/2026/05/01/TCP/">TCP</a>、<a href="/2026/05/01/UDP/">UDP</a> 等传输层协议提供**主机到主机（host-to-host）**的分组投递服务。下面从「<strong>作用与设计目标</strong> → <strong>IPv4 报文与编址原理</strong> → <strong>转发与路由</strong> → <strong>IPv6 差异要点</strong> → <strong>与 DNS&#x2F;TCP 的关系</strong> → <strong>Linux 排障命令示例</strong>」顺序写清楚；命令在 <strong>Linux</strong> 终端演示为主，<strong>macOS</strong> 多数可类比。</p>
<hr>
<h1 id="一、作用与设计目标"><a href="#一、作用与设计目标" class="headerlink" title="一、作用与设计目标"></a>一、作用与设计目标</h1><h2 id="1-IP-解决什么问题"><a href="#1-IP-解决什么问题" class="headerlink" title="1. IP 解决什么问题"></a>1. IP 解决什么问题</h2><ul>
<li><strong>统一寻址</strong>：给互联网上每台参与通信的主机接口分配 <strong>IP 地址</strong>，使跨网段寻址成为可能。  </li>
<li><strong>无连接、尽力交付（best-effort）</strong>：<strong>IP</strong> 不保证分组一定到达、不保证顺序、不负责重传；<strong>可靠性</strong>由上层（如 <a href="/2026/05/01/TCP/">TCP</a>）或应用自行实现。  </li>
<li><strong>多路复用下层链路</strong>：无论是以太网、<strong>Wi‑Fi</strong> 还是隧道，都可承载 <strong>IP</strong> 分组。</li>
</ul>
<h2 id="2-与上下层的关系（简图）"><a href="#2-与上下层的关系（简图）" class="headerlink" title="2. 与上下层的关系（简图）"></a>2. 与上下层的关系（简图）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[ 应用层 HTTP / DNS 客户端 … ]</span><br><span class="line">        ↓</span><br><span class="line">[ 传输层 TCP / UDP ]  ← 端口号区分进程</span><br><span class="line">        ↓</span><br><span class="line">[ 网络层 IP ]         ← 本文：逻辑寻址与分组</span><br><span class="line">        ↓</span><br><span class="line">[ 链路层 以太网 … ]   ← MAC 帧、交换机</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、IPv4-报文与编址原理（概念）"><a href="#二、IPv4-报文与编址原理（概念）" class="headerlink" title="二、IPv4 报文与编址原理（概念）"></a>二、IPv4 报文与编址原理（概念）</h1><h2 id="1-IPv4-首部关键字段（理解排障）"><a href="#1-IPv4-首部关键字段（理解排障）" class="headerlink" title="1. IPv4 首部关键字段（理解排障）"></a>1. IPv4 首部关键字段（理解排障）</h2><table>
<thead>
<tr>
<th>字段（概念）</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>源 &#x2F; 目的地址</strong></td>
<td>各 <strong>32 bit</strong>，点分十进制如 <strong><code>192.168.1.10</code></strong></td>
</tr>
<tr>
<td><strong>TTL</strong></td>
<td>每经路由器减 <strong>1</strong>，到 <strong>0</strong> 丢弃，防路由环路</td>
</tr>
<tr>
<td><strong>协议号</strong></td>
<td>指示载荷是 <strong>TCP(6)</strong>、<strong>UDP(17)</strong>、<strong>ICMP(1)</strong> 等</td>
</tr>
<tr>
<td><strong>总长度 &#x2F; 分片相关</strong></td>
<td>超过链路 <strong>MTU</strong> 时可能触发 <strong>IP 分片</strong>（<strong>IPv6</strong> 主要在源端处理）</td>
</tr>
</tbody></table>
<h2 id="2-子网与-CIDR"><a href="#2-子网与-CIDR" class="headerlink" title="2. 子网与 CIDR"></a>2. 子网与 CIDR</h2><p><strong>IP 地址 &#x3D; 网络前缀 + 主机号</strong>。<strong><code>/24</code></strong> 表示前 <strong>24 bit</strong> 为网络部分，主机部分 <strong>8 bit</strong>，共 <strong>256</strong> 个地址（含网络地址与广播地址用法依场景而定）。同一二层广播域内主机须在<strong>同一子网</strong>才能「直连」互通；跨子网则须走<strong>默认网关</strong>（路由器）。</p>
<p><strong>RFC 1918 私有地址</strong>（内网常用）：</p>
<ul>
<li><strong><code>10.0.0.0/8</code></strong>、<strong><code>172.16.0.0/12</code></strong>、<strong><code>192.168.0.0/16</code></strong></li>
</ul>
<hr>
<h1 id="三、转发与路由（原理）"><a href="#三、转发与路由（原理）" class="headerlink" title="三、转发与路由（原理）"></a>三、转发与路由（原理）</h1><p>路由器根据<strong>路由表</strong>最长前缀匹配决定下一跳；主机则主要使用<strong>默认路由</strong>（<strong><code>0.0.0.0/0</code></strong>）把非本子网流量交给网关。<strong>NAT</strong> 可在私网与公网之间改写地址&#x2F;端口，但 <strong>NAT</strong> 不是传输层协议；端到端排障仍要区分 <strong>「IP 层是否可达」</strong> 与 <strong>「端口映射&#x2F;防火墙策略」</strong>。</p>
<hr>
<h1 id="四、IPv6-要点（与-IPv4-对照）"><a href="#四、IPv6-要点（与-IPv4-对照）" class="headerlink" title="四、IPv6 要点（与 IPv4 对照）"></a>四、IPv6 要点（与 IPv4 对照）</h1><ul>
<li><strong>128 bit</strong> 地址，冒号十六进制表示；存在<strong>链路本地</strong>、<strong>唯一本地</strong>、<strong>全局单播</strong> 等范围。  </li>
<li><strong>邻居发现（NDP）</strong> 承担类似 <strong>IPv4 ARP</strong> 的链路地址解析角色。  </li>
<li>双栈环境下 <a href="/2026/05/01/DNS/">DNS</a> 常同时返回 <strong>A</strong> 与 <strong>AAAA</strong>，应用与运维需同时考虑两条路径的防火墙与 <strong>MTU</strong>（<strong>IPv6</strong> 最小 <strong>MTU</strong> 要求为 <strong>1280</strong>）。</li>
</ul>
<hr>
<h1 id="五、与-TCP、UDP、DNS-的关系"><a href="#五、与-TCP、UDP、DNS-的关系" class="headerlink" title="五、与 TCP、UDP、DNS 的关系"></a>五、与 TCP、UDP、DNS 的关系</h1><table>
<thead>
<tr>
<th>上层协议</th>
<th>如何使用 <strong>IP</strong></th>
</tr>
</thead>
<tbody><tr>
<td><a href="/2026/05/01/TCP/">TCP</a></td>
<td>在 <strong>IP</strong> 分组载荷中放 <strong>TCP 段</strong>（含端口、序号等）</td>
</tr>
<tr>
<td><a href="/2026/05/01/UDP/">UDP</a></td>
<td>载荷为 <strong>UDP 数据报</strong></td>
</tr>
<tr>
<td><a href="/2026/05/01/DNS/">DNS</a></td>
<td>查询&#x2F;响应封装在 <strong>UDP</strong> 或 <strong>TCP</strong> 中，再封装进 <strong>IP</strong></td>
</tr>
</tbody></table>
<hr>
<h1 id="六、代码与命令示例"><a href="#六、代码与命令示例" class="headerlink" title="六、代码与命令示例"></a>六、代码与命令示例</h1><h2 id="1-查看本机-IP-与路由（Linux）"><a href="#1-查看本机-IP-与路由（Linux）" class="headerlink" title="1. 查看本机 IP 与路由（Linux）"></a>1. 查看本机 <strong>IP</strong> 与路由（Linux）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip -4 addr show</span><br><span class="line">ip -4 route show</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip -6 addr show</span><br><span class="line">ip -6 route show</span><br></pre></td></tr></table></figure>

<h2 id="2-连通性：-ping（ICMP-依赖-IP）"><a href="#2-连通性：-ping（ICMP-依赖-IP）" class="headerlink" title="2. 连通性： ping（ICMP 依赖 IP）"></a>2. 连通性： <strong>ping</strong>（<strong>ICMP</strong> 依赖 <strong>IP</strong>）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping -c 4 223.5.5.5</span><br><span class="line">ping -c 4 2400:3200::1</span><br></pre></td></tr></table></figure>

<p>若 <strong>ping</strong> 通 <strong>IP</strong> 但域名不通，优先查 <a href="/2026/05/01/DNS/">DNS</a> 与 <strong><code>/etc/resolv.conf</code></strong>。</p>
<h2 id="3-Python：解析域名到-A-AAAA（调用系统解析器）"><a href="#3-Python：解析域名到-A-AAAA（调用系统解析器）" class="headerlink" title="3. Python：解析域名到 A &#x2F; AAAA（调用系统解析器）"></a>3. <strong>Python</strong>：解析域名到 <strong>A &#x2F; AAAA</strong>（调用系统解析器）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> family, kind, proto, canon, sockaddr <span class="keyword">in</span> socket.getaddrinfo(</span><br><span class="line">    <span class="string">&quot;example.com&quot;</span>, <span class="number">443</span>, <span class="built_in">type</span>=socket.SOCK_STREAM</span><br><span class="line">):</span><br><span class="line">    <span class="built_in">print</span>(family, sockaddr)  <span class="comment"># AF_INET / AF_INET6 与 (ip, port)</span></span><br></pre></td></tr></table></figure>

<h2 id="4-traceroute（路径探测，实现因系统而异）"><a href="#4-traceroute（路径探测，实现因系统而异）" class="headerlink" title="4. traceroute（路径探测，实现因系统而异）"></a>4. <strong>traceroute</strong>（路径探测，实现因系统而异）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">traceroute -4 -n 8.8.8.8</span><br><span class="line">tracepath -n 2400:3200::1</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>无连接</strong>、<strong>尽力交付</strong>；<strong>TTL</strong>、<strong>分片</strong>、<strong>协议号</strong></td>
</tr>
<tr>
<td>作用</td>
<td>全局寻址与跨网段转发基础</td>
</tr>
<tr>
<td>编址</td>
<td><strong>IPv4 &#x2F; IPv6</strong> + <strong>CIDR</strong>；私网 <strong>RFC 1918</strong></td>
</tr>
<tr>
<td>上层</td>
<td><a href="/2026/05/01/TCP/">TCP</a> · <a href="/2026/05/01/UDP/">UDP</a> · <a href="/2026/05/01/DNS/">DNS</a></td>
</tr>
</tbody></table>
<p>更深入主题（<strong>策略路由</strong>、<strong>Anycast</strong>、<strong>IPsec</strong>）以 <strong>RFC</strong> 与发行版 <strong>Networking</strong> 文档为准。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>IP</tag>
      </tags>
  </entry>
  <entry>
    <title>MQTT 详解：发布订阅、QoS 与 mosquitto / Python 示例</title>
    <url>/2026/05/01/Protocol/MQTT/</url>
    <content><![CDATA[<p><strong>MQTT</strong> 是轻量级<strong>发布&#x2F;订阅</strong>消息传输协议（<strong>OASIS MQTT v3.1.1 &#x2F; v5.0</strong>），面向低带宽与不稳定链路，常用于 <strong>IoT</strong> 与遥测。客户端连接 <strong>Broker</strong>，按 <strong>Topic</strong> 发布与订阅消息，彼此<strong>解耦</strong>。下面从「<strong>模型与作用</strong> → <strong>主题与通配符</strong> → <strong>QoS 语义（原理）</strong> → <strong>会话与遗嘱（v5 简述）</strong> → <strong><code>mosquitto</code> 命令行示例</strong> → <strong>Python <code>paho-mqtt</code> 示例</strong>」顺序写清楚；传输层通常为 <a href="/2026/05/01/TCP/">TCP</a> <strong>1883</strong> 或 <strong>TLS 8883</strong>，与 <a href="/2026/05/01/HTTP/">HTTP</a> 请求&#x2F;响应模型不同。</p>
<hr>
<h1 id="一、模型与作用（原理）"><a href="#一、模型与作用（原理）" class="headerlink" title="一、模型与作用（原理）"></a>一、模型与作用（原理）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Publisher --+                +-- Subscriber A</span><br><span class="line">            |    [ Broker ]  |</span><br><span class="line">Publisher --+------路由 Topic--+-- Subscriber B</span><br></pre></td></tr></table></figure>

<p><strong>Broker</strong> 根据 <strong>Topic</strong> 字符串路由消息；发布者不必知道订阅者列表。</p>
<hr>
<h1 id="二、主题与通配符（作用）"><a href="#二、主题与通配符（作用）" class="headerlink" title="二、主题与通配符（作用）"></a>二、主题与通配符（作用）</h1><ul>
<li><strong>层级</strong>：用 <strong><code>/</code></strong> 分层，如 <strong><code>factory/line1/temp</code></strong>。  </li>
<li><strong>单级通配 <code>+</code></strong>：匹配一层，如 <strong><code>factory/+/temp</code></strong>。  </li>
<li><strong>多级通配 <code>#</code></strong>：匹配剩余层级，须在<strong>末尾</strong>（细节以 <strong>v5</strong> 文档为准）。</li>
</ul>
<p><strong>Topic</strong> 大小写敏感；<strong>$SYS&#x2F;</strong> 常为 Broker 系统主题（依实现）。</p>
<hr>
<h1 id="三、QoS（原理）"><a href="#三、QoS（原理）" class="headerlink" title="三、QoS（原理）"></a>三、QoS（原理）</h1><table>
<thead>
<tr>
<th>QoS</th>
<th>名称</th>
<th>语义</th>
</tr>
</thead>
<tbody><tr>
<td><strong>0</strong></td>
<td>至多一次</td>
<td>可能丢，不重试</td>
</tr>
<tr>
<td><strong>1</strong></td>
<td>至少一次</td>
<td>可能重复，需业务幂等</td>
</tr>
<tr>
<td><strong>2</strong></td>
<td>恰好一次</td>
<td>四次握手，开销最大</td>
</tr>
</tbody></table>
<p><strong>QoS</strong> 在<strong>发布方向</strong>与<strong>订阅方向</strong>可分别指定，最终有效语义取<strong>二者较高</strong>一侧规则（以 <strong>Broker</strong> 实现与文档为准）。</p>
<hr>
<h1 id="四、遗嘱消息（Last-Will，概念）"><a href="#四、遗嘱消息（Last-Will，概念）" class="headerlink" title="四、遗嘱消息（Last Will，概念）"></a>四、遗嘱消息（<strong>Last Will</strong>，概念）</h1><p>连接异常断开时 <strong>Broker</strong> 可代发预设 <strong>Payload</strong> 到指定 <strong>Topic</strong>，用于设备离线告警；须正确配置 <strong>Keep Alive</strong> 与心跳。</p>
<hr>
<h1 id="五、命令行示例（Eclipse-Mosquitto-客户端）"><a href="#五、命令行示例（Eclipse-Mosquitto-客户端）" class="headerlink" title="五、命令行示例（Eclipse Mosquitto 客户端）"></a>五、命令行示例（<strong>Eclipse Mosquitto</strong> 客户端）</h1><p>安装（<strong>Debian&#x2F;Ubuntu</strong> 示例）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install -y mosquitto-clients mosquitto</span><br><span class="line"><span class="built_in">sudo</span> systemctl start mosquitto</span><br></pre></td></tr></table></figure>

<p>订阅：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mosquitto_sub -h 127.0.0.1 -t <span class="string">&#x27;demo/#&#x27;</span> -v</span><br></pre></td></tr></table></figure>

<p>发布：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mosquitto_pub -h 127.0.0.1 -t <span class="string">&#x27;demo/sensor&#x27;</span> -m <span class="string">&#x27;&#123;&quot;t&quot;:23.5&#125;&#x27;</span> -q 1</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、Python-示例（paho-mqtt）"><a href="#六、Python-示例（paho-mqtt）" class="headerlink" title="六、Python 示例（paho-mqtt）"></a>六、Python 示例（<code>paho-mqtt</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install paho-mqtt</span><br></pre></td></tr></table></figure>

<p><strong><code>pub.py</code></strong>（<strong><code>paho-mqtt</code> 1.6.x &#x2F; 2.x</strong> 均可；若 <strong>2.x</strong> 告警可改用其推荐构造方式）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> paho.mqtt.client <span class="keyword">as</span> mqtt</span><br><span class="line"></span><br><span class="line">c = mqtt.Client()</span><br><span class="line">c.connect(<span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">1883</span>, <span class="number">60</span>)</span><br><span class="line">c.publish(<span class="string">&quot;demo/sensor&quot;</span>, payload=<span class="string">b&quot;hello-mqtt&quot;</span>, qos=<span class="number">1</span>)</span><br><span class="line">c.disconnect()</span><br></pre></td></tr></table></figure>

<p><strong><code>sub.py</code></strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> paho.mqtt.client <span class="keyword">as</span> mqtt</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">on_connect</span>(<span class="params">client, userdata, flags, rc</span>):</span><br><span class="line">    client.subscribe(<span class="string">&quot;demo/#&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">on_message</span>(<span class="params">client, userdata, msg</span>):</span><br><span class="line">    <span class="built_in">print</span>(msg.topic, msg.payload.decode())</span><br><span class="line"></span><br><span class="line">c = mqtt.Client()</span><br><span class="line">c.on_connect = on_connect</span><br><span class="line">c.on_message = on_message</span><br><span class="line">c.connect(<span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">1883</span>, <span class="number">60</span>)</span><br><span class="line">c.loop_forever()</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python sub.py &amp;</span><br><span class="line">python pub.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、与-HTTP-对比（小结表）"><a href="#七、与-HTTP-对比（小结表）" class="headerlink" title="七、与 HTTP 对比（小结表）"></a>七、与 <a href="/2026/05/01/HTTP/">HTTP</a> 对比（小结表）</h1><table>
<thead>
<tr>
<th></th>
<th><strong>MQTT</strong></th>
<th><strong>HTTP</strong></th>
</tr>
</thead>
<tbody><tr>
<td>模式</td>
<td><strong>Pub&#x2F;Sub</strong></td>
<td><strong>请求&#x2F;响应</strong></td>
</tr>
<tr>
<td>长连接</td>
<td>常见</td>
<td><strong>HTTP&#x2F;1.1</strong> 可 <strong>Keep-Alive</strong></td>
</tr>
<tr>
<td>推送</td>
<td>原生</td>
<td>需 <strong>SSE&#x2F;WebSocket</strong> 等</td>
</tr>
</tbody></table>
<hr>
<h1 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>Broker</strong> 路由；<strong>QoS</strong> 控制投递语义</td>
</tr>
<tr>
<td>作用</td>
<td><strong>IoT</strong>、遥测、解耦微服务事件（视架构）</td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>mosquitto_pub/sub</code></strong>、<strong><code>paho-mqtt</code></strong></td>
</tr>
<tr>
<td>安全</td>
<td><strong>TLS</strong>、<strong>ACL</strong>、强密码或 <strong>mTLS</strong></td>
</tr>
</tbody></table>
<p>生产须限制匿名发布、防 <strong>Topic</strong> 遍历与消息风暴。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>MQTT</tag>
      </tags>
  </entry>
  <entry>
    <title>QUIC 实践：自签名证书与 HTTP/3 服务端、客户端（Python）</title>
    <url>/2026/05/04/Protocol/QUIC-code/</url>
    <content><![CDATA[<p>本文在本地用<strong>自签名证书</strong>跑通 <strong>QUIC</strong> 承载的 <strong>HTTP&#x2F;3</strong>：先用 <strong>OpenSSL</strong> 生成证书与私钥，再用 <strong>Hypercorn</strong> 提供 <strong>HTTP&#x2F;3</strong> 服务端，用 <strong>aioquic</strong> 写最小 <strong>HTTP&#x2F;3</strong> 客户端并完成<strong>证书校验</strong>。协议背景见 <a href="/2026/05/01/QUIC/">QUIC</a> 一文。</p>
<hr>
<h1 id="一、环境与依赖"><a href="#一、环境与依赖" class="headerlink" title="一、环境与依赖"></a>一、环境与依赖</h1><ul>
<li><strong>Python</strong> 3.10+（示例使用 <strong>asyncio</strong>）。  </li>
<li><strong>OpenSSL</strong>（生成 <strong>TLS</strong> 证书）。  </li>
<li><strong>Hypercorn</strong>：内置 <strong>HTTP&#x2F;3</strong>（<strong>QUIC</strong>）服务端（依赖 <strong>aioquic</strong> 等）。  </li>
<li><strong>aioquic</strong>：本示例客户端用其 <strong>QUIC + HTTP&#x2F;3</strong> 栈发请求。  </li>
<li><strong>Starlette</strong>：<strong>ASGI</strong> 应用框架（与 <strong>Hypercorn</strong> 搭配）。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 -m pip install --upgrade <span class="string">&quot;hypercorn[h3]&quot;</span> starlette aioquic</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、生成自签名证书（OpenSSL）"><a href="#二、生成自签名证书（OpenSSL）" class="headerlink" title="二、生成自签名证书（OpenSSL）"></a>二、生成自签名证书（<strong>OpenSSL</strong>）</h1><p>在放服务端脚本的目录执行（<strong>CN</strong> 与 <strong>SAN</strong> 含 <strong>localhost</strong>、<strong>127.0.0.1</strong>，便于本机 <strong><code>https://127.0.0.1:端口</code></strong> 与 <strong>SNI</strong> 校验）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -x509 -newkey rsa:2048 -nodes \</span><br><span class="line">  -keyout key.pem -out cert.pem -days 365 \</span><br><span class="line">  -subj <span class="string">&quot;/CN=localhost&quot;</span> \</span><br><span class="line">  -addext <span class="string">&quot;subjectAltName=DNS:localhost,IP:127.0.0.1&quot;</span></span><br></pre></td></tr></table></figure>

<p>得到 <strong><code>cert.pem</code></strong>（公钥证书，可同时给客户端做<strong>信任锚</strong>）与 <strong><code>key.pem</code></strong>（私钥，仅服务端持有）。自签名仅用于开发&#x2F;实验；生产环境应使用企业 <strong>CA</strong> 或 <strong>ACME</strong>（如 <strong>Let’s Encrypt</strong>）签发的证书。</p>
<hr>
<h1 id="三、HTTP-3-服务端（Hypercorn-Starlette）"><a href="#三、HTTP-3-服务端（Hypercorn-Starlette）" class="headerlink" title="三、HTTP&#x2F;3 服务端（Hypercorn + Starlette）"></a>三、<strong>HTTP&#x2F;3</strong> 服务端（<strong>Hypercorn</strong> + <strong>Starlette</strong>）</h1><p>将下列内容保存为 <strong><code>main.py</code></strong>，与 <strong><code>cert.pem</code></strong>、<strong><code>key.pem</code></strong> 放在同一目录。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> asyncio</span><br><span class="line"><span class="keyword">from</span> pathlib <span class="keyword">import</span> Path</span><br><span class="line"><span class="keyword">from</span> hypercorn.asyncio <span class="keyword">import</span> serve</span><br><span class="line"><span class="keyword">from</span> hypercorn.config <span class="keyword">import</span> Config</span><br><span class="line"><span class="keyword">from</span> starlette.applications <span class="keyword">import</span> Starlette</span><br><span class="line"><span class="keyword">from</span> starlette.responses <span class="keyword">import</span> PlainTextResponse</span><br><span class="line"><span class="keyword">from</span> starlette.routing <span class="keyword">import</span> Route</span><br><span class="line">ROOT = Path(__file__).resolve().parent</span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">root</span>(<span class="params">_</span>):</span><br><span class="line">    <span class="keyword">return</span> PlainTextResponse(<span class="string">&quot;ok from HTTP/3 over QUIC\n&quot;</span>, media_type=<span class="string">&quot;text/plain&quot;</span>)</span><br><span class="line"></span><br><span class="line">app = Starlette(routes=[Route(<span class="string">&quot;/&quot;</span>, root)])</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">main</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    cert = ROOT/<span class="string">&quot;cert.pem&quot;</span></span><br><span class="line">    key = ROOT/<span class="string">&quot;key.pem&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(cert,key)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> cert.is_file() <span class="keyword">or</span> <span class="keyword">not</span> key.is_file():</span><br><span class="line">        <span class="keyword">raise</span> SystemExit(<span class="string">f&quot;ȱ�� <span class="subst">&#123;cert.name&#125;</span> �� <span class="subst">&#123;key.name&#125;</span>�������еڶ��� openssl ����&quot;</span>)</span><br><span class="line">    cfg = Config()</span><br><span class="line">    cfg.bind = [<span class="string">&quot;192.168.1.22:4433&quot;</span>]  <span class="comment"># ������ **TCP**���� **QUIC**</span></span><br><span class="line">    cfg.quic_bind = [<span class="string">&quot;192.168.1.22:4433&quot;</span>]</span><br><span class="line">    cfg.certfile = <span class="built_in">str</span>(cert)</span><br><span class="line">    cfg.keyfile = <span class="built_in">str</span>(key)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;HTTP/3 (QUIC)   https://192.168.1.22:4433/  (Ctrl+C ֹͣ)&quot;</span>)</span><br><span class="line">    <span class="keyword">await</span> serve(app, cfg)</span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    asyncio.run(main())</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>启动</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uv run main.py</span><br><span class="line">/home/gitee/quic_code/cert.pem /home/gitee/quic_code/key.pem</span><br><span class="line">HTTP/3 (QUIC)   https://192.168.1.22:4433/  (Ctrl+C ֹͣ)</span><br><span class="line">[2026-05-04 21:56:38 +0800] [2009834] [INFO] Running on https://192.168.1.22:4433 (CTRL + C to quit)</span><br><span class="line">[2026-05-04 21:56:38 +0800] [2009834] [INFO] Running on https://192.168.1.22:4433 (QUIC) (CTRL + C to quit)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>说明：<strong><code>bind</code></strong> 与 <strong><code>quic_bind</code></strong> 都写 <strong><code>127.0.0.1:4433</code></strong> 时，在 <strong>Linux</strong> 上可同时监听 <strong>TCP</strong> 与 <strong>UDP</strong>（端口号相同、协议不同）。这样 <strong>Chrome</strong> &#x2F; <strong>Firefox</strong> &#x2F; <strong>Edge</strong> 打开 <strong><code>https://127.0.0.1:4433/</code></strong> 会先走 <strong>TCP</strong> <strong>TLS</strong> 拿到页面；支持 <strong>HTTP&#x2F;3</strong> 的浏览器还可能通过 <strong><code>Alt-Svc</code></strong> 等机制再尝试 <strong>QUIC</strong>。若坚持<strong>只开 UDP</strong>（例如 <code>cfg.bind = []</code>），多数浏览器无法仅靠 <strong><code>https://</code></strong> 完成首次加载，因为常见实现会先做 <strong>TCP</strong> 连接。</p>
<h3 id="为何浏览器还常要-TCP、和-QUIC-设计是否冲突"><a href="#为何浏览器还常要-TCP、和-QUIC-设计是否冲突" class="headerlink" title="为何浏览器还常要 TCP、和 QUIC 设计是否冲突"></a>为何浏览器还常要 <strong>TCP</strong>、和 <strong>QUIC</strong> 设计是否冲突</h3><p><strong>不冲突。</strong> <strong>QUIC</strong>（<strong>RFC 9000</strong>）只规定跑在 <strong>UDP</strong> 上；<strong>HTTP&#x2F;3</strong> 也完全可以在<strong>只有 UDP</strong> 的情况下工作（例如本节的 <strong>aioquic</strong> 客户端直连 <strong><code>quic_bind</code></strong>）。</p>
<p>浏览器在地址栏输入 <strong><code>https://主机:端口/</code></strong> 时，多数实现仍沿用历史习惯：先按「<strong>TLS over TCP</strong>」去建<strong>第一条</strong>连接，再通过响应里的 <strong><code>Alt-Svc</code></strong>、或 <strong>DNS HTTPS&#x2F;SVCB</strong> 等线索，得知同一站点也可用 <strong>HTTP&#x2F;3</strong>，之后才发起 <strong>QUIC</strong>。这是<strong>浏览器与 Web 生态的部署&#x2F;发现策略</strong>，不是 <strong>QUIC</strong> 协议里「必须先有 <strong>TCP</strong>」的要求。本地演示若没有 <strong>DNS</strong>、没有事先缓存的 <strong><code>Alt-Svc</code></strong>，<strong>只监听 UDP</strong> 时，用户只输入 <strong><code>https://</code></strong> 就容易连不上，因此示例里<strong>同时开同端口的 TCP TLS</strong> 是为了<strong>兼容这种建连路径</strong>，属于工程上的折中，与 <strong>QUIC</strong> 规范无矛盾。</p>
<h3 id="在浏览器里打开时的证书提示"><a href="#在浏览器里打开时的证书提示" class="headerlink" title="在浏览器里打开时的证书提示"></a>在浏览器里打开时的证书提示</h3><p>自签名证书会触发「不安全」或「您的连接不是私密连接」：本地实验可在 <strong>高级</strong> 中选择继续访问（<strong>Chrome</strong>），或把 <strong><code>cert.pem</code></strong> 导入系统&#x2F;浏览器的<strong>受信任根</strong>（更安全、少打扰）。不要用这种方式处理公网真实用户流量。</p>
<hr>
<h1 id="四、HTTP-3-客户端（aioquic，校验自签名证书）"><a href="#四、HTTP-3-客户端（aioquic，校验自签名证书）" class="headerlink" title="四、HTTP&#x2F;3 客户端（aioquic，校验自签名证书）"></a>四、<strong>HTTP&#x2F;3</strong> 客户端（<strong>aioquic</strong>，校验自签名证书）</h1><p>将下列内容保存为 <strong><code>client.py</code></strong>。用 <strong><code>--ca-certs cert.pem</code></strong> 指定信任的自签名证书（与第二节生成的 <strong><code>cert.pem</code></strong> 一致）；连接 <strong><code>127.0.0.1</code></strong> 时默认 <strong><code>server_name=localhost</code></strong>，以匹配证书 <strong>CN&#x2F;SAN</strong> 与 <strong>TLS</strong> <strong>SNI</strong>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> argparse</span><br><span class="line"><span class="keyword">import</span> asyncio</span><br><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> deque</span><br><span class="line"><span class="keyword">from</span> aioquic.asyncio.client <span class="keyword">import</span> connect</span><br><span class="line"><span class="keyword">from</span> aioquic.asyncio.protocol <span class="keyword">import</span> QuicConnectionProtocol</span><br><span class="line"><span class="keyword">from</span> aioquic.h3.connection <span class="keyword">import</span> H3_ALPN, H3Connection</span><br><span class="line"><span class="keyword">from</span> aioquic.h3.events <span class="keyword">import</span> DataReceived, HeadersReceived, H3Event</span><br><span class="line"><span class="keyword">from</span> aioquic.quic.configuration <span class="keyword">import</span> QuicConfiguration</span><br><span class="line"><span class="keyword">from</span> aioquic.quic.events <span class="keyword">import</span> QuicEvent</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MiniH3Client</span>(<span class="title class_ inherited__">QuicConnectionProtocol</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;最小 HTTP/3 客户端：发 GET，收齐响应体后返回。&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, *args, **kwargs</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__(*args, **kwargs)</span><br><span class="line">        <span class="variable language_">self</span>._h3 = H3Connection(<span class="variable language_">self</span>._quic)</span><br><span class="line">        <span class="variable language_">self</span>._events: <span class="built_in">dict</span>[<span class="built_in">int</span>, deque[H3Event]] = &#123;&#125;</span><br><span class="line">        <span class="variable language_">self</span>._waiters: <span class="built_in">dict</span>[<span class="built_in">int</span>, asyncio.Future[<span class="literal">None</span>]] = &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">quic_event_received</span>(<span class="params">self, event: QuicEvent</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">for</span> he <span class="keyword">in</span> <span class="variable language_">self</span>._h3.handle_event(event):</span><br><span class="line">            <span class="variable language_">self</span>._h3_event(he)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_h3_event</span>(<span class="params">self, event: H3Event</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        sid = event.stream_id</span><br><span class="line">        <span class="variable language_">self</span>._events.setdefault(sid, deque()).append(event)</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(event, DataReceived) <span class="keyword">and</span> event.stream_ended:</span><br><span class="line">            w = <span class="variable language_">self</span>._waiters.pop(sid, <span class="literal">None</span>)</span><br><span class="line">            <span class="keyword">if</span> w <span class="keyword">and</span> <span class="keyword">not</span> w.done():</span><br><span class="line">                w.set_result(<span class="literal">None</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self, authority: <span class="built_in">str</span>, path: <span class="built_in">str</span> = <span class="string">&quot;/&quot;</span></span>) -&gt; <span class="built_in">tuple</span>[<span class="built_in">int</span>, <span class="built_in">bytes</span>]:</span><br><span class="line">        stream_id = <span class="variable language_">self</span>._quic.get_next_available_stream_id()</span><br><span class="line">        <span class="variable language_">self</span>._events[stream_id] = deque()</span><br><span class="line">        fut = asyncio.get_running_loop().create_future()</span><br><span class="line">        <span class="variable language_">self</span>._waiters[stream_id] = fut</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>._h3.send_headers(</span><br><span class="line">            stream_id=stream_id,</span><br><span class="line">            headers=[</span><br><span class="line">                (<span class="string">b&quot;:method&quot;</span>, <span class="string">b&quot;GET&quot;</span>),</span><br><span class="line">                (<span class="string">b&quot;:scheme&quot;</span>, <span class="string">b&quot;https&quot;</span>),</span><br><span class="line">                (<span class="string">b&quot;:authority&quot;</span>, authority.encode()),</span><br><span class="line">                (<span class="string">b&quot;:path&quot;</span>, path.encode()),</span><br><span class="line">            ],</span><br><span class="line">            end_stream=<span class="literal">True</span>,</span><br><span class="line">        )</span><br><span class="line">        <span class="variable language_">self</span>.transmit()</span><br><span class="line">        <span class="keyword">await</span> fut</span><br><span class="line"></span><br><span class="line">        status = <span class="number">0</span></span><br><span class="line">        body = <span class="string">b&quot;&quot;</span></span><br><span class="line">        <span class="keyword">for</span> ev <span class="keyword">in</span> <span class="variable language_">self</span>._events.pop(stream_id, deque()):</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">isinstance</span>(ev, HeadersReceived):</span><br><span class="line">                <span class="keyword">for</span> k, v <span class="keyword">in</span> ev.headers:</span><br><span class="line">                    <span class="keyword">if</span> k == <span class="string">b&quot;:status&quot;</span>:</span><br><span class="line">                        status = <span class="built_in">int</span>(v.decode())</span><br><span class="line">            <span class="keyword">elif</span> <span class="built_in">isinstance</span>(ev, DataReceived):</span><br><span class="line">                body += ev.data</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> status, body</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">args: argparse.Namespace</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">    configuration = QuicConfiguration(is_client=<span class="literal">True</span>, alpn_protocols=H3_ALPN)</span><br><span class="line">    configuration.load_verify_locations(cafile=args.ca_certs)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置 SNI</span></span><br><span class="line">    <span class="keyword">if</span> args.sni:</span><br><span class="line">        sni = args.sni</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        sni = <span class="string">&quot;localhost&quot;</span> <span class="keyword">if</span> args.host == <span class="string">&quot;127.0.0.1&quot;</span> <span class="keyword">else</span> args.host</span><br><span class="line">    configuration.server_name = sni</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">with</span> connect(</span><br><span class="line">        args.host,</span><br><span class="line">        args.port,</span><br><span class="line">        configuration=configuration,</span><br><span class="line">        create_protocol=MiniH3Client,</span><br><span class="line">    ) <span class="keyword">as</span> proto:</span><br><span class="line">        client = proto  <span class="comment"># type: MiniH3Client</span></span><br><span class="line">        authority = <span class="string">f&quot;<span class="subst">&#123;args.host&#125;</span>:<span class="subst">&#123;args.port&#125;</span>&quot;</span></span><br><span class="line">        status, body = <span class="keyword">await</span> client.get(authority, args.path)</span><br><span class="line"></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;HTTP status:&quot;</span>, status)</span><br><span class="line">        <span class="built_in">print</span>(body.decode(errors=<span class="string">&quot;replace&quot;</span>), end=<span class="string">&quot;&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">main</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    parser = argparse.ArgumentParser(description=<span class="string">&quot;HTTP/3 client with custom CA (self-signed)&quot;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&quot;--ca-certs&quot;</span>, default=<span class="string">&quot;cert.pem&quot;</span>, <span class="built_in">help</span>=<span class="string">&quot;信任的服务器证书（自签名时即 cert.pem）&quot;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&quot;--host&quot;</span>, default=<span class="string">&quot;127.0.0.1&quot;</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&quot;--port&quot;</span>, <span class="built_in">type</span>=<span class="built_in">int</span>, default=<span class="number">4433</span>)</span><br><span class="line">    parser.add_argument(<span class="string">&quot;--path&quot;</span>, default=<span class="string">&quot;/&quot;</span>)</span><br><span class="line">    parser.add_argument(</span><br><span class="line">        <span class="string">&quot;--sni&quot;</span>,</span><br><span class="line">        default=<span class="literal">None</span>,</span><br><span class="line">        <span class="built_in">help</span>=<span class="string">&quot;TLS SNI 主机名；默认对 127.0.0.1 使用 localhost 以匹配自签名证书&quot;</span>,</span><br><span class="line">    )</span><br><span class="line">    args = parser.parse_args()</span><br><span class="line">    asyncio.run(run(args))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure>

<p>要点：<strong><code>load_verify_locations(cafile=...)</code></strong> 等价于把该自签名证书当作<strong>信任锚</strong>；<strong><code>verify_mode</code></strong> 保持默认（校验证书链），<strong>不</strong>使用跳过校验的 <strong><code>CERT_NONE</code></strong>，从而真正验证 <strong>QUIC</strong> 上的 <strong>TLS</strong>。</p>
<hr>
<h1 id="五、运行步骤"><a href="#五、运行步骤" class="headerlink" title="五、运行步骤"></a>五、运行步骤</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 生成证书（第二节）</span></span><br><span class="line">openssl req -x509 -newkey rsa:2048 -nodes \</span><br><span class="line">  -keyout key.pem -out cert.pem -days 365 \</span><br><span class="line">  -subj <span class="string">&quot;/CN=192.168.1.22&quot;</span> \</span><br><span class="line">  -addext <span class="string">&quot;subjectAltName=DNS:192.168.1.22,IP:127.0.0.1&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 启动服务端（第三节脚本与 cert.pem、key.pem 同目录）</span></span><br><span class="line">python3 main.py</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 另开终端，运行客户端</span></span><br><span class="line">python3 client.py --ca-certs cert.pem --host 192.168.1.22 --port 4433 --path /</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uv run client.py --ca-certs cert.pem --host 192.168.1.22 --port 4433 --path /</span><br><span class="line">HTTP status: 200</span><br><span class="line">ok from HTTP/3 over QUIC</span><br></pre></td></tr></table></figure>

<p>期望输出含 <strong><code>HTTP status: 200</code></strong> 与正文 <strong><code>ok from HTTP/3 over QUIC</code></strong>。</p>
<hr>
<h1 id="六、用-curl-复测（可选）"><a href="#六、用-curl-复测（可选）" class="headerlink" title="六、用 curl 复测（可选）"></a>六、用 <strong><code>curl</code></strong> 复测（可选）</h1><p>若本机 <strong><code>curl</code></strong> 已启用 <strong>HTTP&#x2F;3</strong>（<strong><code>curl -V</code></strong> 可见 <strong>ngtcp2</strong> &#x2F; <strong>quiche</strong> 等），可再执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sS --http3-only --cacert cert.pem https://192.168.1.22:4433/</span><br></pre></td></tr></table></figure>

<p>未编译 <strong>HTTP&#x2F;3</strong> 的 <strong><code>curl</code></strong> 会报错，可仅用第四节 <strong>Python</strong> 客户端验证。</p>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>步骤</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>证书</td>
<td><strong>OpenSSL</strong> 自签名 <strong><code>cert.pem</code></strong> &#x2F; <strong><code>key.pem</code></strong>，<strong>SAN</strong> 含 <strong>localhost</strong> 与 <strong>127.0.0.1</strong></td>
</tr>
<tr>
<td>服务端</td>
<td><strong>Hypercorn</strong> <strong><code>bind</code>（TCP TLS）+ <code>quic_bind</code>（UDP）</strong> 同端口 <strong><code>4433</code></strong>，便于浏览器访问</td>
</tr>
<tr>
<td>客户端</td>
<td><strong>aioquic</strong> <strong><code>MiniH3Client</code></strong> + <strong><code>load_verify_locations</code></strong>，显式校验自签名</td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/QUIC/">QUIC</a> · <a href="/2026/05/01/UDP/">UDP</a> · <a href="/2026/05/01/HTTPS/">HTTPS</a></td>
</tr>
</tbody></table>
<p>更深入实验可使用 <strong>aioquic</strong> 官方仓库中的 <strong><code>examples/http3_server.py</code></strong> &#x2F; <strong><code>http3_client.py</code></strong>（<strong><code>--ca-certs</code></strong> 同样指向 <strong><code>cert.pem</code></strong>）。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>QUIC</tag>
      </tags>
  </entry>
  <entry>
    <title>RPC 概念详解：原理、形态与 JSON-RPC / curl 示例</title>
    <url>/2026/05/01/Protocol/RPC/</url>
    <content><![CDATA[<p><strong>RPC</strong>（<strong>Remote Procedure Call</strong>，远程过程调用）是一种<strong>编程抽象</strong>：像调用本地函数一样发起远程执行，由 <strong>stub</strong> 负责序列化参数、经网络发送、在服务端 <strong>skeleton</strong> 反序列化并调度真实实现，再返回结果。下面从「<strong>原理与边界</strong> → <strong>常见序列化与传输</strong> → <strong>失败语义与幂等</strong> → <strong>JSON-RPC 2.0 与 curl 示例</strong> → <strong>与 REST、gRPC 对比</strong>」顺序写清楚；<strong>gRPC</strong> 见 <a href="/2026/05/01/GRPC/">gRPC</a>，<strong>Java Dubbo</strong> 见 <a href="/2026/05/01/Dubbo/">Dubbo</a>。</p>
<hr>
<h1 id="一、原理（-stub-与存根）"><a href="#一、原理（-stub-与存根）" class="headerlink" title="一、原理（.stub 与存根）"></a>一、原理（.stub 与存根）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Client                Network                Server</span><br><span class="line"> |  call add(1,2)       |                      |</span><br><span class="line"> |  stub 序列化          |   请求消息            |</span><br><span class="line"> |---------------------&gt;|---------------------&gt;|</span><br><span class="line"> |                      |  反序列化 + 执行 add |</span><br><span class="line"> |                      |   响应消息            |</span><br><span class="line"> |&lt;---------------------|&lt;---------------------|</span><br><span class="line"> |  返回 3               |                      |</span><br></pre></td></tr></table></figure>

<p><strong>RPC</strong> 不规定具体字节格式；可由 <strong>JSON</strong>、<strong>protobuf</strong>、<strong>Thrift</strong> 等定义。</p>
<hr>
<h1 id="二、作用与典型场景"><a href="#二、作用与典型场景" class="headerlink" title="二、作用与典型场景"></a>二、作用与典型场景</h1><ul>
<li><strong>微服务内部</strong>：低延迟、强类型契约（常选 <a href="/2026/05/01/GRPC/">gRPC</a>）。  </li>
<li><strong>链上节点</strong>：<strong>Ethereum JSON-RPC</strong> 等文本 <strong>RPC</strong>。  </li>
<li><strong>脚本自动化</strong>：轻量 <strong>JSON-RPC</strong> 管理接口。</li>
</ul>
<hr>
<h1 id="三、传输与序列化（对照表）"><a href="#三、传输与序列化（对照表）" class="headerlink" title="三、传输与序列化（对照表）"></a>三、传输与序列化（对照表）</h1><table>
<thead>
<tr>
<th>组合</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>JSON + HTTP</strong></td>
<td>人可读，易调试</td>
</tr>
<tr>
<td><strong>protobuf + HTTP&#x2F;2</strong></td>
<td><strong>gRPC</strong> 默认路径</td>
</tr>
<tr>
<td><strong>二进制 + 自定义 TCP</strong></td>
<td>高性能私有协议</td>
</tr>
</tbody></table>
<hr>
<h1 id="四、失败语义与幂等（原理）"><a href="#四、失败语义与幂等（原理）" class="headerlink" title="四、失败语义与幂等（原理）"></a>四、失败语义与幂等（原理）</h1><ul>
<li><strong>超时</strong>：客户端不知服务端是否已执行；<strong>重试</strong>可能导致重复扣款。  </li>
<li><strong>对策</strong>：<strong>幂等键</strong>、<strong>去重表</strong>、<strong>At-least-once</strong> 消费语义与业务配合。</li>
</ul>
<hr>
<h1 id="五、JSON-RPC-2-0-与-curl-示例"><a href="#五、JSON-RPC-2-0-与-curl-示例" class="headerlink" title="五、JSON-RPC 2.0 与 curl 示例"></a>五、JSON-RPC 2.0 与 <code>curl</code> 示例</h1><p><strong>请求体</strong>（<strong><code>application/json</code></strong>）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;jsonrpc&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;subtract&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;params&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="attr">&quot;a&quot;</span><span class="punctuation">:</span> <span class="number">42</span><span class="punctuation">,</span> <span class="attr">&quot;b&quot;</span><span class="punctuation">:</span> <span class="number">23</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sS -X POST <span class="string">&#x27;https://eth.merkle.io/&#x27;</span> \</span><br><span class="line">  -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;method&quot;:&quot;eth_blockNumber&quot;,&quot;params&quot;:[],&quot;id&quot;:1&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p>（公共端点仅供演示，勿在高频生产直接依赖第三方。）</p>
<p><strong>成功响应</strong>形如：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;jsonrpc&quot;</span><span class="punctuation">:</span><span class="string">&quot;2.0&quot;</span><span class="punctuation">,</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span><span class="number">1</span><span class="punctuation">,</span><span class="attr">&quot;result&quot;</span><span class="punctuation">:</span><span class="string">&quot;0x...&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>错误</strong>带 <strong><code>error</code></strong> 对象（<strong><code>code</code> &#x2F; <code>message</code> &#x2F; <code>data</code></strong>）。</p>
<hr>
<h1 id="六、Python-最小-JSON-RPC-客户端（标准库）"><a href="#六、Python-最小-JSON-RPC-客户端（标准库）" class="headerlink" title="六、Python 最小 JSON-RPC 客户端（标准库）"></a>六、Python 最小 <strong>JSON-RPC</strong> 客户端（标准库）</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="keyword">import</span> urllib.request</span><br><span class="line"></span><br><span class="line">req = urllib.request.Request(</span><br><span class="line">    <span class="string">&quot;https://eth.merkle.io/&quot;</span>,</span><br><span class="line">    data=json.dumps(&#123;</span><br><span class="line">        <span class="string">&quot;jsonrpc&quot;</span>: <span class="string">&quot;2.0&quot;</span>,</span><br><span class="line">        <span class="string">&quot;method&quot;</span>: <span class="string">&quot;eth_chainId&quot;</span>,</span><br><span class="line">        <span class="string">&quot;params&quot;</span>: [],</span><br><span class="line">        <span class="string">&quot;id&quot;</span>: <span class="number">1</span>,</span><br><span class="line">    &#125;).encode(),</span><br><span class="line">    headers=&#123;<span class="string">&quot;Content-Type&quot;</span>: <span class="string">&quot;application/json&quot;</span>&#125;,</span><br><span class="line">)</span><br><span class="line"><span class="keyword">with</span> urllib.request.urlopen(req, timeout=<span class="number">10</span>) <span class="keyword">as</span> resp:</span><br><span class="line">    <span class="built_in">print</span>(json.load(resp))</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、与-REST（HTTP-资源风格）对比"><a href="#七、与-REST（HTTP-资源风格）对比" class="headerlink" title="七、与 REST（HTTP 资源风格）对比"></a>七、与 <strong>REST</strong>（<a href="/2026/05/01/HTTP/">HTTP</a> 资源风格）对比</h1><table>
<thead>
<tr>
<th></th>
<th><strong>RPC</strong></th>
<th><strong>REST</strong></th>
</tr>
</thead>
<tbody><tr>
<td>建模</td>
<td><strong>过程&#x2F;动词</strong> 中心</td>
<td><strong>资源&#x2F;名词</strong> 中心</td>
</tr>
<tr>
<td>缓存</td>
<td>较弱</td>
<td><strong>GET</strong> 与缓存头成熟</td>
</tr>
<tr>
<td>浏览器</td>
<td><strong>JSON-RPC</strong> 尚可；<strong>gRPC</strong> 常需网关</td>
<td>友好</td>
</tr>
</tbody></table>
<hr>
<h1 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>stub</strong> 隐藏网络，<strong>序列化 + 传输 + 反序列化</strong></td>
</tr>
<tr>
<td>作用</td>
<td>分布式系统中的「像本地一样调用」</td>
</tr>
<tr>
<td>示例</td>
<td><strong>JSON-RPC + <code>curl</code></strong></td>
</tr>
<tr>
<td>实现</td>
<td><a href="/2026/05/01/GRPC/">gRPC</a> · <a href="/2026/05/01/Dubbo/">Dubbo</a> 等</td>
</tr>
</tbody></table>
<p>选型需同时考虑<strong>观测性</strong>（<strong>trace id</strong>）、<strong>版本演进</strong>与<strong>跨组织边界</strong>（是否暴露 <strong>RPC</strong> 还是 <strong>REST</strong> 网关）。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>RPC</tag>
      </tags>
  </entry>
  <entry>
    <title>SFTP 详解：SSH 子系统、协议栈与 sftp 示例</title>
    <url>/2026/05/01/Protocol/SFTP/</url>
    <content><![CDATA[<p><strong>SFTP</strong>（<strong>SSH File Transfer Protocol</strong>）在 <a href="/2026/05/01/SSH/">SSH</a> 加密通道上提供文件列表、上传、下载、权限修改等能力；与 <a href="/2026/05/01/FTP/">FTP</a> 的<strong>双连接明文模型</strong>无关，也<strong>不等于 FTPS（FTP+TLS）</strong>。下面从「<strong>作用与协议栈</strong> → <strong>与 SCP 区别</strong> → <strong>交互与批处理示例</strong> → <strong><code>ssh</code> 管道技巧</strong>」顺序写清楚。</p>
<hr>
<h1 id="一、作用与协议栈（原理）"><a href="#一、作用与协议栈（原理）" class="headerlink" title="一、作用与协议栈（原理）"></a>一、作用与协议栈（原理）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[ SFTP 子系统：文件操作 PDU ]</span><br><span class="line">            ↓</span><br><span class="line">[ SSH 传输层：加密、完整性、压缩(可选) ]</span><br><span class="line">            ↓</span><br><span class="line">[ TCP ]</span><br><span class="line">            ↓</span><br><span class="line">[ IP ]</span><br></pre></td></tr></table></figure>

<p><strong>SFTP</strong> 是 <strong>SSH</strong> 协议族中的<strong>子系统</strong>，与 <strong>shell</strong> 会话可并存于同一 <strong>SSH</strong> 连接实现上（由服务器策略决定）。</p>
<hr>
<h1 id="二、与-SCP、rsync-over-SSH-的取舍"><a href="#二、与-SCP、rsync-over-SSH-的取舍" class="headerlink" title="二、与 SCP、rsync over SSH 的取舍"></a>二、与 <strong>SCP</strong>、<strong>rsync over SSH</strong> 的取舍</h1><table>
<thead>
<tr>
<th>工具</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SFTP</strong></td>
<td>交互浏览目录、断点续传能力依客户端而定</td>
</tr>
<tr>
<td><strong>scp</strong></td>
<td>单次拷贝简单文件</td>
</tr>
<tr>
<td><strong>rsync -e ssh</strong></td>
<td>增量同步目录，底层仍是 <strong>SSH</strong> 隧道，协议不等同于 <strong>SFTP</strong></td>
</tr>
</tbody></table>
<hr>
<h1 id="三、交互式-sftp"><a href="#三、交互式-sftp" class="headerlink" title="三、交互式 sftp"></a>三、交互式 <code>sftp</code></h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sftp user@example.com</span><br></pre></td></tr></table></figure>

<p>常用子命令：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ls</span><br><span class="line">cd remote_dir</span><br><span class="line">lcd local_dir</span><br><span class="line">get remote.txt</span><br><span class="line">put local.txt</span><br><span class="line">mkdir newdir</span><br><span class="line">bye</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、批处理：-b-脚本文件"><a href="#四、批处理：-b-脚本文件" class="headerlink" title="四、批处理：-b 脚本文件"></a>四、批处理：<code>-b</code> 脚本文件</h1><p><strong><code>batch.txt</code></strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">cd incoming</span><br><span class="line">put /local/path/report.csv</span><br><span class="line">ls -l</span><br><span class="line">bye</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sftp -b batch.txt user@example.com</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、与-SSH-密钥（配合作用）"><a href="#五、与-SSH-密钥（配合作用）" class="headerlink" title="五、与 SSH 密钥（配合作用）"></a>五、与 <strong>SSH 密钥</strong>（配合作用）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N <span class="string">&quot;&quot;</span></span><br><span class="line">ssh-copy-id user@example.com</span><br><span class="line">sftp user@example.com</span><br></pre></td></tr></table></figure>

<p>私钥权限过宽时 <strong>OpenSSH</strong> 会拒绝加载（<strong><code>chmod 600 ~/.ssh/id_ed25519</code></strong>）。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>SSH</strong> 隧道上的 <strong>SFTP</strong> 子系统</td>
</tr>
<tr>
<td>作用</td>
<td>安全文件传输与远程目录管理</td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>sftp</code></strong>、<strong><code>-b</code> 批处理</strong></td>
</tr>
<tr>
<td>对比</td>
<td><a href="/2026/05/01/FTP/">FTP</a> 明文双连接 · <strong>FTPS</strong> 仍双连接</td>
</tr>
</tbody></table>
<p>服务端 <strong>chroot</strong>、<strong>ForceCommand</strong>、<strong>AllowUsers</strong> 等以 <strong><code>sshd_config</code></strong> 与发行版文档为准。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
      </tags>
  </entry>
  <entry>
    <title>QUIC 详解：基于 UDP 的可靠传输、TLS 集成与 HTTP/3</title>
    <url>/2026/05/01/Protocol/QUIC/</url>
    <content><![CDATA[<p><strong>QUIC</strong>（<strong>IETF QUIC</strong>，<strong>RFC 9000</strong>）在 <a href="/2026/05/01/UDP/">UDP</a> 之上实现<strong>加密、多路复用、连接迁移</strong>与<strong>类 <a href="/2026/05/01/TCP/">TCP</a> 的可靠传输</strong>；<strong>HTTP&#x2F;3</strong>（<strong>RFC 9114</strong>）将 <a href="/2026/05/01/HTTP/">HTTP</a> 语义映射到 <strong>QUIC</strong> 流上。下面从「<strong>设计目标</strong> → <strong>连接与完整通信流程（握手、传数据、关闭）</strong> → <strong>流、可靠性与拥塞控制</strong> → <strong>Connection ID 与端口</strong> → <strong>TLS 1.3</strong> → <strong>HTTP&#x2F;3 与 HTTP&#x2F;2</strong> → <strong>抓包与可观测性</strong> → <strong><code>curl</code>&#x2F;Python 验证</strong> → <strong>运维注意</strong>」顺序写清楚。</p>
<hr>
<h1 id="一、设计目标与作用"><a href="#一、设计目标与作用" class="headerlink" title="一、设计目标与作用"></a>一、设计目标与作用</h1><h2 id="1-解决什么问题"><a href="#1-解决什么问题" class="headerlink" title="1. 解决什么问题"></a>1. 解决什么问题</h2><ul>
<li><strong>在 UDP 上重建可靠管道</strong>：丢包检测、重传、拥塞控制发生在 <strong>QUIC</strong> 层，而不依赖 <a href="/2026/05/01/TCP/">TCP</a> 单一流上的队头阻塞。  </li>
<li><strong>默认加密</strong>：传输与握手深度绑定 <strong>TLS 1.3</strong>（<strong>RFC 9001</strong>），减少「明文旁路」与分层带来的额外往返。  </li>
<li><strong>多流与连接标识</strong>：<strong>Stream</strong> 隔离应用数据；<strong>Connection ID</strong> 让四元组变化时仍能延续同一逻辑连接（<strong>连接迁移</strong>）。</li>
</ul>
<h2 id="2-相对-TCP-TLS-的典型取舍"><a href="#2-相对-TCP-TLS-的典型取舍" class="headerlink" title="2. 相对 TCP + TLS 的典型取舍"></a>2. 相对 TCP + TLS 的典型取舍</h2><table>
<thead>
<tr>
<th>问题</th>
<th><strong><a href="/2026/05/01/TCP/">TCP</a> + TLS</strong> 常见表现</th>
<th><strong>QUIC</strong> 思路</th>
</tr>
</thead>
<tbody><tr>
<td><strong>队头阻塞</strong></td>
<td><strong>TCP</strong> 字节流级丢包会阻塞后续所有应用数据</td>
<td><strong>QUIC</strong> 在<strong>流</strong>级相对独立地做丢包恢复</td>
</tr>
<tr>
<td><strong>握手 RTT</strong></td>
<td>常需 <strong>TCP</strong> 三次握手再叠 <strong>TLS</strong> 往返</td>
<td>常把加密与传输握手合并，目标 <strong>1-RTT</strong> 建连（<strong>0-RTT</strong> 为可选加速）</td>
</tr>
<tr>
<td><strong>NAT 重绑定</strong></td>
<td>四元组变化常被视作「新连接」</td>
<td><strong>Connection ID</strong> 标识逻辑连接，支持<strong>连接迁移</strong></td>
</tr>
<tr>
<td><strong>中间设备</strong></td>
<td><strong>TCP</strong> 成熟、穿透好</td>
<td>依赖 <strong><a href="/2026/05/01/UDP/">UDP</a></strong> 放行（如 <strong>UDP&#x2F;443</strong>）；企业网策略需对齐</td>
</tr>
</tbody></table>
<hr>
<h1 id="二、连接与完整通信流程（原理）"><a href="#二、连接与完整通信流程（原理）" class="headerlink" title="二、连接与完整通信流程（原理）"></a>二、连接与完整通信流程（原理）</h1><p><strong>QUIC</strong> 报文封装在 <strong>UDP</strong> 数据报中（常见 <strong>HTTPS</strong> 场景为 <strong>UDP&#x2F;443</strong>）。以下极度简化，省略版本协商、重试包、地址验证等细节。</p>
<h2 id="1-建立连接（简化）"><a href="#1-建立连接（简化）" class="headerlink" title="1. 建立连接（简化）"></a>1. 建立连接（简化）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端                                   服务端</span><br><span class="line">   |    Initial（含加密握手负载）         |</span><br><span class="line">   |------------------------------------&gt;|</span><br><span class="line">   |    Initial / Handshake（应答）      |</span><br><span class="line">   |&lt;------------------------------------|</span><br><span class="line">   |    1-RTT 应用数据（如 HTTP/3）       |</span><br><span class="line">   |&lt;-----------------------------------&gt;|</span><br><span class="line">   |         逻辑上进入「可传数据」阶段    |</span><br></pre></td></tr></table></figure>

<p>首包常带 <strong>TLS ClientHello</strong>；成功后在 <strong>1-RTT</strong> 密钥下发送应用数据。<strong>0-RTT</strong> 可在重复连接时提前带数据，见第五节安全注意。</p>
<h2 id="2-关闭连接（简化）"><a href="#2-关闭连接（简化）" class="headerlink" title="2. 关闭连接（简化）"></a>2. 关闭连接（简化）</h2><p>任一方可发送 <strong>CONNECTION_CLOSE</strong> 帧；对端进入<strong>排空（draining）<strong>期，不再发送新数据。与 <a href="/2026/05/01/TCP/">TCP</a> 的 <strong>FIN&#x2F;ACK</strong> 不同，<strong>QUIC</strong> 在</strong>连接</strong>粒度表达关闭，内部再回收流与 <strong>UDP</strong> 套接字资源。</p>
<h2 id="3-与典型「套接字-协议栈」的对应"><a href="#3-与典型「套接字-协议栈」的对应" class="headerlink" title="3. 与典型「套接字 &#x2F; 协议栈」的对应"></a>3. 与典型「套接字 &#x2F; 协议栈」的对应</h2><p>与 <a href="/2026/05/01/TCP/">TCP</a> 的 <code>connect</code>&#x2F;<code>accept</code> 由内核一条龙完成不同，<strong>QUIC</strong> 一般由<strong>用户态库</strong>（如 <strong>ngtcp2</strong>、<strong>quiche</strong>、<strong>MsQuic</strong>、<strong>aioquic</strong>）或浏览器网络栈实现：底层仍是 <strong><a href="/2026/05/01/UDP/">UDP</a> <code>sendto</code>&#x2F;<code>recvfrom</code></strong>，其上是 <strong>TLS 1.3</strong>、<strong>ACK&#x2F;重传</strong>、<strong>流控</strong>与 <strong>HTTP&#x2F;3</strong> 帧。<strong>HTTP&#x2F;3</strong> 客户端日常通过 <strong><code>curl --http3-only</code></strong> 或内置 <strong>HTTP&#x2F;3</strong> 的 SDK 使用，而不是手写裸 <strong>UDP</strong> 报文。</p>
<hr>
<h1 id="三、流、可靠性与拥塞控制（原理要点）"><a href="#三、流、可靠性与拥塞控制（原理要点）" class="headerlink" title="三、流、可靠性与拥塞控制（原理要点）"></a>三、流、可靠性与拥塞控制（原理要点）</h1><ul>
<li><strong>Connection</strong>：加密上下文、<strong>ACK</strong> 与拥塞控制的归属单位（类似「一条加密了的类 TCP 管道」）。  </li>
<li><strong>Stream</strong>：单向或双向；<strong>HTTP&#x2F;3</strong> 将请求&#x2F;响应映射到不同流，减轻应用级队头阻塞。  </li>
<li><strong>Packet Number</strong>：单调递增，配合 <strong>ACK</strong> 范围与<strong>密钥更新</strong>，降低与 <a href="/2026/05/01/TCP/">TCP</a> 序号相关的歧义；<strong>重传不重用旧 PN</strong>（新包新号）。  </li>
<li><strong>拥塞控制</strong>：在 <strong>Connection</strong> 上运作（如 <strong>Cubic</strong>、<strong>BBR</strong> 等实现依栈而定），与 <a href="/2026/05/01/TCP/">TCP</a> 一样受网络条件约束，但并非「一条字节流管所有多路复用对象」。</li>
</ul>
<hr>
<h1 id="四、Connection-ID、端口与「会话标识」"><a href="#四、Connection-ID、端口与「会话标识」" class="headerlink" title="四、Connection ID、端口与「会话标识」"></a>四、Connection ID、端口与「会话标识」</h1><ul>
<li><strong>UDP 四元组</strong>仍用于路由首个报文；<strong>Connection ID</strong> 使<strong>源 IP&#x2F;端口变化</strong>（如蜂窝切换 <strong>Wi-Fi</strong>）后，对端仍能识别同一 <strong>QUIC</strong> 连接。  </li>
<li>公网 <strong>HTTP&#x2F;3</strong> 常见端口为 <strong>443</strong>（<strong>UDP</strong>），与 <a href="/2026/05/01/TCP/">TCP</a> <strong>443</strong> 并存、互不替代。  </li>
<li>服务端可协商 <strong>CID</strong> 长度与轮换策略，以兼顾隐私与路由。</li>
</ul>
<hr>
<h1 id="五、TLS-1-3-与-QUIC（原理）"><a href="#五、TLS-1-3-与-QUIC（原理）" class="headerlink" title="五、TLS 1.3 与 QUIC（原理）"></a>五、TLS 1.3 与 QUIC（原理）</h1><p><strong>QUIC</strong> 使用 <strong>TLS 1.3</strong> 做<strong>身份认证</strong>与<strong>密钥导出</strong>；载荷由 <strong>AEAD</strong> 保护，<strong>PADDING</strong> 等机制缓解流量分析。<strong>0-RTT</strong> 可加速重复连接，但存在<strong>重放</strong>风险：对金融类写操作应禁用或在应用层做幂等与二次校验。</p>
<hr>
<h1 id="六、HTTP-3-与-HTTP-2-对比（概念）"><a href="#六、HTTP-3-与-HTTP-2-对比（概念）" class="headerlink" title="六、HTTP&#x2F;3 与 HTTP&#x2F;2 对比（概念）"></a>六、HTTP&#x2F;3 与 <a href="/2026/05/01/HTTP/">HTTP&#x2F;2</a> 对比（概念）</h1><table>
<thead>
<tr>
<th>项</th>
<th><strong>HTTP&#x2F;2 over <a href="/2026/05/01/TCP/">TCP</a></strong></th>
<th><strong>HTTP&#x2F;3 over QUIC</strong></th>
</tr>
</thead>
<tbody><tr>
<td>传输</td>
<td><strong>TCP</strong> 字节流</td>
<td><strong><a href="/2026/05/01/UDP/">UDP</a></strong> 上的 <strong>QUIC</strong></td>
</tr>
<tr>
<td>多路复用</td>
<td><strong>H2</strong> 帧共享一条 <strong>TCP</strong> 连接</td>
<td><strong>QUIC</strong> 流级隔离，丢包影响面更小</td>
</tr>
<tr>
<td><strong>TLS</strong></td>
<td>传统分层（先 <strong>TCP</strong> 再 <strong>TLS</strong>）</td>
<td>与 <strong>QUIC</strong> 握手紧耦合</td>
</tr>
</tbody></table>
<p><strong>HTTP</strong> 语义（方法、状态码、首部）仍见 <a href="/2026/05/01/HTTP/">HTTP</a> 一文；<a href="/2026/05/01/HTTPS/">HTTPS</a> 在 <strong>HTTP&#x2F;3</strong> 场景下体现为 <strong>QUIC + TLS 1.3</strong> 的组合。</p>
<hr>
<h1 id="七、抓包与可观测性"><a href="#七、抓包与可观测性" class="headerlink" title="七、抓包与可观测性"></a>七、抓包与可观测性</h1><p>在 <strong>Linux</strong> 上可先看 <strong>UDP</strong> 外层（载荷默认加密）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> tcpdump -n -i any <span class="string">&#x27;udp port 443 and host cloudflare.com&#x27;</span></span><br></pre></td></tr></table></figure>

<p>用支持 <strong>HTTP&#x2F;3</strong> 的浏览器或下文 <strong><code>curl</code></strong> 访问时，可观察到 <strong>UDP</strong> 双向流量。<strong>Wireshark</strong> 在合规前提下可用 <strong>SSLKEYLOGFILE</strong> 或端点导出密钥做解密分析；生产排障更多依赖<strong>端侧日志</strong>、<strong>CDN&#x2F;网关指标</strong>，而非明文抓包。</p>
<hr>
<h1 id="八、工具示例：curl-与-Python（包装调用）"><a href="#八、工具示例：curl-与-Python（包装调用）" class="headerlink" title="八、工具示例：curl 与 Python（包装调用）"></a>八、工具示例：<strong><code>curl</code></strong> 与 <strong>Python</strong>（包装调用）</h1><p>完整 <strong>QUIC</strong> 客户端&#x2F;服务端开发需引入 <strong>QUIC</strong> 实现库；日常验证 <strong>HTTP&#x2F;3</strong> 以 <strong><code>curl</code></strong> 最省事（需 <strong><code>curl</code></strong> 编译时启用 <strong>HTTP&#x2F;3</strong>，如 <strong>ngtcp2</strong>&#x2F;<strong>quiche</strong> 等后端）。</p>
<h2 id="1-curl"><a href="#1-curl" class="headerlink" title="1. curl"></a>1. <strong><code>curl</code></strong></h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl --http3-only -I https://cloudflare.com/</span><br></pre></td></tr></table></figure>

<p>查看当前 <strong><code>curl</code></strong> 是否带 <strong>HTTP&#x2F;3</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -V | grep -i -E <span class="string">&#x27;http3|ngtcp2|quiche&#x27;</span></span><br></pre></td></tr></table></figure>

<p>若报协议不支持，请换用发行版或自行编译带 <strong>HTTP&#x2F;3</strong> 的 <strong><code>curl</code></strong>。</p>
<h2 id="2-Python（通过-subprocess-调用-curl）"><a href="#2-Python（通过-subprocess-调用-curl）" class="headerlink" title="2. Python（通过 subprocess 调用 curl）"></a>2. <strong>Python</strong>（通过 <strong><code>subprocess</code></strong> 调用 <strong><code>curl</code></strong>）</h2><p>丢弃响应体，只取 <strong>HTTP</strong> 状态码（避免 <strong><code>curl -I</code></strong> 把首部打印到 <strong>stdout</strong> 与 <strong><code>%{http_code}</code></strong> 混在一起）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> subprocess</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">http3_status</span>(<span class="params">url: <span class="built_in">str</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">    p = subprocess.run(</span><br><span class="line">        [</span><br><span class="line">            <span class="string">&quot;curl&quot;</span>,</span><br><span class="line">            <span class="string">&quot;-sS&quot;</span>,</span><br><span class="line">            <span class="string">&quot;--http3-only&quot;</span>,</span><br><span class="line">            <span class="string">&quot;-o&quot;</span>,</span><br><span class="line">            <span class="string">&quot;/dev/null&quot;</span>,</span><br><span class="line">            <span class="string">&quot;-w&quot;</span>,</span><br><span class="line">            <span class="string">&quot;%&#123;http_code&#125;&quot;</span>,</span><br><span class="line">            url,</span><br><span class="line">        ],</span><br><span class="line">        capture_output=<span class="literal">True</span>,</span><br><span class="line">        text=<span class="literal">True</span>,</span><br><span class="line">        check=<span class="literal">False</span>,</span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">if</span> p.returncode != <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">raise</span> RuntimeError(p.stderr <span class="keyword">or</span> <span class="string">f&quot;curl exit <span class="subst">&#123;p.returncode&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">int</span>(p.stdout.strip() <span class="keyword">or</span> <span class="string">&quot;0&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;status:&quot;</span>, http3_status(<span class="string">&quot;https://cloudflare.com/&quot;</span>))</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="九、运维注意（落地）"><a href="#九、运维注意（落地）" class="headerlink" title="九、运维注意（落地）"></a>九、运维注意（落地）</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>UDP 放行</strong></td>
<td>防火墙、<strong>ACL</strong>、运营商策略可能拦或未优先 <strong>UDP&#x2F;443</strong></td>
</tr>
<tr>
<td><strong>观测</strong></td>
<td>明文 <strong>QUIC</strong> 少；依赖日志、指标、合规密钥导出</td>
</tr>
<tr>
<td><strong>QoS</strong></td>
<td><strong>UDP</strong> 易被误伤；与网络团队确认分类与限速</td>
</tr>
<tr>
<td><strong>安全</strong></td>
<td>反射放大主要针对未鉴权 <strong>UDP</strong> 服务；正规 <strong>HTTP&#x2F;3</strong> 栈需跟紧 CVE 与版本</td>
</tr>
</tbody></table>
<hr>
<h1 id="十、小结"><a href="#十、小结" class="headerlink" title="十、小结"></a>十、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong><a href="/2026/05/01/UDP/">UDP</a></strong> 上的可靠、加密、多流；<strong>CID</strong> 与迁移</td>
</tr>
<tr>
<td>作用</td>
<td><strong>HTTP&#x2F;3</strong>、移动网络、降低握手延迟</td>
</tr>
<tr>
<td>依赖</td>
<td><strong><a href="/2026/05/01/UDP/">UDP</a></strong>、<strong>TLS 1.3</strong></td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>tcpdump</code> <code>udp port 443</code></strong>、<strong><code>curl --http3-only</code></strong>、<strong>Python + <code>subprocess</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/TCP/">TCP</a> · <a href="/2026/05/01/UDP/">UDP</a> · <a href="/2026/05/01/HTTPS/">HTTPS</a></td>
</tr>
</tbody></table>
<p>实现细节以 <strong>RFC 9000、9001、9114</strong> 与浏览器、<strong>CDN</strong> 发布说明为准。</p>
]]></content>
      <categories>
        <category>QUIC</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>QUIC</tag>
      </tags>
  </entry>
  <entry>
    <title>SSH 详解：加密远程登录、密钥与端口转发示例</title>
    <url>/2026/05/01/Protocol/SSH/</url>
    <content><![CDATA[<p><strong>SSH</strong>（<strong>Secure Shell</strong>）在不安全网络上提供<strong>加密远程 shell</strong>、<strong>文件传输（<a href="/2026/05/01/SFTP/">SFTP</a>）<strong>与</strong>隧道</strong>。常见实现 <strong>OpenSSH</strong> 默认 <strong>TCP&#x2F;22</strong>。下面从「<strong>协议目标</strong> → <strong>主机密钥与用户认证原理</strong> → <strong><code>ssh_config</code> &#x2F; <code>sshd_config</code></strong> → <strong>本地端口转发示例</strong> → <strong>安全运维要点</strong>」顺序写清楚。</p>
<hr>
<h1 id="一、协议目标（作用）"><a href="#一、协议目标（作用）" class="headerlink" title="一、协议目标（作用）"></a>一、协议目标（作用）</h1><ul>
<li><strong>机密性与完整性</strong>：防窃听与篡改。  </li>
<li><strong>身份认证</strong>：至少认证<strong>服务器</strong>；可双向证书或密钥。  </li>
<li><strong>隧道能力</strong>：为其他 <a href="/2026/05/01/TCP/">TCP</a> 应用提供加密通道。</li>
</ul>
<hr>
<h1 id="二、主机密钥与用户认证（原理）"><a href="#二、主机密钥与用户认证（原理）" class="headerlink" title="二、主机密钥与用户认证（原理）"></a>二、主机密钥与用户认证（原理）</h1><h2 id="1-主机身份"><a href="#1-主机身份" class="headerlink" title="1. 主机身份"></a>1. 主机身份</h2><p>首次连接写入 <strong><code>~/.ssh/known_hosts</code></strong>；指纹变更会报警，需区分<strong>重装</strong>与<strong>中间人</strong>。</p>
<h2 id="2-用户认证方式"><a href="#2-用户认证方式" class="headerlink" title="2. 用户认证方式"></a>2. 用户认证方式</h2><table>
<thead>
<tr>
<th>方式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>公钥</strong></td>
<td>推荐；私钥 <strong><code>chmod 600</code></strong></td>
</tr>
<tr>
<td><strong>密码</strong></td>
<td>简单；配合 <strong>fail2ban</strong>、<strong>速率限制</strong></td>
</tr>
<tr>
<td><strong>证书</strong></td>
<td>企业 <strong>SSH CA</strong> 签发主机&#x2F;用户证书</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、-ssh-config-示例"><a href="#三、-ssh-config-示例" class="headerlink" title="三、~/.ssh/config 示例"></a>三、<code>~/.ssh/config</code> 示例</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Host myserver</span><br><span class="line">    HostName 203.0.113.10</span><br><span class="line">    User deploy</span><br><span class="line">    Port 22</span><br><span class="line">    IdentityFile ~/.ssh/id_ed25519</span><br><span class="line">    ServerAliveInterval 30</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh myserver</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、本地端口转发（-L）原理与示例"><a href="#四、本地端口转发（-L）原理与示例" class="headerlink" title="四、本地端口转发（-L）原理与示例"></a>四、本地端口转发（<code>-L</code>）原理与示例</h1><p>把<strong>本机</strong> <strong><code>localhost:LOCAL_PORT</code></strong> 映射到<strong>经 SSH 可达</strong>的 <strong><code>target:REMOTE_PORT</code></strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -N -L 3307:127.0.0.1:3306 user@bastion</span><br></pre></td></tr></table></figure>

<p>本机连接 <strong><code>127.0.0.1:3307</code></strong> 即相当于在 <strong>bastion</strong> 上访问 <strong><code>127.0.0.1:3306</code></strong>（常用于内网数据库跳板）。</p>
<hr>
<h1 id="五、跳板：ProxyJump"><a href="#五、跳板：ProxyJump" class="headerlink" title="五、跳板：ProxyJump"></a>五、跳板：<code>ProxyJump</code></h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -J bastion_user@bastion_host app_user@app_private</span><br></pre></td></tr></table></figure>

<p>等价 <strong><code>~/.ssh/config</code></strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Host app</span><br><span class="line">    HostName 10.0.0.5</span><br><span class="line">    User appuser</span><br><span class="line">    ProxyJump bastion_user@bastion_host</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、sshd-config-常见安全项（概念）"><a href="#六、sshd-config-常见安全项（概念）" class="headerlink" title="六、sshd_config 常见安全项（概念）"></a>六、<code>sshd_config</code> 常见安全项（概念）</h1><ul>
<li><strong><code>PasswordAuthentication no</code></strong>：仅密钥（变更前确保不会锁死自己）。  </li>
<li><strong><code>PermitRootLogin prohibit-password</code></strong> 或 <strong><code>no</code></strong>。  </li>
<li><strong><code>AllowUsers</code></strong> &#x2F; <strong><code>AllowGroups</code></strong> 限制登录账户。</li>
</ul>
<p>修改后：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl reload sshd</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td>加密通道 + 主机密钥 + 用户认证</td>
</tr>
<tr>
<td>作用</td>
<td>远程管理、<a href="/2026/05/01/SFTP/">SFTP</a>、隧道</td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>-L</code></strong>、<strong><code>-J</code></strong>、<strong><code>ssh_config</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/SFTP/">SFTP</a> · <a href="/2026/05/01/TCP/">TCP</a></td>
</tr>
</tbody></table>
<p>审计日志、<strong>MFA</strong>、及时打补丁与<strong>密钥轮换</strong>属生产基线。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP 协议详解：连接、可靠传输与编程示例</title>
    <url>/2026/05/01/Protocol/TCP/</url>
    <content><![CDATA[<p><strong>TCP</strong>（<strong>Transmission Control Protocol</strong>）在 <a href="/2026/05/01/IP/">IP</a> 之上提供<strong>面向连接</strong>的、<strong>可靠的</strong>、<strong>按序</strong>的字节流抽象，是 <a href="/2026/05/01/HTTP/">HTTP</a>、<a href="/2026/05/01/HTTPS/">HTTPS</a>、<a href="/2026/05/01/SSH/">SSH</a> 等协议的主要载体。下面从「<strong>设计目标</strong> → <strong>连接与完整通信流程（握手、传数据、挥手）</strong> → <strong>可靠性与拥塞控制概念</strong> → <strong>端口与套接字</strong> → <strong>抓包</strong> → <strong>C &#x2F; Python 套接字示例</strong>」顺序写清楚。</p>
<hr>
<h1 id="一、设计目标与作用"><a href="#一、设计目标与作用" class="headerlink" title="一、设计目标与作用"></a>一、设计目标与作用</h1><h2 id="1-解决什么问题"><a href="#1-解决什么问题" class="headerlink" title="1. 解决什么问题"></a>1. 解决什么问题</h2><ul>
<li><strong>进程到进程</strong>：仅靠 <a href="/2026/05/01/IP/">IP</a> 只能定位到主机；<strong>TCP</strong> 用<strong>源端口 &#x2F; 目的端口</strong>区分同一主机上的不同应用。  </li>
<li><strong>可靠有序字节流</strong>：通过<strong>序号（Sequence）</strong>、<strong>确认（ACK）</strong>、<strong>重传</strong>、<strong>按序重组</strong>隐藏丢包与乱序。  </li>
<li><strong>流量控制与拥塞控制</strong>：用<strong>接收窗口</strong>限制发送端速率；用<strong>拥塞窗口</strong>与多种算法（<strong>Cubic</strong>、<strong>BBR</strong> 等，依系统）减轻网络拥塞。</li>
</ul>
<h2 id="2-与-UDP-的取舍"><a href="#2-与-UDP-的取舍" class="headerlink" title="2. 与 UDP 的取舍"></a>2. 与 UDP 的取舍</h2><p>需要<strong>稳定管道</strong>、<strong>文件传输</strong>、<strong>远程登录</strong>时选 <strong>TCP</strong>；可容忍丢包、强实时见 <a href="/2026/05/01/UDP/">UDP</a> 与 <a href="/2026/05/01/QUIC/">QUIC</a>。</p>
<hr>
<h1 id="二、连接：三次握手与四次挥手（原理）"><a href="#二、连接：三次握手与四次挥手（原理）" class="headerlink" title="二、连接：三次握手与四次挥手（原理）"></a>二、连接：三次握手与四次挥手（原理）</h1><h2 id="1-建立连接（简化）"><a href="#1-建立连接（简化）" class="headerlink" title="1. 建立连接（简化）"></a>1. 建立连接（简化）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端                         服务端</span><br><span class="line">   |    SYN (seq=x)              |</span><br><span class="line">   |---------------------------&gt;|</span><br><span class="line">   |  SYN-ACK (ack=x+1, seq=y)  |</span><br><span class="line">   |&lt;---------------------------|</span><br><span class="line">   |    ACK (ack=y+1)           |</span><br><span class="line">   |---------------------------&gt;|</span><br><span class="line">   |        已建立 ESTABLISHED    |</span><br></pre></td></tr></table></figure>

<p><strong>SYN</strong> 洪泛、<strong>半连接队列</strong>长度与 <strong>SYN Cookie</strong> 属于安全与调优范畴。</p>
<h2 id="2-关闭连接（简化）"><a href="#2-关闭连接（简化）" class="headerlink" title="2. 关闭连接（简化）"></a>2. 关闭连接（简化）</h2><p>任一方发送 <strong>FIN</strong> 表示本方向不再发送数据；双方都可能 <strong>FIN</strong>，经过 <strong>TIME_WAIT</strong>（约 <strong>2MSL</strong>）状态以便旧报文从网络中消散。<strong>RST</strong> 表示异常复位。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">主动关闭端                         对端</span><br><span class="line">   |    FIN (seq=m)                  |</span><br><span class="line">   |-------------------------------&gt;|</span><br><span class="line">   |    ACK (ack=m+1)                |</span><br><span class="line">   |&lt;-------------------------------|</span><br><span class="line">   |    FIN (seq=n)                  |</span><br><span class="line">   |&lt;-------------------------------|</span><br><span class="line">   |    ACK (ack=n+1)                |</span><br><span class="line">   |-------------------------------&gt;|</span><br><span class="line">   |  主动端常见 TIME_WAIT → CLOSED   |</span><br></pre></td></tr></table></figure>

<h2 id="3-完整通信流程（与套接字-API-对应）"><a href="#3-完整通信流程（与套接字-API-对应）" class="headerlink" title="3. 完整通信流程（与套接字 API 对应）"></a>3. 完整通信流程（与套接字 API 对应）</h2><p>把「原理」落到一次典型会话上，顺序如下。</p>
<ol>
<li><strong>服务端</strong>：<code>socket(AF_INET, SOCK_STREAM, 0)</code> → <code>bind</code> 监听地址与端口 → <code>listen</code>（内核维护 <strong>LISTEN</strong> 与半连接&#x2F;全连接队列）→ <code>accept</code> 阻塞直至三次握手完成，返回<strong>已连接套接字</strong>。  </li>
<li><strong>客户端</strong>：<code>socket</code> → <code>connect</code>（内核发出 <strong>SYN</strong>，完成握手后返回）→ 双方进入 <strong>ESTABLISHED</strong>。  </li>
<li><strong>传数据</strong>：<code>send</code> &#x2F; <code>write</code> 与 <code>recv</code> &#x2F; <code>read</code> 在用户态与内核发送&#x2F;接收缓冲区之间搬运字节；<strong>TCP</strong> 在协议层做<strong>序号、确认、重传、按序递交</strong>；<strong>滑动窗口</strong>与<strong>拥塞控制</strong>限制未确认数据量。  </li>
<li><strong>关闭</strong>：任一方 <code>close</code> 或 <code>shutdown(SHUT_WR)</code> 触发本方向 <strong>FIN</strong>；对端 <strong>ACK</strong> 后可能仍可收数据（<strong>半关闭</strong>）；双方都发完 <strong>FIN</strong> 并确认后，主动关闭端常经历 <strong>TIME_WAIT</strong>，再回收端口与连接控制块。</li>
</ol>
<hr>
<h1 id="三、可靠性与窗口（原理要点）"><a href="#三、可靠性与窗口（原理要点）" class="headerlink" title="三、可靠性与窗口（原理要点）"></a>三、可靠性与窗口（原理要点）</h1><ul>
<li><strong>字节序号</strong>：每个方向独立编号；<strong>ACK</strong> 表示「期望收到的下一个序号」。  </li>
<li><strong>滑动窗口</strong>：已发送未确认的字节不能无限堆积；窗口大小受<strong>接收方通告</strong>与<strong>拥塞算法</strong>共同约束。  </li>
<li><strong>Nagle 与延迟 ACK</strong>：小报文合并与确认延迟可能增加交互延迟；低延迟场景可在套接字层关闭 <strong>Nagle</strong>（<strong><code>TCP_NODELAY</code></strong>），需权衡带宽。</li>
</ul>
<hr>
<h1 id="四、四元组与套接字"><a href="#四、四元组与套接字" class="headerlink" title="四、四元组与套接字"></a>四、四元组与套接字</h1><p>唯一确定一条 <strong>TCP</strong> 连接：<strong>（源 IP，源端口，目的 IP，目的端口）</strong>。服务端 <strong><code>LISTEN</code></strong> 在固定端口（如 <strong>80</strong>），客户端端口通常由内核临时分配。</p>
<hr>
<h1 id="五、抓包示例（观察握手）"><a href="#五、抓包示例（观察握手）" class="headerlink" title="五、抓包示例（观察握手）"></a>五、抓包示例（观察握手）</h1><p>在 <strong>Linux</strong> 上（需装 <strong><code>tcpdump</code></strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> tcpdump -n -i any tcp port 80 and host example.com</span><br></pre></td></tr></table></figure>

<p>用浏览器访问 <strong><code>http://example.com</code></strong>，可看到 <strong>SYN &#x2F; SYN-ACK &#x2F; ACK</strong> 与后续 <strong>PSH-ACK</strong> 数据段。<strong>Wireshark</strong> 的 <strong>「Follow → TCP Stream」</strong> 便于学习报文序列。</p>
<hr>
<h1 id="六、编程示例：C（POSIX-Socket）与-Python-回显"><a href="#六、编程示例：C（POSIX-Socket）与-Python-回显" class="headerlink" title="六、编程示例：C（POSIX Socket）与 Python 回显"></a>六、编程示例：<strong>C</strong>（<strong>POSIX</strong> <strong>Socket</strong>）与 <strong>Python</strong> 回显</h1><p>以下在 <strong>Linux</strong> 下用 <strong>IPv4</strong> 回环地址演示；编译 <strong>C</strong> 需 <strong>GCC</strong> 与标准 <strong>POSIX</strong> 头文件。</p>
<h2 id="1-C-服务端-tcp-echo-server-c"><a href="#1-C-服务端-tcp-echo-server-c" class="headerlink" title="1. C 服务端 tcp_echo_server.c"></a>1. <strong>C</strong> 服务端 <code>tcp_echo_server.c</code></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 9000</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF 4096</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> srv = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (srv &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;socket&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> on = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (setsockopt(srv, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="keyword">sizeof</span>(on)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;setsockopt&quot;</span>);</span><br><span class="line">    close(srv);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">  <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">  addr.sin_family = AF_INET;</span><br><span class="line">  addr.sin_port = htons(PORT);</span><br><span class="line">  addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (bind(srv, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;bind&quot;</span>);</span><br><span class="line">    close(srv);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (listen(srv, <span class="number">1</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;listen&quot;</span>);</span><br><span class="line">    close(srv);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;listening on %d\n&quot;</span>, PORT);</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">peer</span>;</span></span><br><span class="line">  <span class="type">socklen_t</span> plen = <span class="keyword">sizeof</span>(peer);</span><br><span class="line">  <span class="type">int</span> conn = accept(srv, (<span class="keyword">struct</span> sockaddr *)&amp;peer, &amp;plen);</span><br><span class="line">  <span class="keyword">if</span> (conn &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;accept&quot;</span>);</span><br><span class="line">    close(srv);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">char</span> buf[BUF];</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="type">ssize_t</span> n = recv(conn, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;recv&quot;</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">break</span>; <span class="comment">/* 对端关闭写方向 */</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> prefix[] = <span class="string">&quot;echo: &quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (send(conn, prefix, <span class="keyword">sizeof</span>(prefix) - <span class="number">1</span>, <span class="number">0</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;send&quot;</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">ssize_t</span> sent = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (sent &lt; n) &#123;</span><br><span class="line">      <span class="type">ssize_t</span> w = send(conn, buf + sent, (<span class="type">size_t</span>)(n - sent), <span class="number">0</span>);</span><br><span class="line">      <span class="keyword">if</span> (w &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;send&quot;</span>);</span><br><span class="line">        sent = <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      sent += w;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (sent &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  close(conn);</span><br><span class="line">  close(srv);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-C-客户端-tcp-echo-client-c"><a href="#2-C-客户端-tcp-echo-client-c" class="headerlink" title="2. C 客户端 tcp_echo_client.c"></a>2. <strong>C</strong> 客户端 <code>tcp_echo_client.c</code></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 9000</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF 4096</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;socket&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">  <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">  addr.sin_family = AF_INET;</span><br><span class="line">  addr.sin_port = htons(PORT);</span><br><span class="line">  <span class="keyword">if</span> (inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;addr.sin_addr) != <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;inet_pton failed\n&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (connect(fd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;connect&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">const</span> <span class="type">char</span> msg[] = <span class="string">&quot;hello TCP\n&quot;</span>;</span><br><span class="line">  <span class="keyword">if</span> (send(fd, msg, <span class="keyword">sizeof</span>(msg) - <span class="number">1</span>, <span class="number">0</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;send&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">char</span> buf[BUF];</span><br><span class="line">  <span class="type">ssize_t</span> n = recv(fd, buf, <span class="keyword">sizeof</span>(buf) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;recv&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    buf[n] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, buf);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  close(fd);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -Wall -Wextra -O2 -o tcp_echo_server tcp_echo_server.c</span><br><span class="line">gcc -Wall -Wextra -O2 -o tcp_echo_client tcp_echo_client.c</span><br><span class="line">./tcp_echo_server &amp;</span><br><span class="line">./tcp_echo_client</span><br></pre></td></tr></table></figure>

<p>要点：<strong><code>SO_REUSEADDR</code></strong> 便于快速重启绑定同一端口；<strong><code>send</code> 在循环里凑满</strong>是因为 <strong>TCP</strong> 是字节流，单次 <code>send</code> 不保证一次系统调用就把全部载荷推到内核；<strong><code>recv</code> 返回 0</strong> 表示对端已关闭写方向。</p>
<h2 id="3-Python-服务端-server-py"><a href="#3-Python-服务端-server-py" class="headerlink" title="3. Python 服务端 server.py"></a>3. <strong>Python</strong> 服务端 <code>server.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line">HOST = <span class="string">&quot;127.0.0.1&quot;</span></span><br><span class="line">PORT = <span class="number">9000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> socket.socket(socket.AF_INET, socket.SOCK_STREAM) <span class="keyword">as</span> s:</span><br><span class="line">    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">    s.bind((HOST, PORT))</span><br><span class="line">    s.listen(<span class="number">1</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;listening on&quot;</span>, PORT)</span><br><span class="line">    conn, addr = s.accept()</span><br><span class="line">    <span class="keyword">with</span> conn:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;connected from&quot;</span>, addr)</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            data = conn.recv(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> data:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            conn.sendall(<span class="string">b&quot;echo: &quot;</span> + data)</span><br></pre></td></tr></table></figure>

<h2 id="4-客户端-client-py"><a href="#4-客户端-client-py" class="headerlink" title="4. 客户端 client.py"></a>4. 客户端 <code>client.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line">HOST = <span class="string">&quot;127.0.0.1&quot;</span></span><br><span class="line">PORT = <span class="number">9000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> socket.socket(socket.AF_INET, socket.SOCK_STREAM) <span class="keyword">as</span> s:</span><br><span class="line">    s.connect((HOST, PORT))</span><br><span class="line">    s.sendall(<span class="string">b&quot;hello TCP\n&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(s.recv(<span class="number">1024</span>).decode(), end=<span class="string">&quot;&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python server.py &amp;</span><br><span class="line">python client.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>连接</strong>、<strong>序号&#x2F;ACK&#x2F;重传</strong>、<strong>窗口</strong></td>
</tr>
<tr>
<td>作用</td>
<td>可靠有序字节流；进程寻址靠<strong>端口</strong></td>
</tr>
<tr>
<td>依赖</td>
<td><a href="/2026/05/01/IP/">IP</a></td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>tcpdump</code></strong>、<strong>C <code>socket</code>&#x2F;<code>bind</code>&#x2F;<code>listen</code>&#x2F;<code>accept</code>&#x2F;<code>connect</code></strong>、<strong>Python <code>socket.SOCK_STREAM</code></strong></td>
</tr>
</tbody></table>
<p>调优与内核参数（<strong><code>tcp_tw_reuse</code></strong> 等）以发行版文档为准，避免盲目改生产默认值。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>Socket</tag>
      </tags>
  </entry>
  <entry>
    <title>UDP 协议详解：数据报语义、MTU 与示例代码</title>
    <url>/2026/05/01/Protocol/UDP/</url>
    <content><![CDATA[<p><strong>UDP</strong>（<strong>User Datagram Protocol</strong>）在 <a href="/2026/05/01/IP/">IP</a> 之上提供<strong>无连接</strong>的**数据报（datagram）**服务：<strong>不保证送达、不保证顺序、不维护连接状态</strong>，协议头开销小。下面从「<strong>设计目标</strong> → <strong>无连接与完整通信流程（与套接字 API）</strong> → <strong>首部与校验和</strong> → <strong>MTU 与分片</strong> → <strong>典型应用</strong> → <strong>四元组与套接字</strong> → <strong>抓包</strong> → <strong>C &#x2F; Python 套接字示例</strong>」顺序写清楚；与 <a href="/2026/05/01/TCP/">TCP</a> 的对照见第一节表格。</p>
<hr>
<h1 id="一、设计目标与作用"><a href="#一、设计目标与作用" class="headerlink" title="一、设计目标与作用"></a>一、设计目标与作用</h1><h2 id="1-解决什么问题"><a href="#1-解决什么问题" class="headerlink" title="1. 解决什么问题"></a>1. 解决什么问题</h2><ul>
<li><strong>低延迟</strong>：无握手、无连接状态机，首包即可发数据。  </li>
<li><strong>保留报文边界</strong>：一次 <strong><code>sendto</code></strong> 对应一个 <strong>UDP</strong> 数据报；对端 <strong><code>recvfrom</code></strong> 通常一次收到完整数据报（受缓冲区与实现限制；载荷过大时可能失败或行为依平台）。  </li>
<li><strong>实现简单</strong>：适合 <a href="/2026/05/01/DNS/">DNS</a>、实时音视频、指标上报；<strong>可靠性</strong>由应用或 <a href="/2026/05/01/QUIC/">QUIC</a> 等在 <strong>UDP</strong> 之上补充。</li>
</ul>
<h2 id="2-与-TCP-的本质差异"><a href="#2-与-TCP-的本质差异" class="headerlink" title="2. 与 TCP 的本质差异"></a>2. 与 TCP 的本质差异</h2><table>
<thead>
<tr>
<th>维度</th>
<th><strong>UDP</strong></th>
<th><a href="/2026/05/01/TCP/">TCP</a></th>
</tr>
</thead>
<tbody><tr>
<td>连接</td>
<td>无</td>
<td>有（三次握手 &#x2F; 四次挥手）</td>
</tr>
<tr>
<td>交付</td>
<td>尽力，可能丢、乱序、重复</td>
<td>可靠有序（重传隐藏丢包）</td>
</tr>
<tr>
<td>语义</td>
<td><strong>保留消息边界</strong>（数据报）</td>
<td><strong>字节流</strong>（无消息边界）</td>
</tr>
<tr>
<td>套接字</td>
<td><strong><code>bind</code> + <code>recvfrom</code> &#x2F; <code>sendto</code></strong>，无 <strong><code>listen</code>&#x2F;<code>accept</code></strong></td>
<td><strong><code>listen</code>&#x2F;<code>accept</code>&#x2F;<code>connect</code></strong> 等</td>
</tr>
</tbody></table>
<hr>
<h1 id="二、无连接：通信流程（原理）"><a href="#二、无连接：通信流程（原理）" class="headerlink" title="二、无连接：通信流程（原理）"></a>二、无连接：通信流程（原理）</h1><p><strong>UDP</strong> 不建立会话状态；每个数据报独立路由，「流程」体现在<strong>应用与内核 API</strong> 的调用顺序上。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端（任意时刻可发）              服务端（先绑定端口）</span><br><span class="line">   |    UDP 数据报（目的 IP/端口）    |</span><br><span class="line">   |-------------------------------&gt;|</span><br><span class="line">   |    UDP 数据报（可选响应）        |</span><br><span class="line">   |&lt;-------------------------------|</span><br><span class="line">   |    （无协议层 FIN/ACK；可反复）   |</span><br></pre></td></tr></table></figure>

<h2 id="1-完整通信流程（与套接字-API-对应）"><a href="#1-完整通信流程（与套接字-API-对应）" class="headerlink" title="1. 完整通信流程（与套接字 API 对应）"></a>1. 完整通信流程（与套接字 API 对应）</h2><ol>
<li><strong>服务端</strong>：<code>socket(AF_INET, SOCK_DGRAM, 0)</code> → <code>bind</code> 本地地址与端口 → 循环 <strong><code>recvfrom</code></strong>（每次返回<strong>一整报</strong>与<strong>对端地址 <code>sockaddr</code></strong>）→ <strong><code>sendto</code></strong> 把应答写回该地址。无 <strong><code>listen</code></strong>、无 <strong><code>accept</code></strong>。  </li>
<li><strong>客户端</strong>：<code>socket</code> → 可直接 <strong><code>sendto</code></strong> 指定目的地址；首次发送时内核通常分配<strong>临时源端口</strong>。若需读回应，再 <strong><code>recvfrom</code></strong>（可配合超时或 <strong><code>select</code>&#x2F;<code>poll</code></strong>）。  </li>
<li><strong>传数据</strong>：每个数据报自带目的信息；<strong>无序号、无确认</strong>，不保证与发送顺序一致，也不保证不丢。  </li>
<li><strong>关闭</strong>：<code>close</code> 仅释放本进程套接字资源；<strong>不会在协议层通知对端「连接结束」</strong>（与 <a href="/2026/05/01/TCP/">TCP</a> 的 <strong>FIN</strong> 不同）。</li>
</ol>
<hr>
<h1 id="三、UDP-首部与校验和（原理）"><a href="#三、UDP-首部与校验和（原理）" class="headerlink" title="三、UDP 首部与校验和（原理）"></a>三、UDP 首部与校验和（原理）</h1><p><strong>UDP</strong> 首部固定 <strong>8 字节</strong>：源端口、目的端口、长度、校验和。<strong>IPv6</strong> 下 <strong>UDP</strong> 校验和<strong>必填</strong>；<strong>IPv4</strong> 允许为 <strong>0</strong>（表示未使用校验和，实践中少见）。校验覆盖<strong>伪首部</strong>（含 <strong>IP</strong> 地址与协议号）+ <strong>UDP 首部</strong> + <strong>数据</strong>，用于检测传输错误；<strong>通过校验和 ≠ 应用层已处理完毕</strong>（仍可能丢包、乱序）。</p>
<hr>
<h1 id="四、MTU-与分片（为何要注意报文长度）"><a href="#四、MTU-与分片（为何要注意报文长度）" class="headerlink" title="四、MTU 与分片（为何要注意报文长度）"></a>四、MTU 与分片（为何要注意报文长度）</h1><p>以太网常见 <strong>MTU 1500</strong>，扣掉 <strong>IP</strong> 首部后 <strong>UDP</strong> 载荷不宜过大，否则触发 <strong>IP 分片</strong>：任一片丢失常导致<strong>整组重组失败</strong>，丢包代价被放大。应用层常将 <strong>UDP</strong> 报文控制在 <strong>1200 字节左右</strong>以下（穿越未知路径更稳），或自行分片与重传。</p>
<p><strong>Linux</strong> 探测路径 <strong>MTU</strong>（示例）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping -M <span class="keyword">do</span> -s 1472 -c 1 8.8.8.8</span><br></pre></td></tr></table></figure>

<p><strong><code>-M do</code></strong> 设置 <strong>DF（Don’t Fragment）</strong>，逐步调整 <strong><code>-s</code></strong> 可粗测路径能通过的<strong>最大未分片载荷</strong>（与 <strong>ICMP</strong> 实现有关，解读需谨慎）。</p>
<hr>
<h1 id="五、典型应用"><a href="#五、典型应用" class="headerlink" title="五、典型应用"></a>五、典型应用</h1><table>
<thead>
<tr>
<th>应用</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><a href="/2026/05/01/DNS/">DNS</a></td>
<td>传统 <strong>UDP&#x2F;53</strong>；响应过大时回退 <strong>TCP</strong></td>
</tr>
<tr>
<td>实时音视频</td>
<td>丢旧帧比重传更有意义</td>
</tr>
<tr>
<td><a href="/2026/05/01/QUIC/">QUIC</a> &#x2F; <strong>HTTP&#x2F;3</strong></td>
<td>在 <strong>UDP</strong> 上重建可靠与加密</td>
</tr>
</tbody></table>
<hr>
<h1 id="六、四元组与套接字"><a href="#六、四元组与套接字" class="headerlink" title="六、四元组与套接字"></a>六、四元组与套接字</h1><p>与 <a href="/2026/05/01/TCP/">TCP</a> 相同，<strong>IP 层与常见实现</strong>仍用 <strong>（源 IP，源端口，目的 IP，目的端口）</strong> 区分不同「对话」；区别在于 <strong>UDP</strong> 内核<strong>不维护</strong> <a href="/2026/05/01/TCP/">TCP</a> 那样的连接状态机（无 <strong>ESTABLISHED</strong> &#x2F; <strong>TIME_WAIT</strong> 等）。服务端 <strong><code>bind</code></strong> 固定端口（如 <strong>53</strong>），客户端源端口多为临时端口。</p>
<hr>
<h1 id="七、抓包示例（观察数据报）"><a href="#七、抓包示例（观察数据报）" class="headerlink" title="七、抓包示例（观察数据报）"></a>七、抓包示例（观察数据报）</h1><p>在 <strong>Linux</strong> 上（需装 <strong><code>tcpdump</code></strong>），例如抓取本示例端口 <strong>9001</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> tcpdump -n -i any udp port 9001</span><br></pre></td></tr></table></figure>

<p>另开终端运行下文 <strong>C</strong> 或 <strong>Python</strong> 客户端后，可看到双向 <strong>UDP</strong> 报文。<strong>Wireshark</strong> 中按 <strong>UDP</strong> 过滤并查看载荷即可对比 <a href="/2026/05/01/TCP/">TCP</a> 流式会话。</p>
<hr>
<h1 id="八、编程示例：C（POSIX-Socket）与-Python-回显"><a href="#八、编程示例：C（POSIX-Socket）与-Python-回显" class="headerlink" title="八、编程示例：C（POSIX Socket）与 Python 回显"></a>八、编程示例：<strong>C</strong>（<strong>POSIX</strong> <strong>Socket</strong>）与 <strong>Python</strong> 回显</h1><p>以下在 <strong>Linux</strong> 下用 <strong>IPv4</strong> 回环地址演示；端口 <strong>9001</strong>（与 <a href="/2026/05/01/TCP/">TCP</a> 文内 <strong>9000</strong> 错开，避免同时实验时冲突）。编译 <strong>C</strong> 需 <strong>GCC</strong> 与标准 <strong>POSIX</strong> 头文件。</p>
<h2 id="1-C-服务端-udp-echo-server-c"><a href="#1-C-服务端-udp-echo-server-c" class="headerlink" title="1. C 服务端 udp_echo_server.c"></a>1. <strong>C</strong> 服务端 <code>udp_echo_server.c</code></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 9001</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF 2048</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> fd = socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;socket&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> on = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (setsockopt(fd, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="keyword">sizeof</span>(on)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;setsockopt&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">  <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">  addr.sin_family = AF_INET;</span><br><span class="line">  addr.sin_port = htons(PORT);</span><br><span class="line">  addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (bind(fd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;bind&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;UDP echo on %d\n&quot;</span>, PORT);</span><br><span class="line"></span><br><span class="line">  <span class="type">char</span> buf[BUF];</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">peer</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> plen = <span class="keyword">sizeof</span>(peer);</span><br><span class="line">    <span class="type">ssize_t</span> n = recvfrom(fd, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>, (<span class="keyword">struct</span> sockaddr *)&amp;peer, &amp;plen);</span><br><span class="line">    <span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;recvfrom&quot;</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> prefix[] = <span class="string">&quot;echo: &quot;</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">iovec</span> <span class="title">iov</span>[2];</span></span><br><span class="line">    iov[<span class="number">0</span>].iov_base = (<span class="type">void</span> *)prefix;</span><br><span class="line">    iov[<span class="number">0</span>].iov_len = <span class="keyword">sizeof</span>(prefix) - <span class="number">1</span>;</span><br><span class="line">    iov[<span class="number">1</span>].iov_base = buf;</span><br><span class="line">    iov[<span class="number">1</span>].iov_len = (<span class="type">size_t</span>)n;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">msghdr</span> <span class="title">msg</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;msg, <span class="number">0</span>, <span class="keyword">sizeof</span>(msg));</span><br><span class="line">    msg.msg_name = &amp;peer;</span><br><span class="line">    msg.msg_namelen = plen;</span><br><span class="line">    msg.msg_iov = iov;</span><br><span class="line">    msg.msg_iovlen = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sendmsg(fd, &amp;msg, <span class="number">0</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;sendmsg&quot;</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  close(fd);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>为在<strong>不拷贝</strong>的情况下把 <strong><code>echo: </code></strong> 与对端载荷拼成<strong>一个</strong> <strong>UDP</strong> 数据报，示例使用 <strong><code>sendmsg</code></strong>（分散写）。若应答总长度超过路径 <strong>MTU</strong>，可能返回 <strong><code>EMSGSIZE</code></strong>，生产环境需控制单报长度或路径 <strong>MTU</strong> 发现。</p>
<h2 id="2-C-客户端-udp-echo-client-c"><a href="#2-C-客户端-udp-echo-client-c" class="headerlink" title="2. C 客户端 udp_echo_client.c"></a>2. <strong>C</strong> 客户端 <code>udp_echo_client.c</code></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 9001</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF 2048</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">  <span class="type">int</span> fd = socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;socket&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">dst</span>;</span></span><br><span class="line">  <span class="built_in">memset</span>(&amp;dst, <span class="number">0</span>, <span class="keyword">sizeof</span>(dst));</span><br><span class="line">  dst.sin_family = AF_INET;</span><br><span class="line">  dst.sin_port = htons(PORT);</span><br><span class="line">  <span class="keyword">if</span> (inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;dst.sin_addr) != <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;inet_pton failed\n&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">const</span> <span class="type">char</span> msg[] = <span class="string">&quot;hello UDP\n&quot;</span>;</span><br><span class="line">  <span class="keyword">if</span> (sendto(fd, msg, <span class="keyword">sizeof</span>(msg) - <span class="number">1</span>, <span class="number">0</span>, (<span class="keyword">struct</span> sockaddr *)&amp;dst, <span class="keyword">sizeof</span>(dst)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;sendto&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">char</span> buf[BUF];</span><br><span class="line">  <span class="type">ssize_t</span> n = recvfrom(fd, buf, <span class="keyword">sizeof</span>(buf) - <span class="number">1</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">  <span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;recvfrom&quot;</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    buf[n] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, buf);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  close(fd);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -Wall -Wextra -O2 -o udp_echo_server udp_echo_server.c</span><br><span class="line">gcc -Wall -Wextra -O2 -o udp_echo_client udp_echo_client.c</span><br><span class="line">./udp_echo_server &amp;</span><br><span class="line">./udp_echo_client</span><br></pre></td></tr></table></figure>

<p>要点：<strong><code>recvfrom</code> 一次对应一个数据报</strong>（缓冲足够时）；<strong><code>sendto</code>&#x2F;<code>sendmsg</code></strong> 一次尽力发送<strong>一个</strong>逻辑报文。与 <a href="/2026/05/01/TCP/">TCP</a> 不同，一般<strong>不必</strong>为单次应答写「凑满 <code>send</code>」的循环（除非处理 <strong><code>EINTR</code></strong> 等重试语义）。</p>
<h2 id="3-Python-服务端-udp-echo-py"><a href="#3-Python-服务端-udp-echo-py" class="headerlink" title="3. Python 服务端 udp_echo.py"></a>3. <strong>Python</strong> 服务端 <code>udp_echo.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line">HOST = <span class="string">&quot;127.0.0.1&quot;</span></span><br><span class="line">PORT = <span class="number">9001</span></span><br><span class="line"></span><br><span class="line">sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)</span><br><span class="line">sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">sock.bind((HOST, PORT))</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;UDP echo on&quot;</span>, PORT)</span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    data, addr = sock.recvfrom(<span class="number">2048</span>)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> data:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    sock.sendto(<span class="string">b&quot;echo: &quot;</span> + data, addr)</span><br></pre></td></tr></table></figure>

<h2 id="4-客户端-udp-client-py"><a href="#4-客户端-udp-client-py" class="headerlink" title="4. 客户端 udp_client.py"></a>4. 客户端 <code>udp_client.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line">HOST = <span class="string">&quot;127.0.0.1&quot;</span></span><br><span class="line">PORT = <span class="number">9001</span></span><br><span class="line"></span><br><span class="line">sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)</span><br><span class="line">sock.sendto(<span class="string">b&quot;hello UDP\n&quot;</span>, (HOST, PORT))</span><br><span class="line"><span class="built_in">print</span>(sock.recvfrom(<span class="number">2048</span>)[<span class="number">0</span>].decode(), end=<span class="string">&quot;&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python udp_echo.py &amp;</span><br><span class="line">python udp_client.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="九、小结"><a href="#九、小结" class="headerlink" title="九、小结"></a>九、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td>无连接；<strong>校验和</strong>；<strong>数据报边界</strong>；无协议层会话结束信号</td>
</tr>
<tr>
<td>作用</td>
<td>低延迟、实现简单；与 <strong>IP</strong> 分片策略强相关</td>
</tr>
<tr>
<td>依赖</td>
<td><a href="/2026/05/01/IP/">IP</a></td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>ping -M do</code></strong>、<strong><code>tcpdump</code> <code>udp port</code></strong>、<strong>C <code>recvfrom</code>&#x2F;<code>sendto</code>&#x2F;<code>sendmsg</code></strong>、<strong>Python <code>SOCK_DGRAM</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/DNS/">DNS</a> · <a href="/2026/05/01/QUIC/">QUIC</a> · <a href="/2026/05/01/TCP/">TCP</a></td>
</tr>
</tbody></table>
<p>公网服务须防 <strong>UDP 反射放大</strong> 与滥用；配合 <strong>ACL</strong>、<strong>速率限制</strong> 与合法来源校验。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>Socket</tag>
      </tags>
  </entry>
  <entry>
    <title>WebSocket 详解：握手、帧与 Python 示例</title>
    <url>/2026/05/01/Protocol/WebSocket/</url>
    <content><![CDATA[<p><strong>WebSocket</strong>（<strong>RFC 6455</strong>）在单条 <a href="/2026/05/01/TCP/">TCP</a> 连接上提供<strong>全双工</strong>、低帧头的持久信道：先通过 <a href="/2026/05/01/HTTP/">HTTP</a> <strong>Upgrade</strong> 完成握手，再切换到<strong>二进制帧协议</strong>。下面从「<strong>作用与模型</strong> → <strong>握手与 Sec-WebSocket-Accept 原理</strong> → <strong>帧类型</strong> → <strong>关闭与 Ping</strong> → <strong>Python <code>websockets</code> 示例</strong> → <strong>安全注意</strong>」顺序写清楚；加密传输使用 <strong><code>wss://</code></strong>（依赖 <a href="/2026/05/01/HTTPS/">HTTPS</a> 常见部署经验）。</p>
<hr>
<h1 id="一、作用与模型"><a href="#一、作用与模型" class="headerlink" title="一、作用与模型"></a>一、作用与模型</h1><ul>
<li><strong>服务器推送</strong>：行情、协作编辑、游戏状态无需客户端轮询。  </li>
<li><strong>双向二进制</strong>：除文本帧外可传 <strong>ArrayBuffer</strong> 类数据。  </li>
<li><strong>与 SSE 对比</strong>：<strong>SSE</strong> 多为服务端单向文本流，仍基于 <strong>HTTP</strong>；<strong>WebSocket</strong> 真双向。</li>
</ul>
<hr>
<h1 id="二、握手原理（与-HTTP-的关系）"><a href="#二、握手原理（与-HTTP-的关系）" class="headerlink" title="二、握手原理（与 HTTP 的关系）"></a>二、握手原理（与 HTTP 的关系）</h1><h2 id="1-请求关键首部"><a href="#1-请求关键首部" class="headerlink" title="1. 请求关键首部"></a>1. 请求关键首部</h2><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GET</span> <span class="string">/chat</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Host</span><span class="punctuation">: </span>server.example.com</span><br><span class="line"><span class="attribute">Upgrade</span><span class="punctuation">: </span>websocket</span><br><span class="line"><span class="attribute">Connection</span><span class="punctuation">: </span>Upgrade</span><br><span class="line"><span class="attribute">Sec-WebSocket-Key</span><span class="punctuation">: </span>dGhlIHNhbXBsZSBub25jZQ==</span><br><span class="line"><span class="attribute">Sec-WebSocket-Version</span><span class="punctuation">: </span>13</span><br></pre></td></tr></table></figure>

<h2 id="2-响应"><a href="#2-响应" class="headerlink" title="2. 响应"></a>2. 响应</h2><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">101</span> Switching Protocols</span><br><span class="line"><span class="attribute">Upgrade</span><span class="punctuation">: </span>websocket</span><br><span class="line"><span class="attribute">Connection</span><span class="punctuation">: </span>Upgrade</span><br><span class="line"><span class="attribute">Sec-WebSocket-Accept</span><span class="punctuation">: </span>s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</span><br></pre></td></tr></table></figure>

<p><strong><code>Sec-WebSocket-Accept</code></strong> &#x3D; <strong>Base64( SHA1( Sec-WebSocket-Key + GUID ) )</strong>，其中 <strong>GUID</strong> 为 <strong>RFC 6455</strong> 固定魔数；用于证明服务端理解 <strong>WebSocket</strong> 协议。</p>
<hr>
<h1 id="三、帧（原理要点）"><a href="#三、帧（原理要点）" class="headerlink" title="三、帧（原理要点）"></a>三、帧（原理要点）</h1><table>
<thead>
<tr>
<th>类型 opcode</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><strong>0x1</strong></td>
<td>文本（<strong>UTF-8</strong>）</td>
</tr>
<tr>
<td><strong>0x2</strong></td>
<td>二进制</td>
</tr>
<tr>
<td><strong>0x8</strong></td>
<td>关闭</td>
</tr>
<tr>
<td><strong>0x9 &#x2F; 0xA</strong></td>
<td><strong>Ping &#x2F; Pong</strong></td>
</tr>
</tbody></table>
<p>大消息可分片（<strong>FIN</strong> 位与 <strong>opcode</strong> 组合规则见 <strong>RFC 6455</strong>）。</p>
<hr>
<h1 id="四、Python-示例（需-pip-install-websockets）"><a href="#四、Python-示例（需-pip-install-websockets）" class="headerlink" title="四、Python 示例（需 pip install websockets）"></a>四、Python 示例（需 <code>pip install websockets</code>）</h1><h2 id="服务端-ws-server-py"><a href="#服务端-ws-server-py" class="headerlink" title="服务端 ws_server.py"></a>服务端 <code>ws_server.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> asyncio</span><br><span class="line"><span class="keyword">import</span> websockets</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">handler</span>(<span class="params">ws</span>):</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">for</span> message <span class="keyword">in</span> ws:</span><br><span class="line">        <span class="keyword">await</span> ws.send(<span class="string">f&quot;echo: <span class="subst">&#123;message&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">main</span>():</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">with</span> websockets.serve(handler, <span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">8765</span>):</span><br><span class="line">        <span class="keyword">await</span> asyncio.Future()</span><br><span class="line"></span><br><span class="line">asyncio.run(main())</span><br></pre></td></tr></table></figure>

<h2 id="客户端-ws-client-py"><a href="#客户端-ws-client-py" class="headerlink" title="客户端 ws_client.py"></a>客户端 <code>ws_client.py</code></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> asyncio</span><br><span class="line"><span class="keyword">import</span> websockets</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">main</span>():</span><br><span class="line">    uri = <span class="string">&quot;ws://127.0.0.1:8765&quot;</span></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">with</span> websockets.connect(uri) <span class="keyword">as</span> ws:</span><br><span class="line">        <span class="keyword">await</span> ws.send(<span class="string">&quot;hello&quot;</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="keyword">await</span> ws.recv())</span><br><span class="line"></span><br><span class="line">asyncio.run(main())</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install websockets</span><br><span class="line">python ws_server.py &amp;</span><br><span class="line">python ws_client.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、用-curl-观察握手（若服务端支持）"><a href="#五、用-curl-观察握手（若服务端支持）" class="headerlink" title="五、用 curl 观察握手（若服务端支持）"></a>五、用 <code>curl</code> 观察握手（若服务端支持）</h1><p>部分环境可用 <strong><code>curl --include</code></strong> 配合 <strong>Upgrade</strong>（视 <strong>curl</strong> 版本与服务器实现而定）；开发中更常用浏览器 <strong>DevTools → Network → WS</strong>。</p>
<hr>
<h1 id="六、安全注意"><a href="#六、安全注意" class="headerlink" title="六、安全注意"></a>六、安全注意</h1><ul>
<li>生产使用 <strong><code>wss://</code></strong>；校验 <strong>Origin</strong>；首帧或握手带<strong>不可猜测 Token</strong>。  </li>
<li>限流与最大消息长度，防内存与 <strong>CPU</strong> 耗尽。</li>
</ul>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>原理</td>
<td><strong>HTTP 101</strong> 升级 → <strong>帧</strong>多路复用在单 <strong>TCP</strong> 上</td>
</tr>
<tr>
<td>作用</td>
<td>全双工实时通信</td>
</tr>
<tr>
<td>示例</td>
<td><strong><code>websockets</code></strong> 库</td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/HTTP/">HTTP</a> · <a href="/2026/05/01/HTTPS/">HTTPS</a></td>
</tr>
</tbody></table>
<p>反向代理（<strong>Nginx</strong>、<strong>CORS</strong>、<strong>Cookie</strong> 域）配置错误是最常见握手失败原因之一。</p>
]]></content>
      <categories>
        <category>Protocol</category>
      </categories>
      <tags>
        <tag>Protocol</tag>
        <tag>WebSocket</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch</title>
    <url>/2026/05/01/PyTorch/PyTorch/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>PyTorch</category>
      </categories>
      <tags>
        <tag>PyTorch</tag>
      </tags>
  </entry>
  <entry>
    <title>Django</title>
    <url>/2026/05/01/Python/Django/</url>
    <content><![CDATA[<p><strong>Django</strong> 是 Python 生态里常用的全栈 Web 框架，自带 ORM、管理后台、路由与模板。本文假定本机已安装 <strong>uv</strong>，且已有可用的 <strong>Python</strong> 解释器（本站约定 <strong>3.14.3</strong>；<strong>uv</strong> 的安装、<strong><code>uv python install</code> &#x2F; <code>uv python pin</code></strong> 见同栏《Python的UV工具》《UV从0到1创建项目》）。下文只写在该前提下用 <strong>uv</strong> 初始化项目、安装 <strong>Django</strong>、创建工程与跑开发服务器。</p>
<hr>
<h1 id="一、前提"><a href="#一、前提" class="headerlink" title="一、前提"></a>一、前提</h1><ul>
<li><strong>uv</strong> 可用：终端执行 <strong><code>uv --version</code></strong> 有正常输出。  </li>
<li><strong>Python</strong> 已就绪（推荐 <strong>3.14.3</strong>）；进入新项目目录后仍可用 <strong><code>uv python pin 3.14.3</code></strong> 写入 <strong><code>.python-version</code></strong>，与仓库约定一致。  </li>
<li>若尚未安装 <strong>uv</strong> 或未拉取解释器，请先完成上述两篇 <strong>UV</strong> 文档中的步骤。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、用-uv-初始化目录并安装-Django"><a href="#二、用-uv-初始化目录并安装-Django" class="headerlink" title="二、用 uv 初始化目录并安装 Django"></a>二、用 uv 初始化目录并安装 Django</h1><p>在放项目的父目录执行（示例项目名 <strong><code>django-uv-demo</code></strong>，可改名）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> django-uv-demo &amp;&amp; <span class="built_in">cd</span> django-uv-demo</span><br><span class="line">uv init</span><br></pre></td></tr></table></figure>

<p>若 <strong><code>uv init</code></strong> 生成了示例 <strong><code>main.py</code></strong>，可与下文 <strong>Django</strong> 工程并存；介意根目录杂乱时可自行删除，不影响 <strong><code>manage.py</code></strong>。</p>
<p>固定本仓库使用的 <strong>Python</strong> 版本（可选但推荐）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv python pin 3.14.3</span><br></pre></td></tr></table></figure>

<p>安装 <strong>Django</strong> 并写入 <strong><code>pyproject.toml</code> &#x2F; <code>uv.lock</code></strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add django</span><br></pre></td></tr></table></figure>

<p>之后他人克隆仓库后，在同一目录执行 <strong><code>uv sync</code></strong> 即可按锁文件还原环境。</p>
<hr>
<h1 id="三、创建工程与数据库迁移"><a href="#三、创建工程与数据库迁移" class="headerlink" title="三、创建工程与数据库迁移"></a>三、创建工程与数据库迁移</h1><p>在当前目录生成工程（内层包名示例为 <strong><code>config</code></strong>，可改为 <strong><code>mysite</code></strong> 等）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run django-admin startproject config .</span><br></pre></td></tr></table></figure>

<p>目录要点：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">django-uv-demo/</span><br><span class="line">├── pyproject.toml</span><br><span class="line">├── uv.lock</span><br><span class="line">├── manage.py</span><br><span class="line">└── config/              # 工程包：settings、urls、wsgi、asgi</span><br><span class="line">    ├── settings.py</span><br><span class="line">    ├── urls.py</span><br><span class="line">    └── ...</span><br></pre></td></tr></table></figure>

<p>首次建库表：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run python manage.py migrate</span><br></pre></td></tr></table></figure>

<p>新建应用（示例名 <strong><code>polls</code></strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run python manage.py startapp polls</span><br></pre></td></tr></table></figure>

<p>在 <strong><code>config/settings.py</code></strong> 的 <strong><code>INSTALLED_APPS</code></strong> 里注册 <strong><code>polls</code></strong>，再在 <strong><code>config/urls.py</code></strong> 里 <strong><code>include</code></strong> 应用路由（具体写法见 <a href="https://docs.djangoproject.com/zh-hans/4.2/intro/tutorial01/">Django 官方教程</a>）。</p>
<hr>
<h1 id="四、运行开发服务器"><a href="#四、运行开发服务器" class="headerlink" title="四、运行开发服务器"></a>四、运行开发服务器</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run python manage.py runserver</span><br></pre></td></tr></table></figure>

<p>默认：<strong><a href="http://127.0.0.1:8000/">http://127.0.0.1:8000/</a></strong> 。改端口示例：<strong><code>uv run python manage.py runserver 8080</code></strong>。</p>
<hr>
<h1 id="五、小结"><a href="#五、小结" class="headerlink" title="五、小结"></a>五、小结</h1><table>
<thead>
<tr>
<th>场景</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装依赖（含锁定）</td>
<td><strong><code>uv add django</code></strong></td>
</tr>
<tr>
<td>同步环境（克隆后）</td>
<td><strong><code>uv sync</code></strong></td>
</tr>
<tr>
<td>创建工程</td>
<td><strong><code>uv run django-admin startproject &lt;包名&gt; .</code></strong></td>
</tr>
<tr>
<td>迁移</td>
<td><strong><code>uv run python manage.py migrate</code></strong></td>
</tr>
<tr>
<td>开发服务</td>
<td><strong><code>uv run python manage.py runserver</code></strong></td>
</tr>
<tr>
<td>新建应用</td>
<td><strong><code>uv run python manage.py startapp &lt;应用名&gt;</code></strong></td>
</tr>
</tbody></table>
<p>更深入的模型、表单、认证、部署（<strong>Gunicorn</strong>、<strong>Docker</strong> 等）以 <a href="https://docs.djangoproject.com/">Django 文档</a> 为准。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Django</tag>
      </tags>
  </entry>
  <entry>
    <title>FastAPI</title>
    <url>/2026/05/01/Python/FastAPI/</url>
    <content><![CDATA[<p><strong>FastAPI</strong> 是基于类型标注的 <strong>Python</strong> Web 框架，适合写 <strong>API</strong>，常与 <strong>Uvicorn</strong>（<strong>ASGI</strong> 服务器）一起用。本文假定本机已安装 <strong>uv</strong>，且已有可用的 <strong>Python</strong> 解释器（本站约定 <strong>3.14.3</strong>；<strong>uv</strong> 与解释器准备见同栏《Python的UV工具》《UV从0到1创建项目》）。下文只写在该前提下用 <strong>uv</strong> 建项、安装依赖、编写最小应用并用 <strong>Uvicorn</strong> 本地运行。</p>
<hr>
<h1 id="一、前提"><a href="#一、前提" class="headerlink" title="一、前提"></a>一、前提</h1><ul>
<li><strong>uv</strong> 可用：<strong><code>uv --version</code></strong> 正常。  </li>
<li><strong>Python</strong> 已就绪（推荐 <strong>3.14.3</strong>）；新项目里可 <strong><code>uv python pin 3.14.3</code></strong> 固定版本。  </li>
<li>若环境未就绪，请先阅读上述两篇 <strong>UV</strong> 文档。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、用-uv-初始化项目并安装依赖"><a href="#二、用-uv-初始化项目并安装依赖" class="headerlink" title="二、用 uv 初始化项目并安装依赖"></a>二、用 uv 初始化项目并安装依赖</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> fastapi-uv-demo &amp;&amp; <span class="built_in">cd</span> fastapi-uv-demo</span><br><span class="line">uv init</span><br><span class="line">uv python pin 3.14.3</span><br><span class="line">uv add fastapi <span class="string">&quot;uvicorn[standard]&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>fastapi</code></strong>：框架本体。  </li>
<li><strong><code>uvicorn[standard]</code></strong>：本地&#x2F;生产常用的 <strong>ASGI</strong> 服务进程；<strong><code>[standard]</code></strong> 包含 <strong><code>httptools</code>、<code>uvloop</code></strong> 等加速依赖（在 <strong>Windows</strong> 上部分组件会自动降级，属正常行为）。</li>
</ul>
<hr>
<h1 id="三、最小应用（main-py）"><a href="#三、最小应用（main-py）" class="headerlink" title="三、最小应用（main.py）"></a>三、最小应用（<code>main.py</code>）</h1><p>在项目根目录编辑 <strong><code>main.py</code></strong>（若 <strong><code>uv init</code></strong> 已有同名文件，可直接整体替换为下面内容）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">read_root</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;Hello&quot;</span>: <span class="string">&quot;World&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/items/&#123;item_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">read_item</span>(<span class="params">item_id: <span class="built_in">int</span>, q: <span class="built_in">str</span> | <span class="literal">None</span> = <span class="literal">None</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;item_id&quot;</span>: item_id, <span class="string">&quot;q&quot;</span>: q&#125;</span><br></pre></td></tr></table></figure>

<p>目录示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastapi-uv-demo/</span><br><span class="line">├── pyproject.toml</span><br><span class="line">├── uv.lock</span><br><span class="line">└── main.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、本地运行（热重载）"><a href="#四、本地运行（热重载）" class="headerlink" title="四、本地运行（热重载）"></a>四、本地运行（热重载）</h1><p>开发模式（改代码自动重载）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run uvicorn main:app --reload</span><br></pre></td></tr></table></figure>

<p>浏览器访问：<strong><a href="http://127.0.0.1:8000/">http://127.0.0.1:8000/</a></strong> ；路径 <strong><code>/items/42?q=test</code></strong> 可试查询参数。</p>
<p><strong>FastAPI</strong> 自带交互文档：<strong><a href="http://127.0.0.1:8000/docs">http://127.0.0.1:8000/docs</a></strong>（<strong>Swagger UI</strong>）、<strong><a href="http://127.0.0.1:8000/redoc">http://127.0.0.1:8000/redoc</a></strong>（<strong>ReDoc</strong>）。</p>
<hr>
<h1 id="五、小结"><a href="#五、小结" class="headerlink" title="五、小结"></a>五、小结</h1><table>
<thead>
<tr>
<th>场景</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装依赖</td>
<td><strong><code>uv add fastapi &quot;uvicorn[standard]&quot;</code></strong></td>
</tr>
<tr>
<td>同步环境</td>
<td><strong><code>uv sync</code></strong></td>
</tr>
<tr>
<td>开发运行</td>
<td><strong><code>uv run uvicorn main:app --reload</code></strong></td>
</tr>
<tr>
<td>指定 host&#x2F;port</td>
<td><strong><code>uv run uvicorn main:app --host 0.0.0.0 --port 8080</code></strong></td>
</tr>
</tbody></table>
<p>依赖注入、后台任务、<strong>WebSocket</strong>、与 <strong>SQLModel</strong> &#x2F; <strong>SQLAlchemy</strong> 集成等见 <a href="https://fastapi.tiangolo.com/zh/">FastAPI 文档</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>FastAPI</tag>
      </tags>
  </entry>
  <entry>
    <title>Flask</title>
    <url>/2026/05/01/Python/Flask/</url>
    <content><![CDATA[<p><strong>Flask</strong> 是轻量级 <strong>Python Web</strong> 框架，核心小而可扩展，适合 <strong>API</strong> 与中小型站点。本文假定本机已安装 <strong>uv</strong>，且已有可用的 <strong>Python</strong> 解释器（本站约定 <strong>3.14.3</strong>；<strong>uv</strong> 与解释器准备见同栏《Python的UV工具》《UV从0到1创建项目》）。下文只写在该前提下用 <strong>uv</strong> 建项、安装 <strong>Flask</strong>、编写最小应用并用开发服务器运行。</p>
<hr>
<h1 id="一、前提"><a href="#一、前提" class="headerlink" title="一、前提"></a>一、前提</h1><ul>
<li><strong>uv</strong> 可用：<strong><code>uv --version</code></strong> 正常。  </li>
<li><strong>Python</strong> 已就绪（推荐 <strong>3.14.3</strong>）；新项目里可 <strong><code>uv python pin 3.14.3</code></strong> 固定版本。  </li>
<li>若环境未就绪，请先阅读上述两篇 <strong>UV</strong> 文档。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、用-uv-初始化项目并安装-Flask"><a href="#二、用-uv-初始化项目并安装-Flask" class="headerlink" title="二、用 uv 初始化项目并安装 Flask"></a>二、用 uv 初始化项目并安装 Flask</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> flask-uv-demo &amp;&amp; <span class="built_in">cd</span> flask-uv-demo</span><br><span class="line">uv init</span><br><span class="line">uv python pin 3.14.3</span><br><span class="line">uv add flask</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="三、最小应用（app-py）"><a href="#三、最小应用（app-py）" class="headerlink" title="三、最小应用（app.py）"></a>三、最小应用（<code>app.py</code>）</h1><p>在项目根目录新建 <strong><code>app.py</code></strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&quot;/&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">hello</span>():</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;&lt;p&gt;Hello, Flask!&lt;/p&gt;&quot;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&quot;/api/hello&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">api_hello</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;message&quot;</span>: <span class="string">&quot;Hello, API&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>目录示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flask-uv-demo/</span><br><span class="line">├── pyproject.toml</span><br><span class="line">├── uv.lock</span><br><span class="line">└── app.py</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、运行开发服务器"><a href="#四、运行开发服务器" class="headerlink" title="四、运行开发服务器"></a>四、运行开发服务器</h1><p><strong>Flask 3.x</strong> 推荐用 <strong><code>flask</code></strong> 命令（通过 <strong><code>--app</code></strong> 指向应用实例）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run flask --app app run --debug</span><br></pre></td></tr></table></figure>

<p>默认：<strong><a href="http://127.0.0.1:5000/">http://127.0.0.1:5000/</a></strong> 。改端口示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run flask --app app run --debug --port 8000</span><br></pre></td></tr></table></figure>

<p><strong><code>--debug</code></strong> 会打开调试器与自动重载（仅开发环境使用；勿用于公网生产）。</p>
<hr>
<h1 id="五、小结"><a href="#五、小结" class="headerlink" title="五、小结"></a>五、小结</h1><table>
<thead>
<tr>
<th>场景</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装依赖</td>
<td><strong><code>uv add flask</code></strong></td>
</tr>
<tr>
<td>同步环境</td>
<td><strong><code>uv sync</code></strong></td>
</tr>
<tr>
<td>开发运行</td>
<td><strong><code>uv run flask --app app run --debug</code></strong></td>
</tr>
<tr>
<td>指定端口</td>
<td>同上，加 <strong><code>--port 8000</code></strong></td>
</tr>
</tbody></table>
<p>蓝图、<strong>Jinja</strong> 模板、<strong>SQLAlchemy</strong>、生产部署（<strong>Gunicorn</strong> 等）见 <a href="https://flask.palletsprojects.com/">Flask 文档</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Flask</tag>
      </tags>
  </entry>
  <entry>
    <title>Jupyter 入门：经典 Notebook、安装与内核</title>
    <url>/2026/05/01/Python/Jupyter/</url>
    <content><![CDATA[<p><strong>Jupyter</strong> 指以浏览器为界面、<strong>单元格</strong>为单位交互运行代码的笔记本生态；日常所说的「<strong>Notebook</strong>」多指经典 <strong><code>jupyter notebook</code></strong> 界面（文档扩展名 <strong><code>.ipynb</code></strong>）。下面从「<strong>官网与组成</strong> → <strong>环境准备</strong> → <strong>安装与启动</strong> → <strong>内核与 <code>.ipynb</code> 要点</strong> → <strong>与 JupyterLab 的关系</strong>」顺序写清楚；命令在 <strong>Linux &#x2F; macOS</strong> 终端或 <strong>Windows PowerShell</strong> 中执行（已安装 <strong>Python 3.10+</strong> 为前提）。多用户与统一入口见站内 <a href="/2026/05/01/JupyterHub/">JupyterHub 入门</a>；下一代单用户界面见 <a href="/2026/05/01/JupyterLab/">JupyterLab 入门</a>。</p>
<hr>
<h1 id="一、官网与组成"><a href="#一、官网与组成" class="headerlink" title="一、官网与组成"></a>一、官网与组成</h1><ul>
<li><strong>Jupyter 项目主页</strong>：<a href="https://jupyter.org/">https://jupyter.org/</a>  </li>
<li><strong>Notebook 文档（经典界面）</strong>：<a href="https://jupyter-notebook.readthedocs.io/">https://jupyter-notebook.readthedocs.io/</a>  </li>
<li><strong><code>.ipynb</code> 格式说明</strong>：以当前 <strong>nbformat</strong> 文档为准（Notebook 文件为 <strong>JSON</strong>，含代码单元、输出与元数据）。</li>
</ul>
<p><strong>Jupyter</strong> 与具体「前端」解耦：<strong>Notebook</strong> 与 <strong>JupyterLab</strong> 都能打开同一 <strong><code>.ipynb</code></strong>；底层常由 <strong><code>jupyter_server</code></strong> 提供 <strong>HTTP</strong> 与 <strong>WebSocket</strong> 服务。团队若只需<strong>单用户本机或单容器</strong>写分析，<strong>Notebook</strong> 或 <strong>JupyterLab</strong> 二选一即可。</p>
<hr>
<h1 id="二、环境准备"><a href="#二、环境准备" class="headerlink" title="二、环境准备"></a>二、环境准备</h1><ol>
<li>安装 <strong>Python 3.10+</strong>，终端可执行 <strong><code>python --version</code></strong>（或 <strong><code>python3</code></strong>，下文统一写 <strong><code>python</code></strong>，请按本机替换）。  </li>
<li>建议使用<strong>虚拟环境</strong>（<strong><code>venv</code></strong>、<strong><code>conda</code></strong> 或站内 <strong>uv</strong> 工具），避免把 <strong>Jupyter</strong> 装进全局 <strong>site-packages</strong> 难以清理。</li>
</ol>
<hr>
<h1 id="三、安装与启动"><a href="#三、安装与启动" class="headerlink" title="三、安装与启动"></a>三、安装与启动</h1><h2 id="1-安装-notebook-包"><a href="#1-安装-notebook-包" class="headerlink" title="1. 安装 notebook 包"></a>1. 安装 <code>notebook</code> 包</h2><p>在已激活的虚拟环境中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install -U pip</span><br><span class="line">python -m pip install notebook</span><br></pre></td></tr></table></figure>

<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter notebook --version</span><br></pre></td></tr></table></figure>

<h2 id="2-启动经典-Notebook"><a href="#2-启动经典-Notebook" class="headerlink" title="2. 启动经典 Notebook"></a>2. 启动经典 Notebook</h2><p>在项目目录下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter notebook</span><br></pre></td></tr></table></figure>

<p>终端会打印带 <strong><code>token=</code></strong> 的本地 <strong>URL</strong>（默认 <strong><code>127.0.0.1:8888</code></strong>），用浏览器打开即可。首次运行若浏览器未自动打开，请<strong>手动复制完整带 token 的链接</strong>。</p>
<h2 id="3-指定端口与仅本机监听（推荐习惯）"><a href="#3-指定端口与仅本机监听（推荐习惯）" class="headerlink" title="3. 指定端口与仅本机监听（推荐习惯）"></a>3. 指定端口与仅本机监听（推荐习惯）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter notebook --port 8888 --no-browser --ServerApp.ip=127.0.0.1</span><br></pre></td></tr></table></figure>

<p><strong><code>--ServerApp.ip=0.0.0.0</code></strong> 会监听所有网卡，<strong>仅在可信内网或已配反向代理与认证时使用</strong>；公网暴露须配合 <strong>HTTPS</strong>、<strong>强鉴权</strong> 或专用网关（多用户场景更适合 <strong>JupyterHub</strong>）。</p>
<hr>
<h1 id="四、内核与-ipynb-要点"><a href="#四、内核与-ipynb-要点" class="headerlink" title="四、内核与 .ipynb 要点"></a>四、内核与 <code>.ipynb</code> 要点</h1><h2 id="1-内核（Kernel）"><a href="#1-内核（Kernel）" class="headerlink" title="1. 内核（Kernel）"></a>1. 内核（Kernel）</h2><p>内核是<strong>执行代码的进程</strong>；<strong>Python</strong> 环境装了 <strong><code>ipykernel</code></strong> 并注册后，Notebook 右上角可选该内核。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install ipykernel</span><br><span class="line">python -m ipykernel install --user --name=myenv --display-name=<span class="string">&quot;Python (myenv)&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="2-单元格类型"><a href="#2-单元格类型" class="headerlink" title="2. 单元格类型"></a>2. 单元格类型</h2><p>常见为 <strong>Code</strong>（运行代码）与 <strong>Markdown</strong>（写说明）；<strong>Shift+Enter</strong> 执行当前单元并跳到下一单元（具体快捷键以界面 <strong>Help</strong> 为准）。</p>
<h2 id="3-保存与版本"><a href="#3-保存与版本" class="headerlink" title="3. 保存与版本"></a>3. 保存与版本</h2><p><strong><code>.ipynb</code></strong> 含输出时体积会变大；纳入 <strong>Git</strong> 前团队可约定是否<strong>清空输出</strong>再提交，或使用 <strong><code>nbstripout</code></strong> 等工具（按需查阅文档）。</p>
<hr>
<h1 id="五、与-JupyterLab-的取舍"><a href="#五、与-JupyterLab-的取舍" class="headerlink" title="五、与 JupyterLab 的取舍"></a>五、与 JupyterLab 的取舍</h1><table>
<thead>
<tr>
<th>需求</th>
<th>建议</th>
</tr>
</thead>
<tbody><tr>
<td>习惯经典菜单、轻量界面</td>
<td>继续用 <strong><code>jupyter notebook</code></strong></td>
</tr>
<tr>
<td>多文件标签、内置终端、扩展生态</td>
<td>使用 <a href="/2026/05/01/JupyterLab/">JupyterLab</a>（<strong><code>jupyter lab</code></strong>）</td>
</tr>
<tr>
<td>多用户、登录与配额</td>
<td>使用 <a href="/2026/05/01/JupyterHub/">JupyterHub</a></td>
</tr>
</tbody></table>
<p>二者可<strong>同一虚拟环境</strong>内并存：同时安装 <strong><code>notebook</code></strong> 与 <strong><code>jupyterlab</code></strong> 并不冲突。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>定位</td>
<td>浏览器中的交互式笔记本；经典命令 <strong><code>jupyter notebook</code></strong></td>
</tr>
<tr>
<td>安装</td>
<td><strong><code>python -m pip install notebook</code></strong>；常配合 <strong><code>ipykernel</code></strong></td>
</tr>
<tr>
<td>启动</td>
<td>注意 <strong>token</strong> 与 <strong><code>127.0.0.1</code></strong> 监听范围</td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/JupyterLab/">JupyterLab</a> · <a href="/2026/05/01/JupyterHub/">JupyterHub</a></td>
</tr>
</tbody></table>
<p>更完整的配置项（密码、证书、扩展目录）以 <strong><code>jupyter_server</code></strong> &#x2F; <strong>Notebook</strong> 当前大版本文档为准。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Jupyter</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>JupyterHub 入门：多用户架构与部署要点</title>
    <url>/2026/05/01/Python/JupyterHub/</url>
    <content><![CDATA[<p><strong>JupyterHub</strong> 把 <strong>Jupyter Notebook &#x2F; JupyterLab</strong> 扩展为<strong>多用户</strong>场景：统一登录后，为每位用户启动<strong>独立的单用户服务器</strong>（<strong>Notebook</strong> 或 <strong>Lab</strong>），适合高校课程、企业内部分析平台与科研组共享集群。下面从「<strong>官网与适用边界</strong> → <strong>架构角色</strong> → <strong>本地最小试用</strong> → <strong>配置与安全</strong> → <strong>生产与 Kubernetes</strong>」顺序写清楚；单用户写 <strong><code>.ipynb</code></strong> 仍建议先看 <a href="/2026/05/01/Jupyter/">Jupyter 入门</a> 与 <a href="/2026/05/01/JupyterLab/">JupyterLab 入门</a>。<strong>Hub</strong> 的认证、<strong>HTTPS</strong> 与配额属于运维范畴，生产请以官方文档与内控为准。</p>
<hr>
<h1 id="一、官网与适用边界"><a href="#一、官网与适用边界" class="headerlink" title="一、官网与适用边界"></a>一、官网与适用边界</h1><ul>
<li><strong>JupyterHub 文档</strong>：<a href="https://jupyterhub.readthedocs.io/">https://jupyterhub.readthedocs.io/</a>  </li>
<li><strong>教程与部署指南</strong>：<a href="https://jupyterhub.readthedocs.io/en/stable/tutorial/index.html">https://jupyterhub.readthedocs.io/en/stable/tutorial/index.html</a>  </li>
<li><strong>多用户「零到部署」参考（Kubernetes）</strong>：<a href="https://z2jh.jupyter.org/">https://z2jh.jupyter.org/</a></li>
</ul>
<p><strong>JupyterHub</strong> 解决的是「<strong>谁可以登录、登录后跑哪个镜像&#x2F;环境、如何回收资源</strong>」；不替代 <strong>Python</strong> 依赖管理，也不等于 <strong>Kubernetes</strong> 本身——但生产上常与 <strong>K8s</strong>、<strong>反向代理</strong> 组合使用。</p>
<hr>
<h1 id="二、架构角色（概念）"><a href="#二、架构角色（概念）" class="headerlink" title="二、架构角色（概念）"></a>二、架构角色（概念）</h1><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Hub</strong></td>
<td>处理登录、会话与<strong>启动&#x2F;停止</strong>用户服务器</td>
</tr>
<tr>
<td><strong>Proxy</strong></td>
<td>把浏览器请求<strong>按用户路径</strong>转发到对应单用户进程（常见为 <strong>configurable-http-proxy</strong>）</td>
</tr>
<tr>
<td><strong>Single-user server</strong></td>
<td>每个用户自己的 <strong>Jupyter Server</strong>（界面多为 <strong>JupyterLab</strong> 或 <strong>Notebook</strong>）</td>
</tr>
<tr>
<td><strong>Spawner</strong></td>
<td>决定「如何启动」单用户进程（本机进程、<strong>Docker</strong>、<strong>K8s Pod</strong> 等）</td>
</tr>
</tbody></table>
<p>理清「<strong>Hub 进程</strong>」与「<strong>用户内核进程</strong>」分离，有助于排查端口、<strong>Cookie</strong> 与 <strong>WebSocket</strong> 转发问题。</p>
<hr>
<h1 id="三、本地最小试用（pip）"><a href="#三、本地最小试用（pip）" class="headerlink" title="三、本地最小试用（pip）"></a>三、本地最小试用（pip）</h1><p>仅用于<strong>本机理解流程</strong>，不代表生产安全配置。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install -U pip</span><br><span class="line">python -m pip install jupyterhub notebook</span><br></pre></td></tr></table></figure>

<p>生成默认配置并查看路径提示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyterhub --generate-config</span><br></pre></td></tr></table></figure>

<p>在同一虚拟环境、<strong>可接受 Hub 监听非 localhost 的机器</strong>上首次试跑（具体子命令与选项以当前版本 <strong><code>jupyterhub --help</code></strong> 为准）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyterhub</span><br></pre></td></tr></table></figure>

<p>默认可能监听 <strong><code>0.0.0.0:8000</code></strong> 等（随版本变化），<strong>请勿在公网无认证环境长期运行</strong>。首次使用请阅读终端说明与 <strong>RTD</strong> 中 <strong>Getting Started</strong> 的安全警告。</p>
<hr>
<h1 id="四、配置与安全要点"><a href="#四、配置与安全要点" class="headerlink" title="四、配置与安全要点"></a>四、配置与安全要点</h1><h2 id="1-jupyterhub-config-py"><a href="#1-jupyterhub-config-py" class="headerlink" title="1. jupyterhub_config.py"></a>1. <code>jupyterhub_config.py</code></h2><p><strong><code>--generate-config</code></strong> 会生成 <strong><code>jupyterhub_config.py</code></strong> 模板，其中可设置：</p>
<ul>
<li><strong>认证</strong>：<strong>PAM</strong>（类 Unix）、<strong>OAuth</strong>（<strong>GitHub &#x2F; OIDC</strong> 等，需注册应用与回调 <strong>URL</strong>）  </li>
<li><strong>Spawner</strong>：默认 <strong><code>LocalProcessSpawner</code></strong> 适合单机试用；<strong>DockerSpawner</strong>、<strong>KubeSpawner</strong> 用于隔离与弹性  </li>
<li><strong><code>c.JupyterHub.bind_url</code></strong>：监听地址与端口，与反向代理 scheme 对齐</li>
</ul>
<h2 id="2-HTTPS-与-Cookie"><a href="#2-HTTPS-与-Cookie" class="headerlink" title="2. HTTPS 与 Cookie"></a>2. HTTPS 与 Cookie</h2><p>浏览器 <strong>Cookie</strong>、<strong>Secure</strong> 与 <strong><code>SameSite</code></strong> 与是否 <strong>HTTPS</strong> 强相关；对外服务应在 <strong>Hub</strong> 前放置 <strong>TLS</strong> 终结（<strong>Nginx</strong>、<strong>Traefik</strong> 或云负载均衡），并按文档设置 <strong><code>c.JupyterHub.cookie_secret</code></strong> 等密钥文件权限。</p>
<h2 id="3-资源与隔离"><a href="#3-资源与隔离" class="headerlink" title="3. 资源与隔离"></a>3. 资源与隔离</h2><p>多用户共享一台机时，注意 <strong>CPU &#x2F; 内存 &#x2F; 磁盘</strong> 配额与<strong>恶意代码</strong>风险；生产常用<strong>容器或 K8s</strong> 做进程隔离，并限制出站网络。</p>
<hr>
<h1 id="五、生产与-Kubernetes"><a href="#五、生产与-Kubernetes" class="headerlink" title="五、生产与 Kubernetes"></a>五、生产与 Kubernetes</h1><ul>
<li><strong>单机 &#x2F; 虚拟机</strong>：<strong>PAM + Nginx + HTTPS + systemd</strong> 管理 <strong>Hub</strong>，<strong>Spawner</strong> 选 <strong>Docker</strong> 等。  </li>
<li><strong>Kubernetes</strong>：参考 <strong>Zero to JupyterHub</strong>（<strong>Z2JH</strong>）：<a href="https://z2jh.jupyter.org/">https://z2jh.jupyter.org/</a> ，用 <strong>Helm</strong> 维护 <strong>Hub、Proxy、用户镜像</strong> 与存储类。</li>
</ul>
<p>上线前务必完成：<strong>身份源审计</strong>、<strong>镜像供应链</strong>、<strong>持久化 home</strong>、<strong>升级与回滚策略</strong>。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>定位</td>
<td>多用户 <strong>Jupyter</strong> 入口；<strong>Hub + Proxy + 单用户服务</strong></td>
</tr>
<tr>
<td>试用</td>
<td><strong><code>pip install jupyterhub notebook</code></strong> → <strong><code>jupyterhub --generate-config</code></strong> → 阅读官方 <strong>Getting Started</strong></td>
</tr>
<tr>
<td>安全</td>
<td><strong>HTTPS</strong>、强认证、<strong>cookie_secret</strong>；避免公网裸 <strong>Hub</strong></td>
</tr>
<tr>
<td>单用户基础</td>
<td><a href="/2026/05/01/Jupyter/">Jupyter</a> · <a href="/2026/05/01/JupyterLab/">JupyterLab</a></td>
</tr>
<tr>
<td>生产编排</td>
<td><strong>Z2JH</strong>（<strong>K8s</strong>）或等价 <strong>IaC</strong> 方案</td>
</tr>
</tbody></table>
<p><strong>JupyterHub</strong> 大版本升级会牵动 <strong>Spawner</strong>、<strong>OAuthenticator</strong> 等周边包，升级前请在预发环境对照发行说明做回归。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Jupyter</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>JupyterLab 入门：安装、工作区与扩展</title>
    <url>/2026/05/01/Python/JupyterLab/</url>
    <content><![CDATA[<p><strong>JupyterLab</strong> 是 <strong>Jupyter</strong> 项目的下一代<strong>单用户</strong> Web 界面：多文档标签、文件浏览器、终端与笔记本同屏工作流，底层同样基于 <strong><code>jupyter_server</code></strong>。下面从「<strong>官网与定位</strong> → <strong>安装</strong> → <strong>启动与安全习惯</strong> → <strong>工作区与常用面板</strong> → <strong>扩展</strong> → <strong>与 Notebook &#x2F; Hub 的关系</strong>」顺序写清楚；命令在 <strong>Linux &#x2F; macOS</strong> 或 <strong>Windows</strong> 下 <strong>Python 3.10+</strong> 虚拟环境中执行。经典界面见 <a href="/2026/05/01/Jupyter/">Jupyter 入门</a>；多用户入口见 <a href="/2026/05/01/JupyterHub/">JupyterHub 入门</a>。</p>
<hr>
<h1 id="一、官网与定位"><a href="#一、官网与定位" class="headerlink" title="一、官网与定位"></a>一、官网与定位</h1><ul>
<li><strong>JupyterLab 文档</strong>：<a href="https://jupyterlab.readthedocs.io/">https://jupyterlab.readthedocs.io/</a>  </li>
<li><strong>Jupyter 总览</strong>：<a href="https://jupyter.org/">https://jupyter.org/</a></li>
</ul>
<p><strong>JupyterLab</strong> 与 <strong><code>jupyter notebook</code></strong> 共享 <strong><code>.ipynb</code></strong> 与内核模型；迁移时一般<strong>无需改文件格式</strong>，主要是换启动命令与操作习惯。</p>
<hr>
<h1 id="二、安装"><a href="#二、安装" class="headerlink" title="二、安装"></a>二、安装</h1><p>在已激活的虚拟环境中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install -U pip</span><br><span class="line">python -m pip install jupyterlab</span><br></pre></td></tr></table></figure>

<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter lab --version</span><br></pre></td></tr></table></figure>

<p>若与 <strong>经典 Notebook</strong> 同环境共存，可额外保留 <strong><code>notebook</code></strong> 包（见 <a href="/2026/05/01/Jupyter/">Jupyter 入门</a>）。</p>
<hr>
<h1 id="三、启动与安全习惯"><a href="#三、启动与安全习惯" class="headerlink" title="三、启动与安全习惯"></a>三、启动与安全习惯</h1><h2 id="1-默认启动"><a href="#1-默认启动" class="headerlink" title="1. 默认启动"></a>1. 默认启动</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter lab</span><br></pre></td></tr></table></figure>

<p>终端打印的 <strong>URL</strong> 含 <strong><code>token=</code></strong>；<strong><code>--no-browser</code></strong> 适合在远程服务器上只复制链接到本地浏览器打开。</p>
<h2 id="2-端口与监听地址"><a href="#2-端口与监听地址" class="headerlink" title="2. 端口与监听地址"></a>2. 端口与监听地址</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jupyter lab --port 8888 --no-browser --ServerApp.ip=127.0.0.1</span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>127.0.0.1</code></strong>：仅本机访问，适合本地开发。  </li>
<li><strong><code>0.0.0.0</code></strong>：监听所有网卡，便于 <strong>SSH 端口转发</strong> 或容器内访问，但<strong>须配合防火墙与认证</strong>，勿在公网裸奔。</li>
</ul>
<h2 id="3-配置密码与-HTTPS（生产必读）"><a href="#3-配置密码与-HTTPS（生产必读）" class="headerlink" title="3. 配置密码与 HTTPS（生产必读）"></a>3. 配置密码与 HTTPS（生产必读）</h2><p>长期使用可配置 <strong><code>ServerApp.password</code></strong>（哈希）与证书，详见 <strong>Jupyter Server</strong> 文档中的 <strong>Security</strong> 章节；团队统一入口请评估 <strong>JupyterHub</strong>。</p>
<hr>
<h1 id="四、工作区与常用能力"><a href="#四、工作区与常用能力" class="headerlink" title="四、工作区与常用能力"></a>四、工作区与常用能力</h1><table>
<thead>
<tr>
<th>能力</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>文件浏览器</strong></td>
<td>左侧打开工作目录下 <strong><code>.ipynb</code></strong>、文本与图片</td>
</tr>
<tr>
<td><strong>Notebook</strong></td>
<td>与经典界面相同的单元格执行模型</td>
</tr>
<tr>
<td><strong>Console</strong></td>
<td>与内核绑定的「控制台」，适合试片段代码</td>
</tr>
<tr>
<td><strong>Terminal</strong></td>
<td>内置终端（依赖系统 <strong>shell</strong>），在远程环境非常方便</td>
</tr>
<tr>
<td><strong>Workspace</strong></td>
<td>布局可保存；关闭前注意 <strong>File → Save Workspace</strong>（以当前版本菜单为准）</td>
</tr>
</tbody></table>
<hr>
<h1 id="五、扩展（Extensions）"><a href="#五、扩展（Extensions）" class="headerlink" title="五、扩展（Extensions）"></a>五、扩展（Extensions）</h1><p><strong>JupyterLab 3+</strong> 起多数扩展以 <strong>Python pip 包</strong> 分发，安装后重启 <strong>Lab</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m pip install jupyterlab-git</span><br></pre></td></tr></table></figure>

<p>具体扩展名以 <strong>PyPI</strong> 与扩展文档为准；<strong>Extension Manager</strong> 是否在界面默认启用，取决于管理员策略与版本（详见 <strong>JupyterLab</strong> 文档 <strong>Extensions</strong> 一节）。</p>
<hr>
<h1 id="六、与-Jupyter-JupyterHub-的关系"><a href="#六、与-Jupyter-JupyterHub-的关系" class="headerlink" title="六、与 Jupyter &#x2F; JupyterHub 的关系"></a>六、与 Jupyter &#x2F; JupyterHub 的关系</h1><ul>
<li><strong>经典 Notebook</strong>：见 <a href="/2026/05/01/Jupyter/">Jupyter 入门</a>，命令为 <strong><code>jupyter notebook</code></strong>。  </li>
<li><strong>JupyterHub</strong>：在 <strong>Hub</strong> 后为用户各自拉起 <strong>单用户服务器</strong>，前端常仍是 <strong>JupyterLab</strong>；部署与认证见 <a href="/2026/05/01/JupyterHub/">JupyterHub 入门</a>。</li>
</ul>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>安装</td>
<td><strong><code>python -m pip install jupyterlab</code></strong></td>
</tr>
<tr>
<td>启动</td>
<td><strong><code>jupyter lab</code></strong>，使用终端中的 <strong>token URL</strong></td>
</tr>
<tr>
<td>安全</td>
<td>优先 <strong><code>127.0.0.1</code></strong>；远程用 <strong>SSH 转发</strong> 或 <strong>Hub + HTTPS</strong></td>
</tr>
<tr>
<td>延伸</td>
<td><a href="/2026/05/01/Jupyter/">Jupyter</a> · <a href="/2026/05/01/JupyterHub/">JupyterHub</a></td>
</tr>
</tbody></table>
<p>界面与快捷键随 <strong>JupyterLab</strong> 大版本迭代会有差异，以你安装的版本对应文档为准。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Jupyter</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python的UV工具</title>
    <url>/2026/04/30/Python/Python-UV/</url>
    <content><![CDATA[<p><strong>uv</strong> 是由 <a href="https://astral.sh/">Astral</a> 发布的 Python 包与项目管理工具，用 Rust 编写，可快速解析依赖、安装包、管理解释器与虚拟环境，并兼容常见的 <code>pip</code> &#x2F; <code>venv</code> 工作流。下面按安装、解释器、固定版本、虚拟环境、依赖与项目命令分步说明（以 Linux &#x2F; macOS 为主，Windows 附安装方式）；文中示例统一以 <strong>Python 3.14.3</strong> 为例。</p>
<hr>
<h1 id="一、安装-uv"><a href="#一、安装-uv" class="headerlink" title="一、安装 uv"></a>一、安装 uv</h1><h2 id="1-Linux-macOS（官方安装脚本）"><a href="#1-Linux-macOS（官方安装脚本）" class="headerlink" title="1. Linux &#x2F; macOS（官方安装脚本）"></a>1. Linux &#x2F; macOS（官方安装脚本）</h2><p>在终端执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -LsSf https://astral.sh/uv/install.sh | sh</span><br></pre></td></tr></table></figure>

<p>安装完成后按提示将 <code>uv</code> 加入 <code>PATH</code>（脚本一般会提示 <code>source</code> 某个 env 文件）。</p>
<h2 id="2-Windows（PowerShell）"><a href="#2-Windows（PowerShell）" class="headerlink" title="2. Windows（PowerShell）"></a>2. Windows（PowerShell）</h2><p>以当前用户执行（无需管理员时亦可）：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">powershell <span class="literal">-ExecutionPolicy</span> ByPass <span class="literal">-c</span> <span class="string">&quot;irm https://astral.sh/uv/install.ps1 | iex&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-验证"><a href="#3-验证" class="headerlink" title="3. 验证"></a>3. 验证</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv --version</span><br></pre></td></tr></table></figure>

<p>能输出版本号即表示安装成功。更多方式见官方文档：<a href="https://docs.astral.sh/uv/getting-started/installation/">Installation</a>。</p>
<hr>
<h1 id="二、Python-解释器"><a href="#二、Python-解释器" class="headerlink" title="二、Python 解释器"></a>二、Python 解释器</h1><h2 id="1-查看本机已安装的版本"><a href="#1-查看本机已安装的版本" class="headerlink" title="1. 查看本机已安装的版本"></a>1. 查看本机已安装的版本</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv python list</span><br></pre></td></tr></table></figure>

<h2 id="2-安装指定-CPython（示例-3-14-3）"><a href="#2-安装指定-CPython（示例-3-14-3）" class="headerlink" title="2. 安装指定 CPython（示例 3.14.3）"></a>2. 安装指定 CPython（示例 3.14.3）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv python install 3.14.3</span><br></pre></td></tr></table></figure>

<p>也可写 <code>3.14</code>，由 uv 解析为可用补丁版本。uv 会下载并管理多版本解释器，便于不同项目选用不同解释器。</p>
<h2 id="3-为项目固定-Python-版本（uv-python-pin）"><a href="#3-为项目固定-Python-版本（uv-python-pin）" class="headerlink" title="3. 为项目固定 Python 版本（uv python pin）"></a>3. 为项目固定 Python 版本（<code>uv python pin</code>）</h2><p>在项目根目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/your/project</span><br><span class="line">uv python pin 3.14.3</span><br></pre></td></tr></table></figure>

<p>会在当前目录生成（或更新）<strong><code>.python-version</code></strong>，记录要用的版本；之后在本目录及子目录执行 <code>uv venv</code>、<code>uv run</code>、<code>uv sync</code> 等时，uv 会优先按该文件选用解释器，与 <strong>pyenv</strong> 等工具习惯一致。</p>
<p>说明：</p>
<ul>
<li>若项目里已有 <strong><code>pyproject.toml</code></strong> 的 <strong><code>[project] requires-python</code></strong>，所 pin 的版本须满足该约束，否则 uv 会报错；需要换版本时应同时改 <code>requires-python</code> 与 pin，或先用 <code>uv python pin --no-project</code> 跳过校验（慎用）。</li>
<li>查看当前 pin：若已有 <code>.python-version</code>，执行 <code>uv python pin</code>（不写版本号）会打印当前固定版本；若无该文件则会报错。</li>
<li>取消 pin：<code>uv python pin --rm</code>。</li>
<li>仅在当前用户全局默认：可用 <code>uv python pin 3.14.3 --global</code>（具体行为以官方文档为准）。</li>
</ul>
<p>命令参考：<a href="https://docs.astral.sh/uv/reference/cli/#uv-python-pin">uv python pin</a>。</p>
<hr>
<h1 id="三、虚拟环境"><a href="#三、虚拟环境" class="headerlink" title="三、虚拟环境"></a>三、虚拟环境</h1><h2 id="1-在项目目录创建虚拟环境"><a href="#1-在项目目录创建虚拟环境" class="headerlink" title="1. 在项目目录创建虚拟环境"></a>1. 在项目目录创建虚拟环境</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/your/project</span><br><span class="line">uv venv</span><br></pre></td></tr></table></figure>

<p>默认创建目录 <code>.venv</code>。指定 Python 版本（与 pin 一致时可省略，由 <code>.python-version</code> 决定）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv venv --python 3.14.3</span><br></pre></td></tr></table></figure>

<h2 id="2-激活虚拟环境"><a href="#2-激活虚拟环境" class="headerlink" title="2. 激活虚拟环境"></a>2. 激活虚拟环境</h2><p><strong>Linux &#x2F; macOS：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> .venv/bin/activate</span><br></pre></td></tr></table></figure>

<p><strong>Windows（cmd）：</strong></p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">.venv\Scripts\activate.bat</span><br></pre></td></tr></table></figure>

<p><strong>Windows（PowerShell）：</strong></p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">.venv\Scripts\Activate.ps1</span><br></pre></td></tr></table></figure>

<h2 id="3-退出虚拟环境"><a href="#3-退出虚拟环境" class="headerlink" title="3. 退出虚拟环境"></a>3. 退出虚拟环境</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">deactivate</span><br></pre></td></tr></table></figure>

<p>也可不手动激活，用 <code>uv run</code> 在环境内执行命令（见下文）。</p>
<hr>
<h1 id="四、依赖安装与项目管理"><a href="#四、依赖安装与项目管理" class="headerlink" title="四、依赖安装与项目管理"></a>四、依赖安装与项目管理</h1><h2 id="1-用-uv-安装包（类似-pip）"><a href="#1-用-uv-安装包（类似-pip）" class="headerlink" title="1. 用 uv 安装包（类似 pip）"></a>1. 用 uv 安装包（类似 pip）</h2><p>在已激活的虚拟环境中，或配合 <code>--python</code> 使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv pip install requests</span><br></pre></td></tr></table></figure>

<p>查看已安装包：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv pip list</span><br></pre></td></tr></table></figure>

<p>国内访问 PyPI 较慢时，可临时指定镜像，例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv pip install -i https://pypi.tuna.tsinghua.edu.cn/simple requests</span><br></pre></td></tr></table></figure>

<h2 id="2-初始化项目（生成-pyproject-toml）"><a href="#2-初始化项目（生成-pyproject-toml）" class="headerlink" title="2. 初始化项目（生成 pyproject.toml）"></a>2. 初始化项目（生成 pyproject.toml）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> myapp &amp;&amp; <span class="built_in">cd</span> myapp</span><br><span class="line">uv init</span><br></pre></td></tr></table></figure>

<p>按需编辑 <code>pyproject.toml</code> 中的依赖后，在项目根目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv <span class="built_in">sync</span></span><br></pre></td></tr></table></figure>

<p>会按声明安装依赖并维护锁文件（若使用 uv 的 lock 工作流）。</p>
<h2 id="4-在固定环境中运行命令"><a href="#4-在固定环境中运行命令" class="headerlink" title="4. 在固定环境中运行命令"></a>4. 在固定环境中运行命令</h2><p>无需先 <code>activate</code>，可在项目根使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run python main.py</span><br><span class="line">uv run pytest</span><br></pre></td></tr></table></figure>

<p>uv 会按项目配置选择解释器与依赖环境。</p>
<hr>
<h1 id="五、常用命令速查"><a href="#五、常用命令速查" class="headerlink" title="五、常用命令速查"></a>五、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>示例命令</th>
</tr>
</thead>
<tbody><tr>
<td>查看版本</td>
<td><code>uv --version</code></td>
</tr>
<tr>
<td>安装&#x2F;列出 Python</td>
<td><code>uv python install 3.14.3</code>、<code>uv python list</code></td>
</tr>
<tr>
<td>固定项目 Python</td>
<td><code>uv python pin 3.14.3</code>（写入 <code>.python-version</code>）</td>
</tr>
<tr>
<td>创建虚拟环境</td>
<td><code>uv venv</code>、<code>uv venv --python 3.14.3</code></td>
</tr>
<tr>
<td>安装依赖</td>
<td><code>uv pip install 包名</code>、<code>uv sync</code></td>
</tr>
<tr>
<td>运行脚本&#x2F;工具</td>
<td><code>uv run python app.py</code>、<code>uv run 命令</code></td>
</tr>
<tr>
<td>初始化项目</td>
<td><code>uv init</code></td>
</tr>
</tbody></table>
<p>更完整的子命令与配置见官方文档：<a href="https://docs.astral.sh/uv/">uv 文档</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
      </tags>
  </entry>
  <entry>
    <title>网页自动化（Python + Selenium）</title>
    <url>/2026/05/01/Python/Selenium/</url>
    <content><![CDATA[<p><strong>Selenium</strong> 用代码驱动真实浏览器做点击、填表、截图与回归验证，是 <strong>Python</strong> 里最常见的网页自动化方案之一。下面按「准备环境 → 安装库 → 浏览器与驱动 → 第一个脚本 → 无头与常用选项 → 等待与定位要点」顺序写清楚；版本以 <strong>Selenium 4.x</strong> 与 <strong>Python 3</strong> 为准。</p>
<hr>
<h1 id="一、环境准备（必须）"><a href="#一、环境准备（必须）" class="headerlink" title="一、环境准备（必须）"></a>一、环境准备（必须）</h1><ol>
<li><strong>Python 3.14.3</strong>（本站 <strong>Python</strong> 栏统一版本；<strong>Selenium</strong> 亦兼容 <strong>3.10+</strong>）：终端能执行 <strong><code>python3 -V</code></strong> 与 <strong><code>pip --version</code></strong>。  </li>
<li><strong>Google Chrome</strong> 或 <strong>Microsoft Edge</strong>（<strong>Chromium</strong> 系）或 <strong>Mozilla Firefox</strong> 之一，用于本地实际打开页面。  </li>
<li><strong>编辑器</strong>：<strong>VS Code</strong>、<strong>PyCharm</strong> 等任选其一。</li>
</ol>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 -V</span><br><span class="line">pip3 --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、安装-Selenium"><a href="#二、安装-Selenium" class="headerlink" title="二、安装 Selenium"></a>二、安装 Selenium</h1><h2 id="1-建议先建虚拟环境（可选）"><a href="#1-建议先建虚拟环境（可选）" class="headerlink" title="1. 建议先建虚拟环境（可选）"></a>1. 建议先建虚拟环境（可选）</h2><p>在项目目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 -m venv .venv</span><br><span class="line"><span class="built_in">source</span> .venv/bin/activate   <span class="comment"># Windows CMD: .venv\Scripts\activate.bat</span></span><br></pre></td></tr></table></figure>

<p>提示符前出现 <strong><code>(.venv)</code></strong> 即表示已进入虚拟环境。</p>
<h2 id="2-用-pip-安装"><a href="#2-用-pip-安装" class="headerlink" title="2. 用 pip 安装"></a>2. 用 pip 安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -U pip</span><br><span class="line">pip install selenium</span><br></pre></td></tr></table></figure>

<p>国内下载慢可临时指定镜像（示例为清华源）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install selenium -i https://pypi.tuna.tsinghua.edu.cn/simple</span><br></pre></td></tr></table></figure>

<h2 id="3-确认版本"><a href="#3-确认版本" class="headerlink" title="3. 确认版本"></a>3. 确认版本</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 -c <span class="string">&quot;import selenium; print(selenium.__version__)&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="三、浏览器与驱动"><a href="#三、浏览器与驱动" class="headerlink" title="三、浏览器与驱动"></a>三、浏览器与驱动</h1><h2 id="1-Selenium-Manager（推荐，Selenium-4-6-）"><a href="#1-Selenium-Manager（推荐，Selenium-4-6-）" class="headerlink" title="1. Selenium Manager（推荐，Selenium 4.6+）"></a>1. Selenium Manager（推荐，Selenium 4.6+）</h2><p><strong>Selenium 4.6</strong> 起内置 <strong>Selenium Manager</strong>，在常见环境下会自动解析本机已安装的 <strong>Chrome &#x2F; Edge &#x2F; Firefox</strong> 并匹配驱动，<strong>多数情况无需再手动下载 <code>chromedriver</code></strong>。若启动报错，先升级 <strong>Selenium</strong> 与浏览器到较新版本。</p>
<h2 id="2-仍失败时的排查顺序"><a href="#2-仍失败时的排查顺序" class="headerlink" title="2. 仍失败时的排查顺序"></a>2. 仍失败时的排查顺序</h2><ol>
<li>浏览器是否已安装且能手动打开。  </li>
<li><strong><code>pip install -U selenium</code></strong> 是否已升级到当前 <strong>4.x</strong> 最新补丁版。  </li>
<li><strong>Linux</strong> 无图形界面时是否已使用下文「无头」方式，或已配置 <strong>Xvfb</strong> 等显示环境。  </li>
<li>企业环境若禁止自动拉驱动，再按官方文档改为<strong>显式指定驱动路径</strong>或使用自带浏览器的容器镜像。</li>
</ol>
<p>官方文档：<a href="https://www.selenium.dev/documentation/">https://www.selenium.dev/documentation/</a>  </p>
<hr>
<h1 id="四、第一个脚本（打开网页并打印标题）"><a href="#四、第一个脚本（打开网页并打印标题）" class="headerlink" title="四、第一个脚本（打开网页并打印标题）"></a>四、第一个脚本（打开网页并打印标题）</h1><p>在任意目录新建 <strong><code>demo_selenium.py</code></strong>，写入：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.chrome.options <span class="keyword">import</span> Options</span><br><span class="line"></span><br><span class="line">options = Options()</span><br><span class="line"><span class="comment"># 如需无头，取消下一行注释</span></span><br><span class="line"><span class="comment"># options.add_argument(&quot;--headless=new&quot;)</span></span><br><span class="line"></span><br><span class="line">driver = webdriver.Chrome(options=options)</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    driver.get(<span class="string">&quot;https://example.com&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(driver.title)</span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    driver.quit()</span><br></pre></td></tr></table></figure>

<p>运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 demo_selenium.py</span><br></pre></td></tr></table></figure>

<p>终端应打印 <strong><code>Example Domain</code></strong>（或当前页面 <strong><code>&lt;title&gt;</code></strong>）。若报错 <strong><code>SessionNotCreatedException</code></strong>，优先按 <strong><code># 三</code></strong> 升级 <strong>Selenium</strong> 与浏览器。</p>
<hr>
<h1 id="五、无头模式与常用启动参数"><a href="#五、无头模式与常用启动参数" class="headerlink" title="五、无头模式与常用启动参数"></a>五、无头模式与常用启动参数</h1><h2 id="1-Chrome-Edge-无头（Chromium-系）"><a href="#1-Chrome-Edge-无头（Chromium-系）" class="headerlink" title="1. Chrome &#x2F; Edge 无头（Chromium 系）"></a>1. Chrome &#x2F; Edge 无头（Chromium 系）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">options = Options()</span><br><span class="line">options.add_argument(<span class="string">&quot;--headless=new&quot;</span>)</span><br><span class="line">options.add_argument(<span class="string">&quot;--no-sandbox&quot;</span>)          <span class="comment"># 部分 CI / 容器环境需要</span></span><br><span class="line">options.add_argument(<span class="string">&quot;--disable-dev-shm-usage&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="2-窗口大小（无头时避免布局与真实用户差异过大）"><a href="#2-窗口大小（无头时避免布局与真实用户差异过大）" class="headerlink" title="2. 窗口大小（无头时避免布局与真实用户差异过大）"></a>2. 窗口大小（无头时避免布局与真实用户差异过大）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">options.add_argument(<span class="string">&quot;--window-size=1280,900&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="3-Firefox-无头（若使用-webdriver-Firefox）"><a href="#3-Firefox-无头（若使用-webdriver-Firefox）" class="headerlink" title="3. Firefox 无头（若使用 webdriver.Firefox）"></a>3. Firefox 无头（若使用 <code>webdriver.Firefox</code>）</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium.webdriver <span class="keyword">import</span> FirefoxOptions</span><br><span class="line"></span><br><span class="line">ff = FirefoxOptions()</span><br><span class="line">ff.add_argument(<span class="string">&quot;-headless&quot;</span>)</span><br><span class="line">driver = webdriver.Firefox(options=ff)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、等待与元素定位（避免「找不到元素」）"><a href="#六、等待与元素定位（避免「找不到元素」）" class="headerlink" title="六、等待与元素定位（避免「找不到元素」）"></a>六、等待与元素定位（避免「找不到元素」）</h1><p>页面加载有异步请求时，<strong>固定 <code>sleep</code></strong> 不稳，建议：</p>
<ol>
<li><strong>显式等待</strong>：<strong><code>WebDriverWait</code></strong> + <strong><code>expected_conditions</code></strong>，在超时时间内轮询条件。  </li>
<li><strong>定位方式</strong>：优先 <strong><code>id</code> &#x2F; <code>data-testid</code> &#x2F; 稳定 <code>name</code></strong>；少用绝对 <strong>XPath</strong>。  </li>
<li><strong>点击被挡</strong>：先滚动到视区 <strong><code>element.location_once_scrolled_into_view</code></strong>，或等待浮层消失。</li>
</ol>
<p>示例（点击「可点击」的按钮）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium.webdriver.common.by <span class="keyword">import</span> By</span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.support <span class="keyword">import</span> expected_conditions <span class="keyword">as</span> EC</span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.support.ui <span class="keyword">import</span> WebDriverWait</span><br><span class="line"></span><br><span class="line">wait = WebDriverWait(driver, <span class="number">10</span>)</span><br><span class="line">btn = wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, <span class="string">&quot;button.submit&quot;</span>)))</span><br><span class="line">btn.click()</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、与-pytest-搭配（可选）"><a href="#七、与-pytest-搭配（可选）" class="headerlink" title="七、与 pytest 搭配（可选）"></a>七、与 pytest 搭配（可选）</h1><ol>
<li>安装：<strong><code>pip install pytest</code></strong>。  </li>
<li>测试文件命名 <strong><code>test_*.py</code></strong>，用 <strong><code>assert</code></strong> 断言页面结果。  </li>
<li>每个用例结束务必 <strong><code>driver.quit()</code></strong>，或用 <strong><code>pytest</code> fixture</strong> 统一创建与销毁浏览器，避免僵尸进程。</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pytest -q</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h1><table>
<thead>
<tr>
<th>步骤</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td>环境</td>
<td><strong>Python 3</strong> + 浏览器 + <strong>pip</strong></td>
</tr>
<tr>
<td>安装</td>
<td><strong><code>pip install selenium</code></strong>（建议虚拟环境）</td>
</tr>
<tr>
<td>驱动</td>
<td><strong>Selenium 4.6+</strong> 优先依赖 <strong>Selenium Manager</strong></td>
</tr>
<tr>
<td>入门</td>
<td><strong><code>webdriver.Chrome()</code></strong> + <strong><code>get</code> &#x2F; <code>title</code> &#x2F; <code>quit</code></strong></td>
</tr>
<tr>
<td>稳定</td>
<td><strong>显式等待</strong> + 稳定定位 + 无头参数按需配置</td>
</tr>
</tbody></table>
<p>更复杂的录制回放、分布式 <strong>Grid</strong>、与 <strong>CI</strong> 集成等，可在掌握上述流程后查阅 <strong>Selenium</strong> 官方文档按场景扩展。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Selenium</tag>
        <tag>自动化</tag>
      </tags>
  </entry>
  <entry>
    <title>UV从0到1创建项目</title>
    <url>/2026/05/01/Python/UV%E4%BB%8E0%E5%88%B01%E5%88%9B%E5%BB%BA%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<p>本文从<strong>空目录</strong>开始，用 <strong>uv</strong> 初始化 Python 项目、固定解释器版本、创建虚拟环境、声明依赖并运行代码。假定你已能使用终端；<strong>uv 的安装与全局说明</strong>见同栏《Python的UV工具》一文。示例以 <strong>Python 3.14.3</strong> 为例，可按需改成你本机已安装的版本。</p>
<hr>
<h1 id="一、准备目录与检查-uv"><a href="#一、准备目录与检查-uv" class="headerlink" title="一、准备目录与检查 uv"></a>一、准备目录与检查 uv</h1><h2 id="1-新建项目文件夹"><a href="#1-新建项目文件夹" class="headerlink" title="1. 新建项目文件夹"></a>1. 新建项目文件夹</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> hello-uv &amp;&amp; <span class="built_in">cd</span> hello-uv</span><br></pre></td></tr></table></figure>

<p>路径与项目名可自定，下文以当前目录为「项目根」。</p>
<h2 id="2-确认-uv-可用"><a href="#2-确认-uv-可用" class="headerlink" title="2. 确认 uv 可用"></a>2. 确认 uv 可用</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv --version</span><br></pre></td></tr></table></figure>

<p>若命令不存在，请先完成 uv 安装后再继续。</p>
<hr>
<h1 id="二、初始化项目（uv-init）"><a href="#二、初始化项目（uv-init）" class="headerlink" title="二、初始化项目（uv init）"></a>二、初始化项目（<code>uv init</code>）</h1><h2 id="1-生成-pyproject-toml-等骨架"><a href="#1-生成-pyproject-toml-等骨架" class="headerlink" title="1. 生成 pyproject.toml 等骨架"></a>1. 生成 <code>pyproject.toml</code> 等骨架</h2><p>在项目根执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv init</span><br></pre></td></tr></table></figure>

<p>会生成（具体以你本机 uv 版本为准）<strong><code>pyproject.toml</code></strong>，部分版本还会生成 <strong><code>.gitignore</code></strong>、<strong><code>README.md</code></strong> 或示例入口文件等。</p>
<h2 id="2-指定包名（可选）"><a href="#2-指定包名（可选）" class="headerlink" title="2. 指定包名（可选）"></a>2. 指定包名（可选）</h2><p>若希望 <code>pyproject.toml</code> 里项目名与目录名不同，可使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv init --name hello_uv</span><br></pre></td></tr></table></figure>

<p>尚未建目录时也可一步完成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv init hello-uv &amp;&amp; <span class="built_in">cd</span> hello-uv</span><br></pre></td></tr></table></figure>

<h2 id="3-常用国内-PyPI-镜像地址（整段复制）"><a href="#3-常用国内-PyPI-镜像地址（整段复制）" class="headerlink" title="3. 常用国内 PyPI 镜像地址（整段复制）"></a>3. 常用国内 PyPI 镜像地址（整段复制）</h2><p>终端里 <strong><code>uv pip install -i &lt;地址&gt;</code></strong>、或在 <strong><code>pyproject.toml</code></strong> 里填 <strong><code>[[tool.uv.index]]</code> 的 <code>url</code></strong> 时，可直接复制下列 <strong>simple</strong> 根地址（末尾一般带 <strong><code>/simple</code></strong>）：</p>
<ul>
<li><strong>清华</strong>：<code>https://pypi.tuna.tsinghua.edu.cn/simple</code></li>
<li><strong>阿里云</strong>：<code>https://mirrors.aliyun.com/pypi/simple</code></li>
<li><strong>腾讯云</strong>：<code>https://mirrors.cloud.tencent.com/pypi/simple</code></li>
<li><strong>华为云</strong>：<code>https://repo.huaweicloud.com/repository/pypi/simple</code></li>
</ul>
<h2 id="4-在-pyproject-toml-中为-uv-配置默认索引（可选）"><a href="#4-在-pyproject-toml-中为-uv-配置默认索引（可选）" class="headerlink" title="4. 在 pyproject.toml 中为 uv 配置默认索引（可选）"></a>4. 在 <code>pyproject.toml</code> 中为 uv 配置默认索引（可选）</h2><p>在已有 <strong><code>pyproject.toml</code></strong>（<code>uv init</code> 之后）里使用 <strong><code>[[tool.uv.index]]</code></strong>，让 <strong><code>uv sync</code> &#x2F; <code>uv add</code></strong> 等默认走国内源；将 <strong><code>url</code></strong> 换成 <strong>二、§3</strong> 中任一条即可。示例（<strong>阿里云</strong> 为默认索引）：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[[tool.uv.index]]</span></span><br><span class="line"><span class="attr">url</span> = <span class="string">&quot;https://mirrors.aliyun.com/pypi/simple&quot;</span></span><br><span class="line"><span class="attr">default</span> = <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<ul>
<li>需要 <strong>官方 PyPI 作备用</strong> 时，可再加一段 <strong><code>[[tool.uv.index]]</code></strong>，<code>url = &quot;https://pypi.org/simple&quot;</code>，且 <strong><code>default = false</code></strong>（多索引细节以 <a href="https://docs.astral.sh/uv/configuration/indexes/">uv 文档</a> 为准）。  </li>
<li>改完后建议执行 <strong><code>uv lock</code></strong> &#x2F; <strong><code>uv sync</code></strong> 验证能否正常解析依赖。</li>
</ul>
<hr>
<h1 id="三、固定-Python-版本与虚拟环境"><a href="#三、固定-Python-版本与虚拟环境" class="headerlink" title="三、固定 Python 版本与虚拟环境"></a>三、固定 Python 版本与虚拟环境</h1><h2 id="1-安装并固定解释器（可选但推荐）"><a href="#1-安装并固定解释器（可选但推荐）" class="headerlink" title="1. 安装并固定解释器（可选但推荐）"></a>1. 安装并固定解释器（可选但推荐）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv python install 3.14.3</span><br><span class="line">uv python pin 3.14.3</span><br></pre></td></tr></table></figure>

<p>会在项目根写入 <strong><code>.python-version</code></strong>，便于团队与 CI 使用同一 Python 小版本。</p>
<h2 id="2-创建本地虚拟环境"><a href="#2-创建本地虚拟环境" class="headerlink" title="2. 创建本地虚拟环境"></a>2. 创建本地虚拟环境</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv venv</span><br></pre></td></tr></table></figure>

<p>默认得到 <strong><code>.venv/</code></strong>。若要与 pin 的版本一致，可显式指定：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv venv --python 3.14.3</span><br></pre></td></tr></table></figure>

<h2 id="3-激活虚拟环境（可选）"><a href="#3-激活虚拟环境（可选）" class="headerlink" title="3. 激活虚拟环境（可选）"></a>3. 激活虚拟环境（可选）</h2><p>需要与传统工作流一致时再激活：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Linux / macOS</span></span><br><span class="line"><span class="built_in">source</span> .venv/bin/activate</span><br></pre></td></tr></table></figure>

<p>日常更推荐直接用 <strong><code>uv run</code></strong>（见下文），无需每次 <code>activate</code>。</p>
<hr>
<h1 id="四、添加依赖并同步"><a href="#四、添加依赖并同步" class="headerlink" title="四、添加依赖并同步"></a>四、添加依赖并同步</h1><h2 id="1-用-uv-add-声明依赖"><a href="#1-用-uv-add-声明依赖" class="headerlink" title="1. 用 uv add 声明依赖"></a>1. 用 <code>uv add</code> 声明依赖</h2><p>例如增加 <strong>requests</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add requests</span><br></pre></td></tr></table></figure>

<p><code>uv</code> 会更新 <strong><code>pyproject.toml</code></strong> 中的依赖声明，并维护 <strong><code>uv.lock</code></strong>（锁文件）。若未配置 <strong>二、§4</strong> 的默认索引、且拉包较慢，可临时用 <strong><code>uv pip install -i &lt;镜像&gt;</code></strong>（镜像见 <strong>二、§3</strong>）。</p>
<h2 id="2-安装锁文件中的依赖（uv-sync）"><a href="#2-安装锁文件中的依赖（uv-sync）" class="headerlink" title="2. 安装锁文件中的依赖（uv sync）"></a>2. 安装锁文件中的依赖（<code>uv sync</code>）</h2><p>在已有 <code>uv.lock</code> 的机器上（含克隆仓库后），在项目根执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv <span class="built_in">sync</span></span><br></pre></td></tr></table></figure>

<p>会按锁文件创建或更新 <strong><code>.venv</code></strong> 中的包，保证环境可复现。</p>
<hr>
<h1 id="五、编写代码与运行"><a href="#五、编写代码与运行" class="headerlink" title="五、编写代码与运行"></a>五、编写代码与运行</h1><h2 id="1-入口文件"><a href="#1-入口文件" class="headerlink" title="1. 入口文件"></a>1. 入口文件</h2><p>若 <code>uv init</code> 后已有示例 <strong><code>main.py</code></strong>（或你自行创建 <code>main.py</code>），可写入：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">main</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Hello from uv&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure>

<p>实际文件名以你项目为准。</p>
<h2 id="2-在项目环境中运行"><a href="#2-在项目环境中运行" class="headerlink" title="2. 在项目环境中运行"></a>2. 在项目环境中运行</h2><p>不激活 venv 时，在项目根执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run python main.py</span><br></pre></td></tr></table></figure>

<p>若在 <strong><code>pyproject.toml</code></strong> 的 <strong><code>[project.scripts]</code></strong> 里配置了控制台入口（例如 <code>hello = &quot;hello_uv.cli:main&quot;</code>），安装同步后可按 uv 提示使用 <strong><code>uv run hello</code></strong> 等形式；未配置时只用 <strong><code>uv run python …</code></strong> 即可。</p>
<hr>
<h1 id="六、目录与文件速览"><a href="#六、目录与文件速览" class="headerlink" title="六、目录与文件速览"></a>六、目录与文件速览</h1><p>初始化并添加依赖后，常见布局类似：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hello-uv/</span><br><span class="line">├── .python-version     # uv python pin 生成（若执行过 pin）</span><br><span class="line">├── .venv/              # 本地虚拟环境（勿提交或按需 .gitignore）</span><br><span class="line">├── pyproject.toml      # 项目元数据与依赖声明</span><br><span class="line">├── uv.lock             # 锁文件，建议纳入版本控制</span><br><span class="line">└── main.py             # 或其它源码目录（以实际为准）</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、常用命令速查"><a href="#七、常用命令速查" class="headerlink" title="七、常用命令速查"></a>七、常用命令速查</h1><table>
<thead>
<tr>
<th>步骤</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>初始化</td>
<td><code>uv init</code>、<code>uv init --name 包名</code></td>
</tr>
<tr>
<td>固定版本</td>
<td><code>uv python pin 3.14.3</code></td>
</tr>
<tr>
<td>建环境</td>
<td><code>uv venv</code>、<code>uv venv --python 3.14.3</code></td>
</tr>
<tr>
<td>加依赖</td>
<td><code>uv add 包名</code></td>
</tr>
<tr>
<td>按锁安装</td>
<td><code>uv sync</code></td>
</tr>
<tr>
<td>运行</td>
<td><code>uv run python 脚本.py</code>、<code>uv run 命令</code></td>
</tr>
</tbody></table>
<p>更多子命令与配置见官方文档：<a href="https://docs.astral.sh/uv/">uv 文档</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
      </tags>
  </entry>
  <entry>
    <title>UV项目开发添加Pyright</title>
    <url>/2026/05/01/Python/UV%E9%A1%B9%E7%9B%AE%E5%BC%80%E5%8F%91%E6%B7%BB%E5%8A%A0Pyright/</url>
    <content><![CDATA[<p><strong>Pyright</strong> 是微软开源的 <strong>Python 静态类型检查器</strong>，与 VS Code &#x2F; Cursor 的 <strong>Pylance</strong> 同源，适合在提交前发现类型错误。官方 CLI 多通过 <strong>Node.js</strong> 分发；在纯 <strong>uv + PyPI</strong> 工作流里，常用与 Pyright 配置兼容的 <strong>BasedPyright</strong>（维护活跃、一条 <code>uv add</code> 即可）。本文假定你已有 <strong>uv 项目</strong>；初始化步骤见《UV从0到1创建项目》。命令默认在<strong>项目根目录</strong>执行。</p>
<hr>
<h1 id="一、安装类型检查-CLI"><a href="#一、安装类型检查-CLI" class="headerlink" title="一、安装类型检查 CLI"></a>一、安装类型检查 CLI</h1><h2 id="1-推荐：BasedPyright（与-Pyright-配置兼容）"><a href="#1-推荐：BasedPyright（与-Pyright-配置兼容）" class="headerlink" title="1. 推荐：BasedPyright（与 Pyright 配置兼容）"></a>1. 推荐：BasedPyright（与 Pyright 配置兼容）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add --dev basedpyright</span><br></pre></td></tr></table></figure>

<h2 id="2-验证"><a href="#2-验证" class="headerlink" title="2. 验证"></a>2. 验证</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run basedpyright --version</span><br></pre></td></tr></table></figure>

<h2 id="3-可选：使用微软原版-Pyright（Node）"><a href="#3-可选：使用微软原版-Pyright（Node）" class="headerlink" title="3. 可选：使用微软原版 Pyright（Node）"></a>3. 可选：使用微软原版 Pyright（Node）</h2><p>若团队统一使用 <strong>npm</strong> 安装的 Pyright，可在项目外加装 Node 后使用 <code>npx pyright</code>，或在 CI 里 <code>npm install -g pyright</code> 后调用 <strong><code>pyright</code></strong>；配置与下文 <strong><code>[tool.pyright]</code></strong> 键名一致（写在 <strong><code>pyproject.toml</code></strong> 即可）。</p>
<hr>
<h1 id="二、在-pyproject-toml-中配置"><a href="#二、在-pyproject-toml-中配置" class="headerlink" title="二、在 pyproject.toml 中配置"></a>二、在 <code>pyproject.toml</code> 中配置</h1><p>在 <strong><code>pyproject.toml</code></strong> 中增加 <strong><code>[tool.pyright]</code></strong>（<strong>BasedPyright</strong> 与 <strong>Pyright</strong> &#x2F; <strong>Pylance</strong> 均会读取，勿再单独维护 <strong><code>pyrightconfig.json</code></strong>，避免两处不一致）：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[tool.pyright]</span></span><br><span class="line"><span class="attr">include</span> = [<span class="string">&quot;src&quot;</span>]</span><br><span class="line"><span class="attr">exclude</span> = [<span class="string">&quot;.venv&quot;</span>, <span class="string">&quot;**/__pycache__&quot;</span>]</span><br><span class="line"><span class="attr">pythonVersion</span> = <span class="string">&quot;3.14&quot;</span></span><br><span class="line"><span class="attr">typeCheckingMode</span> = <span class="string">&quot;standard&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>include</code></strong>：按仓库实际源码目录修改（例如 <code>[&quot;hello_uv&quot;]</code> 或 <code>[&quot;.&quot;]</code>）。  </li>
<li><strong><code>pythonVersion</code></strong>：与 <strong><code>[project] requires-python</code></strong> &#x2F; 本机解释器大版本一致。  </li>
<li><strong><code>typeCheckingMode</code></strong>：常用 <strong><code>basic</code></strong> &#x2F; <strong><code>standard</code></strong> &#x2F; <strong><code>strict</code></strong>，由严到宽可调。</li>
</ul>
<p>更多键名与语义见：<a href="https://microsoft.github.io/pyright/#/configuration">Pyright Configuration</a>。</p>
<hr>
<h1 id="三、运行检查"><a href="#三、运行检查" class="headerlink" title="三、运行检查"></a>三、运行检查</h1><h2 id="1-全项目扫描"><a href="#1-全项目扫描" class="headerlink" title="1. 全项目扫描"></a>1. 全项目扫描</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run basedpyright</span><br></pre></td></tr></table></figure>

<p>未指定路径时，会按 <strong><code>pyproject.toml</code></strong> 里 <strong><code>[tool.pyright]</code></strong> 的 <strong><code>include</code></strong> 等项解析工程。</p>
<h2 id="2-指定目录或文件"><a href="#2-指定目录或文件" class="headerlink" title="2. 指定目录或文件"></a>2. 指定目录或文件</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run basedpyright src/hello_uv</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、为类型检查补全注解（简要）"><a href="#四、为类型检查补全注解（简要）" class="headerlink" title="四、为类型检查补全注解（简要）"></a>四、为类型检查补全注解（简要）</h1><p>Pyright 依赖 <strong><code>py.typed</code></strong> 标记与类型存根：</p>
<ul>
<li>若你发布<strong>库</strong>，在包根放置空文件 <strong><code>py.typed</code></strong>（PEP 561），并对公共 API 逐步补类型。  </li>
<li>应用项目可在入口与关键模块为函数参数、返回值写上 <strong><code>str</code></strong>、<strong><code>list[str]</code></strong> 等，复杂结构用 <strong><code>TypedDict</code></strong>、<strong><code>Protocol</code></strong>。</li>
</ul>
<p>详细规则见官方文档：<a href="https://microsoft.github.io/pyright/#/configuration">Pyright Configuration</a>。</p>
<hr>
<h1 id="五、与编辑器、Ruff-的关系"><a href="#五、与编辑器、Ruff-的关系" class="headerlink" title="五、与编辑器、Ruff 的关系"></a>五、与编辑器、Ruff 的关系</h1><h2 id="1-VS-Code-Cursor"><a href="#1-VS-Code-Cursor" class="headerlink" title="1. VS Code &#x2F; Cursor"></a>1. VS Code &#x2F; Cursor</h2><p>安装 <strong>Pylance</strong>（或内置语言服务）后，会在编辑时读取 <strong><code>pyproject.toml</code></strong> 中的 <strong><code>[tool.pyright]</code></strong>；终端里 <strong><code>uv run basedpyright</code></strong> 与编辑器诊断应共用同一 <strong><code>pyproject.toml</code></strong>，避免两套配置。</p>
<h2 id="2-与-Ruff-分工"><a href="#2-与-Ruff-分工" class="headerlink" title="2. 与 Ruff 分工"></a>2. 与 Ruff 分工</h2><ul>
<li><strong>Ruff</strong>：风格、部分 lint、格式化（见《UV项目开发添加Ruff》）。  </li>
<li><strong>Pyright &#x2F; BasedPyright</strong>：类型与导入解析等静态语义。二者可同时启用；避免用 Ruff 的 <strong><code>ANN</code></strong> 等与 Pyright 重复的规则集，减少噪音。</li>
</ul>
<hr>
<h1 id="六、常用命令速查"><a href="#六、常用命令速查" class="headerlink" title="六、常用命令速查"></a>六、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装（PyPI）</td>
<td><code>uv add --dev basedpyright</code></td>
</tr>
<tr>
<td>版本</td>
<td><code>uv run basedpyright --version</code></td>
</tr>
<tr>
<td>按配置检查</td>
<td><code>uv run basedpyright</code></td>
</tr>
<tr>
<td>检查子路径</td>
<td><code>uv run basedpyright src/包名</code></td>
</tr>
</tbody></table>
<p>更多说明：<a href="https://docs.basedpyright.com/">BasedPyright</a> · <a href="https://github.com/microsoft/pyright">Pyright</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
        <tag>Pyright</tag>
      </tags>
  </entry>
  <entry>
    <title>UV项目开发添加Pytest</title>
    <url>/2026/05/01/Python/UV%E9%A1%B9%E7%9B%AE%E5%BC%80%E5%8F%91%E6%B7%BB%E5%8A%A0Pytest/</url>
    <content><![CDATA[<p><strong>pytest</strong> 是 Python 生态里常用的<strong>测试框架</strong>，用断言函数与简洁的用例命名即可组织测试。本文在 <strong>uv 项目</strong>中以开发依赖安装 <strong>pytest</strong>，用 <strong><code>uv run</code></strong> 执行测试，并在 <strong><code>pyproject.toml</code></strong> 中集中配置。假定已有 <strong><code>pyproject.toml</code></strong>；若还没有，请先完成《UV从0到1创建项目》。命令默认在<strong>项目根目录</strong>执行。</p>
<hr>
<h1 id="一、安装-pytest"><a href="#一、安装-pytest" class="headerlink" title="一、安装 pytest"></a>一、安装 pytest</h1><h2 id="1-作为开发依赖添加"><a href="#1-作为开发依赖添加" class="headerlink" title="1. 作为开发依赖添加"></a>1. 作为开发依赖添加</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add --dev pytest</span><br></pre></td></tr></table></figure>

<h2 id="2-确认可执行文件"><a href="#2-确认可执行文件" class="headerlink" title="2. 确认可执行文件"></a>2. 确认可执行文件</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest --version</span><br></pre></td></tr></table></figure>

<p>能输出版本号即表示当前虚拟环境中已可用 pytest。</p>
<hr>
<h1 id="二、目录与示例用例"><a href="#二、目录与示例用例" class="headerlink" title="二、目录与示例用例"></a>二、目录与示例用例</h1><h2 id="1-推荐目录布局"><a href="#1-推荐目录布局" class="headerlink" title="1. 推荐目录布局"></a>1. 推荐目录布局</h2><p>常见两种（二选一即可，团队统一即可）：</p>
<ul>
<li><strong><code>tests/</code></strong> 与源码平级：适合应用项目。  </li>
<li>源码包内 <strong><code>tests/</code></strong> 或 <strong><code>test_*.py</code></strong>：按包结构拆分。</li>
</ul>
<p>下文以仓库根下 <strong><code>tests/</code></strong> 为例。</p>
<h2 id="2-第一个测试文件"><a href="#2-第一个测试文件" class="headerlink" title="2. 第一个测试文件"></a>2. 第一个测试文件</h2><p>创建 <strong><code>tests/test_example.py</code></strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">a: <span class="built_in">int</span>, b: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">    <span class="keyword">return</span> a + b</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test_add</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="keyword">assert</span> add(<span class="number">1</span>, <span class="number">2</span>) == <span class="number">3</span></span><br></pre></td></tr></table></figure>

<h2 id="3-运行全部测试"><a href="#3-运行全部测试" class="headerlink" title="3. 运行全部测试"></a>3. 运行全部测试</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest</span><br></pre></td></tr></table></figure>

<p>pytest 会递归收集 <strong><code>test_*.py</code></strong> &#x2F; <strong><code>*_test.py</code></strong> 中以 <strong><code>test_</code></strong> 开头的函数或类方法。</p>
<hr>
<h1 id="三、在-pyproject-toml-中配置-pytest"><a href="#三、在-pyproject-toml-中配置-pytest" class="headerlink" title="三、在 pyproject.toml 中配置 pytest"></a>三、在 <code>pyproject.toml</code> 中配置 pytest</h1><h2 id="1-最小可用配置"><a href="#1-最小可用配置" class="headerlink" title="1. 最小可用配置"></a>1. 最小可用配置</h2><p>在 <strong><code>pyproject.toml</code></strong> 中增加（或合并进已有块）：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[tool.pytest.ini_options]</span></span><br><span class="line"><span class="attr">minversion</span> = <span class="string">&quot;8.0&quot;</span></span><br><span class="line"><span class="attr">testpaths</span> = [<span class="string">&quot;tests&quot;</span>]</span><br><span class="line"><span class="attr">pythonpath</span> = [<span class="string">&quot;.&quot;</span>]</span><br><span class="line"><span class="attr">addopts</span> = <span class="string">&quot;-ra -q&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>testpaths</code></strong>：只从 <strong><code>tests</code></strong> 收集，避免误扫其它目录。  </li>
<li><strong><code>pythonpath</code></strong>：把项目根加入 <strong>PYTHONPATH</strong>，便于 <code>from mypkg import ...</code> 式导入（按你包名调整）。  </li>
<li><strong><code>addopts</code></strong>：示例为较安静输出 <strong><code>-q</code></strong> 与失败摘要 <strong><code>-ra</code></strong>，可按喜好修改。</li>
</ul>
<h2 id="2-标记（markers）示例"><a href="#2-标记（markers）示例" class="headerlink" title="2. 标记（markers）示例"></a>2. 标记（markers）示例</h2><p>用于给用例打标签（慢测、集成测等）：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[tool.pytest.ini_options]</span></span><br><span class="line"><span class="attr">markers</span> = [</span><br><span class="line">    <span class="string">&quot;slow: marks tests as slow (deselect with &#x27;-m \&quot;not slow\&quot;&#x27;)&quot;</span>,</span><br><span class="line">    <span class="string">&quot;integration: integration tests&quot;</span>,</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>用例上写：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pytest</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@pytest.mark.slow</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test_heavy</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="keyword">assert</span> <span class="literal">True</span></span><br></pre></td></tr></table></figure>

<p>只跑非慢测：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest -m <span class="string">&quot;not slow&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、常用运行方式"><a href="#四、常用运行方式" class="headerlink" title="四、常用运行方式"></a>四、常用运行方式</h1><h2 id="1-指定文件或节点"><a href="#1-指定文件或节点" class="headerlink" title="1. 指定文件或节点"></a>1. 指定文件或节点</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest tests/test_example.py</span><br><span class="line">uv run pytest tests/test_example.py::test_add</span><br></pre></td></tr></table></figure>

<h2 id="2-失败即停、打印局部变量"><a href="#2-失败即停、打印局部变量" class="headerlink" title="2. 失败即停、打印局部变量"></a>2. 失败即停、打印局部变量</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest -x</span><br><span class="line">uv run pytest -l</span><br></pre></td></tr></table></figure>

<h2 id="3-并行（可选插件）"><a href="#3-并行（可选插件）" class="headerlink" title="3. 并行（可选插件）"></a>3. 并行（可选插件）</h2><p>若已 <strong><code>uv add --dev pytest-xdist</code></strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest -n auto</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、覆盖率（可选）"><a href="#五、覆盖率（可选）" class="headerlink" title="五、覆盖率（可选）"></a>五、覆盖率（可选）</h1><h2 id="1-安装-pytest-cov"><a href="#1-安装-pytest-cov" class="headerlink" title="1. 安装 pytest-cov"></a>1. 安装 pytest-cov</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add --dev pytest-cov</span><br></pre></td></tr></table></figure>

<h2 id="2-命令行生成报告"><a href="#2-命令行生成报告" class="headerlink" title="2. 命令行生成报告"></a>2. 命令行生成报告</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pytest --cov=src --cov-report=term-missing</span><br></pre></td></tr></table></figure>

<p>将 <strong><code>src</code></strong> 换成你的包目录名；或在 <strong><code>[tool.pytest.ini_options]</code></strong> 的 <strong><code>addopts</code></strong> 里固定 <strong><code>--cov=...</code></strong>（团队约定后再写死）。</p>
<hr>
<h1 id="六、与-CI、pre-commit"><a href="#六、与-CI、pre-commit" class="headerlink" title="六、与 CI、pre-commit"></a>六、与 CI、pre-commit</h1><ul>
<li><strong>CI</strong>：在 <strong><code>uv sync</code></strong> 后执行 <strong><code>uv run pytest</code></strong>，与本地一致。  </li>
<li><strong>pre-commit</strong>：可将 <strong><code>uv run pytest</code></strong> 接到 <strong><code>local</code> hook</strong>；注意全量测试较慢，常见做法是 <strong>CI 跑全量</strong>、pre-commit 只做 <strong>ruff</strong> 等轻量检查（见《UV项目开发添加pre-commit》）。</li>
</ul>
<hr>
<h1 id="七、常用命令速查"><a href="#七、常用命令速查" class="headerlink" title="七、常用命令速查"></a>七、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装</td>
<td><code>uv add --dev pytest</code></td>
</tr>
<tr>
<td>版本</td>
<td><code>uv run pytest --version</code></td>
</tr>
<tr>
<td>默认收集运行</td>
<td><code>uv run pytest</code></td>
</tr>
<tr>
<td>匹配标记</td>
<td><code>uv run pytest -m &quot;not slow&quot;</code></td>
</tr>
<tr>
<td>覆盖率</td>
<td><code>uv run pytest --cov=包或路径 --cov-report=term-missing</code></td>
</tr>
</tbody></table>
<p>官方文档：<a href="https://docs.pytest.org/">pytest</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
        <tag>Pytest</tag>
      </tags>
  </entry>
  <entry>
    <title>UV项目开发添加Ruff</title>
    <url>/2026/05/01/Python/UV%E9%A1%B9%E7%9B%AE%E5%BC%80%E5%8F%91%E6%B7%BB%E5%8A%A0Ruff/</url>
    <content><![CDATA[<p><strong>Ruff</strong> 是 <a href="https://astral.sh/">Astral</a> 旗下的 Python <strong>极快</strong> linter 与 formatter，可替代或搭配 flake8、isort、pyupgrade 等工具链。本文假定你已有 <strong>uv 管理的项目</strong>（含 <code>pyproject.toml</code>）；若还没有，可先按《UV从0到1创建项目》完成初始化。下文命令均在<strong>项目根目录</strong>执行。</p>
<hr>
<h1 id="一、在-uv-项目中安装-Ruff"><a href="#一、在-uv-项目中安装-Ruff" class="headerlink" title="一、在 uv 项目中安装 Ruff"></a>一、在 uv 项目中安装 Ruff</h1><h2 id="1-作为开发依赖添加"><a href="#1-作为开发依赖添加" class="headerlink" title="1. 作为开发依赖添加"></a>1. 作为开发依赖添加</h2><p>将 <strong>Ruff</strong> 写入开发依赖组（不进入生产安装的最小集合，便于 CI 与本地统一）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add --dev ruff</span><br></pre></td></tr></table></figure>

<p>若你使用 uv 的 <strong>dependency-groups</strong> 语法，等价写法可能是 <code>uv add --group dev ruff</code>，以本机 <code>uv add --help</code> 为准。</p>
<h2 id="2-确认可执行文件"><a href="#2-确认可执行文件" class="headerlink" title="2. 确认可执行文件"></a>2. 确认可执行文件</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run ruff --version</span><br></pre></td></tr></table></figure>

<p>能输出版本号即表示当前项目的虚拟环境里已可用 Ruff。</p>
<hr>
<h1 id="二、在-pyproject-toml-中配置-Ruff"><a href="#二、在-pyproject-toml-中配置-Ruff" class="headerlink" title="二、在 pyproject.toml 中配置 Ruff"></a>二、在 <code>pyproject.toml</code> 中配置 Ruff</h1><h2 id="1-最小可用配置"><a href="#1-最小可用配置" class="headerlink" title="1. 最小可用配置"></a>1. 最小可用配置</h2><p>在 <strong><code>pyproject.toml</code></strong> 末尾增加（或合并进已有 <strong><code>[tool.ruff]</code></strong>）：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[tool.ruff]</span></span><br><span class="line"><span class="attr">target-version</span> = <span class="string">&quot;py314&quot;</span>   <span class="comment"># 与项目 Python 一致；若 Ruff 版本较旧可改为 py313 等</span></span><br><span class="line"><span class="attr">line-length</span> = <span class="number">88</span></span><br><span class="line"></span><br><span class="line"><span class="section">[tool.ruff.lint]</span></span><br><span class="line"><span class="attr">select</span> = [<span class="string">&quot;E&quot;</span>, <span class="string">&quot;F&quot;</span>, <span class="string">&quot;I&quot;</span>, <span class="string">&quot;UP&quot;</span>, <span class="string">&quot;B&quot;</span>]   <span class="comment"># 按需增减规则集</span></span><br><span class="line"><span class="attr">ignore</span> = []</span><br><span class="line"></span><br><span class="line"><span class="section">[tool.ruff.format]</span></span><br><span class="line"><span class="attr">quote-style</span> = <span class="string">&quot;double&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>target-version</code></strong>：与 <code>requires-python</code> &#x2F; 实际运行版本对齐，避免规则与语法不匹配。  </li>
<li><strong><code>select</code></strong>：示例包含 pycodestyle 类（E）、Pyflakes（F）、import 排序（I）、pyupgrade（UP）、bugbear（B）；可按团队规范调整。</li>
</ul>
<h2 id="2-指定要检查的路径（可选）"><a href="#2-指定要检查的路径（可选）" class="headerlink" title="2. 指定要检查的路径（可选）"></a>2. 指定要检查的路径（可选）</h2><p>默认可对仓库根目录递归检查；若只想扫源码目录，可加：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[tool.ruff]</span></span><br><span class="line"><span class="attr">src</span> = [<span class="string">&quot;src&quot;</span>]</span><br></pre></td></tr></table></figure>

<p>目录名按你项目实际修改（例如只有 <code>hello_uv</code> 包目录则写 <code>[&quot;hello_uv&quot;]</code>）。</p>
<hr>
<h1 id="三、日常检查与格式化"><a href="#三、日常检查与格式化" class="headerlink" title="三、日常检查与格式化"></a>三、日常检查与格式化</h1><h2 id="1-静态检查（lint）"><a href="#1-静态检查（lint）" class="headerlink" title="1. 静态检查（lint）"></a>1. 静态检查（lint）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run ruff check .</span><br></pre></td></tr></table></figure>

<p>仅检查 <strong><code>src</code></strong> 时：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run ruff check src</span><br></pre></td></tr></table></figure>

<h2 id="2-自动修复可修问题"><a href="#2-自动修复可修问题" class="headerlink" title="2. 自动修复可修问题"></a>2. 自动修复可修问题</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run ruff check . --fix</span><br></pre></td></tr></table></figure>

<p><strong>建议</strong>在提交前于分支上执行，并 review diff；部分规则不会自动修复。</p>
<h2 id="3-格式化（formatter）"><a href="#3-格式化（formatter）" class="headerlink" title="3. 格式化（formatter）"></a>3. 格式化（formatter）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run ruff format .</span><br></pre></td></tr></table></figure>

<p>与 <strong>Black</strong> 风格接近；若团队已有 Black，需统一约定再启用 Ruff format。</p>
<hr>
<h1 id="四、与-uv-run-以外的用法"><a href="#四、与-uv-run-以外的用法" class="headerlink" title="四、与 uv run 以外的用法"></a>四、与 <code>uv run</code> 以外的用法</h1><h2 id="1-激活虚拟环境后直接使用"><a href="#1-激活虚拟环境后直接使用" class="headerlink" title="1. 激活虚拟环境后直接使用"></a>1. 激活虚拟环境后直接使用</h2><p>若已 <code>source .venv/bin/activate</code>（Linux &#x2F; macOS），可直接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ruff check .</span><br><span class="line">ruff format .</span><br></pre></td></tr></table></figure>

<h2 id="2-在-CI-中调用"><a href="#2-在-CI-中调用" class="headerlink" title="2. 在 CI 中调用"></a>2. 在 CI 中调用</h2><p>在流水线里进入仓库根目录后执行与本地相同的 <strong><code>uv sync</code></strong>（含 dev 依赖），再 <strong><code>uv run ruff check .</code></strong>，保证与开发机一致。</p>
<hr>
<h1 id="五、可选：忽略文件与-VS-Code"><a href="#五、可选：忽略文件与-VS-Code" class="headerlink" title="五、可选：忽略文件与 VS Code"></a>五、可选：忽略文件与 VS Code</h1><h2 id="1-ruffignore"><a href="#1-ruffignore" class="headerlink" title="1. .ruffignore"></a>1. <code>.ruffignore</code></h2><p>与 <code>.gitignore</code> 类似，在项目根创建 <strong><code>.ruffignore</code></strong>，排除迁移脚本、生成代码等：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.venv/</span><br><span class="line">__pycache__/</span><br><span class="line">*.egg-info/</span><br></pre></td></tr></table></figure>

<h2 id="2-编辑器"><a href="#2-编辑器" class="headerlink" title="2. 编辑器"></a>2. 编辑器</h2><p>VS Code &#x2F; Cursor 可安装官方 <strong>Ruff</strong> 扩展，与本地 <strong><code>uv run</code></strong> 或激活后的解释器指向同一 <code>.venv</code>，便于保存时格式化与行内提示。详见 <a href="https://docs.astral.sh/ruff/editors/setup/">Ruff 编辑器集成</a>。</p>
<hr>
<h1 id="六、常用命令速查"><a href="#六、常用命令速查" class="headerlink" title="六、常用命令速查"></a>六、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装为开发依赖</td>
<td><code>uv add --dev ruff</code></td>
</tr>
<tr>
<td>版本检查</td>
<td><code>uv run ruff --version</code></td>
</tr>
<tr>
<td>Lint 全仓库</td>
<td><code>uv run ruff check .</code></td>
</tr>
<tr>
<td>Lint 并修复</td>
<td><code>uv run ruff check . --fix</code></td>
</tr>
<tr>
<td>格式化</td>
<td><code>uv run ruff format .</code></td>
</tr>
</tbody></table>
<p>官方文档：<a href="https://docs.astral.sh/ruff/">Ruff</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
        <tag>Ruff</tag>
      </tags>
  </entry>
  <entry>
    <title>UV项目开发添加pre-commit</title>
    <url>/2026/05/01/Python/UV%E9%A1%B9%E7%9B%AE%E5%BC%80%E5%8F%91%E6%B7%BB%E5%8A%A0pre-commit/</url>
    <content><![CDATA[<p><strong>pre-commit</strong> 会在 <strong><code>git commit</code></strong> 前按 <strong><code>.pre-commit-config.yaml</code></strong> 里<strong>从上到下的顺序</strong>依次执行钩子，适合把「轻量修复 → Lint&#x2F;格式 → 类型检查 → 测试」串成<strong>前置流水线</strong>。本文在 <strong>uv 项目</strong>中安装 <strong>pre-commit</strong>，并用 <strong><code>local</code> + <code>uv run</code></strong> 调用与 <strong><code>uv.lock</code></strong> 一致的 <strong>Ruff</strong>、<strong>BasedPyright</strong>（与 Pyright 配置兼容）、<strong>pytest</strong>。假定仓库已用 Git 管理；初始化见《UV从0到1创建项目》，各工具单独说明见《UV项目开发添加Ruff》《UV项目开发添加Pyright》《UV项目开发添加Pytest》。命令默认在<strong>项目根目录</strong>执行。</p>
<hr>
<h1 id="一、安装-pre-commit-与前置工具"><a href="#一、安装-pre-commit-与前置工具" class="headerlink" title="一、安装 pre-commit 与前置工具"></a>一、安装 pre-commit 与前置工具</h1><h2 id="1-开发依赖（一次装齐）"><a href="#1-开发依赖（一次装齐）" class="headerlink" title="1. 开发依赖（一次装齐）"></a>1. 开发依赖（一次装齐）</h2><p>提交前若要跑 <strong>Ruff &#x2F; BasedPyright &#x2F; pytest</strong>，需先作为开发依赖安装（若已装可跳过对应包）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv add --dev pre-commit ruff basedpyright pytest</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>Ruff</strong>：Lint 与格式化（见《UV项目开发添加Ruff》）。  </li>
<li><strong>BasedPyright</strong>：静态类型检查（见《UV项目开发添加Pyright》）。  </li>
<li><strong>pytest</strong>：单元测试（见《UV项目开发添加Pytest》）。</li>
</ul>
<h2 id="2-确认-pre-commit"><a href="#2-确认-pre-commit" class="headerlink" title="2. 确认 pre-commit"></a>2. 确认 pre-commit</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pre-commit --version</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="二、添加配置文件"><a href="#二、添加配置文件" class="headerlink" title="二、添加配置文件"></a>二、添加配置文件</h1><h2 id="1-新建-pre-commit-config-yaml（含-Ruff、Pyright、pytest-前置链）"><a href="#1-新建-pre-commit-config-yaml（含-Ruff、Pyright、pytest-前置链）" class="headerlink" title="1. 新建 .pre-commit-config.yaml（含 Ruff、Pyright、pytest 前置链）"></a>1. 新建 <code>.pre-commit-config.yaml</code>（含 Ruff、Pyright、pytest 前置链）</h2><p>在项目根创建 <strong><code>.pre-commit-config.yaml</code></strong>。下面示例顺序为：<strong>通用清理 → Ruff → BasedPyright → pytest</strong>（可按团队习惯删减或调整顺序）。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">repos:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">repo:</span> <span class="string">https://github.com/pre-commit/pre-commit-hooks</span></span><br><span class="line">    <span class="attr">rev:</span> <span class="string">v5.0.0</span></span><br><span class="line">    <span class="attr">hooks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">trailing-whitespace</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">end-of-file-fixer</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">check-yaml</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">check-added-large-files</span></span><br><span class="line"></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">repo:</span> <span class="string">local</span></span><br><span class="line">    <span class="attr">hooks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">ruff-check</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">ruff</span> <span class="string">check</span> <span class="string">(uv)</span></span><br><span class="line">        <span class="attr">entry:</span> <span class="string">uv</span> <span class="string">run</span> <span class="string">ruff</span> <span class="string">check</span> <span class="string">--fix</span></span><br><span class="line">        <span class="attr">language:</span> <span class="string">system</span></span><br><span class="line">        <span class="attr">types_or:</span> [<span class="string">python</span>, <span class="string">pyi</span>]</span><br><span class="line">        <span class="attr">pass_filenames:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">ruff-format</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">ruff</span> <span class="string">format</span> <span class="string">(uv)</span></span><br><span class="line">        <span class="attr">entry:</span> <span class="string">uv</span> <span class="string">run</span> <span class="string">ruff</span> <span class="string">format</span></span><br><span class="line">        <span class="attr">language:</span> <span class="string">system</span></span><br><span class="line">        <span class="attr">types_or:</span> [<span class="string">python</span>, <span class="string">pyi</span>]</span><br><span class="line">        <span class="attr">pass_filenames:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">basedpyright</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">basedpyright</span> <span class="string">(uv)</span></span><br><span class="line">        <span class="attr">entry:</span> <span class="string">uv</span> <span class="string">run</span> <span class="string">basedpyright</span></span><br><span class="line">        <span class="attr">language:</span> <span class="string">system</span></span><br><span class="line">        <span class="attr">types_or:</span> [<span class="string">python</span>, <span class="string">pyi</span>]</span><br><span class="line">        <span class="attr">pass_filenames:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">pytest</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">pytest</span> <span class="string">(uv)</span></span><br><span class="line">        <span class="attr">entry:</span> <span class="string">uv</span> <span class="string">run</span> <span class="string">pytest</span></span><br><span class="line">        <span class="attr">language:</span> <span class="string">system</span></span><br><span class="line">        <span class="attr">pass_filenames:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><strong><code>pre-commit-hooks</code> 的 <code>rev</code></strong>：须与 <a href="https://github.com/pre-commit/pre-commit-hooks/releases">Releases</a> 上 tag 一致，文中为示例，可用 <strong><code>uv run pre-commit autoupdate</code></strong> 或手动更新。  </li>
<li><strong>Ruff</strong>：<strong><code>local</code> + <code>uv run</code></strong>，版本与 <strong><code>uv.lock</code></strong> 中 <strong><code>ruff</code></strong> 完全一致；<strong><code>pass_filenames: true</code></strong> 时仅对暂存 <strong><code>.py</code> &#x2F; <code>.pyi</code></strong> 跑 <strong><code>check --fix</code></strong> 与 <strong><code>format</code></strong>。建议顺序为先 <strong><code>ruff check --fix</code></strong> 再 <strong><code>ruff format</code></strong>（与常见习惯一致）。若某次提交未包含 Python 文件，这两个钩子会被跳过。  </li>
<li><strong>BasedPyright</strong>：<strong><code>pass_filenames: true</code></strong> 时把暂存路径传给 <strong><code>uv run basedpyright</code></strong>（需与 <strong><code>pyproject.toml</code></strong> 里 <strong><code>[tool.pyright]</code></strong> 的 <strong><code>include</code></strong> 等不矛盾）；若要整仓与 CI 一致可改为 <strong><code>pass_filenames: false</code></strong>。  </li>
<li><strong>pytest</strong>：示例为 <strong><code>pass_filenames: false</code></strong>，每次 <strong><code>uv run pytest</code></strong>（范围由 <strong><code>pyproject.toml</code></strong> 中 <strong><code>testpaths</code></strong> 等决定）；若需减负可改为 <strong><code>pass_filenames: true</code></strong> 并加 <strong><code>files: ^tests/</code></strong> 等。</li>
</ul>
<h2 id="2-与-uv-lock-对齐"><a href="#2-与-uv-lock-对齐" class="headerlink" title="2. 与 uv.lock 对齐"></a>2. 与 <code>uv.lock</code> 对齐</h2><p><strong>Ruff、BasedPyright、pytest</strong> 均通过 <strong><code>uv run</code></strong> 调用，<strong>与本地及 CI 的 <code>uv sync</code> 结果一致</strong>；仅 <strong><code>pre-commit-hooks</code></strong> 仍由 pre-commit 按 <strong><code>rev</code></strong> 拉取，与 Python 工具链版本无关。</p>
<hr>
<h1 id="三、安装-Git-钩子"><a href="#三、安装-Git-钩子" class="headerlink" title="三、安装 Git 钩子"></a>三、安装 Git 钩子</h1><h2 id="1-一次性安装到当前仓库"><a href="#1-一次性安装到当前仓库" class="headerlink" title="1. 一次性安装到当前仓库"></a>1. 一次性安装到当前仓库</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pre-commit install</span><br></pre></td></tr></table></figure>

<p>之后每次 <strong><code>git commit</code></strong> 会按配置<strong>自上而下</strong>执行：先通用钩子与 Ruff，再 <strong>BasedPyright</strong>，最后 <strong>pytest</strong>。</p>
<h2 id="2-手动跑一遍（不提交也可测）"><a href="#2-手动跑一遍（不提交也可测）" class="headerlink" title="2. 手动跑一遍（不提交也可测）"></a>2. 手动跑一遍（不提交也可测）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pre-commit run --all-files</span><br></pre></td></tr></table></figure>

<p>首次运行主要拉取 <strong><code>pre-commit-hooks</code></strong> 指定 <strong><code>rev</code></strong>；<strong><code>local</code></strong> 钩子不另建虚拟环境，直接调用本机 <strong><code>uv</code></strong>。</p>
<hr>
<h1 id="四、CI-与团队协作"><a href="#四、CI-与团队协作" class="headerlink" title="四、CI 与团队协作"></a>四、CI 与团队协作</h1><h2 id="1-在流水线中复现"><a href="#1-在流水线中复现" class="headerlink" title="1. 在流水线中复现"></a>1. 在流水线中复现</h2><p>CI 在 <strong><code>uv sync</code></strong>（含 dev）之后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run pre-commit run --all-files</span><br></pre></td></tr></table></figure>

<p>与本地同一套 <strong>Ruff → Pyright → pytest</strong> 前置链，减少环境漂移。</p>
<h2 id="2-跳过钩子（慎用）"><a href="#2-跳过钩子（慎用）" class="headerlink" title="2. 跳过钩子（慎用）"></a>2. 跳过钩子（慎用）</h2><p>仅应急排障：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit --no-verify</span><br></pre></td></tr></table></figure>

<p>勿作为常规绕过。</p>
<hr>
<h1 id="五、性能与拆分建议"><a href="#五、性能与拆分建议" class="headerlink" title="五、性能与拆分建议"></a>五、性能与拆分建议</h1><ul>
<li><strong>pytest 全量</strong>放在每次 commit 可能偏慢：可只在 <strong>CI</strong> 跑全量，本地 pre-commit 保留 <strong>Ruff + BasedPyright</strong>，或给 <strong>pytest</strong> 加 <strong><code>stages: [manual]</code></strong> 改为手动 <strong><code>pre-commit run pytest</code></strong>。  </li>
<li><strong>BasedPyright</strong> 对大仓库也偏慢：<strong><code>pass_filenames: true</code></strong> 只检查暂存文件通常更轻。详见《UV项目开发添加Pyright》中配置说明。</li>
</ul>
<hr>
<h1 id="六、常用命令速查"><a href="#六、常用命令速查" class="headerlink" title="六、常用命令速查"></a>六、常用命令速查</h1><table>
<thead>
<tr>
<th>目的</th>
<th>命令</th>
</tr>
</thead>
<tbody><tr>
<td>安装开发依赖</td>
<td><code>uv add --dev pre-commit ruff basedpyright pytest</code></td>
</tr>
<tr>
<td>安装 Git 钩子</td>
<td><code>uv run pre-commit install</code></td>
</tr>
<tr>
<td>全量跑 hooks</td>
<td><code>uv run pre-commit run --all-files</code></td>
</tr>
<tr>
<td>更新远程 rev</td>
<td><code>uv run pre-commit autoupdate</code></td>
</tr>
</tbody></table>
<p>官方文档：<a href="https://pre-commit.com/">pre-commit.com</a>。</p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>UV</tag>
        <tag>pre-commit</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis Bitmap 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/Bitmap%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Bitmap</strong> 不是独立「类型名」出现在 <strong><code>TYPE</code></strong> 里：底层仍是 <strong>String</strong>，通过 <strong><code>SETBIT</code> &#x2F; <code>GETBIT</code></strong> 把值当作<strong>位数组</strong>操作（偏移上限很大，实际受配置 <strong><code>proto-max-bulk-len</code></strong> 与内存约束）。适合<strong>签到、日活标记、布隆过滤器的底层</strong>等。</p>
<hr>
<h1 id="一、Bitmap-是什么"><a href="#一、Bitmap-是什么" class="headerlink" title="一、Bitmap 是什么"></a>一、Bitmap 是什么</h1><ol>
<li>按<strong>位偏移</strong>读写 <strong>0&#x2F;1</strong>，可 <strong><code>BITCOUNT</code></strong> 统计 <strong>1</strong> 的个数，<strong><code>BITOP AND/OR/XOR/NOT</code></strong> 做位运算。  </li>
<li><strong>省空间</strong>：大量布尔标记时比「每个用户一个 <strong>String key</strong>」更紧凑（仍要算总内存）。  </li>
<li><strong>offset</strong> 从 <strong>0</strong> 开始；未设置位视为 <strong>0</strong>。</li>
</ol>
<hr>
<h1 id="二、常用命令"><a href="#二、常用命令" class="headerlink" title="二、常用命令"></a>二、常用命令</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>SETBIT key offset value</code></strong></td>
<td><strong>value</strong> 为 <strong>0 或 1</strong></td>
</tr>
<tr>
<td><strong><code>GETBIT key offset</code></strong></td>
<td>读某一位</td>
</tr>
<tr>
<td><strong><code>BITCOUNT key [start end]</code></strong></td>
<td>统计 <strong>1</strong> 的个数（字节区间可选）</td>
</tr>
<tr>
<td><strong><code>BITOP operation destkey key [key ...]</code></strong></td>
<td>多 <strong>key</strong> 位运算写入 <strong>dest</strong></td>
</tr>
<tr>
<td><strong><code>BITPOS</code></strong></td>
<td>找第一个 <strong>0&#x2F;1</strong> 的位置</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>签到</strong>：<strong><code>sign:2026:05:user:1001</code></strong> 用日期映射 <strong>offset</strong>（需统一<strong>映射规则</strong>防冲突）。  </li>
<li><strong>日活标记</strong>：<strong><code>dau:2026-05-03</code></strong> 下 <strong>offset &#x3D; 用户整型 ID</strong>（<strong>ID 空间稀疏</strong>时浪费内存，要评估）。  </li>
<li><strong>与 HyperLogLog</strong>：<strong>Bitmap</strong> 偏「<strong>谁来了</strong>」的<strong>位图</strong>；<strong>UV 近似</strong>可用 <strong>HLL</strong>。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SETBIT sign:user:1 0 1</span><br><span class="line">redis-cli SETBIT sign:user:1 1 1</span><br><span class="line">redis-cli GETBIT sign:user:1 0</span><br><span class="line">redis-cli BITCOUNT sign:user:1</span><br><span class="line"></span><br><span class="line">redis-cli SETBIT bm:a 0 1</span><br><span class="line">redis-cli SETBIT bm:a 2 1</span><br><span class="line">redis-cli SETBIT bm:b 1 1</span><br><span class="line">redis-cli BITOP AND bm:and bm:a bm:b</span><br><span class="line">redis-cli GET bm:and</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>稀疏 ID</strong>：用户 <strong>ID</strong> 为 <strong>十亿级</strong> 时，<strong><code>SETBIT dau 1000000000 1</code></strong> 会促使<strong>底层分配很长空间</strong>，可能<strong>极度浪费</strong>；需<strong>分桶</strong>（按用户段拆 <strong>key</strong>）或换结构。  </li>
<li><strong>TTL</strong>：位图也是 <strong>String</strong>，可 <strong><code>EXPIRE</code></strong>。  </li>
<li><strong>统计口径</strong>：<strong><code>BITCOUNT</code></strong> 是「<strong>1 的个数</strong>」，业务上对应「签到天数」等要先设计好 <strong>offset</strong> 含义。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>HyperLogLog数据类型、应用场景及案例.md</code></strong></td>
<td>近似 <strong>UV</strong></td>
</tr>
<tr>
<td><strong><code>String数据类型、应用场景及案例.md</code></strong></td>
<td>普通 <strong>KV</strong></td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>Bitmap</tag>
        <tag>位图</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis Geospatial 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/Geospatial%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis 3.2+</strong> 提供 <strong>GEO</strong> 相关命令：底层用 <strong>Sorted Set</strong> 存储<strong>经编码的坐标</strong>，对外提供<strong>距离计算、半径查询、哈希</strong>等能力。适合「<strong>附近的人 &#x2F; 门店</strong>」等 <strong>LBS</strong>。</p>
<hr>
<h1 id="一、Geospatial-是什么"><a href="#一、Geospatial-是什么" class="headerlink" title="一、Geospatial 是什么"></a>一、Geospatial 是什么</h1><ol>
<li>数据仍落在 <strong>ZSet</strong> 上：<strong><code>TYPE key</code></strong> 一般为 <strong><code>zset</code></strong>；<strong>member</strong> 为地点 <strong>ID</strong>，<strong>score</strong> 为 <strong>Geohash 编码</strong>（使用者通常只调 <strong>GEO</strong> 命令）。  </li>
<li>坐标使用<strong>经度、纬度</strong>（<strong><code>GEOADD</code></strong> 参数顺序为 <strong><code>lon lat</code></strong>，勿写反）。  </li>
<li>地球模型为<strong>球面</strong>近似，极高精度场景需自行评估误差。</li>
</ol>
<hr>
<h1 id="二、常用命令"><a href="#二、常用命令" class="headerlink" title="二、常用命令"></a>二、常用命令</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>GEOADD key lon lat member</code></strong></td>
<td>添加或更新坐标</td>
</tr>
<tr>
<td><strong><code>GEODIST key m1 m2 [单位]</code></strong></td>
<td>两点距离；单位 <strong><code>m</code> &#x2F; <code>km</code> &#x2F; <code>ft</code> &#x2F; <code>mi</code></strong></td>
</tr>
<tr>
<td><strong><code>GEORADIUS</code> &#x2F; <code>GEORADIUSBYMEMBER</code></strong></td>
<td>以坐标点 &#x2F; 已知成员为中心，半径内成员（新版本有 <strong><code>GEOSEARCH</code></strong>，以文档为准）</td>
</tr>
<tr>
<td><strong><code>GEOPOS</code> &#x2F; <code>GEOHASH</code></strong></td>
<td>取坐标 &#x2F; <strong>Geohash</strong> 字符串</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>附近门店</strong>：<strong><code>GEOADD shops …</code></strong>，用户定位后 <strong><code>GEORADIUS</code></strong>。  </li>
<li><strong>配送范围校验</strong>：<strong><code>GEODIST</code></strong> 与阈值比较。  </li>
<li><strong>与外部地图</strong>：<strong>Redis</strong> 做<strong>粗筛</strong>，精确路径导航交给地图 <strong>API</strong>。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli GEOADD cities:cn 121.4737 31.2304 <span class="string">&quot;Shanghai&quot;</span> 116.4074 39.9042 <span class="string">&quot;Beijing&quot;</span></span><br><span class="line">redis-cli GEODIST cities:cn Shanghai Beijing km</span><br><span class="line">redis-cli GEORADIUS cities:cn 120.0 31.0 300 km WITHDIST ASC</span><br><span class="line">redis-cli GEOHASH cities:cn Shanghai</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>经度纬度顺序</strong>：<strong><code>lon</code> 在前 <code>lat</code> 在后</strong>。  </li>
<li><strong>跨版本 API</strong>：<strong>Redis 6.2+</strong> 引入 <strong><code>GEOSEARCH</code></strong> 等，生产以<strong>当前版本文档</strong>为准。  </li>
<li><strong>数据量</strong>：全国级 <strong>POI</strong> 全进一个 <strong>key</strong> 可能过大，可按<strong>城市分片</strong>多个 <strong>GEO key</strong>。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>ZSet数据类型、应用场景及案例.md</code></strong></td>
<td><strong>score</strong> 排序与排行榜（非 <strong>GEO</strong> 语义时直接用 <strong>ZSet</strong>）</td>
</tr>
<tr>
<td><strong><code>Redis-Docker.md</code></strong></td>
<td>端口与密码</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>GEO</tag>
        <tag>地理位置</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis Hash 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/Hash%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis Hash</strong> 是一个 <strong>field → value</strong> 的映射表，适合表示「一个对象的多属性」：可对<strong>单个字段</strong>读写，避免整颗 <strong>String JSON</strong> 反复序列化&#x2F;反序列化。下文按「命令 → 场景 → <strong><code>redis-cli</code></strong> 案例 → 注意点」组织,部署见 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、Hash-是什么"><a href="#一、Hash-是什么" class="headerlink" title="一、Hash 是什么"></a>一、Hash 是什么</h1><ol>
<li>一个 <strong>key</strong> 下挂多对 <strong>field &#x2F; value</strong>，<strong>field</strong> 与 <strong>value</strong> 均为字符串（二进制安全）。  </li>
<li><strong><code>TYPE key</code></strong> 显示 <strong><code>hash</code></strong>。  </li>
<li>适合「用户资料、购物车行、配置项分组」等<strong>属性较多且常单独更新</strong>的场景。</li>
</ol>
<hr>
<h1 id="二、常用命令（必会子集）"><a href="#二、常用命令（必会子集）" class="headerlink" title="二、常用命令（必会子集）"></a>二、常用命令（必会子集）</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>HSET key field value [field value …]</code></strong></td>
<td>设置一个或多个字段（<strong>Redis 4.0+</strong> 推荐 <strong><code>HSET</code></strong> 多字段；旧 <strong><code>HMSET</code></strong> 已废弃）</td>
</tr>
<tr>
<td><strong><code>HGET</code> &#x2F; <code>HMGET</code> &#x2F; <code>HGETALL</code></strong></td>
<td>读单字段 &#x2F; 多字段 &#x2F; 全表</td>
</tr>
<tr>
<td><strong><code>HDEL</code></strong></td>
<td>删字段</td>
</tr>
<tr>
<td><strong><code>HEXISTS</code> &#x2F; <code>HLEN</code></strong></td>
<td>字段是否存在 &#x2F; 字段个数</td>
</tr>
<tr>
<td><strong><code>HINCRBY</code> &#x2F; <code>HINCRBYFLOAT</code></strong></td>
<td>字段数值原子增减</td>
</tr>
<tr>
<td><strong><code>HSCAN</code></strong></td>
<td>大 <strong>Hash</strong> 迭代遍历（生产避免 <strong><code>HGETALL</code></strong> 撑爆网络）</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>用户资料</strong>：<strong><code>user:1001</code></strong> 下 <strong><code>name</code> &#x2F; <code>vip</code> &#x2F; <code>city</code></strong> 等，改一项只动一个 <strong>field</strong>。  </li>
<li><strong>购物车行</strong>：<strong><code>cart:uid:1001</code></strong> 下 <strong><code>sku:2001</code> → 数量 JSON 或纯数字</strong>。  </li>
<li><strong>对象缓存</strong>：比整串 <strong>String</strong> 更易做<strong>局部更新</strong>；仍要注意<strong>过期策略</strong>与<strong>缓存一致性</strong>。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli HSET user:1001 name Alice vip 1 city Shanghai</span><br><span class="line">redis-cli HGET user:1001 name</span><br><span class="line">redis-cli HMGET user:1001 name city</span><br><span class="line">redis-cli HINCRBY user:1001 vip 1</span><br><span class="line">redis-cli HGETALL user:1001</span><br><span class="line">redis-cli HDEL user:1001 city</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>大 Hash</strong>：字段极多时 <strong><code>HGETALL</code></strong> 成本高，列表页用 <strong><code>HSCAN</code></strong> 或只取需要的 <strong><code>HMGET</code></strong>。  </li>
<li><strong>小 Hash 编码</strong>：字段少且短时可能 <strong>ziplist</strong> 等紧凑编码，内存更省（<strong><code>OBJECT ENCODING</code></strong> 可观察）。  </li>
<li><strong>与 String JSON 选型</strong>：读多写少、整体替换频繁 → <strong>String</strong>；字段独立更新多 → <strong>Hash</strong>。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>String数据类型、应用场景及案例.md</code></strong></td>
<td>整串缓存、计数、<strong><code>SET</code> 选项</strong></td>
</tr>
<tr>
<td><strong><code>Redis-Docker.md</code></strong></td>
<td><strong><code>requirepass</code></strong>、持久化卷</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>Hash</tag>
        <tag>缓存</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis HyperLogLog 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/HyperLogLog%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>HyperLogLog（HLL）</strong> 是 <strong>Redis 2.8.9+</strong> 提供的<strong>概率型数据结构</strong>：用<strong>固定约 12KB</strong>（标准实现）内存估算<strong>集合基数</strong>（<strong>不重复元素个数</strong>），结果<strong>有误差</strong>（标准误差约 <strong>0.81%</strong>），换<strong>极低内存</strong>统计 <strong>UV</strong>。部署见 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、HyperLogLog-是什么"><a href="#一、HyperLogLog-是什么" class="headerlink" title="一、HyperLogLog 是什么"></a>一、HyperLogLog 是什么</h1><ol>
<li>不是存明细，只支持 <strong><code>PFADD</code> 添加元素</strong>、<strong><code>PFCOUNT</code> 估算基数</strong>、<strong><code>PFMERGE</code> 合并多个 HLL</strong>。  </li>
<li><strong>适合</strong>：海量 <strong>UV</strong>、<strong>IP 去重计数</strong>等<strong>允许近似</strong>的报表。  </li>
<li><strong>不适合</strong>：必须<strong>精确</strong>计数、需要<strong>枚举</strong>曾出现过的元素。</li>
</ol>
<hr>
<h1 id="二、常用命令"><a href="#二、常用命令" class="headerlink" title="二、常用命令"></a>二、常用命令</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>PFADD key element [element ...]</code></strong></td>
<td>向 <strong>HLL</strong> 增加元素（可批量）</td>
</tr>
<tr>
<td><strong><code>PFCOUNT key [key ...]</code></strong></td>
<td>单个或合并估算基数</td>
</tr>
<tr>
<td><strong><code>PFMERGE destkey sourcekey [sourcekey ...]</code></strong></td>
<td>合并到 <strong>dest</strong></td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>页面 &#x2F; 活动 UV</strong>：<strong><code>PFADD uv:2026-05-03:page1 &lt;userId 或 sessionId&gt;</code></strong>，定时 <strong><code>PFCOUNT</code></strong>。  </li>
<li><strong>多维度合并</strong>：按天分 <strong>key</strong>，日报用 <strong><code>PFMERGE</code></strong> 聚合成周 <strong>UV</strong>。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli PFADD uv:demo u1 u2 u3 u2 u1</span><br><span class="line">redis-cli PFCOUNT uv:demo</span><br><span class="line"></span><br><span class="line">redis-cli PFADD uv:day1 a b c</span><br><span class="line">redis-cli PFADD uv:day2 c d e</span><br><span class="line">redis-cli PFMERGE uv:week uv:day1 uv:day2</span><br><span class="line">redis-cli PFCOUNT uv:week</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>误差</strong>：报表若要求<strong>精确 UV</strong>，需 <strong>Set</strong>（内存大）或<strong>离线数仓</strong>。  </li>
<li><strong>元素语义</strong>：<strong><code>PFADD</code></strong> 把参数当<strong>字节串</strong>；业务上应用<strong>统一 ID</strong>（如用户 <strong>ID</strong>），勿混多种粒度。  </li>
<li><strong>无法查询成员</strong>：不能问「谁访问过」，只能问「大约多少人」。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>Set数据类型、应用场景及案例.md</code></strong></td>
<td>精确去重（内存换准确）</td>
</tr>
<tr>
<td><strong><code>Bitmap数据类型、应用场景及案例.md</code></strong></td>
<td>按位统计另一种思路</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>HyperLogLog</tag>
        <tag>基数统计</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis List 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/List%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis List</strong> 是<strong>双向链表</strong>上的字符串元素序列，按下标与范围访问，两端 <strong><code>LPUSH</code> &#x2F; <code>RPUSH</code> &#x2F; <code>LPOP</code> &#x2F; <code>RPOP</code></strong> 时间复杂度多为 <strong>O(1)</strong>。适合<strong>队列、栈、最新 N 条动态</strong>等场景。写法对齐同仓库 <strong>Hexo</strong> 教程：<strong>小节 + 命令表 + 可复制示例</strong>。部署见 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、List-是什么"><a href="#一、List-是什么" class="headerlink" title="一、List 是什么"></a>一、List 是什么</h1><ol>
<li><strong>有序</strong>、<strong>可重复</strong>；同一 <strong>value</strong> 可出现多次。  </li>
<li><strong><code>TYPE key</code></strong> 为 <strong><code>list</code></strong>。  </li>
<li><strong>中间插入&#x2F;删除</strong>代价高于两端（需遍历指针）；设计时尽量<strong>从两头消费</strong>。</li>
</ol>
<hr>
<h1 id="二、常用命令（必会子集）"><a href="#二、常用命令（必会子集）" class="headerlink" title="二、常用命令（必会子集）"></a>二、常用命令（必会子集）</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>LPUSH</code> &#x2F; <code>RPUSH</code></strong></td>
<td>左&#x2F;右入队</td>
</tr>
<tr>
<td><strong><code>LPOP</code> &#x2F; <code>RPOP</code></strong></td>
<td>左&#x2F;右出队</td>
</tr>
<tr>
<td><strong><code>LRANGE key start stop</code></strong></td>
<td>闭区间下标范围（<strong><code>-1</code></strong> 表末尾）</td>
</tr>
<tr>
<td><strong><code>LLEN</code></strong></td>
<td>长度</td>
</tr>
<tr>
<td><strong><code>LINDEX</code> &#x2F; <code>LSET</code></strong></td>
<td>按下标读 &#x2F; 写</td>
</tr>
<tr>
<td><strong><code>BLPOP</code> &#x2F; <code>BRPOP</code></strong></td>
<td>阻塞出队（<strong>简单队列</strong>常用）</td>
</tr>
</tbody></table>
<p><strong>说明</strong>：高版本提供 <strong><code>LMOVE</code> &#x2F; <code>BLMOVE</code></strong> 等，用于<strong>安全队列</strong>迁移元素；与已废弃的 <strong><code>BRPOPLPUSH</code></strong> 同类需求可查当前版本文档。</p>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>FIFO 队列</strong>：<strong><code>RPUSH</code> + <code>LPOP</code></strong>（或反向一对）。  </li>
<li><strong>最新消息流</strong>：<strong><code>LPUSH</code> + <code>LTRIM key 0 99</code></strong> 保留最近 <strong>100</strong> 条。  </li>
<li><strong>阻塞消费</strong>：<strong><code>BLPOP</code></strong> 多个 <strong>key</strong> 时按顺序等待，可做<strong>简单任务队列</strong>（复杂语义建议 <strong>Stream</strong> 或专业消息中间件）。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli DEL mq:email</span><br><span class="line">redis-cli RPUSH mq:email <span class="string">&quot;job1&quot;</span> <span class="string">&quot;job2&quot;</span></span><br><span class="line">redis-cli LPOP mq:email</span><br><span class="line"></span><br><span class="line">redis-cli LPUSH feed:user:1 <span class="string">&quot;msg3&quot;</span> <span class="string">&quot;msg2&quot;</span> <span class="string">&quot;msg1&quot;</span></span><br><span class="line">redis-cli LRANGE feed:user:1 0 9</span><br><span class="line">redis-cli LTRIM feed:user:1 0 19</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>消费者崩溃</strong>：<strong><code>LPOP</code></strong> 后数据即丢，需业务<strong>确认机制</strong>或改用 <strong><code>BRPOP</code> + 处理完再 ack&#96;</strong> 等模式；严肃场景评估 <strong>Stream</strong>、<strong>RabbitMQ</strong> 等。  </li>
<li><strong>长列表</strong>：<strong><code>LRANGE 0 -1</code></strong> 大列表慎用；分页用 <strong><code>LRANGE</code></strong> 分段。  </li>
<li><strong>与 Set&#x2F;ZSet 区别</strong>：<strong>List 有序可重复</strong>；<strong>Set 无序唯一</strong>；<strong>ZSet 有序唯一带分值</strong>。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>Set数据类型、应用场景及案例.md</code></strong> &#x2F; <strong><code>ZSet数据类型、应用场景及案例.md</code></strong></td>
<td>去重集合、排行榜</td>
</tr>
<tr>
<td><strong><code>Redis-Docker.md</code></strong></td>
<td>连接与安全</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>List</tag>
        <tag>队列</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 部署 Redis</title>
    <url>/2026/05/01/Redis/Redis-Docker/</url>
    <content><![CDATA[<p>在 <strong>Docker</strong> 中运行 <strong>Redis</strong>，适合本地缓存、会话与消息流式处理、或在 <strong>Kubernetes</strong> 中单实例部署。下文给出<strong>镜像选型</strong>、<strong>一条命令起容器</strong>、<strong>Compose 一键起栈</strong>与<strong>一份多文档 K8s 清单</strong>；<strong><code>requirepass</code></strong> 与数据卷请按环境自行替换，勿把生产密钥提交进仓库。</p>
<hr>
<h1 id="一、前置条件"><a href="#一、前置条件" class="headerlink" title="一、前置条件"></a>一、前置条件</h1><p>本机已安装 <strong><code>docker</code></strong>，并能使用 <strong><code>docker compose</code></strong>（<strong>Compose V2</strong> 插件，命令为 <strong><code>docker compose</code></strong>；若仍使用旧版独立程序，则将下文中的 <strong><code>docker compose</code></strong> 换成 <strong><code>docker-compose</code></strong>）。</p>
<p>验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>满足即可，与宿主机操作系统无关。</p>
<hr>
<h1 id="二、Redis-镜像选择"><a href="#二、Redis-镜像选择" class="headerlink" title="二、Redis 镜像选择"></a>二、Redis 镜像选择</h1><h2 id="1-官方镜像（推荐）"><a href="#1-官方镜像（推荐）" class="headerlink" title="1. 官方镜像（推荐）"></a>1. 官方镜像（推荐）</h2><ul>
<li><strong>仓库</strong>：<strong><code>redis</code></strong>（Docker Hub 官方库：<a href="https://hub.docker.com/_/redis/tags">https://hub.docker.com/_/redis/tags</a>）。  </li>
<li><strong>常用标签</strong>：  <ul>
<li><strong><code>redis:7.2</code></strong>：<strong>7.2.x</strong> 线，生态与客户端兼容面广。  </li>
<li><strong><code>redis:7.4</code></strong> 或 <strong><code>redis:latest</code></strong>：较新线（以 Hub 标签为准）。</li>
</ul>
</li>
<li><strong>生产建议</strong>：固定小版本标签（如 <strong><code>redis:7.2.5</code></strong>）或记录 <strong>digest</strong>，避免 <strong><code>latest</code></strong> 漂移导致行为变化。</li>
</ul>
<h2 id="2-变体说明"><a href="#2-变体说明" class="headerlink" title="2. 变体说明"></a>2. 变体说明</h2><table>
<thead>
<tr>
<th>需求</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>持久化</strong></td>
<td>开发常用 <strong><code>--appendonly yes</code></strong>（<strong>AOF</strong>）；也可配合 <strong><code>RDB</code></strong> 快照参数，按数据重要性选择。</td>
</tr>
<tr>
<td><strong>访问控制</strong></td>
<td>通过 <strong><code>redis-server --requirepass &lt;密码&gt;</code></strong> 或 <strong><code>ACL</code></strong> 配置；未设置密码时<strong>任何人可读写</strong>，仅用于隔离网络内的临时环境。</td>
</tr>
<tr>
<td><strong>ARM64</strong>（Apple Silicon 等）</td>
<td>官方 <strong><code>redis</code></strong> 多架构镜像一般可直接拉取；若某标签缺架构，换标签或查 Hub <strong>OS&#x2F;ARCH</strong>。</td>
</tr>
<tr>
<td><strong>极简&#x2F;定制</strong></td>
<td>社区有 <strong><code>bitnami/redis</code></strong> 等，环境变量名与默认配置与官方不同，切换镜像时需对照该镜像文档。</td>
</tr>
</tbody></table>
<h2 id="3-数据目录"><a href="#3-数据目录" class="headerlink" title="3. 数据目录"></a>3. 数据目录</h2><p>默认数据目录 <strong><code>/data</code></strong>（<strong>AOF</strong> &#x2F; <strong>RDB</strong> 文件所在）；挂载卷后便于备份与 <strong><code>docker compose down -v</code></strong> 时区分是否保留数据。</p>
<hr>
<p><strong>1Panel（推荐）</strong>：在 <strong>Linux</strong> 宿主机上若希望少手写命令、仍以容器方式跑 <strong>Redis</strong>，可先安装 <a href="https://1panel.cn/"><strong>1Panel</strong></a>（开源 <strong>Linux</strong> 运维面板），在 <strong>「应用商店」</strong> 中选择 <strong>Redis</strong> 一键部署：由面板管理镜像版本、数据卷、端口、<strong><code>requirepass</code></strong> 与（可选）持久化策略及备份。与下文 <strong><code>docker run</code> &#x2F; Compose &#x2F; K8s<code>** 的卷路径、密码与 **</code>redis-server&#96; 参数</strong>对齐或排障时，自 <strong># 三</strong> 起各节仍可作对照。适合单台 <strong>VPS</strong>、家庭服务器等「快速起缓存 &#x2F; 会话存储」的场景。</p>
<hr>
<h1 id="三、单容器一键部署（docker-run）"><a href="#三、单容器一键部署（docker-run）" class="headerlink" title="三、单容器一键部署（docker run）"></a>三、单容器一键部署（<code>docker run</code>）</h1><p>在<strong>当前目录</strong>建数据目录（仅首次，可选）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ./redis-data</span><br></pre></td></tr></table></figure>

<p><strong>一条命令</strong>启动（<strong>AOF</strong> + <strong>密码</strong>；示例仅供本地，请替换密码）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name redis-dev -p 6379:6379 -v <span class="string">&quot;<span class="subst">$(pwd)</span>/redis-data:/data&quot;</span> redis:7.2 redis-server --appendonly <span class="built_in">yes</span> --requirepass dev-change-me</span><br></pre></td></tr></table></figure>

<p>常用运维一条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f redis-dev</span><br><span class="line">docker stop redis-dev &amp;&amp; docker <span class="built_in">rm</span> redis-dev</span><br></pre></td></tr></table></figure>

<p>本机探测：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli -h 127.0.0.1 -p 6379 -a dev-change-me ping</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、Docker-Compose-一键部署"><a href="#四、Docker-Compose-一键部署" class="headerlink" title="四、Docker Compose 一键部署"></a>四、Docker Compose 一键部署</h1><p>将下面内容保存为 <strong><code>docker-compose.redis.yml</code></strong>（与 <strong><code># 一</code></strong> 要求一致：已能执行 <strong><code>docker compose</code></strong>）。敏感项建议放在同目录 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>），勿长期写死在仓库。</p>
<p><strong>一条命令</strong>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.redis.yml up -d</span><br></pre></td></tr></table></figure>

<p>以下为 <strong><code>docker-compose.redis.yml</code></strong> 全文：<strong><code>version</code></strong>、<strong><code>name</code></strong>、<strong>命名卷</strong>、<strong>健康检查</strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">redis-stack</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">redis:7.2</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">redis-7</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;6379:6379&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">REDIS_PASSWORD:</span> <span class="string">$&#123;REDIS_PASSWORD:-dev-change-me&#125;</span></span><br><span class="line">    <span class="attr">command:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">redis-server</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">--appendonly</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;yes&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">--requirepass</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;$&#123;REDIS_PASSWORD:-dev-change-me&#125;&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">redis_data:/data</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;redis-cli -a \&quot;$$REDIS_PASSWORD\&quot; ping | grep PONG&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">3s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">12</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">10s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">redis_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br></pre></td></tr></table></figure>

<p>同目录可选 <strong><code>.env</code></strong>（勿提交）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">REDIS_PASSWORD=your-strong-password</span><br></pre></td></tr></table></figure>

<p>查看状态、日志、停止并删除容器（保留数据卷）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.redis.yml ps</span><br><span class="line">docker compose -f docker-compose.redis.yml logs -f redis</span><br><span class="line">docker compose -f docker-compose.redis.yml down</span><br></pre></td></tr></table></figure>

<p><strong><code>down</code></strong> 默认保留 <strong><code>redis_data</code></strong>；若需清空数据加 <strong><code>docker compose -f docker-compose.redis.yml down -v</code></strong>（<strong>会删库</strong>）。</p>
<hr>
<h1 id="五、Kubernetes-一键部署（单文件完整清单）"><a href="#五、Kubernetes-一键部署（单文件完整清单）" class="headerlink" title="五、Kubernetes 一键部署（单文件完整清单）"></a>五、Kubernetes 一键部署（单文件完整清单）</h1><p>将下面内容保存为 <strong><code>redis-k8s.yaml</code></strong> 后，<strong>一条命令</strong>应用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f redis-k8s.yaml</span><br></pre></td></tr></table></figure>

<p>清单含 <strong>Secret</strong>（密码）、<strong>PVC</strong>、<strong>Deployment</strong>、<strong>ClusterIP Service（6379）</strong>。未写 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>；无默认 <strong>SC</strong> 时请在 <strong>PVC</strong> 中补上 <strong><code>storageClassName</code></strong>。命名空间为 <strong><code>default</code></strong>，可按需修改 <strong><code>metadata.namespace</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">redis-secret</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">redis-password:</span> <span class="string">&quot;请替换为强密码&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">redis-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">10Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">redis</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">redis</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">redis</span></span><br><span class="line">  <span class="attr">strategy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">Recreate</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">redis</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">redis</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">redis:7.2</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">6379</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">redis</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">REDIS_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">redis-secret</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">redis-password</span></span><br><span class="line">          <span class="attr">command:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">/bin/sh</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">exec</span> <span class="string">redis-server</span> <span class="string">--appendonly</span> <span class="literal">yes</span> <span class="string">--requirepass</span> <span class="string">&quot;$REDIS_PASSWORD&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/data</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">exec:</span></span><br><span class="line">              <span class="attr">command:</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">/bin/sh</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">redis-cli</span> <span class="string">-a</span> <span class="string">&quot;$REDIS_PASSWORD&quot;</span> <span class="string">ping</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">livenessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">10</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">6</span></span><br><span class="line">          <span class="attr">resources:</span></span><br><span class="line">            <span class="attr">requests:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;128Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;100m&quot;</span></span><br><span class="line">            <span class="attr">limits:</span></span><br><span class="line">              <span class="attr">memory:</span> <span class="string">&quot;512Mi&quot;</span></span><br><span class="line">              <span class="attr">cpu:</span> <span class="string">&quot;500m&quot;</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">redis-data</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">redis</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">redis</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">redis</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">redis</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">6379</span></span><br></pre></td></tr></table></figure>

<p>查看资源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get deploy,svc,pvc -l app=redis</span><br></pre></td></tr></table></figure>

<p>集群内连接示例（按 <strong>namespace</strong> 与 <strong>Secret</strong> 替换密码）：<strong><code>redis://:请替换@redis.default.svc.cluster.local:6379/0</code></strong>（客户端库参数名可能为 <strong><code>password</code></strong> &#x2F; <strong><code>username</code></strong>，以所用驱动为准）。</p>
<p><strong>说明</strong>：<strong><code>strategy: Recreate</code></strong> 避免 <strong>RWO</strong> 卷被多副本 <strong>RollingUpdate</strong> 同时挂载；<strong>主从、哨兵、集群</strong> 请使用 <strong>Redis Operator</strong>、<strong>Helm Chart</strong> 或 <strong>StatefulSet</strong> 方案，勿直接水平扩展上述 <strong>Deployment</strong> 当作分片集群使用。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>前置</strong></td>
<td>已安装 <strong><code>docker</code></strong> 与 <strong><code>docker compose</code></strong>（或 <strong><code>docker-compose</code></strong>）</td>
</tr>
<tr>
<td><strong>镜像</strong></td>
<td>优先官方 <strong><code>redis:7.2</code></strong> &#x2F; 固定补丁标签；<strong><code>--requirepass</code></strong> 与 <strong><code>ACL</code></strong> 按安全要求配置</td>
</tr>
<tr>
<td><strong>单容器</strong></td>
<td><strong><code>mkdir</code></strong>（可选）后一条 <strong><code>docker run ... redis-server ...</code></strong></td>
</tr>
<tr>
<td><strong>Compose</strong></td>
<td>保存 <strong><code>docker-compose.redis.yml</code></strong> 后 <strong><code>docker compose -f ... up -d</code></strong></td>
</tr>
<tr>
<td><strong>Kubernetes</strong></td>
<td>保存 <strong><code>redis-k8s.yaml</code></strong> 后 <strong><code>kubectl apply -f ...</code></strong></td>
</tr>
</tbody></table>
<p>生产环境请启用 <strong>TLS</strong>、<strong>ACL</strong>、持久化策略与监控；<strong>Secret</strong> 建议对接 <strong>External Secrets</strong> &#x2F; <strong>Sealed Secrets</strong> 等管控流程。</p>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis 入门：安装、CLI 与数据类型</title>
    <url>/2026/05/01/Redis/Redis/</url>
    <content><![CDATA[<p><strong>Redis</strong> 是内存型数据结构存储，常用作<strong>缓存</strong>、<strong>会话</strong>、<strong>排行榜</strong>、<strong>消息流</strong>与<strong>分布式锁</strong>等；单线程执行命令模型简单，但<strong>持久化</strong>、<strong>主从</strong>与<strong>集群</strong>仍须按业务规划。下面从「<strong>官网与概念</strong> → <strong>安装验证</strong> → <strong>启动与 <code>redis-cli</code></strong> → <strong>常用数据类型</strong> → <strong>持久化与安全要点</strong> → <strong>与容器部署的关系</strong>」顺序写清楚；命令在 <strong>Linux &#x2F; macOS</strong> 终端或 <strong>Windows WSL</strong> 中均可使用（生产环境请固定版本并限制暴露面）。</p>
<hr>
<h1 id="一、官网与定位"><a href="#一、官网与定位" class="headerlink" title="一、官网与定位"></a>一、官网与定位</h1><ul>
<li><strong>Redis 文档（当前产品站）</strong>：<a href="https://redis.io/docs/">https://redis.io/docs/</a>  </li>
<li><strong>下载与发行说明</strong>：<a href="https://redis.io/downloads/">https://redis.io/downloads/</a>  </li>
<li><strong>命令参考</strong>：<a href="https://redis.io/commands/">https://redis.io/commands/</a></li>
</ul>
<p><strong>Redis</strong> 以<strong>键值</strong>为核心，值支持多种<strong>数据类型</strong>（字符串、哈希、列表等）；默认监听 <strong><code>6379</code></strong>。与关系型数据库不同，数据主要在内存中，须结合<strong>淘汰策略</strong>、<strong>持久化</strong>与<strong>备份</strong>设计，避免单点丢数。</p>
<hr>
<h1 id="二、安装与验证"><a href="#二、安装与验证" class="headerlink" title="二、安装与验证"></a>二、安装与验证</h1><h2 id="1-Linux（包管理器，适合快速体验）"><a href="#1-Linux（包管理器，适合快速体验）" class="headerlink" title="1. Linux（包管理器，适合快速体验）"></a>1. Linux（包管理器，适合快速体验）</h2><p><strong>Debian &#x2F; Ubuntu</strong> 示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y redis-server</span><br><span class="line">redis-server --version</span><br></pre></td></tr></table></figure>

<p><strong>RHEL &#x2F; Rocky &#x2F; Fedora</strong> 可使用 <strong><code>dnf install redis</code></strong>（包名以发行版仓库为准）。</p>
<h2 id="2-macOS（Homebrew）"><a href="#2-macOS（Homebrew）" class="headerlink" title="2. macOS（Homebrew）"></a>2. macOS（Homebrew）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install redis</span><br><span class="line">redis-server --version</span><br></pre></td></tr></table></figure>

<h2 id="3-Windows-与容器"><a href="#3-Windows-与容器" class="headerlink" title="3. Windows 与容器"></a>3. Windows 与容器</h2><p><strong>Redis</strong> 官方以 <strong>Linux</strong> 为主要运行环境；<strong>Windows</strong> 本地开发常见做法是 <strong>WSL2</strong>、虚拟机或 <strong>Docker</strong>。若希望用 <strong>Docker Compose &#x2F; Kubernetes</strong> 一键起栈、配置 <strong><code>requirepass</code></strong> 与卷，见站内 <a href="/2026/05/01/Redis-Docker/">Docker 部署 Redis</a>。</p>
<hr>
<h1 id="三、启动服务与-redis-cli"><a href="#三、启动服务与-redis-cli" class="headerlink" title="三、启动服务与 redis-cli"></a>三、启动服务与 <code>redis-cli</code></h1><h2 id="1-前台-后台启动（Linux-包安装常见路径）"><a href="#1-前台-后台启动（Linux-包安装常见路径）" class="headerlink" title="1. 前台 &#x2F; 后台启动（Linux 包安装常见路径）"></a>1. 前台 &#x2F; 后台启动（Linux 包安装常见路径）</h2><p>前台调试（终端占用，<strong>Ctrl+C</strong> 结束）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server</span><br></pre></td></tr></table></figure>

<p>或使用发行版提供的 <strong>systemd</strong> 服务（名称可能是 <strong><code>redis</code></strong> 或 <strong><code>redis-server</code></strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> --now redis-server</span><br><span class="line"><span class="built_in">sudo</span> systemctl status redis-server</span><br></pre></td></tr></table></figure>

<h2 id="2-连接与探测"><a href="#2-连接与探测" class="headerlink" title="2. 连接与探测"></a>2. 连接与探测</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli ping</span><br></pre></td></tr></table></figure>

<p>返回 <strong><code>PONG</code></strong> 表示服务端已响应。指定主机与端口：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli -h 127.0.0.1 -p 6379 ping</span><br></pre></td></tr></table></figure>

<p>若启用了密码（见下文「六」），需 <strong><code>AUTH &lt;密码&gt;</code></strong> 或 <strong><code>redis-cli -a &lt;密码&gt;</code></strong>（<strong><code>-a</code> 可能进 shell 历史，生产慎用</strong>）。</p>
<h2 id="3-两条最常用读写命令"><a href="#3-两条最常用读写命令" class="headerlink" title="3. 两条最常用读写命令"></a>3. 两条最常用读写命令</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">127.0.0.1:6379&gt; SET mykey &quot;hello&quot;</span><br><span class="line">OK</span><br><span class="line">127.0.0.1:6379&gt; GET mykey</span><br><span class="line">&quot;hello&quot;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="四、常用数据类型（速览）"><a href="#四、常用数据类型（速览）" class="headerlink" title="四、常用数据类型（速览）"></a>四、常用数据类型（速览）</h1><table>
<thead>
<tr>
<th>类型</th>
<th>典型用途</th>
<th>命令示例</th>
</tr>
</thead>
<tbody><tr>
<td><strong>String</strong></td>
<td>缓存、计数器</td>
<td><strong><code>SET</code> &#x2F; <code>GET</code> &#x2F; <code>INCR</code></strong></td>
</tr>
<tr>
<td><strong>Hash</strong></td>
<td>对象字段</td>
<td><strong><code>HSET</code> &#x2F; <code>HGET</code> &#x2F; <code>HGETALL</code></strong></td>
</tr>
<tr>
<td><strong>List</strong></td>
<td>队列、时间线</td>
<td><strong><code>LPUSH</code> &#x2F; <code>RPOP</code> &#x2F; <code>LRANGE</code></strong></td>
</tr>
<tr>
<td><strong>Set</strong></td>
<td>去重集合</td>
<td><strong><code>SADD</code> &#x2F; <code>SMEMBERS</code> &#x2F; <code>SISMEMBER</code></strong></td>
</tr>
<tr>
<td><strong>Sorted Set</strong></td>
<td>排行榜</td>
<td><strong><code>ZADD</code> &#x2F; <code>ZRANGE</code> &#x2F; <code>ZRANK</code></strong></td>
</tr>
<tr>
<td><strong>Stream</strong></td>
<td>消息流、消费者组</td>
<td><strong><code>XADD</code> &#x2F; <code>XREADGROUP</code></strong></td>
</tr>
</tbody></table>
<p>更多语义与复杂度见官方 <strong>Commands</strong> 文档；<strong><code>KEYS *</code></strong> 在生产大库上可能阻塞，排查键名请优先 <strong><code>SCAN</code></strong>。</p>
<hr>
<h1 id="五、持久化：RDB-与-AOF"><a href="#五、持久化：RDB-与-AOF" class="headerlink" title="五、持久化：RDB 与 AOF"></a>五、持久化：RDB 与 AOF</h1><table>
<thead>
<tr>
<th>方式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>RDB</strong></td>
<td>按策略生成<strong>时间点快照</strong>（<strong><code>.rdb</code></strong>），恢复快、可能丢失最后一次快照后的写入</td>
</tr>
<tr>
<td><strong>AOF</strong></td>
<td>追加写日志（<strong><code>.aof</code></strong>），可配置 <strong><code>fsync</code></strong> 频率，<strong>更耐久</strong>、文件通常更大</td>
</tr>
</tbody></table>
<p>配置在 <strong><code>redis.conf</code></strong> 中（包安装路径因发行版而异，常见 <strong><code>/etc/redis/redis.conf</code></strong>）。<strong>Docker</strong> 场景下常通过 <strong><code>redis-server</code> 参数</strong> 或挂载配置覆盖，与 <a href="/2026/05/01/Redis-Docker/">Docker 部署 Redis</a> 中 <strong><code>--appendonly</code></strong> 等示例对照即可。</p>
<hr>
<h1 id="六、安全与网络要点"><a href="#六、安全与网络要点" class="headerlink" title="六、安全与网络要点"></a>六、安全与网络要点</h1><ol>
<li><strong>默认无密码</strong>时，只要网络能连上 <strong><code>6379</code></strong> 即可读写，<strong>仅允许本机或可信内网</strong>；公网暴露须配 <strong><code>requirepass</code></strong> 与 <strong>Redis ACL</strong>，并配合防火墙、<strong>TLS</strong>（视版本与部署形态）。  </li>
<li><strong><code>bind</code></strong>：默认若监听 <strong><code>0.0.0.0</code></strong>，表示所有网卡可连；缩小监听地址可减少误暴露。  </li>
<li><strong>危险命令</strong>：<strong><code>FLUSHALL</code></strong>、<strong><code>CONFIG</code></strong> 等在生产应通过 <strong><code>rename-command</code></strong> 禁用或限制执行身份。</li>
</ol>
<hr>
<h1 id="七、小结"><a href="#七、小结" class="headerlink" title="七、小结"></a>七、小结</h1><table>
<thead>
<tr>
<th>场景</th>
<th>做法</th>
</tr>
</thead>
<tbody><tr>
<td>安装验证</td>
<td><strong><code>redis-server --version</code></strong>；<strong><code>redis-cli ping</code></strong></td>
</tr>
<tr>
<td>本地读写</td>
<td><strong><code>SET</code> &#x2F; <code>GET</code></strong>；按业务选 <strong>Hash &#x2F; List &#x2F; ZSet</strong> 等</td>
</tr>
<tr>
<td>持久化</td>
<td><strong><code>redis.conf</code></strong> 中 <strong>RDB &#x2F; AOF</strong> 策略；容器见 <strong>Redis-Docker</strong> 文</td>
</tr>
<tr>
<td>安全</td>
<td><strong>密码 &#x2F; ACL</strong> + 网络隔离；避免公网裸 <strong><code>6379</code></strong></td>
</tr>
<tr>
<td>容器与编排</td>
<td><a href="/2026/05/01/Redis-Docker/">Docker 部署 Redis</a></td>
</tr>
</tbody></table>
<p>更深入的<strong>主从复制</strong>、<strong>Sentinel 高可用</strong>、<strong>Cluster 分片</strong>与客户端连接池调优，请以当前 <strong>Redis</strong> 大版本官方文档为准，并与业务 <strong>RPO&#x2F;RTO</strong> 一起评审。</p>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>缓存</tag>
        <tag>NoSQL</tag>
        <tag>redis-cli</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis Set 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/Set%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis Set</strong> 是<strong>无序</strong>字符串<strong>集合</strong>，成员<strong>唯一</strong>（再次 <strong><code>SADD</code></strong> 同一成员不会重复插入）。适合<strong>标签、去重集合、交并差集合运算</strong>等。姊妹文：<strong><code>ZSet数据类型、应用场景及案例.md</code></strong>（需<strong>分值排序</strong>时用 <strong>ZSet</strong>）；部署 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、Set-是什么"><a href="#一、Set-是什么" class="headerlink" title="一、Set 是什么"></a>一、Set 是什么</h1><ol>
<li><strong><code>TYPE key</code></strong> 为 <strong><code>set</code></strong>。  </li>
<li><strong>不保证顺序</strong>（遍历顺序实现依赖，勿当有序结构用）。  </li>
<li><strong>成员即字符串</strong>；集合间可做 <strong><code>SINTER</code> &#x2F; <code>SUNION</code> &#x2F; <code>SDIFF</code></strong>。</li>
</ol>
<hr>
<h1 id="二、常用命令（必会子集）"><a href="#二、常用命令（必会子集）" class="headerlink" title="二、常用命令（必会子集）"></a>二、常用命令（必会子集）</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>SADD</code> &#x2F; <code>SREM</code></strong></td>
<td>增删成员</td>
</tr>
<tr>
<td><strong><code>SMEMBERS</code></strong></td>
<td>返回全部成员（大集合慎用）</td>
</tr>
<tr>
<td><strong><code>SISMEMBER</code> &#x2F; <code>SCARD</code></strong></td>
<td>是否在集合 &#x2F; 成员个数</td>
</tr>
<tr>
<td><strong><code>SRANDMEMBER</code> &#x2F; <code>SPOP</code></strong></td>
<td>随机读 &#x2F; 随机弹出</td>
</tr>
<tr>
<td><strong><code>SINTER</code> &#x2F; <code>SUNION</code> &#x2F; <code>SDIFF</code></strong></td>
<td>交 &#x2F; 并 &#x2F; 差（可带 <strong><code>STORE</code></strong> 写到新 <strong>key</strong>）</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>标签</strong>：<strong><code>SADD article:7:tags Java Redis</code></strong>。  </li>
<li><strong>抽奖池 &#x2F; 去重 ID 池</strong>：<strong><code>SPOP</code></strong> 不放回抽样（需理解<strong>并发下</strong>多个客户端抢 <strong>SPOP</strong> 的语义）。  </li>
<li><strong>共同关注</strong>：<strong><code>SINTER user:1:following user:2:following</code></strong>。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SADD skills:user:1 Java Go Redis</span><br><span class="line">redis-cli SISMEMBER skills:user:1 Java</span><br><span class="line">redis-cli SCARD skills:user:1</span><br><span class="line"></span><br><span class="line">redis-cli SADD <span class="built_in">set</span>:a 1 2 3</span><br><span class="line">redis-cli SADD <span class="built_in">set</span>:b 2 3 4</span><br><span class="line">redis-cli SINTER <span class="built_in">set</span>:a <span class="built_in">set</span>:b</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>大 Set</strong>：<strong><code>SMEMBERS</code></strong> 一次性拉全量可能阻塞与占带宽，用 <strong><code>SSCAN</code></strong> 迭代。  </li>
<li><strong>与 List</strong>：<strong>Set</strong> 无序且唯一；<strong>List</strong> 有序可重复。  </li>
<li><strong>需要按分数排序</strong>（排行榜）→ 用 <strong>ZSet</strong>，不要用 <strong>Set</strong> 硬撑。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>ZSet数据类型、应用场景及案例.md</code></strong></td>
<td>分值、排名、范围查询</td>
</tr>
<tr>
<td><strong><code>List数据类型、应用场景及案例.md</code></strong></td>
<td>队列与有序序列</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>Set</tag>
        <tag>去重</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis String 数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/String%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis String</strong> 是最常用的类型：本质是<strong>二进制安全</strong>的字节序列（可存文本、<strong>JSON</strong>、序列化对象、图片片段等），单值上限约 <strong>512MB</strong>（实际业务应远小于该上限）。下文按「命令 → 场景 → <strong><code>redis-cli</code></strong> 案例 → 注意点」组织，部署见 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、String-是什么（和「键」的关系）"><a href="#一、String-是什么（和「键」的关系）" class="headerlink" title="一、String 是什么（和「键」的关系）"></a>一、String 是什么（和「键」的关系）</h1><ol>
<li><strong>Redis</strong> 是 <strong>KV</strong> 模型：<strong>键（key）</strong> 永远是字符串；<strong>值（value）</strong> 可以是 <strong>String &#x2F; Hash &#x2F; List</strong> 等类型之一。  </li>
<li>本文说的 <strong>String</strong> 指 <strong>value 的类型</strong>为 <strong>string</strong>（<strong><code>TYPE key</code></strong> 显示 <strong><code>string</code></strong>）。  </li>
<li><strong>编码</strong>：底层可能为 <strong><code>int</code></strong>、<strong><code>embstr</code></strong>、<strong><code>raw</code></strong> 等（<strong><code>OBJECT ENCODING key</code></strong> 可查看），对使用者多数时候<strong>透明</strong>，选型时更关心<strong>语义与命令</strong>。</li>
</ol>
<hr>
<h1 id="二、常用命令（必会子集）"><a href="#二、常用命令（必会子集）" class="headerlink" title="二、常用命令（必会子集）"></a>二、常用命令（必会子集）</h1><h2 id="1-读写与删除"><a href="#1-读写与删除" class="headerlink" title="1. 读写与删除"></a>1. 读写与删除</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET user:1001 <span class="string">&quot;Alice&quot;</span></span><br><span class="line">redis-cli GET user:1001</span><br><span class="line">redis-cli DEL user:1001</span><br></pre></td></tr></table></figure>

<p><strong><code>MSET</code> &#x2F; <code>MGET</code></strong>：一次多键，减少往返（注意原子性与键命名规范）。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli MSET a 1 b 2 c 3</span><br><span class="line">redis-cli MGET a b c</span><br></pre></td></tr></table></figure>

<h2 id="2-带条件的写入与过期（业务高频）"><a href="#2-带条件的写入与过期（业务高频）" class="headerlink" title="2. 带条件的写入与过期（业务高频）"></a>2. 带条件的写入与过期（业务高频）</h2><p><strong><code>SET</code></strong> 常用选项（<strong>Redis 2.6+</strong> 语法，具体以你版本文档为准）：</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>EX 秒</code> &#x2F; <code>PX 毫秒</code></strong></td>
<td>设置<strong>过期时间</strong>（<strong>TTL</strong>）</td>
</tr>
<tr>
<td><strong><code>NX</code></strong></td>
<td>仅当 <strong>key 不存在</strong> 时写入（常用于<strong>抢锁占位</strong>）</td>
</tr>
<tr>
<td><strong><code>XX</code></strong></td>
<td>仅当 <strong>key 已存在</strong> 时覆盖</td>
</tr>
</tbody></table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET session:abc <span class="string">&quot;uid=1001&quot;</span> EX 3600 NX</span><br><span class="line">redis-cli TTL session:abc</span><br></pre></td></tr></table></figure>

<h2 id="3-计数器：INCR-DECR-INCRBY"><a href="#3-计数器：INCR-DECR-INCRBY" class="headerlink" title="3. 计数器：INCR &#x2F; DECR &#x2F; INCRBY"></a>3. 计数器：<code>INCR</code> &#x2F; <code>DECR</code> &#x2F; <code>INCRBY</code></h2><p><strong><code>INCR</code></strong> 在<strong>值可解析为整数</strong>时原子自增；不存在时按 <strong>0</strong> 开始。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET article:42:views 0</span><br><span class="line">redis-cli INCR article:42:views</span><br><span class="line">redis-cli INCRBY article:42:views 10</span><br></pre></td></tr></table></figure>

<h2 id="4-其它实用命令"><a href="#4-其它实用命令" class="headerlink" title="4. 其它实用命令"></a>4. 其它实用命令</h2><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>APPEND</code></strong></td>
<td>尾部追加字节</td>
</tr>
<tr>
<td><strong><code>STRLEN</code></strong></td>
<td>返回字节长度</td>
</tr>
<tr>
<td><strong><code>GETRANGE</code> &#x2F; <code>SETRANGE</code></strong></td>
<td>按偏移读写子串（仍整体是 <strong>String</strong>）</td>
</tr>
<tr>
<td><strong><code>GETSET</code></strong></td>
<td>读旧值并写新值（单原子操作，适合简单状态切换）</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>缓存整页或对象</strong>：值存 <strong>JSON</strong> 字符串，配合 <strong><code>EX</code></strong> 做<strong>短期缓存</strong>；更新数据库后<strong>删键</strong>或<strong>覆盖</strong>避免脏读。  </li>
<li><strong>会话 &#x2F; 登录态</strong>：<strong><code>SET … EX …</code></strong> 存 <strong>token → 用户信息</strong>；过期自动消失。  </li>
<li><strong>计数与限流</strong>：<strong><code>INCR</code></strong> 做浏览量、接口调用次数；与<strong>滑动窗口</strong>等逻辑配合（复杂限流可再引入 <strong>Hash</strong> &#x2F; <strong>ZSet</strong>）。  </li>
<li><strong>分布式锁（谨慎）</strong>：<strong><code>SET key token NX PX ttl</code></strong> 仅占位；完整锁需<strong>唯一 token</strong>、<strong>Lua 校验身份释放</strong>、<strong>续期</strong>、<strong>Redlock 争议</strong>等专题，本文只给<strong>入门占位</strong>思路。  </li>
<li><strong>配置与功能开关</strong>：<strong><code>SET feature:x:enabled 1</code></strong>，读时 <strong><code>GET</code></strong> 转布尔。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli-可直接敲）"><a href="#四、综合案例（redis-cli-可直接敲）" class="headerlink" title="四、综合案例（redis-cli 可直接敲）"></a>四、综合案例（<code>redis-cli</code> 可直接敲）</h1><p>以下默认本机 <strong><code>redis-cli</code></strong> 无密码；若启用了 <strong><code>requirepass</code></strong>，请加 <strong><code>-a &lt;密码&gt;</code></strong>（见 <strong><code>Redis-Docker.md</code></strong>）。</p>
<h2 id="1-用户信息缓存（带过期）"><a href="#1-用户信息缓存（带过期）" class="headerlink" title="1. 用户信息缓存（带过期）"></a>1. 用户信息缓存（带过期）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET user:1001 <span class="string">&quot;&#123;\&quot;name\&quot;:\&quot;Alice\&quot;,\&quot;vip\&quot;:1&#125;&quot;</span> EX 300</span><br><span class="line">redis-cli GET user:1001</span><br><span class="line">redis-cli TTL user:1001</span><br></pre></td></tr></table></figure>

<h2 id="2-文章阅读量"><a href="#2-文章阅读量" class="headerlink" title="2. 文章阅读量"></a>2. 文章阅读量</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET post:7:views 0</span><br><span class="line">redis-cli INCR post:7:views</span><br><span class="line">redis-cli GET post:7:views</span><br></pre></td></tr></table></figure>

<h2 id="3-简单「占位锁」（演示用，生产请用成熟方案）"><a href="#3-简单「占位锁」（演示用，生产请用成熟方案）" class="headerlink" title="3. 简单「占位锁」（演示用，生产请用成熟方案）"></a>3. 简单「占位锁」（演示用，生产请用成熟方案）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 占坑成功返回 OK；重复执行因 NX 失败返回 (nil)</span></span><br><span class="line">redis-cli SET lock:order:8899 <span class="string">&quot;worker-1&quot;</span> NX EX 30</span><br><span class="line">redis-cli GET lock:order:8899</span><br><span class="line">redis-cli DEL lock:order:8899</span><br></pre></td></tr></table></figure>

<h2 id="4-条件更新：仅存在时改"><a href="#4-条件更新：仅存在时改" class="headerlink" title="4. 条件更新：仅存在时改"></a>4. 条件更新：仅存在时改</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli SET config:site:title <span class="string">&quot;旧标题&quot;</span></span><br><span class="line">redis-cli SET config:site:title <span class="string">&quot;新标题&quot;</span> XX</span><br><span class="line">redis-cli GET config:site:title</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项与选型"><a href="#五、注意事项与选型" class="headerlink" title="五、注意事项与选型"></a>五、注意事项与选型</h1><ol>
<li><strong>大 value</strong>：单 <strong>String</strong> 过大占用内存与网络带宽，<strong>序列化大对象</strong>可评估 <strong>Hash 分字段</strong>或<strong>拆 key</strong>。  </li>
<li><strong>热 key</strong>：同一 <strong>key</strong> 超高并发时，<strong>单线程处理</strong>该 <strong>key</strong> 易成瓶颈，需业务侧<strong>分片</strong>或换结构。  </li>
<li><strong>KEY 命名</strong>：建议 <strong><code>业务:实体:id:属性&gt;</code></strong> 一类可读前缀，避免<strong>全局 <code>KEYS *</code></strong> 在生产使用（用 <strong><code>SCAN</code></strong>）。  </li>
<li><strong>持久化与丢失</strong>：<strong>String</strong> 在内存里，是否落盘取决于 <strong>RDB&#x2F;AOF</strong> 与策略；<strong>缓存≠数据库</strong>，关键数据以<strong>库</strong>为准并设计<strong>降级</strong>。  </li>
<li><strong>字符集</strong>：<strong>Redis String</strong> 是<strong>字节</strong>；<strong>UTF-8</strong> 文本由客户端编码&#x2F;解码，不要在服务端假设「一定是中文几个字符」与<strong>字节长度</strong>一一对应。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>Hash数据类型、应用场景及案例.md</code></strong></td>
<td>字段级对象</td>
</tr>
<tr>
<td><strong><code>List数据类型、应用场景及案例.md</code></strong></td>
<td>队列 &#x2F; 栈</td>
</tr>
<tr>
<td><strong><code>Set数据类型、应用场景及案例.md</code></strong></td>
<td>去重集合</td>
</tr>
<tr>
<td><strong><code>ZSet数据类型、应用场景及案例.md</code></strong></td>
<td>排行榜 &#x2F; 延时队列</td>
</tr>
<tr>
<td><strong><code>HyperLogLog数据类型、应用场景及案例.md</code></strong></td>
<td>近似 <strong>UV</strong></td>
</tr>
<tr>
<td><strong><code>Bitmap数据类型、应用场景及案例.md</code></strong></td>
<td>位图签到等</td>
</tr>
<tr>
<td><strong><code>Geospatial数据类型、应用场景及案例.md</code></strong></td>
<td><strong>LBS</strong></td>
</tr>
<tr>
<td><strong><code>Redis-Docker.md</code></strong></td>
<td>容器启动、<strong><code>requirepass</code></strong>、<strong>AOF</strong> 卷</td>
</tr>
<tr>
<td><strong><code>Redis.md</code></strong></td>
<td>总览（可后续补充）</td>
</tr>
</tbody></table>
<p>更复杂的消费组、持久化流等，可另文写 <strong>Stream</strong> 等类型。</p>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>缓存</tag>
        <tag>String</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis ZSet（Sorted Set）数据类型、应用场景及案例</title>
    <url>/2026/05/03/Redis/ZSet%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E3%80%81%E5%BA%94%E7%94%A8%E5%9C%BA%E6%99%AF%E5%8F%8A%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><strong>Redis ZSet（Sorted Set）</strong> 是<strong>成员唯一</strong>、且每个成员带一个 <strong>score（双精度浮点）</strong> 的有序集合；按 <strong>score</strong> 排序，<strong>score</strong> 相同则按<strong>字典序</strong>排成员。适合<strong>排行榜、延时队列（score&#x3D;时间戳）<strong>等。</strong><code>Set数据类型、应用场景及案例.md</code></strong>；部署 <strong><code>Redis-Docker.md</code></strong>。</p>
<hr>
<h1 id="一、ZSet-是什么"><a href="#一、ZSet-是什么" class="headerlink" title="一、ZSet 是什么"></a>一、ZSet 是什么</h1><ol>
<li><strong><code>TYPE key</code></strong> 为 <strong><code>zset</code></strong>。  </li>
<li><strong>member 唯一</strong>；同一 <strong>member</strong> 再次 <strong><code>ZADD</code></strong> 会<strong>更新 score</strong>。  </li>
<li>范围查询、排名 <strong><code>ZRANK</code>&#x2F;<code>ZREVRANK</code></strong> 为高频 API。</li>
</ol>
<hr>
<h1 id="二、常用命令（必会子集）"><a href="#二、常用命令（必会子集）" class="headerlink" title="二、常用命令（必会子集）"></a>二、常用命令（必会子集）</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>ZADD key score member</code></strong></td>
<td>添加或更新分值</td>
</tr>
<tr>
<td><strong><code>ZRANGE</code> &#x2F; <code>ZREVRANGE</code></strong></td>
<td>按排名升&#x2F;降序取片段（<strong><code>WITHSCORES</code></strong> 带出分数）</td>
</tr>
<tr>
<td><strong><code>ZRANGEBYSCORE</code></strong></td>
<td>按 <strong>score</strong> 区间取</td>
</tr>
<tr>
<td><strong><code>ZRANK</code> &#x2F; <code>ZREVRANK</code></strong></td>
<td>升序 &#x2F; 降序名次（<strong>0</strong> 基）</td>
</tr>
<tr>
<td><strong><code>ZSCORE</code> &#x2F; <code>ZCARD</code> &#x2F; <code>ZINCRBY</code></strong></td>
<td>查分 &#x2F; 成员数 &#x2F; 原子加分</td>
</tr>
<tr>
<td><strong><code>ZREM</code> &#x2F; <code>ZPOPMIN</code> &#x2F; <code>ZPOPMAX</code></strong></td>
<td>删除成员 &#x2F; 弹出最小&#x2F;大</td>
</tr>
</tbody></table>
<hr>
<h1 id="三、典型应用场景"><a href="#三、典型应用场景" class="headerlink" title="三、典型应用场景"></a>三、典型应用场景</h1><ol>
<li><strong>游戏 &#x2F; 活动排行榜</strong>：<strong>member&#x3D;用户 ID</strong>，<strong>score&#x3D;积分</strong>。  </li>
<li><strong>延时任务</strong>：<strong>score&#x3D;执行时间戳</strong>，定时 <strong><code>ZRANGEBYSCORE</code></strong> 拉到期任务再 <strong><code>ZREM</code></strong>。  </li>
<li><strong>热搜权重</strong>：结合时间衰减可周期性重算 <strong>score</strong>（逻辑在应用层）。</li>
</ol>
<hr>
<h1 id="四、综合案例（redis-cli）"><a href="#四、综合案例（redis-cli）" class="headerlink" title="四、综合案例（redis-cli）"></a>四、综合案例（<code>redis-cli</code>）</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli ZADD board:week1 100 user:1 200 user:2 150 user:3</span><br><span class="line">redis-cli ZREVRANGE board:week1 0 9 WITHSCORES</span><br><span class="line">redis-cli ZRANK board:week1 user:3</span><br><span class="line">redis-cli ZINCRBY board:week1 50 user:3</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="五、注意事项"><a href="#五、注意事项" class="headerlink" title="五、注意事项"></a>五、注意事项</h1><ol>
<li><strong>热 key</strong>：单排行榜 <strong>key</strong> 超高并发时考虑<strong>分片</strong>或<strong>本地缓存 + 合并</strong>。  </li>
<li><strong>大 ZSet</strong>：避免一次 <strong><code>ZRANGE 0 -1</code></strong>；分页用 <strong><code>ZRANGE</code></strong> 窗口。  </li>
<li><strong>score 精度</strong>：浮点比较注意<strong>误差</strong>；时间戳建议统一<strong>毫秒或秒</strong>。</li>
</ol>
<hr>
<h1 id="六、延伸阅读"><a href="#六、延伸阅读" class="headerlink" title="六、延伸阅读"></a>六、延伸阅读</h1><table>
<thead>
<tr>
<th>文档</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>Set数据类型、应用场景及案例.md</code></strong></td>
<td>只要去重、不要排序</td>
</tr>
<tr>
<td><strong><code>List数据类型、应用场景及案例.md</code></strong></td>
<td>简单队列</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>ZSet</tag>
        <tag>排行榜</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust</title>
    <url>/2026/05/01/Rust/Rust/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Rust</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 入门：环境、骨架与启动</title>
    <url>/2026/05/01/SpringBoot/SpringBoot/</url>
    <content><![CDATA[<p><strong>Spring Boot</strong> 在 <strong>Spring Framework</strong> 之上提供约定优于配置的起步依赖、内嵌 <strong>Servlet</strong> 容器与生产级运维钩子，用少量配置即可跑起 <strong>Web</strong>、<strong>数据访问</strong>、<strong>消息</strong> 等能力。下文从<strong>官方文档入口</strong>、<strong>环境</strong>、<strong>创建第一个应用</strong>、<strong>主类与配置</strong>到<strong>常用延伸</strong>，按步骤写清楚；与 <strong>Spring Cloud</strong> 同栈的 <strong>BOM</strong> 与 <strong>Gradle</strong> 细节见站内 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>，<strong>Gradle</strong> 安装与镜像见 <strong><code>source/_posts/Java/gradle.md</code></strong>（《Gradle 构建工具》）。</p>
<hr>
<h1 id="一、官网与文档"><a href="#一、官网与文档" class="headerlink" title="一、官网与文档"></a>一、官网与文档</h1><ul>
<li><strong>Spring Boot 项目页</strong>：<a href="https://spring.io/projects/spring-boot">https://spring.io/projects/spring-boot</a>  </li>
<li><strong>参考手册（当前稳定版）</strong>：<a href="https://docs.spring.io/spring-boot/reference/">https://docs.spring.io/spring-boot/reference/</a>  </li>
<li><strong>Spring Initializr</strong>（在线生成工程）：<a href="https://start.spring.io/">https://start.spring.io/</a>  </li>
<li><strong>Spring Guides</strong>：<a href="https://spring.io/guides">https://spring.io/guides</a></li>
</ul>
<p>版本线简述：<strong>Spring Boot 3.5.x</strong> 为 <strong>3.x</strong> 长期支持线之一；<strong>Spring Boot 4.0.x</strong> 与 <strong>Spring Framework 7</strong>、<strong>Spring Cloud 2025.1.x</strong> 同世代，站内 <strong>SCA 2025.1.x</strong> 示例采用 <strong>Boot 4.0.x</strong>。新建工程前请到 <strong>Spring Boot</strong> 发行说明核对与你所选 <strong>JDK</strong>、依赖的兼容性。</p>
<hr>
<h1 id="二、环境准备"><a href="#二、环境准备" class="headerlink" title="二、环境准备"></a>二、环境准备</h1><h2 id="1-JDK"><a href="#1-JDK" class="headerlink" title="1. JDK"></a>1. JDK</h2><p><strong>Spring Boot 3.x &#x2F; 4.x</strong> 要求 <strong>Java 17</strong> 及以上（部分组件推荐 <strong>21</strong>）。终端验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -version</span><br><span class="line">javac -version</span><br></pre></td></tr></table></figure>

<h2 id="2-IDE（可选但推荐）"><a href="#2-IDE（可选但推荐）" class="headerlink" title="2. IDE（可选但推荐）"></a>2. IDE（可选但推荐）</h2><p><strong>IntelliJ IDEA</strong>、<strong>VS Code</strong>（<strong>Extension Pack for Java</strong>）、<strong>Eclipse</strong> 均可；下文以「能打开 <strong>Maven</strong> &#x2F; <strong>Gradle</strong> 工程并运行 <strong><code>main</code></strong>」为前提。</p>
<h2 id="3-构建工具"><a href="#3-构建工具" class="headerlink" title="3. 构建工具"></a>3. 构建工具</h2><ul>
<li><strong>Maven</strong>：多数示例以 <strong><code>pom.xml</code></strong> 展示依赖；本机需能执行 <strong><code>mvn</code></strong>。  </li>
<li><strong>Gradle</strong>：推荐 <strong>Kotlin DSL</strong>（<strong><code>build.gradle.kts</code></strong>）；安装、<strong>Wrapper</strong> 与国内镜像见 <strong><code>source/_posts/Java/gradle.md</code></strong>。</li>
</ul>
<hr>
<h1 id="三、创建第一个-Spring-Boot-应用"><a href="#三、创建第一个-Spring-Boot-应用" class="headerlink" title="三、创建第一个 Spring Boot 应用"></a>三、创建第一个 Spring Boot 应用</h1><h2 id="1-Spring-Initializr（最快）"><a href="#1-Spring-Initializr（最快）" class="headerlink" title="1. Spring Initializr（最快）"></a>1. Spring Initializr（最快）</h2><ol>
<li>打开 <a href="https://start.spring.io/">https://start.spring.io/</a>  </li>
<li><strong>Project</strong> 选 <strong>Maven</strong> 或 <strong>Gradle</strong>；<strong>Language</strong> 选 <strong>Java</strong>；<strong>Spring Boot</strong> 选稳定版（与上节版本线一致）。  </li>
<li><strong>Group &#x2F; Artifact</strong> 按规范填写（如 <strong><code>com.example</code></strong> &#x2F; <strong><code>demo</code></strong>）。  </li>
<li><strong>Dependencies</strong> 至少勾选 <strong>Spring Web</strong>（若要健康检查再加 <strong>Spring Boot Actuator</strong>）。  </li>
<li>点击 <strong>Generate</strong>，解压后<strong>用 IDE 打开根目录</strong>（含 <strong><code>pom.xml</code></strong> 或 <strong><code>build.gradle.kts</code></strong> 的目录）。</li>
</ol>
<h2 id="2-IntelliJ-IDEA-向导"><a href="#2-IntelliJ-IDEA-向导" class="headerlink" title="2. IntelliJ IDEA 向导"></a>2. IntelliJ IDEA 向导</h2><p><strong>File → New → Project</strong> 中选择 <strong>Spring Initializr</strong>（或 <strong>New Project</strong> 中带 <strong>Spring Boot</strong> 的向导，视 <strong>IDEA</strong> 版本而定），与网页版 <strong>Initializr</strong> 等价：选 <strong>JDK</strong>、<strong>Boot</strong> 版本与依赖后生成工程。</p>
<h2 id="3-与手写构建脚本的关系"><a href="#3-与手写构建脚本的关系" class="headerlink" title="3. 与手写构建脚本的关系"></a>3. 与手写构建脚本的关系</h2><p><strong>Initializr</strong> 生成的是<strong>可编译骨架</strong>；若需叠加 <strong>Spring Cloud Alibaba</strong> 等 <strong>BOM</strong>，往往要在 <strong><code>pom.xml</code> &#x2F; <code>build.gradle.kts</code></strong> 中<strong>手工补充</strong> <strong><code>dependencyManagement</code></strong> 与依赖坐标，与 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a> 一文中的写法一致，<strong>不要</strong>假设向导已包含 <strong>Cloud</strong>。</p>
<hr>
<h1 id="四、工程骨架与启动"><a href="#四、工程骨架与启动" class="headerlink" title="四、工程骨架与启动"></a>四、工程骨架与启动</h1><h2 id="1-主类"><a href="#1-主类" class="headerlink" title="1. 主类"></a>1. 主类</h2><p>默认生成带 <strong><code>@SpringBootApplication</code></strong> 的 <strong><code>*Application</code></strong> 类，作为入口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DemoApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(DemoApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>@SpringBootApplication</code></strong> 等价于 <strong><code>@Configuration</code></strong>、<strong><code>@EnableAutoConfiguration</code></strong>、<strong><code>@ComponentScan</code></strong> 的组合，用于触发自动配置与组件扫描。</p>
<h2 id="2-配置：application-properties-或-application-yaml"><a href="#2-配置：application-properties-或-application-yaml" class="headerlink" title="2. 配置：application.properties 或 application.yaml"></a>2. 配置：<code>application.properties</code> 或 <code>application.yaml</code></h2><p>在 <strong><code>src/main/resources/</code></strong> 下编辑 <strong><code>application.properties</code></strong>（或 <strong><code>application.yaml</code></strong>），例如指定端口与应用名：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">demo</span></span><br></pre></td></tr></table></figure>

<p>配置键前缀以当前 <strong>Boot</strong> 版本文档为准；升级大版本时留意<strong>废弃属性</strong>说明。</p>
<h2 id="3-运行方式"><a href="#3-运行方式" class="headerlink" title="3. 运行方式"></a>3. 运行方式</h2><table>
<thead>
<tr>
<th>方式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>IDE</strong></td>
<td>右键 <strong><code>main</code></strong> → <strong>Run</strong></td>
</tr>
<tr>
<td><strong>Maven</strong></td>
<td><strong><code>./mvnw spring-boot:run</code></strong>（项目含 <strong>Wrapper</strong> 时）</td>
</tr>
<tr>
<td><strong>Gradle</strong></td>
<td><strong><code>./gradlew bootRun</code></strong></td>
</tr>
</tbody></table>
<p>启动成功后，若已引入 <strong>spring-boot-starter-web</strong>，默认可访问 <strong><a href="http://127.0.0.1:8080/">http://127.0.0.1:8080/</a></strong>（无映射时可能 <strong>404</strong>，属正常；可加 <strong><code>@RestController</code></strong> 做 <strong><code>/</code></strong> 返回验证）。</p>
<hr>
<h1 id="五、常用能力与延伸"><a href="#五、常用能力与延伸" class="headerlink" title="五、常用能力与延伸"></a>五、常用能力与延伸</h1><h2 id="1-Actuator-与-Profile"><a href="#1-Actuator-与-Profile" class="headerlink" title="1. Actuator 与 Profile"></a>1. Actuator 与 Profile</h2><ul>
<li><strong>Actuator</strong>：依赖 <strong><code>spring-boot-starter-actuator</code></strong>，暴露 <strong><code>/actuator/health</code></strong> 等端点；生产务必限制暴露面并加鉴权。  </li>
<li><strong>Profile</strong>：用 <strong><code>spring.profiles.active</code></strong> 区分 <strong><code>dev</code> &#x2F; <code>prod</code></strong>，配合 <strong><code>application-dev.yaml</code></strong> 等多文件配置。</li>
</ul>
<h2 id="2-与-Spring-Cloud-的关系"><a href="#2-与-Spring-Cloud-的关系" class="headerlink" title="2. 与 Spring Cloud 的关系"></a>2. 与 Spring Cloud 的关系</h2><p><strong>Spring Boot</strong> 解决「单应用如何快速装配与运行」；<strong>Spring Cloud</strong> 在 <strong>Boot</strong> 之上提供注册发现、配置中心、网关等<strong>分布式系统</strong>能力。微服务路线在掌握 <strong>Boot</strong> 后，可按 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a> 引入 <strong>BOM</strong> 与 <strong>Nacos</strong> 等组件。</p>
<h2 id="3-容器化与部署"><a href="#3-容器化与部署" class="headerlink" title="3. 容器化与部署"></a>3. 容器化与部署</h2><p><strong>Boot</strong> 应用通常打包为 <strong><code>jar</code></strong>（<strong><code>java -jar</code></strong> 运行）或制作为容器镜像；与 <strong>Docker</strong>、<strong>Kubernetes</strong> 相关的端口、探针、<strong>JVM</strong> 参数需与 <strong><code>server.port</code></strong>、<strong>Actuator</strong> 路径一并规划，详见 <strong>Spring Boot</strong> 文档中 <strong>Deployment</strong>、<strong>Container Images</strong> 等章节。</p>
<hr>
<h1 id="六、小结"><a href="#六、小结" class="headerlink" title="六、小结"></a>六、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>定位</td>
<td>基于 <strong>Spring</strong> 的快速开发与运行框架</td>
</tr>
<tr>
<td>环境</td>
<td><strong>JDK 17+</strong>；<strong>Maven</strong> 或 <strong>Gradle</strong></td>
</tr>
<tr>
<td>创建</td>
<td><strong>start.spring.io</strong> 或 <strong>IDEA Initializr</strong></td>
</tr>
<tr>
<td>入口</td>
<td><strong><code>@SpringBootApplication</code></strong> + <strong><code>SpringApplication.run</code></strong></td>
</tr>
<tr>
<td>配置</td>
<td><strong><code>src/main/resources/application*.yaml</code></strong></td>
</tr>
<tr>
<td>延伸</td>
<td><strong>Cloud &#x2F; SCA</strong> 见站内 <strong>SpringCloudAlibaba</strong>；<strong>Gradle</strong> 见 <strong><code>source/_posts/Java/gradle.md</code></strong></td>
</tr>
</tbody></table>
<p>按 <strong>「二 → 三 → 四」</strong> 顺序即可在本地得到可运行的最小 <strong>Web</strong> 应用；再按需叠加数据访问、安全、观测与 <strong>Spring Cloud</strong> 组件。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>SpringBoot</tag>
        <tag>Gradle</tag>
        <tag>Maven</tag>
        <tag>Spring</tag>
        <tag>IntelliJ IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>Apache Dubbo 简介与 Spring Cloud 对齐要点</title>
    <url>/2026/05/01/SpringCloud/Dubbo/</url>
    <content><![CDATA[<p><strong>Apache Dubbo</strong> 是面向 <strong>Java</strong> 的高性能 <strong>RPC</strong> 与服务治理框架，提供注册发现、负载均衡、路由、超时与容错等能力；与基于 <strong>HTTP</strong> 的 <strong>OpenFeign</strong> 等可并存，按接口粒度做进程间调用。下文按「文档入口 → 核心概念 → 与 <strong>Spring Cloud Alibaba（SCA）</strong> 同栈 → 依赖与配置思路」组织，便于和站内 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>、<a href="/2026/05/01/Nacos/">Nacos 版本选型与容器化部署</a> 对照阅读。</p>
<hr>
<h1 id="一、官网与文档"><a href="#一、官网与文档" class="headerlink" title="一、官网与文档"></a>一、官网与文档</h1><ul>
<li><strong>Apache Dubbo 官网</strong>：<a href="https://dubbo.apache.org/zh-cn/">https://dubbo.apache.org/zh-cn/</a>  </li>
<li><strong>GitHub</strong>：<a href="https://github.com/apache/dubbo">https://github.com/apache/dubbo</a>  </li>
<li><strong>快速开始（Java &#x2F; Spring Boot）</strong>：<a href="https://dubbo.apache.org/zh-cn/overview/mannual/java-sdk/quick-start/starter/">https://dubbo.apache.org/zh-cn/overview/mannual/java-sdk/quick-start/starter/</a>  </li>
<li><strong>与 Spring Boot 集成说明</strong>：以官网 <strong>「手册 → Java SDK → 配置 → Spring Boot」</strong> 当前章节为准（属性前缀、自动配置类名随 <strong>3.x</strong> 小版本可能调整）。  </li>
<li><strong>SCA 与周边组件</strong>：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a></li>
</ul>
<hr>
<h1 id="二、Apache-Dubbo-是什么"><a href="#二、Apache-Dubbo-是什么" class="headerlink" title="二、Apache Dubbo 是什么"></a>二、Apache Dubbo 是什么</h1><h2 id="1-在微服务里的位置"><a href="#1-在微服务里的位置" class="headerlink" title="1. 在微服务里的位置"></a>1. 在微服务里的位置</h2><p><strong>Dubbo</strong> 解决的是「<strong>带契约的进程间调用</strong>」：<strong>Provider</strong> 暴露接口，<strong>Consumer</strong> 通过注册中心发现地址，运行时走 <strong>Dubbo</strong> 自有协议或 <strong>Triple</strong>（基于 <strong>gRPC</strong>）等，而不是每个服务都手写 <strong>URL</strong> 与重试逻辑。与 <strong>Spring Cloud</strong> 中的 <strong>REST + 负载均衡</strong> 相比，<strong>RPC</strong> 通常二进制序列化、元数据更紧凑，适合高频、低延迟的内部调用。</p>
<h2 id="2-核心角色与一次调用"><a href="#2-核心角色与一次调用" class="headerlink" title="2. 核心角色与一次调用"></a>2. 核心角色与一次调用</h2><table>
<thead>
<tr>
<th>角色</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Provider</strong></td>
<td>暴露服务实现，向注册中心注册自己</td>
</tr>
<tr>
<td><strong>Consumer</strong></td>
<td>引用接口代理，从注册中心拉取 <strong>Provider</strong> 列表并调用</td>
</tr>
<tr>
<td><strong>Registry</strong></td>
<td>常见为 <strong>Nacos</strong>、<strong>ZooKeeper</strong> 等；地址与元数据由框架维护</td>
</tr>
<tr>
<td><strong>Protocol &#x2F; Port</strong></td>
<td><strong>Dubbo 3</strong> 常用 <strong>Triple</strong>（<strong><code>tri</code></strong>）或经典 <strong>dubbo</strong> 协议；端口可固定或由 <strong><code>port: -1</code></strong> 自增分配</td>
</tr>
</tbody></table>
<h2 id="3-与「Spring-Cloud-全家桶」的关系"><a href="#3-与「Spring-Cloud-全家桶」的关系" class="headerlink" title="3. 与「Spring Cloud 全家桶」的关系"></a>3. 与「Spring Cloud 全家桶」的关系</h2><p><strong>Dubbo</strong> 不是 <strong>Spring Cloud</strong> 子项目，但可通过 <strong>Spring Boot</strong> 自动配置与 <strong>Nacos</strong> 等注册中心无缝衔接；业务代码仍可用 <strong>Spring</strong> 管理 <strong>Bean</strong>、事务与 <strong>AOP</strong>。与 <strong>Sentinel</strong>、<strong>Seata</strong> 等叠加时，按各组件官方文档分别引入依赖与规则即可。</p>
<hr>
<h1 id="三、与-Spring-Cloud-Alibaba-同栈"><a href="#三、与-Spring-Cloud-Alibaba-同栈" class="headerlink" title="三、与 Spring Cloud Alibaba 同栈"></a>三、与 Spring Cloud Alibaba 同栈</h1><h2 id="1-注册中心与版本意识"><a href="#1-注册中心与版本意识" class="headerlink" title="1. 注册中心与版本意识"></a>1. 注册中心与版本意识</h2><p>生产上常把 <strong>Nacos</strong> 既作 <strong>Spring Cloud</strong> 注册发现，又作 <strong>Dubbo</strong> 的 <strong><code>registry</code></strong>：<strong>Nacos Server</strong> 大版本须与客户端、<strong>SCA</strong> BOM 对齐，避免混用大版本。部署与端口说明见 <a href="/2026/05/01/Nacos/">Nacos 版本选型与容器化部署</a>；<strong>Gradle</strong> 父工程 <strong>BOM</strong> 与 <strong>Boot &#x2F; Cloud</strong> 线见 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>。</p>
<p><strong>Dubbo</strong> 自身发行节奏独立于 <strong>SCA</strong>：引入 <strong><code>dubbo-bom</code></strong>（或 <strong>Dubbo</strong> 官方文档推荐的 <strong>BOM</strong> 写法）管理 <strong><code>org.apache.dubbo:*</code></strong> 版本，再与 <strong>SCA</strong> 管理的 <strong>Nacos Client</strong> 等一起做一次 <strong><code>./gradlew dependencies</code></strong> 核对，确认无冲突传递依赖。</p>
<h2 id="2-依赖入口（Gradle-Kotlin-DSL）"><a href="#2-依赖入口（Gradle-Kotlin-DSL）" class="headerlink" title="2. 依赖入口（Gradle Kotlin DSL）"></a>2. 依赖入口（Gradle Kotlin DSL）</h2><p>在已通过 <strong><code>spring-cloud-alibaba-dependencies</code></strong> 等方式对齐 <strong>Cloud &#x2F; Nacos</strong> 的前提下，为应用增加 <strong>Dubbo Spring Boot</strong> 起步依赖（<strong><code>dubbo-bom</code></strong> 小版本升级时请对照 <a href="https://central.sonatype.com/artifact/org.apache.dubbo/dubbo-bom">Maven Central dubbo-bom</a> 与 <a href="https://github.com/apache/dubbo/releases">Dubbo Releases</a>）：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">dependencyManagement &#123;</span><br><span class="line">    imports &#123;</span><br><span class="line">        <span class="comment">// 与 Spring Cloud / SCA BOM 并列；以下为 Central 上 3.3.x 当前 release 线示例</span></span><br><span class="line">        mavenBom(<span class="string">&quot;org.apache.dubbo:dubbo-bom:3.3.6&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;org.apache.dubbo:dubbo-spring-boot-starter&quot;</span>)</span><br><span class="line">    <span class="comment">// 若已使用 SCA 的 Nacos 注册发现，Dubbo 侧 registry 仍指向同一 Nacos 即可</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>若 <strong>Central</strong> 上 <strong><code>dubbo-bom</code></strong> 已高于 <strong><code>3.3.6</code></strong>，请替换版本号，并与官网 <strong>「快速开始」</strong> 中的示例保持同一发行线。</p>
<hr>
<h1 id="四、配置思路（application-yaml）"><a href="#四、配置思路（application-yaml）" class="headerlink" title="四、配置思路（application.yaml）"></a>四、配置思路（<code>application.yaml</code>）</h1><p>以下为<strong>示意</strong>（<strong><code>registry</code> 地址、应用名、协议端口</strong>按环境修改）；属性名以你锁定的 <strong>Dubbo</strong> 版本文档为准。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">dubbo:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">demo-dubbo-app</span></span><br><span class="line">  <span class="attr">registry:</span></span><br><span class="line">    <span class="attr">address:</span> <span class="string">nacos://127.0.0.1:8848</span></span><br><span class="line">  <span class="attr">protocol:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">tri</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">-1</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>nacos://</code></strong>：与 <strong>Nacos</strong> 控制台、<strong><code>8848</code></strong> 及鉴权配置一致；集群或 <strong>namespace</strong> 等见 <strong>Dubbo</strong> 手册中 <strong>Nacos</strong> 注册中心专节。  </li>
<li><strong><code>tri</code></strong>：<strong>Dubbo 3</strong> 推荐的 <strong>Triple</strong> 协议；与 <strong>gRPC</strong> 生态互通场景请对照官网「<strong>Triple</strong>」说明。  </li>
<li><strong><code>@DubboService</code> &#x2F; <code>@DubboReference</code></strong>：在 <strong>Spring Boot</strong> 下暴露与消费服务的常用注解，示例代码见官网快速开始。</li>
</ul>
<hr>
<h1 id="五、小结"><a href="#五、小结" class="headerlink" title="五、小结"></a>五、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>定位</td>
<td><strong>Java RPC</strong> 与治理框架，常与 <strong>Nacos</strong> 同栈</td>
</tr>
<tr>
<td>版本</td>
<td><strong>Dubbo</strong> 用 <strong><code>dubbo-bom</code></strong> 管理；<strong>Nacos</strong> 与 <strong>SCA</strong> 对齐见站内 <strong>Nacos</strong>、<strong>SpringCloudAlibaba</strong> 文</td>
</tr>
<tr>
<td>依赖</td>
<td><strong><code>dubbo-spring-boot-starter</code></strong> + <strong>BOM</strong>；与 <strong>SCA</strong> 依赖并存时做一次依赖树检查</td>
</tr>
<tr>
<td>配置</td>
<td><strong><code>registry.address</code></strong> 指向 <strong>Nacos</strong>；协议常用 <strong><code>tri</code></strong></td>
</tr>
<tr>
<td>文档</td>
<td><a href="https://dubbo.apache.org/zh-cn/">https://dubbo.apache.org/zh-cn/</a> · <a href="https://github.com/apache/dubbo">https://github.com/apache/dubbo</a></td>
</tr>
</tbody></table>
<p>更细的集群、<strong>Mesh</strong>、<strong>可观测性</strong>与性能调优以 <strong>Apache Dubbo</strong> 当前大版本手册为准；若需单独成文的 <strong>Provider &#x2F; Consumer</strong> 多模块示例，可在现有 <strong>SCA</strong> 工程上按官网「快速开始」增量添加 <strong>Dubbo</strong> 模块后再整理进站。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>RPC</tag>
        <tag>SpringCloud</tag>
        <tag>Dubbo</tag>
        <tag>Microservices</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>Nacos</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Alibaba 接入 RocketMQ（SCA 2025.1.x 与 Spring Cloud Stream）</title>
    <url>/2026/05/01/SpringCloud/RocketMQ/</url>
    <content><![CDATA[<p><strong>RocketMQ</strong> 在 <strong>Spring Cloud Alibaba（SCA）</strong> 中通过 <strong><code>spring-cloud-starter-stream-rocketmq</code></strong> 绑定 <strong>Spring Cloud Stream</strong>，与 <strong>Spring Boot 4.x &#x2F; Spring Cloud 2025.1.x</strong> 同一套 BOM 管理客户端版本。BOM 选型、<strong>Gradle</strong> 工程骨架见 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>；注册发现可与 <a href="/2026/05/01/Nacos/">Nacos 版本选型与容器化部署</a> 对齐。</p>
<hr>
<h2 id="与-Spring-Cloud-Alibaba-的版本关系"><a href="#与-Spring-Cloud-Alibaba-的版本关系" class="headerlink" title="与 Spring Cloud Alibaba 的版本关系"></a>与 Spring Cloud Alibaba 的版本关系</h2><table>
<thead>
<tr>
<th>项</th>
<th>建议</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SCA BOM</strong></td>
<td><strong>2025.1.0.0</strong>（及后续 <strong>2025.1.0.x</strong>）</td>
<td>以 <a href="https://github.com/alibaba/spring-cloud-alibaba/releases">GitHub Releases</a> 为准</td>
</tr>
<tr>
<td><strong>RocketMQ Client（BOM 管理）</strong></td>
<td><strong>5.3.1</strong></td>
<td>见 <strong><code>spring-cloud-alibaba-dependencies</code></strong> 中 <strong><code>rocketmq.version</code></strong></td>
</tr>
<tr>
<td><strong>Spring Cloud Stream</strong></td>
<td>随 <strong>Spring Cloud 2025.1.x</strong> 管理</td>
<td><strong>SCA 2025.1.0.0</strong> 起 RocketMQ 模块已适配 <strong>Spring Boot 4.0</strong></td>
</tr>
<tr>
<td><strong>NameServer &#x2F; Broker</strong></td>
<td>与客户端 <strong>5.3.x</strong> 同主版本线</td>
<td>本地验证可用下文 <strong>Docker Compose</strong>；生产以官方运维手册为准</td>
</tr>
</tbody></table>
<p>应用侧<strong>不要</strong>手写 <strong>RocketMQ</strong> 客户端补丁号，交给 <strong><code>com.alibaba.cloud:spring-cloud-alibaba-dependencies</code></strong>。</p>
<hr>
<h2 id="官方文档与示例"><a href="#官方文档与示例" class="headerlink" title="官方文档与示例"></a>官方文档与示例</h2><ul>
<li><strong>SCA 2025.x RocketMQ 快速开始</strong>（函数式 <strong><code>StreamBridge</code> &#x2F; <code>Consumer</code></strong> 模型）：<a href="https://sca.aliyun.com/docs/2025.x/user-guide/rocketmq/quick-start/">https://sca.aliyun.com/docs/2025.x/user-guide/rocketmq/quick-start/</a>  </li>
<li><strong>RocketMQ</strong> 官网：<a href="https://rocketmq.apache.org/">https://rocketmq.apache.org/</a>  </li>
<li><strong>示例仓库</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/tree/2025.1.x/spring-cloud-alibaba-examples/rocketmq-example">https://github.com/alibaba/spring-cloud-alibaba/tree/2025.1.x/spring-cloud-alibaba-examples/rocketmq-example</a></li>
</ul>
<p><strong>说明</strong>：旧版示例中的 <strong><code>@EnableBinding</code> &#x2F; <code>@StreamListener</code></strong> 已属历史写法；<strong>2025.x</strong> 文档以 <strong><code>spring.cloud.stream.function.definition</code></strong>、<strong><code>StreamBridge</code></strong> 与 <strong><code>Consumer&lt;Message&lt;…&gt;&gt;</code></strong> 为准。</p>
<hr>
<h2 id="依赖（Gradle-Kotlin-DSL）"><a href="#依赖（Gradle-Kotlin-DSL）" class="headerlink" title="依赖（Gradle Kotlin DSL）"></a>依赖（Gradle Kotlin DSL）</h2><p>在已引入 <strong>SCA BOM</strong> 的 <strong><code>build.gradle.kts</code></strong> 中增加（版本由 BOM 管理，不写号）：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;com.alibaba.cloud:spring-cloud-starter-stream-rocketmq&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>若仅需同步 <strong>JSON</strong> 消息体，可再配合 <strong><code>spring-boot-starter-json</code></strong>（Boot Web 场景通常已间接引入）。</p>
<hr>
<h2 id="本地启动-NameServer-与-Broker（Docker-Compose）"><a href="#本地启动-NameServer-与-Broker（Docker-Compose）" class="headerlink" title="本地启动 NameServer 与 Broker（Docker Compose）"></a>本地启动 NameServer 与 Broker（Docker Compose）</h2><p>下面为最小 <strong>Compose</strong>（开发用）：<strong>NameServer 9876</strong>，<strong>Broker</strong> 依赖同一文件内的 <strong><code>namesrv</code></strong>。应用在宿主机上运行时，<strong>NameServer 地址</strong>填 <strong><code>127.0.0.1:9876</code></strong>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">rocketmq-531-dev</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">namesrv:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.1</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">rmq-namesrv</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9876:9876&quot;</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">sh</span> <span class="string">mqnamesrv</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">broker:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">apache/rocketmq:5.3.1</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">rmq-broker</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">namesrv</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10909:10909&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10911:10911&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;10912:10912&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">NAMESRV_ADDR:</span> <span class="string">&quot;namesrv:9876&quot;</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">sh</span> <span class="string">mqbroker</span> <span class="string">-n</span> <span class="string">namesrv:9876</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose up -d</span><br></pre></td></tr></table></figure>

<p>创建业务 <strong>Topic</strong>（在含 <strong><code>mqadmin</code></strong> 的镜像或发行包内执行，<strong><code>-n</code></strong> 指向可达的 <strong>NameServer</strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -it rmq-broker sh mqadmin updateTopic -n namesrv:9876 -t demo-topic -c DefaultCluster</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="生产者：StreamBridge-application-yaml"><a href="#生产者：StreamBridge-application-yaml" class="headerlink" title="生产者：StreamBridge + application.yaml"></a>生产者：<code>StreamBridge</code> + <code>application.yaml</code></h2><p><strong><code>application.yaml</code></strong> 示例（端口与 <strong>binding</strong> 名可按项目调整）：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">28085</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">rocketmq-producer-demo</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">stream:</span></span><br><span class="line">      <span class="attr">rocketmq:</span></span><br><span class="line">        <span class="attr">binder:</span></span><br><span class="line">          <span class="attr">name-server:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:9876</span></span><br><span class="line">        <span class="attr">bindings:</span></span><br><span class="line">          <span class="attr">demo-out-0:</span></span><br><span class="line">            <span class="attr">producer:</span></span><br><span class="line">              <span class="attr">group:</span> <span class="string">demo-producer-group</span></span><br><span class="line">      <span class="attr">bindings:</span></span><br><span class="line">        <span class="attr">demo-out-0:</span></span><br><span class="line">          <span class="attr">destination:</span> <span class="string">demo-topic</span></span><br></pre></td></tr></table></figure>

<p>发送侧在启动类或 <strong><code>@Configuration</code></strong> 中注入 <strong><code>StreamBridge</code></strong>，向 <strong><code>demo-out-0</code></strong> 发送 <strong><code>Message</code></strong> 或 <strong><code>GenericMessage</code></strong> 即可；完整代码与广播、顺序、延时等场景见官网 <strong>快速开始</strong> 与 <strong><code>rocketmq-example</code></strong>。</p>
<hr>
<h2 id="消费者：函数式-Consumer"><a href="#消费者：函数式-Consumer" class="headerlink" title="消费者：函数式 Consumer"></a>消费者：函数式 <code>Consumer</code></h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">28086</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">rocketmq-consumer-demo</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">stream:</span></span><br><span class="line">      <span class="attr">function:</span></span><br><span class="line">        <span class="attr">definition:</span> <span class="string">demoConsumer;</span></span><br><span class="line">      <span class="attr">rocketmq:</span></span><br><span class="line">        <span class="attr">binder:</span></span><br><span class="line">          <span class="attr">name-server:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:9876</span></span><br><span class="line">      <span class="attr">bindings:</span></span><br><span class="line">        <span class="attr">demoConsumer-in-0:</span></span><br><span class="line">          <span class="attr">destination:</span> <span class="string">demo-topic</span></span><br><span class="line">          <span class="attr">group:</span> <span class="string">demo-consumer-group</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> Consumer&lt;Message&lt;String&gt;&gt; <span class="title function_">demoConsumer</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msg -&gt; &#123;</span><br><span class="line">        <span class="comment">// 处理 msg.getPayload()</span></span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>广播消费时，在 <strong><code>spring.cloud.stream.rocketmq.bindings.&lt;bindingName&gt;.consumer</code></strong> 下设置 <strong><code>messageModel: BROADCASTING</code></strong>（详见官方 <strong>快速开始</strong> 小节）。</p>
<hr>
<h2 id="与-Nacos、多环境"><a href="#与-Nacos、多环境" class="headerlink" title="与 Nacos、多环境"></a>与 Nacos、多环境</h2><ul>
<li><strong>Topic &#x2F; 消费组</strong>：在 <strong>Broker</strong> 与运维规范中统一命名；<strong><code>destination</code></strong> 与 <strong><code>group</code></strong> 与 <strong>RocketMQ</strong> 控制台中的资源一致。  </li>
<li><strong>NameServer 地址</strong>：开发用 <strong><code>127.0.0.1:9876</code></strong>；<strong>Kubernetes</strong> 内用 <strong>Service DNS</strong>（如 <strong><code>rmq-namesrv.default.svc.cluster.local:9876</code></strong>）。  </li>
<li>若全栈使用 <strong>Nacos</strong>，<strong>SCA</strong> 仍按各模块 <strong><code>application.yaml</code></strong> 配置 <strong>RocketMQ</strong>；注册发现与消息中间件职责分离，勿混为一谈。</li>
</ul>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul>
<li><strong>SCA 版本说明</strong>：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a>  </li>
<li><strong>SCA 2025.1.0.0 Release</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0">https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0</a>  </li>
<li><strong>Docker Hub <code>apache/rocketmq</code></strong>：<a href="https://hub.docker.com/r/apache/rocketmq/tags">https://hub.docker.com/r/apache/rocketmq/tags</a></li>
</ul>
<p>固定 <strong>SCA BOM 2025.1.0.0</strong> 与 <strong>RocketMQ 5.3.1</strong> 客户端后，按上文 <strong>Compose</strong> 起 <strong>Broker</strong>，用 <strong>Stream</strong> 函数式配置即可在 <strong>Boot 4</strong> 线上验证发收消息；进阶事务、过滤、轨迹等见 <strong>RocketMQ</strong> 官方文档与 <strong>SCA</strong> 进阶章节。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>RocketMQ</tag>
        <tag>SpringCloud</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>SpringCloudStream</tag>
      </tags>
  </entry>
  <entry>
    <title>Nacos 版本选型与容器化部署（对齐 Spring Cloud Alibaba）</title>
    <url>/2026/05/01/SpringCloud/Nacos/</url>
    <content><![CDATA[<p><strong>Nacos</strong> 作注册中心与配置中心时，<strong>Server 大版本应与客户端同世代</strong>，并与 <strong>Spring Cloud Alibaba（SCA）</strong> BOM 里管理的 <strong>nacos-client</strong> 一致。站内 Gradle 搭建与 SCA BOM 选型见 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>。</p>
<hr>
<h2 id="与-Spring-Cloud-Alibaba-的版本关系"><a href="#与-Spring-Cloud-Alibaba-的版本关系" class="headerlink" title="与 Spring Cloud Alibaba 的版本关系"></a>与 Spring Cloud Alibaba 的版本关系</h2><h3 id="SCA-2025-1-x-主线"><a href="#SCA-2025-1-x-主线" class="headerlink" title="SCA 2025.1.x 主线"></a>SCA 2025.1.x 主线</h3><table>
<thead>
<tr>
<th>组件</th>
<th>建议版本</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>Spring Cloud Alibaba BOM</td>
<td><strong>2025.1.0.0</strong>（及后续 <strong>2025.1.0.x</strong> 补丁）</td>
<td>以 <a href="https://github.com/alibaba/spring-cloud-alibaba/releases">GitHub Releases</a> 为准</td>
</tr>
<tr>
<td>nacos-client（由 BOM 管理）</td>
<td><strong>3.1.1</strong></td>
<td><strong>2025.1.0.0</strong> 发行说明明确从 3.0.3 升级至 <strong>3.1.1</strong></td>
</tr>
<tr>
<td><strong>Nacos Server</strong></td>
<td><strong>3.1.x</strong>，本文镜像钉 <strong>v3.1.1</strong></td>
<td>与 <strong>nacos-client 3.1.x</strong> 同世代；镜像见 <a href="https://hub.docker.com/r/nacos/nacos-server/tags">Docker Hub nacos&#x2F;nacos-server</a></td>
</tr>
</tbody></table>
<p>应用侧 <code>spring.cloud.nacos.discovery.server-addr</code> 仍指向 <strong>8848</strong> 所在地址即可；客户端由 SCA 与 <strong>Nacos</strong> 协议栈处理 <strong>gRPC</strong> 等端口，一般无需在业务配置里手写 <strong>9848&#x2F;9849</strong>。</p>
<hr>
<h2 id="Nacos-3-x-端口与控制台"><a href="#Nacos-3-x-端口与控制台" class="headerlink" title="Nacos 3.x 端口与控制台"></a>Nacos 3.x 端口与控制台</h2><p><strong>Nacos 3.x</strong> 默认将<strong>控制台</strong>与核心进程分离：控制台常见为 <strong>8080</strong>（路径 <strong><code>/index.html</code></strong>），<strong>8848</strong> 仍为服务端主端口；容器示例还会映射 <strong>9848</strong>（及集群场景下相关端口）。与 <strong>Nacos 2.x</strong> 单端口习惯不同，部署防火墙与 <strong>Service</strong> 端口时要同时放行控制台与客户端所需端口。官方说明见 <a href="https://github.com/nacos-group/nacos-docker">nacos-group&#x2F;nacos-docker</a> README。</p>
<p>自 <strong>Nacos 2.2.1</strong> 起镜像不再内置默认 <strong><code>NACOS_AUTH_*</code></strong>，<strong>必须</strong>自行传入，否则启动报错；<strong>Nacos 3.x</strong> 快速启动示例同样要求 <strong><code>NACOS_AUTH_TOKEN</code></strong>、<strong><code>NACOS_AUTH_IDENTITY_KEY</code></strong>、<strong><code>NACOS_AUTH_IDENTITY_VALUE</code></strong>。以下为占位符，<strong>生产环境请换成高强度随机值并纳入密钥管理</strong>。</p>
<hr>
<h2 id="Docker-单容器"><a href="#Docker-单容器" class="headerlink" title="Docker 单容器"></a>Docker 单容器</h2><p><strong>Standalone + 内嵌存储（开发&#x2F;本地验证）</strong>，镜像与 SCA <strong>2025.1.0.0</strong> 对齐为 <strong>v3.1.1</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 请先导出为真实密钥，勿将示例值用于生产</span></span><br><span class="line"><span class="built_in">export</span> NACOS_AUTH_TOKEN=<span class="string">&#x27;请替换为长度足够的随机串&#x27;</span></span><br><span class="line"><span class="built_in">export</span> NACOS_AUTH_IDENTITY_KEY=<span class="string">&#x27;serverIdentityKey&#x27;</span></span><br><span class="line"><span class="built_in">export</span> NACOS_AUTH_IDENTITY_VALUE=<span class="string">&#x27;serverIdentityValue&#x27;</span></span><br><span class="line"></span><br><span class="line">docker run -d --name nacos \</span><br><span class="line">  -e MODE=standalone \</span><br><span class="line">  -e NACOS_AUTH_TOKEN \</span><br><span class="line">  -e NACOS_AUTH_IDENTITY_KEY \</span><br><span class="line">  -e NACOS_AUTH_IDENTITY_VALUE \</span><br><span class="line">  -p 8080:8080 \</span><br><span class="line">  -p 8848:8848 \</span><br><span class="line">  -p 9848:9848 \</span><br><span class="line">  nacos/nacos-server:v3.1.1</span><br></pre></td></tr></table></figure>

<p><strong>ARM64</strong>（如 Apple Silicon）可选用 <strong><code>nacos/nacos-server:v3.1.1-slim</code></strong>（官方 README 说明 <strong>-slim</strong> 支持 <strong>arm</strong>）。</p>
<p>控制台：<strong><a href="http://127.0.0.1:8080/index.html">http://127.0.0.1:8080/index.html</a></strong> ，默认用户 <strong><code>nacos</code> &#x2F; <code>nacos</code></strong>（首次登录后应修改密码）。业务注册地址示例：<strong><code>127.0.0.1:8848</code></strong>。</p>
<p>可选 JVM 与数据源等环境变量见官方 <a href="https://github.com/nacos-group/nacos-docker#common-property-configuration">Common property configuration</a>（如 <strong><code>JVM_XMS</code></strong> &#x2F; <strong><code>JVM_XMX</code></strong>）。<strong>外置 MySQL</strong> 的建库、<strong>schema</strong> 与 <strong><code>MYSQL_*</code></strong> 环境变量见下文 <strong><code>## 外置 MySQL（容器连接库表）</code></strong>。</p>
<hr>
<h2 id="Docker-Compose"><a href="#Docker-Compose" class="headerlink" title="Docker Compose"></a>Docker Compose</h2><p>下面为完整 <strong><code>docker-compose.yml</code></strong>（<strong>Compose file format 3.8</strong>），与上文 <strong>Docker</strong> 单容器参数等价；<strong><code>env_file</code></strong> 可再拆出 <strong><code>.env</code></strong>，此处为单文件自包含示例（<strong><code>NACOS_AUTH_*</code> 仍为占位，部署前请替换</strong>）。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">nacos-standalone-derby</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">nacos:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">nacos/nacos-server:v3.1.1</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">nacos-standalone-derby</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MODE:</span> <span class="string">standalone</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_TOKEN:</span> <span class="string">&quot;请替换为长度足够的随机串&quot;</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_IDENTITY_KEY:</span> <span class="string">&quot;serverIdentityKey&quot;</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_IDENTITY_VALUE:</span> <span class="string">&quot;serverIdentityValue&quot;</span></span><br><span class="line">      <span class="comment"># JVM_XMS: &quot;512m&quot;</span></span><br><span class="line">      <span class="comment"># JVM_XMX: &quot;512m&quot;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:8080&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8848:8848&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9848:9848&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos_data:/home/nacos/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos_logs:/home/nacos/logs</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">nacos_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">nacos_logs:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line"></span><br><span class="line"><span class="attr">networks:</span></span><br><span class="line">  <span class="attr">default:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">nacos-standalone-derby-net</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">bridge</span></span><br></pre></td></tr></table></figure>

<p>若希望密钥不进 <strong>Compose</strong> 文件，可改回 <strong><code>environment: ${VAR}</code></strong> 并同目录提供 <strong><code>.env</code></strong>（加入 <strong><code>.gitignore</code></strong>）。</p>
<p>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.yml up -d</span><br></pre></td></tr></table></figure>

<p><strong>集群</strong>或控制台与节点拆分等，可直接复用官方 <strong><code>example/</code></strong> 下 <strong><code>cluster-hostname.yaml</code></strong>、<strong><code>standalone-independent-mysql.yaml</code></strong>（仅 <strong>Nacos 3.x</strong>）等，并把镜像版本改为 <strong><code>v3.1.1</code></strong>；与 <strong>MySQL</strong> 同库的 <strong>Compose</strong> 见下文。</p>
<hr>
<h2 id="外置-MySQL（容器连接库表）"><a href="#外置-MySQL（容器连接库表）" class="headerlink" title="外置 MySQL（容器连接库表）"></a>外置 MySQL（容器连接库表）</h2><p><strong>Nacos</strong> 官方镜像通过环境变量切换存储：<strong>不设置</strong> <strong><code>SPRING_DATASOURCE_PLATFORM</code></strong>（或为空）时使用内嵌 <strong>Derby</strong>；设为 <strong><code>mysql</code></strong> 时使用外置 <strong>MySQL</strong>。变量名与 <strong><code>nacos-docker</code></strong> 中 <strong><code>env/nacos-standalone-mysql.env</code></strong> 一致。</p>
<h3 id="1-前置：建库与表结构"><a href="#1-前置：建库与表结构" class="headerlink" title="1. 前置：建库与表结构"></a>1. 前置：建库与表结构</h3><ol>
<li>在 <strong>MySQL 8.x</strong> 中创建库（示例库名 <strong><code>nacos_devtest</code></strong>），字符集 <strong><code>utf8mb4</code></strong> &#x2F; 排序规则 <strong><code>utf8mb4_unicode_ci</code></strong>（与官方 <strong>Compose</strong> 一致即可）。  </li>
<li>为 <strong>Nacos</strong> 单独业务账号并授权该库（<strong>最小权限</strong>）。  </li>
<li><strong>首次部署前</strong>在目标库执行与 <strong>Server 版本匹配</strong> 的 <strong><code>mysql-schema.sql</code></strong>（官方仓库路径随版本调整，当前可在 <strong>Nacos</strong> 仓库中查找 <strong><code>mysql-schema.sql</code></strong>，例如 <a href="https://github.com/alibaba/nacos/tree/develop/plugin-default-impl/nacos-default-datasource-plugin/nacos-datasource-plugin-mysql/src/main/resources/META-INF">nacos 仓库内 mysql 插件资源目录</a> 下的脚本；升级 <strong>Server</strong> 大版本时务必对照发行说明是否需迁移脚本）。  </li>
<li>容器网络： <strong><code>MYSQL_SERVICE_HOST</code></strong> 必须填 <strong>Nacos 容器能解析到的地址</strong>——同 <strong>Compose</strong> 栈内用服务名（如 <strong><code>mysql</code></strong>）；宿主机上的库在 <strong>Linux</strong> 上常用宿主机网关 <strong>IP</strong> 或 <strong><code>host.docker.internal</code></strong>（视 <strong>Docker</strong> 版本而定），<strong>不要用</strong> <strong><code>127.0.0.1</code></strong> 指宿主机上的 <strong>MySQL</strong>。</li>
</ol>
<h3 id="2-常用环境变量（官方镜像）"><a href="#2-常用环境变量（官方镜像）" class="headerlink" title="2. 常用环境变量（官方镜像）"></a>2. 常用环境变量（官方镜像）</h3><table>
<thead>
<tr>
<th>变量</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>SPRING_DATASOURCE_PLATFORM</code></strong></td>
<td>固定为 <strong><code>mysql</code></strong> 启用外置库</td>
<td><strong><code>mysql</code></strong></td>
</tr>
<tr>
<td><strong><code>MYSQL_SERVICE_HOST</code></strong></td>
<td><strong>MySQL</strong> 主机名或 <strong>IP</strong></td>
<td><strong><code>mysql</code></strong>（同网络服务名）</td>
</tr>
<tr>
<td><strong><code>MYSQL_SERVICE_PORT</code></strong></td>
<td>端口</td>
<td><strong><code>3306</code></strong></td>
</tr>
<tr>
<td><strong><code>MYSQL_SERVICE_DB_NAME</code></strong></td>
<td>库名</td>
<td><strong><code>nacos_devtest</code></strong></td>
</tr>
<tr>
<td><strong><code>MYSQL_SERVICE_USER</code></strong> &#x2F; <strong><code>MYSQL_SERVICE_PASSWORD</code></strong></td>
<td>账号密码</td>
<td>由你方创建</td>
</tr>
<tr>
<td><strong><code>MYSQL_SERVICE_DB_PARAM</code></strong></td>
<td><strong>JDBC</strong> 查询串（时区、<strong>SSL</strong> 等）</td>
<td>见下方示例</td>
</tr>
<tr>
<td><strong><code>MYSQL_DATABASE_NUM</code></strong></td>
<td>多数据源数量</td>
<td>一般为 <strong><code>1</code></strong>，默认可不写</td>
</tr>
</tbody></table>
<p><strong><code>MYSQL_SERVICE_DB_PARAM</code></strong> 可与官方示例一致（按需改时区、<strong>SSL</strong>）：</p>
<p><code>characterEncoding=utf8&amp;connectTimeout=1000&amp;socketTimeout=3000&amp;autoReconnect=true&amp;useUnicode=true&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true</code></p>
<p>仍须保留 <strong><code>NACOS_AUTH_*</code></strong> 等与鉴权相关的变量（见上文 <strong>Standalone</strong> 示例）。</p>
<h3 id="3-Docker-单容器（指向已有-MySQL）"><a href="#3-Docker-单容器（指向已有-MySQL）" class="headerlink" title="3. Docker 单容器（指向已有 MySQL）"></a>3. Docker 单容器（指向已有 MySQL）</h3><p>在 <strong><code>SPRING_DATASOURCE_PLATFORM=mysql</code></strong> 及 <strong><code>MYSQL_*</code></strong> 齐全的前提下启动（<strong><code>MYSQL_SERVICE_HOST</code></strong> 改为可达地址）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> NACOS_AUTH_TOKEN=<span class="string">&#x27;…&#x27;</span></span><br><span class="line"><span class="built_in">export</span> NACOS_AUTH_IDENTITY_KEY=<span class="string">&#x27;…&#x27;</span></span><br><span class="line"><span class="built_in">export</span> NACOS_AUTH_IDENTITY_VALUE=<span class="string">&#x27;…&#x27;</span></span><br><span class="line"></span><br><span class="line">docker run -d --name nacos \</span><br><span class="line">  -e MODE=standalone \</span><br><span class="line">  -e SPRING_DATASOURCE_PLATFORM=mysql \</span><br><span class="line">  -e MYSQL_SERVICE_HOST=你的MySQL主机 \</span><br><span class="line">  -e MYSQL_SERVICE_PORT=3306 \</span><br><span class="line">  -e MYSQL_SERVICE_DB_NAME=nacos_devtest \</span><br><span class="line">  -e MYSQL_SERVICE_USER=nacos \</span><br><span class="line">  -e MYSQL_SERVICE_PASSWORD=<span class="string">&#x27;你的密码&#x27;</span> \</span><br><span class="line">  -e MYSQL_SERVICE_DB_PARAM=<span class="string">&#x27;characterEncoding=utf8&amp;connectTimeout=1000&amp;socketTimeout=3000&amp;autoReconnect=true&amp;useUnicode=true&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true&#x27;</span> \</span><br><span class="line">  -e NACOS_AUTH_TOKEN -e NACOS_AUTH_IDENTITY_KEY -e NACOS_AUTH_IDENTITY_VALUE \</span><br><span class="line">  -p 8080:8080 -p 8848:8848 -p 9848:9848 \</span><br><span class="line">  nacos/nacos-server:v3.1.1</span><br></pre></td></tr></table></figure>

<h3 id="4-Docker-Compose（Nacos-MySQL-同栈）"><a href="#4-Docker-Compose（Nacos-MySQL-同栈）" class="headerlink" title="4. Docker Compose（Nacos + MySQL 同栈）"></a>4. Docker Compose（Nacos + MySQL 同栈）</h3><p>与 <strong><code>nacos-docker</code></strong> 的 <strong><code>example/standalone-mysql.yaml</code></strong> 思路一致：<strong><code>mysql</code></strong> 先就绪并导入 <strong>schema</strong>，<strong><code>nacos</code></strong> 通过服务名连库。完整 <strong><code>docker-compose.mysql.yml</code></strong> 如下（<strong><code>./mysql-init/</code></strong> 下放置官方 <strong><code>mysql-schema.sql</code></strong> 后首次启动会自动建表；<strong><code>MYSQL_ROOT_PASSWORD</code></strong>、<strong><code>NACOS_AUTH_*</code></strong> 等均为占位）。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">name:</span> <span class="string">nacos-standalone-mysql</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">nacos-mysql</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">&quot;root-change-me&quot;</span></span><br><span class="line">      <span class="attr">MYSQL_DATABASE:</span> <span class="string">&quot;nacos_devtest&quot;</span></span><br><span class="line">      <span class="attr">MYSQL_USER:</span> <span class="string">&quot;nacos&quot;</span></span><br><span class="line">      <span class="attr">MYSQL_PASSWORD:</span> <span class="string">&quot;nacos-change-me&quot;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;3306:3306&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql_data:/var/lib/mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./mysql-init:/docker-entrypoint-initdb.d:ro</span></span><br><span class="line">    <span class="attr">command:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;--character-set-server=utf8mb4&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;--collation-server=utf8mb4_unicode_ci&quot;</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD&quot;</span>, <span class="string">&quot;mysqladmin&quot;</span>, <span class="string">&quot;ping&quot;</span>, <span class="string">&quot;-h&quot;</span>, <span class="string">&quot;localhost&quot;</span>, <span class="string">&quot;-uroot&quot;</span>, <span class="string">&quot;-proot-change-me&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">30</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">40s</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">nacos:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">nacos/nacos-server:v3.1.1</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">nacos-standalone-mysql</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="attr">mysql:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MODE:</span> <span class="string">standalone</span></span><br><span class="line">      <span class="attr">SPRING_DATASOURCE_PLATFORM:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_HOST:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_PORT:</span> <span class="string">&quot;3306&quot;</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_DB_NAME:</span> <span class="string">nacos_devtest</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_USER:</span> <span class="string">nacos</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_PASSWORD:</span> <span class="string">nacos-change-me</span></span><br><span class="line">      <span class="attr">MYSQL_SERVICE_DB_PARAM:</span> <span class="string">&quot;characterEncoding=utf8&amp;connectTimeout=1000&amp;socketTimeout=3000&amp;autoReconnect=true&amp;useUnicode=true&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true&quot;</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_TOKEN:</span> <span class="string">&quot;请替换为长度足够的随机串&quot;</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_IDENTITY_KEY:</span> <span class="string">&quot;serverIdentityKey&quot;</span></span><br><span class="line">      <span class="attr">NACOS_AUTH_IDENTITY_VALUE:</span> <span class="string">&quot;serverIdentityValue&quot;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:8080&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8848:8848&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9848:9848&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos_data:/home/nacos/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos_logs:/home/nacos/logs</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">mysql_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">nacos_data:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">nacos_logs:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line"></span><br><span class="line"><span class="attr">networks:</span></span><br><span class="line">  <span class="attr">default:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">nacos-standalone-mysql-net</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">bridge</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose -f docker-compose.mysql.yml up -d</span><br></pre></td></tr></table></figure>

<p>生产请把敏感项改为 <strong><code>.env</code> &#x2F; 外部 Secret</strong>；官方 <strong><code>mysql-init.sh</code></strong>、<strong><code>env/mysql.env</code></strong> 见 <a href="https://github.com/nacos-group/nacos-docker">nacos-docker</a>。</p>
<h3 id="5-Kubernetes（单文件完整清单）"><a href="#5-Kubernetes（单文件完整清单）" class="headerlink" title="5. Kubernetes（单文件完整清单）"></a>5. Kubernetes（单文件完整清单）</h3><p>集群内 <strong>MySQL + Nacos</strong> 可用下面<strong>一份多文档 YAML</strong> 一次应用（<strong><code>default</code></strong> 命名空间；<strong><code>nacos-data</code> &#x2F; <code>nacos-logs</code></strong> 的 <strong>PVC</strong> 未写 <strong><code>storageClassName</code></strong> 时将使用集群默认 <strong>StorageClass</strong>，无默认时请自行补上）。<strong>MySQL</strong> 数据目录示例为 <strong><code>emptyDir</code></strong>，仅适合演示；上生产请为 <strong>MySQL</strong> 使用 <strong>PVC</strong>（或云托管库），并在库就绪后按上文「前置」导入 <strong><code>mysql-schema.sql</code></strong>。<strong>MySQL</strong> 就绪探针使用 <strong><code>tcpSocket</code></strong> 检测 <strong>3306</strong>。</p>
<p><strong><code>nacos-stack-mysql.yaml</code></strong>（完整示例）：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_TOKEN:</span> <span class="string">&quot;请替换为长度足够的随机串&quot;</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_IDENTITY_KEY:</span> <span class="string">&quot;serverIdentityKey&quot;</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_IDENTITY_VALUE:</span> <span class="string">&quot;serverIdentityValue&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">&quot;root-change-me&quot;</span></span><br><span class="line">  <span class="attr">MYSQL_APP_USER:</span> <span class="string">&quot;nacos&quot;</span></span><br><span class="line">  <span class="attr">MYSQL_APP_PASSWORD:</span> <span class="string">&quot;nacos-change-me&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">10Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">5Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">3306</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">3306</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">mysql</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">3306</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">mysql</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_ROOT_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_ROOT_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_DATABASE</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;nacos_devtest&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_USER</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_APP_USER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_APP_PASSWORD</span></span><br><span class="line">          <span class="attr">args:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;--character-set-server=utf8mb4&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">&quot;--collation-server=utf8mb4_unicode_ci&quot;</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql-data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/var/lib/mysql</span></span><br><span class="line">          <span class="attr">readinessProbe:</span></span><br><span class="line">            <span class="attr">tcpSocket:</span></span><br><span class="line">              <span class="attr">port:</span> <span class="number">3306</span></span><br><span class="line">            <span class="attr">initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line">            <span class="attr">periodSeconds:</span> <span class="number">5</span></span><br><span class="line">            <span class="attr">timeoutSeconds:</span> <span class="number">3</span></span><br><span class="line">            <span class="attr">failureThreshold:</span> <span class="number">30</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">mysql-data</span></span><br><span class="line">          <span class="attr">emptyDir:</span> &#123;&#125;</span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">initContainers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">wait-mysql</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">busybox:1.36</span></span><br><span class="line">          <span class="attr">command:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">sh</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">-c</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">|</span></span><br><span class="line"><span class="string">              until nc -z mysql 3306; do echo waiting for mysql; sleep 2; done</span></span><br><span class="line"><span class="string"></span>      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">nacos-data</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">nacos-logs</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">nacos/nacos-server:v3.1.1</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8080</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">console</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8848</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9848</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">grpc-client</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/nacos/data</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/nacos/logs</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MODE</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;standalone&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">SPRING_DATASOURCE_PLATFORM</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;mysql&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_HOST</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;mysql&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_PORT</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;3306&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_DB_NAME</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;nacos_devtest&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_USER</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_APP_USER</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_PASSWORD</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-db</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">MYSQL_APP_PASSWORD</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MYSQL_SERVICE_DB_PARAM</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;characterEncoding=utf8&amp;connectTimeout=1000&amp;socketTimeout=3000&amp;autoReconnect=true&amp;useUnicode=true&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_TOKEN</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_TOKEN</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_IDENTITY_KEY</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_IDENTITY_KEY</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_IDENTITY_VALUE</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_IDENTITY_VALUE</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">console</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">8080</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">8848</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">8848</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">grpc-client</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9848</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9848</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f nacos-stack-mysql.yaml</span><br></pre></td></tr></table></figure>

<p><strong>MySQL</strong> Pod 就绪后，请在 <strong><code>nacos_devtest</code></strong> 中执行官方 <strong><code>mysql-schema.sql</code></strong>（与「前置」一致），再视需要重启 <strong>Nacos</strong> Pod 以连库成功。</p>
<hr>
<h2 id="Kubernetes（内嵌-Derby，单文件完整清单）"><a href="#Kubernetes（内嵌-Derby，单文件完整清单）" class="headerlink" title="Kubernetes（内嵌 Derby，单文件完整清单）"></a>Kubernetes（内嵌 Derby，单文件完整清单）</h2><p>与 <strong>MySQL</strong> 方案并列：内嵌 <strong>Derby</strong> 时只需 <strong>Nacos</strong> 本体，可用 <strong><code>nacos-stack-derby.yaml</code></strong> 一次创建 <strong>Secret、PVC、Deployment、Service</strong>（<strong><code>default</code></strong> 命名空间；<strong>PVC</strong> 未指定 <strong><code>storageClassName</code></strong> 时使用集群默认 <strong>StorageClass</strong>）。</p>
<p><strong><code>nacos-stack-derby.yaml</code></strong>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">Opaque</span></span><br><span class="line"><span class="attr">stringData:</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_TOKEN:</span> <span class="string">&quot;请替换为长度足够的随机串&quot;</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_IDENTITY_KEY:</span> <span class="string">&quot;serverIdentityKey&quot;</span></span><br><span class="line">  <span class="attr">NACOS_AUTH_IDENTITY_VALUE:</span> <span class="string">&quot;serverIdentityValue&quot;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">10Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PersistentVolumeClaim</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">accessModes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">ReadWriteOnce</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">    <span class="attr">requests:</span></span><br><span class="line">      <span class="attr">storage:</span> <span class="string">5Gi</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">nacos-data</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">          <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">            <span class="attr">claimName:</span> <span class="string">nacos-logs</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">nacos/nacos-server:v3.1.1</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8080</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">console</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8848</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">9848</span></span><br><span class="line">              <span class="attr">name:</span> <span class="string">grpc-client</span></span><br><span class="line">          <span class="attr">volumeMounts:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-data</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/nacos/data</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nacos-logs</span></span><br><span class="line">              <span class="attr">mountPath:</span> <span class="string">/home/nacos/logs</span></span><br><span class="line">          <span class="attr">env:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">MODE</span></span><br><span class="line">              <span class="attr">value:</span> <span class="string">&quot;standalone&quot;</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_TOKEN</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_TOKEN</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_IDENTITY_KEY</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_IDENTITY_KEY</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">NACOS_AUTH_IDENTITY_VALUE</span></span><br><span class="line">              <span class="attr">valueFrom:</span></span><br><span class="line">                <span class="attr">secretKeyRef:</span></span><br><span class="line">                  <span class="attr">name:</span> <span class="string">nacos-auth</span></span><br><span class="line">                  <span class="attr">key:</span> <span class="string">NACOS_AUTH_IDENTITY_VALUE</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nacos</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">console</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">8080</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">8848</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">8848</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">grpc-client</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">9848</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9848</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f nacos-stack-derby.yaml</span><br></pre></td></tr></table></figure>

<p>集群内 <strong><code>server-addr</code></strong> 示例：<strong><code>nacos.default.svc.cluster.local:8848</code></strong>。<strong>ReadWriteOnce</strong> 与多副本 <strong>Deployment</strong> 不兼容；<strong>Nacos</strong> 集群请改用 <strong>StatefulSet</strong> 或 <strong>RWX</strong> 存储，勿多副本共用单块 <strong>RWO</strong> 卷。</p>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul>
<li><strong>Nacos</strong> 官网：<a href="https://nacos.io/">https://nacos.io/</a>  </li>
<li><strong>Nacos Docker</strong> 仓库与 <strong>Compose</strong> 示例：<a href="https://github.com/nacos-group/nacos-docker">https://github.com/nacos-group/nacos-docker</a>  </li>
<li><strong>Spring Cloud Alibaba</strong> 版本说明：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a>  </li>
<li><strong>SCA 2025.1.0.0 Release</strong>（含 <strong>nacos-client 3.1.1</strong>）：<a href="https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0">https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0</a></li>
</ul>
<p>按上表固定 <strong>Server v3.1.1</strong> 与 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a> 一文中的 <strong>SCA 2025.1.0.0</strong>，即可在本地或 <strong>Kubernetes</strong> 上与示例工程对齐完成注册发现验证；上生产前请完成鉴权密钥、存储与高可用方案评审。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>Kubernetes</tag>
        <tag>SpringCloud</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>Nacos</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Alibaba 接入 Seata（SCA 2025.1.x 与 Server 2.5）</title>
    <url>/2026/05/01/SpringCloud/Seata/</url>
    <content><![CDATA[<p><strong>Seata</strong> 提供分布式事务（<strong>AT &#x2F; TCC &#x2F; Saga</strong> 等）；在 <strong>Spring Cloud Alibaba</strong> 中通过 <strong><code>spring-cloud-starter-alibaba-seata</code></strong> 接入。<strong>SCA 2025.1.0.0</strong> BOM 将 <strong>Seata</strong> 客户端对齐到 <strong>2.5.0</strong>，<strong>Server</strong> 大版本应与之一致。工程骨架与 BOM 见 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>；注册与配置中心常用 <strong>Nacos</strong>，部署参考 <a href="/2026/05/01/Nacos/">Nacos 版本选型与容器化部署</a>。</p>
<hr>
<h2 id="与-Spring-Cloud-Alibaba-的版本关系"><a href="#与-Spring-Cloud-Alibaba-的版本关系" class="headerlink" title="与 Spring Cloud Alibaba 的版本关系"></a>与 Spring Cloud Alibaba 的版本关系</h2><table>
<thead>
<tr>
<th>项</th>
<th>建议</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SCA BOM</strong></td>
<td><strong>2025.1.0.0</strong></td>
<td><a href="https://github.com/alibaba/spring-cloud-alibaba/releases">Releases</a></td>
</tr>
<tr>
<td><strong>Seata Client（BOM）</strong></td>
<td><strong>2.5.0</strong></td>
<td><strong><code>spring-cloud-alibaba-dependencies</code></strong> 中 <strong><code>seata.version</code></strong></td>
</tr>
<tr>
<td><strong>Seata Server</strong></td>
<td><strong>2.5.x</strong>（与客户端同世代）</td>
<td>镜像示例：<strong><code>seataio/seata-server:2.5.0</code></strong>；发行包见 <a href="https://github.com/seata/seata/releases">Seata Releases</a></td>
</tr>
<tr>
<td><strong>业务库（AT）</strong></td>
<td>每库 <strong><code>undo_log</code></strong></td>
<td><strong>InnoDB</strong>；脚本见 <a href="https://github.com/seata/seata/tree/develop/script/client">Seata script&#x2F;client</a> 下对应数据库</td>
</tr>
<tr>
<td><strong>Server 存事务元数据（db）</strong></td>
<td><strong><code>global_table</code> &#x2F; <code>branch_table</code> &#x2F; <code>lock_table</code> &#x2F; <code>distributed_lock</code></strong></td>
<td>见 <a href="https://github.com/seata/seata/tree/develop/script/server/db">Seata script&#x2F;server&#x2F;db</a></td>
</tr>
</tbody></table>
<hr>
<h2 id="官方文档与示例"><a href="#官方文档与示例" class="headerlink" title="官方文档与示例"></a>官方文档与示例</h2><ul>
<li><strong>SCA 2025.x Seata 快速开始</strong>：<a href="https://sca.aliyun.com/docs/2025.x/user-guide/seata/quick-start/">https://sca.aliyun.com/docs/2025.x/user-guide/seata/quick-start/</a>  </li>
<li><strong>Seata</strong> 官网：<a href="https://seata.io/">https://seata.io/</a>  </li>
<li><strong>多服务示例</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/tree/2025.1.x/spring-cloud-alibaba-examples/seata-example">https://github.com/alibaba/spring-cloud-alibaba/tree/2025.1.x/spring-cloud-alibaba-examples/seata-example</a></li>
</ul>
<hr>
<h2 id="依赖（Gradle-Kotlin-DSL）"><a href="#依赖（Gradle-Kotlin-DSL）" class="headerlink" title="依赖（Gradle Kotlin DSL）"></a>依赖（Gradle Kotlin DSL）</h2><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;com.alibaba.cloud:spring-cloud-starter-alibaba-seata&quot;</span>)</span><br><span class="line">    <span class="comment">// 若使用 JDBC 数据源参与 AT，按需增加 spring-jdbc / 连接池 / 驱动</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="AT-模式：业务库-undo-log"><a href="#AT-模式：业务库-undo-log" class="headerlink" title="AT 模式：业务库 undo_log"></a>AT 模式：业务库 <code>undo_log</code></h2><p>在参与 <strong>AT</strong> 分支的<strong>每个业务库</strong>执行官方 <strong><code>undo_log</code></strong> 建表脚本（引擎 <strong>InnoDB</strong>）。<strong>SCA</strong> 快速开始中有示例 <strong>DDL</strong>；升级 <strong>Seata</strong> 大版本时请对照发行说明是否调整表结构。</p>
<hr>
<h2 id="Seata-Server-快速启动（本地验证）"><a href="#Seata-Server-快速启动（本地验证）" class="headerlink" title="Seata Server 快速启动（本地验证）"></a>Seata Server 快速启动（本地验证）</h2><h3 id="1-单机文件存储（最简单）"><a href="#1-单机文件存储（最简单）" class="headerlink" title="1. 单机文件存储（最简单）"></a>1. 单机文件存储（最简单）</h3><p>适合本机联调：<strong>元数据</strong>存 <strong>Server</strong> 本地文件，不配外置库。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name seata-server \</span><br><span class="line">  -p 8091:8091 -p 7091:7091 \</span><br><span class="line">  seataio/seata-server:2.5.0</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：镜像标签以 <a href="https://hub.docker.com/r/seataio/seata-server/tags">Docker Hub seataio&#x2F;seata-server</a> 为准；若无对应 <strong>patch</strong> 标签，可改用同 <strong>2.5.x</strong> 已发布标签，或直接使用 <a href="https://github.com/seata/seata/releases">Seata Releases</a> 中的 <strong><code>seata-server</code></strong> 发行包启动。</p>
<ul>
<li><strong>TC</strong> 默认端口 <strong>8091</strong>；控制台常见 <strong>7091</strong>（以镜像说明为准）。  </li>
<li>客户端 <strong><code>tx-service-group</code></strong> 与 <strong><code>service.vgroupMapping.*</code></strong> 必须与 <strong>Server</strong> 侧 <strong><code>registry</code></strong> 可见的 <strong><code>cluster</code></strong> 一致（见下文 <strong><code>application.yaml</code></strong>）。</li>
</ul>
<h3 id="2-db-模式-Nacos（与官方示例一致）"><a href="#2-db-模式-Nacos（与官方示例一致）" class="headerlink" title="2. db 模式 + Nacos（与官方示例一致）"></a>2. db 模式 + Nacos（与官方示例一致）</h3><p>生产或多人环境：在 <strong>MySQL</strong> 中导入 <strong>Server</strong> 侧表结构；<strong>Server</strong> 使用 <strong><code>store.mode: db</code></strong>；注册与配置使用 <strong>Nacos</strong>（<strong><code>dataId</code></strong> 如 <strong><code>seataServer.properties</code></strong>、<strong><code>seata.properties</code></strong> 等按运维规范）。步骤与 <strong>SQL</strong> 汇总见 <strong>SCA 快速开始</strong> 与 <strong>Seata</strong> 部署文档。</p>
<hr>
<h2 id="应用侧最小配置（application-yaml）"><a href="#应用侧最小配置（application-yaml）" class="headerlink" title="应用侧最小配置（application.yaml）"></a>应用侧最小配置（<code>application.yaml</code>）</h2><p>以下演示 <strong>客户端</strong> 指向本机 <strong>TC</strong>，<strong><code>registry.type: file</code></strong> 且通过 <strong><code>grouplist</code></strong> 指定 <strong>TC</strong> 地址（与单机 <strong>Docker</strong> 对应）。<strong>生产</strong>请改为 <strong>Nacos &#x2F; Etcd</strong> 等与 <strong>Server</strong> 一致的注册、配置方式。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">seata:</span></span><br><span class="line">  <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">tx-service-group:</span> <span class="string">demo_tx_group</span></span><br><span class="line">  <span class="attr">service:</span></span><br><span class="line">    <span class="attr">vgroup-mapping:</span></span><br><span class="line">      <span class="attr">demo_tx_group:</span> <span class="string">default</span></span><br><span class="line">    <span class="attr">grouplist:</span></span><br><span class="line">      <span class="attr">default:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8091</span></span><br><span class="line">  <span class="attr">registry:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">file</span></span><br><span class="line">  <span class="attr">config:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">file</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>tx-service-group</code></strong>：与 <strong>Seata Server</strong>、<strong>Nacos</strong> 中 <strong><code>service.vgroupMapping.&lt;your-group&gt;=default</code></strong> 等资源一致。  </li>
<li><strong><code>@GlobalTransactional</code></strong> 或 <strong>TCC &#x2F; Saga</strong> API 标注在业务入口方法上；<strong>RestTemplate &#x2F; Feign</strong> 与 <strong>SCA</strong> 组合时 <strong>XID</strong> 传播由 starter 处理（详见官方「对 Spring Cloud 支持点」）。</li>
</ul>
<p>将 <strong><code>grouplist.default</code></strong> 换成你的 <strong>TC</strong> 可达地址；若 <strong>TC</strong> 在 <strong>Docker</strong> 而应用在宿主机，一般为 <strong><code>127.0.0.1:8091</code></strong>。</p>
<hr>
<h2 id="与-Nacos-配置中心（官方示例路径）"><a href="#与-Nacos-配置中心（官方示例路径）" class="headerlink" title="与 Nacos 配置中心（官方示例路径）"></a>与 Nacos 配置中心（官方示例路径）</h2><p>使用 <strong>Nacos</strong> 托管 <strong>Seata</strong> 规则时，常见做法是在 <strong>Nacos</strong> 创建 <strong><code>seata.properties</code></strong>（或按 <strong>Server 2.5</strong> 文档拆分多 <strong>dataId</strong>），并包含 <strong><code>service.vgroupMapping.&lt;应用tx-group&gt;=default</code></strong> 等项；<strong>Server</strong> 与业务应用均指向同一 <strong>Nacos</strong> 地址与命名空间。具体 <strong>Key</strong> 列表可参考 <strong>Seata</strong> 仓库 <strong><code>script/config-center/config.txt</code></strong> 并结合当前 <strong>Server</strong> 版本说明裁剪。</p>
<hr>
<h2 id="联调检查"><a href="#联调检查" class="headerlink" title="联调检查"></a>联调检查</h2><ol>
<li><strong>日志</strong>：发起全局事务时，各参与服务日志中 <strong>XID</strong> 应一致且随请求变化。  </li>
<li><strong>数据</strong>：模拟下游失败后，<strong>AT</strong> 应回滚业务表与 <strong><code>undo_log</code></strong> 状态符合预期。  </li>
<li><strong>控制台</strong>：<strong>Server 2.x</strong> 控制台可观察全局事务与锁（端口以实际镜像&#x2F;启动参数为准）。</li>
</ol>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul>
<li><strong>SCA 版本说明</strong>：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a>  </li>
<li><strong>Seata 部署指南</strong>：<a href="https://seata.io/zh-cn/docs/ops/deploy-guide-beginner.html">https://seata.io/zh-cn/docs/ops/deploy-guide-beginner.html</a>  </li>
<li><strong>SCA 2025.1.0.0 Release</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0">https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0</a></li>
</ul>
<p><strong>SCA 2025.1.0.0</strong> + <strong>Seata 2.5.0</strong> 组合下，先完成 <strong><code>undo_log</code></strong> 与 <strong>Server</strong> 可达地址，再配 <strong><code>tx-service-group</code> &#x2F; <code>vgroup-mapping</code></strong>，即可在微服务中验证 <strong>AT</strong>；上生产前请完成 <strong>db 模式</strong>、高可用 <strong>TC</strong> 与 <strong>Nacos</strong> 配置评审。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>MySQL</tag>
        <tag>SpringCloud</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>Nacos</tag>
        <tag>Seata</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Alibaba 接入 Sentinel（SCA 2025.1.x 与控制台）</title>
    <url>/2026/05/01/SpringCloud/Sentinel/</url>
    <content><![CDATA[<p><strong>Sentinel</strong> 提供流量控制、熔断降级、系统自适应保护等能力；在 <strong>Spring Cloud Alibaba</strong> 中通过 <strong><code>spring-cloud-starter-alibaba-sentinel</code></strong> 开箱接入 <strong>Web</strong> 与常见远程调用场景。<strong>SCA 2025.1.0.0</strong> 将 <strong>Sentinel</strong> 核心库对齐到 <strong>1.8.9</strong>，并与 <strong>Spring Boot 4.x &#x2F; Jackson 3.x</strong> 兼容（见 <a href="https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0">Release 说明</a>）。BOM 与 <strong>Gradle</strong> 工程见 <a href="/2026/05/01/SpringCloudAlibaba/">Spring Cloud Alibaba 与 Gradle 从零搭建</a>。</p>
<hr>
<h2 id="与-Spring-Cloud-Alibaba-的版本关系"><a href="#与-Spring-Cloud-Alibaba-的版本关系" class="headerlink" title="与 Spring Cloud Alibaba 的版本关系"></a>与 Spring Cloud Alibaba 的版本关系</h2><table>
<thead>
<tr>
<th>项</th>
<th>建议</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SCA BOM</strong></td>
<td><strong>2025.1.0.0</strong></td>
<td>以 <a href="https://github.com/alibaba/spring-cloud-alibaba/releases">GitHub Releases</a> 为准</td>
</tr>
<tr>
<td><strong>Sentinel Core（BOM）</strong></td>
<td><strong>1.8.9</strong></td>
<td><strong><code>spring-cloud-alibaba-dependencies</code></strong> 中 <strong><code>sentinel.version</code></strong></td>
</tr>
<tr>
<td><strong>Sentinel Dashboard</strong></td>
<td>与 <strong>1.8.x</strong> 控制台匹配</td>
<td>建议从 <a href="https://github.com/alibaba/Sentinel/releases">Sentinel Releases</a> 下载 <strong><code>sentinel-dashboard-1.8.9.jar</code></strong>（或与 <strong>1.8.9</strong> 同线的控制台包）</td>
</tr>
</tbody></table>
<hr>
<h2 id="官方文档"><a href="#官方文档" class="headerlink" title="官方文档"></a>官方文档</h2><ul>
<li><strong>SCA 2025.x Sentinel 快速开始</strong>：<a href="https://sca.aliyun.com/docs/2025.x/user-guide/sentinel/quick-start/">https://sca.aliyun.com/docs/2025.x/user-guide/sentinel/quick-start/</a>  </li>
<li><strong>Sentinel 文档（规则、注解）</strong>：<a href="https://sentinelguard.io/zh-cn/docs/introduction.html">https://sentinelguard.io/zh-cn/docs/introduction.html</a></li>
</ul>
<hr>
<h2 id="依赖（Gradle-Kotlin-DSL）"><a href="#依赖（Gradle-Kotlin-DSL）" class="headerlink" title="依赖（Gradle Kotlin DSL）"></a>依赖（Gradle Kotlin DSL）</h2><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;com.alibaba.cloud:spring-cloud-starter-alibaba-sentinel&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>引入后，<strong>Spring MVC</strong> 暴露的 <strong>HTTP</strong> 接口默认会作为资源被统计（是否限流仍由规则决定）；可与 <strong>OpenFeign</strong>、<strong>RestTemplate</strong> 等扩展一起使用（按需加 <strong><code>spring-cloud-starter-openfeign</code></strong> 等）。</p>
<hr>
<h2 id="连接-Sentinel-Dashboard"><a href="#连接-Sentinel-Dashboard" class="headerlink" title="连接 Sentinel Dashboard"></a>连接 Sentinel Dashboard</h2><p><strong>Dashboard</strong> 独立进程运行；应用通过 <strong><code>transport</code></strong> 与控制台通信。</p>
<p><strong><code>application.yaml</code></strong> 示例：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">sentinel:</span></span><br><span class="line">      <span class="attr">transport:</span></span><br><span class="line">        <span class="attr">dashboard:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8858</span></span><br><span class="line">        <span class="attr">port:</span> <span class="number">8719</span></span><br></pre></td></tr></table></figure>

<ul>
<li><strong><code>dashboard</code></strong>：<strong>控制台</strong> <strong><code>IP:端口</code></strong>（勿与业务 <strong>HTTP</strong> 端口冲突；示例控制台常用 <strong>8858</strong>）。  </li>
<li><strong><code>port</code></strong>：应用侧与 <strong>Dashboard</strong> 通信的本地端口，未占用时可保持默认 <strong>8719</strong>。</li>
</ul>
<p>启动控制台（示例将控制台 <strong>HTTP</strong> 设为 <strong>8858</strong>）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -Dserver.port=8858 \</span><br><span class="line">  -Dcsp.sentinel.dashboard.server=localhost:8858 \</span><br><span class="line">  -jar sentinel-dashboard-1.8.9.jar</span><br></pre></td></tr></table></figure>

<p>浏览器访问 <strong><code>http://127.0.0.1:8858</code></strong>（默认账号密码以 <strong>Dashboard</strong> 发行说明为准，首次登录后应修改）。</p>
<p>若 <strong>Dashboard</strong> 挂在 <strong>context-path</strong> 下，需配置 <strong><code>spring.cloud.sentinel.transport.api-path</code></strong> 等项，见 <strong>SCA</strong> 高级文档与 <a href="https://github.com/alibaba/spring-cloud-alibaba/issues/1818">Issue 讨论</a>。</p>
<hr>
<h2 id="标识资源：-SentinelResource"><a href="#标识资源：-SentinelResource" class="headerlink" title="标识资源：@SentinelResource"></a>标识资源：<code>@SentinelResource</code></h2><p>对需要单独配置流控、熔断的方法使用 <strong><code>@SentinelResource</code></strong>，例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;/hello&quot;)</span></span><br><span class="line"><span class="meta">@SentinelResource(value = &quot;hello&quot;, blockHandler = &quot;helloBlock&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">hello</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;OK&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">helloBlock</span><span class="params">(BlockException ex)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;限流或降级&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>blockHandler</code> &#x2F; <code>fallback</code> &#x2F; <code>blockHandlerClass</code></strong> 等用法见 <a href="https://github.com/alibaba/Sentinel/wiki/%E6%B3%A8%E8%A7%A3%E6%94%AF%E6%8C%81">Sentinel 注解支持</a>。</p>
<hr>
<h2 id="Feign-与-RestTemplate（可选）"><a href="#Feign-与-RestTemplate（可选）" class="headerlink" title="Feign 与 RestTemplate（可选）"></a>Feign 与 RestTemplate（可选）</h2><ul>
<li><strong>Feign</strong>：在启用 <strong>Feign</strong> 的前提下，配置 <strong><code>feign.sentinel.enabled=true</code></strong>（<strong>Spring Cloud 2025.x</strong> 下属性名以当前 <strong>SCA</strong> 文档为准），使远程调用资源纳入 <strong>Sentinel</strong>。  </li>
<li><strong>RestTemplate</strong>：按 <strong>SCA</strong> 文档添加 <strong><code>@SentinelRestTemplate</code></strong> 或推荐的全局 <strong><code>RestTemplate</code></strong> 封装方式。</li>
</ul>
<p>具体包名与自动配置类随 <strong>Boot 4</strong> 可能有细微差异，以 <strong><code>spring-cloud-starter-alibaba-sentinel</code></strong> 当前版本说明为准。</p>
<hr>
<h2 id="规则持久化（简述）"><a href="#规则持久化（简述）" class="headerlink" title="规则持久化（简述）"></a>规则持久化（简述）</h2><p>默认规则存内存；生产可将流控、降级等规则放到 <strong>Nacos</strong>、<strong>Redis</strong> 等数据源，使用 <strong><code>sentinel-datasource-*</code></strong> 与 <strong>SCA</strong> 文档中的 <strong><code>spring.cloud.sentinel.datasource</code></strong> 配置。与 <a href="/2026/05/01/Nacos/">Nacos</a> 同栈时，可统一由 <strong>Nacos</strong> 推送规则。</p>
<hr>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul>
<li><strong>SCA 版本说明</strong>：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a>  </li>
<li><strong>Sentinel GitHub</strong>：<a href="https://github.com/alibaba/Sentinel">https://github.com/alibaba/Sentinel</a>  </li>
<li><strong>SCA 2025.1.0.0 Release</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0">https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2025.1.0.0</a></li>
</ul>
<p>固定 <strong>SCA BOM 2025.1.0.0</strong> 后，增加 <strong><code>spring-cloud-starter-alibaba-sentinel</code></strong>、配置 <strong><code>transport.dashboard</code></strong> 并启动 <strong>1.8.9</strong> 控制台，即可在界面上配置 <strong>QPS</strong>、线程数、熔断等规则，与 <strong>Seata</strong>、<strong>RocketMQ</strong> 等组件可并存使用。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>SpringCloud</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>Sentinel</tag>
        <tag>限流</tag>
        <tag>降级</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Alibaba 与 Gradle 从零搭建</title>
    <url>/2026/05/01/SpringCloud/SpringCloudAlibaba/</url>
    <content><![CDATA[<p>Spring Cloud Alibaba（简称 <strong>SCA</strong>）在 Spring Cloud 之上整合 <strong>Nacos、Sentinel、Seata、RocketMQ</strong> 等组件，便于做注册发现、配置、限流与分布式事务。本文先说明 <strong>版本怎么选</strong>，再用 <strong>IntelliJ IDEA</strong> 创建 <strong>Gradle（Kotlin DSL）</strong> 工程，按后文替换构建脚本与依赖，得到可启动、可连 <strong>Nacos</strong> 做服务注册的最小示例；构建工具安装与镜像加速见站内 <strong><code>gradle.md</code></strong>。</p>
<hr>
<h1 id="一、官网与文档"><a href="#一、官网与文档" class="headerlink" title="一、官网与文档"></a>一、官网与文档</h1><ul>
<li><strong>中文官网</strong>：<a href="https://sca.aliyun.com/">https://sca.aliyun.com/</a></li>
<li><strong>GitHub</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba">https://github.com/alibaba/spring-cloud-alibaba</a>  </li>
<li><strong>版本说明（与 Spring Boot &#x2F; Spring Cloud 对齐关系）</strong>：<a href="https://sca.aliyun.com/docs/2025.x/overview/version-explain/">https://sca.aliyun.com/docs/2025.x/overview/version-explain/</a>  </li>
<li><strong>发行说明与变更</strong>：<a href="https://github.com/alibaba/spring-cloud-alibaba/releases">https://github.com/alibaba/spring-cloud-alibaba/releases</a></li>
</ul>
<hr>
<h1 id="二、版本选型（先做这一步）"><a href="#二、版本选型（先做这一步）" class="headerlink" title="二、版本选型（先做这一步）"></a>二、版本选型（先做这一步）</h1><p>SCA 以 <strong>BOM 三位版本号</strong>（如 <strong><code>2025.1.0.0</code></strong>）发布，须与 <strong>Spring Boot、Spring Cloud</strong> 同一世代，不能混用旧版 Boot 配新版 SCA。</p>
<h2 id="1-当前两条主线（摘自官方-README-分支说明）"><a href="#1-当前两条主线（摘自官方-README-分支说明）" class="headerlink" title="1. 当前两条主线（摘自官方 README 分支说明）"></a>1. 当前两条主线（摘自官方 README 分支说明）</h2><table>
<thead>
<tr>
<th>路线</th>
<th>Spring Boot</th>
<th>Spring Cloud</th>
<th>SCA BOM（示例）</th>
<th>JDK</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td><strong>2025.1.x</strong>（<strong>本文采用</strong>）</td>
<td><strong>4.0.x</strong></td>
<td><strong>2025.1.x</strong></td>
<td><strong>2025.1.0.0</strong>（当前 GA）</td>
<td><strong>17+</strong></td>
<td>新项目、跟官方最新发布版</td>
</tr>
<tr>
<td><strong>2025.0.x</strong></td>
<td><strong>3.5.x</strong></td>
<td><strong>2025.0.x</strong></td>
<td><strong>2025.0.0.x</strong>（以 Central 为准）</td>
<td><strong>17+</strong></td>
<td>暂不升级 Boot 4、存量 Boot 3.5</td>
</tr>
</tbody></table>
<h2 id="2-本文固定组合（与-SCA-2025-1-0-0-发行说明一致）"><a href="#2-本文固定组合（与-SCA-2025-1-0-0-发行说明一致）" class="headerlink" title="2. 本文固定组合（与 SCA 2025.1.0.0 发行说明一致）"></a>2. 本文固定组合（与 SCA <strong>2025.1.0.0</strong> 发行说明一致）</h2><ul>
<li><strong>Spring Cloud Alibaba BOM</strong>：<strong><code>2025.1.0.0</code></strong>（<code>com.alibaba.cloud:spring-cloud-alibaba-dependencies</code>）  </li>
<li><strong>Spring Boot</strong>：<strong><code>4.0.4</code></strong>（4.0 线补丁，仍属官方所述 <strong>Boot 4.0.x</strong>）  </li>
<li><strong>Spring Cloud BOM</strong>：<strong><code>2025.1.0</code></strong>（<code>org.springframework.cloud:spring-cloud-dependencies</code>）  </li>
<li><strong>JDK</strong>：<strong>17</strong>（官方要求 <strong>17+</strong>，可用 <strong>21</strong>）  </li>
<li><strong>Gradle</strong>：与 <strong><code>gradle.md</code></strong> 一致，示例 Wrapper 使用 <strong>9.5.0</strong></li>
</ul>
<p><strong>2025.1.x</strong> 起 Nacos 配置侧已弱化 <strong><code>bootstrap.yml</code></strong>，推荐 <strong><code>spring.config.import</code></strong> 或直接在 <strong><code>application.yaml</code></strong> 中配置；本文最小示例只用 <strong><code>application.yaml</code> + Nacos 注册发现</strong>。</p>
<p>升级小版本前请到 <strong>GitHub Releases</strong> 与 <strong>sca.aliyun.com</strong> 核对是否仍有更新的 <strong>2025.1.0.x</strong> 补丁版，并替换下文的 BOM 与 Boot 小版本号。</p>
<hr>
<h1 id="三、环境与前置"><a href="#三、环境与前置" class="headerlink" title="三、环境与前置"></a>三、环境与前置</h1><ol>
<li>安装 <strong>JDK 17+</strong>，终端执行 <strong><code>java -version</code></strong>。  </li>
<li>安装 <strong>IntelliJ IDEA</strong>（<strong>Community &#x2F; Ultimate</strong> 均可；下文以 <strong>Ultimate</strong> 菜单名为准，社区版对应项名称可能略有差异）。  </li>
<li>安装 <strong>Gradle</strong>（或使用下文 <strong><code>gradle wrapper</code></strong> 生成 Wrapper，本机需已装 Gradle 一次），参见 <strong><code>source/_posts/Java/gradle.md</code></strong>；亦可在 IDEA 内用自带 <strong>Gradle</strong> 执行同步与 <strong><code>bootRun</code></strong>。  </li>
<li>若要验证注册发现：本机或服务器已部署 <strong>Nacos 2.x &#x2F; 3.x</strong>（示例地址 <strong><code>127.0.0.1:8848</code></strong>）。无 Nacos 时工程仍可 <strong>编译</strong>；启动后若连不上注册中心，控制台会有连接相关日志。</li>
</ol>
<hr>
<h1 id="四、用-IntelliJ-IDEA-创建工程（替换原手动建目录）"><a href="#四、用-IntelliJ-IDEA-创建工程（替换原手动建目录）" class="headerlink" title="四、用 IntelliJ IDEA 创建工程（替换原手动建目录）"></a>四、用 IntelliJ IDEA 创建工程（替换原手动建目录）</h1><p><strong>不要</strong>使用 <strong>Spring Initializr</strong> 向导直接生成完整 Boot 工程：本文 <strong><code>build.gradle.kts</code></strong> 需手写 <strong>Spring Cloud &#x2F; SCA BOM</strong>，与 Initializr 默认模板不一致。请用 <strong>IDEA「Java + Gradle」向导</strong> 建骨架，<strong>Gradle DSL 必须选 Kotlin</strong>（与 <strong>「五～七」</strong> 文件名一致），再按 <strong>「五～七」</strong> 覆盖脚本。</p>
<h2 id="1-新建项目：左侧「Java」-构建系统「Gradle」"><a href="#1-新建项目：左侧「Java」-构建系统「Gradle」" class="headerlink" title="1. 新建项目：左侧「Java」+ 构建系统「Gradle」"></a>1. 新建项目：左侧「Java」+ 构建系统「Gradle」</h2><p>与 <strong>IntelliJ IDEA</strong> 新版「新建项目」界面一致（欢迎页 <strong>New Project</strong> &#x2F; 菜单 <strong>File → New → Project</strong>）：</p>
<ol>
<li>左侧在 <strong>New Project</strong> 下选 <strong>Java</strong>；右侧 <strong>Build system（构建系统）</strong> 选 <strong>Gradle</strong>（与 <strong>IDEA</strong> 当前「<strong>Java + Gradle</strong>」向导一致）。若你习惯用左侧 <strong>Gradle</strong> 向导，同样必须把 <strong>Gradle DSL</strong> 设为 <strong>Kotlin</strong>，效果等价。  </li>
<li>右侧 <strong>Name（名称）</strong>：填 <strong><code>sca-nacos-demo</code></strong>（与下文工程名一致）。  </li>
<li><strong>Location（位置）</strong>：选父目录，<strong>IDEA</strong> 会在其下创建 <strong><code>sca-nacos-demo</code></strong> 文件夹。  </li>
<li><strong>Create Git repository（创建 Git 仓库）</strong>：按需勾选（团队协同时建议勾选）。  </li>
<li><strong>Build system（构建系统）</strong>：选 <strong>Gradle</strong>。  </li>
<li><strong>JDK</strong>：选 <strong>17</strong> 或 <strong>21</strong>（须满足「二」中 <strong>17+</strong>；与 <strong><code>build.gradle.kts</code></strong> 里 <strong><code>JavaLanguageVersion.of(17)</code></strong>  toolchain 一致时，选 <strong>17</strong> 最省事）。  </li>
<li><strong>Gradle DSL</strong>：<strong>务必选 Kotlin</strong>（生成 <strong><code>build.gradle.kts</code></strong> &#x2F; <strong><code>settings.gradle.kts</code></strong>）。向导默认有时是 <strong>Groovy</strong>（生成 <strong><code>build.gradle</code></strong>），<strong>与本文不符</strong>。若已误选 <strong>Groovy</strong>：最省事是重新 <strong>New Project</strong> 并改选 <strong>Kotlin</strong>；或手动新建 <strong><code>build.gradle.kts</code></strong>、<strong><code>settings.gradle.kts</code></strong>，删除 <strong><code>build.gradle</code></strong>、<strong><code>settings.gradle</code></strong> 后 <strong>Reload Gradle Project</strong>。  </li>
<li><strong>Add sample code（添加示例代码）</strong>：建议<strong>取消勾选</strong>，减少默认 <strong><code>Main.java</code></strong>；若保留，完成 <strong>「九」</strong> 前删除向导生成的示例类即可。  </li>
<li>点击 <strong>Create（创建）</strong>。<strong>IDEA</strong> 会生成 <strong><code>src/main/java</code></strong>、<strong><code>src/main/resources</code></strong>、<strong><code>gradle/</code></strong> 等，无需再手动 <strong><code>mkdir</code></strong>。</li>
</ol>
<h2 id="2-创建-Java-包与源码目录"><a href="#2-创建-Java-包与源码目录" class="headerlink" title="2. 创建 Java 包与源码目录"></a>2. 创建 Java 包与源码目录</h2><ol>
<li>在 <strong>Project</strong> 视图中展开 <strong><code>src/main/java</code></strong>。  </li>
<li>在 <strong><code>java</code></strong> 上右键 → <strong>New → Package</strong>，输入 <strong><code>com.example.sca</code></strong> 并确认。  </li>
<li>后续 <strong>「九」</strong> 中的 <strong><code>ScaDemoApplication.java</code></strong>、<strong><code>HelloController.java</code></strong> 均建在该包下；<strong><code>application.yaml</code></strong> 放在 <strong><code>src/main/resources/</code></strong>。</li>
</ol>
<h2 id="3-覆盖-Gradle-脚本（与「五～七」对应）"><a href="#3-覆盖-Gradle-脚本（与「五～七」对应）" class="headerlink" title="3. 覆盖 Gradle 脚本（与「五～七」对应）"></a>3. 覆盖 Gradle 脚本（与「五～七」对应）</h2><p>向导生成的 <strong><code>settings.gradle.kts</code></strong>、<strong><code>build.gradle.kts</code></strong> 往往带演示性 <strong><code>main</code> 函数</strong>或默认依赖，请打开文件后<strong>整段替换</strong>为 <strong>「五」「七」</strong> 中的内容；新建 <strong><code>gradle.properties</code></strong> 并写入 <strong>「六」</strong>。保存后点击右上角 <strong>Load Gradle Changes</strong>（或 <strong>Gradle</strong> 工具窗口的刷新），等待依赖解析完成。</p>
<p>下文默认<strong>模块根目录</strong>为 <strong><code>sca-nacos-demo/</code></strong>（即包含 <strong><code>build.gradle.kts</code></strong> 的目录），Java 包名为 <strong><code>com.example.sca</code></strong>。</p>
<hr>
<h1 id="五、settings-gradle-kts"><a href="#五、settings-gradle-kts" class="headerlink" title="五、settings.gradle.kts"></a>五、<code>settings.gradle.kts</code></h1><p>在 <strong><code>sca-nacos-demo/settings.gradle.kts</code></strong> 写入：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">rootProject.name = <span class="string">&quot;sca-nacos-demo&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、gradle-properties（版本集中管理）"><a href="#六、gradle-properties（版本集中管理）" class="headerlink" title="六、gradle.properties（版本集中管理）"></a>六、<code>gradle.properties</code>（版本集中管理）</h1><p>在 <strong><code>sca-nacos-demo/gradle.properties</code></strong> 写入：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 与「二、版本选型」一致；升级 Cloud / SCA 时改这里，并与下方 build.gradle.kts 中 Boot 插件版本对齐</span></span><br><span class="line"><span class="attr">springCloudVersion</span>=<span class="string">2025.1.0</span></span><br><span class="line"><span class="attr">springCloudAlibabaVersion</span>=<span class="string">2025.1.0.0</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="七、build-gradle-kts（Spring-Boot-Cloud-SCA-BOM）"><a href="#七、build-gradle-kts（Spring-Boot-Cloud-SCA-BOM）" class="headerlink" title="七、build.gradle.kts（Spring Boot + Cloud + SCA BOM）"></a>七、<code>build.gradle.kts</code>（Spring Boot + Cloud + SCA BOM）</h1><p>在 <strong><code>sca-nacos-demo/build.gradle.kts</code></strong> 写入：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Spring Boot 与「二、版本选型」一致（4.0.x）；小版本升级时请对照 SCA 发行说明</span></span><br><span class="line">plugins &#123;</span><br><span class="line">    java</span><br><span class="line">    id(<span class="string">&quot;org.springframework.boot&quot;</span>) version <span class="string">&quot;4.0.4&quot;</span></span><br><span class="line">    id(<span class="string">&quot;io.spring.dependency-management&quot;</span>) version <span class="string">&quot;1.1.7&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">group = <span class="string">&quot;com.example&quot;</span></span><br><span class="line">version = <span class="string">&quot;0.0.1-SNAPSHOT&quot;</span></span><br><span class="line"></span><br><span class="line">java &#123;</span><br><span class="line">    toolchain &#123;</span><br><span class="line">        languageVersion.<span class="keyword">set</span>(JavaLanguageVersion.of(<span class="number">17</span>))</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">repositories &#123;</span><br><span class="line">    mavenCentral()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">dependencyManagement &#123;</span><br><span class="line">    imports &#123;</span><br><span class="line">        mavenBom(<span class="string">&quot;org.springframework.cloud:spring-cloud-dependencies:<span class="subst">$&#123;property(<span class="string">&quot;springCloudVersion&quot;</span>)&#125;</span>&quot;</span>)</span><br><span class="line">        mavenBom(<span class="string">&quot;com.alibaba.cloud:spring-cloud-alibaba-dependencies:<span class="subst">$&#123;property(<span class="string">&quot;springCloudAlibabaVersion&quot;</span>)&#125;</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">dependencies &#123;</span><br><span class="line">    implementation(<span class="string">&quot;org.springframework.boot:spring-boot-starter-web&quot;</span>)</span><br><span class="line">    implementation(<span class="string">&quot;org.springframework.boot:spring-boot-starter-actuator&quot;</span>)</span><br><span class="line">    implementation(<span class="string">&quot;com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-discovery&quot;</span>)</span><br><span class="line">    testImplementation(<span class="string">&quot;org.springframework.boot:spring-boot-starter-test&quot;</span>)</span><br><span class="line">    testRuntimeOnly(<span class="string">&quot;org.junit.platform:junit-platform-launcher&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">tasks.withType&lt;Test&gt; &#123;</span><br><span class="line">    useJUnitPlatform()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><strong><code>org.springframework.boot</code></strong> 插件会管理 <strong>Spring Boot</strong> 自带依赖版本；<strong><code>spring-cloud-dependencies</code></strong> 与 <strong><code>spring-cloud-alibaba-dependencies</code></strong> 通过 <strong><code>mavenBom</code></strong> 对齐 Cloud 与 SCA。  </li>
<li>仅 <strong><code>nacos-discovery</code></strong> 即可在 Nacos 控制台看到服务实例；后续若要配置中心，再增加 <strong><code>spring-cloud-starter-alibaba-nacos-config</code></strong> 并按官方文档改用 <strong><code>spring.config.import</code></strong>。</li>
</ul>
<hr>
<h1 id="八、生成-Gradle-Wrapper"><a href="#八、生成-Gradle-Wrapper" class="headerlink" title="八、生成 Gradle Wrapper"></a>八、生成 Gradle Wrapper</h1><p>在 <strong>IDEA</strong> 底部打开 <strong>Terminal</strong>（<strong>View → Tool Windows → Terminal</strong>），确认当前目录为项目根 <strong><code>sca-nacos-demo/</code></strong>（含 <strong><code>build.gradle.kts</code></strong>），执行（版本与 <strong><code>gradle.md</code></strong> 对齐）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gradle wrapper --gradle-version=9.5.0</span><br></pre></td></tr></table></figure>

<p>若未装全局 <strong>Gradle</strong>，可在 <strong>File → Settings → Build, Execution, Deployment → Build Tools → Gradle</strong> 中查看 <strong>Gradle</strong> 分发路径，或在 <strong>Gradle</strong> 工具窗口对 <strong><code>wrapper</code></strong> 任务执行一次（视 IDEA 版本与项目是否已含 <strong>wrapper</strong> 配置而定）；生成 <strong><code>gradlew</code></strong>、<strong><code>gradlew.bat</code></strong> 与 <strong><code>gradle/wrapper/</code></strong> 后，团队可统一用 <strong><code>./gradlew</code></strong>，无需本机全局安装同版本 Gradle。</p>
<hr>
<h1 id="九、主类、接口与配置"><a href="#九、主类、接口与配置" class="headerlink" title="九、主类、接口与配置"></a>九、主类、接口与配置</h1><h2 id="1-启动类-ScaDemoApplication-java"><a href="#1-启动类-ScaDemoApplication-java" class="headerlink" title="1. 启动类 ScaDemoApplication.java"></a>1. 启动类 <code>ScaDemoApplication.java</code></h2><p>路径：<strong><code>src/main/java/com/example/sca/ScaDemoApplication.java</code></strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.sca;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ScaDemoApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(ScaDemoApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-示例接口-HelloController-java"><a href="#2-示例接口-HelloController-java" class="headerlink" title="2. 示例接口 HelloController.java"></a>2. 示例接口 <code>HelloController.java</code></h2><p>路径：<strong><code>src/main/java/com/example/sca/HelloController.java</code></strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.sca;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">hello</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Spring Cloud Alibaba + Gradle OK&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-application-yaml"><a href="#3-application-yaml" class="headerlink" title="3. application.yaml"></a>3. <code>application.yaml</code></h2><p>路径：<strong><code>src/main/resources/application.yaml</code></strong></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">sca-nacos-demo</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">discovery:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br><span class="line">        <span class="comment"># 多环境可再配置 namespace、group 等，见官方 Nacos 文档</span></span><br><span class="line"></span><br><span class="line"><span class="attr">management:</span></span><br><span class="line">  <span class="attr">endpoints:</span></span><br><span class="line">    <span class="attr">web:</span></span><br><span class="line">      <span class="attr">exposure:</span></span><br><span class="line">        <span class="attr">include:</span> <span class="string">health,info</span></span><br></pre></td></tr></table></figure>

<p>将 <strong><code>server-addr</code></strong> 改成你的 Nacos 地址；若暂无 Nacos，可先注释掉 <strong><code>spring.cloud.nacos</code></strong> 整段，仅验证 Web 与构建（注释后不再向 Nacos 注册）。</p>
<hr>
<h1 id="十、构建与运行"><a href="#十、构建与运行" class="headerlink" title="十、构建与运行"></a>十、构建与运行</h1><p><strong>方式一（终端）</strong>：在 <strong>IDEA Terminal</strong> 中于项目根执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./gradlew clean bootRun</span><br></pre></td></tr></table></figure>

<p><strong>方式二（IDEA）</strong>：右侧 <strong>Gradle</strong> 工具窗口展开 <strong><code>sca-nacos-demo → Tasks → application</code></strong>，双击 <strong><code>bootRun</code></strong>（首次前可先执行 <strong><code>build</code></strong> 或 <strong><code>classes</code></strong> 确认无报错）。</p>
<p>浏览器访问：<strong><a href="http://127.0.0.1:8080/">http://127.0.0.1:8080/</a></strong> ，应看到返回字符串。<br>若已配置 Nacos 且服务正常，在 Nacos 控制台 <strong>「服务管理 → 服务列表」</strong> 中应出现 <strong><code>sca-nacos-demo</code></strong> 实例。</p>
<p>打包：在终端执行 <strong><code>./gradlew bootJar</code></strong>，或在 <strong>Gradle → Tasks → build → bootJar</strong>。产物在 <strong><code>build/libs/</code></strong> 下。</p>
<hr>
<h1 id="十一、小结"><a href="#十一、小结" class="headerlink" title="十一、小结"></a>十一、小结</h1><table>
<thead>
<tr>
<th>项</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>SCA BOM</td>
<td><strong><code>2025.1.0.0</code></strong>（发布版；升级请看 GitHub Releases）</td>
</tr>
<tr>
<td>Spring Boot &#x2F; Cloud</td>
<td><strong><code>4.0.4</code></strong> + <strong><code>2025.1.0</code></strong>，与 <strong>2025.1.x</strong> 主线一致</td>
</tr>
<tr>
<td>创建方式</td>
<td><strong>IntelliJ IDEA</strong>：<strong>Gradle + Kotlin DSL</strong> 向导，再覆盖 <strong>「五～七」</strong> 脚本</td>
</tr>
<tr>
<td>构建</td>
<td><strong>Gradle Kotlin DSL</strong>，<strong>Wrapper 9.5.0</strong></td>
</tr>
<tr>
<td>注册发现</td>
<td><strong><code>spring-cloud-starter-alibaba-nacos-discovery</code></strong> + <strong><code>application.yaml</code></strong></td>
</tr>
<tr>
<td>官方文档</td>
<td><a href="https://sca.aliyun.com/">https://sca.aliyun.com/</a> · <a href="https://github.com/alibaba/spring-cloud-alibaba">https://github.com/alibaba/spring-cloud-alibaba</a></td>
</tr>
</tbody></table>
<p>按上述顺序即可用 <strong>IDEA</strong> 得到可维护的最小 <strong>Spring Cloud Alibaba + Gradle</strong> 工程；需要 <strong>Sentinel &#x2F; Seata &#x2F; RocketMQ</strong> 时在 <strong><code>build.gradle.kts</code></strong> 增加对应 <strong><code>spring-cloud-starter-alibaba-*</code></strong> 依赖，并查阅官网各组件章节继续配置即可。</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>Gradle</tag>
        <tag>IntelliJ IDEA</tag>
        <tag>SpringCloud</tag>
        <tag>SpringCloudAlibaba</tag>
        <tag>Nacos</tag>
      </tags>
  </entry>
</search>
